
// Testing CSR 0x0
	csrr x7, 0x0	// Read CSR
	li x11, -1
	csrrw x28, 0x0, x11	// Write all 1s to CSR
	csrrw x28, 0x0, x0	// Write all 0s to CSR
	csrrs x28, 0x0, x11	// Set all CSR bits
	csrrc x28, 0x0, x11	// Clear all CSR bits
	csrrw x28, 0x0, x7	// Restore CSR

// Testing CSR 0x1
	csrr x12, 0x1	// Read CSR
	li x30, -1
	csrrw x13, 0x1, x30	// Write all 1s to CSR
	csrrw x13, 0x1, x0	// Write all 0s to CSR
	csrrs x13, 0x1, x30	// Set all CSR bits
	csrrc x13, 0x1, x30	// Clear all CSR bits
	csrrw x13, 0x1, x12	// Restore CSR

// Testing CSR 0x2
	csrr x13, 0x2	// Read CSR
	li x26, -1
	csrrw x11, 0x2, x26	// Write all 1s to CSR
	csrrw x11, 0x2, x0	// Write all 0s to CSR
	csrrs x11, 0x2, x26	// Set all CSR bits
	csrrc x11, 0x2, x26	// Clear all CSR bits
	csrrw x11, 0x2, x13	// Restore CSR

// Testing CSR 0x3
	csrr x9, 0x3	// Read CSR
	li x8, -1
	csrrw x29, 0x3, x8	// Write all 1s to CSR
	csrrw x29, 0x3, x0	// Write all 0s to CSR
	csrrs x29, 0x3, x8	// Set all CSR bits
	csrrc x29, 0x3, x8	// Clear all CSR bits
	csrrw x29, 0x3, x9	// Restore CSR

// Testing CSR 0x4
	csrr x23, 0x4	// Read CSR
	li x24, -1
	csrrw x16, 0x4, x24	// Write all 1s to CSR
	csrrw x16, 0x4, x0	// Write all 0s to CSR
	csrrs x16, 0x4, x24	// Set all CSR bits
	csrrc x16, 0x4, x24	// Clear all CSR bits
	csrrw x16, 0x4, x23	// Restore CSR

// Testing CSR 0x5
	csrr x11, 0x5	// Read CSR
	li x26, -1
	csrrw x10, 0x5, x26	// Write all 1s to CSR
	csrrw x10, 0x5, x0	// Write all 0s to CSR
	csrrs x10, 0x5, x26	// Set all CSR bits
	csrrc x10, 0x5, x26	// Clear all CSR bits
	csrrw x10, 0x5, x11	// Restore CSR

// Testing CSR 0x6
	csrr x19, 0x6	// Read CSR
	li x11, -1
	csrrw x6, 0x6, x11	// Write all 1s to CSR
	csrrw x6, 0x6, x0	// Write all 0s to CSR
	csrrs x6, 0x6, x11	// Set all CSR bits
	csrrc x6, 0x6, x11	// Clear all CSR bits
	csrrw x6, 0x6, x19	// Restore CSR

// Testing CSR 0x7
	csrr x26, 0x7	// Read CSR
	li x20, -1
	csrrw x1, 0x7, x20	// Write all 1s to CSR
	csrrw x1, 0x7, x0	// Write all 0s to CSR
	csrrs x1, 0x7, x20	// Set all CSR bits
	csrrc x1, 0x7, x20	// Clear all CSR bits
	csrrw x1, 0x7, x26	// Restore CSR

// Testing CSR 0x8
	csrr x1, 0x8	// Read CSR
	li x23, -1
	csrrw x29, 0x8, x23	// Write all 1s to CSR
	csrrw x29, 0x8, x0	// Write all 0s to CSR
	csrrs x29, 0x8, x23	// Set all CSR bits
	csrrc x29, 0x8, x23	// Clear all CSR bits
	csrrw x29, 0x8, x1	// Restore CSR

// Testing CSR 0x9
	csrr x25, 0x9	// Read CSR
	li x9, -1
	csrrw x31, 0x9, x9	// Write all 1s to CSR
	csrrw x31, 0x9, x0	// Write all 0s to CSR
	csrrs x31, 0x9, x9	// Set all CSR bits
	csrrc x31, 0x9, x9	// Clear all CSR bits
	csrrw x31, 0x9, x25	// Restore CSR

// Testing CSR 0xa
	csrr x6, 0xa	// Read CSR
	li x2, -1
	csrrw x4, 0xa, x2	// Write all 1s to CSR
	csrrw x4, 0xa, x0	// Write all 0s to CSR
	csrrs x4, 0xa, x2	// Set all CSR bits
	csrrc x4, 0xa, x2	// Clear all CSR bits
	csrrw x4, 0xa, x6	// Restore CSR

// Testing CSR 0xb
	csrr x25, 0xb	// Read CSR
	li x13, -1
	csrrw x19, 0xb, x13	// Write all 1s to CSR
	csrrw x19, 0xb, x0	// Write all 0s to CSR
	csrrs x19, 0xb, x13	// Set all CSR bits
	csrrc x19, 0xb, x13	// Clear all CSR bits
	csrrw x19, 0xb, x25	// Restore CSR

// Testing CSR 0xc
	csrr x30, 0xc	// Read CSR
	li x31, -1
	csrrw x29, 0xc, x31	// Write all 1s to CSR
	csrrw x29, 0xc, x0	// Write all 0s to CSR
	csrrs x29, 0xc, x31	// Set all CSR bits
	csrrc x29, 0xc, x31	// Clear all CSR bits
	csrrw x29, 0xc, x30	// Restore CSR

// Testing CSR 0xd
	csrr x28, 0xd	// Read CSR
	li x6, -1
	csrrw x25, 0xd, x6	// Write all 1s to CSR
	csrrw x25, 0xd, x0	// Write all 0s to CSR
	csrrs x25, 0xd, x6	// Set all CSR bits
	csrrc x25, 0xd, x6	// Clear all CSR bits
	csrrw x25, 0xd, x28	// Restore CSR

// Testing CSR 0xe
	csrr x30, 0xe	// Read CSR
	li x6, -1
	csrrw x19, 0xe, x6	// Write all 1s to CSR
	csrrw x19, 0xe, x0	// Write all 0s to CSR
	csrrs x19, 0xe, x6	// Set all CSR bits
	csrrc x19, 0xe, x6	// Clear all CSR bits
	csrrw x19, 0xe, x30	// Restore CSR

// Testing CSR 0xf
	csrr x26, 0xf	// Read CSR
	li x16, -1
	csrrw x4, 0xf, x16	// Write all 1s to CSR
	csrrw x4, 0xf, x0	// Write all 0s to CSR
	csrrs x4, 0xf, x16	// Set all CSR bits
	csrrc x4, 0xf, x16	// Clear all CSR bits
	csrrw x4, 0xf, x26	// Restore CSR

// Testing CSR 0x10
	csrr x21, 0x10	// Read CSR
	li x10, -1
	csrrw x7, 0x10, x10	// Write all 1s to CSR
	csrrw x7, 0x10, x0	// Write all 0s to CSR
	csrrs x7, 0x10, x10	// Set all CSR bits
	csrrc x7, 0x10, x10	// Clear all CSR bits
	csrrw x7, 0x10, x21	// Restore CSR

// Testing CSR 0x11
	csrr x9, 0x11	// Read CSR
	li x27, -1
	csrrw x28, 0x11, x27	// Write all 1s to CSR
	csrrw x28, 0x11, x0	// Write all 0s to CSR
	csrrs x28, 0x11, x27	// Set all CSR bits
	csrrc x28, 0x11, x27	// Clear all CSR bits
	csrrw x28, 0x11, x9	// Restore CSR

// Testing CSR 0x12
	csrr x5, 0x12	// Read CSR
	li x24, -1
	csrrw x12, 0x12, x24	// Write all 1s to CSR
	csrrw x12, 0x12, x0	// Write all 0s to CSR
	csrrs x12, 0x12, x24	// Set all CSR bits
	csrrc x12, 0x12, x24	// Clear all CSR bits
	csrrw x12, 0x12, x5	// Restore CSR

// Testing CSR 0x13
	csrr x7, 0x13	// Read CSR
	li x18, -1
	csrrw x5, 0x13, x18	// Write all 1s to CSR
	csrrw x5, 0x13, x0	// Write all 0s to CSR
	csrrs x5, 0x13, x18	// Set all CSR bits
	csrrc x5, 0x13, x18	// Clear all CSR bits
	csrrw x5, 0x13, x7	// Restore CSR

// Testing CSR 0x14
	csrr x8, 0x14	// Read CSR
	li x19, -1
	csrrw x2, 0x14, x19	// Write all 1s to CSR
	csrrw x2, 0x14, x0	// Write all 0s to CSR
	csrrs x2, 0x14, x19	// Set all CSR bits
	csrrc x2, 0x14, x19	// Clear all CSR bits
	csrrw x2, 0x14, x8	// Restore CSR

// Testing CSR 0x15
	csrr x22, 0x15	// Read CSR
	li x21, -1
	csrrw x8, 0x15, x21	// Write all 1s to CSR
	csrrw x8, 0x15, x0	// Write all 0s to CSR
	csrrs x8, 0x15, x21	// Set all CSR bits
	csrrc x8, 0x15, x21	// Clear all CSR bits
	csrrw x8, 0x15, x22	// Restore CSR

// Testing CSR 0x16
	csrr x13, 0x16	// Read CSR
	li x31, -1
	csrrw x2, 0x16, x31	// Write all 1s to CSR
	csrrw x2, 0x16, x0	// Write all 0s to CSR
	csrrs x2, 0x16, x31	// Set all CSR bits
	csrrc x2, 0x16, x31	// Clear all CSR bits
	csrrw x2, 0x16, x13	// Restore CSR

// Testing CSR 0x17
	csrr x9, 0x17	// Read CSR
	li x26, -1
	csrrw x27, 0x17, x26	// Write all 1s to CSR
	csrrw x27, 0x17, x0	// Write all 0s to CSR
	csrrs x27, 0x17, x26	// Set all CSR bits
	csrrc x27, 0x17, x26	// Clear all CSR bits
	csrrw x27, 0x17, x9	// Restore CSR

// Testing CSR 0x18
	csrr x19, 0x18	// Read CSR
	li x15, -1
	csrrw x16, 0x18, x15	// Write all 1s to CSR
	csrrw x16, 0x18, x0	// Write all 0s to CSR
	csrrs x16, 0x18, x15	// Set all CSR bits
	csrrc x16, 0x18, x15	// Clear all CSR bits
	csrrw x16, 0x18, x19	// Restore CSR

// Testing CSR 0x19
	csrr x11, 0x19	// Read CSR
	li x12, -1
	csrrw x28, 0x19, x12	// Write all 1s to CSR
	csrrw x28, 0x19, x0	// Write all 0s to CSR
	csrrs x28, 0x19, x12	// Set all CSR bits
	csrrc x28, 0x19, x12	// Clear all CSR bits
	csrrw x28, 0x19, x11	// Restore CSR

// Testing CSR 0x1a
	csrr x23, 0x1a	// Read CSR
	li x16, -1
	csrrw x18, 0x1a, x16	// Write all 1s to CSR
	csrrw x18, 0x1a, x0	// Write all 0s to CSR
	csrrs x18, 0x1a, x16	// Set all CSR bits
	csrrc x18, 0x1a, x16	// Clear all CSR bits
	csrrw x18, 0x1a, x23	// Restore CSR

// Testing CSR 0x1b
	csrr x4, 0x1b	// Read CSR
	li x10, -1
	csrrw x26, 0x1b, x10	// Write all 1s to CSR
	csrrw x26, 0x1b, x0	// Write all 0s to CSR
	csrrs x26, 0x1b, x10	// Set all CSR bits
	csrrc x26, 0x1b, x10	// Clear all CSR bits
	csrrw x26, 0x1b, x4	// Restore CSR

// Testing CSR 0x1c
	csrr x20, 0x1c	// Read CSR
	li x4, -1
	csrrw x9, 0x1c, x4	// Write all 1s to CSR
	csrrw x9, 0x1c, x0	// Write all 0s to CSR
	csrrs x9, 0x1c, x4	// Set all CSR bits
	csrrc x9, 0x1c, x4	// Clear all CSR bits
	csrrw x9, 0x1c, x20	// Restore CSR

// Testing CSR 0x1d
	csrr x2, 0x1d	// Read CSR
	li x22, -1
	csrrw x30, 0x1d, x22	// Write all 1s to CSR
	csrrw x30, 0x1d, x0	// Write all 0s to CSR
	csrrs x30, 0x1d, x22	// Set all CSR bits
	csrrc x30, 0x1d, x22	// Clear all CSR bits
	csrrw x30, 0x1d, x2	// Restore CSR

// Testing CSR 0x1e
	csrr x4, 0x1e	// Read CSR
	li x18, -1
	csrrw x29, 0x1e, x18	// Write all 1s to CSR
	csrrw x29, 0x1e, x0	// Write all 0s to CSR
	csrrs x29, 0x1e, x18	// Set all CSR bits
	csrrc x29, 0x1e, x18	// Clear all CSR bits
	csrrw x29, 0x1e, x4	// Restore CSR

// Testing CSR 0x1f
	csrr x29, 0x1f	// Read CSR
	li x22, -1
	csrrw x26, 0x1f, x22	// Write all 1s to CSR
	csrrw x26, 0x1f, x0	// Write all 0s to CSR
	csrrs x26, 0x1f, x22	// Set all CSR bits
	csrrc x26, 0x1f, x22	// Clear all CSR bits
	csrrw x26, 0x1f, x29	// Restore CSR

// Testing CSR 0x20
	csrr x29, 0x20	// Read CSR
	li x2, -1
	csrrw x19, 0x20, x2	// Write all 1s to CSR
	csrrw x19, 0x20, x0	// Write all 0s to CSR
	csrrs x19, 0x20, x2	// Set all CSR bits
	csrrc x19, 0x20, x2	// Clear all CSR bits
	csrrw x19, 0x20, x29	// Restore CSR

// Testing CSR 0x21
	csrr x24, 0x21	// Read CSR
	li x3, -1
	csrrw x15, 0x21, x3	// Write all 1s to CSR
	csrrw x15, 0x21, x0	// Write all 0s to CSR
	csrrs x15, 0x21, x3	// Set all CSR bits
	csrrc x15, 0x21, x3	// Clear all CSR bits
	csrrw x15, 0x21, x24	// Restore CSR

// Testing CSR 0x22
	csrr x11, 0x22	// Read CSR
	li x16, -1
	csrrw x17, 0x22, x16	// Write all 1s to CSR
	csrrw x17, 0x22, x0	// Write all 0s to CSR
	csrrs x17, 0x22, x16	// Set all CSR bits
	csrrc x17, 0x22, x16	// Clear all CSR bits
	csrrw x17, 0x22, x11	// Restore CSR

// Testing CSR 0x23
	csrr x25, 0x23	// Read CSR
	li x23, -1
	csrrw x11, 0x23, x23	// Write all 1s to CSR
	csrrw x11, 0x23, x0	// Write all 0s to CSR
	csrrs x11, 0x23, x23	// Set all CSR bits
	csrrc x11, 0x23, x23	// Clear all CSR bits
	csrrw x11, 0x23, x25	// Restore CSR

// Testing CSR 0x24
	csrr x16, 0x24	// Read CSR
	li x10, -1
	csrrw x15, 0x24, x10	// Write all 1s to CSR
	csrrw x15, 0x24, x0	// Write all 0s to CSR
	csrrs x15, 0x24, x10	// Set all CSR bits
	csrrc x15, 0x24, x10	// Clear all CSR bits
	csrrw x15, 0x24, x16	// Restore CSR

// Testing CSR 0x25
	csrr x19, 0x25	// Read CSR
	li x11, -1
	csrrw x1, 0x25, x11	// Write all 1s to CSR
	csrrw x1, 0x25, x0	// Write all 0s to CSR
	csrrs x1, 0x25, x11	// Set all CSR bits
	csrrc x1, 0x25, x11	// Clear all CSR bits
	csrrw x1, 0x25, x19	// Restore CSR

// Testing CSR 0x26
	csrr x14, 0x26	// Read CSR
	li x13, -1
	csrrw x2, 0x26, x13	// Write all 1s to CSR
	csrrw x2, 0x26, x0	// Write all 0s to CSR
	csrrs x2, 0x26, x13	// Set all CSR bits
	csrrc x2, 0x26, x13	// Clear all CSR bits
	csrrw x2, 0x26, x14	// Restore CSR

// Testing CSR 0x27
	csrr x31, 0x27	// Read CSR
	li x5, -1
	csrrw x24, 0x27, x5	// Write all 1s to CSR
	csrrw x24, 0x27, x0	// Write all 0s to CSR
	csrrs x24, 0x27, x5	// Set all CSR bits
	csrrc x24, 0x27, x5	// Clear all CSR bits
	csrrw x24, 0x27, x31	// Restore CSR

// Testing CSR 0x28
	csrr x26, 0x28	// Read CSR
	li x16, -1
	csrrw x20, 0x28, x16	// Write all 1s to CSR
	csrrw x20, 0x28, x0	// Write all 0s to CSR
	csrrs x20, 0x28, x16	// Set all CSR bits
	csrrc x20, 0x28, x16	// Clear all CSR bits
	csrrw x20, 0x28, x26	// Restore CSR

// Testing CSR 0x29
	csrr x19, 0x29	// Read CSR
	li x7, -1
	csrrw x31, 0x29, x7	// Write all 1s to CSR
	csrrw x31, 0x29, x0	// Write all 0s to CSR
	csrrs x31, 0x29, x7	// Set all CSR bits
	csrrc x31, 0x29, x7	// Clear all CSR bits
	csrrw x31, 0x29, x19	// Restore CSR

// Testing CSR 0x2a
	csrr x28, 0x2a	// Read CSR
	li x10, -1
	csrrw x18, 0x2a, x10	// Write all 1s to CSR
	csrrw x18, 0x2a, x0	// Write all 0s to CSR
	csrrs x18, 0x2a, x10	// Set all CSR bits
	csrrc x18, 0x2a, x10	// Clear all CSR bits
	csrrw x18, 0x2a, x28	// Restore CSR

// Testing CSR 0x2b
	csrr x19, 0x2b	// Read CSR
	li x9, -1
	csrrw x7, 0x2b, x9	// Write all 1s to CSR
	csrrw x7, 0x2b, x0	// Write all 0s to CSR
	csrrs x7, 0x2b, x9	// Set all CSR bits
	csrrc x7, 0x2b, x9	// Clear all CSR bits
	csrrw x7, 0x2b, x19	// Restore CSR

// Testing CSR 0x2c
	csrr x16, 0x2c	// Read CSR
	li x12, -1
	csrrw x23, 0x2c, x12	// Write all 1s to CSR
	csrrw x23, 0x2c, x0	// Write all 0s to CSR
	csrrs x23, 0x2c, x12	// Set all CSR bits
	csrrc x23, 0x2c, x12	// Clear all CSR bits
	csrrw x23, 0x2c, x16	// Restore CSR

// Testing CSR 0x2d
	csrr x20, 0x2d	// Read CSR
	li x25, -1
	csrrw x30, 0x2d, x25	// Write all 1s to CSR
	csrrw x30, 0x2d, x0	// Write all 0s to CSR
	csrrs x30, 0x2d, x25	// Set all CSR bits
	csrrc x30, 0x2d, x25	// Clear all CSR bits
	csrrw x30, 0x2d, x20	// Restore CSR

// Testing CSR 0x2e
	csrr x22, 0x2e	// Read CSR
	li x16, -1
	csrrw x14, 0x2e, x16	// Write all 1s to CSR
	csrrw x14, 0x2e, x0	// Write all 0s to CSR
	csrrs x14, 0x2e, x16	// Set all CSR bits
	csrrc x14, 0x2e, x16	// Clear all CSR bits
	csrrw x14, 0x2e, x22	// Restore CSR

// Testing CSR 0x2f
	csrr x6, 0x2f	// Read CSR
	li x18, -1
	csrrw x28, 0x2f, x18	// Write all 1s to CSR
	csrrw x28, 0x2f, x0	// Write all 0s to CSR
	csrrs x28, 0x2f, x18	// Set all CSR bits
	csrrc x28, 0x2f, x18	// Clear all CSR bits
	csrrw x28, 0x2f, x6	// Restore CSR

// Testing CSR 0x30
	csrr x18, 0x30	// Read CSR
	li x4, -1
	csrrw x22, 0x30, x4	// Write all 1s to CSR
	csrrw x22, 0x30, x0	// Write all 0s to CSR
	csrrs x22, 0x30, x4	// Set all CSR bits
	csrrc x22, 0x30, x4	// Clear all CSR bits
	csrrw x22, 0x30, x18	// Restore CSR

// Testing CSR 0x31
	csrr x31, 0x31	// Read CSR
	li x23, -1
	csrrw x24, 0x31, x23	// Write all 1s to CSR
	csrrw x24, 0x31, x0	// Write all 0s to CSR
	csrrs x24, 0x31, x23	// Set all CSR bits
	csrrc x24, 0x31, x23	// Clear all CSR bits
	csrrw x24, 0x31, x31	// Restore CSR

// Testing CSR 0x32
	csrr x18, 0x32	// Read CSR
	li x14, -1
	csrrw x16, 0x32, x14	// Write all 1s to CSR
	csrrw x16, 0x32, x0	// Write all 0s to CSR
	csrrs x16, 0x32, x14	// Set all CSR bits
	csrrc x16, 0x32, x14	// Clear all CSR bits
	csrrw x16, 0x32, x18	// Restore CSR

// Testing CSR 0x33
	csrr x3, 0x33	// Read CSR
	li x19, -1
	csrrw x7, 0x33, x19	// Write all 1s to CSR
	csrrw x7, 0x33, x0	// Write all 0s to CSR
	csrrs x7, 0x33, x19	// Set all CSR bits
	csrrc x7, 0x33, x19	// Clear all CSR bits
	csrrw x7, 0x33, x3	// Restore CSR

// Testing CSR 0x34
	csrr x9, 0x34	// Read CSR
	li x20, -1
	csrrw x14, 0x34, x20	// Write all 1s to CSR
	csrrw x14, 0x34, x0	// Write all 0s to CSR
	csrrs x14, 0x34, x20	// Set all CSR bits
	csrrc x14, 0x34, x20	// Clear all CSR bits
	csrrw x14, 0x34, x9	// Restore CSR

// Testing CSR 0x35
	csrr x7, 0x35	// Read CSR
	li x4, -1
	csrrw x14, 0x35, x4	// Write all 1s to CSR
	csrrw x14, 0x35, x0	// Write all 0s to CSR
	csrrs x14, 0x35, x4	// Set all CSR bits
	csrrc x14, 0x35, x4	// Clear all CSR bits
	csrrw x14, 0x35, x7	// Restore CSR

// Testing CSR 0x36
	csrr x19, 0x36	// Read CSR
	li x21, -1
	csrrw x8, 0x36, x21	// Write all 1s to CSR
	csrrw x8, 0x36, x0	// Write all 0s to CSR
	csrrs x8, 0x36, x21	// Set all CSR bits
	csrrc x8, 0x36, x21	// Clear all CSR bits
	csrrw x8, 0x36, x19	// Restore CSR

// Testing CSR 0x37
	csrr x15, 0x37	// Read CSR
	li x20, -1
	csrrw x18, 0x37, x20	// Write all 1s to CSR
	csrrw x18, 0x37, x0	// Write all 0s to CSR
	csrrs x18, 0x37, x20	// Set all CSR bits
	csrrc x18, 0x37, x20	// Clear all CSR bits
	csrrw x18, 0x37, x15	// Restore CSR

// Testing CSR 0x38
	csrr x26, 0x38	// Read CSR
	li x28, -1
	csrrw x8, 0x38, x28	// Write all 1s to CSR
	csrrw x8, 0x38, x0	// Write all 0s to CSR
	csrrs x8, 0x38, x28	// Set all CSR bits
	csrrc x8, 0x38, x28	// Clear all CSR bits
	csrrw x8, 0x38, x26	// Restore CSR

// Testing CSR 0x39
	csrr x19, 0x39	// Read CSR
	li x9, -1
	csrrw x1, 0x39, x9	// Write all 1s to CSR
	csrrw x1, 0x39, x0	// Write all 0s to CSR
	csrrs x1, 0x39, x9	// Set all CSR bits
	csrrc x1, 0x39, x9	// Clear all CSR bits
	csrrw x1, 0x39, x19	// Restore CSR

// Testing CSR 0x3a
	csrr x23, 0x3a	// Read CSR
	li x26, -1
	csrrw x24, 0x3a, x26	// Write all 1s to CSR
	csrrw x24, 0x3a, x0	// Write all 0s to CSR
	csrrs x24, 0x3a, x26	// Set all CSR bits
	csrrc x24, 0x3a, x26	// Clear all CSR bits
	csrrw x24, 0x3a, x23	// Restore CSR

// Testing CSR 0x3b
	csrr x10, 0x3b	// Read CSR
	li x14, -1
	csrrw x13, 0x3b, x14	// Write all 1s to CSR
	csrrw x13, 0x3b, x0	// Write all 0s to CSR
	csrrs x13, 0x3b, x14	// Set all CSR bits
	csrrc x13, 0x3b, x14	// Clear all CSR bits
	csrrw x13, 0x3b, x10	// Restore CSR

// Testing CSR 0x3c
	csrr x31, 0x3c	// Read CSR
	li x12, -1
	csrrw x28, 0x3c, x12	// Write all 1s to CSR
	csrrw x28, 0x3c, x0	// Write all 0s to CSR
	csrrs x28, 0x3c, x12	// Set all CSR bits
	csrrc x28, 0x3c, x12	// Clear all CSR bits
	csrrw x28, 0x3c, x31	// Restore CSR

// Testing CSR 0x3d
	csrr x31, 0x3d	// Read CSR
	li x10, -1
	csrrw x1, 0x3d, x10	// Write all 1s to CSR
	csrrw x1, 0x3d, x0	// Write all 0s to CSR
	csrrs x1, 0x3d, x10	// Set all CSR bits
	csrrc x1, 0x3d, x10	// Clear all CSR bits
	csrrw x1, 0x3d, x31	// Restore CSR

// Testing CSR 0x3e
	csrr x8, 0x3e	// Read CSR
	li x9, -1
	csrrw x13, 0x3e, x9	// Write all 1s to CSR
	csrrw x13, 0x3e, x0	// Write all 0s to CSR
	csrrs x13, 0x3e, x9	// Set all CSR bits
	csrrc x13, 0x3e, x9	// Clear all CSR bits
	csrrw x13, 0x3e, x8	// Restore CSR

// Testing CSR 0x3f
	csrr x31, 0x3f	// Read CSR
	li x7, -1
	csrrw x23, 0x3f, x7	// Write all 1s to CSR
	csrrw x23, 0x3f, x0	// Write all 0s to CSR
	csrrs x23, 0x3f, x7	// Set all CSR bits
	csrrc x23, 0x3f, x7	// Clear all CSR bits
	csrrw x23, 0x3f, x31	// Restore CSR

// Testing CSR 0x40
	csrr x25, 0x40	// Read CSR
	li x3, -1
	csrrw x12, 0x40, x3	// Write all 1s to CSR
	csrrw x12, 0x40, x0	// Write all 0s to CSR
	csrrs x12, 0x40, x3	// Set all CSR bits
	csrrc x12, 0x40, x3	// Clear all CSR bits
	csrrw x12, 0x40, x25	// Restore CSR

// Testing CSR 0x41
	csrr x1, 0x41	// Read CSR
	li x5, -1
	csrrw x15, 0x41, x5	// Write all 1s to CSR
	csrrw x15, 0x41, x0	// Write all 0s to CSR
	csrrs x15, 0x41, x5	// Set all CSR bits
	csrrc x15, 0x41, x5	// Clear all CSR bits
	csrrw x15, 0x41, x1	// Restore CSR

// Testing CSR 0x42
	csrr x11, 0x42	// Read CSR
	li x1, -1
	csrrw x17, 0x42, x1	// Write all 1s to CSR
	csrrw x17, 0x42, x0	// Write all 0s to CSR
	csrrs x17, 0x42, x1	// Set all CSR bits
	csrrc x17, 0x42, x1	// Clear all CSR bits
	csrrw x17, 0x42, x11	// Restore CSR

// Testing CSR 0x43
	csrr x21, 0x43	// Read CSR
	li x18, -1
	csrrw x23, 0x43, x18	// Write all 1s to CSR
	csrrw x23, 0x43, x0	// Write all 0s to CSR
	csrrs x23, 0x43, x18	// Set all CSR bits
	csrrc x23, 0x43, x18	// Clear all CSR bits
	csrrw x23, 0x43, x21	// Restore CSR

// Testing CSR 0x44
	csrr x3, 0x44	// Read CSR
	li x2, -1
	csrrw x24, 0x44, x2	// Write all 1s to CSR
	csrrw x24, 0x44, x0	// Write all 0s to CSR
	csrrs x24, 0x44, x2	// Set all CSR bits
	csrrc x24, 0x44, x2	// Clear all CSR bits
	csrrw x24, 0x44, x3	// Restore CSR

// Testing CSR 0x45
	csrr x30, 0x45	// Read CSR
	li x24, -1
	csrrw x9, 0x45, x24	// Write all 1s to CSR
	csrrw x9, 0x45, x0	// Write all 0s to CSR
	csrrs x9, 0x45, x24	// Set all CSR bits
	csrrc x9, 0x45, x24	// Clear all CSR bits
	csrrw x9, 0x45, x30	// Restore CSR

// Testing CSR 0x46
	csrr x26, 0x46	// Read CSR
	li x13, -1
	csrrw x29, 0x46, x13	// Write all 1s to CSR
	csrrw x29, 0x46, x0	// Write all 0s to CSR
	csrrs x29, 0x46, x13	// Set all CSR bits
	csrrc x29, 0x46, x13	// Clear all CSR bits
	csrrw x29, 0x46, x26	// Restore CSR

// Testing CSR 0x47
	csrr x31, 0x47	// Read CSR
	li x15, -1
	csrrw x26, 0x47, x15	// Write all 1s to CSR
	csrrw x26, 0x47, x0	// Write all 0s to CSR
	csrrs x26, 0x47, x15	// Set all CSR bits
	csrrc x26, 0x47, x15	// Clear all CSR bits
	csrrw x26, 0x47, x31	// Restore CSR

// Testing CSR 0x48
	csrr x13, 0x48	// Read CSR
	li x27, -1
	csrrw x18, 0x48, x27	// Write all 1s to CSR
	csrrw x18, 0x48, x0	// Write all 0s to CSR
	csrrs x18, 0x48, x27	// Set all CSR bits
	csrrc x18, 0x48, x27	// Clear all CSR bits
	csrrw x18, 0x48, x13	// Restore CSR

// Testing CSR 0x49
	csrr x15, 0x49	// Read CSR
	li x21, -1
	csrrw x5, 0x49, x21	// Write all 1s to CSR
	csrrw x5, 0x49, x0	// Write all 0s to CSR
	csrrs x5, 0x49, x21	// Set all CSR bits
	csrrc x5, 0x49, x21	// Clear all CSR bits
	csrrw x5, 0x49, x15	// Restore CSR

// Testing CSR 0x4a
	csrr x4, 0x4a	// Read CSR
	li x30, -1
	csrrw x1, 0x4a, x30	// Write all 1s to CSR
	csrrw x1, 0x4a, x0	// Write all 0s to CSR
	csrrs x1, 0x4a, x30	// Set all CSR bits
	csrrc x1, 0x4a, x30	// Clear all CSR bits
	csrrw x1, 0x4a, x4	// Restore CSR

// Testing CSR 0x4b
	csrr x20, 0x4b	// Read CSR
	li x31, -1
	csrrw x14, 0x4b, x31	// Write all 1s to CSR
	csrrw x14, 0x4b, x0	// Write all 0s to CSR
	csrrs x14, 0x4b, x31	// Set all CSR bits
	csrrc x14, 0x4b, x31	// Clear all CSR bits
	csrrw x14, 0x4b, x20	// Restore CSR

// Testing CSR 0x4c
	csrr x11, 0x4c	// Read CSR
	li x7, -1
	csrrw x18, 0x4c, x7	// Write all 1s to CSR
	csrrw x18, 0x4c, x0	// Write all 0s to CSR
	csrrs x18, 0x4c, x7	// Set all CSR bits
	csrrc x18, 0x4c, x7	// Clear all CSR bits
	csrrw x18, 0x4c, x11	// Restore CSR

// Testing CSR 0x4d
	csrr x26, 0x4d	// Read CSR
	li x9, -1
	csrrw x12, 0x4d, x9	// Write all 1s to CSR
	csrrw x12, 0x4d, x0	// Write all 0s to CSR
	csrrs x12, 0x4d, x9	// Set all CSR bits
	csrrc x12, 0x4d, x9	// Clear all CSR bits
	csrrw x12, 0x4d, x26	// Restore CSR

// Testing CSR 0x4e
	csrr x27, 0x4e	// Read CSR
	li x26, -1
	csrrw x17, 0x4e, x26	// Write all 1s to CSR
	csrrw x17, 0x4e, x0	// Write all 0s to CSR
	csrrs x17, 0x4e, x26	// Set all CSR bits
	csrrc x17, 0x4e, x26	// Clear all CSR bits
	csrrw x17, 0x4e, x27	// Restore CSR

// Testing CSR 0x4f
	csrr x30, 0x4f	// Read CSR
	li x11, -1
	csrrw x19, 0x4f, x11	// Write all 1s to CSR
	csrrw x19, 0x4f, x0	// Write all 0s to CSR
	csrrs x19, 0x4f, x11	// Set all CSR bits
	csrrc x19, 0x4f, x11	// Clear all CSR bits
	csrrw x19, 0x4f, x30	// Restore CSR

// Testing CSR 0x50
	csrr x15, 0x50	// Read CSR
	li x17, -1
	csrrw x30, 0x50, x17	// Write all 1s to CSR
	csrrw x30, 0x50, x0	// Write all 0s to CSR
	csrrs x30, 0x50, x17	// Set all CSR bits
	csrrc x30, 0x50, x17	// Clear all CSR bits
	csrrw x30, 0x50, x15	// Restore CSR

// Testing CSR 0x51
	csrr x24, 0x51	// Read CSR
	li x6, -1
	csrrw x2, 0x51, x6	// Write all 1s to CSR
	csrrw x2, 0x51, x0	// Write all 0s to CSR
	csrrs x2, 0x51, x6	// Set all CSR bits
	csrrc x2, 0x51, x6	// Clear all CSR bits
	csrrw x2, 0x51, x24	// Restore CSR

// Testing CSR 0x52
	csrr x15, 0x52	// Read CSR
	li x29, -1
	csrrw x10, 0x52, x29	// Write all 1s to CSR
	csrrw x10, 0x52, x0	// Write all 0s to CSR
	csrrs x10, 0x52, x29	// Set all CSR bits
	csrrc x10, 0x52, x29	// Clear all CSR bits
	csrrw x10, 0x52, x15	// Restore CSR

// Testing CSR 0x53
	csrr x8, 0x53	// Read CSR
	li x11, -1
	csrrw x7, 0x53, x11	// Write all 1s to CSR
	csrrw x7, 0x53, x0	// Write all 0s to CSR
	csrrs x7, 0x53, x11	// Set all CSR bits
	csrrc x7, 0x53, x11	// Clear all CSR bits
	csrrw x7, 0x53, x8	// Restore CSR

// Testing CSR 0x54
	csrr x16, 0x54	// Read CSR
	li x11, -1
	csrrw x24, 0x54, x11	// Write all 1s to CSR
	csrrw x24, 0x54, x0	// Write all 0s to CSR
	csrrs x24, 0x54, x11	// Set all CSR bits
	csrrc x24, 0x54, x11	// Clear all CSR bits
	csrrw x24, 0x54, x16	// Restore CSR

// Testing CSR 0x55
	csrr x19, 0x55	// Read CSR
	li x28, -1
	csrrw x16, 0x55, x28	// Write all 1s to CSR
	csrrw x16, 0x55, x0	// Write all 0s to CSR
	csrrs x16, 0x55, x28	// Set all CSR bits
	csrrc x16, 0x55, x28	// Clear all CSR bits
	csrrw x16, 0x55, x19	// Restore CSR

// Testing CSR 0x56
	csrr x2, 0x56	// Read CSR
	li x27, -1
	csrrw x11, 0x56, x27	// Write all 1s to CSR
	csrrw x11, 0x56, x0	// Write all 0s to CSR
	csrrs x11, 0x56, x27	// Set all CSR bits
	csrrc x11, 0x56, x27	// Clear all CSR bits
	csrrw x11, 0x56, x2	// Restore CSR

// Testing CSR 0x57
	csrr x23, 0x57	// Read CSR
	li x19, -1
	csrrw x16, 0x57, x19	// Write all 1s to CSR
	csrrw x16, 0x57, x0	// Write all 0s to CSR
	csrrs x16, 0x57, x19	// Set all CSR bits
	csrrc x16, 0x57, x19	// Clear all CSR bits
	csrrw x16, 0x57, x23	// Restore CSR

// Testing CSR 0x58
	csrr x12, 0x58	// Read CSR
	li x9, -1
	csrrw x20, 0x58, x9	// Write all 1s to CSR
	csrrw x20, 0x58, x0	// Write all 0s to CSR
	csrrs x20, 0x58, x9	// Set all CSR bits
	csrrc x20, 0x58, x9	// Clear all CSR bits
	csrrw x20, 0x58, x12	// Restore CSR

// Testing CSR 0x59
	csrr x29, 0x59	// Read CSR
	li x22, -1
	csrrw x10, 0x59, x22	// Write all 1s to CSR
	csrrw x10, 0x59, x0	// Write all 0s to CSR
	csrrs x10, 0x59, x22	// Set all CSR bits
	csrrc x10, 0x59, x22	// Clear all CSR bits
	csrrw x10, 0x59, x29	// Restore CSR

// Testing CSR 0x5a
	csrr x15, 0x5a	// Read CSR
	li x19, -1
	csrrw x21, 0x5a, x19	// Write all 1s to CSR
	csrrw x21, 0x5a, x0	// Write all 0s to CSR
	csrrs x21, 0x5a, x19	// Set all CSR bits
	csrrc x21, 0x5a, x19	// Clear all CSR bits
	csrrw x21, 0x5a, x15	// Restore CSR

// Testing CSR 0x5b
	csrr x23, 0x5b	// Read CSR
	li x11, -1
	csrrw x18, 0x5b, x11	// Write all 1s to CSR
	csrrw x18, 0x5b, x0	// Write all 0s to CSR
	csrrs x18, 0x5b, x11	// Set all CSR bits
	csrrc x18, 0x5b, x11	// Clear all CSR bits
	csrrw x18, 0x5b, x23	// Restore CSR

// Testing CSR 0x5c
	csrr x17, 0x5c	// Read CSR
	li x24, -1
	csrrw x4, 0x5c, x24	// Write all 1s to CSR
	csrrw x4, 0x5c, x0	// Write all 0s to CSR
	csrrs x4, 0x5c, x24	// Set all CSR bits
	csrrc x4, 0x5c, x24	// Clear all CSR bits
	csrrw x4, 0x5c, x17	// Restore CSR

// Testing CSR 0x5d
	csrr x3, 0x5d	// Read CSR
	li x18, -1
	csrrw x5, 0x5d, x18	// Write all 1s to CSR
	csrrw x5, 0x5d, x0	// Write all 0s to CSR
	csrrs x5, 0x5d, x18	// Set all CSR bits
	csrrc x5, 0x5d, x18	// Clear all CSR bits
	csrrw x5, 0x5d, x3	// Restore CSR

// Testing CSR 0x5e
	csrr x20, 0x5e	// Read CSR
	li x9, -1
	csrrw x19, 0x5e, x9	// Write all 1s to CSR
	csrrw x19, 0x5e, x0	// Write all 0s to CSR
	csrrs x19, 0x5e, x9	// Set all CSR bits
	csrrc x19, 0x5e, x9	// Clear all CSR bits
	csrrw x19, 0x5e, x20	// Restore CSR

// Testing CSR 0x5f
	csrr x24, 0x5f	// Read CSR
	li x30, -1
	csrrw x16, 0x5f, x30	// Write all 1s to CSR
	csrrw x16, 0x5f, x0	// Write all 0s to CSR
	csrrs x16, 0x5f, x30	// Set all CSR bits
	csrrc x16, 0x5f, x30	// Clear all CSR bits
	csrrw x16, 0x5f, x24	// Restore CSR

// Testing CSR 0x60
	csrr x9, 0x60	// Read CSR
	li x3, -1
	csrrw x6, 0x60, x3	// Write all 1s to CSR
	csrrw x6, 0x60, x0	// Write all 0s to CSR
	csrrs x6, 0x60, x3	// Set all CSR bits
	csrrc x6, 0x60, x3	// Clear all CSR bits
	csrrw x6, 0x60, x9	// Restore CSR

// Testing CSR 0x61
	csrr x7, 0x61	// Read CSR
	li x27, -1
	csrrw x29, 0x61, x27	// Write all 1s to CSR
	csrrw x29, 0x61, x0	// Write all 0s to CSR
	csrrs x29, 0x61, x27	// Set all CSR bits
	csrrc x29, 0x61, x27	// Clear all CSR bits
	csrrw x29, 0x61, x7	// Restore CSR

// Testing CSR 0x62
	csrr x28, 0x62	// Read CSR
	li x21, -1
	csrrw x11, 0x62, x21	// Write all 1s to CSR
	csrrw x11, 0x62, x0	// Write all 0s to CSR
	csrrs x11, 0x62, x21	// Set all CSR bits
	csrrc x11, 0x62, x21	// Clear all CSR bits
	csrrw x11, 0x62, x28	// Restore CSR

// Testing CSR 0x63
	csrr x6, 0x63	// Read CSR
	li x7, -1
	csrrw x27, 0x63, x7	// Write all 1s to CSR
	csrrw x27, 0x63, x0	// Write all 0s to CSR
	csrrs x27, 0x63, x7	// Set all CSR bits
	csrrc x27, 0x63, x7	// Clear all CSR bits
	csrrw x27, 0x63, x6	// Restore CSR

// Testing CSR 0x64
	csrr x18, 0x64	// Read CSR
	li x29, -1
	csrrw x9, 0x64, x29	// Write all 1s to CSR
	csrrw x9, 0x64, x0	// Write all 0s to CSR
	csrrs x9, 0x64, x29	// Set all CSR bits
	csrrc x9, 0x64, x29	// Clear all CSR bits
	csrrw x9, 0x64, x18	// Restore CSR

// Testing CSR 0x65
	csrr x27, 0x65	// Read CSR
	li x13, -1
	csrrw x7, 0x65, x13	// Write all 1s to CSR
	csrrw x7, 0x65, x0	// Write all 0s to CSR
	csrrs x7, 0x65, x13	// Set all CSR bits
	csrrc x7, 0x65, x13	// Clear all CSR bits
	csrrw x7, 0x65, x27	// Restore CSR

// Testing CSR 0x66
	csrr x17, 0x66	// Read CSR
	li x3, -1
	csrrw x24, 0x66, x3	// Write all 1s to CSR
	csrrw x24, 0x66, x0	// Write all 0s to CSR
	csrrs x24, 0x66, x3	// Set all CSR bits
	csrrc x24, 0x66, x3	// Clear all CSR bits
	csrrw x24, 0x66, x17	// Restore CSR

// Testing CSR 0x67
	csrr x24, 0x67	// Read CSR
	li x30, -1
	csrrw x9, 0x67, x30	// Write all 1s to CSR
	csrrw x9, 0x67, x0	// Write all 0s to CSR
	csrrs x9, 0x67, x30	// Set all CSR bits
	csrrc x9, 0x67, x30	// Clear all CSR bits
	csrrw x9, 0x67, x24	// Restore CSR

// Testing CSR 0x68
	csrr x15, 0x68	// Read CSR
	li x24, -1
	csrrw x29, 0x68, x24	// Write all 1s to CSR
	csrrw x29, 0x68, x0	// Write all 0s to CSR
	csrrs x29, 0x68, x24	// Set all CSR bits
	csrrc x29, 0x68, x24	// Clear all CSR bits
	csrrw x29, 0x68, x15	// Restore CSR

// Testing CSR 0x69
	csrr x2, 0x69	// Read CSR
	li x17, -1
	csrrw x22, 0x69, x17	// Write all 1s to CSR
	csrrw x22, 0x69, x0	// Write all 0s to CSR
	csrrs x22, 0x69, x17	// Set all CSR bits
	csrrc x22, 0x69, x17	// Clear all CSR bits
	csrrw x22, 0x69, x2	// Restore CSR

// Testing CSR 0x6a
	csrr x29, 0x6a	// Read CSR
	li x11, -1
	csrrw x6, 0x6a, x11	// Write all 1s to CSR
	csrrw x6, 0x6a, x0	// Write all 0s to CSR
	csrrs x6, 0x6a, x11	// Set all CSR bits
	csrrc x6, 0x6a, x11	// Clear all CSR bits
	csrrw x6, 0x6a, x29	// Restore CSR

// Testing CSR 0x6b
	csrr x5, 0x6b	// Read CSR
	li x22, -1
	csrrw x18, 0x6b, x22	// Write all 1s to CSR
	csrrw x18, 0x6b, x0	// Write all 0s to CSR
	csrrs x18, 0x6b, x22	// Set all CSR bits
	csrrc x18, 0x6b, x22	// Clear all CSR bits
	csrrw x18, 0x6b, x5	// Restore CSR

// Testing CSR 0x6c
	csrr x21, 0x6c	// Read CSR
	li x15, -1
	csrrw x7, 0x6c, x15	// Write all 1s to CSR
	csrrw x7, 0x6c, x0	// Write all 0s to CSR
	csrrs x7, 0x6c, x15	// Set all CSR bits
	csrrc x7, 0x6c, x15	// Clear all CSR bits
	csrrw x7, 0x6c, x21	// Restore CSR

// Testing CSR 0x6d
	csrr x8, 0x6d	// Read CSR
	li x23, -1
	csrrw x14, 0x6d, x23	// Write all 1s to CSR
	csrrw x14, 0x6d, x0	// Write all 0s to CSR
	csrrs x14, 0x6d, x23	// Set all CSR bits
	csrrc x14, 0x6d, x23	// Clear all CSR bits
	csrrw x14, 0x6d, x8	// Restore CSR

// Testing CSR 0x6e
	csrr x1, 0x6e	// Read CSR
	li x6, -1
	csrrw x9, 0x6e, x6	// Write all 1s to CSR
	csrrw x9, 0x6e, x0	// Write all 0s to CSR
	csrrs x9, 0x6e, x6	// Set all CSR bits
	csrrc x9, 0x6e, x6	// Clear all CSR bits
	csrrw x9, 0x6e, x1	// Restore CSR

// Testing CSR 0x6f
	csrr x4, 0x6f	// Read CSR
	li x1, -1
	csrrw x18, 0x6f, x1	// Write all 1s to CSR
	csrrw x18, 0x6f, x0	// Write all 0s to CSR
	csrrs x18, 0x6f, x1	// Set all CSR bits
	csrrc x18, 0x6f, x1	// Clear all CSR bits
	csrrw x18, 0x6f, x4	// Restore CSR

// Testing CSR 0x70
	csrr x9, 0x70	// Read CSR
	li x13, -1
	csrrw x1, 0x70, x13	// Write all 1s to CSR
	csrrw x1, 0x70, x0	// Write all 0s to CSR
	csrrs x1, 0x70, x13	// Set all CSR bits
	csrrc x1, 0x70, x13	// Clear all CSR bits
	csrrw x1, 0x70, x9	// Restore CSR

// Testing CSR 0x71
	csrr x26, 0x71	// Read CSR
	li x1, -1
	csrrw x2, 0x71, x1	// Write all 1s to CSR
	csrrw x2, 0x71, x0	// Write all 0s to CSR
	csrrs x2, 0x71, x1	// Set all CSR bits
	csrrc x2, 0x71, x1	// Clear all CSR bits
	csrrw x2, 0x71, x26	// Restore CSR

// Testing CSR 0x72
	csrr x6, 0x72	// Read CSR
	li x8, -1
	csrrw x7, 0x72, x8	// Write all 1s to CSR
	csrrw x7, 0x72, x0	// Write all 0s to CSR
	csrrs x7, 0x72, x8	// Set all CSR bits
	csrrc x7, 0x72, x8	// Clear all CSR bits
	csrrw x7, 0x72, x6	// Restore CSR

// Testing CSR 0x73
	csrr x2, 0x73	// Read CSR
	li x21, -1
	csrrw x9, 0x73, x21	// Write all 1s to CSR
	csrrw x9, 0x73, x0	// Write all 0s to CSR
	csrrs x9, 0x73, x21	// Set all CSR bits
	csrrc x9, 0x73, x21	// Clear all CSR bits
	csrrw x9, 0x73, x2	// Restore CSR

// Testing CSR 0x74
	csrr x17, 0x74	// Read CSR
	li x16, -1
	csrrw x14, 0x74, x16	// Write all 1s to CSR
	csrrw x14, 0x74, x0	// Write all 0s to CSR
	csrrs x14, 0x74, x16	// Set all CSR bits
	csrrc x14, 0x74, x16	// Clear all CSR bits
	csrrw x14, 0x74, x17	// Restore CSR

// Testing CSR 0x75
	csrr x1, 0x75	// Read CSR
	li x21, -1
	csrrw x17, 0x75, x21	// Write all 1s to CSR
	csrrw x17, 0x75, x0	// Write all 0s to CSR
	csrrs x17, 0x75, x21	// Set all CSR bits
	csrrc x17, 0x75, x21	// Clear all CSR bits
	csrrw x17, 0x75, x1	// Restore CSR

// Testing CSR 0x76
	csrr x21, 0x76	// Read CSR
	li x4, -1
	csrrw x23, 0x76, x4	// Write all 1s to CSR
	csrrw x23, 0x76, x0	// Write all 0s to CSR
	csrrs x23, 0x76, x4	// Set all CSR bits
	csrrc x23, 0x76, x4	// Clear all CSR bits
	csrrw x23, 0x76, x21	// Restore CSR

// Testing CSR 0x77
	csrr x20, 0x77	// Read CSR
	li x12, -1
	csrrw x19, 0x77, x12	// Write all 1s to CSR
	csrrw x19, 0x77, x0	// Write all 0s to CSR
	csrrs x19, 0x77, x12	// Set all CSR bits
	csrrc x19, 0x77, x12	// Clear all CSR bits
	csrrw x19, 0x77, x20	// Restore CSR

// Testing CSR 0x78
	csrr x8, 0x78	// Read CSR
	li x6, -1
	csrrw x20, 0x78, x6	// Write all 1s to CSR
	csrrw x20, 0x78, x0	// Write all 0s to CSR
	csrrs x20, 0x78, x6	// Set all CSR bits
	csrrc x20, 0x78, x6	// Clear all CSR bits
	csrrw x20, 0x78, x8	// Restore CSR

// Testing CSR 0x79
	csrr x8, 0x79	// Read CSR
	li x3, -1
	csrrw x21, 0x79, x3	// Write all 1s to CSR
	csrrw x21, 0x79, x0	// Write all 0s to CSR
	csrrs x21, 0x79, x3	// Set all CSR bits
	csrrc x21, 0x79, x3	// Clear all CSR bits
	csrrw x21, 0x79, x8	// Restore CSR

// Testing CSR 0x7a
	csrr x31, 0x7a	// Read CSR
	li x15, -1
	csrrw x30, 0x7a, x15	// Write all 1s to CSR
	csrrw x30, 0x7a, x0	// Write all 0s to CSR
	csrrs x30, 0x7a, x15	// Set all CSR bits
	csrrc x30, 0x7a, x15	// Clear all CSR bits
	csrrw x30, 0x7a, x31	// Restore CSR

// Testing CSR 0x7b
	csrr x12, 0x7b	// Read CSR
	li x2, -1
	csrrw x21, 0x7b, x2	// Write all 1s to CSR
	csrrw x21, 0x7b, x0	// Write all 0s to CSR
	csrrs x21, 0x7b, x2	// Set all CSR bits
	csrrc x21, 0x7b, x2	// Clear all CSR bits
	csrrw x21, 0x7b, x12	// Restore CSR

// Testing CSR 0x7c
	csrr x6, 0x7c	// Read CSR
	li x14, -1
	csrrw x24, 0x7c, x14	// Write all 1s to CSR
	csrrw x24, 0x7c, x0	// Write all 0s to CSR
	csrrs x24, 0x7c, x14	// Set all CSR bits
	csrrc x24, 0x7c, x14	// Clear all CSR bits
	csrrw x24, 0x7c, x6	// Restore CSR

// Testing CSR 0x7d
	csrr x21, 0x7d	// Read CSR
	li x5, -1
	csrrw x28, 0x7d, x5	// Write all 1s to CSR
	csrrw x28, 0x7d, x0	// Write all 0s to CSR
	csrrs x28, 0x7d, x5	// Set all CSR bits
	csrrc x28, 0x7d, x5	// Clear all CSR bits
	csrrw x28, 0x7d, x21	// Restore CSR

// Testing CSR 0x7e
	csrr x7, 0x7e	// Read CSR
	li x24, -1
	csrrw x28, 0x7e, x24	// Write all 1s to CSR
	csrrw x28, 0x7e, x0	// Write all 0s to CSR
	csrrs x28, 0x7e, x24	// Set all CSR bits
	csrrc x28, 0x7e, x24	// Clear all CSR bits
	csrrw x28, 0x7e, x7	// Restore CSR

// Testing CSR 0x7f
	csrr x16, 0x7f	// Read CSR
	li x28, -1
	csrrw x17, 0x7f, x28	// Write all 1s to CSR
	csrrw x17, 0x7f, x0	// Write all 0s to CSR
	csrrs x17, 0x7f, x28	// Set all CSR bits
	csrrc x17, 0x7f, x28	// Clear all CSR bits
	csrrw x17, 0x7f, x16	// Restore CSR

// Testing CSR 0x80
	csrr x10, 0x80	// Read CSR
	li x22, -1
	csrrw x17, 0x80, x22	// Write all 1s to CSR
	csrrw x17, 0x80, x0	// Write all 0s to CSR
	csrrs x17, 0x80, x22	// Set all CSR bits
	csrrc x17, 0x80, x22	// Clear all CSR bits
	csrrw x17, 0x80, x10	// Restore CSR

// Testing CSR 0x81
	csrr x16, 0x81	// Read CSR
	li x30, -1
	csrrw x31, 0x81, x30	// Write all 1s to CSR
	csrrw x31, 0x81, x0	// Write all 0s to CSR
	csrrs x31, 0x81, x30	// Set all CSR bits
	csrrc x31, 0x81, x30	// Clear all CSR bits
	csrrw x31, 0x81, x16	// Restore CSR

// Testing CSR 0x82
	csrr x1, 0x82	// Read CSR
	li x16, -1
	csrrw x27, 0x82, x16	// Write all 1s to CSR
	csrrw x27, 0x82, x0	// Write all 0s to CSR
	csrrs x27, 0x82, x16	// Set all CSR bits
	csrrc x27, 0x82, x16	// Clear all CSR bits
	csrrw x27, 0x82, x1	// Restore CSR

// Testing CSR 0x83
	csrr x17, 0x83	// Read CSR
	li x13, -1
	csrrw x18, 0x83, x13	// Write all 1s to CSR
	csrrw x18, 0x83, x0	// Write all 0s to CSR
	csrrs x18, 0x83, x13	// Set all CSR bits
	csrrc x18, 0x83, x13	// Clear all CSR bits
	csrrw x18, 0x83, x17	// Restore CSR

// Testing CSR 0x84
	csrr x10, 0x84	// Read CSR
	li x14, -1
	csrrw x7, 0x84, x14	// Write all 1s to CSR
	csrrw x7, 0x84, x0	// Write all 0s to CSR
	csrrs x7, 0x84, x14	// Set all CSR bits
	csrrc x7, 0x84, x14	// Clear all CSR bits
	csrrw x7, 0x84, x10	// Restore CSR

// Testing CSR 0x85
	csrr x20, 0x85	// Read CSR
	li x10, -1
	csrrw x31, 0x85, x10	// Write all 1s to CSR
	csrrw x31, 0x85, x0	// Write all 0s to CSR
	csrrs x31, 0x85, x10	// Set all CSR bits
	csrrc x31, 0x85, x10	// Clear all CSR bits
	csrrw x31, 0x85, x20	// Restore CSR

// Testing CSR 0x86
	csrr x25, 0x86	// Read CSR
	li x20, -1
	csrrw x3, 0x86, x20	// Write all 1s to CSR
	csrrw x3, 0x86, x0	// Write all 0s to CSR
	csrrs x3, 0x86, x20	// Set all CSR bits
	csrrc x3, 0x86, x20	// Clear all CSR bits
	csrrw x3, 0x86, x25	// Restore CSR

// Testing CSR 0x87
	csrr x28, 0x87	// Read CSR
	li x13, -1
	csrrw x10, 0x87, x13	// Write all 1s to CSR
	csrrw x10, 0x87, x0	// Write all 0s to CSR
	csrrs x10, 0x87, x13	// Set all CSR bits
	csrrc x10, 0x87, x13	// Clear all CSR bits
	csrrw x10, 0x87, x28	// Restore CSR

// Testing CSR 0x88
	csrr x1, 0x88	// Read CSR
	li x27, -1
	csrrw x31, 0x88, x27	// Write all 1s to CSR
	csrrw x31, 0x88, x0	// Write all 0s to CSR
	csrrs x31, 0x88, x27	// Set all CSR bits
	csrrc x31, 0x88, x27	// Clear all CSR bits
	csrrw x31, 0x88, x1	// Restore CSR

// Testing CSR 0x89
	csrr x21, 0x89	// Read CSR
	li x2, -1
	csrrw x4, 0x89, x2	// Write all 1s to CSR
	csrrw x4, 0x89, x0	// Write all 0s to CSR
	csrrs x4, 0x89, x2	// Set all CSR bits
	csrrc x4, 0x89, x2	// Clear all CSR bits
	csrrw x4, 0x89, x21	// Restore CSR

// Testing CSR 0x8a
	csrr x1, 0x8a	// Read CSR
	li x4, -1
	csrrw x28, 0x8a, x4	// Write all 1s to CSR
	csrrw x28, 0x8a, x0	// Write all 0s to CSR
	csrrs x28, 0x8a, x4	// Set all CSR bits
	csrrc x28, 0x8a, x4	// Clear all CSR bits
	csrrw x28, 0x8a, x1	// Restore CSR

// Testing CSR 0x8b
	csrr x5, 0x8b	// Read CSR
	li x21, -1
	csrrw x1, 0x8b, x21	// Write all 1s to CSR
	csrrw x1, 0x8b, x0	// Write all 0s to CSR
	csrrs x1, 0x8b, x21	// Set all CSR bits
	csrrc x1, 0x8b, x21	// Clear all CSR bits
	csrrw x1, 0x8b, x5	// Restore CSR

// Testing CSR 0x8c
	csrr x10, 0x8c	// Read CSR
	li x6, -1
	csrrw x2, 0x8c, x6	// Write all 1s to CSR
	csrrw x2, 0x8c, x0	// Write all 0s to CSR
	csrrs x2, 0x8c, x6	// Set all CSR bits
	csrrc x2, 0x8c, x6	// Clear all CSR bits
	csrrw x2, 0x8c, x10	// Restore CSR

// Testing CSR 0x8d
	csrr x20, 0x8d	// Read CSR
	li x8, -1
	csrrw x17, 0x8d, x8	// Write all 1s to CSR
	csrrw x17, 0x8d, x0	// Write all 0s to CSR
	csrrs x17, 0x8d, x8	// Set all CSR bits
	csrrc x17, 0x8d, x8	// Clear all CSR bits
	csrrw x17, 0x8d, x20	// Restore CSR

// Testing CSR 0x8e
	csrr x24, 0x8e	// Read CSR
	li x5, -1
	csrrw x26, 0x8e, x5	// Write all 1s to CSR
	csrrw x26, 0x8e, x0	// Write all 0s to CSR
	csrrs x26, 0x8e, x5	// Set all CSR bits
	csrrc x26, 0x8e, x5	// Clear all CSR bits
	csrrw x26, 0x8e, x24	// Restore CSR

// Testing CSR 0x8f
	csrr x15, 0x8f	// Read CSR
	li x23, -1
	csrrw x29, 0x8f, x23	// Write all 1s to CSR
	csrrw x29, 0x8f, x0	// Write all 0s to CSR
	csrrs x29, 0x8f, x23	// Set all CSR bits
	csrrc x29, 0x8f, x23	// Clear all CSR bits
	csrrw x29, 0x8f, x15	// Restore CSR

// Testing CSR 0x90
	csrr x2, 0x90	// Read CSR
	li x26, -1
	csrrw x12, 0x90, x26	// Write all 1s to CSR
	csrrw x12, 0x90, x0	// Write all 0s to CSR
	csrrs x12, 0x90, x26	// Set all CSR bits
	csrrc x12, 0x90, x26	// Clear all CSR bits
	csrrw x12, 0x90, x2	// Restore CSR

// Testing CSR 0x91
	csrr x27, 0x91	// Read CSR
	li x25, -1
	csrrw x10, 0x91, x25	// Write all 1s to CSR
	csrrw x10, 0x91, x0	// Write all 0s to CSR
	csrrs x10, 0x91, x25	// Set all CSR bits
	csrrc x10, 0x91, x25	// Clear all CSR bits
	csrrw x10, 0x91, x27	// Restore CSR

// Testing CSR 0x92
	csrr x15, 0x92	// Read CSR
	li x8, -1
	csrrw x31, 0x92, x8	// Write all 1s to CSR
	csrrw x31, 0x92, x0	// Write all 0s to CSR
	csrrs x31, 0x92, x8	// Set all CSR bits
	csrrc x31, 0x92, x8	// Clear all CSR bits
	csrrw x31, 0x92, x15	// Restore CSR

// Testing CSR 0x93
	csrr x24, 0x93	// Read CSR
	li x17, -1
	csrrw x12, 0x93, x17	// Write all 1s to CSR
	csrrw x12, 0x93, x0	// Write all 0s to CSR
	csrrs x12, 0x93, x17	// Set all CSR bits
	csrrc x12, 0x93, x17	// Clear all CSR bits
	csrrw x12, 0x93, x24	// Restore CSR

// Testing CSR 0x94
	csrr x30, 0x94	// Read CSR
	li x29, -1
	csrrw x13, 0x94, x29	// Write all 1s to CSR
	csrrw x13, 0x94, x0	// Write all 0s to CSR
	csrrs x13, 0x94, x29	// Set all CSR bits
	csrrc x13, 0x94, x29	// Clear all CSR bits
	csrrw x13, 0x94, x30	// Restore CSR

// Testing CSR 0x95
	csrr x20, 0x95	// Read CSR
	li x11, -1
	csrrw x16, 0x95, x11	// Write all 1s to CSR
	csrrw x16, 0x95, x0	// Write all 0s to CSR
	csrrs x16, 0x95, x11	// Set all CSR bits
	csrrc x16, 0x95, x11	// Clear all CSR bits
	csrrw x16, 0x95, x20	// Restore CSR

// Testing CSR 0x96
	csrr x3, 0x96	// Read CSR
	li x21, -1
	csrrw x8, 0x96, x21	// Write all 1s to CSR
	csrrw x8, 0x96, x0	// Write all 0s to CSR
	csrrs x8, 0x96, x21	// Set all CSR bits
	csrrc x8, 0x96, x21	// Clear all CSR bits
	csrrw x8, 0x96, x3	// Restore CSR

// Testing CSR 0x97
	csrr x19, 0x97	// Read CSR
	li x31, -1
	csrrw x27, 0x97, x31	// Write all 1s to CSR
	csrrw x27, 0x97, x0	// Write all 0s to CSR
	csrrs x27, 0x97, x31	// Set all CSR bits
	csrrc x27, 0x97, x31	// Clear all CSR bits
	csrrw x27, 0x97, x19	// Restore CSR

// Testing CSR 0x98
	csrr x3, 0x98	// Read CSR
	li x15, -1
	csrrw x2, 0x98, x15	// Write all 1s to CSR
	csrrw x2, 0x98, x0	// Write all 0s to CSR
	csrrs x2, 0x98, x15	// Set all CSR bits
	csrrc x2, 0x98, x15	// Clear all CSR bits
	csrrw x2, 0x98, x3	// Restore CSR

// Testing CSR 0x99
	csrr x30, 0x99	// Read CSR
	li x7, -1
	csrrw x9, 0x99, x7	// Write all 1s to CSR
	csrrw x9, 0x99, x0	// Write all 0s to CSR
	csrrs x9, 0x99, x7	// Set all CSR bits
	csrrc x9, 0x99, x7	// Clear all CSR bits
	csrrw x9, 0x99, x30	// Restore CSR

// Testing CSR 0x9a
	csrr x23, 0x9a	// Read CSR
	li x4, -1
	csrrw x13, 0x9a, x4	// Write all 1s to CSR
	csrrw x13, 0x9a, x0	// Write all 0s to CSR
	csrrs x13, 0x9a, x4	// Set all CSR bits
	csrrc x13, 0x9a, x4	// Clear all CSR bits
	csrrw x13, 0x9a, x23	// Restore CSR

// Testing CSR 0x9b
	csrr x31, 0x9b	// Read CSR
	li x21, -1
	csrrw x12, 0x9b, x21	// Write all 1s to CSR
	csrrw x12, 0x9b, x0	// Write all 0s to CSR
	csrrs x12, 0x9b, x21	// Set all CSR bits
	csrrc x12, 0x9b, x21	// Clear all CSR bits
	csrrw x12, 0x9b, x31	// Restore CSR

// Testing CSR 0x9c
	csrr x31, 0x9c	// Read CSR
	li x17, -1
	csrrw x13, 0x9c, x17	// Write all 1s to CSR
	csrrw x13, 0x9c, x0	// Write all 0s to CSR
	csrrs x13, 0x9c, x17	// Set all CSR bits
	csrrc x13, 0x9c, x17	// Clear all CSR bits
	csrrw x13, 0x9c, x31	// Restore CSR

// Testing CSR 0x9d
	csrr x10, 0x9d	// Read CSR
	li x19, -1
	csrrw x12, 0x9d, x19	// Write all 1s to CSR
	csrrw x12, 0x9d, x0	// Write all 0s to CSR
	csrrs x12, 0x9d, x19	// Set all CSR bits
	csrrc x12, 0x9d, x19	// Clear all CSR bits
	csrrw x12, 0x9d, x10	// Restore CSR

// Testing CSR 0x9e
	csrr x4, 0x9e	// Read CSR
	li x20, -1
	csrrw x31, 0x9e, x20	// Write all 1s to CSR
	csrrw x31, 0x9e, x0	// Write all 0s to CSR
	csrrs x31, 0x9e, x20	// Set all CSR bits
	csrrc x31, 0x9e, x20	// Clear all CSR bits
	csrrw x31, 0x9e, x4	// Restore CSR

// Testing CSR 0x9f
	csrr x18, 0x9f	// Read CSR
	li x22, -1
	csrrw x17, 0x9f, x22	// Write all 1s to CSR
	csrrw x17, 0x9f, x0	// Write all 0s to CSR
	csrrs x17, 0x9f, x22	// Set all CSR bits
	csrrc x17, 0x9f, x22	// Clear all CSR bits
	csrrw x17, 0x9f, x18	// Restore CSR

// Testing CSR 0xa0
	csrr x30, 0xa0	// Read CSR
	li x19, -1
	csrrw x2, 0xa0, x19	// Write all 1s to CSR
	csrrw x2, 0xa0, x0	// Write all 0s to CSR
	csrrs x2, 0xa0, x19	// Set all CSR bits
	csrrc x2, 0xa0, x19	// Clear all CSR bits
	csrrw x2, 0xa0, x30	// Restore CSR

// Testing CSR 0xa1
	csrr x21, 0xa1	// Read CSR
	li x30, -1
	csrrw x3, 0xa1, x30	// Write all 1s to CSR
	csrrw x3, 0xa1, x0	// Write all 0s to CSR
	csrrs x3, 0xa1, x30	// Set all CSR bits
	csrrc x3, 0xa1, x30	// Clear all CSR bits
	csrrw x3, 0xa1, x21	// Restore CSR

// Testing CSR 0xa2
	csrr x22, 0xa2	// Read CSR
	li x27, -1
	csrrw x8, 0xa2, x27	// Write all 1s to CSR
	csrrw x8, 0xa2, x0	// Write all 0s to CSR
	csrrs x8, 0xa2, x27	// Set all CSR bits
	csrrc x8, 0xa2, x27	// Clear all CSR bits
	csrrw x8, 0xa2, x22	// Restore CSR

// Testing CSR 0xa3
	csrr x21, 0xa3	// Read CSR
	li x16, -1
	csrrw x1, 0xa3, x16	// Write all 1s to CSR
	csrrw x1, 0xa3, x0	// Write all 0s to CSR
	csrrs x1, 0xa3, x16	// Set all CSR bits
	csrrc x1, 0xa3, x16	// Clear all CSR bits
	csrrw x1, 0xa3, x21	// Restore CSR

// Testing CSR 0xa4
	csrr x14, 0xa4	// Read CSR
	li x20, -1
	csrrw x7, 0xa4, x20	// Write all 1s to CSR
	csrrw x7, 0xa4, x0	// Write all 0s to CSR
	csrrs x7, 0xa4, x20	// Set all CSR bits
	csrrc x7, 0xa4, x20	// Clear all CSR bits
	csrrw x7, 0xa4, x14	// Restore CSR

// Testing CSR 0xa5
	csrr x8, 0xa5	// Read CSR
	li x31, -1
	csrrw x21, 0xa5, x31	// Write all 1s to CSR
	csrrw x21, 0xa5, x0	// Write all 0s to CSR
	csrrs x21, 0xa5, x31	// Set all CSR bits
	csrrc x21, 0xa5, x31	// Clear all CSR bits
	csrrw x21, 0xa5, x8	// Restore CSR

// Testing CSR 0xa6
	csrr x9, 0xa6	// Read CSR
	li x12, -1
	csrrw x2, 0xa6, x12	// Write all 1s to CSR
	csrrw x2, 0xa6, x0	// Write all 0s to CSR
	csrrs x2, 0xa6, x12	// Set all CSR bits
	csrrc x2, 0xa6, x12	// Clear all CSR bits
	csrrw x2, 0xa6, x9	// Restore CSR

// Testing CSR 0xa7
	csrr x11, 0xa7	// Read CSR
	li x4, -1
	csrrw x1, 0xa7, x4	// Write all 1s to CSR
	csrrw x1, 0xa7, x0	// Write all 0s to CSR
	csrrs x1, 0xa7, x4	// Set all CSR bits
	csrrc x1, 0xa7, x4	// Clear all CSR bits
	csrrw x1, 0xa7, x11	// Restore CSR

// Testing CSR 0xa8
	csrr x12, 0xa8	// Read CSR
	li x22, -1
	csrrw x6, 0xa8, x22	// Write all 1s to CSR
	csrrw x6, 0xa8, x0	// Write all 0s to CSR
	csrrs x6, 0xa8, x22	// Set all CSR bits
	csrrc x6, 0xa8, x22	// Clear all CSR bits
	csrrw x6, 0xa8, x12	// Restore CSR

// Testing CSR 0xa9
	csrr x21, 0xa9	// Read CSR
	li x17, -1
	csrrw x30, 0xa9, x17	// Write all 1s to CSR
	csrrw x30, 0xa9, x0	// Write all 0s to CSR
	csrrs x30, 0xa9, x17	// Set all CSR bits
	csrrc x30, 0xa9, x17	// Clear all CSR bits
	csrrw x30, 0xa9, x21	// Restore CSR

// Testing CSR 0xaa
	csrr x7, 0xaa	// Read CSR
	li x11, -1
	csrrw x31, 0xaa, x11	// Write all 1s to CSR
	csrrw x31, 0xaa, x0	// Write all 0s to CSR
	csrrs x31, 0xaa, x11	// Set all CSR bits
	csrrc x31, 0xaa, x11	// Clear all CSR bits
	csrrw x31, 0xaa, x7	// Restore CSR

// Testing CSR 0xab
	csrr x10, 0xab	// Read CSR
	li x2, -1
	csrrw x8, 0xab, x2	// Write all 1s to CSR
	csrrw x8, 0xab, x0	// Write all 0s to CSR
	csrrs x8, 0xab, x2	// Set all CSR bits
	csrrc x8, 0xab, x2	// Clear all CSR bits
	csrrw x8, 0xab, x10	// Restore CSR

// Testing CSR 0xac
	csrr x17, 0xac	// Read CSR
	li x8, -1
	csrrw x24, 0xac, x8	// Write all 1s to CSR
	csrrw x24, 0xac, x0	// Write all 0s to CSR
	csrrs x24, 0xac, x8	// Set all CSR bits
	csrrc x24, 0xac, x8	// Clear all CSR bits
	csrrw x24, 0xac, x17	// Restore CSR

// Testing CSR 0xad
	csrr x30, 0xad	// Read CSR
	li x29, -1
	csrrw x22, 0xad, x29	// Write all 1s to CSR
	csrrw x22, 0xad, x0	// Write all 0s to CSR
	csrrs x22, 0xad, x29	// Set all CSR bits
	csrrc x22, 0xad, x29	// Clear all CSR bits
	csrrw x22, 0xad, x30	// Restore CSR

// Testing CSR 0xae
	csrr x20, 0xae	// Read CSR
	li x29, -1
	csrrw x16, 0xae, x29	// Write all 1s to CSR
	csrrw x16, 0xae, x0	// Write all 0s to CSR
	csrrs x16, 0xae, x29	// Set all CSR bits
	csrrc x16, 0xae, x29	// Clear all CSR bits
	csrrw x16, 0xae, x20	// Restore CSR

// Testing CSR 0xaf
	csrr x4, 0xaf	// Read CSR
	li x18, -1
	csrrw x15, 0xaf, x18	// Write all 1s to CSR
	csrrw x15, 0xaf, x0	// Write all 0s to CSR
	csrrs x15, 0xaf, x18	// Set all CSR bits
	csrrc x15, 0xaf, x18	// Clear all CSR bits
	csrrw x15, 0xaf, x4	// Restore CSR

// Testing CSR 0xb0
	csrr x9, 0xb0	// Read CSR
	li x24, -1
	csrrw x21, 0xb0, x24	// Write all 1s to CSR
	csrrw x21, 0xb0, x0	// Write all 0s to CSR
	csrrs x21, 0xb0, x24	// Set all CSR bits
	csrrc x21, 0xb0, x24	// Clear all CSR bits
	csrrw x21, 0xb0, x9	// Restore CSR

// Testing CSR 0xb1
	csrr x18, 0xb1	// Read CSR
	li x21, -1
	csrrw x11, 0xb1, x21	// Write all 1s to CSR
	csrrw x11, 0xb1, x0	// Write all 0s to CSR
	csrrs x11, 0xb1, x21	// Set all CSR bits
	csrrc x11, 0xb1, x21	// Clear all CSR bits
	csrrw x11, 0xb1, x18	// Restore CSR

// Testing CSR 0xb2
	csrr x9, 0xb2	// Read CSR
	li x28, -1
	csrrw x19, 0xb2, x28	// Write all 1s to CSR
	csrrw x19, 0xb2, x0	// Write all 0s to CSR
	csrrs x19, 0xb2, x28	// Set all CSR bits
	csrrc x19, 0xb2, x28	// Clear all CSR bits
	csrrw x19, 0xb2, x9	// Restore CSR

// Testing CSR 0xb3
	csrr x20, 0xb3	// Read CSR
	li x8, -1
	csrrw x2, 0xb3, x8	// Write all 1s to CSR
	csrrw x2, 0xb3, x0	// Write all 0s to CSR
	csrrs x2, 0xb3, x8	// Set all CSR bits
	csrrc x2, 0xb3, x8	// Clear all CSR bits
	csrrw x2, 0xb3, x20	// Restore CSR

// Testing CSR 0xb4
	csrr x30, 0xb4	// Read CSR
	li x15, -1
	csrrw x25, 0xb4, x15	// Write all 1s to CSR
	csrrw x25, 0xb4, x0	// Write all 0s to CSR
	csrrs x25, 0xb4, x15	// Set all CSR bits
	csrrc x25, 0xb4, x15	// Clear all CSR bits
	csrrw x25, 0xb4, x30	// Restore CSR

// Testing CSR 0xb5
	csrr x26, 0xb5	// Read CSR
	li x11, -1
	csrrw x14, 0xb5, x11	// Write all 1s to CSR
	csrrw x14, 0xb5, x0	// Write all 0s to CSR
	csrrs x14, 0xb5, x11	// Set all CSR bits
	csrrc x14, 0xb5, x11	// Clear all CSR bits
	csrrw x14, 0xb5, x26	// Restore CSR

// Testing CSR 0xb6
	csrr x15, 0xb6	// Read CSR
	li x6, -1
	csrrw x30, 0xb6, x6	// Write all 1s to CSR
	csrrw x30, 0xb6, x0	// Write all 0s to CSR
	csrrs x30, 0xb6, x6	// Set all CSR bits
	csrrc x30, 0xb6, x6	// Clear all CSR bits
	csrrw x30, 0xb6, x15	// Restore CSR

// Testing CSR 0xb7
	csrr x16, 0xb7	// Read CSR
	li x26, -1
	csrrw x18, 0xb7, x26	// Write all 1s to CSR
	csrrw x18, 0xb7, x0	// Write all 0s to CSR
	csrrs x18, 0xb7, x26	// Set all CSR bits
	csrrc x18, 0xb7, x26	// Clear all CSR bits
	csrrw x18, 0xb7, x16	// Restore CSR

// Testing CSR 0xb8
	csrr x4, 0xb8	// Read CSR
	li x8, -1
	csrrw x11, 0xb8, x8	// Write all 1s to CSR
	csrrw x11, 0xb8, x0	// Write all 0s to CSR
	csrrs x11, 0xb8, x8	// Set all CSR bits
	csrrc x11, 0xb8, x8	// Clear all CSR bits
	csrrw x11, 0xb8, x4	// Restore CSR

// Testing CSR 0xb9
	csrr x21, 0xb9	// Read CSR
	li x28, -1
	csrrw x17, 0xb9, x28	// Write all 1s to CSR
	csrrw x17, 0xb9, x0	// Write all 0s to CSR
	csrrs x17, 0xb9, x28	// Set all CSR bits
	csrrc x17, 0xb9, x28	// Clear all CSR bits
	csrrw x17, 0xb9, x21	// Restore CSR

// Testing CSR 0xba
	csrr x11, 0xba	// Read CSR
	li x31, -1
	csrrw x12, 0xba, x31	// Write all 1s to CSR
	csrrw x12, 0xba, x0	// Write all 0s to CSR
	csrrs x12, 0xba, x31	// Set all CSR bits
	csrrc x12, 0xba, x31	// Clear all CSR bits
	csrrw x12, 0xba, x11	// Restore CSR

// Testing CSR 0xbb
	csrr x23, 0xbb	// Read CSR
	li x28, -1
	csrrw x19, 0xbb, x28	// Write all 1s to CSR
	csrrw x19, 0xbb, x0	// Write all 0s to CSR
	csrrs x19, 0xbb, x28	// Set all CSR bits
	csrrc x19, 0xbb, x28	// Clear all CSR bits
	csrrw x19, 0xbb, x23	// Restore CSR

// Testing CSR 0xbc
	csrr x29, 0xbc	// Read CSR
	li x14, -1
	csrrw x26, 0xbc, x14	// Write all 1s to CSR
	csrrw x26, 0xbc, x0	// Write all 0s to CSR
	csrrs x26, 0xbc, x14	// Set all CSR bits
	csrrc x26, 0xbc, x14	// Clear all CSR bits
	csrrw x26, 0xbc, x29	// Restore CSR

// Testing CSR 0xbd
	csrr x11, 0xbd	// Read CSR
	li x23, -1
	csrrw x30, 0xbd, x23	// Write all 1s to CSR
	csrrw x30, 0xbd, x0	// Write all 0s to CSR
	csrrs x30, 0xbd, x23	// Set all CSR bits
	csrrc x30, 0xbd, x23	// Clear all CSR bits
	csrrw x30, 0xbd, x11	// Restore CSR

// Testing CSR 0xbe
	csrr x18, 0xbe	// Read CSR
	li x17, -1
	csrrw x3, 0xbe, x17	// Write all 1s to CSR
	csrrw x3, 0xbe, x0	// Write all 0s to CSR
	csrrs x3, 0xbe, x17	// Set all CSR bits
	csrrc x3, 0xbe, x17	// Clear all CSR bits
	csrrw x3, 0xbe, x18	// Restore CSR

// Testing CSR 0xbf
	csrr x18, 0xbf	// Read CSR
	li x10, -1
	csrrw x5, 0xbf, x10	// Write all 1s to CSR
	csrrw x5, 0xbf, x0	// Write all 0s to CSR
	csrrs x5, 0xbf, x10	// Set all CSR bits
	csrrc x5, 0xbf, x10	// Clear all CSR bits
	csrrw x5, 0xbf, x18	// Restore CSR

// Testing CSR 0xc0
	csrr x28, 0xc0	// Read CSR
	li x12, -1
	csrrw x24, 0xc0, x12	// Write all 1s to CSR
	csrrw x24, 0xc0, x0	// Write all 0s to CSR
	csrrs x24, 0xc0, x12	// Set all CSR bits
	csrrc x24, 0xc0, x12	// Clear all CSR bits
	csrrw x24, 0xc0, x28	// Restore CSR

// Testing CSR 0xc1
	csrr x8, 0xc1	// Read CSR
	li x25, -1
	csrrw x16, 0xc1, x25	// Write all 1s to CSR
	csrrw x16, 0xc1, x0	// Write all 0s to CSR
	csrrs x16, 0xc1, x25	// Set all CSR bits
	csrrc x16, 0xc1, x25	// Clear all CSR bits
	csrrw x16, 0xc1, x8	// Restore CSR

// Testing CSR 0xc2
	csrr x21, 0xc2	// Read CSR
	li x5, -1
	csrrw x28, 0xc2, x5	// Write all 1s to CSR
	csrrw x28, 0xc2, x0	// Write all 0s to CSR
	csrrs x28, 0xc2, x5	// Set all CSR bits
	csrrc x28, 0xc2, x5	// Clear all CSR bits
	csrrw x28, 0xc2, x21	// Restore CSR

// Testing CSR 0xc3
	csrr x9, 0xc3	// Read CSR
	li x3, -1
	csrrw x5, 0xc3, x3	// Write all 1s to CSR
	csrrw x5, 0xc3, x0	// Write all 0s to CSR
	csrrs x5, 0xc3, x3	// Set all CSR bits
	csrrc x5, 0xc3, x3	// Clear all CSR bits
	csrrw x5, 0xc3, x9	// Restore CSR

// Testing CSR 0xc4
	csrr x28, 0xc4	// Read CSR
	li x11, -1
	csrrw x3, 0xc4, x11	// Write all 1s to CSR
	csrrw x3, 0xc4, x0	// Write all 0s to CSR
	csrrs x3, 0xc4, x11	// Set all CSR bits
	csrrc x3, 0xc4, x11	// Clear all CSR bits
	csrrw x3, 0xc4, x28	// Restore CSR

// Testing CSR 0xc5
	csrr x9, 0xc5	// Read CSR
	li x26, -1
	csrrw x18, 0xc5, x26	// Write all 1s to CSR
	csrrw x18, 0xc5, x0	// Write all 0s to CSR
	csrrs x18, 0xc5, x26	// Set all CSR bits
	csrrc x18, 0xc5, x26	// Clear all CSR bits
	csrrw x18, 0xc5, x9	// Restore CSR

// Testing CSR 0xc6
	csrr x2, 0xc6	// Read CSR
	li x1, -1
	csrrw x15, 0xc6, x1	// Write all 1s to CSR
	csrrw x15, 0xc6, x0	// Write all 0s to CSR
	csrrs x15, 0xc6, x1	// Set all CSR bits
	csrrc x15, 0xc6, x1	// Clear all CSR bits
	csrrw x15, 0xc6, x2	// Restore CSR

// Testing CSR 0xc7
	csrr x3, 0xc7	// Read CSR
	li x26, -1
	csrrw x31, 0xc7, x26	// Write all 1s to CSR
	csrrw x31, 0xc7, x0	// Write all 0s to CSR
	csrrs x31, 0xc7, x26	// Set all CSR bits
	csrrc x31, 0xc7, x26	// Clear all CSR bits
	csrrw x31, 0xc7, x3	// Restore CSR

// Testing CSR 0xc8
	csrr x2, 0xc8	// Read CSR
	li x12, -1
	csrrw x21, 0xc8, x12	// Write all 1s to CSR
	csrrw x21, 0xc8, x0	// Write all 0s to CSR
	csrrs x21, 0xc8, x12	// Set all CSR bits
	csrrc x21, 0xc8, x12	// Clear all CSR bits
	csrrw x21, 0xc8, x2	// Restore CSR

// Testing CSR 0xc9
	csrr x18, 0xc9	// Read CSR
	li x2, -1
	csrrw x26, 0xc9, x2	// Write all 1s to CSR
	csrrw x26, 0xc9, x0	// Write all 0s to CSR
	csrrs x26, 0xc9, x2	// Set all CSR bits
	csrrc x26, 0xc9, x2	// Clear all CSR bits
	csrrw x26, 0xc9, x18	// Restore CSR

// Testing CSR 0xca
	csrr x26, 0xca	// Read CSR
	li x14, -1
	csrrw x23, 0xca, x14	// Write all 1s to CSR
	csrrw x23, 0xca, x0	// Write all 0s to CSR
	csrrs x23, 0xca, x14	// Set all CSR bits
	csrrc x23, 0xca, x14	// Clear all CSR bits
	csrrw x23, 0xca, x26	// Restore CSR

// Testing CSR 0xcb
	csrr x18, 0xcb	// Read CSR
	li x17, -1
	csrrw x9, 0xcb, x17	// Write all 1s to CSR
	csrrw x9, 0xcb, x0	// Write all 0s to CSR
	csrrs x9, 0xcb, x17	// Set all CSR bits
	csrrc x9, 0xcb, x17	// Clear all CSR bits
	csrrw x9, 0xcb, x18	// Restore CSR

// Testing CSR 0xcc
	csrr x1, 0xcc	// Read CSR
	li x16, -1
	csrrw x8, 0xcc, x16	// Write all 1s to CSR
	csrrw x8, 0xcc, x0	// Write all 0s to CSR
	csrrs x8, 0xcc, x16	// Set all CSR bits
	csrrc x8, 0xcc, x16	// Clear all CSR bits
	csrrw x8, 0xcc, x1	// Restore CSR

// Testing CSR 0xcd
	csrr x22, 0xcd	// Read CSR
	li x3, -1
	csrrw x28, 0xcd, x3	// Write all 1s to CSR
	csrrw x28, 0xcd, x0	// Write all 0s to CSR
	csrrs x28, 0xcd, x3	// Set all CSR bits
	csrrc x28, 0xcd, x3	// Clear all CSR bits
	csrrw x28, 0xcd, x22	// Restore CSR

// Testing CSR 0xce
	csrr x21, 0xce	// Read CSR
	li x31, -1
	csrrw x18, 0xce, x31	// Write all 1s to CSR
	csrrw x18, 0xce, x0	// Write all 0s to CSR
	csrrs x18, 0xce, x31	// Set all CSR bits
	csrrc x18, 0xce, x31	// Clear all CSR bits
	csrrw x18, 0xce, x21	// Restore CSR

// Testing CSR 0xcf
	csrr x22, 0xcf	// Read CSR
	li x1, -1
	csrrw x7, 0xcf, x1	// Write all 1s to CSR
	csrrw x7, 0xcf, x0	// Write all 0s to CSR
	csrrs x7, 0xcf, x1	// Set all CSR bits
	csrrc x7, 0xcf, x1	// Clear all CSR bits
	csrrw x7, 0xcf, x22	// Restore CSR

// Testing CSR 0xd0
	csrr x11, 0xd0	// Read CSR
	li x30, -1
	csrrw x12, 0xd0, x30	// Write all 1s to CSR
	csrrw x12, 0xd0, x0	// Write all 0s to CSR
	csrrs x12, 0xd0, x30	// Set all CSR bits
	csrrc x12, 0xd0, x30	// Clear all CSR bits
	csrrw x12, 0xd0, x11	// Restore CSR

// Testing CSR 0xd1
	csrr x24, 0xd1	// Read CSR
	li x6, -1
	csrrw x23, 0xd1, x6	// Write all 1s to CSR
	csrrw x23, 0xd1, x0	// Write all 0s to CSR
	csrrs x23, 0xd1, x6	// Set all CSR bits
	csrrc x23, 0xd1, x6	// Clear all CSR bits
	csrrw x23, 0xd1, x24	// Restore CSR

// Testing CSR 0xd2
	csrr x17, 0xd2	// Read CSR
	li x5, -1
	csrrw x24, 0xd2, x5	// Write all 1s to CSR
	csrrw x24, 0xd2, x0	// Write all 0s to CSR
	csrrs x24, 0xd2, x5	// Set all CSR bits
	csrrc x24, 0xd2, x5	// Clear all CSR bits
	csrrw x24, 0xd2, x17	// Restore CSR

// Testing CSR 0xd3
	csrr x21, 0xd3	// Read CSR
	li x30, -1
	csrrw x13, 0xd3, x30	// Write all 1s to CSR
	csrrw x13, 0xd3, x0	// Write all 0s to CSR
	csrrs x13, 0xd3, x30	// Set all CSR bits
	csrrc x13, 0xd3, x30	// Clear all CSR bits
	csrrw x13, 0xd3, x21	// Restore CSR

// Testing CSR 0xd4
	csrr x27, 0xd4	// Read CSR
	li x2, -1
	csrrw x15, 0xd4, x2	// Write all 1s to CSR
	csrrw x15, 0xd4, x0	// Write all 0s to CSR
	csrrs x15, 0xd4, x2	// Set all CSR bits
	csrrc x15, 0xd4, x2	// Clear all CSR bits
	csrrw x15, 0xd4, x27	// Restore CSR

// Testing CSR 0xd5
	csrr x4, 0xd5	// Read CSR
	li x30, -1
	csrrw x19, 0xd5, x30	// Write all 1s to CSR
	csrrw x19, 0xd5, x0	// Write all 0s to CSR
	csrrs x19, 0xd5, x30	// Set all CSR bits
	csrrc x19, 0xd5, x30	// Clear all CSR bits
	csrrw x19, 0xd5, x4	// Restore CSR

// Testing CSR 0xd6
	csrr x15, 0xd6	// Read CSR
	li x20, -1
	csrrw x11, 0xd6, x20	// Write all 1s to CSR
	csrrw x11, 0xd6, x0	// Write all 0s to CSR
	csrrs x11, 0xd6, x20	// Set all CSR bits
	csrrc x11, 0xd6, x20	// Clear all CSR bits
	csrrw x11, 0xd6, x15	// Restore CSR

// Testing CSR 0xd7
	csrr x31, 0xd7	// Read CSR
	li x29, -1
	csrrw x2, 0xd7, x29	// Write all 1s to CSR
	csrrw x2, 0xd7, x0	// Write all 0s to CSR
	csrrs x2, 0xd7, x29	// Set all CSR bits
	csrrc x2, 0xd7, x29	// Clear all CSR bits
	csrrw x2, 0xd7, x31	// Restore CSR

// Testing CSR 0xd8
	csrr x6, 0xd8	// Read CSR
	li x24, -1
	csrrw x10, 0xd8, x24	// Write all 1s to CSR
	csrrw x10, 0xd8, x0	// Write all 0s to CSR
	csrrs x10, 0xd8, x24	// Set all CSR bits
	csrrc x10, 0xd8, x24	// Clear all CSR bits
	csrrw x10, 0xd8, x6	// Restore CSR

// Testing CSR 0xd9
	csrr x6, 0xd9	// Read CSR
	li x17, -1
	csrrw x7, 0xd9, x17	// Write all 1s to CSR
	csrrw x7, 0xd9, x0	// Write all 0s to CSR
	csrrs x7, 0xd9, x17	// Set all CSR bits
	csrrc x7, 0xd9, x17	// Clear all CSR bits
	csrrw x7, 0xd9, x6	// Restore CSR

// Testing CSR 0xda
	csrr x8, 0xda	// Read CSR
	li x20, -1
	csrrw x13, 0xda, x20	// Write all 1s to CSR
	csrrw x13, 0xda, x0	// Write all 0s to CSR
	csrrs x13, 0xda, x20	// Set all CSR bits
	csrrc x13, 0xda, x20	// Clear all CSR bits
	csrrw x13, 0xda, x8	// Restore CSR

// Testing CSR 0xdb
	csrr x5, 0xdb	// Read CSR
	li x7, -1
	csrrw x11, 0xdb, x7	// Write all 1s to CSR
	csrrw x11, 0xdb, x0	// Write all 0s to CSR
	csrrs x11, 0xdb, x7	// Set all CSR bits
	csrrc x11, 0xdb, x7	// Clear all CSR bits
	csrrw x11, 0xdb, x5	// Restore CSR

// Testing CSR 0xdc
	csrr x30, 0xdc	// Read CSR
	li x20, -1
	csrrw x15, 0xdc, x20	// Write all 1s to CSR
	csrrw x15, 0xdc, x0	// Write all 0s to CSR
	csrrs x15, 0xdc, x20	// Set all CSR bits
	csrrc x15, 0xdc, x20	// Clear all CSR bits
	csrrw x15, 0xdc, x30	// Restore CSR

// Testing CSR 0xdd
	csrr x27, 0xdd	// Read CSR
	li x29, -1
	csrrw x2, 0xdd, x29	// Write all 1s to CSR
	csrrw x2, 0xdd, x0	// Write all 0s to CSR
	csrrs x2, 0xdd, x29	// Set all CSR bits
	csrrc x2, 0xdd, x29	// Clear all CSR bits
	csrrw x2, 0xdd, x27	// Restore CSR

// Testing CSR 0xde
	csrr x6, 0xde	// Read CSR
	li x31, -1
	csrrw x12, 0xde, x31	// Write all 1s to CSR
	csrrw x12, 0xde, x0	// Write all 0s to CSR
	csrrs x12, 0xde, x31	// Set all CSR bits
	csrrc x12, 0xde, x31	// Clear all CSR bits
	csrrw x12, 0xde, x6	// Restore CSR

// Testing CSR 0xdf
	csrr x14, 0xdf	// Read CSR
	li x31, -1
	csrrw x4, 0xdf, x31	// Write all 1s to CSR
	csrrw x4, 0xdf, x0	// Write all 0s to CSR
	csrrs x4, 0xdf, x31	// Set all CSR bits
	csrrc x4, 0xdf, x31	// Clear all CSR bits
	csrrw x4, 0xdf, x14	// Restore CSR

// Testing CSR 0xe0
	csrr x22, 0xe0	// Read CSR
	li x4, -1
	csrrw x19, 0xe0, x4	// Write all 1s to CSR
	csrrw x19, 0xe0, x0	// Write all 0s to CSR
	csrrs x19, 0xe0, x4	// Set all CSR bits
	csrrc x19, 0xe0, x4	// Clear all CSR bits
	csrrw x19, 0xe0, x22	// Restore CSR

// Testing CSR 0xe1
	csrr x14, 0xe1	// Read CSR
	li x12, -1
	csrrw x15, 0xe1, x12	// Write all 1s to CSR
	csrrw x15, 0xe1, x0	// Write all 0s to CSR
	csrrs x15, 0xe1, x12	// Set all CSR bits
	csrrc x15, 0xe1, x12	// Clear all CSR bits
	csrrw x15, 0xe1, x14	// Restore CSR

// Testing CSR 0xe2
	csrr x29, 0xe2	// Read CSR
	li x18, -1
	csrrw x20, 0xe2, x18	// Write all 1s to CSR
	csrrw x20, 0xe2, x0	// Write all 0s to CSR
	csrrs x20, 0xe2, x18	// Set all CSR bits
	csrrc x20, 0xe2, x18	// Clear all CSR bits
	csrrw x20, 0xe2, x29	// Restore CSR

// Testing CSR 0xe3
	csrr x11, 0xe3	// Read CSR
	li x4, -1
	csrrw x12, 0xe3, x4	// Write all 1s to CSR
	csrrw x12, 0xe3, x0	// Write all 0s to CSR
	csrrs x12, 0xe3, x4	// Set all CSR bits
	csrrc x12, 0xe3, x4	// Clear all CSR bits
	csrrw x12, 0xe3, x11	// Restore CSR

// Testing CSR 0xe4
	csrr x21, 0xe4	// Read CSR
	li x2, -1
	csrrw x25, 0xe4, x2	// Write all 1s to CSR
	csrrw x25, 0xe4, x0	// Write all 0s to CSR
	csrrs x25, 0xe4, x2	// Set all CSR bits
	csrrc x25, 0xe4, x2	// Clear all CSR bits
	csrrw x25, 0xe4, x21	// Restore CSR

// Testing CSR 0xe5
	csrr x15, 0xe5	// Read CSR
	li x27, -1
	csrrw x12, 0xe5, x27	// Write all 1s to CSR
	csrrw x12, 0xe5, x0	// Write all 0s to CSR
	csrrs x12, 0xe5, x27	// Set all CSR bits
	csrrc x12, 0xe5, x27	// Clear all CSR bits
	csrrw x12, 0xe5, x15	// Restore CSR

// Testing CSR 0xe6
	csrr x10, 0xe6	// Read CSR
	li x21, -1
	csrrw x17, 0xe6, x21	// Write all 1s to CSR
	csrrw x17, 0xe6, x0	// Write all 0s to CSR
	csrrs x17, 0xe6, x21	// Set all CSR bits
	csrrc x17, 0xe6, x21	// Clear all CSR bits
	csrrw x17, 0xe6, x10	// Restore CSR

// Testing CSR 0xe7
	csrr x5, 0xe7	// Read CSR
	li x27, -1
	csrrw x10, 0xe7, x27	// Write all 1s to CSR
	csrrw x10, 0xe7, x0	// Write all 0s to CSR
	csrrs x10, 0xe7, x27	// Set all CSR bits
	csrrc x10, 0xe7, x27	// Clear all CSR bits
	csrrw x10, 0xe7, x5	// Restore CSR

// Testing CSR 0xe8
	csrr x22, 0xe8	// Read CSR
	li x4, -1
	csrrw x14, 0xe8, x4	// Write all 1s to CSR
	csrrw x14, 0xe8, x0	// Write all 0s to CSR
	csrrs x14, 0xe8, x4	// Set all CSR bits
	csrrc x14, 0xe8, x4	// Clear all CSR bits
	csrrw x14, 0xe8, x22	// Restore CSR

// Testing CSR 0xe9
	csrr x4, 0xe9	// Read CSR
	li x24, -1
	csrrw x17, 0xe9, x24	// Write all 1s to CSR
	csrrw x17, 0xe9, x0	// Write all 0s to CSR
	csrrs x17, 0xe9, x24	// Set all CSR bits
	csrrc x17, 0xe9, x24	// Clear all CSR bits
	csrrw x17, 0xe9, x4	// Restore CSR

// Testing CSR 0xea
	csrr x3, 0xea	// Read CSR
	li x19, -1
	csrrw x27, 0xea, x19	// Write all 1s to CSR
	csrrw x27, 0xea, x0	// Write all 0s to CSR
	csrrs x27, 0xea, x19	// Set all CSR bits
	csrrc x27, 0xea, x19	// Clear all CSR bits
	csrrw x27, 0xea, x3	// Restore CSR

// Testing CSR 0xeb
	csrr x6, 0xeb	// Read CSR
	li x4, -1
	csrrw x22, 0xeb, x4	// Write all 1s to CSR
	csrrw x22, 0xeb, x0	// Write all 0s to CSR
	csrrs x22, 0xeb, x4	// Set all CSR bits
	csrrc x22, 0xeb, x4	// Clear all CSR bits
	csrrw x22, 0xeb, x6	// Restore CSR

// Testing CSR 0xec
	csrr x23, 0xec	// Read CSR
	li x29, -1
	csrrw x31, 0xec, x29	// Write all 1s to CSR
	csrrw x31, 0xec, x0	// Write all 0s to CSR
	csrrs x31, 0xec, x29	// Set all CSR bits
	csrrc x31, 0xec, x29	// Clear all CSR bits
	csrrw x31, 0xec, x23	// Restore CSR

// Testing CSR 0xed
	csrr x23, 0xed	// Read CSR
	li x25, -1
	csrrw x7, 0xed, x25	// Write all 1s to CSR
	csrrw x7, 0xed, x0	// Write all 0s to CSR
	csrrs x7, 0xed, x25	// Set all CSR bits
	csrrc x7, 0xed, x25	// Clear all CSR bits
	csrrw x7, 0xed, x23	// Restore CSR

// Testing CSR 0xee
	csrr x6, 0xee	// Read CSR
	li x21, -1
	csrrw x8, 0xee, x21	// Write all 1s to CSR
	csrrw x8, 0xee, x0	// Write all 0s to CSR
	csrrs x8, 0xee, x21	// Set all CSR bits
	csrrc x8, 0xee, x21	// Clear all CSR bits
	csrrw x8, 0xee, x6	// Restore CSR

// Testing CSR 0xef
	csrr x31, 0xef	// Read CSR
	li x19, -1
	csrrw x14, 0xef, x19	// Write all 1s to CSR
	csrrw x14, 0xef, x0	// Write all 0s to CSR
	csrrs x14, 0xef, x19	// Set all CSR bits
	csrrc x14, 0xef, x19	// Clear all CSR bits
	csrrw x14, 0xef, x31	// Restore CSR

// Testing CSR 0xf0
	csrr x10, 0xf0	// Read CSR
	li x30, -1
	csrrw x24, 0xf0, x30	// Write all 1s to CSR
	csrrw x24, 0xf0, x0	// Write all 0s to CSR
	csrrs x24, 0xf0, x30	// Set all CSR bits
	csrrc x24, 0xf0, x30	// Clear all CSR bits
	csrrw x24, 0xf0, x10	// Restore CSR

// Testing CSR 0xf1
	csrr x1, 0xf1	// Read CSR
	li x15, -1
	csrrw x2, 0xf1, x15	// Write all 1s to CSR
	csrrw x2, 0xf1, x0	// Write all 0s to CSR
	csrrs x2, 0xf1, x15	// Set all CSR bits
	csrrc x2, 0xf1, x15	// Clear all CSR bits
	csrrw x2, 0xf1, x1	// Restore CSR

// Testing CSR 0xf2
	csrr x7, 0xf2	// Read CSR
	li x15, -1
	csrrw x1, 0xf2, x15	// Write all 1s to CSR
	csrrw x1, 0xf2, x0	// Write all 0s to CSR
	csrrs x1, 0xf2, x15	// Set all CSR bits
	csrrc x1, 0xf2, x15	// Clear all CSR bits
	csrrw x1, 0xf2, x7	// Restore CSR

// Testing CSR 0xf3
	csrr x3, 0xf3	// Read CSR
	li x5, -1
	csrrw x28, 0xf3, x5	// Write all 1s to CSR
	csrrw x28, 0xf3, x0	// Write all 0s to CSR
	csrrs x28, 0xf3, x5	// Set all CSR bits
	csrrc x28, 0xf3, x5	// Clear all CSR bits
	csrrw x28, 0xf3, x3	// Restore CSR

// Testing CSR 0xf4
	csrr x17, 0xf4	// Read CSR
	li x12, -1
	csrrw x19, 0xf4, x12	// Write all 1s to CSR
	csrrw x19, 0xf4, x0	// Write all 0s to CSR
	csrrs x19, 0xf4, x12	// Set all CSR bits
	csrrc x19, 0xf4, x12	// Clear all CSR bits
	csrrw x19, 0xf4, x17	// Restore CSR

// Testing CSR 0xf5
	csrr x14, 0xf5	// Read CSR
	li x27, -1
	csrrw x9, 0xf5, x27	// Write all 1s to CSR
	csrrw x9, 0xf5, x0	// Write all 0s to CSR
	csrrs x9, 0xf5, x27	// Set all CSR bits
	csrrc x9, 0xf5, x27	// Clear all CSR bits
	csrrw x9, 0xf5, x14	// Restore CSR

// Testing CSR 0xf6
	csrr x6, 0xf6	// Read CSR
	li x20, -1
	csrrw x17, 0xf6, x20	// Write all 1s to CSR
	csrrw x17, 0xf6, x0	// Write all 0s to CSR
	csrrs x17, 0xf6, x20	// Set all CSR bits
	csrrc x17, 0xf6, x20	// Clear all CSR bits
	csrrw x17, 0xf6, x6	// Restore CSR

// Testing CSR 0xf7
	csrr x2, 0xf7	// Read CSR
	li x25, -1
	csrrw x11, 0xf7, x25	// Write all 1s to CSR
	csrrw x11, 0xf7, x0	// Write all 0s to CSR
	csrrs x11, 0xf7, x25	// Set all CSR bits
	csrrc x11, 0xf7, x25	// Clear all CSR bits
	csrrw x11, 0xf7, x2	// Restore CSR

// Testing CSR 0xf8
	csrr x11, 0xf8	// Read CSR
	li x10, -1
	csrrw x30, 0xf8, x10	// Write all 1s to CSR
	csrrw x30, 0xf8, x0	// Write all 0s to CSR
	csrrs x30, 0xf8, x10	// Set all CSR bits
	csrrc x30, 0xf8, x10	// Clear all CSR bits
	csrrw x30, 0xf8, x11	// Restore CSR

// Testing CSR 0xf9
	csrr x5, 0xf9	// Read CSR
	li x25, -1
	csrrw x10, 0xf9, x25	// Write all 1s to CSR
	csrrw x10, 0xf9, x0	// Write all 0s to CSR
	csrrs x10, 0xf9, x25	// Set all CSR bits
	csrrc x10, 0xf9, x25	// Clear all CSR bits
	csrrw x10, 0xf9, x5	// Restore CSR

// Testing CSR 0xfa
	csrr x25, 0xfa	// Read CSR
	li x6, -1
	csrrw x22, 0xfa, x6	// Write all 1s to CSR
	csrrw x22, 0xfa, x0	// Write all 0s to CSR
	csrrs x22, 0xfa, x6	// Set all CSR bits
	csrrc x22, 0xfa, x6	// Clear all CSR bits
	csrrw x22, 0xfa, x25	// Restore CSR

// Testing CSR 0xfb
	csrr x18, 0xfb	// Read CSR
	li x23, -1
	csrrw x12, 0xfb, x23	// Write all 1s to CSR
	csrrw x12, 0xfb, x0	// Write all 0s to CSR
	csrrs x12, 0xfb, x23	// Set all CSR bits
	csrrc x12, 0xfb, x23	// Clear all CSR bits
	csrrw x12, 0xfb, x18	// Restore CSR

// Testing CSR 0xfc
	csrr x24, 0xfc	// Read CSR
	li x21, -1
	csrrw x6, 0xfc, x21	// Write all 1s to CSR
	csrrw x6, 0xfc, x0	// Write all 0s to CSR
	csrrs x6, 0xfc, x21	// Set all CSR bits
	csrrc x6, 0xfc, x21	// Clear all CSR bits
	csrrw x6, 0xfc, x24	// Restore CSR

// Testing CSR 0xfd
	csrr x8, 0xfd	// Read CSR
	li x21, -1
	csrrw x16, 0xfd, x21	// Write all 1s to CSR
	csrrw x16, 0xfd, x0	// Write all 0s to CSR
	csrrs x16, 0xfd, x21	// Set all CSR bits
	csrrc x16, 0xfd, x21	// Clear all CSR bits
	csrrw x16, 0xfd, x8	// Restore CSR

// Testing CSR 0xfe
	csrr x1, 0xfe	// Read CSR
	li x15, -1
	csrrw x6, 0xfe, x15	// Write all 1s to CSR
	csrrw x6, 0xfe, x0	// Write all 0s to CSR
	csrrs x6, 0xfe, x15	// Set all CSR bits
	csrrc x6, 0xfe, x15	// Clear all CSR bits
	csrrw x6, 0xfe, x1	// Restore CSR

// Testing CSR 0xff
	csrr x24, 0xff	// Read CSR
	li x9, -1
	csrrw x25, 0xff, x9	// Write all 1s to CSR
	csrrw x25, 0xff, x0	// Write all 0s to CSR
	csrrs x25, 0xff, x9	// Set all CSR bits
	csrrc x25, 0xff, x9	// Clear all CSR bits
	csrrw x25, 0xff, x24	// Restore CSR

// Testing CSR 0x100
	csrr x13, 0x100	// Read CSR
	li x1, -1
	csrrw x19, 0x100, x1	// Write all 1s to CSR
	csrrw x19, 0x100, x0	// Write all 0s to CSR
	csrrs x19, 0x100, x1	// Set all CSR bits
	csrrc x19, 0x100, x1	// Clear all CSR bits
	csrrw x19, 0x100, x13	// Restore CSR

// Testing CSR 0x101
	csrr x17, 0x101	// Read CSR
	li x23, -1
	csrrw x14, 0x101, x23	// Write all 1s to CSR
	csrrw x14, 0x101, x0	// Write all 0s to CSR
	csrrs x14, 0x101, x23	// Set all CSR bits
	csrrc x14, 0x101, x23	// Clear all CSR bits
	csrrw x14, 0x101, x17	// Restore CSR

// Testing CSR 0x102
	csrr x3, 0x102	// Read CSR
	li x6, -1
	csrrw x22, 0x102, x6	// Write all 1s to CSR
	csrrw x22, 0x102, x0	// Write all 0s to CSR
	csrrs x22, 0x102, x6	// Set all CSR bits
	csrrc x22, 0x102, x6	// Clear all CSR bits
	csrrw x22, 0x102, x3	// Restore CSR

// Testing CSR 0x103
	csrr x13, 0x103	// Read CSR
	li x3, -1
	csrrw x30, 0x103, x3	// Write all 1s to CSR
	csrrw x30, 0x103, x0	// Write all 0s to CSR
	csrrs x30, 0x103, x3	// Set all CSR bits
	csrrc x30, 0x103, x3	// Clear all CSR bits
	csrrw x30, 0x103, x13	// Restore CSR

// Testing CSR 0x104
	csrr x31, 0x104	// Read CSR
	li x4, -1
	csrrw x28, 0x104, x4	// Write all 1s to CSR
	csrrw x28, 0x104, x0	// Write all 0s to CSR
	csrrs x28, 0x104, x4	// Set all CSR bits
	csrrc x28, 0x104, x4	// Clear all CSR bits
	csrrw x28, 0x104, x31	// Restore CSR

// Testing CSR 0x105
	csrr x31, 0x105	// Read CSR
	li x7, -1
	csrrw x21, 0x105, x7	// Write all 1s to CSR
	csrrw x21, 0x105, x0	// Write all 0s to CSR
	csrrs x21, 0x105, x7	// Set all CSR bits
	csrrc x21, 0x105, x7	// Clear all CSR bits
	csrrw x21, 0x105, x31	// Restore CSR

// Testing CSR 0x106
	csrr x14, 0x106	// Read CSR
	li x9, -1
	csrrw x5, 0x106, x9	// Write all 1s to CSR
	csrrw x5, 0x106, x0	// Write all 0s to CSR
	csrrs x5, 0x106, x9	// Set all CSR bits
	csrrc x5, 0x106, x9	// Clear all CSR bits
	csrrw x5, 0x106, x14	// Restore CSR

// Testing CSR 0x107
	csrr x22, 0x107	// Read CSR
	li x9, -1
	csrrw x31, 0x107, x9	// Write all 1s to CSR
	csrrw x31, 0x107, x0	// Write all 0s to CSR
	csrrs x31, 0x107, x9	// Set all CSR bits
	csrrc x31, 0x107, x9	// Clear all CSR bits
	csrrw x31, 0x107, x22	// Restore CSR

// Testing CSR 0x108
	csrr x23, 0x108	// Read CSR
	li x30, -1
	csrrw x24, 0x108, x30	// Write all 1s to CSR
	csrrw x24, 0x108, x0	// Write all 0s to CSR
	csrrs x24, 0x108, x30	// Set all CSR bits
	csrrc x24, 0x108, x30	// Clear all CSR bits
	csrrw x24, 0x108, x23	// Restore CSR

// Testing CSR 0x109
	csrr x31, 0x109	// Read CSR
	li x24, -1
	csrrw x28, 0x109, x24	// Write all 1s to CSR
	csrrw x28, 0x109, x0	// Write all 0s to CSR
	csrrs x28, 0x109, x24	// Set all CSR bits
	csrrc x28, 0x109, x24	// Clear all CSR bits
	csrrw x28, 0x109, x31	// Restore CSR

// Testing CSR 0x10a
	csrr x24, 0x10a	// Read CSR
	li x29, -1
	csrrw x30, 0x10a, x29	// Write all 1s to CSR
	csrrw x30, 0x10a, x0	// Write all 0s to CSR
	csrrs x30, 0x10a, x29	// Set all CSR bits
	csrrc x30, 0x10a, x29	// Clear all CSR bits
	csrrw x30, 0x10a, x24	// Restore CSR

// Testing CSR 0x10b
	csrr x16, 0x10b	// Read CSR
	li x30, -1
	csrrw x28, 0x10b, x30	// Write all 1s to CSR
	csrrw x28, 0x10b, x0	// Write all 0s to CSR
	csrrs x28, 0x10b, x30	// Set all CSR bits
	csrrc x28, 0x10b, x30	// Clear all CSR bits
	csrrw x28, 0x10b, x16	// Restore CSR

// Testing CSR 0x10c
	csrr x24, 0x10c	// Read CSR
	li x6, -1
	csrrw x27, 0x10c, x6	// Write all 1s to CSR
	csrrw x27, 0x10c, x0	// Write all 0s to CSR
	csrrs x27, 0x10c, x6	// Set all CSR bits
	csrrc x27, 0x10c, x6	// Clear all CSR bits
	csrrw x27, 0x10c, x24	// Restore CSR

// Testing CSR 0x10d
	csrr x13, 0x10d	// Read CSR
	li x7, -1
	csrrw x26, 0x10d, x7	// Write all 1s to CSR
	csrrw x26, 0x10d, x0	// Write all 0s to CSR
	csrrs x26, 0x10d, x7	// Set all CSR bits
	csrrc x26, 0x10d, x7	// Clear all CSR bits
	csrrw x26, 0x10d, x13	// Restore CSR

// Testing CSR 0x10e
	csrr x10, 0x10e	// Read CSR
	li x13, -1
	csrrw x29, 0x10e, x13	// Write all 1s to CSR
	csrrw x29, 0x10e, x0	// Write all 0s to CSR
	csrrs x29, 0x10e, x13	// Set all CSR bits
	csrrc x29, 0x10e, x13	// Clear all CSR bits
	csrrw x29, 0x10e, x10	// Restore CSR

// Testing CSR 0x10f
	csrr x21, 0x10f	// Read CSR
	li x29, -1
	csrrw x17, 0x10f, x29	// Write all 1s to CSR
	csrrw x17, 0x10f, x0	// Write all 0s to CSR
	csrrs x17, 0x10f, x29	// Set all CSR bits
	csrrc x17, 0x10f, x29	// Clear all CSR bits
	csrrw x17, 0x10f, x21	// Restore CSR

// Testing CSR 0x110
	csrr x26, 0x110	// Read CSR
	li x31, -1
	csrrw x7, 0x110, x31	// Write all 1s to CSR
	csrrw x7, 0x110, x0	// Write all 0s to CSR
	csrrs x7, 0x110, x31	// Set all CSR bits
	csrrc x7, 0x110, x31	// Clear all CSR bits
	csrrw x7, 0x110, x26	// Restore CSR

// Testing CSR 0x111
	csrr x1, 0x111	// Read CSR
	li x27, -1
	csrrw x3, 0x111, x27	// Write all 1s to CSR
	csrrw x3, 0x111, x0	// Write all 0s to CSR
	csrrs x3, 0x111, x27	// Set all CSR bits
	csrrc x3, 0x111, x27	// Clear all CSR bits
	csrrw x3, 0x111, x1	// Restore CSR

// Testing CSR 0x112
	csrr x15, 0x112	// Read CSR
	li x12, -1
	csrrw x7, 0x112, x12	// Write all 1s to CSR
	csrrw x7, 0x112, x0	// Write all 0s to CSR
	csrrs x7, 0x112, x12	// Set all CSR bits
	csrrc x7, 0x112, x12	// Clear all CSR bits
	csrrw x7, 0x112, x15	// Restore CSR

// Testing CSR 0x113
	csrr x13, 0x113	// Read CSR
	li x14, -1
	csrrw x28, 0x113, x14	// Write all 1s to CSR
	csrrw x28, 0x113, x0	// Write all 0s to CSR
	csrrs x28, 0x113, x14	// Set all CSR bits
	csrrc x28, 0x113, x14	// Clear all CSR bits
	csrrw x28, 0x113, x13	// Restore CSR

// Testing CSR 0x114
	csrr x2, 0x114	// Read CSR
	li x3, -1
	csrrw x31, 0x114, x3	// Write all 1s to CSR
	csrrw x31, 0x114, x0	// Write all 0s to CSR
	csrrs x31, 0x114, x3	// Set all CSR bits
	csrrc x31, 0x114, x3	// Clear all CSR bits
	csrrw x31, 0x114, x2	// Restore CSR

// Testing CSR 0x115
	csrr x12, 0x115	// Read CSR
	li x18, -1
	csrrw x7, 0x115, x18	// Write all 1s to CSR
	csrrw x7, 0x115, x0	// Write all 0s to CSR
	csrrs x7, 0x115, x18	// Set all CSR bits
	csrrc x7, 0x115, x18	// Clear all CSR bits
	csrrw x7, 0x115, x12	// Restore CSR

// Testing CSR 0x116
	csrr x17, 0x116	// Read CSR
	li x31, -1
	csrrw x7, 0x116, x31	// Write all 1s to CSR
	csrrw x7, 0x116, x0	// Write all 0s to CSR
	csrrs x7, 0x116, x31	// Set all CSR bits
	csrrc x7, 0x116, x31	// Clear all CSR bits
	csrrw x7, 0x116, x17	// Restore CSR

// Testing CSR 0x117
	csrr x12, 0x117	// Read CSR
	li x15, -1
	csrrw x8, 0x117, x15	// Write all 1s to CSR
	csrrw x8, 0x117, x0	// Write all 0s to CSR
	csrrs x8, 0x117, x15	// Set all CSR bits
	csrrc x8, 0x117, x15	// Clear all CSR bits
	csrrw x8, 0x117, x12	// Restore CSR

// Testing CSR 0x118
	csrr x31, 0x118	// Read CSR
	li x16, -1
	csrrw x28, 0x118, x16	// Write all 1s to CSR
	csrrw x28, 0x118, x0	// Write all 0s to CSR
	csrrs x28, 0x118, x16	// Set all CSR bits
	csrrc x28, 0x118, x16	// Clear all CSR bits
	csrrw x28, 0x118, x31	// Restore CSR

// Testing CSR 0x119
	csrr x3, 0x119	// Read CSR
	li x7, -1
	csrrw x12, 0x119, x7	// Write all 1s to CSR
	csrrw x12, 0x119, x0	// Write all 0s to CSR
	csrrs x12, 0x119, x7	// Set all CSR bits
	csrrc x12, 0x119, x7	// Clear all CSR bits
	csrrw x12, 0x119, x3	// Restore CSR

// Testing CSR 0x11a
	csrr x21, 0x11a	// Read CSR
	li x12, -1
	csrrw x13, 0x11a, x12	// Write all 1s to CSR
	csrrw x13, 0x11a, x0	// Write all 0s to CSR
	csrrs x13, 0x11a, x12	// Set all CSR bits
	csrrc x13, 0x11a, x12	// Clear all CSR bits
	csrrw x13, 0x11a, x21	// Restore CSR

// Testing CSR 0x11b
	csrr x15, 0x11b	// Read CSR
	li x8, -1
	csrrw x17, 0x11b, x8	// Write all 1s to CSR
	csrrw x17, 0x11b, x0	// Write all 0s to CSR
	csrrs x17, 0x11b, x8	// Set all CSR bits
	csrrc x17, 0x11b, x8	// Clear all CSR bits
	csrrw x17, 0x11b, x15	// Restore CSR

// Testing CSR 0x11c
	csrr x25, 0x11c	// Read CSR
	li x5, -1
	csrrw x17, 0x11c, x5	// Write all 1s to CSR
	csrrw x17, 0x11c, x0	// Write all 0s to CSR
	csrrs x17, 0x11c, x5	// Set all CSR bits
	csrrc x17, 0x11c, x5	// Clear all CSR bits
	csrrw x17, 0x11c, x25	// Restore CSR

// Testing CSR 0x11d
	csrr x4, 0x11d	// Read CSR
	li x28, -1
	csrrw x22, 0x11d, x28	// Write all 1s to CSR
	csrrw x22, 0x11d, x0	// Write all 0s to CSR
	csrrs x22, 0x11d, x28	// Set all CSR bits
	csrrc x22, 0x11d, x28	// Clear all CSR bits
	csrrw x22, 0x11d, x4	// Restore CSR

// Testing CSR 0x11e
	csrr x30, 0x11e	// Read CSR
	li x13, -1
	csrrw x14, 0x11e, x13	// Write all 1s to CSR
	csrrw x14, 0x11e, x0	// Write all 0s to CSR
	csrrs x14, 0x11e, x13	// Set all CSR bits
	csrrc x14, 0x11e, x13	// Clear all CSR bits
	csrrw x14, 0x11e, x30	// Restore CSR

// Testing CSR 0x11f
	csrr x10, 0x11f	// Read CSR
	li x20, -1
	csrrw x12, 0x11f, x20	// Write all 1s to CSR
	csrrw x12, 0x11f, x0	// Write all 0s to CSR
	csrrs x12, 0x11f, x20	// Set all CSR bits
	csrrc x12, 0x11f, x20	// Clear all CSR bits
	csrrw x12, 0x11f, x10	// Restore CSR

// Testing CSR 0x120
	csrr x15, 0x120	// Read CSR
	li x17, -1
	csrrw x18, 0x120, x17	// Write all 1s to CSR
	csrrw x18, 0x120, x0	// Write all 0s to CSR
	csrrs x18, 0x120, x17	// Set all CSR bits
	csrrc x18, 0x120, x17	// Clear all CSR bits
	csrrw x18, 0x120, x15	// Restore CSR

// Testing CSR 0x121
	csrr x8, 0x121	// Read CSR
	li x30, -1
	csrrw x9, 0x121, x30	// Write all 1s to CSR
	csrrw x9, 0x121, x0	// Write all 0s to CSR
	csrrs x9, 0x121, x30	// Set all CSR bits
	csrrc x9, 0x121, x30	// Clear all CSR bits
	csrrw x9, 0x121, x8	// Restore CSR

// Testing CSR 0x122
	csrr x22, 0x122	// Read CSR
	li x31, -1
	csrrw x12, 0x122, x31	// Write all 1s to CSR
	csrrw x12, 0x122, x0	// Write all 0s to CSR
	csrrs x12, 0x122, x31	// Set all CSR bits
	csrrc x12, 0x122, x31	// Clear all CSR bits
	csrrw x12, 0x122, x22	// Restore CSR

// Testing CSR 0x123
	csrr x12, 0x123	// Read CSR
	li x7, -1
	csrrw x26, 0x123, x7	// Write all 1s to CSR
	csrrw x26, 0x123, x0	// Write all 0s to CSR
	csrrs x26, 0x123, x7	// Set all CSR bits
	csrrc x26, 0x123, x7	// Clear all CSR bits
	csrrw x26, 0x123, x12	// Restore CSR

// Testing CSR 0x124
	csrr x3, 0x124	// Read CSR
	li x12, -1
	csrrw x20, 0x124, x12	// Write all 1s to CSR
	csrrw x20, 0x124, x0	// Write all 0s to CSR
	csrrs x20, 0x124, x12	// Set all CSR bits
	csrrc x20, 0x124, x12	// Clear all CSR bits
	csrrw x20, 0x124, x3	// Restore CSR

// Testing CSR 0x125
	csrr x5, 0x125	// Read CSR
	li x3, -1
	csrrw x9, 0x125, x3	// Write all 1s to CSR
	csrrw x9, 0x125, x0	// Write all 0s to CSR
	csrrs x9, 0x125, x3	// Set all CSR bits
	csrrc x9, 0x125, x3	// Clear all CSR bits
	csrrw x9, 0x125, x5	// Restore CSR

// Testing CSR 0x126
	csrr x7, 0x126	// Read CSR
	li x4, -1
	csrrw x2, 0x126, x4	// Write all 1s to CSR
	csrrw x2, 0x126, x0	// Write all 0s to CSR
	csrrs x2, 0x126, x4	// Set all CSR bits
	csrrc x2, 0x126, x4	// Clear all CSR bits
	csrrw x2, 0x126, x7	// Restore CSR

// Testing CSR 0x127
	csrr x14, 0x127	// Read CSR
	li x17, -1
	csrrw x12, 0x127, x17	// Write all 1s to CSR
	csrrw x12, 0x127, x0	// Write all 0s to CSR
	csrrs x12, 0x127, x17	// Set all CSR bits
	csrrc x12, 0x127, x17	// Clear all CSR bits
	csrrw x12, 0x127, x14	// Restore CSR

// Testing CSR 0x128
	csrr x28, 0x128	// Read CSR
	li x24, -1
	csrrw x20, 0x128, x24	// Write all 1s to CSR
	csrrw x20, 0x128, x0	// Write all 0s to CSR
	csrrs x20, 0x128, x24	// Set all CSR bits
	csrrc x20, 0x128, x24	// Clear all CSR bits
	csrrw x20, 0x128, x28	// Restore CSR

// Testing CSR 0x129
	csrr x14, 0x129	// Read CSR
	li x6, -1
	csrrw x17, 0x129, x6	// Write all 1s to CSR
	csrrw x17, 0x129, x0	// Write all 0s to CSR
	csrrs x17, 0x129, x6	// Set all CSR bits
	csrrc x17, 0x129, x6	// Clear all CSR bits
	csrrw x17, 0x129, x14	// Restore CSR

// Testing CSR 0x12a
	csrr x6, 0x12a	// Read CSR
	li x9, -1
	csrrw x25, 0x12a, x9	// Write all 1s to CSR
	csrrw x25, 0x12a, x0	// Write all 0s to CSR
	csrrs x25, 0x12a, x9	// Set all CSR bits
	csrrc x25, 0x12a, x9	// Clear all CSR bits
	csrrw x25, 0x12a, x6	// Restore CSR

// Testing CSR 0x12b
	csrr x1, 0x12b	// Read CSR
	li x8, -1
	csrrw x17, 0x12b, x8	// Write all 1s to CSR
	csrrw x17, 0x12b, x0	// Write all 0s to CSR
	csrrs x17, 0x12b, x8	// Set all CSR bits
	csrrc x17, 0x12b, x8	// Clear all CSR bits
	csrrw x17, 0x12b, x1	// Restore CSR

// Testing CSR 0x12c
	csrr x16, 0x12c	// Read CSR
	li x1, -1
	csrrw x13, 0x12c, x1	// Write all 1s to CSR
	csrrw x13, 0x12c, x0	// Write all 0s to CSR
	csrrs x13, 0x12c, x1	// Set all CSR bits
	csrrc x13, 0x12c, x1	// Clear all CSR bits
	csrrw x13, 0x12c, x16	// Restore CSR

// Testing CSR 0x12d
	csrr x3, 0x12d	// Read CSR
	li x18, -1
	csrrw x8, 0x12d, x18	// Write all 1s to CSR
	csrrw x8, 0x12d, x0	// Write all 0s to CSR
	csrrs x8, 0x12d, x18	// Set all CSR bits
	csrrc x8, 0x12d, x18	// Clear all CSR bits
	csrrw x8, 0x12d, x3	// Restore CSR

// Testing CSR 0x12e
	csrr x8, 0x12e	// Read CSR
	li x28, -1
	csrrw x24, 0x12e, x28	// Write all 1s to CSR
	csrrw x24, 0x12e, x0	// Write all 0s to CSR
	csrrs x24, 0x12e, x28	// Set all CSR bits
	csrrc x24, 0x12e, x28	// Clear all CSR bits
	csrrw x24, 0x12e, x8	// Restore CSR

// Testing CSR 0x12f
	csrr x13, 0x12f	// Read CSR
	li x31, -1
	csrrw x24, 0x12f, x31	// Write all 1s to CSR
	csrrw x24, 0x12f, x0	// Write all 0s to CSR
	csrrs x24, 0x12f, x31	// Set all CSR bits
	csrrc x24, 0x12f, x31	// Clear all CSR bits
	csrrw x24, 0x12f, x13	// Restore CSR

// Testing CSR 0x130
	csrr x24, 0x130	// Read CSR
	li x15, -1
	csrrw x8, 0x130, x15	// Write all 1s to CSR
	csrrw x8, 0x130, x0	// Write all 0s to CSR
	csrrs x8, 0x130, x15	// Set all CSR bits
	csrrc x8, 0x130, x15	// Clear all CSR bits
	csrrw x8, 0x130, x24	// Restore CSR

// Testing CSR 0x131
	csrr x6, 0x131	// Read CSR
	li x28, -1
	csrrw x12, 0x131, x28	// Write all 1s to CSR
	csrrw x12, 0x131, x0	// Write all 0s to CSR
	csrrs x12, 0x131, x28	// Set all CSR bits
	csrrc x12, 0x131, x28	// Clear all CSR bits
	csrrw x12, 0x131, x6	// Restore CSR

// Testing CSR 0x132
	csrr x5, 0x132	// Read CSR
	li x6, -1
	csrrw x11, 0x132, x6	// Write all 1s to CSR
	csrrw x11, 0x132, x0	// Write all 0s to CSR
	csrrs x11, 0x132, x6	// Set all CSR bits
	csrrc x11, 0x132, x6	// Clear all CSR bits
	csrrw x11, 0x132, x5	// Restore CSR

// Testing CSR 0x133
	csrr x13, 0x133	// Read CSR
	li x23, -1
	csrrw x18, 0x133, x23	// Write all 1s to CSR
	csrrw x18, 0x133, x0	// Write all 0s to CSR
	csrrs x18, 0x133, x23	// Set all CSR bits
	csrrc x18, 0x133, x23	// Clear all CSR bits
	csrrw x18, 0x133, x13	// Restore CSR

// Testing CSR 0x134
	csrr x12, 0x134	// Read CSR
	li x11, -1
	csrrw x26, 0x134, x11	// Write all 1s to CSR
	csrrw x26, 0x134, x0	// Write all 0s to CSR
	csrrs x26, 0x134, x11	// Set all CSR bits
	csrrc x26, 0x134, x11	// Clear all CSR bits
	csrrw x26, 0x134, x12	// Restore CSR

// Testing CSR 0x135
	csrr x13, 0x135	// Read CSR
	li x6, -1
	csrrw x15, 0x135, x6	// Write all 1s to CSR
	csrrw x15, 0x135, x0	// Write all 0s to CSR
	csrrs x15, 0x135, x6	// Set all CSR bits
	csrrc x15, 0x135, x6	// Clear all CSR bits
	csrrw x15, 0x135, x13	// Restore CSR

// Testing CSR 0x136
	csrr x13, 0x136	// Read CSR
	li x5, -1
	csrrw x23, 0x136, x5	// Write all 1s to CSR
	csrrw x23, 0x136, x0	// Write all 0s to CSR
	csrrs x23, 0x136, x5	// Set all CSR bits
	csrrc x23, 0x136, x5	// Clear all CSR bits
	csrrw x23, 0x136, x13	// Restore CSR

// Testing CSR 0x137
	csrr x29, 0x137	// Read CSR
	li x24, -1
	csrrw x15, 0x137, x24	// Write all 1s to CSR
	csrrw x15, 0x137, x0	// Write all 0s to CSR
	csrrs x15, 0x137, x24	// Set all CSR bits
	csrrc x15, 0x137, x24	// Clear all CSR bits
	csrrw x15, 0x137, x29	// Restore CSR

// Testing CSR 0x138
	csrr x11, 0x138	// Read CSR
	li x21, -1
	csrrw x6, 0x138, x21	// Write all 1s to CSR
	csrrw x6, 0x138, x0	// Write all 0s to CSR
	csrrs x6, 0x138, x21	// Set all CSR bits
	csrrc x6, 0x138, x21	// Clear all CSR bits
	csrrw x6, 0x138, x11	// Restore CSR

// Testing CSR 0x139
	csrr x20, 0x139	// Read CSR
	li x28, -1
	csrrw x2, 0x139, x28	// Write all 1s to CSR
	csrrw x2, 0x139, x0	// Write all 0s to CSR
	csrrs x2, 0x139, x28	// Set all CSR bits
	csrrc x2, 0x139, x28	// Clear all CSR bits
	csrrw x2, 0x139, x20	// Restore CSR

// Testing CSR 0x13a
	csrr x25, 0x13a	// Read CSR
	li x2, -1
	csrrw x15, 0x13a, x2	// Write all 1s to CSR
	csrrw x15, 0x13a, x0	// Write all 0s to CSR
	csrrs x15, 0x13a, x2	// Set all CSR bits
	csrrc x15, 0x13a, x2	// Clear all CSR bits
	csrrw x15, 0x13a, x25	// Restore CSR

// Testing CSR 0x13b
	csrr x9, 0x13b	// Read CSR
	li x8, -1
	csrrw x29, 0x13b, x8	// Write all 1s to CSR
	csrrw x29, 0x13b, x0	// Write all 0s to CSR
	csrrs x29, 0x13b, x8	// Set all CSR bits
	csrrc x29, 0x13b, x8	// Clear all CSR bits
	csrrw x29, 0x13b, x9	// Restore CSR

// Testing CSR 0x13c
	csrr x4, 0x13c	// Read CSR
	li x1, -1
	csrrw x20, 0x13c, x1	// Write all 1s to CSR
	csrrw x20, 0x13c, x0	// Write all 0s to CSR
	csrrs x20, 0x13c, x1	// Set all CSR bits
	csrrc x20, 0x13c, x1	// Clear all CSR bits
	csrrw x20, 0x13c, x4	// Restore CSR

// Testing CSR 0x13d
	csrr x16, 0x13d	// Read CSR
	li x27, -1
	csrrw x30, 0x13d, x27	// Write all 1s to CSR
	csrrw x30, 0x13d, x0	// Write all 0s to CSR
	csrrs x30, 0x13d, x27	// Set all CSR bits
	csrrc x30, 0x13d, x27	// Clear all CSR bits
	csrrw x30, 0x13d, x16	// Restore CSR

// Testing CSR 0x13e
	csrr x16, 0x13e	// Read CSR
	li x8, -1
	csrrw x31, 0x13e, x8	// Write all 1s to CSR
	csrrw x31, 0x13e, x0	// Write all 0s to CSR
	csrrs x31, 0x13e, x8	// Set all CSR bits
	csrrc x31, 0x13e, x8	// Clear all CSR bits
	csrrw x31, 0x13e, x16	// Restore CSR

// Testing CSR 0x13f
	csrr x8, 0x13f	// Read CSR
	li x20, -1
	csrrw x5, 0x13f, x20	// Write all 1s to CSR
	csrrw x5, 0x13f, x0	// Write all 0s to CSR
	csrrs x5, 0x13f, x20	// Set all CSR bits
	csrrc x5, 0x13f, x20	// Clear all CSR bits
	csrrw x5, 0x13f, x8	// Restore CSR

// Testing CSR 0x140
	csrr x31, 0x140	// Read CSR
	li x23, -1
	csrrw x19, 0x140, x23	// Write all 1s to CSR
	csrrw x19, 0x140, x0	// Write all 0s to CSR
	csrrs x19, 0x140, x23	// Set all CSR bits
	csrrc x19, 0x140, x23	// Clear all CSR bits
	csrrw x19, 0x140, x31	// Restore CSR

// Testing CSR 0x141
	csrr x22, 0x141	// Read CSR
	li x7, -1
	csrrw x10, 0x141, x7	// Write all 1s to CSR
	csrrw x10, 0x141, x0	// Write all 0s to CSR
	csrrs x10, 0x141, x7	// Set all CSR bits
	csrrc x10, 0x141, x7	// Clear all CSR bits
	csrrw x10, 0x141, x22	// Restore CSR

// Testing CSR 0x142
	csrr x23, 0x142	// Read CSR
	li x24, -1
	csrrw x18, 0x142, x24	// Write all 1s to CSR
	csrrw x18, 0x142, x0	// Write all 0s to CSR
	csrrs x18, 0x142, x24	// Set all CSR bits
	csrrc x18, 0x142, x24	// Clear all CSR bits
	csrrw x18, 0x142, x23	// Restore CSR

// Testing CSR 0x143
	csrr x2, 0x143	// Read CSR
	li x13, -1
	csrrw x30, 0x143, x13	// Write all 1s to CSR
	csrrw x30, 0x143, x0	// Write all 0s to CSR
	csrrs x30, 0x143, x13	// Set all CSR bits
	csrrc x30, 0x143, x13	// Clear all CSR bits
	csrrw x30, 0x143, x2	// Restore CSR

// Testing CSR 0x144
	csrr x2, 0x144	// Read CSR
	li x24, -1
	csrrw x29, 0x144, x24	// Write all 1s to CSR
	csrrw x29, 0x144, x0	// Write all 0s to CSR
	csrrs x29, 0x144, x24	// Set all CSR bits
	csrrc x29, 0x144, x24	// Clear all CSR bits
	csrrw x29, 0x144, x2	// Restore CSR

// Testing CSR 0x145
	csrr x18, 0x145	// Read CSR
	li x21, -1
	csrrw x17, 0x145, x21	// Write all 1s to CSR
	csrrw x17, 0x145, x0	// Write all 0s to CSR
	csrrs x17, 0x145, x21	// Set all CSR bits
	csrrc x17, 0x145, x21	// Clear all CSR bits
	csrrw x17, 0x145, x18	// Restore CSR

// Testing CSR 0x146
	csrr x21, 0x146	// Read CSR
	li x27, -1
	csrrw x29, 0x146, x27	// Write all 1s to CSR
	csrrw x29, 0x146, x0	// Write all 0s to CSR
	csrrs x29, 0x146, x27	// Set all CSR bits
	csrrc x29, 0x146, x27	// Clear all CSR bits
	csrrw x29, 0x146, x21	// Restore CSR

// Testing CSR 0x147
	csrr x15, 0x147	// Read CSR
	li x25, -1
	csrrw x28, 0x147, x25	// Write all 1s to CSR
	csrrw x28, 0x147, x0	// Write all 0s to CSR
	csrrs x28, 0x147, x25	// Set all CSR bits
	csrrc x28, 0x147, x25	// Clear all CSR bits
	csrrw x28, 0x147, x15	// Restore CSR

// Testing CSR 0x148
	csrr x28, 0x148	// Read CSR
	li x23, -1
	csrrw x29, 0x148, x23	// Write all 1s to CSR
	csrrw x29, 0x148, x0	// Write all 0s to CSR
	csrrs x29, 0x148, x23	// Set all CSR bits
	csrrc x29, 0x148, x23	// Clear all CSR bits
	csrrw x29, 0x148, x28	// Restore CSR

// Testing CSR 0x149
	csrr x14, 0x149	// Read CSR
	li x8, -1
	csrrw x23, 0x149, x8	// Write all 1s to CSR
	csrrw x23, 0x149, x0	// Write all 0s to CSR
	csrrs x23, 0x149, x8	// Set all CSR bits
	csrrc x23, 0x149, x8	// Clear all CSR bits
	csrrw x23, 0x149, x14	// Restore CSR

// Testing CSR 0x14a
	csrr x3, 0x14a	// Read CSR
	li x26, -1
	csrrw x27, 0x14a, x26	// Write all 1s to CSR
	csrrw x27, 0x14a, x0	// Write all 0s to CSR
	csrrs x27, 0x14a, x26	// Set all CSR bits
	csrrc x27, 0x14a, x26	// Clear all CSR bits
	csrrw x27, 0x14a, x3	// Restore CSR

// Testing CSR 0x14b
	csrr x24, 0x14b	// Read CSR
	li x12, -1
	csrrw x20, 0x14b, x12	// Write all 1s to CSR
	csrrw x20, 0x14b, x0	// Write all 0s to CSR
	csrrs x20, 0x14b, x12	// Set all CSR bits
	csrrc x20, 0x14b, x12	// Clear all CSR bits
	csrrw x20, 0x14b, x24	// Restore CSR

// Testing CSR 0x14c
	csrr x11, 0x14c	// Read CSR
	li x8, -1
	csrrw x20, 0x14c, x8	// Write all 1s to CSR
	csrrw x20, 0x14c, x0	// Write all 0s to CSR
	csrrs x20, 0x14c, x8	// Set all CSR bits
	csrrc x20, 0x14c, x8	// Clear all CSR bits
	csrrw x20, 0x14c, x11	// Restore CSR

// Testing CSR 0x14d
	csrr x7, 0x14d	// Read CSR
	li x18, -1
	csrrw x27, 0x14d, x18	// Write all 1s to CSR
	csrrw x27, 0x14d, x0	// Write all 0s to CSR
	csrrs x27, 0x14d, x18	// Set all CSR bits
	csrrc x27, 0x14d, x18	// Clear all CSR bits
	csrrw x27, 0x14d, x7	// Restore CSR

// Testing CSR 0x14e
	csrr x15, 0x14e	// Read CSR
	li x3, -1
	csrrw x30, 0x14e, x3	// Write all 1s to CSR
	csrrw x30, 0x14e, x0	// Write all 0s to CSR
	csrrs x30, 0x14e, x3	// Set all CSR bits
	csrrc x30, 0x14e, x3	// Clear all CSR bits
	csrrw x30, 0x14e, x15	// Restore CSR

// Testing CSR 0x14f
	csrr x4, 0x14f	// Read CSR
	li x2, -1
	csrrw x6, 0x14f, x2	// Write all 1s to CSR
	csrrw x6, 0x14f, x0	// Write all 0s to CSR
	csrrs x6, 0x14f, x2	// Set all CSR bits
	csrrc x6, 0x14f, x2	// Clear all CSR bits
	csrrw x6, 0x14f, x4	// Restore CSR

// Testing CSR 0x150
	csrr x18, 0x150	// Read CSR
	li x17, -1
	csrrw x11, 0x150, x17	// Write all 1s to CSR
	csrrw x11, 0x150, x0	// Write all 0s to CSR
	csrrs x11, 0x150, x17	// Set all CSR bits
	csrrc x11, 0x150, x17	// Clear all CSR bits
	csrrw x11, 0x150, x18	// Restore CSR

// Testing CSR 0x151
	csrr x16, 0x151	// Read CSR
	li x14, -1
	csrrw x28, 0x151, x14	// Write all 1s to CSR
	csrrw x28, 0x151, x0	// Write all 0s to CSR
	csrrs x28, 0x151, x14	// Set all CSR bits
	csrrc x28, 0x151, x14	// Clear all CSR bits
	csrrw x28, 0x151, x16	// Restore CSR

// Testing CSR 0x152
	csrr x25, 0x152	// Read CSR
	li x15, -1
	csrrw x23, 0x152, x15	// Write all 1s to CSR
	csrrw x23, 0x152, x0	// Write all 0s to CSR
	csrrs x23, 0x152, x15	// Set all CSR bits
	csrrc x23, 0x152, x15	// Clear all CSR bits
	csrrw x23, 0x152, x25	// Restore CSR

// Testing CSR 0x153
	csrr x22, 0x153	// Read CSR
	li x5, -1
	csrrw x3, 0x153, x5	// Write all 1s to CSR
	csrrw x3, 0x153, x0	// Write all 0s to CSR
	csrrs x3, 0x153, x5	// Set all CSR bits
	csrrc x3, 0x153, x5	// Clear all CSR bits
	csrrw x3, 0x153, x22	// Restore CSR

// Testing CSR 0x154
	csrr x10, 0x154	// Read CSR
	li x27, -1
	csrrw x24, 0x154, x27	// Write all 1s to CSR
	csrrw x24, 0x154, x0	// Write all 0s to CSR
	csrrs x24, 0x154, x27	// Set all CSR bits
	csrrc x24, 0x154, x27	// Clear all CSR bits
	csrrw x24, 0x154, x10	// Restore CSR

// Testing CSR 0x155
	csrr x27, 0x155	// Read CSR
	li x20, -1
	csrrw x31, 0x155, x20	// Write all 1s to CSR
	csrrw x31, 0x155, x0	// Write all 0s to CSR
	csrrs x31, 0x155, x20	// Set all CSR bits
	csrrc x31, 0x155, x20	// Clear all CSR bits
	csrrw x31, 0x155, x27	// Restore CSR

// Testing CSR 0x156
	csrr x9, 0x156	// Read CSR
	li x23, -1
	csrrw x15, 0x156, x23	// Write all 1s to CSR
	csrrw x15, 0x156, x0	// Write all 0s to CSR
	csrrs x15, 0x156, x23	// Set all CSR bits
	csrrc x15, 0x156, x23	// Clear all CSR bits
	csrrw x15, 0x156, x9	// Restore CSR

// Testing CSR 0x157
	csrr x5, 0x157	// Read CSR
	li x31, -1
	csrrw x18, 0x157, x31	// Write all 1s to CSR
	csrrw x18, 0x157, x0	// Write all 0s to CSR
	csrrs x18, 0x157, x31	// Set all CSR bits
	csrrc x18, 0x157, x31	// Clear all CSR bits
	csrrw x18, 0x157, x5	// Restore CSR

// Testing CSR 0x158
	csrr x27, 0x158	// Read CSR
	li x5, -1
	csrrw x20, 0x158, x5	// Write all 1s to CSR
	csrrw x20, 0x158, x0	// Write all 0s to CSR
	csrrs x20, 0x158, x5	// Set all CSR bits
	csrrc x20, 0x158, x5	// Clear all CSR bits
	csrrw x20, 0x158, x27	// Restore CSR

// Testing CSR 0x159
	csrr x20, 0x159	// Read CSR
	li x29, -1
	csrrw x11, 0x159, x29	// Write all 1s to CSR
	csrrw x11, 0x159, x0	// Write all 0s to CSR
	csrrs x11, 0x159, x29	// Set all CSR bits
	csrrc x11, 0x159, x29	// Clear all CSR bits
	csrrw x11, 0x159, x20	// Restore CSR

// Testing CSR 0x15a
	csrr x22, 0x15a	// Read CSR
	li x10, -1
	csrrw x18, 0x15a, x10	// Write all 1s to CSR
	csrrw x18, 0x15a, x0	// Write all 0s to CSR
	csrrs x18, 0x15a, x10	// Set all CSR bits
	csrrc x18, 0x15a, x10	// Clear all CSR bits
	csrrw x18, 0x15a, x22	// Restore CSR

// Testing CSR 0x15b
	csrr x13, 0x15b	// Read CSR
	li x17, -1
	csrrw x16, 0x15b, x17	// Write all 1s to CSR
	csrrw x16, 0x15b, x0	// Write all 0s to CSR
	csrrs x16, 0x15b, x17	// Set all CSR bits
	csrrc x16, 0x15b, x17	// Clear all CSR bits
	csrrw x16, 0x15b, x13	// Restore CSR

// Testing CSR 0x15c
	csrr x11, 0x15c	// Read CSR
	li x30, -1
	csrrw x25, 0x15c, x30	// Write all 1s to CSR
	csrrw x25, 0x15c, x0	// Write all 0s to CSR
	csrrs x25, 0x15c, x30	// Set all CSR bits
	csrrc x25, 0x15c, x30	// Clear all CSR bits
	csrrw x25, 0x15c, x11	// Restore CSR

// Testing CSR 0x15d
	csrr x27, 0x15d	// Read CSR
	li x15, -1
	csrrw x25, 0x15d, x15	// Write all 1s to CSR
	csrrw x25, 0x15d, x0	// Write all 0s to CSR
	csrrs x25, 0x15d, x15	// Set all CSR bits
	csrrc x25, 0x15d, x15	// Clear all CSR bits
	csrrw x25, 0x15d, x27	// Restore CSR

// Testing CSR 0x15e
	csrr x18, 0x15e	// Read CSR
	li x19, -1
	csrrw x25, 0x15e, x19	// Write all 1s to CSR
	csrrw x25, 0x15e, x0	// Write all 0s to CSR
	csrrs x25, 0x15e, x19	// Set all CSR bits
	csrrc x25, 0x15e, x19	// Clear all CSR bits
	csrrw x25, 0x15e, x18	// Restore CSR

// Testing CSR 0x15f
	csrr x30, 0x15f	// Read CSR
	li x31, -1
	csrrw x15, 0x15f, x31	// Write all 1s to CSR
	csrrw x15, 0x15f, x0	// Write all 0s to CSR
	csrrs x15, 0x15f, x31	// Set all CSR bits
	csrrc x15, 0x15f, x31	// Clear all CSR bits
	csrrw x15, 0x15f, x30	// Restore CSR

// Testing CSR 0x160
	csrr x27, 0x160	// Read CSR
	li x6, -1
	csrrw x15, 0x160, x6	// Write all 1s to CSR
	csrrw x15, 0x160, x0	// Write all 0s to CSR
	csrrs x15, 0x160, x6	// Set all CSR bits
	csrrc x15, 0x160, x6	// Clear all CSR bits
	csrrw x15, 0x160, x27	// Restore CSR

// Testing CSR 0x161
	csrr x14, 0x161	// Read CSR
	li x19, -1
	csrrw x27, 0x161, x19	// Write all 1s to CSR
	csrrw x27, 0x161, x0	// Write all 0s to CSR
	csrrs x27, 0x161, x19	// Set all CSR bits
	csrrc x27, 0x161, x19	// Clear all CSR bits
	csrrw x27, 0x161, x14	// Restore CSR

// Testing CSR 0x162
	csrr x4, 0x162	// Read CSR
	li x14, -1
	csrrw x20, 0x162, x14	// Write all 1s to CSR
	csrrw x20, 0x162, x0	// Write all 0s to CSR
	csrrs x20, 0x162, x14	// Set all CSR bits
	csrrc x20, 0x162, x14	// Clear all CSR bits
	csrrw x20, 0x162, x4	// Restore CSR

// Testing CSR 0x163
	csrr x5, 0x163	// Read CSR
	li x27, -1
	csrrw x20, 0x163, x27	// Write all 1s to CSR
	csrrw x20, 0x163, x0	// Write all 0s to CSR
	csrrs x20, 0x163, x27	// Set all CSR bits
	csrrc x20, 0x163, x27	// Clear all CSR bits
	csrrw x20, 0x163, x5	// Restore CSR

// Testing CSR 0x164
	csrr x31, 0x164	// Read CSR
	li x25, -1
	csrrw x21, 0x164, x25	// Write all 1s to CSR
	csrrw x21, 0x164, x0	// Write all 0s to CSR
	csrrs x21, 0x164, x25	// Set all CSR bits
	csrrc x21, 0x164, x25	// Clear all CSR bits
	csrrw x21, 0x164, x31	// Restore CSR

// Testing CSR 0x165
	csrr x1, 0x165	// Read CSR
	li x7, -1
	csrrw x21, 0x165, x7	// Write all 1s to CSR
	csrrw x21, 0x165, x0	// Write all 0s to CSR
	csrrs x21, 0x165, x7	// Set all CSR bits
	csrrc x21, 0x165, x7	// Clear all CSR bits
	csrrw x21, 0x165, x1	// Restore CSR

// Testing CSR 0x166
	csrr x7, 0x166	// Read CSR
	li x5, -1
	csrrw x8, 0x166, x5	// Write all 1s to CSR
	csrrw x8, 0x166, x0	// Write all 0s to CSR
	csrrs x8, 0x166, x5	// Set all CSR bits
	csrrc x8, 0x166, x5	// Clear all CSR bits
	csrrw x8, 0x166, x7	// Restore CSR

// Testing CSR 0x167
	csrr x27, 0x167	// Read CSR
	li x22, -1
	csrrw x21, 0x167, x22	// Write all 1s to CSR
	csrrw x21, 0x167, x0	// Write all 0s to CSR
	csrrs x21, 0x167, x22	// Set all CSR bits
	csrrc x21, 0x167, x22	// Clear all CSR bits
	csrrw x21, 0x167, x27	// Restore CSR

// Testing CSR 0x168
	csrr x31, 0x168	// Read CSR
	li x18, -1
	csrrw x25, 0x168, x18	// Write all 1s to CSR
	csrrw x25, 0x168, x0	// Write all 0s to CSR
	csrrs x25, 0x168, x18	// Set all CSR bits
	csrrc x25, 0x168, x18	// Clear all CSR bits
	csrrw x25, 0x168, x31	// Restore CSR

// Testing CSR 0x169
	csrr x15, 0x169	// Read CSR
	li x17, -1
	csrrw x12, 0x169, x17	// Write all 1s to CSR
	csrrw x12, 0x169, x0	// Write all 0s to CSR
	csrrs x12, 0x169, x17	// Set all CSR bits
	csrrc x12, 0x169, x17	// Clear all CSR bits
	csrrw x12, 0x169, x15	// Restore CSR

// Testing CSR 0x16a
	csrr x6, 0x16a	// Read CSR
	li x25, -1
	csrrw x8, 0x16a, x25	// Write all 1s to CSR
	csrrw x8, 0x16a, x0	// Write all 0s to CSR
	csrrs x8, 0x16a, x25	// Set all CSR bits
	csrrc x8, 0x16a, x25	// Clear all CSR bits
	csrrw x8, 0x16a, x6	// Restore CSR

// Testing CSR 0x16b
	csrr x19, 0x16b	// Read CSR
	li x25, -1
	csrrw x14, 0x16b, x25	// Write all 1s to CSR
	csrrw x14, 0x16b, x0	// Write all 0s to CSR
	csrrs x14, 0x16b, x25	// Set all CSR bits
	csrrc x14, 0x16b, x25	// Clear all CSR bits
	csrrw x14, 0x16b, x19	// Restore CSR

// Testing CSR 0x16c
	csrr x22, 0x16c	// Read CSR
	li x13, -1
	csrrw x11, 0x16c, x13	// Write all 1s to CSR
	csrrw x11, 0x16c, x0	// Write all 0s to CSR
	csrrs x11, 0x16c, x13	// Set all CSR bits
	csrrc x11, 0x16c, x13	// Clear all CSR bits
	csrrw x11, 0x16c, x22	// Restore CSR

// Testing CSR 0x16d
	csrr x7, 0x16d	// Read CSR
	li x29, -1
	csrrw x17, 0x16d, x29	// Write all 1s to CSR
	csrrw x17, 0x16d, x0	// Write all 0s to CSR
	csrrs x17, 0x16d, x29	// Set all CSR bits
	csrrc x17, 0x16d, x29	// Clear all CSR bits
	csrrw x17, 0x16d, x7	// Restore CSR

// Testing CSR 0x16e
	csrr x14, 0x16e	// Read CSR
	li x20, -1
	csrrw x4, 0x16e, x20	// Write all 1s to CSR
	csrrw x4, 0x16e, x0	// Write all 0s to CSR
	csrrs x4, 0x16e, x20	// Set all CSR bits
	csrrc x4, 0x16e, x20	// Clear all CSR bits
	csrrw x4, 0x16e, x14	// Restore CSR

// Testing CSR 0x16f
	csrr x7, 0x16f	// Read CSR
	li x28, -1
	csrrw x12, 0x16f, x28	// Write all 1s to CSR
	csrrw x12, 0x16f, x0	// Write all 0s to CSR
	csrrs x12, 0x16f, x28	// Set all CSR bits
	csrrc x12, 0x16f, x28	// Clear all CSR bits
	csrrw x12, 0x16f, x7	// Restore CSR

// Testing CSR 0x170
	csrr x9, 0x170	// Read CSR
	li x19, -1
	csrrw x21, 0x170, x19	// Write all 1s to CSR
	csrrw x21, 0x170, x0	// Write all 0s to CSR
	csrrs x21, 0x170, x19	// Set all CSR bits
	csrrc x21, 0x170, x19	// Clear all CSR bits
	csrrw x21, 0x170, x9	// Restore CSR

// Testing CSR 0x171
	csrr x18, 0x171	// Read CSR
	li x25, -1
	csrrw x13, 0x171, x25	// Write all 1s to CSR
	csrrw x13, 0x171, x0	// Write all 0s to CSR
	csrrs x13, 0x171, x25	// Set all CSR bits
	csrrc x13, 0x171, x25	// Clear all CSR bits
	csrrw x13, 0x171, x18	// Restore CSR

// Testing CSR 0x172
	csrr x23, 0x172	// Read CSR
	li x1, -1
	csrrw x7, 0x172, x1	// Write all 1s to CSR
	csrrw x7, 0x172, x0	// Write all 0s to CSR
	csrrs x7, 0x172, x1	// Set all CSR bits
	csrrc x7, 0x172, x1	// Clear all CSR bits
	csrrw x7, 0x172, x23	// Restore CSR

// Testing CSR 0x173
	csrr x2, 0x173	// Read CSR
	li x16, -1
	csrrw x11, 0x173, x16	// Write all 1s to CSR
	csrrw x11, 0x173, x0	// Write all 0s to CSR
	csrrs x11, 0x173, x16	// Set all CSR bits
	csrrc x11, 0x173, x16	// Clear all CSR bits
	csrrw x11, 0x173, x2	// Restore CSR

// Testing CSR 0x174
	csrr x10, 0x174	// Read CSR
	li x31, -1
	csrrw x12, 0x174, x31	// Write all 1s to CSR
	csrrw x12, 0x174, x0	// Write all 0s to CSR
	csrrs x12, 0x174, x31	// Set all CSR bits
	csrrc x12, 0x174, x31	// Clear all CSR bits
	csrrw x12, 0x174, x10	// Restore CSR

// Testing CSR 0x175
	csrr x29, 0x175	// Read CSR
	li x21, -1
	csrrw x5, 0x175, x21	// Write all 1s to CSR
	csrrw x5, 0x175, x0	// Write all 0s to CSR
	csrrs x5, 0x175, x21	// Set all CSR bits
	csrrc x5, 0x175, x21	// Clear all CSR bits
	csrrw x5, 0x175, x29	// Restore CSR

// Testing CSR 0x176
	csrr x14, 0x176	// Read CSR
	li x9, -1
	csrrw x18, 0x176, x9	// Write all 1s to CSR
	csrrw x18, 0x176, x0	// Write all 0s to CSR
	csrrs x18, 0x176, x9	// Set all CSR bits
	csrrc x18, 0x176, x9	// Clear all CSR bits
	csrrw x18, 0x176, x14	// Restore CSR

// Testing CSR 0x177
	csrr x6, 0x177	// Read CSR
	li x30, -1
	csrrw x29, 0x177, x30	// Write all 1s to CSR
	csrrw x29, 0x177, x0	// Write all 0s to CSR
	csrrs x29, 0x177, x30	// Set all CSR bits
	csrrc x29, 0x177, x30	// Clear all CSR bits
	csrrw x29, 0x177, x6	// Restore CSR

// Testing CSR 0x178
	csrr x14, 0x178	// Read CSR
	li x30, -1
	csrrw x20, 0x178, x30	// Write all 1s to CSR
	csrrw x20, 0x178, x0	// Write all 0s to CSR
	csrrs x20, 0x178, x30	// Set all CSR bits
	csrrc x20, 0x178, x30	// Clear all CSR bits
	csrrw x20, 0x178, x14	// Restore CSR

// Testing CSR 0x179
	csrr x22, 0x179	// Read CSR
	li x2, -1
	csrrw x15, 0x179, x2	// Write all 1s to CSR
	csrrw x15, 0x179, x0	// Write all 0s to CSR
	csrrs x15, 0x179, x2	// Set all CSR bits
	csrrc x15, 0x179, x2	// Clear all CSR bits
	csrrw x15, 0x179, x22	// Restore CSR

// Testing CSR 0x17a
	csrr x22, 0x17a	// Read CSR
	li x8, -1
	csrrw x29, 0x17a, x8	// Write all 1s to CSR
	csrrw x29, 0x17a, x0	// Write all 0s to CSR
	csrrs x29, 0x17a, x8	// Set all CSR bits
	csrrc x29, 0x17a, x8	// Clear all CSR bits
	csrrw x29, 0x17a, x22	// Restore CSR

// Testing CSR 0x17b
	csrr x22, 0x17b	// Read CSR
	li x10, -1
	csrrw x25, 0x17b, x10	// Write all 1s to CSR
	csrrw x25, 0x17b, x0	// Write all 0s to CSR
	csrrs x25, 0x17b, x10	// Set all CSR bits
	csrrc x25, 0x17b, x10	// Clear all CSR bits
	csrrw x25, 0x17b, x22	// Restore CSR

// Testing CSR 0x17c
	csrr x20, 0x17c	// Read CSR
	li x28, -1
	csrrw x22, 0x17c, x28	// Write all 1s to CSR
	csrrw x22, 0x17c, x0	// Write all 0s to CSR
	csrrs x22, 0x17c, x28	// Set all CSR bits
	csrrc x22, 0x17c, x28	// Clear all CSR bits
	csrrw x22, 0x17c, x20	// Restore CSR

// Testing CSR 0x17d
	csrr x10, 0x17d	// Read CSR
	li x5, -1
	csrrw x7, 0x17d, x5	// Write all 1s to CSR
	csrrw x7, 0x17d, x0	// Write all 0s to CSR
	csrrs x7, 0x17d, x5	// Set all CSR bits
	csrrc x7, 0x17d, x5	// Clear all CSR bits
	csrrw x7, 0x17d, x10	// Restore CSR

// Testing CSR 0x17e
	csrr x27, 0x17e	// Read CSR
	li x4, -1
	csrrw x13, 0x17e, x4	// Write all 1s to CSR
	csrrw x13, 0x17e, x0	// Write all 0s to CSR
	csrrs x13, 0x17e, x4	// Set all CSR bits
	csrrc x13, 0x17e, x4	// Clear all CSR bits
	csrrw x13, 0x17e, x27	// Restore CSR

// Testing CSR 0x17f
	csrr x7, 0x17f	// Read CSR
	li x3, -1
	csrrw x25, 0x17f, x3	// Write all 1s to CSR
	csrrw x25, 0x17f, x0	// Write all 0s to CSR
	csrrs x25, 0x17f, x3	// Set all CSR bits
	csrrc x25, 0x17f, x3	// Clear all CSR bits
	csrrw x25, 0x17f, x7	// Restore CSR

// Testing CSR 0x180
	csrr x30, 0x180	// Read CSR
	li x14, -1
	csrrw x21, 0x180, x14	// Write all 1s to CSR
	csrrw x21, 0x180, x0	// Write all 0s to CSR
	csrrs x21, 0x180, x14	// Set all CSR bits
	csrrc x21, 0x180, x14	// Clear all CSR bits
	csrrw x21, 0x180, x30	// Restore CSR

// Testing CSR 0x181
	csrr x29, 0x181	// Read CSR
	li x9, -1
	csrrw x24, 0x181, x9	// Write all 1s to CSR
	csrrw x24, 0x181, x0	// Write all 0s to CSR
	csrrs x24, 0x181, x9	// Set all CSR bits
	csrrc x24, 0x181, x9	// Clear all CSR bits
	csrrw x24, 0x181, x29	// Restore CSR

// Testing CSR 0x182
	csrr x29, 0x182	// Read CSR
	li x8, -1
	csrrw x10, 0x182, x8	// Write all 1s to CSR
	csrrw x10, 0x182, x0	// Write all 0s to CSR
	csrrs x10, 0x182, x8	// Set all CSR bits
	csrrc x10, 0x182, x8	// Clear all CSR bits
	csrrw x10, 0x182, x29	// Restore CSR

// Testing CSR 0x183
	csrr x18, 0x183	// Read CSR
	li x27, -1
	csrrw x20, 0x183, x27	// Write all 1s to CSR
	csrrw x20, 0x183, x0	// Write all 0s to CSR
	csrrs x20, 0x183, x27	// Set all CSR bits
	csrrc x20, 0x183, x27	// Clear all CSR bits
	csrrw x20, 0x183, x18	// Restore CSR

// Testing CSR 0x184
	csrr x17, 0x184	// Read CSR
	li x13, -1
	csrrw x31, 0x184, x13	// Write all 1s to CSR
	csrrw x31, 0x184, x0	// Write all 0s to CSR
	csrrs x31, 0x184, x13	// Set all CSR bits
	csrrc x31, 0x184, x13	// Clear all CSR bits
	csrrw x31, 0x184, x17	// Restore CSR

// Testing CSR 0x185
	csrr x6, 0x185	// Read CSR
	li x1, -1
	csrrw x16, 0x185, x1	// Write all 1s to CSR
	csrrw x16, 0x185, x0	// Write all 0s to CSR
	csrrs x16, 0x185, x1	// Set all CSR bits
	csrrc x16, 0x185, x1	// Clear all CSR bits
	csrrw x16, 0x185, x6	// Restore CSR

// Testing CSR 0x186
	csrr x17, 0x186	// Read CSR
	li x19, -1
	csrrw x14, 0x186, x19	// Write all 1s to CSR
	csrrw x14, 0x186, x0	// Write all 0s to CSR
	csrrs x14, 0x186, x19	// Set all CSR bits
	csrrc x14, 0x186, x19	// Clear all CSR bits
	csrrw x14, 0x186, x17	// Restore CSR

// Testing CSR 0x187
	csrr x3, 0x187	// Read CSR
	li x15, -1
	csrrw x6, 0x187, x15	// Write all 1s to CSR
	csrrw x6, 0x187, x0	// Write all 0s to CSR
	csrrs x6, 0x187, x15	// Set all CSR bits
	csrrc x6, 0x187, x15	// Clear all CSR bits
	csrrw x6, 0x187, x3	// Restore CSR

// Testing CSR 0x188
	csrr x22, 0x188	// Read CSR
	li x18, -1
	csrrw x17, 0x188, x18	// Write all 1s to CSR
	csrrw x17, 0x188, x0	// Write all 0s to CSR
	csrrs x17, 0x188, x18	// Set all CSR bits
	csrrc x17, 0x188, x18	// Clear all CSR bits
	csrrw x17, 0x188, x22	// Restore CSR

// Testing CSR 0x189
	csrr x5, 0x189	// Read CSR
	li x14, -1
	csrrw x26, 0x189, x14	// Write all 1s to CSR
	csrrw x26, 0x189, x0	// Write all 0s to CSR
	csrrs x26, 0x189, x14	// Set all CSR bits
	csrrc x26, 0x189, x14	// Clear all CSR bits
	csrrw x26, 0x189, x5	// Restore CSR

// Testing CSR 0x18a
	csrr x1, 0x18a	// Read CSR
	li x18, -1
	csrrw x22, 0x18a, x18	// Write all 1s to CSR
	csrrw x22, 0x18a, x0	// Write all 0s to CSR
	csrrs x22, 0x18a, x18	// Set all CSR bits
	csrrc x22, 0x18a, x18	// Clear all CSR bits
	csrrw x22, 0x18a, x1	// Restore CSR

// Testing CSR 0x18b
	csrr x22, 0x18b	// Read CSR
	li x28, -1
	csrrw x1, 0x18b, x28	// Write all 1s to CSR
	csrrw x1, 0x18b, x0	// Write all 0s to CSR
	csrrs x1, 0x18b, x28	// Set all CSR bits
	csrrc x1, 0x18b, x28	// Clear all CSR bits
	csrrw x1, 0x18b, x22	// Restore CSR

// Testing CSR 0x18c
	csrr x27, 0x18c	// Read CSR
	li x10, -1
	csrrw x11, 0x18c, x10	// Write all 1s to CSR
	csrrw x11, 0x18c, x0	// Write all 0s to CSR
	csrrs x11, 0x18c, x10	// Set all CSR bits
	csrrc x11, 0x18c, x10	// Clear all CSR bits
	csrrw x11, 0x18c, x27	// Restore CSR

// Testing CSR 0x18d
	csrr x11, 0x18d	// Read CSR
	li x21, -1
	csrrw x24, 0x18d, x21	// Write all 1s to CSR
	csrrw x24, 0x18d, x0	// Write all 0s to CSR
	csrrs x24, 0x18d, x21	// Set all CSR bits
	csrrc x24, 0x18d, x21	// Clear all CSR bits
	csrrw x24, 0x18d, x11	// Restore CSR

// Testing CSR 0x18e
	csrr x11, 0x18e	// Read CSR
	li x25, -1
	csrrw x3, 0x18e, x25	// Write all 1s to CSR
	csrrw x3, 0x18e, x0	// Write all 0s to CSR
	csrrs x3, 0x18e, x25	// Set all CSR bits
	csrrc x3, 0x18e, x25	// Clear all CSR bits
	csrrw x3, 0x18e, x11	// Restore CSR

// Testing CSR 0x18f
	csrr x29, 0x18f	// Read CSR
	li x30, -1
	csrrw x8, 0x18f, x30	// Write all 1s to CSR
	csrrw x8, 0x18f, x0	// Write all 0s to CSR
	csrrs x8, 0x18f, x30	// Set all CSR bits
	csrrc x8, 0x18f, x30	// Clear all CSR bits
	csrrw x8, 0x18f, x29	// Restore CSR

// Testing CSR 0x190
	csrr x9, 0x190	// Read CSR
	li x28, -1
	csrrw x12, 0x190, x28	// Write all 1s to CSR
	csrrw x12, 0x190, x0	// Write all 0s to CSR
	csrrs x12, 0x190, x28	// Set all CSR bits
	csrrc x12, 0x190, x28	// Clear all CSR bits
	csrrw x12, 0x190, x9	// Restore CSR

// Testing CSR 0x191
	csrr x9, 0x191	// Read CSR
	li x6, -1
	csrrw x28, 0x191, x6	// Write all 1s to CSR
	csrrw x28, 0x191, x0	// Write all 0s to CSR
	csrrs x28, 0x191, x6	// Set all CSR bits
	csrrc x28, 0x191, x6	// Clear all CSR bits
	csrrw x28, 0x191, x9	// Restore CSR

// Testing CSR 0x192
	csrr x21, 0x192	// Read CSR
	li x19, -1
	csrrw x8, 0x192, x19	// Write all 1s to CSR
	csrrw x8, 0x192, x0	// Write all 0s to CSR
	csrrs x8, 0x192, x19	// Set all CSR bits
	csrrc x8, 0x192, x19	// Clear all CSR bits
	csrrw x8, 0x192, x21	// Restore CSR

// Testing CSR 0x193
	csrr x5, 0x193	// Read CSR
	li x29, -1
	csrrw x27, 0x193, x29	// Write all 1s to CSR
	csrrw x27, 0x193, x0	// Write all 0s to CSR
	csrrs x27, 0x193, x29	// Set all CSR bits
	csrrc x27, 0x193, x29	// Clear all CSR bits
	csrrw x27, 0x193, x5	// Restore CSR

// Testing CSR 0x194
	csrr x28, 0x194	// Read CSR
	li x25, -1
	csrrw x10, 0x194, x25	// Write all 1s to CSR
	csrrw x10, 0x194, x0	// Write all 0s to CSR
	csrrs x10, 0x194, x25	// Set all CSR bits
	csrrc x10, 0x194, x25	// Clear all CSR bits
	csrrw x10, 0x194, x28	// Restore CSR

// Testing CSR 0x195
	csrr x21, 0x195	// Read CSR
	li x22, -1
	csrrw x16, 0x195, x22	// Write all 1s to CSR
	csrrw x16, 0x195, x0	// Write all 0s to CSR
	csrrs x16, 0x195, x22	// Set all CSR bits
	csrrc x16, 0x195, x22	// Clear all CSR bits
	csrrw x16, 0x195, x21	// Restore CSR

// Testing CSR 0x196
	csrr x14, 0x196	// Read CSR
	li x15, -1
	csrrw x5, 0x196, x15	// Write all 1s to CSR
	csrrw x5, 0x196, x0	// Write all 0s to CSR
	csrrs x5, 0x196, x15	// Set all CSR bits
	csrrc x5, 0x196, x15	// Clear all CSR bits
	csrrw x5, 0x196, x14	// Restore CSR

// Testing CSR 0x197
	csrr x2, 0x197	// Read CSR
	li x23, -1
	csrrw x12, 0x197, x23	// Write all 1s to CSR
	csrrw x12, 0x197, x0	// Write all 0s to CSR
	csrrs x12, 0x197, x23	// Set all CSR bits
	csrrc x12, 0x197, x23	// Clear all CSR bits
	csrrw x12, 0x197, x2	// Restore CSR

// Testing CSR 0x198
	csrr x28, 0x198	// Read CSR
	li x2, -1
	csrrw x11, 0x198, x2	// Write all 1s to CSR
	csrrw x11, 0x198, x0	// Write all 0s to CSR
	csrrs x11, 0x198, x2	// Set all CSR bits
	csrrc x11, 0x198, x2	// Clear all CSR bits
	csrrw x11, 0x198, x28	// Restore CSR

// Testing CSR 0x199
	csrr x4, 0x199	// Read CSR
	li x7, -1
	csrrw x20, 0x199, x7	// Write all 1s to CSR
	csrrw x20, 0x199, x0	// Write all 0s to CSR
	csrrs x20, 0x199, x7	// Set all CSR bits
	csrrc x20, 0x199, x7	// Clear all CSR bits
	csrrw x20, 0x199, x4	// Restore CSR

// Testing CSR 0x19a
	csrr x25, 0x19a	// Read CSR
	li x27, -1
	csrrw x1, 0x19a, x27	// Write all 1s to CSR
	csrrw x1, 0x19a, x0	// Write all 0s to CSR
	csrrs x1, 0x19a, x27	// Set all CSR bits
	csrrc x1, 0x19a, x27	// Clear all CSR bits
	csrrw x1, 0x19a, x25	// Restore CSR

// Testing CSR 0x19b
	csrr x5, 0x19b	// Read CSR
	li x10, -1
	csrrw x25, 0x19b, x10	// Write all 1s to CSR
	csrrw x25, 0x19b, x0	// Write all 0s to CSR
	csrrs x25, 0x19b, x10	// Set all CSR bits
	csrrc x25, 0x19b, x10	// Clear all CSR bits
	csrrw x25, 0x19b, x5	// Restore CSR

// Testing CSR 0x19c
	csrr x23, 0x19c	// Read CSR
	li x2, -1
	csrrw x8, 0x19c, x2	// Write all 1s to CSR
	csrrw x8, 0x19c, x0	// Write all 0s to CSR
	csrrs x8, 0x19c, x2	// Set all CSR bits
	csrrc x8, 0x19c, x2	// Clear all CSR bits
	csrrw x8, 0x19c, x23	// Restore CSR

// Testing CSR 0x19d
	csrr x17, 0x19d	// Read CSR
	li x20, -1
	csrrw x1, 0x19d, x20	// Write all 1s to CSR
	csrrw x1, 0x19d, x0	// Write all 0s to CSR
	csrrs x1, 0x19d, x20	// Set all CSR bits
	csrrc x1, 0x19d, x20	// Clear all CSR bits
	csrrw x1, 0x19d, x17	// Restore CSR

// Testing CSR 0x19e
	csrr x22, 0x19e	// Read CSR
	li x4, -1
	csrrw x21, 0x19e, x4	// Write all 1s to CSR
	csrrw x21, 0x19e, x0	// Write all 0s to CSR
	csrrs x21, 0x19e, x4	// Set all CSR bits
	csrrc x21, 0x19e, x4	// Clear all CSR bits
	csrrw x21, 0x19e, x22	// Restore CSR

// Testing CSR 0x19f
	csrr x2, 0x19f	// Read CSR
	li x6, -1
	csrrw x26, 0x19f, x6	// Write all 1s to CSR
	csrrw x26, 0x19f, x0	// Write all 0s to CSR
	csrrs x26, 0x19f, x6	// Set all CSR bits
	csrrc x26, 0x19f, x6	// Clear all CSR bits
	csrrw x26, 0x19f, x2	// Restore CSR

// Testing CSR 0x1a0
	csrr x1, 0x1a0	// Read CSR
	li x16, -1
	csrrw x20, 0x1a0, x16	// Write all 1s to CSR
	csrrw x20, 0x1a0, x0	// Write all 0s to CSR
	csrrs x20, 0x1a0, x16	// Set all CSR bits
	csrrc x20, 0x1a0, x16	// Clear all CSR bits
	csrrw x20, 0x1a0, x1	// Restore CSR

// Testing CSR 0x1a1
	csrr x29, 0x1a1	// Read CSR
	li x6, -1
	csrrw x5, 0x1a1, x6	// Write all 1s to CSR
	csrrw x5, 0x1a1, x0	// Write all 0s to CSR
	csrrs x5, 0x1a1, x6	// Set all CSR bits
	csrrc x5, 0x1a1, x6	// Clear all CSR bits
	csrrw x5, 0x1a1, x29	// Restore CSR

// Testing CSR 0x1a2
	csrr x10, 0x1a2	// Read CSR
	li x31, -1
	csrrw x7, 0x1a2, x31	// Write all 1s to CSR
	csrrw x7, 0x1a2, x0	// Write all 0s to CSR
	csrrs x7, 0x1a2, x31	// Set all CSR bits
	csrrc x7, 0x1a2, x31	// Clear all CSR bits
	csrrw x7, 0x1a2, x10	// Restore CSR

// Testing CSR 0x1a3
	csrr x12, 0x1a3	// Read CSR
	li x17, -1
	csrrw x4, 0x1a3, x17	// Write all 1s to CSR
	csrrw x4, 0x1a3, x0	// Write all 0s to CSR
	csrrs x4, 0x1a3, x17	// Set all CSR bits
	csrrc x4, 0x1a3, x17	// Clear all CSR bits
	csrrw x4, 0x1a3, x12	// Restore CSR

// Testing CSR 0x1a4
	csrr x20, 0x1a4	// Read CSR
	li x12, -1
	csrrw x2, 0x1a4, x12	// Write all 1s to CSR
	csrrw x2, 0x1a4, x0	// Write all 0s to CSR
	csrrs x2, 0x1a4, x12	// Set all CSR bits
	csrrc x2, 0x1a4, x12	// Clear all CSR bits
	csrrw x2, 0x1a4, x20	// Restore CSR

// Testing CSR 0x1a5
	csrr x11, 0x1a5	// Read CSR
	li x20, -1
	csrrw x3, 0x1a5, x20	// Write all 1s to CSR
	csrrw x3, 0x1a5, x0	// Write all 0s to CSR
	csrrs x3, 0x1a5, x20	// Set all CSR bits
	csrrc x3, 0x1a5, x20	// Clear all CSR bits
	csrrw x3, 0x1a5, x11	// Restore CSR

// Testing CSR 0x1a6
	csrr x31, 0x1a6	// Read CSR
	li x1, -1
	csrrw x11, 0x1a6, x1	// Write all 1s to CSR
	csrrw x11, 0x1a6, x0	// Write all 0s to CSR
	csrrs x11, 0x1a6, x1	// Set all CSR bits
	csrrc x11, 0x1a6, x1	// Clear all CSR bits
	csrrw x11, 0x1a6, x31	// Restore CSR

// Testing CSR 0x1a7
	csrr x10, 0x1a7	// Read CSR
	li x9, -1
	csrrw x28, 0x1a7, x9	// Write all 1s to CSR
	csrrw x28, 0x1a7, x0	// Write all 0s to CSR
	csrrs x28, 0x1a7, x9	// Set all CSR bits
	csrrc x28, 0x1a7, x9	// Clear all CSR bits
	csrrw x28, 0x1a7, x10	// Restore CSR

// Testing CSR 0x1a8
	csrr x16, 0x1a8	// Read CSR
	li x29, -1
	csrrw x6, 0x1a8, x29	// Write all 1s to CSR
	csrrw x6, 0x1a8, x0	// Write all 0s to CSR
	csrrs x6, 0x1a8, x29	// Set all CSR bits
	csrrc x6, 0x1a8, x29	// Clear all CSR bits
	csrrw x6, 0x1a8, x16	// Restore CSR

// Testing CSR 0x1a9
	csrr x31, 0x1a9	// Read CSR
	li x6, -1
	csrrw x17, 0x1a9, x6	// Write all 1s to CSR
	csrrw x17, 0x1a9, x0	// Write all 0s to CSR
	csrrs x17, 0x1a9, x6	// Set all CSR bits
	csrrc x17, 0x1a9, x6	// Clear all CSR bits
	csrrw x17, 0x1a9, x31	// Restore CSR

// Testing CSR 0x1aa
	csrr x23, 0x1aa	// Read CSR
	li x9, -1
	csrrw x27, 0x1aa, x9	// Write all 1s to CSR
	csrrw x27, 0x1aa, x0	// Write all 0s to CSR
	csrrs x27, 0x1aa, x9	// Set all CSR bits
	csrrc x27, 0x1aa, x9	// Clear all CSR bits
	csrrw x27, 0x1aa, x23	// Restore CSR

// Testing CSR 0x1ab
	csrr x15, 0x1ab	// Read CSR
	li x24, -1
	csrrw x4, 0x1ab, x24	// Write all 1s to CSR
	csrrw x4, 0x1ab, x0	// Write all 0s to CSR
	csrrs x4, 0x1ab, x24	// Set all CSR bits
	csrrc x4, 0x1ab, x24	// Clear all CSR bits
	csrrw x4, 0x1ab, x15	// Restore CSR

// Testing CSR 0x1ac
	csrr x3, 0x1ac	// Read CSR
	li x15, -1
	csrrw x12, 0x1ac, x15	// Write all 1s to CSR
	csrrw x12, 0x1ac, x0	// Write all 0s to CSR
	csrrs x12, 0x1ac, x15	// Set all CSR bits
	csrrc x12, 0x1ac, x15	// Clear all CSR bits
	csrrw x12, 0x1ac, x3	// Restore CSR

// Testing CSR 0x1ad
	csrr x10, 0x1ad	// Read CSR
	li x30, -1
	csrrw x5, 0x1ad, x30	// Write all 1s to CSR
	csrrw x5, 0x1ad, x0	// Write all 0s to CSR
	csrrs x5, 0x1ad, x30	// Set all CSR bits
	csrrc x5, 0x1ad, x30	// Clear all CSR bits
	csrrw x5, 0x1ad, x10	// Restore CSR

// Testing CSR 0x1ae
	csrr x10, 0x1ae	// Read CSR
	li x11, -1
	csrrw x30, 0x1ae, x11	// Write all 1s to CSR
	csrrw x30, 0x1ae, x0	// Write all 0s to CSR
	csrrs x30, 0x1ae, x11	// Set all CSR bits
	csrrc x30, 0x1ae, x11	// Clear all CSR bits
	csrrw x30, 0x1ae, x10	// Restore CSR

// Testing CSR 0x1af
	csrr x3, 0x1af	// Read CSR
	li x10, -1
	csrrw x27, 0x1af, x10	// Write all 1s to CSR
	csrrw x27, 0x1af, x0	// Write all 0s to CSR
	csrrs x27, 0x1af, x10	// Set all CSR bits
	csrrc x27, 0x1af, x10	// Clear all CSR bits
	csrrw x27, 0x1af, x3	// Restore CSR

// Testing CSR 0x1b0
	csrr x12, 0x1b0	// Read CSR
	li x11, -1
	csrrw x13, 0x1b0, x11	// Write all 1s to CSR
	csrrw x13, 0x1b0, x0	// Write all 0s to CSR
	csrrs x13, 0x1b0, x11	// Set all CSR bits
	csrrc x13, 0x1b0, x11	// Clear all CSR bits
	csrrw x13, 0x1b0, x12	// Restore CSR

// Testing CSR 0x1b1
	csrr x2, 0x1b1	// Read CSR
	li x6, -1
	csrrw x19, 0x1b1, x6	// Write all 1s to CSR
	csrrw x19, 0x1b1, x0	// Write all 0s to CSR
	csrrs x19, 0x1b1, x6	// Set all CSR bits
	csrrc x19, 0x1b1, x6	// Clear all CSR bits
	csrrw x19, 0x1b1, x2	// Restore CSR

// Testing CSR 0x1b2
	csrr x13, 0x1b2	// Read CSR
	li x2, -1
	csrrw x10, 0x1b2, x2	// Write all 1s to CSR
	csrrw x10, 0x1b2, x0	// Write all 0s to CSR
	csrrs x10, 0x1b2, x2	// Set all CSR bits
	csrrc x10, 0x1b2, x2	// Clear all CSR bits
	csrrw x10, 0x1b2, x13	// Restore CSR

// Testing CSR 0x1b3
	csrr x27, 0x1b3	// Read CSR
	li x2, -1
	csrrw x14, 0x1b3, x2	// Write all 1s to CSR
	csrrw x14, 0x1b3, x0	// Write all 0s to CSR
	csrrs x14, 0x1b3, x2	// Set all CSR bits
	csrrc x14, 0x1b3, x2	// Clear all CSR bits
	csrrw x14, 0x1b3, x27	// Restore CSR

// Testing CSR 0x1b4
	csrr x11, 0x1b4	// Read CSR
	li x7, -1
	csrrw x14, 0x1b4, x7	// Write all 1s to CSR
	csrrw x14, 0x1b4, x0	// Write all 0s to CSR
	csrrs x14, 0x1b4, x7	// Set all CSR bits
	csrrc x14, 0x1b4, x7	// Clear all CSR bits
	csrrw x14, 0x1b4, x11	// Restore CSR

// Testing CSR 0x1b5
	csrr x29, 0x1b5	// Read CSR
	li x9, -1
	csrrw x21, 0x1b5, x9	// Write all 1s to CSR
	csrrw x21, 0x1b5, x0	// Write all 0s to CSR
	csrrs x21, 0x1b5, x9	// Set all CSR bits
	csrrc x21, 0x1b5, x9	// Clear all CSR bits
	csrrw x21, 0x1b5, x29	// Restore CSR

// Testing CSR 0x1b6
	csrr x27, 0x1b6	// Read CSR
	li x31, -1
	csrrw x7, 0x1b6, x31	// Write all 1s to CSR
	csrrw x7, 0x1b6, x0	// Write all 0s to CSR
	csrrs x7, 0x1b6, x31	// Set all CSR bits
	csrrc x7, 0x1b6, x31	// Clear all CSR bits
	csrrw x7, 0x1b6, x27	// Restore CSR

// Testing CSR 0x1b7
	csrr x6, 0x1b7	// Read CSR
	li x15, -1
	csrrw x25, 0x1b7, x15	// Write all 1s to CSR
	csrrw x25, 0x1b7, x0	// Write all 0s to CSR
	csrrs x25, 0x1b7, x15	// Set all CSR bits
	csrrc x25, 0x1b7, x15	// Clear all CSR bits
	csrrw x25, 0x1b7, x6	// Restore CSR

// Testing CSR 0x1b8
	csrr x25, 0x1b8	// Read CSR
	li x8, -1
	csrrw x11, 0x1b8, x8	// Write all 1s to CSR
	csrrw x11, 0x1b8, x0	// Write all 0s to CSR
	csrrs x11, 0x1b8, x8	// Set all CSR bits
	csrrc x11, 0x1b8, x8	// Clear all CSR bits
	csrrw x11, 0x1b8, x25	// Restore CSR

// Testing CSR 0x1b9
	csrr x14, 0x1b9	// Read CSR
	li x30, -1
	csrrw x2, 0x1b9, x30	// Write all 1s to CSR
	csrrw x2, 0x1b9, x0	// Write all 0s to CSR
	csrrs x2, 0x1b9, x30	// Set all CSR bits
	csrrc x2, 0x1b9, x30	// Clear all CSR bits
	csrrw x2, 0x1b9, x14	// Restore CSR

// Testing CSR 0x1ba
	csrr x1, 0x1ba	// Read CSR
	li x8, -1
	csrrw x14, 0x1ba, x8	// Write all 1s to CSR
	csrrw x14, 0x1ba, x0	// Write all 0s to CSR
	csrrs x14, 0x1ba, x8	// Set all CSR bits
	csrrc x14, 0x1ba, x8	// Clear all CSR bits
	csrrw x14, 0x1ba, x1	// Restore CSR

// Testing CSR 0x1bb
	csrr x16, 0x1bb	// Read CSR
	li x25, -1
	csrrw x8, 0x1bb, x25	// Write all 1s to CSR
	csrrw x8, 0x1bb, x0	// Write all 0s to CSR
	csrrs x8, 0x1bb, x25	// Set all CSR bits
	csrrc x8, 0x1bb, x25	// Clear all CSR bits
	csrrw x8, 0x1bb, x16	// Restore CSR

// Testing CSR 0x1bc
	csrr x11, 0x1bc	// Read CSR
	li x21, -1
	csrrw x8, 0x1bc, x21	// Write all 1s to CSR
	csrrw x8, 0x1bc, x0	// Write all 0s to CSR
	csrrs x8, 0x1bc, x21	// Set all CSR bits
	csrrc x8, 0x1bc, x21	// Clear all CSR bits
	csrrw x8, 0x1bc, x11	// Restore CSR

// Testing CSR 0x1bd
	csrr x11, 0x1bd	// Read CSR
	li x23, -1
	csrrw x25, 0x1bd, x23	// Write all 1s to CSR
	csrrw x25, 0x1bd, x0	// Write all 0s to CSR
	csrrs x25, 0x1bd, x23	// Set all CSR bits
	csrrc x25, 0x1bd, x23	// Clear all CSR bits
	csrrw x25, 0x1bd, x11	// Restore CSR

// Testing CSR 0x1be
	csrr x28, 0x1be	// Read CSR
	li x31, -1
	csrrw x9, 0x1be, x31	// Write all 1s to CSR
	csrrw x9, 0x1be, x0	// Write all 0s to CSR
	csrrs x9, 0x1be, x31	// Set all CSR bits
	csrrc x9, 0x1be, x31	// Clear all CSR bits
	csrrw x9, 0x1be, x28	// Restore CSR

// Testing CSR 0x1bf
	csrr x19, 0x1bf	// Read CSR
	li x31, -1
	csrrw x30, 0x1bf, x31	// Write all 1s to CSR
	csrrw x30, 0x1bf, x0	// Write all 0s to CSR
	csrrs x30, 0x1bf, x31	// Set all CSR bits
	csrrc x30, 0x1bf, x31	// Clear all CSR bits
	csrrw x30, 0x1bf, x19	// Restore CSR

// Testing CSR 0x1c0
	csrr x27, 0x1c0	// Read CSR
	li x10, -1
	csrrw x29, 0x1c0, x10	// Write all 1s to CSR
	csrrw x29, 0x1c0, x0	// Write all 0s to CSR
	csrrs x29, 0x1c0, x10	// Set all CSR bits
	csrrc x29, 0x1c0, x10	// Clear all CSR bits
	csrrw x29, 0x1c0, x27	// Restore CSR

// Testing CSR 0x1c1
	csrr x13, 0x1c1	// Read CSR
	li x28, -1
	csrrw x10, 0x1c1, x28	// Write all 1s to CSR
	csrrw x10, 0x1c1, x0	// Write all 0s to CSR
	csrrs x10, 0x1c1, x28	// Set all CSR bits
	csrrc x10, 0x1c1, x28	// Clear all CSR bits
	csrrw x10, 0x1c1, x13	// Restore CSR

// Testing CSR 0x1c2
	csrr x5, 0x1c2	// Read CSR
	li x4, -1
	csrrw x18, 0x1c2, x4	// Write all 1s to CSR
	csrrw x18, 0x1c2, x0	// Write all 0s to CSR
	csrrs x18, 0x1c2, x4	// Set all CSR bits
	csrrc x18, 0x1c2, x4	// Clear all CSR bits
	csrrw x18, 0x1c2, x5	// Restore CSR

// Testing CSR 0x1c3
	csrr x25, 0x1c3	// Read CSR
	li x23, -1
	csrrw x21, 0x1c3, x23	// Write all 1s to CSR
	csrrw x21, 0x1c3, x0	// Write all 0s to CSR
	csrrs x21, 0x1c3, x23	// Set all CSR bits
	csrrc x21, 0x1c3, x23	// Clear all CSR bits
	csrrw x21, 0x1c3, x25	// Restore CSR

// Testing CSR 0x1c4
	csrr x28, 0x1c4	// Read CSR
	li x14, -1
	csrrw x1, 0x1c4, x14	// Write all 1s to CSR
	csrrw x1, 0x1c4, x0	// Write all 0s to CSR
	csrrs x1, 0x1c4, x14	// Set all CSR bits
	csrrc x1, 0x1c4, x14	// Clear all CSR bits
	csrrw x1, 0x1c4, x28	// Restore CSR

// Testing CSR 0x1c5
	csrr x13, 0x1c5	// Read CSR
	li x10, -1
	csrrw x17, 0x1c5, x10	// Write all 1s to CSR
	csrrw x17, 0x1c5, x0	// Write all 0s to CSR
	csrrs x17, 0x1c5, x10	// Set all CSR bits
	csrrc x17, 0x1c5, x10	// Clear all CSR bits
	csrrw x17, 0x1c5, x13	// Restore CSR

// Testing CSR 0x1c6
	csrr x4, 0x1c6	// Read CSR
	li x22, -1
	csrrw x3, 0x1c6, x22	// Write all 1s to CSR
	csrrw x3, 0x1c6, x0	// Write all 0s to CSR
	csrrs x3, 0x1c6, x22	// Set all CSR bits
	csrrc x3, 0x1c6, x22	// Clear all CSR bits
	csrrw x3, 0x1c6, x4	// Restore CSR

// Testing CSR 0x1c7
	csrr x29, 0x1c7	// Read CSR
	li x18, -1
	csrrw x19, 0x1c7, x18	// Write all 1s to CSR
	csrrw x19, 0x1c7, x0	// Write all 0s to CSR
	csrrs x19, 0x1c7, x18	// Set all CSR bits
	csrrc x19, 0x1c7, x18	// Clear all CSR bits
	csrrw x19, 0x1c7, x29	// Restore CSR

// Testing CSR 0x1c8
	csrr x8, 0x1c8	// Read CSR
	li x6, -1
	csrrw x11, 0x1c8, x6	// Write all 1s to CSR
	csrrw x11, 0x1c8, x0	// Write all 0s to CSR
	csrrs x11, 0x1c8, x6	// Set all CSR bits
	csrrc x11, 0x1c8, x6	// Clear all CSR bits
	csrrw x11, 0x1c8, x8	// Restore CSR

// Testing CSR 0x1c9
	csrr x23, 0x1c9	// Read CSR
	li x20, -1
	csrrw x12, 0x1c9, x20	// Write all 1s to CSR
	csrrw x12, 0x1c9, x0	// Write all 0s to CSR
	csrrs x12, 0x1c9, x20	// Set all CSR bits
	csrrc x12, 0x1c9, x20	// Clear all CSR bits
	csrrw x12, 0x1c9, x23	// Restore CSR

// Testing CSR 0x1ca
	csrr x19, 0x1ca	// Read CSR
	li x23, -1
	csrrw x20, 0x1ca, x23	// Write all 1s to CSR
	csrrw x20, 0x1ca, x0	// Write all 0s to CSR
	csrrs x20, 0x1ca, x23	// Set all CSR bits
	csrrc x20, 0x1ca, x23	// Clear all CSR bits
	csrrw x20, 0x1ca, x19	// Restore CSR

// Testing CSR 0x1cb
	csrr x1, 0x1cb	// Read CSR
	li x14, -1
	csrrw x15, 0x1cb, x14	// Write all 1s to CSR
	csrrw x15, 0x1cb, x0	// Write all 0s to CSR
	csrrs x15, 0x1cb, x14	// Set all CSR bits
	csrrc x15, 0x1cb, x14	// Clear all CSR bits
	csrrw x15, 0x1cb, x1	// Restore CSR

// Testing CSR 0x1cc
	csrr x24, 0x1cc	// Read CSR
	li x4, -1
	csrrw x22, 0x1cc, x4	// Write all 1s to CSR
	csrrw x22, 0x1cc, x0	// Write all 0s to CSR
	csrrs x22, 0x1cc, x4	// Set all CSR bits
	csrrc x22, 0x1cc, x4	// Clear all CSR bits
	csrrw x22, 0x1cc, x24	// Restore CSR

// Testing CSR 0x1cd
	csrr x6, 0x1cd	// Read CSR
	li x15, -1
	csrrw x2, 0x1cd, x15	// Write all 1s to CSR
	csrrw x2, 0x1cd, x0	// Write all 0s to CSR
	csrrs x2, 0x1cd, x15	// Set all CSR bits
	csrrc x2, 0x1cd, x15	// Clear all CSR bits
	csrrw x2, 0x1cd, x6	// Restore CSR

// Testing CSR 0x1ce
	csrr x27, 0x1ce	// Read CSR
	li x8, -1
	csrrw x21, 0x1ce, x8	// Write all 1s to CSR
	csrrw x21, 0x1ce, x0	// Write all 0s to CSR
	csrrs x21, 0x1ce, x8	// Set all CSR bits
	csrrc x21, 0x1ce, x8	// Clear all CSR bits
	csrrw x21, 0x1ce, x27	// Restore CSR

// Testing CSR 0x1cf
	csrr x18, 0x1cf	// Read CSR
	li x8, -1
	csrrw x17, 0x1cf, x8	// Write all 1s to CSR
	csrrw x17, 0x1cf, x0	// Write all 0s to CSR
	csrrs x17, 0x1cf, x8	// Set all CSR bits
	csrrc x17, 0x1cf, x8	// Clear all CSR bits
	csrrw x17, 0x1cf, x18	// Restore CSR

// Testing CSR 0x1d0
	csrr x6, 0x1d0	// Read CSR
	li x24, -1
	csrrw x25, 0x1d0, x24	// Write all 1s to CSR
	csrrw x25, 0x1d0, x0	// Write all 0s to CSR
	csrrs x25, 0x1d0, x24	// Set all CSR bits
	csrrc x25, 0x1d0, x24	// Clear all CSR bits
	csrrw x25, 0x1d0, x6	// Restore CSR

// Testing CSR 0x1d1
	csrr x16, 0x1d1	// Read CSR
	li x12, -1
	csrrw x3, 0x1d1, x12	// Write all 1s to CSR
	csrrw x3, 0x1d1, x0	// Write all 0s to CSR
	csrrs x3, 0x1d1, x12	// Set all CSR bits
	csrrc x3, 0x1d1, x12	// Clear all CSR bits
	csrrw x3, 0x1d1, x16	// Restore CSR

// Testing CSR 0x1d2
	csrr x23, 0x1d2	// Read CSR
	li x31, -1
	csrrw x29, 0x1d2, x31	// Write all 1s to CSR
	csrrw x29, 0x1d2, x0	// Write all 0s to CSR
	csrrs x29, 0x1d2, x31	// Set all CSR bits
	csrrc x29, 0x1d2, x31	// Clear all CSR bits
	csrrw x29, 0x1d2, x23	// Restore CSR

// Testing CSR 0x1d3
	csrr x9, 0x1d3	// Read CSR
	li x20, -1
	csrrw x22, 0x1d3, x20	// Write all 1s to CSR
	csrrw x22, 0x1d3, x0	// Write all 0s to CSR
	csrrs x22, 0x1d3, x20	// Set all CSR bits
	csrrc x22, 0x1d3, x20	// Clear all CSR bits
	csrrw x22, 0x1d3, x9	// Restore CSR

// Testing CSR 0x1d4
	csrr x6, 0x1d4	// Read CSR
	li x16, -1
	csrrw x22, 0x1d4, x16	// Write all 1s to CSR
	csrrw x22, 0x1d4, x0	// Write all 0s to CSR
	csrrs x22, 0x1d4, x16	// Set all CSR bits
	csrrc x22, 0x1d4, x16	// Clear all CSR bits
	csrrw x22, 0x1d4, x6	// Restore CSR

// Testing CSR 0x1d5
	csrr x4, 0x1d5	// Read CSR
	li x19, -1
	csrrw x21, 0x1d5, x19	// Write all 1s to CSR
	csrrw x21, 0x1d5, x0	// Write all 0s to CSR
	csrrs x21, 0x1d5, x19	// Set all CSR bits
	csrrc x21, 0x1d5, x19	// Clear all CSR bits
	csrrw x21, 0x1d5, x4	// Restore CSR

// Testing CSR 0x1d6
	csrr x19, 0x1d6	// Read CSR
	li x29, -1
	csrrw x4, 0x1d6, x29	// Write all 1s to CSR
	csrrw x4, 0x1d6, x0	// Write all 0s to CSR
	csrrs x4, 0x1d6, x29	// Set all CSR bits
	csrrc x4, 0x1d6, x29	// Clear all CSR bits
	csrrw x4, 0x1d6, x19	// Restore CSR

// Testing CSR 0x1d7
	csrr x28, 0x1d7	// Read CSR
	li x18, -1
	csrrw x24, 0x1d7, x18	// Write all 1s to CSR
	csrrw x24, 0x1d7, x0	// Write all 0s to CSR
	csrrs x24, 0x1d7, x18	// Set all CSR bits
	csrrc x24, 0x1d7, x18	// Clear all CSR bits
	csrrw x24, 0x1d7, x28	// Restore CSR

// Testing CSR 0x1d8
	csrr x18, 0x1d8	// Read CSR
	li x24, -1
	csrrw x15, 0x1d8, x24	// Write all 1s to CSR
	csrrw x15, 0x1d8, x0	// Write all 0s to CSR
	csrrs x15, 0x1d8, x24	// Set all CSR bits
	csrrc x15, 0x1d8, x24	// Clear all CSR bits
	csrrw x15, 0x1d8, x18	// Restore CSR

// Testing CSR 0x1d9
	csrr x24, 0x1d9	// Read CSR
	li x30, -1
	csrrw x5, 0x1d9, x30	// Write all 1s to CSR
	csrrw x5, 0x1d9, x0	// Write all 0s to CSR
	csrrs x5, 0x1d9, x30	// Set all CSR bits
	csrrc x5, 0x1d9, x30	// Clear all CSR bits
	csrrw x5, 0x1d9, x24	// Restore CSR

// Testing CSR 0x1da
	csrr x17, 0x1da	// Read CSR
	li x27, -1
	csrrw x23, 0x1da, x27	// Write all 1s to CSR
	csrrw x23, 0x1da, x0	// Write all 0s to CSR
	csrrs x23, 0x1da, x27	// Set all CSR bits
	csrrc x23, 0x1da, x27	// Clear all CSR bits
	csrrw x23, 0x1da, x17	// Restore CSR

// Testing CSR 0x1db
	csrr x12, 0x1db	// Read CSR
	li x25, -1
	csrrw x17, 0x1db, x25	// Write all 1s to CSR
	csrrw x17, 0x1db, x0	// Write all 0s to CSR
	csrrs x17, 0x1db, x25	// Set all CSR bits
	csrrc x17, 0x1db, x25	// Clear all CSR bits
	csrrw x17, 0x1db, x12	// Restore CSR

// Testing CSR 0x1dc
	csrr x17, 0x1dc	// Read CSR
	li x11, -1
	csrrw x19, 0x1dc, x11	// Write all 1s to CSR
	csrrw x19, 0x1dc, x0	// Write all 0s to CSR
	csrrs x19, 0x1dc, x11	// Set all CSR bits
	csrrc x19, 0x1dc, x11	// Clear all CSR bits
	csrrw x19, 0x1dc, x17	// Restore CSR

// Testing CSR 0x1dd
	csrr x18, 0x1dd	// Read CSR
	li x13, -1
	csrrw x29, 0x1dd, x13	// Write all 1s to CSR
	csrrw x29, 0x1dd, x0	// Write all 0s to CSR
	csrrs x29, 0x1dd, x13	// Set all CSR bits
	csrrc x29, 0x1dd, x13	// Clear all CSR bits
	csrrw x29, 0x1dd, x18	// Restore CSR

// Testing CSR 0x1de
	csrr x4, 0x1de	// Read CSR
	li x27, -1
	csrrw x29, 0x1de, x27	// Write all 1s to CSR
	csrrw x29, 0x1de, x0	// Write all 0s to CSR
	csrrs x29, 0x1de, x27	// Set all CSR bits
	csrrc x29, 0x1de, x27	// Clear all CSR bits
	csrrw x29, 0x1de, x4	// Restore CSR

// Testing CSR 0x1df
	csrr x5, 0x1df	// Read CSR
	li x12, -1
	csrrw x16, 0x1df, x12	// Write all 1s to CSR
	csrrw x16, 0x1df, x0	// Write all 0s to CSR
	csrrs x16, 0x1df, x12	// Set all CSR bits
	csrrc x16, 0x1df, x12	// Clear all CSR bits
	csrrw x16, 0x1df, x5	// Restore CSR

// Testing CSR 0x1e0
	csrr x8, 0x1e0	// Read CSR
	li x14, -1
	csrrw x15, 0x1e0, x14	// Write all 1s to CSR
	csrrw x15, 0x1e0, x0	// Write all 0s to CSR
	csrrs x15, 0x1e0, x14	// Set all CSR bits
	csrrc x15, 0x1e0, x14	// Clear all CSR bits
	csrrw x15, 0x1e0, x8	// Restore CSR

// Testing CSR 0x1e1
	csrr x22, 0x1e1	// Read CSR
	li x17, -1
	csrrw x2, 0x1e1, x17	// Write all 1s to CSR
	csrrw x2, 0x1e1, x0	// Write all 0s to CSR
	csrrs x2, 0x1e1, x17	// Set all CSR bits
	csrrc x2, 0x1e1, x17	// Clear all CSR bits
	csrrw x2, 0x1e1, x22	// Restore CSR

// Testing CSR 0x1e2
	csrr x23, 0x1e2	// Read CSR
	li x24, -1
	csrrw x20, 0x1e2, x24	// Write all 1s to CSR
	csrrw x20, 0x1e2, x0	// Write all 0s to CSR
	csrrs x20, 0x1e2, x24	// Set all CSR bits
	csrrc x20, 0x1e2, x24	// Clear all CSR bits
	csrrw x20, 0x1e2, x23	// Restore CSR

// Testing CSR 0x1e3
	csrr x2, 0x1e3	// Read CSR
	li x6, -1
	csrrw x21, 0x1e3, x6	// Write all 1s to CSR
	csrrw x21, 0x1e3, x0	// Write all 0s to CSR
	csrrs x21, 0x1e3, x6	// Set all CSR bits
	csrrc x21, 0x1e3, x6	// Clear all CSR bits
	csrrw x21, 0x1e3, x2	// Restore CSR

// Testing CSR 0x1e4
	csrr x14, 0x1e4	// Read CSR
	li x6, -1
	csrrw x17, 0x1e4, x6	// Write all 1s to CSR
	csrrw x17, 0x1e4, x0	// Write all 0s to CSR
	csrrs x17, 0x1e4, x6	// Set all CSR bits
	csrrc x17, 0x1e4, x6	// Clear all CSR bits
	csrrw x17, 0x1e4, x14	// Restore CSR

// Testing CSR 0x1e5
	csrr x26, 0x1e5	// Read CSR
	li x19, -1
	csrrw x14, 0x1e5, x19	// Write all 1s to CSR
	csrrw x14, 0x1e5, x0	// Write all 0s to CSR
	csrrs x14, 0x1e5, x19	// Set all CSR bits
	csrrc x14, 0x1e5, x19	// Clear all CSR bits
	csrrw x14, 0x1e5, x26	// Restore CSR

// Testing CSR 0x1e6
	csrr x6, 0x1e6	// Read CSR
	li x23, -1
	csrrw x13, 0x1e6, x23	// Write all 1s to CSR
	csrrw x13, 0x1e6, x0	// Write all 0s to CSR
	csrrs x13, 0x1e6, x23	// Set all CSR bits
	csrrc x13, 0x1e6, x23	// Clear all CSR bits
	csrrw x13, 0x1e6, x6	// Restore CSR

// Testing CSR 0x1e7
	csrr x3, 0x1e7	// Read CSR
	li x9, -1
	csrrw x8, 0x1e7, x9	// Write all 1s to CSR
	csrrw x8, 0x1e7, x0	// Write all 0s to CSR
	csrrs x8, 0x1e7, x9	// Set all CSR bits
	csrrc x8, 0x1e7, x9	// Clear all CSR bits
	csrrw x8, 0x1e7, x3	// Restore CSR

// Testing CSR 0x1e8
	csrr x5, 0x1e8	// Read CSR
	li x22, -1
	csrrw x7, 0x1e8, x22	// Write all 1s to CSR
	csrrw x7, 0x1e8, x0	// Write all 0s to CSR
	csrrs x7, 0x1e8, x22	// Set all CSR bits
	csrrc x7, 0x1e8, x22	// Clear all CSR bits
	csrrw x7, 0x1e8, x5	// Restore CSR

// Testing CSR 0x1e9
	csrr x23, 0x1e9	// Read CSR
	li x15, -1
	csrrw x6, 0x1e9, x15	// Write all 1s to CSR
	csrrw x6, 0x1e9, x0	// Write all 0s to CSR
	csrrs x6, 0x1e9, x15	// Set all CSR bits
	csrrc x6, 0x1e9, x15	// Clear all CSR bits
	csrrw x6, 0x1e9, x23	// Restore CSR

// Testing CSR 0x1ea
	csrr x16, 0x1ea	// Read CSR
	li x22, -1
	csrrw x8, 0x1ea, x22	// Write all 1s to CSR
	csrrw x8, 0x1ea, x0	// Write all 0s to CSR
	csrrs x8, 0x1ea, x22	// Set all CSR bits
	csrrc x8, 0x1ea, x22	// Clear all CSR bits
	csrrw x8, 0x1ea, x16	// Restore CSR

// Testing CSR 0x1eb
	csrr x2, 0x1eb	// Read CSR
	li x24, -1
	csrrw x7, 0x1eb, x24	// Write all 1s to CSR
	csrrw x7, 0x1eb, x0	// Write all 0s to CSR
	csrrs x7, 0x1eb, x24	// Set all CSR bits
	csrrc x7, 0x1eb, x24	// Clear all CSR bits
	csrrw x7, 0x1eb, x2	// Restore CSR

// Testing CSR 0x1ec
	csrr x21, 0x1ec	// Read CSR
	li x2, -1
	csrrw x9, 0x1ec, x2	// Write all 1s to CSR
	csrrw x9, 0x1ec, x0	// Write all 0s to CSR
	csrrs x9, 0x1ec, x2	// Set all CSR bits
	csrrc x9, 0x1ec, x2	// Clear all CSR bits
	csrrw x9, 0x1ec, x21	// Restore CSR

// Testing CSR 0x1ed
	csrr x2, 0x1ed	// Read CSR
	li x18, -1
	csrrw x12, 0x1ed, x18	// Write all 1s to CSR
	csrrw x12, 0x1ed, x0	// Write all 0s to CSR
	csrrs x12, 0x1ed, x18	// Set all CSR bits
	csrrc x12, 0x1ed, x18	// Clear all CSR bits
	csrrw x12, 0x1ed, x2	// Restore CSR

// Testing CSR 0x1ee
	csrr x18, 0x1ee	// Read CSR
	li x7, -1
	csrrw x22, 0x1ee, x7	// Write all 1s to CSR
	csrrw x22, 0x1ee, x0	// Write all 0s to CSR
	csrrs x22, 0x1ee, x7	// Set all CSR bits
	csrrc x22, 0x1ee, x7	// Clear all CSR bits
	csrrw x22, 0x1ee, x18	// Restore CSR

// Testing CSR 0x1ef
	csrr x27, 0x1ef	// Read CSR
	li x29, -1
	csrrw x24, 0x1ef, x29	// Write all 1s to CSR
	csrrw x24, 0x1ef, x0	// Write all 0s to CSR
	csrrs x24, 0x1ef, x29	// Set all CSR bits
	csrrc x24, 0x1ef, x29	// Clear all CSR bits
	csrrw x24, 0x1ef, x27	// Restore CSR

// Testing CSR 0x1f0
	csrr x29, 0x1f0	// Read CSR
	li x15, -1
	csrrw x17, 0x1f0, x15	// Write all 1s to CSR
	csrrw x17, 0x1f0, x0	// Write all 0s to CSR
	csrrs x17, 0x1f0, x15	// Set all CSR bits
	csrrc x17, 0x1f0, x15	// Clear all CSR bits
	csrrw x17, 0x1f0, x29	// Restore CSR

// Testing CSR 0x1f1
	csrr x7, 0x1f1	// Read CSR
	li x23, -1
	csrrw x4, 0x1f1, x23	// Write all 1s to CSR
	csrrw x4, 0x1f1, x0	// Write all 0s to CSR
	csrrs x4, 0x1f1, x23	// Set all CSR bits
	csrrc x4, 0x1f1, x23	// Clear all CSR bits
	csrrw x4, 0x1f1, x7	// Restore CSR

// Testing CSR 0x1f2
	csrr x27, 0x1f2	// Read CSR
	li x12, -1
	csrrw x10, 0x1f2, x12	// Write all 1s to CSR
	csrrw x10, 0x1f2, x0	// Write all 0s to CSR
	csrrs x10, 0x1f2, x12	// Set all CSR bits
	csrrc x10, 0x1f2, x12	// Clear all CSR bits
	csrrw x10, 0x1f2, x27	// Restore CSR

// Testing CSR 0x1f3
	csrr x7, 0x1f3	// Read CSR
	li x8, -1
	csrrw x6, 0x1f3, x8	// Write all 1s to CSR
	csrrw x6, 0x1f3, x0	// Write all 0s to CSR
	csrrs x6, 0x1f3, x8	// Set all CSR bits
	csrrc x6, 0x1f3, x8	// Clear all CSR bits
	csrrw x6, 0x1f3, x7	// Restore CSR

// Testing CSR 0x1f4
	csrr x4, 0x1f4	// Read CSR
	li x25, -1
	csrrw x14, 0x1f4, x25	// Write all 1s to CSR
	csrrw x14, 0x1f4, x0	// Write all 0s to CSR
	csrrs x14, 0x1f4, x25	// Set all CSR bits
	csrrc x14, 0x1f4, x25	// Clear all CSR bits
	csrrw x14, 0x1f4, x4	// Restore CSR

// Testing CSR 0x1f5
	csrr x12, 0x1f5	// Read CSR
	li x26, -1
	csrrw x31, 0x1f5, x26	// Write all 1s to CSR
	csrrw x31, 0x1f5, x0	// Write all 0s to CSR
	csrrs x31, 0x1f5, x26	// Set all CSR bits
	csrrc x31, 0x1f5, x26	// Clear all CSR bits
	csrrw x31, 0x1f5, x12	// Restore CSR

// Testing CSR 0x1f6
	csrr x19, 0x1f6	// Read CSR
	li x28, -1
	csrrw x15, 0x1f6, x28	// Write all 1s to CSR
	csrrw x15, 0x1f6, x0	// Write all 0s to CSR
	csrrs x15, 0x1f6, x28	// Set all CSR bits
	csrrc x15, 0x1f6, x28	// Clear all CSR bits
	csrrw x15, 0x1f6, x19	// Restore CSR

// Testing CSR 0x1f7
	csrr x10, 0x1f7	// Read CSR
	li x27, -1
	csrrw x5, 0x1f7, x27	// Write all 1s to CSR
	csrrw x5, 0x1f7, x0	// Write all 0s to CSR
	csrrs x5, 0x1f7, x27	// Set all CSR bits
	csrrc x5, 0x1f7, x27	// Clear all CSR bits
	csrrw x5, 0x1f7, x10	// Restore CSR

// Testing CSR 0x1f8
	csrr x26, 0x1f8	// Read CSR
	li x2, -1
	csrrw x28, 0x1f8, x2	// Write all 1s to CSR
	csrrw x28, 0x1f8, x0	// Write all 0s to CSR
	csrrs x28, 0x1f8, x2	// Set all CSR bits
	csrrc x28, 0x1f8, x2	// Clear all CSR bits
	csrrw x28, 0x1f8, x26	// Restore CSR

// Testing CSR 0x1f9
	csrr x24, 0x1f9	// Read CSR
	li x21, -1
	csrrw x19, 0x1f9, x21	// Write all 1s to CSR
	csrrw x19, 0x1f9, x0	// Write all 0s to CSR
	csrrs x19, 0x1f9, x21	// Set all CSR bits
	csrrc x19, 0x1f9, x21	// Clear all CSR bits
	csrrw x19, 0x1f9, x24	// Restore CSR

// Testing CSR 0x1fa
	csrr x29, 0x1fa	// Read CSR
	li x21, -1
	csrrw x23, 0x1fa, x21	// Write all 1s to CSR
	csrrw x23, 0x1fa, x0	// Write all 0s to CSR
	csrrs x23, 0x1fa, x21	// Set all CSR bits
	csrrc x23, 0x1fa, x21	// Clear all CSR bits
	csrrw x23, 0x1fa, x29	// Restore CSR

// Testing CSR 0x1fb
	csrr x21, 0x1fb	// Read CSR
	li x1, -1
	csrrw x28, 0x1fb, x1	// Write all 1s to CSR
	csrrw x28, 0x1fb, x0	// Write all 0s to CSR
	csrrs x28, 0x1fb, x1	// Set all CSR bits
	csrrc x28, 0x1fb, x1	// Clear all CSR bits
	csrrw x28, 0x1fb, x21	// Restore CSR

// Testing CSR 0x1fc
	csrr x18, 0x1fc	// Read CSR
	li x20, -1
	csrrw x3, 0x1fc, x20	// Write all 1s to CSR
	csrrw x3, 0x1fc, x0	// Write all 0s to CSR
	csrrs x3, 0x1fc, x20	// Set all CSR bits
	csrrc x3, 0x1fc, x20	// Clear all CSR bits
	csrrw x3, 0x1fc, x18	// Restore CSR

// Testing CSR 0x1fd
	csrr x14, 0x1fd	// Read CSR
	li x12, -1
	csrrw x15, 0x1fd, x12	// Write all 1s to CSR
	csrrw x15, 0x1fd, x0	// Write all 0s to CSR
	csrrs x15, 0x1fd, x12	// Set all CSR bits
	csrrc x15, 0x1fd, x12	// Clear all CSR bits
	csrrw x15, 0x1fd, x14	// Restore CSR

// Testing CSR 0x1fe
	csrr x22, 0x1fe	// Read CSR
	li x2, -1
	csrrw x28, 0x1fe, x2	// Write all 1s to CSR
	csrrw x28, 0x1fe, x0	// Write all 0s to CSR
	csrrs x28, 0x1fe, x2	// Set all CSR bits
	csrrc x28, 0x1fe, x2	// Clear all CSR bits
	csrrw x28, 0x1fe, x22	// Restore CSR

// Testing CSR 0x1ff
	csrr x14, 0x1ff	// Read CSR
	li x10, -1
	csrrw x13, 0x1ff, x10	// Write all 1s to CSR
	csrrw x13, 0x1ff, x0	// Write all 0s to CSR
	csrrs x13, 0x1ff, x10	// Set all CSR bits
	csrrc x13, 0x1ff, x10	// Clear all CSR bits
	csrrw x13, 0x1ff, x14	// Restore CSR

// Testing CSR 0x200
	csrr x6, 0x200	// Read CSR
	li x7, -1
	csrrw x15, 0x200, x7	// Write all 1s to CSR
	csrrw x15, 0x200, x0	// Write all 0s to CSR
	csrrs x15, 0x200, x7	// Set all CSR bits
	csrrc x15, 0x200, x7	// Clear all CSR bits
	csrrw x15, 0x200, x6	// Restore CSR

// Testing CSR 0x201
	csrr x2, 0x201	// Read CSR
	li x4, -1
	csrrw x23, 0x201, x4	// Write all 1s to CSR
	csrrw x23, 0x201, x0	// Write all 0s to CSR
	csrrs x23, 0x201, x4	// Set all CSR bits
	csrrc x23, 0x201, x4	// Clear all CSR bits
	csrrw x23, 0x201, x2	// Restore CSR

// Testing CSR 0x202
	csrr x31, 0x202	// Read CSR
	li x21, -1
	csrrw x5, 0x202, x21	// Write all 1s to CSR
	csrrw x5, 0x202, x0	// Write all 0s to CSR
	csrrs x5, 0x202, x21	// Set all CSR bits
	csrrc x5, 0x202, x21	// Clear all CSR bits
	csrrw x5, 0x202, x31	// Restore CSR

// Testing CSR 0x203
	csrr x18, 0x203	// Read CSR
	li x10, -1
	csrrw x16, 0x203, x10	// Write all 1s to CSR
	csrrw x16, 0x203, x0	// Write all 0s to CSR
	csrrs x16, 0x203, x10	// Set all CSR bits
	csrrc x16, 0x203, x10	// Clear all CSR bits
	csrrw x16, 0x203, x18	// Restore CSR

// Testing CSR 0x204
	csrr x19, 0x204	// Read CSR
	li x30, -1
	csrrw x25, 0x204, x30	// Write all 1s to CSR
	csrrw x25, 0x204, x0	// Write all 0s to CSR
	csrrs x25, 0x204, x30	// Set all CSR bits
	csrrc x25, 0x204, x30	// Clear all CSR bits
	csrrw x25, 0x204, x19	// Restore CSR

// Testing CSR 0x205
	csrr x27, 0x205	// Read CSR
	li x15, -1
	csrrw x12, 0x205, x15	// Write all 1s to CSR
	csrrw x12, 0x205, x0	// Write all 0s to CSR
	csrrs x12, 0x205, x15	// Set all CSR bits
	csrrc x12, 0x205, x15	// Clear all CSR bits
	csrrw x12, 0x205, x27	// Restore CSR

// Testing CSR 0x206
	csrr x30, 0x206	// Read CSR
	li x10, -1
	csrrw x3, 0x206, x10	// Write all 1s to CSR
	csrrw x3, 0x206, x0	// Write all 0s to CSR
	csrrs x3, 0x206, x10	// Set all CSR bits
	csrrc x3, 0x206, x10	// Clear all CSR bits
	csrrw x3, 0x206, x30	// Restore CSR

// Testing CSR 0x207
	csrr x22, 0x207	// Read CSR
	li x8, -1
	csrrw x19, 0x207, x8	// Write all 1s to CSR
	csrrw x19, 0x207, x0	// Write all 0s to CSR
	csrrs x19, 0x207, x8	// Set all CSR bits
	csrrc x19, 0x207, x8	// Clear all CSR bits
	csrrw x19, 0x207, x22	// Restore CSR

// Testing CSR 0x208
	csrr x25, 0x208	// Read CSR
	li x23, -1
	csrrw x19, 0x208, x23	// Write all 1s to CSR
	csrrw x19, 0x208, x0	// Write all 0s to CSR
	csrrs x19, 0x208, x23	// Set all CSR bits
	csrrc x19, 0x208, x23	// Clear all CSR bits
	csrrw x19, 0x208, x25	// Restore CSR

// Testing CSR 0x209
	csrr x18, 0x209	// Read CSR
	li x13, -1
	csrrw x27, 0x209, x13	// Write all 1s to CSR
	csrrw x27, 0x209, x0	// Write all 0s to CSR
	csrrs x27, 0x209, x13	// Set all CSR bits
	csrrc x27, 0x209, x13	// Clear all CSR bits
	csrrw x27, 0x209, x18	// Restore CSR

// Testing CSR 0x20a
	csrr x13, 0x20a	// Read CSR
	li x24, -1
	csrrw x11, 0x20a, x24	// Write all 1s to CSR
	csrrw x11, 0x20a, x0	// Write all 0s to CSR
	csrrs x11, 0x20a, x24	// Set all CSR bits
	csrrc x11, 0x20a, x24	// Clear all CSR bits
	csrrw x11, 0x20a, x13	// Restore CSR

// Testing CSR 0x20b
	csrr x9, 0x20b	// Read CSR
	li x22, -1
	csrrw x13, 0x20b, x22	// Write all 1s to CSR
	csrrw x13, 0x20b, x0	// Write all 0s to CSR
	csrrs x13, 0x20b, x22	// Set all CSR bits
	csrrc x13, 0x20b, x22	// Clear all CSR bits
	csrrw x13, 0x20b, x9	// Restore CSR

// Testing CSR 0x20c
	csrr x5, 0x20c	// Read CSR
	li x14, -1
	csrrw x15, 0x20c, x14	// Write all 1s to CSR
	csrrw x15, 0x20c, x0	// Write all 0s to CSR
	csrrs x15, 0x20c, x14	// Set all CSR bits
	csrrc x15, 0x20c, x14	// Clear all CSR bits
	csrrw x15, 0x20c, x5	// Restore CSR

// Testing CSR 0x20d
	csrr x13, 0x20d	// Read CSR
	li x22, -1
	csrrw x10, 0x20d, x22	// Write all 1s to CSR
	csrrw x10, 0x20d, x0	// Write all 0s to CSR
	csrrs x10, 0x20d, x22	// Set all CSR bits
	csrrc x10, 0x20d, x22	// Clear all CSR bits
	csrrw x10, 0x20d, x13	// Restore CSR

// Testing CSR 0x20e
	csrr x8, 0x20e	// Read CSR
	li x22, -1
	csrrw x10, 0x20e, x22	// Write all 1s to CSR
	csrrw x10, 0x20e, x0	// Write all 0s to CSR
	csrrs x10, 0x20e, x22	// Set all CSR bits
	csrrc x10, 0x20e, x22	// Clear all CSR bits
	csrrw x10, 0x20e, x8	// Restore CSR

// Testing CSR 0x20f
	csrr x24, 0x20f	// Read CSR
	li x26, -1
	csrrw x7, 0x20f, x26	// Write all 1s to CSR
	csrrw x7, 0x20f, x0	// Write all 0s to CSR
	csrrs x7, 0x20f, x26	// Set all CSR bits
	csrrc x7, 0x20f, x26	// Clear all CSR bits
	csrrw x7, 0x20f, x24	// Restore CSR

// Testing CSR 0x210
	csrr x23, 0x210	// Read CSR
	li x2, -1
	csrrw x6, 0x210, x2	// Write all 1s to CSR
	csrrw x6, 0x210, x0	// Write all 0s to CSR
	csrrs x6, 0x210, x2	// Set all CSR bits
	csrrc x6, 0x210, x2	// Clear all CSR bits
	csrrw x6, 0x210, x23	// Restore CSR

// Testing CSR 0x211
	csrr x28, 0x211	// Read CSR
	li x1, -1
	csrrw x12, 0x211, x1	// Write all 1s to CSR
	csrrw x12, 0x211, x0	// Write all 0s to CSR
	csrrs x12, 0x211, x1	// Set all CSR bits
	csrrc x12, 0x211, x1	// Clear all CSR bits
	csrrw x12, 0x211, x28	// Restore CSR

// Testing CSR 0x212
	csrr x29, 0x212	// Read CSR
	li x31, -1
	csrrw x13, 0x212, x31	// Write all 1s to CSR
	csrrw x13, 0x212, x0	// Write all 0s to CSR
	csrrs x13, 0x212, x31	// Set all CSR bits
	csrrc x13, 0x212, x31	// Clear all CSR bits
	csrrw x13, 0x212, x29	// Restore CSR

// Testing CSR 0x213
	csrr x18, 0x213	// Read CSR
	li x5, -1
	csrrw x21, 0x213, x5	// Write all 1s to CSR
	csrrw x21, 0x213, x0	// Write all 0s to CSR
	csrrs x21, 0x213, x5	// Set all CSR bits
	csrrc x21, 0x213, x5	// Clear all CSR bits
	csrrw x21, 0x213, x18	// Restore CSR

// Testing CSR 0x214
	csrr x7, 0x214	// Read CSR
	li x29, -1
	csrrw x18, 0x214, x29	// Write all 1s to CSR
	csrrw x18, 0x214, x0	// Write all 0s to CSR
	csrrs x18, 0x214, x29	// Set all CSR bits
	csrrc x18, 0x214, x29	// Clear all CSR bits
	csrrw x18, 0x214, x7	// Restore CSR

// Testing CSR 0x215
	csrr x21, 0x215	// Read CSR
	li x13, -1
	csrrw x29, 0x215, x13	// Write all 1s to CSR
	csrrw x29, 0x215, x0	// Write all 0s to CSR
	csrrs x29, 0x215, x13	// Set all CSR bits
	csrrc x29, 0x215, x13	// Clear all CSR bits
	csrrw x29, 0x215, x21	// Restore CSR

// Testing CSR 0x216
	csrr x7, 0x216	// Read CSR
	li x1, -1
	csrrw x4, 0x216, x1	// Write all 1s to CSR
	csrrw x4, 0x216, x0	// Write all 0s to CSR
	csrrs x4, 0x216, x1	// Set all CSR bits
	csrrc x4, 0x216, x1	// Clear all CSR bits
	csrrw x4, 0x216, x7	// Restore CSR

// Testing CSR 0x217
	csrr x4, 0x217	// Read CSR
	li x30, -1
	csrrw x23, 0x217, x30	// Write all 1s to CSR
	csrrw x23, 0x217, x0	// Write all 0s to CSR
	csrrs x23, 0x217, x30	// Set all CSR bits
	csrrc x23, 0x217, x30	// Clear all CSR bits
	csrrw x23, 0x217, x4	// Restore CSR

// Testing CSR 0x218
	csrr x28, 0x218	// Read CSR
	li x23, -1
	csrrw x7, 0x218, x23	// Write all 1s to CSR
	csrrw x7, 0x218, x0	// Write all 0s to CSR
	csrrs x7, 0x218, x23	// Set all CSR bits
	csrrc x7, 0x218, x23	// Clear all CSR bits
	csrrw x7, 0x218, x28	// Restore CSR

// Testing CSR 0x219
	csrr x4, 0x219	// Read CSR
	li x2, -1
	csrrw x14, 0x219, x2	// Write all 1s to CSR
	csrrw x14, 0x219, x0	// Write all 0s to CSR
	csrrs x14, 0x219, x2	// Set all CSR bits
	csrrc x14, 0x219, x2	// Clear all CSR bits
	csrrw x14, 0x219, x4	// Restore CSR

// Testing CSR 0x21a
	csrr x9, 0x21a	// Read CSR
	li x20, -1
	csrrw x25, 0x21a, x20	// Write all 1s to CSR
	csrrw x25, 0x21a, x0	// Write all 0s to CSR
	csrrs x25, 0x21a, x20	// Set all CSR bits
	csrrc x25, 0x21a, x20	// Clear all CSR bits
	csrrw x25, 0x21a, x9	// Restore CSR

// Testing CSR 0x21b
	csrr x20, 0x21b	// Read CSR
	li x5, -1
	csrrw x25, 0x21b, x5	// Write all 1s to CSR
	csrrw x25, 0x21b, x0	// Write all 0s to CSR
	csrrs x25, 0x21b, x5	// Set all CSR bits
	csrrc x25, 0x21b, x5	// Clear all CSR bits
	csrrw x25, 0x21b, x20	// Restore CSR

// Testing CSR 0x21c
	csrr x29, 0x21c	// Read CSR
	li x5, -1
	csrrw x20, 0x21c, x5	// Write all 1s to CSR
	csrrw x20, 0x21c, x0	// Write all 0s to CSR
	csrrs x20, 0x21c, x5	// Set all CSR bits
	csrrc x20, 0x21c, x5	// Clear all CSR bits
	csrrw x20, 0x21c, x29	// Restore CSR

// Testing CSR 0x21d
	csrr x30, 0x21d	// Read CSR
	li x8, -1
	csrrw x12, 0x21d, x8	// Write all 1s to CSR
	csrrw x12, 0x21d, x0	// Write all 0s to CSR
	csrrs x12, 0x21d, x8	// Set all CSR bits
	csrrc x12, 0x21d, x8	// Clear all CSR bits
	csrrw x12, 0x21d, x30	// Restore CSR

// Testing CSR 0x21e
	csrr x16, 0x21e	// Read CSR
	li x20, -1
	csrrw x18, 0x21e, x20	// Write all 1s to CSR
	csrrw x18, 0x21e, x0	// Write all 0s to CSR
	csrrs x18, 0x21e, x20	// Set all CSR bits
	csrrc x18, 0x21e, x20	// Clear all CSR bits
	csrrw x18, 0x21e, x16	// Restore CSR

// Testing CSR 0x21f
	csrr x15, 0x21f	// Read CSR
	li x12, -1
	csrrw x5, 0x21f, x12	// Write all 1s to CSR
	csrrw x5, 0x21f, x0	// Write all 0s to CSR
	csrrs x5, 0x21f, x12	// Set all CSR bits
	csrrc x5, 0x21f, x12	// Clear all CSR bits
	csrrw x5, 0x21f, x15	// Restore CSR

// Testing CSR 0x220
	csrr x22, 0x220	// Read CSR
	li x10, -1
	csrrw x8, 0x220, x10	// Write all 1s to CSR
	csrrw x8, 0x220, x0	// Write all 0s to CSR
	csrrs x8, 0x220, x10	// Set all CSR bits
	csrrc x8, 0x220, x10	// Clear all CSR bits
	csrrw x8, 0x220, x22	// Restore CSR

// Testing CSR 0x221
	csrr x12, 0x221	// Read CSR
	li x17, -1
	csrrw x25, 0x221, x17	// Write all 1s to CSR
	csrrw x25, 0x221, x0	// Write all 0s to CSR
	csrrs x25, 0x221, x17	// Set all CSR bits
	csrrc x25, 0x221, x17	// Clear all CSR bits
	csrrw x25, 0x221, x12	// Restore CSR

// Testing CSR 0x222
	csrr x1, 0x222	// Read CSR
	li x21, -1
	csrrw x28, 0x222, x21	// Write all 1s to CSR
	csrrw x28, 0x222, x0	// Write all 0s to CSR
	csrrs x28, 0x222, x21	// Set all CSR bits
	csrrc x28, 0x222, x21	// Clear all CSR bits
	csrrw x28, 0x222, x1	// Restore CSR

// Testing CSR 0x223
	csrr x16, 0x223	// Read CSR
	li x7, -1
	csrrw x6, 0x223, x7	// Write all 1s to CSR
	csrrw x6, 0x223, x0	// Write all 0s to CSR
	csrrs x6, 0x223, x7	// Set all CSR bits
	csrrc x6, 0x223, x7	// Clear all CSR bits
	csrrw x6, 0x223, x16	// Restore CSR

// Testing CSR 0x224
	csrr x20, 0x224	// Read CSR
	li x13, -1
	csrrw x4, 0x224, x13	// Write all 1s to CSR
	csrrw x4, 0x224, x0	// Write all 0s to CSR
	csrrs x4, 0x224, x13	// Set all CSR bits
	csrrc x4, 0x224, x13	// Clear all CSR bits
	csrrw x4, 0x224, x20	// Restore CSR

// Testing CSR 0x225
	csrr x23, 0x225	// Read CSR
	li x29, -1
	csrrw x18, 0x225, x29	// Write all 1s to CSR
	csrrw x18, 0x225, x0	// Write all 0s to CSR
	csrrs x18, 0x225, x29	// Set all CSR bits
	csrrc x18, 0x225, x29	// Clear all CSR bits
	csrrw x18, 0x225, x23	// Restore CSR

// Testing CSR 0x226
	csrr x2, 0x226	// Read CSR
	li x20, -1
	csrrw x26, 0x226, x20	// Write all 1s to CSR
	csrrw x26, 0x226, x0	// Write all 0s to CSR
	csrrs x26, 0x226, x20	// Set all CSR bits
	csrrc x26, 0x226, x20	// Clear all CSR bits
	csrrw x26, 0x226, x2	// Restore CSR

// Testing CSR 0x227
	csrr x27, 0x227	// Read CSR
	li x3, -1
	csrrw x4, 0x227, x3	// Write all 1s to CSR
	csrrw x4, 0x227, x0	// Write all 0s to CSR
	csrrs x4, 0x227, x3	// Set all CSR bits
	csrrc x4, 0x227, x3	// Clear all CSR bits
	csrrw x4, 0x227, x27	// Restore CSR

// Testing CSR 0x228
	csrr x10, 0x228	// Read CSR
	li x30, -1
	csrrw x11, 0x228, x30	// Write all 1s to CSR
	csrrw x11, 0x228, x0	// Write all 0s to CSR
	csrrs x11, 0x228, x30	// Set all CSR bits
	csrrc x11, 0x228, x30	// Clear all CSR bits
	csrrw x11, 0x228, x10	// Restore CSR

// Testing CSR 0x229
	csrr x18, 0x229	// Read CSR
	li x12, -1
	csrrw x3, 0x229, x12	// Write all 1s to CSR
	csrrw x3, 0x229, x0	// Write all 0s to CSR
	csrrs x3, 0x229, x12	// Set all CSR bits
	csrrc x3, 0x229, x12	// Clear all CSR bits
	csrrw x3, 0x229, x18	// Restore CSR

// Testing CSR 0x22a
	csrr x14, 0x22a	// Read CSR
	li x4, -1
	csrrw x20, 0x22a, x4	// Write all 1s to CSR
	csrrw x20, 0x22a, x0	// Write all 0s to CSR
	csrrs x20, 0x22a, x4	// Set all CSR bits
	csrrc x20, 0x22a, x4	// Clear all CSR bits
	csrrw x20, 0x22a, x14	// Restore CSR

// Testing CSR 0x22b
	csrr x6, 0x22b	// Read CSR
	li x29, -1
	csrrw x18, 0x22b, x29	// Write all 1s to CSR
	csrrw x18, 0x22b, x0	// Write all 0s to CSR
	csrrs x18, 0x22b, x29	// Set all CSR bits
	csrrc x18, 0x22b, x29	// Clear all CSR bits
	csrrw x18, 0x22b, x6	// Restore CSR

// Testing CSR 0x22c
	csrr x31, 0x22c	// Read CSR
	li x28, -1
	csrrw x16, 0x22c, x28	// Write all 1s to CSR
	csrrw x16, 0x22c, x0	// Write all 0s to CSR
	csrrs x16, 0x22c, x28	// Set all CSR bits
	csrrc x16, 0x22c, x28	// Clear all CSR bits
	csrrw x16, 0x22c, x31	// Restore CSR

// Testing CSR 0x22d
	csrr x9, 0x22d	// Read CSR
	li x7, -1
	csrrw x21, 0x22d, x7	// Write all 1s to CSR
	csrrw x21, 0x22d, x0	// Write all 0s to CSR
	csrrs x21, 0x22d, x7	// Set all CSR bits
	csrrc x21, 0x22d, x7	// Clear all CSR bits
	csrrw x21, 0x22d, x9	// Restore CSR

// Testing CSR 0x22e
	csrr x12, 0x22e	// Read CSR
	li x29, -1
	csrrw x20, 0x22e, x29	// Write all 1s to CSR
	csrrw x20, 0x22e, x0	// Write all 0s to CSR
	csrrs x20, 0x22e, x29	// Set all CSR bits
	csrrc x20, 0x22e, x29	// Clear all CSR bits
	csrrw x20, 0x22e, x12	// Restore CSR

// Testing CSR 0x22f
	csrr x24, 0x22f	// Read CSR
	li x30, -1
	csrrw x4, 0x22f, x30	// Write all 1s to CSR
	csrrw x4, 0x22f, x0	// Write all 0s to CSR
	csrrs x4, 0x22f, x30	// Set all CSR bits
	csrrc x4, 0x22f, x30	// Clear all CSR bits
	csrrw x4, 0x22f, x24	// Restore CSR

// Testing CSR 0x230
	csrr x31, 0x230	// Read CSR
	li x28, -1
	csrrw x19, 0x230, x28	// Write all 1s to CSR
	csrrw x19, 0x230, x0	// Write all 0s to CSR
	csrrs x19, 0x230, x28	// Set all CSR bits
	csrrc x19, 0x230, x28	// Clear all CSR bits
	csrrw x19, 0x230, x31	// Restore CSR

// Testing CSR 0x231
	csrr x14, 0x231	// Read CSR
	li x2, -1
	csrrw x26, 0x231, x2	// Write all 1s to CSR
	csrrw x26, 0x231, x0	// Write all 0s to CSR
	csrrs x26, 0x231, x2	// Set all CSR bits
	csrrc x26, 0x231, x2	// Clear all CSR bits
	csrrw x26, 0x231, x14	// Restore CSR

// Testing CSR 0x232
	csrr x28, 0x232	// Read CSR
	li x31, -1
	csrrw x4, 0x232, x31	// Write all 1s to CSR
	csrrw x4, 0x232, x0	// Write all 0s to CSR
	csrrs x4, 0x232, x31	// Set all CSR bits
	csrrc x4, 0x232, x31	// Clear all CSR bits
	csrrw x4, 0x232, x28	// Restore CSR

// Testing CSR 0x233
	csrr x15, 0x233	// Read CSR
	li x23, -1
	csrrw x20, 0x233, x23	// Write all 1s to CSR
	csrrw x20, 0x233, x0	// Write all 0s to CSR
	csrrs x20, 0x233, x23	// Set all CSR bits
	csrrc x20, 0x233, x23	// Clear all CSR bits
	csrrw x20, 0x233, x15	// Restore CSR

// Testing CSR 0x234
	csrr x16, 0x234	// Read CSR
	li x21, -1
	csrrw x25, 0x234, x21	// Write all 1s to CSR
	csrrw x25, 0x234, x0	// Write all 0s to CSR
	csrrs x25, 0x234, x21	// Set all CSR bits
	csrrc x25, 0x234, x21	// Clear all CSR bits
	csrrw x25, 0x234, x16	// Restore CSR

// Testing CSR 0x235
	csrr x26, 0x235	// Read CSR
	li x6, -1
	csrrw x14, 0x235, x6	// Write all 1s to CSR
	csrrw x14, 0x235, x0	// Write all 0s to CSR
	csrrs x14, 0x235, x6	// Set all CSR bits
	csrrc x14, 0x235, x6	// Clear all CSR bits
	csrrw x14, 0x235, x26	// Restore CSR

// Testing CSR 0x236
	csrr x3, 0x236	// Read CSR
	li x9, -1
	csrrw x20, 0x236, x9	// Write all 1s to CSR
	csrrw x20, 0x236, x0	// Write all 0s to CSR
	csrrs x20, 0x236, x9	// Set all CSR bits
	csrrc x20, 0x236, x9	// Clear all CSR bits
	csrrw x20, 0x236, x3	// Restore CSR

// Testing CSR 0x237
	csrr x10, 0x237	// Read CSR
	li x1, -1
	csrrw x6, 0x237, x1	// Write all 1s to CSR
	csrrw x6, 0x237, x0	// Write all 0s to CSR
	csrrs x6, 0x237, x1	// Set all CSR bits
	csrrc x6, 0x237, x1	// Clear all CSR bits
	csrrw x6, 0x237, x10	// Restore CSR

// Testing CSR 0x238
	csrr x11, 0x238	// Read CSR
	li x23, -1
	csrrw x18, 0x238, x23	// Write all 1s to CSR
	csrrw x18, 0x238, x0	// Write all 0s to CSR
	csrrs x18, 0x238, x23	// Set all CSR bits
	csrrc x18, 0x238, x23	// Clear all CSR bits
	csrrw x18, 0x238, x11	// Restore CSR

// Testing CSR 0x239
	csrr x25, 0x239	// Read CSR
	li x11, -1
	csrrw x9, 0x239, x11	// Write all 1s to CSR
	csrrw x9, 0x239, x0	// Write all 0s to CSR
	csrrs x9, 0x239, x11	// Set all CSR bits
	csrrc x9, 0x239, x11	// Clear all CSR bits
	csrrw x9, 0x239, x25	// Restore CSR

// Testing CSR 0x23a
	csrr x19, 0x23a	// Read CSR
	li x16, -1
	csrrw x7, 0x23a, x16	// Write all 1s to CSR
	csrrw x7, 0x23a, x0	// Write all 0s to CSR
	csrrs x7, 0x23a, x16	// Set all CSR bits
	csrrc x7, 0x23a, x16	// Clear all CSR bits
	csrrw x7, 0x23a, x19	// Restore CSR

// Testing CSR 0x23b
	csrr x20, 0x23b	// Read CSR
	li x15, -1
	csrrw x19, 0x23b, x15	// Write all 1s to CSR
	csrrw x19, 0x23b, x0	// Write all 0s to CSR
	csrrs x19, 0x23b, x15	// Set all CSR bits
	csrrc x19, 0x23b, x15	// Clear all CSR bits
	csrrw x19, 0x23b, x20	// Restore CSR

// Testing CSR 0x23c
	csrr x7, 0x23c	// Read CSR
	li x16, -1
	csrrw x3, 0x23c, x16	// Write all 1s to CSR
	csrrw x3, 0x23c, x0	// Write all 0s to CSR
	csrrs x3, 0x23c, x16	// Set all CSR bits
	csrrc x3, 0x23c, x16	// Clear all CSR bits
	csrrw x3, 0x23c, x7	// Restore CSR

// Testing CSR 0x23d
	csrr x3, 0x23d	// Read CSR
	li x9, -1
	csrrw x8, 0x23d, x9	// Write all 1s to CSR
	csrrw x8, 0x23d, x0	// Write all 0s to CSR
	csrrs x8, 0x23d, x9	// Set all CSR bits
	csrrc x8, 0x23d, x9	// Clear all CSR bits
	csrrw x8, 0x23d, x3	// Restore CSR

// Testing CSR 0x23e
	csrr x25, 0x23e	// Read CSR
	li x23, -1
	csrrw x26, 0x23e, x23	// Write all 1s to CSR
	csrrw x26, 0x23e, x0	// Write all 0s to CSR
	csrrs x26, 0x23e, x23	// Set all CSR bits
	csrrc x26, 0x23e, x23	// Clear all CSR bits
	csrrw x26, 0x23e, x25	// Restore CSR

// Testing CSR 0x23f
	csrr x16, 0x23f	// Read CSR
	li x26, -1
	csrrw x12, 0x23f, x26	// Write all 1s to CSR
	csrrw x12, 0x23f, x0	// Write all 0s to CSR
	csrrs x12, 0x23f, x26	// Set all CSR bits
	csrrc x12, 0x23f, x26	// Clear all CSR bits
	csrrw x12, 0x23f, x16	// Restore CSR

// Testing CSR 0x240
	csrr x8, 0x240	// Read CSR
	li x3, -1
	csrrw x28, 0x240, x3	// Write all 1s to CSR
	csrrw x28, 0x240, x0	// Write all 0s to CSR
	csrrs x28, 0x240, x3	// Set all CSR bits
	csrrc x28, 0x240, x3	// Clear all CSR bits
	csrrw x28, 0x240, x8	// Restore CSR

// Testing CSR 0x241
	csrr x20, 0x241	// Read CSR
	li x11, -1
	csrrw x16, 0x241, x11	// Write all 1s to CSR
	csrrw x16, 0x241, x0	// Write all 0s to CSR
	csrrs x16, 0x241, x11	// Set all CSR bits
	csrrc x16, 0x241, x11	// Clear all CSR bits
	csrrw x16, 0x241, x20	// Restore CSR

// Testing CSR 0x242
	csrr x26, 0x242	// Read CSR
	li x5, -1
	csrrw x11, 0x242, x5	// Write all 1s to CSR
	csrrw x11, 0x242, x0	// Write all 0s to CSR
	csrrs x11, 0x242, x5	// Set all CSR bits
	csrrc x11, 0x242, x5	// Clear all CSR bits
	csrrw x11, 0x242, x26	// Restore CSR

// Testing CSR 0x243
	csrr x18, 0x243	// Read CSR
	li x22, -1
	csrrw x19, 0x243, x22	// Write all 1s to CSR
	csrrw x19, 0x243, x0	// Write all 0s to CSR
	csrrs x19, 0x243, x22	// Set all CSR bits
	csrrc x19, 0x243, x22	// Clear all CSR bits
	csrrw x19, 0x243, x18	// Restore CSR

// Testing CSR 0x244
	csrr x7, 0x244	// Read CSR
	li x16, -1
	csrrw x5, 0x244, x16	// Write all 1s to CSR
	csrrw x5, 0x244, x0	// Write all 0s to CSR
	csrrs x5, 0x244, x16	// Set all CSR bits
	csrrc x5, 0x244, x16	// Clear all CSR bits
	csrrw x5, 0x244, x7	// Restore CSR

// Testing CSR 0x245
	csrr x11, 0x245	// Read CSR
	li x7, -1
	csrrw x21, 0x245, x7	// Write all 1s to CSR
	csrrw x21, 0x245, x0	// Write all 0s to CSR
	csrrs x21, 0x245, x7	// Set all CSR bits
	csrrc x21, 0x245, x7	// Clear all CSR bits
	csrrw x21, 0x245, x11	// Restore CSR

// Testing CSR 0x246
	csrr x4, 0x246	// Read CSR
	li x7, -1
	csrrw x13, 0x246, x7	// Write all 1s to CSR
	csrrw x13, 0x246, x0	// Write all 0s to CSR
	csrrs x13, 0x246, x7	// Set all CSR bits
	csrrc x13, 0x246, x7	// Clear all CSR bits
	csrrw x13, 0x246, x4	// Restore CSR

// Testing CSR 0x247
	csrr x15, 0x247	// Read CSR
	li x29, -1
	csrrw x5, 0x247, x29	// Write all 1s to CSR
	csrrw x5, 0x247, x0	// Write all 0s to CSR
	csrrs x5, 0x247, x29	// Set all CSR bits
	csrrc x5, 0x247, x29	// Clear all CSR bits
	csrrw x5, 0x247, x15	// Restore CSR

// Testing CSR 0x248
	csrr x16, 0x248	// Read CSR
	li x9, -1
	csrrw x30, 0x248, x9	// Write all 1s to CSR
	csrrw x30, 0x248, x0	// Write all 0s to CSR
	csrrs x30, 0x248, x9	// Set all CSR bits
	csrrc x30, 0x248, x9	// Clear all CSR bits
	csrrw x30, 0x248, x16	// Restore CSR

// Testing CSR 0x249
	csrr x23, 0x249	// Read CSR
	li x1, -1
	csrrw x7, 0x249, x1	// Write all 1s to CSR
	csrrw x7, 0x249, x0	// Write all 0s to CSR
	csrrs x7, 0x249, x1	// Set all CSR bits
	csrrc x7, 0x249, x1	// Clear all CSR bits
	csrrw x7, 0x249, x23	// Restore CSR

// Testing CSR 0x24a
	csrr x29, 0x24a	// Read CSR
	li x5, -1
	csrrw x8, 0x24a, x5	// Write all 1s to CSR
	csrrw x8, 0x24a, x0	// Write all 0s to CSR
	csrrs x8, 0x24a, x5	// Set all CSR bits
	csrrc x8, 0x24a, x5	// Clear all CSR bits
	csrrw x8, 0x24a, x29	// Restore CSR

// Testing CSR 0x24b
	csrr x25, 0x24b	// Read CSR
	li x20, -1
	csrrw x11, 0x24b, x20	// Write all 1s to CSR
	csrrw x11, 0x24b, x0	// Write all 0s to CSR
	csrrs x11, 0x24b, x20	// Set all CSR bits
	csrrc x11, 0x24b, x20	// Clear all CSR bits
	csrrw x11, 0x24b, x25	// Restore CSR

// Testing CSR 0x24c
	csrr x22, 0x24c	// Read CSR
	li x8, -1
	csrrw x27, 0x24c, x8	// Write all 1s to CSR
	csrrw x27, 0x24c, x0	// Write all 0s to CSR
	csrrs x27, 0x24c, x8	// Set all CSR bits
	csrrc x27, 0x24c, x8	// Clear all CSR bits
	csrrw x27, 0x24c, x22	// Restore CSR

// Testing CSR 0x24d
	csrr x7, 0x24d	// Read CSR
	li x21, -1
	csrrw x15, 0x24d, x21	// Write all 1s to CSR
	csrrw x15, 0x24d, x0	// Write all 0s to CSR
	csrrs x15, 0x24d, x21	// Set all CSR bits
	csrrc x15, 0x24d, x21	// Clear all CSR bits
	csrrw x15, 0x24d, x7	// Restore CSR

// Testing CSR 0x24e
	csrr x10, 0x24e	// Read CSR
	li x16, -1
	csrrw x17, 0x24e, x16	// Write all 1s to CSR
	csrrw x17, 0x24e, x0	// Write all 0s to CSR
	csrrs x17, 0x24e, x16	// Set all CSR bits
	csrrc x17, 0x24e, x16	// Clear all CSR bits
	csrrw x17, 0x24e, x10	// Restore CSR

// Testing CSR 0x24f
	csrr x25, 0x24f	// Read CSR
	li x14, -1
	csrrw x8, 0x24f, x14	// Write all 1s to CSR
	csrrw x8, 0x24f, x0	// Write all 0s to CSR
	csrrs x8, 0x24f, x14	// Set all CSR bits
	csrrc x8, 0x24f, x14	// Clear all CSR bits
	csrrw x8, 0x24f, x25	// Restore CSR

// Testing CSR 0x250
	csrr x29, 0x250	// Read CSR
	li x30, -1
	csrrw x10, 0x250, x30	// Write all 1s to CSR
	csrrw x10, 0x250, x0	// Write all 0s to CSR
	csrrs x10, 0x250, x30	// Set all CSR bits
	csrrc x10, 0x250, x30	// Clear all CSR bits
	csrrw x10, 0x250, x29	// Restore CSR

// Testing CSR 0x251
	csrr x8, 0x251	// Read CSR
	li x28, -1
	csrrw x13, 0x251, x28	// Write all 1s to CSR
	csrrw x13, 0x251, x0	// Write all 0s to CSR
	csrrs x13, 0x251, x28	// Set all CSR bits
	csrrc x13, 0x251, x28	// Clear all CSR bits
	csrrw x13, 0x251, x8	// Restore CSR

// Testing CSR 0x252
	csrr x3, 0x252	// Read CSR
	li x6, -1
	csrrw x25, 0x252, x6	// Write all 1s to CSR
	csrrw x25, 0x252, x0	// Write all 0s to CSR
	csrrs x25, 0x252, x6	// Set all CSR bits
	csrrc x25, 0x252, x6	// Clear all CSR bits
	csrrw x25, 0x252, x3	// Restore CSR

// Testing CSR 0x253
	csrr x3, 0x253	// Read CSR
	li x28, -1
	csrrw x13, 0x253, x28	// Write all 1s to CSR
	csrrw x13, 0x253, x0	// Write all 0s to CSR
	csrrs x13, 0x253, x28	// Set all CSR bits
	csrrc x13, 0x253, x28	// Clear all CSR bits
	csrrw x13, 0x253, x3	// Restore CSR

// Testing CSR 0x254
	csrr x11, 0x254	// Read CSR
	li x3, -1
	csrrw x26, 0x254, x3	// Write all 1s to CSR
	csrrw x26, 0x254, x0	// Write all 0s to CSR
	csrrs x26, 0x254, x3	// Set all CSR bits
	csrrc x26, 0x254, x3	// Clear all CSR bits
	csrrw x26, 0x254, x11	// Restore CSR

// Testing CSR 0x255
	csrr x1, 0x255	// Read CSR
	li x31, -1
	csrrw x23, 0x255, x31	// Write all 1s to CSR
	csrrw x23, 0x255, x0	// Write all 0s to CSR
	csrrs x23, 0x255, x31	// Set all CSR bits
	csrrc x23, 0x255, x31	// Clear all CSR bits
	csrrw x23, 0x255, x1	// Restore CSR

// Testing CSR 0x256
	csrr x1, 0x256	// Read CSR
	li x28, -1
	csrrw x5, 0x256, x28	// Write all 1s to CSR
	csrrw x5, 0x256, x0	// Write all 0s to CSR
	csrrs x5, 0x256, x28	// Set all CSR bits
	csrrc x5, 0x256, x28	// Clear all CSR bits
	csrrw x5, 0x256, x1	// Restore CSR

// Testing CSR 0x257
	csrr x25, 0x257	// Read CSR
	li x28, -1
	csrrw x13, 0x257, x28	// Write all 1s to CSR
	csrrw x13, 0x257, x0	// Write all 0s to CSR
	csrrs x13, 0x257, x28	// Set all CSR bits
	csrrc x13, 0x257, x28	// Clear all CSR bits
	csrrw x13, 0x257, x25	// Restore CSR

// Testing CSR 0x258
	csrr x10, 0x258	// Read CSR
	li x14, -1
	csrrw x26, 0x258, x14	// Write all 1s to CSR
	csrrw x26, 0x258, x0	// Write all 0s to CSR
	csrrs x26, 0x258, x14	// Set all CSR bits
	csrrc x26, 0x258, x14	// Clear all CSR bits
	csrrw x26, 0x258, x10	// Restore CSR

// Testing CSR 0x259
	csrr x30, 0x259	// Read CSR
	li x7, -1
	csrrw x17, 0x259, x7	// Write all 1s to CSR
	csrrw x17, 0x259, x0	// Write all 0s to CSR
	csrrs x17, 0x259, x7	// Set all CSR bits
	csrrc x17, 0x259, x7	// Clear all CSR bits
	csrrw x17, 0x259, x30	// Restore CSR

// Testing CSR 0x25a
	csrr x15, 0x25a	// Read CSR
	li x4, -1
	csrrw x3, 0x25a, x4	// Write all 1s to CSR
	csrrw x3, 0x25a, x0	// Write all 0s to CSR
	csrrs x3, 0x25a, x4	// Set all CSR bits
	csrrc x3, 0x25a, x4	// Clear all CSR bits
	csrrw x3, 0x25a, x15	// Restore CSR

// Testing CSR 0x25b
	csrr x27, 0x25b	// Read CSR
	li x21, -1
	csrrw x18, 0x25b, x21	// Write all 1s to CSR
	csrrw x18, 0x25b, x0	// Write all 0s to CSR
	csrrs x18, 0x25b, x21	// Set all CSR bits
	csrrc x18, 0x25b, x21	// Clear all CSR bits
	csrrw x18, 0x25b, x27	// Restore CSR

// Testing CSR 0x25c
	csrr x6, 0x25c	// Read CSR
	li x4, -1
	csrrw x12, 0x25c, x4	// Write all 1s to CSR
	csrrw x12, 0x25c, x0	// Write all 0s to CSR
	csrrs x12, 0x25c, x4	// Set all CSR bits
	csrrc x12, 0x25c, x4	// Clear all CSR bits
	csrrw x12, 0x25c, x6	// Restore CSR

// Testing CSR 0x25d
	csrr x8, 0x25d	// Read CSR
	li x19, -1
	csrrw x21, 0x25d, x19	// Write all 1s to CSR
	csrrw x21, 0x25d, x0	// Write all 0s to CSR
	csrrs x21, 0x25d, x19	// Set all CSR bits
	csrrc x21, 0x25d, x19	// Clear all CSR bits
	csrrw x21, 0x25d, x8	// Restore CSR

// Testing CSR 0x25e
	csrr x10, 0x25e	// Read CSR
	li x29, -1
	csrrw x12, 0x25e, x29	// Write all 1s to CSR
	csrrw x12, 0x25e, x0	// Write all 0s to CSR
	csrrs x12, 0x25e, x29	// Set all CSR bits
	csrrc x12, 0x25e, x29	// Clear all CSR bits
	csrrw x12, 0x25e, x10	// Restore CSR

// Testing CSR 0x25f
	csrr x7, 0x25f	// Read CSR
	li x22, -1
	csrrw x2, 0x25f, x22	// Write all 1s to CSR
	csrrw x2, 0x25f, x0	// Write all 0s to CSR
	csrrs x2, 0x25f, x22	// Set all CSR bits
	csrrc x2, 0x25f, x22	// Clear all CSR bits
	csrrw x2, 0x25f, x7	// Restore CSR

// Testing CSR 0x260
	csrr x13, 0x260	// Read CSR
	li x6, -1
	csrrw x25, 0x260, x6	// Write all 1s to CSR
	csrrw x25, 0x260, x0	// Write all 0s to CSR
	csrrs x25, 0x260, x6	// Set all CSR bits
	csrrc x25, 0x260, x6	// Clear all CSR bits
	csrrw x25, 0x260, x13	// Restore CSR

// Testing CSR 0x261
	csrr x10, 0x261	// Read CSR
	li x15, -1
	csrrw x23, 0x261, x15	// Write all 1s to CSR
	csrrw x23, 0x261, x0	// Write all 0s to CSR
	csrrs x23, 0x261, x15	// Set all CSR bits
	csrrc x23, 0x261, x15	// Clear all CSR bits
	csrrw x23, 0x261, x10	// Restore CSR

// Testing CSR 0x262
	csrr x6, 0x262	// Read CSR
	li x14, -1
	csrrw x21, 0x262, x14	// Write all 1s to CSR
	csrrw x21, 0x262, x0	// Write all 0s to CSR
	csrrs x21, 0x262, x14	// Set all CSR bits
	csrrc x21, 0x262, x14	// Clear all CSR bits
	csrrw x21, 0x262, x6	// Restore CSR

// Testing CSR 0x263
	csrr x9, 0x263	// Read CSR
	li x3, -1
	csrrw x20, 0x263, x3	// Write all 1s to CSR
	csrrw x20, 0x263, x0	// Write all 0s to CSR
	csrrs x20, 0x263, x3	// Set all CSR bits
	csrrc x20, 0x263, x3	// Clear all CSR bits
	csrrw x20, 0x263, x9	// Restore CSR

// Testing CSR 0x264
	csrr x19, 0x264	// Read CSR
	li x10, -1
	csrrw x5, 0x264, x10	// Write all 1s to CSR
	csrrw x5, 0x264, x0	// Write all 0s to CSR
	csrrs x5, 0x264, x10	// Set all CSR bits
	csrrc x5, 0x264, x10	// Clear all CSR bits
	csrrw x5, 0x264, x19	// Restore CSR

// Testing CSR 0x265
	csrr x5, 0x265	// Read CSR
	li x27, -1
	csrrw x6, 0x265, x27	// Write all 1s to CSR
	csrrw x6, 0x265, x0	// Write all 0s to CSR
	csrrs x6, 0x265, x27	// Set all CSR bits
	csrrc x6, 0x265, x27	// Clear all CSR bits
	csrrw x6, 0x265, x5	// Restore CSR

// Testing CSR 0x266
	csrr x26, 0x266	// Read CSR
	li x13, -1
	csrrw x2, 0x266, x13	// Write all 1s to CSR
	csrrw x2, 0x266, x0	// Write all 0s to CSR
	csrrs x2, 0x266, x13	// Set all CSR bits
	csrrc x2, 0x266, x13	// Clear all CSR bits
	csrrw x2, 0x266, x26	// Restore CSR

// Testing CSR 0x267
	csrr x14, 0x267	// Read CSR
	li x10, -1
	csrrw x19, 0x267, x10	// Write all 1s to CSR
	csrrw x19, 0x267, x0	// Write all 0s to CSR
	csrrs x19, 0x267, x10	// Set all CSR bits
	csrrc x19, 0x267, x10	// Clear all CSR bits
	csrrw x19, 0x267, x14	// Restore CSR

// Testing CSR 0x268
	csrr x29, 0x268	// Read CSR
	li x25, -1
	csrrw x4, 0x268, x25	// Write all 1s to CSR
	csrrw x4, 0x268, x0	// Write all 0s to CSR
	csrrs x4, 0x268, x25	// Set all CSR bits
	csrrc x4, 0x268, x25	// Clear all CSR bits
	csrrw x4, 0x268, x29	// Restore CSR

// Testing CSR 0x269
	csrr x13, 0x269	// Read CSR
	li x11, -1
	csrrw x18, 0x269, x11	// Write all 1s to CSR
	csrrw x18, 0x269, x0	// Write all 0s to CSR
	csrrs x18, 0x269, x11	// Set all CSR bits
	csrrc x18, 0x269, x11	// Clear all CSR bits
	csrrw x18, 0x269, x13	// Restore CSR

// Testing CSR 0x26a
	csrr x17, 0x26a	// Read CSR
	li x26, -1
	csrrw x1, 0x26a, x26	// Write all 1s to CSR
	csrrw x1, 0x26a, x0	// Write all 0s to CSR
	csrrs x1, 0x26a, x26	// Set all CSR bits
	csrrc x1, 0x26a, x26	// Clear all CSR bits
	csrrw x1, 0x26a, x17	// Restore CSR

// Testing CSR 0x26b
	csrr x10, 0x26b	// Read CSR
	li x6, -1
	csrrw x2, 0x26b, x6	// Write all 1s to CSR
	csrrw x2, 0x26b, x0	// Write all 0s to CSR
	csrrs x2, 0x26b, x6	// Set all CSR bits
	csrrc x2, 0x26b, x6	// Clear all CSR bits
	csrrw x2, 0x26b, x10	// Restore CSR

// Testing CSR 0x26c
	csrr x25, 0x26c	// Read CSR
	li x17, -1
	csrrw x1, 0x26c, x17	// Write all 1s to CSR
	csrrw x1, 0x26c, x0	// Write all 0s to CSR
	csrrs x1, 0x26c, x17	// Set all CSR bits
	csrrc x1, 0x26c, x17	// Clear all CSR bits
	csrrw x1, 0x26c, x25	// Restore CSR

// Testing CSR 0x26d
	csrr x18, 0x26d	// Read CSR
	li x30, -1
	csrrw x25, 0x26d, x30	// Write all 1s to CSR
	csrrw x25, 0x26d, x0	// Write all 0s to CSR
	csrrs x25, 0x26d, x30	// Set all CSR bits
	csrrc x25, 0x26d, x30	// Clear all CSR bits
	csrrw x25, 0x26d, x18	// Restore CSR

// Testing CSR 0x26e
	csrr x30, 0x26e	// Read CSR
	li x12, -1
	csrrw x10, 0x26e, x12	// Write all 1s to CSR
	csrrw x10, 0x26e, x0	// Write all 0s to CSR
	csrrs x10, 0x26e, x12	// Set all CSR bits
	csrrc x10, 0x26e, x12	// Clear all CSR bits
	csrrw x10, 0x26e, x30	// Restore CSR

// Testing CSR 0x26f
	csrr x19, 0x26f	// Read CSR
	li x16, -1
	csrrw x1, 0x26f, x16	// Write all 1s to CSR
	csrrw x1, 0x26f, x0	// Write all 0s to CSR
	csrrs x1, 0x26f, x16	// Set all CSR bits
	csrrc x1, 0x26f, x16	// Clear all CSR bits
	csrrw x1, 0x26f, x19	// Restore CSR

// Testing CSR 0x270
	csrr x3, 0x270	// Read CSR
	li x17, -1
	csrrw x30, 0x270, x17	// Write all 1s to CSR
	csrrw x30, 0x270, x0	// Write all 0s to CSR
	csrrs x30, 0x270, x17	// Set all CSR bits
	csrrc x30, 0x270, x17	// Clear all CSR bits
	csrrw x30, 0x270, x3	// Restore CSR

// Testing CSR 0x271
	csrr x10, 0x271	// Read CSR
	li x26, -1
	csrrw x18, 0x271, x26	// Write all 1s to CSR
	csrrw x18, 0x271, x0	// Write all 0s to CSR
	csrrs x18, 0x271, x26	// Set all CSR bits
	csrrc x18, 0x271, x26	// Clear all CSR bits
	csrrw x18, 0x271, x10	// Restore CSR

// Testing CSR 0x272
	csrr x20, 0x272	// Read CSR
	li x19, -1
	csrrw x2, 0x272, x19	// Write all 1s to CSR
	csrrw x2, 0x272, x0	// Write all 0s to CSR
	csrrs x2, 0x272, x19	// Set all CSR bits
	csrrc x2, 0x272, x19	// Clear all CSR bits
	csrrw x2, 0x272, x20	// Restore CSR

// Testing CSR 0x273
	csrr x9, 0x273	// Read CSR
	li x16, -1
	csrrw x15, 0x273, x16	// Write all 1s to CSR
	csrrw x15, 0x273, x0	// Write all 0s to CSR
	csrrs x15, 0x273, x16	// Set all CSR bits
	csrrc x15, 0x273, x16	// Clear all CSR bits
	csrrw x15, 0x273, x9	// Restore CSR

// Testing CSR 0x274
	csrr x6, 0x274	// Read CSR
	li x28, -1
	csrrw x31, 0x274, x28	// Write all 1s to CSR
	csrrw x31, 0x274, x0	// Write all 0s to CSR
	csrrs x31, 0x274, x28	// Set all CSR bits
	csrrc x31, 0x274, x28	// Clear all CSR bits
	csrrw x31, 0x274, x6	// Restore CSR

// Testing CSR 0x275
	csrr x23, 0x275	// Read CSR
	li x8, -1
	csrrw x1, 0x275, x8	// Write all 1s to CSR
	csrrw x1, 0x275, x0	// Write all 0s to CSR
	csrrs x1, 0x275, x8	// Set all CSR bits
	csrrc x1, 0x275, x8	// Clear all CSR bits
	csrrw x1, 0x275, x23	// Restore CSR

// Testing CSR 0x276
	csrr x16, 0x276	// Read CSR
	li x30, -1
	csrrw x28, 0x276, x30	// Write all 1s to CSR
	csrrw x28, 0x276, x0	// Write all 0s to CSR
	csrrs x28, 0x276, x30	// Set all CSR bits
	csrrc x28, 0x276, x30	// Clear all CSR bits
	csrrw x28, 0x276, x16	// Restore CSR

// Testing CSR 0x277
	csrr x26, 0x277	// Read CSR
	li x27, -1
	csrrw x2, 0x277, x27	// Write all 1s to CSR
	csrrw x2, 0x277, x0	// Write all 0s to CSR
	csrrs x2, 0x277, x27	// Set all CSR bits
	csrrc x2, 0x277, x27	// Clear all CSR bits
	csrrw x2, 0x277, x26	// Restore CSR

// Testing CSR 0x278
	csrr x30, 0x278	// Read CSR
	li x26, -1
	csrrw x11, 0x278, x26	// Write all 1s to CSR
	csrrw x11, 0x278, x0	// Write all 0s to CSR
	csrrs x11, 0x278, x26	// Set all CSR bits
	csrrc x11, 0x278, x26	// Clear all CSR bits
	csrrw x11, 0x278, x30	// Restore CSR

// Testing CSR 0x279
	csrr x22, 0x279	// Read CSR
	li x15, -1
	csrrw x30, 0x279, x15	// Write all 1s to CSR
	csrrw x30, 0x279, x0	// Write all 0s to CSR
	csrrs x30, 0x279, x15	// Set all CSR bits
	csrrc x30, 0x279, x15	// Clear all CSR bits
	csrrw x30, 0x279, x22	// Restore CSR

// Testing CSR 0x27a
	csrr x5, 0x27a	// Read CSR
	li x4, -1
	csrrw x27, 0x27a, x4	// Write all 1s to CSR
	csrrw x27, 0x27a, x0	// Write all 0s to CSR
	csrrs x27, 0x27a, x4	// Set all CSR bits
	csrrc x27, 0x27a, x4	// Clear all CSR bits
	csrrw x27, 0x27a, x5	// Restore CSR

// Testing CSR 0x27b
	csrr x6, 0x27b	// Read CSR
	li x27, -1
	csrrw x30, 0x27b, x27	// Write all 1s to CSR
	csrrw x30, 0x27b, x0	// Write all 0s to CSR
	csrrs x30, 0x27b, x27	// Set all CSR bits
	csrrc x30, 0x27b, x27	// Clear all CSR bits
	csrrw x30, 0x27b, x6	// Restore CSR

// Testing CSR 0x27c
	csrr x30, 0x27c	// Read CSR
	li x21, -1
	csrrw x14, 0x27c, x21	// Write all 1s to CSR
	csrrw x14, 0x27c, x0	// Write all 0s to CSR
	csrrs x14, 0x27c, x21	// Set all CSR bits
	csrrc x14, 0x27c, x21	// Clear all CSR bits
	csrrw x14, 0x27c, x30	// Restore CSR

// Testing CSR 0x27d
	csrr x5, 0x27d	// Read CSR
	li x29, -1
	csrrw x2, 0x27d, x29	// Write all 1s to CSR
	csrrw x2, 0x27d, x0	// Write all 0s to CSR
	csrrs x2, 0x27d, x29	// Set all CSR bits
	csrrc x2, 0x27d, x29	// Clear all CSR bits
	csrrw x2, 0x27d, x5	// Restore CSR

// Testing CSR 0x27e
	csrr x14, 0x27e	// Read CSR
	li x29, -1
	csrrw x8, 0x27e, x29	// Write all 1s to CSR
	csrrw x8, 0x27e, x0	// Write all 0s to CSR
	csrrs x8, 0x27e, x29	// Set all CSR bits
	csrrc x8, 0x27e, x29	// Clear all CSR bits
	csrrw x8, 0x27e, x14	// Restore CSR

// Testing CSR 0x27f
	csrr x21, 0x27f	// Read CSR
	li x6, -1
	csrrw x12, 0x27f, x6	// Write all 1s to CSR
	csrrw x12, 0x27f, x0	// Write all 0s to CSR
	csrrs x12, 0x27f, x6	// Set all CSR bits
	csrrc x12, 0x27f, x6	// Clear all CSR bits
	csrrw x12, 0x27f, x21	// Restore CSR

// Testing CSR 0x280
	csrr x6, 0x280	// Read CSR
	li x29, -1
	csrrw x3, 0x280, x29	// Write all 1s to CSR
	csrrw x3, 0x280, x0	// Write all 0s to CSR
	csrrs x3, 0x280, x29	// Set all CSR bits
	csrrc x3, 0x280, x29	// Clear all CSR bits
	csrrw x3, 0x280, x6	// Restore CSR

// Testing CSR 0x281
	csrr x21, 0x281	// Read CSR
	li x2, -1
	csrrw x5, 0x281, x2	// Write all 1s to CSR
	csrrw x5, 0x281, x0	// Write all 0s to CSR
	csrrs x5, 0x281, x2	// Set all CSR bits
	csrrc x5, 0x281, x2	// Clear all CSR bits
	csrrw x5, 0x281, x21	// Restore CSR

// Testing CSR 0x282
	csrr x20, 0x282	// Read CSR
	li x5, -1
	csrrw x10, 0x282, x5	// Write all 1s to CSR
	csrrw x10, 0x282, x0	// Write all 0s to CSR
	csrrs x10, 0x282, x5	// Set all CSR bits
	csrrc x10, 0x282, x5	// Clear all CSR bits
	csrrw x10, 0x282, x20	// Restore CSR

// Testing CSR 0x283
	csrr x8, 0x283	// Read CSR
	li x18, -1
	csrrw x25, 0x283, x18	// Write all 1s to CSR
	csrrw x25, 0x283, x0	// Write all 0s to CSR
	csrrs x25, 0x283, x18	// Set all CSR bits
	csrrc x25, 0x283, x18	// Clear all CSR bits
	csrrw x25, 0x283, x8	// Restore CSR

// Testing CSR 0x284
	csrr x6, 0x284	// Read CSR
	li x25, -1
	csrrw x9, 0x284, x25	// Write all 1s to CSR
	csrrw x9, 0x284, x0	// Write all 0s to CSR
	csrrs x9, 0x284, x25	// Set all CSR bits
	csrrc x9, 0x284, x25	// Clear all CSR bits
	csrrw x9, 0x284, x6	// Restore CSR

// Testing CSR 0x285
	csrr x30, 0x285	// Read CSR
	li x7, -1
	csrrw x1, 0x285, x7	// Write all 1s to CSR
	csrrw x1, 0x285, x0	// Write all 0s to CSR
	csrrs x1, 0x285, x7	// Set all CSR bits
	csrrc x1, 0x285, x7	// Clear all CSR bits
	csrrw x1, 0x285, x30	// Restore CSR

// Testing CSR 0x286
	csrr x1, 0x286	// Read CSR
	li x14, -1
	csrrw x4, 0x286, x14	// Write all 1s to CSR
	csrrw x4, 0x286, x0	// Write all 0s to CSR
	csrrs x4, 0x286, x14	// Set all CSR bits
	csrrc x4, 0x286, x14	// Clear all CSR bits
	csrrw x4, 0x286, x1	// Restore CSR

// Testing CSR 0x287
	csrr x19, 0x287	// Read CSR
	li x18, -1
	csrrw x11, 0x287, x18	// Write all 1s to CSR
	csrrw x11, 0x287, x0	// Write all 0s to CSR
	csrrs x11, 0x287, x18	// Set all CSR bits
	csrrc x11, 0x287, x18	// Clear all CSR bits
	csrrw x11, 0x287, x19	// Restore CSR

// Testing CSR 0x288
	csrr x7, 0x288	// Read CSR
	li x18, -1
	csrrw x2, 0x288, x18	// Write all 1s to CSR
	csrrw x2, 0x288, x0	// Write all 0s to CSR
	csrrs x2, 0x288, x18	// Set all CSR bits
	csrrc x2, 0x288, x18	// Clear all CSR bits
	csrrw x2, 0x288, x7	// Restore CSR

// Testing CSR 0x289
	csrr x1, 0x289	// Read CSR
	li x10, -1
	csrrw x21, 0x289, x10	// Write all 1s to CSR
	csrrw x21, 0x289, x0	// Write all 0s to CSR
	csrrs x21, 0x289, x10	// Set all CSR bits
	csrrc x21, 0x289, x10	// Clear all CSR bits
	csrrw x21, 0x289, x1	// Restore CSR

// Testing CSR 0x28a
	csrr x23, 0x28a	// Read CSR
	li x4, -1
	csrrw x11, 0x28a, x4	// Write all 1s to CSR
	csrrw x11, 0x28a, x0	// Write all 0s to CSR
	csrrs x11, 0x28a, x4	// Set all CSR bits
	csrrc x11, 0x28a, x4	// Clear all CSR bits
	csrrw x11, 0x28a, x23	// Restore CSR

// Testing CSR 0x28b
	csrr x19, 0x28b	// Read CSR
	li x12, -1
	csrrw x3, 0x28b, x12	// Write all 1s to CSR
	csrrw x3, 0x28b, x0	// Write all 0s to CSR
	csrrs x3, 0x28b, x12	// Set all CSR bits
	csrrc x3, 0x28b, x12	// Clear all CSR bits
	csrrw x3, 0x28b, x19	// Restore CSR

// Testing CSR 0x28c
	csrr x26, 0x28c	// Read CSR
	li x7, -1
	csrrw x11, 0x28c, x7	// Write all 1s to CSR
	csrrw x11, 0x28c, x0	// Write all 0s to CSR
	csrrs x11, 0x28c, x7	// Set all CSR bits
	csrrc x11, 0x28c, x7	// Clear all CSR bits
	csrrw x11, 0x28c, x26	// Restore CSR

// Testing CSR 0x28d
	csrr x27, 0x28d	// Read CSR
	li x26, -1
	csrrw x19, 0x28d, x26	// Write all 1s to CSR
	csrrw x19, 0x28d, x0	// Write all 0s to CSR
	csrrs x19, 0x28d, x26	// Set all CSR bits
	csrrc x19, 0x28d, x26	// Clear all CSR bits
	csrrw x19, 0x28d, x27	// Restore CSR

// Testing CSR 0x28e
	csrr x17, 0x28e	// Read CSR
	li x29, -1
	csrrw x8, 0x28e, x29	// Write all 1s to CSR
	csrrw x8, 0x28e, x0	// Write all 0s to CSR
	csrrs x8, 0x28e, x29	// Set all CSR bits
	csrrc x8, 0x28e, x29	// Clear all CSR bits
	csrrw x8, 0x28e, x17	// Restore CSR

// Testing CSR 0x28f
	csrr x9, 0x28f	// Read CSR
	li x11, -1
	csrrw x27, 0x28f, x11	// Write all 1s to CSR
	csrrw x27, 0x28f, x0	// Write all 0s to CSR
	csrrs x27, 0x28f, x11	// Set all CSR bits
	csrrc x27, 0x28f, x11	// Clear all CSR bits
	csrrw x27, 0x28f, x9	// Restore CSR

// Testing CSR 0x290
	csrr x23, 0x290	// Read CSR
	li x15, -1
	csrrw x30, 0x290, x15	// Write all 1s to CSR
	csrrw x30, 0x290, x0	// Write all 0s to CSR
	csrrs x30, 0x290, x15	// Set all CSR bits
	csrrc x30, 0x290, x15	// Clear all CSR bits
	csrrw x30, 0x290, x23	// Restore CSR

// Testing CSR 0x291
	csrr x1, 0x291	// Read CSR
	li x19, -1
	csrrw x18, 0x291, x19	// Write all 1s to CSR
	csrrw x18, 0x291, x0	// Write all 0s to CSR
	csrrs x18, 0x291, x19	// Set all CSR bits
	csrrc x18, 0x291, x19	// Clear all CSR bits
	csrrw x18, 0x291, x1	// Restore CSR

// Testing CSR 0x292
	csrr x10, 0x292	// Read CSR
	li x12, -1
	csrrw x26, 0x292, x12	// Write all 1s to CSR
	csrrw x26, 0x292, x0	// Write all 0s to CSR
	csrrs x26, 0x292, x12	// Set all CSR bits
	csrrc x26, 0x292, x12	// Clear all CSR bits
	csrrw x26, 0x292, x10	// Restore CSR

// Testing CSR 0x293
	csrr x27, 0x293	// Read CSR
	li x18, -1
	csrrw x8, 0x293, x18	// Write all 1s to CSR
	csrrw x8, 0x293, x0	// Write all 0s to CSR
	csrrs x8, 0x293, x18	// Set all CSR bits
	csrrc x8, 0x293, x18	// Clear all CSR bits
	csrrw x8, 0x293, x27	// Restore CSR

// Testing CSR 0x294
	csrr x2, 0x294	// Read CSR
	li x25, -1
	csrrw x14, 0x294, x25	// Write all 1s to CSR
	csrrw x14, 0x294, x0	// Write all 0s to CSR
	csrrs x14, 0x294, x25	// Set all CSR bits
	csrrc x14, 0x294, x25	// Clear all CSR bits
	csrrw x14, 0x294, x2	// Restore CSR

// Testing CSR 0x295
	csrr x27, 0x295	// Read CSR
	li x30, -1
	csrrw x23, 0x295, x30	// Write all 1s to CSR
	csrrw x23, 0x295, x0	// Write all 0s to CSR
	csrrs x23, 0x295, x30	// Set all CSR bits
	csrrc x23, 0x295, x30	// Clear all CSR bits
	csrrw x23, 0x295, x27	// Restore CSR

// Testing CSR 0x296
	csrr x24, 0x296	// Read CSR
	li x22, -1
	csrrw x19, 0x296, x22	// Write all 1s to CSR
	csrrw x19, 0x296, x0	// Write all 0s to CSR
	csrrs x19, 0x296, x22	// Set all CSR bits
	csrrc x19, 0x296, x22	// Clear all CSR bits
	csrrw x19, 0x296, x24	// Restore CSR

// Testing CSR 0x297
	csrr x2, 0x297	// Read CSR
	li x5, -1
	csrrw x10, 0x297, x5	// Write all 1s to CSR
	csrrw x10, 0x297, x0	// Write all 0s to CSR
	csrrs x10, 0x297, x5	// Set all CSR bits
	csrrc x10, 0x297, x5	// Clear all CSR bits
	csrrw x10, 0x297, x2	// Restore CSR

// Testing CSR 0x298
	csrr x30, 0x298	// Read CSR
	li x25, -1
	csrrw x20, 0x298, x25	// Write all 1s to CSR
	csrrw x20, 0x298, x0	// Write all 0s to CSR
	csrrs x20, 0x298, x25	// Set all CSR bits
	csrrc x20, 0x298, x25	// Clear all CSR bits
	csrrw x20, 0x298, x30	// Restore CSR

// Testing CSR 0x299
	csrr x16, 0x299	// Read CSR
	li x24, -1
	csrrw x1, 0x299, x24	// Write all 1s to CSR
	csrrw x1, 0x299, x0	// Write all 0s to CSR
	csrrs x1, 0x299, x24	// Set all CSR bits
	csrrc x1, 0x299, x24	// Clear all CSR bits
	csrrw x1, 0x299, x16	// Restore CSR

// Testing CSR 0x29a
	csrr x26, 0x29a	// Read CSR
	li x10, -1
	csrrw x30, 0x29a, x10	// Write all 1s to CSR
	csrrw x30, 0x29a, x0	// Write all 0s to CSR
	csrrs x30, 0x29a, x10	// Set all CSR bits
	csrrc x30, 0x29a, x10	// Clear all CSR bits
	csrrw x30, 0x29a, x26	// Restore CSR

// Testing CSR 0x29b
	csrr x18, 0x29b	// Read CSR
	li x3, -1
	csrrw x4, 0x29b, x3	// Write all 1s to CSR
	csrrw x4, 0x29b, x0	// Write all 0s to CSR
	csrrs x4, 0x29b, x3	// Set all CSR bits
	csrrc x4, 0x29b, x3	// Clear all CSR bits
	csrrw x4, 0x29b, x18	// Restore CSR

// Testing CSR 0x29c
	csrr x8, 0x29c	// Read CSR
	li x16, -1
	csrrw x2, 0x29c, x16	// Write all 1s to CSR
	csrrw x2, 0x29c, x0	// Write all 0s to CSR
	csrrs x2, 0x29c, x16	// Set all CSR bits
	csrrc x2, 0x29c, x16	// Clear all CSR bits
	csrrw x2, 0x29c, x8	// Restore CSR

// Testing CSR 0x29d
	csrr x2, 0x29d	// Read CSR
	li x20, -1
	csrrw x13, 0x29d, x20	// Write all 1s to CSR
	csrrw x13, 0x29d, x0	// Write all 0s to CSR
	csrrs x13, 0x29d, x20	// Set all CSR bits
	csrrc x13, 0x29d, x20	// Clear all CSR bits
	csrrw x13, 0x29d, x2	// Restore CSR

// Testing CSR 0x29e
	csrr x31, 0x29e	// Read CSR
	li x7, -1
	csrrw x11, 0x29e, x7	// Write all 1s to CSR
	csrrw x11, 0x29e, x0	// Write all 0s to CSR
	csrrs x11, 0x29e, x7	// Set all CSR bits
	csrrc x11, 0x29e, x7	// Clear all CSR bits
	csrrw x11, 0x29e, x31	// Restore CSR

// Testing CSR 0x29f
	csrr x14, 0x29f	// Read CSR
	li x16, -1
	csrrw x13, 0x29f, x16	// Write all 1s to CSR
	csrrw x13, 0x29f, x0	// Write all 0s to CSR
	csrrs x13, 0x29f, x16	// Set all CSR bits
	csrrc x13, 0x29f, x16	// Clear all CSR bits
	csrrw x13, 0x29f, x14	// Restore CSR

// Testing CSR 0x2a0
	csrr x21, 0x2a0	// Read CSR
	li x27, -1
	csrrw x4, 0x2a0, x27	// Write all 1s to CSR
	csrrw x4, 0x2a0, x0	// Write all 0s to CSR
	csrrs x4, 0x2a0, x27	// Set all CSR bits
	csrrc x4, 0x2a0, x27	// Clear all CSR bits
	csrrw x4, 0x2a0, x21	// Restore CSR

// Testing CSR 0x2a1
	csrr x15, 0x2a1	// Read CSR
	li x5, -1
	csrrw x18, 0x2a1, x5	// Write all 1s to CSR
	csrrw x18, 0x2a1, x0	// Write all 0s to CSR
	csrrs x18, 0x2a1, x5	// Set all CSR bits
	csrrc x18, 0x2a1, x5	// Clear all CSR bits
	csrrw x18, 0x2a1, x15	// Restore CSR

// Testing CSR 0x2a2
	csrr x28, 0x2a2	// Read CSR
	li x18, -1
	csrrw x30, 0x2a2, x18	// Write all 1s to CSR
	csrrw x30, 0x2a2, x0	// Write all 0s to CSR
	csrrs x30, 0x2a2, x18	// Set all CSR bits
	csrrc x30, 0x2a2, x18	// Clear all CSR bits
	csrrw x30, 0x2a2, x28	// Restore CSR

// Testing CSR 0x2a3
	csrr x18, 0x2a3	// Read CSR
	li x4, -1
	csrrw x1, 0x2a3, x4	// Write all 1s to CSR
	csrrw x1, 0x2a3, x0	// Write all 0s to CSR
	csrrs x1, 0x2a3, x4	// Set all CSR bits
	csrrc x1, 0x2a3, x4	// Clear all CSR bits
	csrrw x1, 0x2a3, x18	// Restore CSR

// Testing CSR 0x2a4
	csrr x18, 0x2a4	// Read CSR
	li x9, -1
	csrrw x21, 0x2a4, x9	// Write all 1s to CSR
	csrrw x21, 0x2a4, x0	// Write all 0s to CSR
	csrrs x21, 0x2a4, x9	// Set all CSR bits
	csrrc x21, 0x2a4, x9	// Clear all CSR bits
	csrrw x21, 0x2a4, x18	// Restore CSR

// Testing CSR 0x2a5
	csrr x1, 0x2a5	// Read CSR
	li x16, -1
	csrrw x24, 0x2a5, x16	// Write all 1s to CSR
	csrrw x24, 0x2a5, x0	// Write all 0s to CSR
	csrrs x24, 0x2a5, x16	// Set all CSR bits
	csrrc x24, 0x2a5, x16	// Clear all CSR bits
	csrrw x24, 0x2a5, x1	// Restore CSR

// Testing CSR 0x2a6
	csrr x4, 0x2a6	// Read CSR
	li x12, -1
	csrrw x29, 0x2a6, x12	// Write all 1s to CSR
	csrrw x29, 0x2a6, x0	// Write all 0s to CSR
	csrrs x29, 0x2a6, x12	// Set all CSR bits
	csrrc x29, 0x2a6, x12	// Clear all CSR bits
	csrrw x29, 0x2a6, x4	// Restore CSR

// Testing CSR 0x2a7
	csrr x22, 0x2a7	// Read CSR
	li x5, -1
	csrrw x31, 0x2a7, x5	// Write all 1s to CSR
	csrrw x31, 0x2a7, x0	// Write all 0s to CSR
	csrrs x31, 0x2a7, x5	// Set all CSR bits
	csrrc x31, 0x2a7, x5	// Clear all CSR bits
	csrrw x31, 0x2a7, x22	// Restore CSR

// Testing CSR 0x2a8
	csrr x2, 0x2a8	// Read CSR
	li x7, -1
	csrrw x29, 0x2a8, x7	// Write all 1s to CSR
	csrrw x29, 0x2a8, x0	// Write all 0s to CSR
	csrrs x29, 0x2a8, x7	// Set all CSR bits
	csrrc x29, 0x2a8, x7	// Clear all CSR bits
	csrrw x29, 0x2a8, x2	// Restore CSR

// Testing CSR 0x2a9
	csrr x6, 0x2a9	// Read CSR
	li x3, -1
	csrrw x18, 0x2a9, x3	// Write all 1s to CSR
	csrrw x18, 0x2a9, x0	// Write all 0s to CSR
	csrrs x18, 0x2a9, x3	// Set all CSR bits
	csrrc x18, 0x2a9, x3	// Clear all CSR bits
	csrrw x18, 0x2a9, x6	// Restore CSR

// Testing CSR 0x2aa
	csrr x16, 0x2aa	// Read CSR
	li x19, -1
	csrrw x22, 0x2aa, x19	// Write all 1s to CSR
	csrrw x22, 0x2aa, x0	// Write all 0s to CSR
	csrrs x22, 0x2aa, x19	// Set all CSR bits
	csrrc x22, 0x2aa, x19	// Clear all CSR bits
	csrrw x22, 0x2aa, x16	// Restore CSR

// Testing CSR 0x2ab
	csrr x23, 0x2ab	// Read CSR
	li x13, -1
	csrrw x15, 0x2ab, x13	// Write all 1s to CSR
	csrrw x15, 0x2ab, x0	// Write all 0s to CSR
	csrrs x15, 0x2ab, x13	// Set all CSR bits
	csrrc x15, 0x2ab, x13	// Clear all CSR bits
	csrrw x15, 0x2ab, x23	// Restore CSR

// Testing CSR 0x2ac
	csrr x26, 0x2ac	// Read CSR
	li x6, -1
	csrrw x11, 0x2ac, x6	// Write all 1s to CSR
	csrrw x11, 0x2ac, x0	// Write all 0s to CSR
	csrrs x11, 0x2ac, x6	// Set all CSR bits
	csrrc x11, 0x2ac, x6	// Clear all CSR bits
	csrrw x11, 0x2ac, x26	// Restore CSR

// Testing CSR 0x2ad
	csrr x17, 0x2ad	// Read CSR
	li x19, -1
	csrrw x7, 0x2ad, x19	// Write all 1s to CSR
	csrrw x7, 0x2ad, x0	// Write all 0s to CSR
	csrrs x7, 0x2ad, x19	// Set all CSR bits
	csrrc x7, 0x2ad, x19	// Clear all CSR bits
	csrrw x7, 0x2ad, x17	// Restore CSR

// Testing CSR 0x2ae
	csrr x21, 0x2ae	// Read CSR
	li x19, -1
	csrrw x7, 0x2ae, x19	// Write all 1s to CSR
	csrrw x7, 0x2ae, x0	// Write all 0s to CSR
	csrrs x7, 0x2ae, x19	// Set all CSR bits
	csrrc x7, 0x2ae, x19	// Clear all CSR bits
	csrrw x7, 0x2ae, x21	// Restore CSR

// Testing CSR 0x2af
	csrr x15, 0x2af	// Read CSR
	li x29, -1
	csrrw x31, 0x2af, x29	// Write all 1s to CSR
	csrrw x31, 0x2af, x0	// Write all 0s to CSR
	csrrs x31, 0x2af, x29	// Set all CSR bits
	csrrc x31, 0x2af, x29	// Clear all CSR bits
	csrrw x31, 0x2af, x15	// Restore CSR

// Testing CSR 0x2b0
	csrr x15, 0x2b0	// Read CSR
	li x28, -1
	csrrw x25, 0x2b0, x28	// Write all 1s to CSR
	csrrw x25, 0x2b0, x0	// Write all 0s to CSR
	csrrs x25, 0x2b0, x28	// Set all CSR bits
	csrrc x25, 0x2b0, x28	// Clear all CSR bits
	csrrw x25, 0x2b0, x15	// Restore CSR

// Testing CSR 0x2b1
	csrr x17, 0x2b1	// Read CSR
	li x7, -1
	csrrw x31, 0x2b1, x7	// Write all 1s to CSR
	csrrw x31, 0x2b1, x0	// Write all 0s to CSR
	csrrs x31, 0x2b1, x7	// Set all CSR bits
	csrrc x31, 0x2b1, x7	// Clear all CSR bits
	csrrw x31, 0x2b1, x17	// Restore CSR

// Testing CSR 0x2b2
	csrr x30, 0x2b2	// Read CSR
	li x1, -1
	csrrw x28, 0x2b2, x1	// Write all 1s to CSR
	csrrw x28, 0x2b2, x0	// Write all 0s to CSR
	csrrs x28, 0x2b2, x1	// Set all CSR bits
	csrrc x28, 0x2b2, x1	// Clear all CSR bits
	csrrw x28, 0x2b2, x30	// Restore CSR

// Testing CSR 0x2b3
	csrr x22, 0x2b3	// Read CSR
	li x18, -1
	csrrw x30, 0x2b3, x18	// Write all 1s to CSR
	csrrw x30, 0x2b3, x0	// Write all 0s to CSR
	csrrs x30, 0x2b3, x18	// Set all CSR bits
	csrrc x30, 0x2b3, x18	// Clear all CSR bits
	csrrw x30, 0x2b3, x22	// Restore CSR

// Testing CSR 0x2b4
	csrr x26, 0x2b4	// Read CSR
	li x21, -1
	csrrw x31, 0x2b4, x21	// Write all 1s to CSR
	csrrw x31, 0x2b4, x0	// Write all 0s to CSR
	csrrs x31, 0x2b4, x21	// Set all CSR bits
	csrrc x31, 0x2b4, x21	// Clear all CSR bits
	csrrw x31, 0x2b4, x26	// Restore CSR

// Testing CSR 0x2b5
	csrr x25, 0x2b5	// Read CSR
	li x1, -1
	csrrw x8, 0x2b5, x1	// Write all 1s to CSR
	csrrw x8, 0x2b5, x0	// Write all 0s to CSR
	csrrs x8, 0x2b5, x1	// Set all CSR bits
	csrrc x8, 0x2b5, x1	// Clear all CSR bits
	csrrw x8, 0x2b5, x25	// Restore CSR

// Testing CSR 0x2b6
	csrr x31, 0x2b6	// Read CSR
	li x13, -1
	csrrw x1, 0x2b6, x13	// Write all 1s to CSR
	csrrw x1, 0x2b6, x0	// Write all 0s to CSR
	csrrs x1, 0x2b6, x13	// Set all CSR bits
	csrrc x1, 0x2b6, x13	// Clear all CSR bits
	csrrw x1, 0x2b6, x31	// Restore CSR

// Testing CSR 0x2b7
	csrr x26, 0x2b7	// Read CSR
	li x23, -1
	csrrw x24, 0x2b7, x23	// Write all 1s to CSR
	csrrw x24, 0x2b7, x0	// Write all 0s to CSR
	csrrs x24, 0x2b7, x23	// Set all CSR bits
	csrrc x24, 0x2b7, x23	// Clear all CSR bits
	csrrw x24, 0x2b7, x26	// Restore CSR

// Testing CSR 0x2b8
	csrr x5, 0x2b8	// Read CSR
	li x2, -1
	csrrw x23, 0x2b8, x2	// Write all 1s to CSR
	csrrw x23, 0x2b8, x0	// Write all 0s to CSR
	csrrs x23, 0x2b8, x2	// Set all CSR bits
	csrrc x23, 0x2b8, x2	// Clear all CSR bits
	csrrw x23, 0x2b8, x5	// Restore CSR

// Testing CSR 0x2b9
	csrr x6, 0x2b9	// Read CSR
	li x10, -1
	csrrw x24, 0x2b9, x10	// Write all 1s to CSR
	csrrw x24, 0x2b9, x0	// Write all 0s to CSR
	csrrs x24, 0x2b9, x10	// Set all CSR bits
	csrrc x24, 0x2b9, x10	// Clear all CSR bits
	csrrw x24, 0x2b9, x6	// Restore CSR

// Testing CSR 0x2ba
	csrr x12, 0x2ba	// Read CSR
	li x29, -1
	csrrw x28, 0x2ba, x29	// Write all 1s to CSR
	csrrw x28, 0x2ba, x0	// Write all 0s to CSR
	csrrs x28, 0x2ba, x29	// Set all CSR bits
	csrrc x28, 0x2ba, x29	// Clear all CSR bits
	csrrw x28, 0x2ba, x12	// Restore CSR

// Testing CSR 0x2bb
	csrr x8, 0x2bb	// Read CSR
	li x2, -1
	csrrw x16, 0x2bb, x2	// Write all 1s to CSR
	csrrw x16, 0x2bb, x0	// Write all 0s to CSR
	csrrs x16, 0x2bb, x2	// Set all CSR bits
	csrrc x16, 0x2bb, x2	// Clear all CSR bits
	csrrw x16, 0x2bb, x8	// Restore CSR

// Testing CSR 0x2bc
	csrr x11, 0x2bc	// Read CSR
	li x25, -1
	csrrw x15, 0x2bc, x25	// Write all 1s to CSR
	csrrw x15, 0x2bc, x0	// Write all 0s to CSR
	csrrs x15, 0x2bc, x25	// Set all CSR bits
	csrrc x15, 0x2bc, x25	// Clear all CSR bits
	csrrw x15, 0x2bc, x11	// Restore CSR

// Testing CSR 0x2bd
	csrr x31, 0x2bd	// Read CSR
	li x19, -1
	csrrw x20, 0x2bd, x19	// Write all 1s to CSR
	csrrw x20, 0x2bd, x0	// Write all 0s to CSR
	csrrs x20, 0x2bd, x19	// Set all CSR bits
	csrrc x20, 0x2bd, x19	// Clear all CSR bits
	csrrw x20, 0x2bd, x31	// Restore CSR

// Testing CSR 0x2be
	csrr x27, 0x2be	// Read CSR
	li x8, -1
	csrrw x17, 0x2be, x8	// Write all 1s to CSR
	csrrw x17, 0x2be, x0	// Write all 0s to CSR
	csrrs x17, 0x2be, x8	// Set all CSR bits
	csrrc x17, 0x2be, x8	// Clear all CSR bits
	csrrw x17, 0x2be, x27	// Restore CSR

// Testing CSR 0x2bf
	csrr x24, 0x2bf	// Read CSR
	li x29, -1
	csrrw x11, 0x2bf, x29	// Write all 1s to CSR
	csrrw x11, 0x2bf, x0	// Write all 0s to CSR
	csrrs x11, 0x2bf, x29	// Set all CSR bits
	csrrc x11, 0x2bf, x29	// Clear all CSR bits
	csrrw x11, 0x2bf, x24	// Restore CSR

// Testing CSR 0x2c0
	csrr x11, 0x2c0	// Read CSR
	li x9, -1
	csrrw x12, 0x2c0, x9	// Write all 1s to CSR
	csrrw x12, 0x2c0, x0	// Write all 0s to CSR
	csrrs x12, 0x2c0, x9	// Set all CSR bits
	csrrc x12, 0x2c0, x9	// Clear all CSR bits
	csrrw x12, 0x2c0, x11	// Restore CSR

// Testing CSR 0x2c1
	csrr x5, 0x2c1	// Read CSR
	li x19, -1
	csrrw x4, 0x2c1, x19	// Write all 1s to CSR
	csrrw x4, 0x2c1, x0	// Write all 0s to CSR
	csrrs x4, 0x2c1, x19	// Set all CSR bits
	csrrc x4, 0x2c1, x19	// Clear all CSR bits
	csrrw x4, 0x2c1, x5	// Restore CSR

// Testing CSR 0x2c2
	csrr x30, 0x2c2	// Read CSR
	li x1, -1
	csrrw x15, 0x2c2, x1	// Write all 1s to CSR
	csrrw x15, 0x2c2, x0	// Write all 0s to CSR
	csrrs x15, 0x2c2, x1	// Set all CSR bits
	csrrc x15, 0x2c2, x1	// Clear all CSR bits
	csrrw x15, 0x2c2, x30	// Restore CSR

// Testing CSR 0x2c3
	csrr x2, 0x2c3	// Read CSR
	li x11, -1
	csrrw x24, 0x2c3, x11	// Write all 1s to CSR
	csrrw x24, 0x2c3, x0	// Write all 0s to CSR
	csrrs x24, 0x2c3, x11	// Set all CSR bits
	csrrc x24, 0x2c3, x11	// Clear all CSR bits
	csrrw x24, 0x2c3, x2	// Restore CSR

// Testing CSR 0x2c4
	csrr x4, 0x2c4	// Read CSR
	li x31, -1
	csrrw x22, 0x2c4, x31	// Write all 1s to CSR
	csrrw x22, 0x2c4, x0	// Write all 0s to CSR
	csrrs x22, 0x2c4, x31	// Set all CSR bits
	csrrc x22, 0x2c4, x31	// Clear all CSR bits
	csrrw x22, 0x2c4, x4	// Restore CSR

// Testing CSR 0x2c5
	csrr x7, 0x2c5	// Read CSR
	li x5, -1
	csrrw x6, 0x2c5, x5	// Write all 1s to CSR
	csrrw x6, 0x2c5, x0	// Write all 0s to CSR
	csrrs x6, 0x2c5, x5	// Set all CSR bits
	csrrc x6, 0x2c5, x5	// Clear all CSR bits
	csrrw x6, 0x2c5, x7	// Restore CSR

// Testing CSR 0x2c6
	csrr x20, 0x2c6	// Read CSR
	li x19, -1
	csrrw x25, 0x2c6, x19	// Write all 1s to CSR
	csrrw x25, 0x2c6, x0	// Write all 0s to CSR
	csrrs x25, 0x2c6, x19	// Set all CSR bits
	csrrc x25, 0x2c6, x19	// Clear all CSR bits
	csrrw x25, 0x2c6, x20	// Restore CSR

// Testing CSR 0x2c7
	csrr x4, 0x2c7	// Read CSR
	li x18, -1
	csrrw x31, 0x2c7, x18	// Write all 1s to CSR
	csrrw x31, 0x2c7, x0	// Write all 0s to CSR
	csrrs x31, 0x2c7, x18	// Set all CSR bits
	csrrc x31, 0x2c7, x18	// Clear all CSR bits
	csrrw x31, 0x2c7, x4	// Restore CSR

// Testing CSR 0x2c8
	csrr x30, 0x2c8	// Read CSR
	li x26, -1
	csrrw x12, 0x2c8, x26	// Write all 1s to CSR
	csrrw x12, 0x2c8, x0	// Write all 0s to CSR
	csrrs x12, 0x2c8, x26	// Set all CSR bits
	csrrc x12, 0x2c8, x26	// Clear all CSR bits
	csrrw x12, 0x2c8, x30	// Restore CSR

// Testing CSR 0x2c9
	csrr x10, 0x2c9	// Read CSR
	li x5, -1
	csrrw x19, 0x2c9, x5	// Write all 1s to CSR
	csrrw x19, 0x2c9, x0	// Write all 0s to CSR
	csrrs x19, 0x2c9, x5	// Set all CSR bits
	csrrc x19, 0x2c9, x5	// Clear all CSR bits
	csrrw x19, 0x2c9, x10	// Restore CSR

// Testing CSR 0x2ca
	csrr x9, 0x2ca	// Read CSR
	li x23, -1
	csrrw x27, 0x2ca, x23	// Write all 1s to CSR
	csrrw x27, 0x2ca, x0	// Write all 0s to CSR
	csrrs x27, 0x2ca, x23	// Set all CSR bits
	csrrc x27, 0x2ca, x23	// Clear all CSR bits
	csrrw x27, 0x2ca, x9	// Restore CSR

// Testing CSR 0x2cb
	csrr x21, 0x2cb	// Read CSR
	li x2, -1
	csrrw x28, 0x2cb, x2	// Write all 1s to CSR
	csrrw x28, 0x2cb, x0	// Write all 0s to CSR
	csrrs x28, 0x2cb, x2	// Set all CSR bits
	csrrc x28, 0x2cb, x2	// Clear all CSR bits
	csrrw x28, 0x2cb, x21	// Restore CSR

// Testing CSR 0x2cc
	csrr x12, 0x2cc	// Read CSR
	li x13, -1
	csrrw x26, 0x2cc, x13	// Write all 1s to CSR
	csrrw x26, 0x2cc, x0	// Write all 0s to CSR
	csrrs x26, 0x2cc, x13	// Set all CSR bits
	csrrc x26, 0x2cc, x13	// Clear all CSR bits
	csrrw x26, 0x2cc, x12	// Restore CSR

// Testing CSR 0x2cd
	csrr x15, 0x2cd	// Read CSR
	li x11, -1
	csrrw x5, 0x2cd, x11	// Write all 1s to CSR
	csrrw x5, 0x2cd, x0	// Write all 0s to CSR
	csrrs x5, 0x2cd, x11	// Set all CSR bits
	csrrc x5, 0x2cd, x11	// Clear all CSR bits
	csrrw x5, 0x2cd, x15	// Restore CSR

// Testing CSR 0x2ce
	csrr x6, 0x2ce	// Read CSR
	li x24, -1
	csrrw x17, 0x2ce, x24	// Write all 1s to CSR
	csrrw x17, 0x2ce, x0	// Write all 0s to CSR
	csrrs x17, 0x2ce, x24	// Set all CSR bits
	csrrc x17, 0x2ce, x24	// Clear all CSR bits
	csrrw x17, 0x2ce, x6	// Restore CSR

// Testing CSR 0x2cf
	csrr x30, 0x2cf	// Read CSR
	li x17, -1
	csrrw x5, 0x2cf, x17	// Write all 1s to CSR
	csrrw x5, 0x2cf, x0	// Write all 0s to CSR
	csrrs x5, 0x2cf, x17	// Set all CSR bits
	csrrc x5, 0x2cf, x17	// Clear all CSR bits
	csrrw x5, 0x2cf, x30	// Restore CSR

// Testing CSR 0x2d0
	csrr x14, 0x2d0	// Read CSR
	li x23, -1
	csrrw x7, 0x2d0, x23	// Write all 1s to CSR
	csrrw x7, 0x2d0, x0	// Write all 0s to CSR
	csrrs x7, 0x2d0, x23	// Set all CSR bits
	csrrc x7, 0x2d0, x23	// Clear all CSR bits
	csrrw x7, 0x2d0, x14	// Restore CSR

// Testing CSR 0x2d1
	csrr x8, 0x2d1	// Read CSR
	li x3, -1
	csrrw x18, 0x2d1, x3	// Write all 1s to CSR
	csrrw x18, 0x2d1, x0	// Write all 0s to CSR
	csrrs x18, 0x2d1, x3	// Set all CSR bits
	csrrc x18, 0x2d1, x3	// Clear all CSR bits
	csrrw x18, 0x2d1, x8	// Restore CSR

// Testing CSR 0x2d2
	csrr x19, 0x2d2	// Read CSR
	li x16, -1
	csrrw x5, 0x2d2, x16	// Write all 1s to CSR
	csrrw x5, 0x2d2, x0	// Write all 0s to CSR
	csrrs x5, 0x2d2, x16	// Set all CSR bits
	csrrc x5, 0x2d2, x16	// Clear all CSR bits
	csrrw x5, 0x2d2, x19	// Restore CSR

// Testing CSR 0x2d3
	csrr x17, 0x2d3	// Read CSR
	li x22, -1
	csrrw x26, 0x2d3, x22	// Write all 1s to CSR
	csrrw x26, 0x2d3, x0	// Write all 0s to CSR
	csrrs x26, 0x2d3, x22	// Set all CSR bits
	csrrc x26, 0x2d3, x22	// Clear all CSR bits
	csrrw x26, 0x2d3, x17	// Restore CSR

// Testing CSR 0x2d4
	csrr x9, 0x2d4	// Read CSR
	li x3, -1
	csrrw x6, 0x2d4, x3	// Write all 1s to CSR
	csrrw x6, 0x2d4, x0	// Write all 0s to CSR
	csrrs x6, 0x2d4, x3	// Set all CSR bits
	csrrc x6, 0x2d4, x3	// Clear all CSR bits
	csrrw x6, 0x2d4, x9	// Restore CSR

// Testing CSR 0x2d5
	csrr x29, 0x2d5	// Read CSR
	li x20, -1
	csrrw x1, 0x2d5, x20	// Write all 1s to CSR
	csrrw x1, 0x2d5, x0	// Write all 0s to CSR
	csrrs x1, 0x2d5, x20	// Set all CSR bits
	csrrc x1, 0x2d5, x20	// Clear all CSR bits
	csrrw x1, 0x2d5, x29	// Restore CSR

// Testing CSR 0x2d6
	csrr x5, 0x2d6	// Read CSR
	li x10, -1
	csrrw x16, 0x2d6, x10	// Write all 1s to CSR
	csrrw x16, 0x2d6, x0	// Write all 0s to CSR
	csrrs x16, 0x2d6, x10	// Set all CSR bits
	csrrc x16, 0x2d6, x10	// Clear all CSR bits
	csrrw x16, 0x2d6, x5	// Restore CSR

// Testing CSR 0x2d7
	csrr x8, 0x2d7	// Read CSR
	li x6, -1
	csrrw x31, 0x2d7, x6	// Write all 1s to CSR
	csrrw x31, 0x2d7, x0	// Write all 0s to CSR
	csrrs x31, 0x2d7, x6	// Set all CSR bits
	csrrc x31, 0x2d7, x6	// Clear all CSR bits
	csrrw x31, 0x2d7, x8	// Restore CSR

// Testing CSR 0x2d8
	csrr x30, 0x2d8	// Read CSR
	li x28, -1
	csrrw x26, 0x2d8, x28	// Write all 1s to CSR
	csrrw x26, 0x2d8, x0	// Write all 0s to CSR
	csrrs x26, 0x2d8, x28	// Set all CSR bits
	csrrc x26, 0x2d8, x28	// Clear all CSR bits
	csrrw x26, 0x2d8, x30	// Restore CSR

// Testing CSR 0x2d9
	csrr x22, 0x2d9	// Read CSR
	li x29, -1
	csrrw x24, 0x2d9, x29	// Write all 1s to CSR
	csrrw x24, 0x2d9, x0	// Write all 0s to CSR
	csrrs x24, 0x2d9, x29	// Set all CSR bits
	csrrc x24, 0x2d9, x29	// Clear all CSR bits
	csrrw x24, 0x2d9, x22	// Restore CSR

// Testing CSR 0x2da
	csrr x19, 0x2da	// Read CSR
	li x29, -1
	csrrw x20, 0x2da, x29	// Write all 1s to CSR
	csrrw x20, 0x2da, x0	// Write all 0s to CSR
	csrrs x20, 0x2da, x29	// Set all CSR bits
	csrrc x20, 0x2da, x29	// Clear all CSR bits
	csrrw x20, 0x2da, x19	// Restore CSR

// Testing CSR 0x2db
	csrr x12, 0x2db	// Read CSR
	li x27, -1
	csrrw x17, 0x2db, x27	// Write all 1s to CSR
	csrrw x17, 0x2db, x0	// Write all 0s to CSR
	csrrs x17, 0x2db, x27	// Set all CSR bits
	csrrc x17, 0x2db, x27	// Clear all CSR bits
	csrrw x17, 0x2db, x12	// Restore CSR

// Testing CSR 0x2dc
	csrr x2, 0x2dc	// Read CSR
	li x8, -1
	csrrw x18, 0x2dc, x8	// Write all 1s to CSR
	csrrw x18, 0x2dc, x0	// Write all 0s to CSR
	csrrs x18, 0x2dc, x8	// Set all CSR bits
	csrrc x18, 0x2dc, x8	// Clear all CSR bits
	csrrw x18, 0x2dc, x2	// Restore CSR

// Testing CSR 0x2dd
	csrr x14, 0x2dd	// Read CSR
	li x18, -1
	csrrw x16, 0x2dd, x18	// Write all 1s to CSR
	csrrw x16, 0x2dd, x0	// Write all 0s to CSR
	csrrs x16, 0x2dd, x18	// Set all CSR bits
	csrrc x16, 0x2dd, x18	// Clear all CSR bits
	csrrw x16, 0x2dd, x14	// Restore CSR

// Testing CSR 0x2de
	csrr x1, 0x2de	// Read CSR
	li x23, -1
	csrrw x19, 0x2de, x23	// Write all 1s to CSR
	csrrw x19, 0x2de, x0	// Write all 0s to CSR
	csrrs x19, 0x2de, x23	// Set all CSR bits
	csrrc x19, 0x2de, x23	// Clear all CSR bits
	csrrw x19, 0x2de, x1	// Restore CSR

// Testing CSR 0x2df
	csrr x1, 0x2df	// Read CSR
	li x22, -1
	csrrw x23, 0x2df, x22	// Write all 1s to CSR
	csrrw x23, 0x2df, x0	// Write all 0s to CSR
	csrrs x23, 0x2df, x22	// Set all CSR bits
	csrrc x23, 0x2df, x22	// Clear all CSR bits
	csrrw x23, 0x2df, x1	// Restore CSR

// Testing CSR 0x2e0
	csrr x21, 0x2e0	// Read CSR
	li x8, -1
	csrrw x4, 0x2e0, x8	// Write all 1s to CSR
	csrrw x4, 0x2e0, x0	// Write all 0s to CSR
	csrrs x4, 0x2e0, x8	// Set all CSR bits
	csrrc x4, 0x2e0, x8	// Clear all CSR bits
	csrrw x4, 0x2e0, x21	// Restore CSR

// Testing CSR 0x2e1
	csrr x2, 0x2e1	// Read CSR
	li x30, -1
	csrrw x1, 0x2e1, x30	// Write all 1s to CSR
	csrrw x1, 0x2e1, x0	// Write all 0s to CSR
	csrrs x1, 0x2e1, x30	// Set all CSR bits
	csrrc x1, 0x2e1, x30	// Clear all CSR bits
	csrrw x1, 0x2e1, x2	// Restore CSR

// Testing CSR 0x2e2
	csrr x12, 0x2e2	// Read CSR
	li x7, -1
	csrrw x27, 0x2e2, x7	// Write all 1s to CSR
	csrrw x27, 0x2e2, x0	// Write all 0s to CSR
	csrrs x27, 0x2e2, x7	// Set all CSR bits
	csrrc x27, 0x2e2, x7	// Clear all CSR bits
	csrrw x27, 0x2e2, x12	// Restore CSR

// Testing CSR 0x2e3
	csrr x27, 0x2e3	// Read CSR
	li x2, -1
	csrrw x28, 0x2e3, x2	// Write all 1s to CSR
	csrrw x28, 0x2e3, x0	// Write all 0s to CSR
	csrrs x28, 0x2e3, x2	// Set all CSR bits
	csrrc x28, 0x2e3, x2	// Clear all CSR bits
	csrrw x28, 0x2e3, x27	// Restore CSR

// Testing CSR 0x2e4
	csrr x23, 0x2e4	// Read CSR
	li x16, -1
	csrrw x2, 0x2e4, x16	// Write all 1s to CSR
	csrrw x2, 0x2e4, x0	// Write all 0s to CSR
	csrrs x2, 0x2e4, x16	// Set all CSR bits
	csrrc x2, 0x2e4, x16	// Clear all CSR bits
	csrrw x2, 0x2e4, x23	// Restore CSR

// Testing CSR 0x2e5
	csrr x26, 0x2e5	// Read CSR
	li x18, -1
	csrrw x23, 0x2e5, x18	// Write all 1s to CSR
	csrrw x23, 0x2e5, x0	// Write all 0s to CSR
	csrrs x23, 0x2e5, x18	// Set all CSR bits
	csrrc x23, 0x2e5, x18	// Clear all CSR bits
	csrrw x23, 0x2e5, x26	// Restore CSR

// Testing CSR 0x2e6
	csrr x30, 0x2e6	// Read CSR
	li x24, -1
	csrrw x28, 0x2e6, x24	// Write all 1s to CSR
	csrrw x28, 0x2e6, x0	// Write all 0s to CSR
	csrrs x28, 0x2e6, x24	// Set all CSR bits
	csrrc x28, 0x2e6, x24	// Clear all CSR bits
	csrrw x28, 0x2e6, x30	// Restore CSR

// Testing CSR 0x2e7
	csrr x8, 0x2e7	// Read CSR
	li x9, -1
	csrrw x24, 0x2e7, x9	// Write all 1s to CSR
	csrrw x24, 0x2e7, x0	// Write all 0s to CSR
	csrrs x24, 0x2e7, x9	// Set all CSR bits
	csrrc x24, 0x2e7, x9	// Clear all CSR bits
	csrrw x24, 0x2e7, x8	// Restore CSR

// Testing CSR 0x2e8
	csrr x27, 0x2e8	// Read CSR
	li x5, -1
	csrrw x11, 0x2e8, x5	// Write all 1s to CSR
	csrrw x11, 0x2e8, x0	// Write all 0s to CSR
	csrrs x11, 0x2e8, x5	// Set all CSR bits
	csrrc x11, 0x2e8, x5	// Clear all CSR bits
	csrrw x11, 0x2e8, x27	// Restore CSR

// Testing CSR 0x2e9
	csrr x10, 0x2e9	// Read CSR
	li x8, -1
	csrrw x15, 0x2e9, x8	// Write all 1s to CSR
	csrrw x15, 0x2e9, x0	// Write all 0s to CSR
	csrrs x15, 0x2e9, x8	// Set all CSR bits
	csrrc x15, 0x2e9, x8	// Clear all CSR bits
	csrrw x15, 0x2e9, x10	// Restore CSR

// Testing CSR 0x2ea
	csrr x1, 0x2ea	// Read CSR
	li x19, -1
	csrrw x9, 0x2ea, x19	// Write all 1s to CSR
	csrrw x9, 0x2ea, x0	// Write all 0s to CSR
	csrrs x9, 0x2ea, x19	// Set all CSR bits
	csrrc x9, 0x2ea, x19	// Clear all CSR bits
	csrrw x9, 0x2ea, x1	// Restore CSR

// Testing CSR 0x2eb
	csrr x15, 0x2eb	// Read CSR
	li x28, -1
	csrrw x29, 0x2eb, x28	// Write all 1s to CSR
	csrrw x29, 0x2eb, x0	// Write all 0s to CSR
	csrrs x29, 0x2eb, x28	// Set all CSR bits
	csrrc x29, 0x2eb, x28	// Clear all CSR bits
	csrrw x29, 0x2eb, x15	// Restore CSR

// Testing CSR 0x2ec
	csrr x11, 0x2ec	// Read CSR
	li x13, -1
	csrrw x14, 0x2ec, x13	// Write all 1s to CSR
	csrrw x14, 0x2ec, x0	// Write all 0s to CSR
	csrrs x14, 0x2ec, x13	// Set all CSR bits
	csrrc x14, 0x2ec, x13	// Clear all CSR bits
	csrrw x14, 0x2ec, x11	// Restore CSR

// Testing CSR 0x2ed
	csrr x11, 0x2ed	// Read CSR
	li x9, -1
	csrrw x31, 0x2ed, x9	// Write all 1s to CSR
	csrrw x31, 0x2ed, x0	// Write all 0s to CSR
	csrrs x31, 0x2ed, x9	// Set all CSR bits
	csrrc x31, 0x2ed, x9	// Clear all CSR bits
	csrrw x31, 0x2ed, x11	// Restore CSR

// Testing CSR 0x2ee
	csrr x2, 0x2ee	// Read CSR
	li x4, -1
	csrrw x1, 0x2ee, x4	// Write all 1s to CSR
	csrrw x1, 0x2ee, x0	// Write all 0s to CSR
	csrrs x1, 0x2ee, x4	// Set all CSR bits
	csrrc x1, 0x2ee, x4	// Clear all CSR bits
	csrrw x1, 0x2ee, x2	// Restore CSR

// Testing CSR 0x2ef
	csrr x25, 0x2ef	// Read CSR
	li x11, -1
	csrrw x16, 0x2ef, x11	// Write all 1s to CSR
	csrrw x16, 0x2ef, x0	// Write all 0s to CSR
	csrrs x16, 0x2ef, x11	// Set all CSR bits
	csrrc x16, 0x2ef, x11	// Clear all CSR bits
	csrrw x16, 0x2ef, x25	// Restore CSR

// Testing CSR 0x2f0
	csrr x21, 0x2f0	// Read CSR
	li x15, -1
	csrrw x6, 0x2f0, x15	// Write all 1s to CSR
	csrrw x6, 0x2f0, x0	// Write all 0s to CSR
	csrrs x6, 0x2f0, x15	// Set all CSR bits
	csrrc x6, 0x2f0, x15	// Clear all CSR bits
	csrrw x6, 0x2f0, x21	// Restore CSR

// Testing CSR 0x2f1
	csrr x21, 0x2f1	// Read CSR
	li x8, -1
	csrrw x4, 0x2f1, x8	// Write all 1s to CSR
	csrrw x4, 0x2f1, x0	// Write all 0s to CSR
	csrrs x4, 0x2f1, x8	// Set all CSR bits
	csrrc x4, 0x2f1, x8	// Clear all CSR bits
	csrrw x4, 0x2f1, x21	// Restore CSR

// Testing CSR 0x2f2
	csrr x3, 0x2f2	// Read CSR
	li x5, -1
	csrrw x9, 0x2f2, x5	// Write all 1s to CSR
	csrrw x9, 0x2f2, x0	// Write all 0s to CSR
	csrrs x9, 0x2f2, x5	// Set all CSR bits
	csrrc x9, 0x2f2, x5	// Clear all CSR bits
	csrrw x9, 0x2f2, x3	// Restore CSR

// Testing CSR 0x2f3
	csrr x14, 0x2f3	// Read CSR
	li x23, -1
	csrrw x1, 0x2f3, x23	// Write all 1s to CSR
	csrrw x1, 0x2f3, x0	// Write all 0s to CSR
	csrrs x1, 0x2f3, x23	// Set all CSR bits
	csrrc x1, 0x2f3, x23	// Clear all CSR bits
	csrrw x1, 0x2f3, x14	// Restore CSR

// Testing CSR 0x2f4
	csrr x4, 0x2f4	// Read CSR
	li x17, -1
	csrrw x16, 0x2f4, x17	// Write all 1s to CSR
	csrrw x16, 0x2f4, x0	// Write all 0s to CSR
	csrrs x16, 0x2f4, x17	// Set all CSR bits
	csrrc x16, 0x2f4, x17	// Clear all CSR bits
	csrrw x16, 0x2f4, x4	// Restore CSR

// Testing CSR 0x2f5
	csrr x9, 0x2f5	// Read CSR
	li x18, -1
	csrrw x26, 0x2f5, x18	// Write all 1s to CSR
	csrrw x26, 0x2f5, x0	// Write all 0s to CSR
	csrrs x26, 0x2f5, x18	// Set all CSR bits
	csrrc x26, 0x2f5, x18	// Clear all CSR bits
	csrrw x26, 0x2f5, x9	// Restore CSR

// Testing CSR 0x2f6
	csrr x19, 0x2f6	// Read CSR
	li x20, -1
	csrrw x21, 0x2f6, x20	// Write all 1s to CSR
	csrrw x21, 0x2f6, x0	// Write all 0s to CSR
	csrrs x21, 0x2f6, x20	// Set all CSR bits
	csrrc x21, 0x2f6, x20	// Clear all CSR bits
	csrrw x21, 0x2f6, x19	// Restore CSR

// Testing CSR 0x2f7
	csrr x10, 0x2f7	// Read CSR
	li x8, -1
	csrrw x28, 0x2f7, x8	// Write all 1s to CSR
	csrrw x28, 0x2f7, x0	// Write all 0s to CSR
	csrrs x28, 0x2f7, x8	// Set all CSR bits
	csrrc x28, 0x2f7, x8	// Clear all CSR bits
	csrrw x28, 0x2f7, x10	// Restore CSR

// Testing CSR 0x2f8
	csrr x24, 0x2f8	// Read CSR
	li x4, -1
	csrrw x22, 0x2f8, x4	// Write all 1s to CSR
	csrrw x22, 0x2f8, x0	// Write all 0s to CSR
	csrrs x22, 0x2f8, x4	// Set all CSR bits
	csrrc x22, 0x2f8, x4	// Clear all CSR bits
	csrrw x22, 0x2f8, x24	// Restore CSR

// Testing CSR 0x2f9
	csrr x29, 0x2f9	// Read CSR
	li x25, -1
	csrrw x6, 0x2f9, x25	// Write all 1s to CSR
	csrrw x6, 0x2f9, x0	// Write all 0s to CSR
	csrrs x6, 0x2f9, x25	// Set all CSR bits
	csrrc x6, 0x2f9, x25	// Clear all CSR bits
	csrrw x6, 0x2f9, x29	// Restore CSR

// Testing CSR 0x2fa
	csrr x22, 0x2fa	// Read CSR
	li x30, -1
	csrrw x21, 0x2fa, x30	// Write all 1s to CSR
	csrrw x21, 0x2fa, x0	// Write all 0s to CSR
	csrrs x21, 0x2fa, x30	// Set all CSR bits
	csrrc x21, 0x2fa, x30	// Clear all CSR bits
	csrrw x21, 0x2fa, x22	// Restore CSR

// Testing CSR 0x2fb
	csrr x5, 0x2fb	// Read CSR
	li x20, -1
	csrrw x23, 0x2fb, x20	// Write all 1s to CSR
	csrrw x23, 0x2fb, x0	// Write all 0s to CSR
	csrrs x23, 0x2fb, x20	// Set all CSR bits
	csrrc x23, 0x2fb, x20	// Clear all CSR bits
	csrrw x23, 0x2fb, x5	// Restore CSR

// Testing CSR 0x2fc
	csrr x24, 0x2fc	// Read CSR
	li x1, -1
	csrrw x6, 0x2fc, x1	// Write all 1s to CSR
	csrrw x6, 0x2fc, x0	// Write all 0s to CSR
	csrrs x6, 0x2fc, x1	// Set all CSR bits
	csrrc x6, 0x2fc, x1	// Clear all CSR bits
	csrrw x6, 0x2fc, x24	// Restore CSR

// Testing CSR 0x2fd
	csrr x4, 0x2fd	// Read CSR
	li x20, -1
	csrrw x3, 0x2fd, x20	// Write all 1s to CSR
	csrrw x3, 0x2fd, x0	// Write all 0s to CSR
	csrrs x3, 0x2fd, x20	// Set all CSR bits
	csrrc x3, 0x2fd, x20	// Clear all CSR bits
	csrrw x3, 0x2fd, x4	// Restore CSR

// Testing CSR 0x2fe
	csrr x26, 0x2fe	// Read CSR
	li x21, -1
	csrrw x19, 0x2fe, x21	// Write all 1s to CSR
	csrrw x19, 0x2fe, x0	// Write all 0s to CSR
	csrrs x19, 0x2fe, x21	// Set all CSR bits
	csrrc x19, 0x2fe, x21	// Clear all CSR bits
	csrrw x19, 0x2fe, x26	// Restore CSR

// Testing CSR 0x2ff
	csrr x29, 0x2ff	// Read CSR
	li x18, -1
	csrrw x26, 0x2ff, x18	// Write all 1s to CSR
	csrrw x26, 0x2ff, x0	// Write all 0s to CSR
	csrrs x26, 0x2ff, x18	// Set all CSR bits
	csrrc x26, 0x2ff, x18	// Clear all CSR bits
	csrrw x26, 0x2ff, x29	// Restore CSR

// Testing CSR 0x300
	csrr x27, 0x300	// Read CSR
	li x10, -1
	csrrw x17, 0x300, x10	// Write all 1s to CSR
	csrrw x17, 0x300, x0	// Write all 0s to CSR
	csrrs x17, 0x300, x10	// Set all CSR bits
	csrrc x17, 0x300, x10	// Clear all CSR bits
	csrrw x17, 0x300, x27	// Restore CSR

// Testing CSR 0x301
	csrr x15, 0x301	// Read CSR
	li x26, -1
	csrrw x13, 0x301, x26	// Write all 1s to CSR
	csrrw x13, 0x301, x0	// Write all 0s to CSR
	csrrs x13, 0x301, x26	// Set all CSR bits
	csrrc x13, 0x301, x26	// Clear all CSR bits
	csrrw x13, 0x301, x15	// Restore CSR

// Testing CSR 0x302
	csrr x1, 0x302	// Read CSR
	li x28, -1
	csrrw x5, 0x302, x28	// Write all 1s to CSR
	csrrw x5, 0x302, x0	// Write all 0s to CSR
	csrrs x5, 0x302, x28	// Set all CSR bits
	csrrc x5, 0x302, x28	// Clear all CSR bits
	csrrw x5, 0x302, x1	// Restore CSR

// Testing CSR 0x303
	csrr x8, 0x303	// Read CSR
	li x28, -1
	csrrw x19, 0x303, x28	// Write all 1s to CSR
	csrrw x19, 0x303, x0	// Write all 0s to CSR
	csrrs x19, 0x303, x28	// Set all CSR bits
	csrrc x19, 0x303, x28	// Clear all CSR bits
	csrrw x19, 0x303, x8	// Restore CSR

// Testing CSR 0x304
	csrr x26, 0x304	// Read CSR
	li x7, -1
	csrrw x16, 0x304, x7	// Write all 1s to CSR
	csrrw x16, 0x304, x0	// Write all 0s to CSR
	csrrs x16, 0x304, x7	// Set all CSR bits
	csrrc x16, 0x304, x7	// Clear all CSR bits
	csrrw x16, 0x304, x26	// Restore CSR

// Testing CSR 0x305
	csrr x27, 0x305	// Read CSR
	li x28, -1
	csrrw x14, 0x305, x28	// Write all 1s to CSR
	csrrw x14, 0x305, x0	// Write all 0s to CSR
	csrrs x14, 0x305, x28	// Set all CSR bits
	csrrc x14, 0x305, x28	// Clear all CSR bits
	csrrw x14, 0x305, x27	// Restore CSR

// Testing CSR 0x306
	csrr x20, 0x306	// Read CSR
	li x19, -1
	csrrw x26, 0x306, x19	// Write all 1s to CSR
	csrrw x26, 0x306, x0	// Write all 0s to CSR
	csrrs x26, 0x306, x19	// Set all CSR bits
	csrrc x26, 0x306, x19	// Clear all CSR bits
	csrrw x26, 0x306, x20	// Restore CSR

// Testing CSR 0x307
	csrr x14, 0x307	// Read CSR
	li x9, -1
	csrrw x8, 0x307, x9	// Write all 1s to CSR
	csrrw x8, 0x307, x0	// Write all 0s to CSR
	csrrs x8, 0x307, x9	// Set all CSR bits
	csrrc x8, 0x307, x9	// Clear all CSR bits
	csrrw x8, 0x307, x14	// Restore CSR

// Testing CSR 0x308
	csrr x19, 0x308	// Read CSR
	li x24, -1
	csrrw x17, 0x308, x24	// Write all 1s to CSR
	csrrw x17, 0x308, x0	// Write all 0s to CSR
	csrrs x17, 0x308, x24	// Set all CSR bits
	csrrc x17, 0x308, x24	// Clear all CSR bits
	csrrw x17, 0x308, x19	// Restore CSR

// Testing CSR 0x309
	csrr x1, 0x309	// Read CSR
	li x21, -1
	csrrw x25, 0x309, x21	// Write all 1s to CSR
	csrrw x25, 0x309, x0	// Write all 0s to CSR
	csrrs x25, 0x309, x21	// Set all CSR bits
	csrrc x25, 0x309, x21	// Clear all CSR bits
	csrrw x25, 0x309, x1	// Restore CSR

// Testing CSR 0x30a
	csrr x13, 0x30a	// Read CSR
	li x5, -1
	csrrw x29, 0x30a, x5	// Write all 1s to CSR
	csrrw x29, 0x30a, x0	// Write all 0s to CSR
	csrrs x29, 0x30a, x5	// Set all CSR bits
	csrrc x29, 0x30a, x5	// Clear all CSR bits
	csrrw x29, 0x30a, x13	// Restore CSR

// Testing CSR 0x30b
	csrr x21, 0x30b	// Read CSR
	li x11, -1
	csrrw x4, 0x30b, x11	// Write all 1s to CSR
	csrrw x4, 0x30b, x0	// Write all 0s to CSR
	csrrs x4, 0x30b, x11	// Set all CSR bits
	csrrc x4, 0x30b, x11	// Clear all CSR bits
	csrrw x4, 0x30b, x21	// Restore CSR

// Testing CSR 0x30c
	csrr x8, 0x30c	// Read CSR
	li x3, -1
	csrrw x7, 0x30c, x3	// Write all 1s to CSR
	csrrw x7, 0x30c, x0	// Write all 0s to CSR
	csrrs x7, 0x30c, x3	// Set all CSR bits
	csrrc x7, 0x30c, x3	// Clear all CSR bits
	csrrw x7, 0x30c, x8	// Restore CSR

// Testing CSR 0x30d
	csrr x12, 0x30d	// Read CSR
	li x31, -1
	csrrw x25, 0x30d, x31	// Write all 1s to CSR
	csrrw x25, 0x30d, x0	// Write all 0s to CSR
	csrrs x25, 0x30d, x31	// Set all CSR bits
	csrrc x25, 0x30d, x31	// Clear all CSR bits
	csrrw x25, 0x30d, x12	// Restore CSR

// Testing CSR 0x30e
	csrr x14, 0x30e	// Read CSR
	li x5, -1
	csrrw x30, 0x30e, x5	// Write all 1s to CSR
	csrrw x30, 0x30e, x0	// Write all 0s to CSR
	csrrs x30, 0x30e, x5	// Set all CSR bits
	csrrc x30, 0x30e, x5	// Clear all CSR bits
	csrrw x30, 0x30e, x14	// Restore CSR

// Testing CSR 0x30f
	csrr x21, 0x30f	// Read CSR
	li x6, -1
	csrrw x4, 0x30f, x6	// Write all 1s to CSR
	csrrw x4, 0x30f, x0	// Write all 0s to CSR
	csrrs x4, 0x30f, x6	// Set all CSR bits
	csrrc x4, 0x30f, x6	// Clear all CSR bits
	csrrw x4, 0x30f, x21	// Restore CSR

// Testing CSR 0x310
	csrr x3, 0x310	// Read CSR
	li x16, -1
	csrrw x2, 0x310, x16	// Write all 1s to CSR
	csrrw x2, 0x310, x0	// Write all 0s to CSR
	csrrs x2, 0x310, x16	// Set all CSR bits
	csrrc x2, 0x310, x16	// Clear all CSR bits
	csrrw x2, 0x310, x3	// Restore CSR

// Testing CSR 0x311
	csrr x11, 0x311	// Read CSR
	li x3, -1
	csrrw x16, 0x311, x3	// Write all 1s to CSR
	csrrw x16, 0x311, x0	// Write all 0s to CSR
	csrrs x16, 0x311, x3	// Set all CSR bits
	csrrc x16, 0x311, x3	// Clear all CSR bits
	csrrw x16, 0x311, x11	// Restore CSR

// Testing CSR 0x312
	csrr x3, 0x312	// Read CSR
	li x29, -1
	csrrw x13, 0x312, x29	// Write all 1s to CSR
	csrrw x13, 0x312, x0	// Write all 0s to CSR
	csrrs x13, 0x312, x29	// Set all CSR bits
	csrrc x13, 0x312, x29	// Clear all CSR bits
	csrrw x13, 0x312, x3	// Restore CSR

// Testing CSR 0x313
	csrr x18, 0x313	// Read CSR
	li x4, -1
	csrrw x23, 0x313, x4	// Write all 1s to CSR
	csrrw x23, 0x313, x0	// Write all 0s to CSR
	csrrs x23, 0x313, x4	// Set all CSR bits
	csrrc x23, 0x313, x4	// Clear all CSR bits
	csrrw x23, 0x313, x18	// Restore CSR

// Testing CSR 0x314
	csrr x8, 0x314	// Read CSR
	li x4, -1
	csrrw x12, 0x314, x4	// Write all 1s to CSR
	csrrw x12, 0x314, x0	// Write all 0s to CSR
	csrrs x12, 0x314, x4	// Set all CSR bits
	csrrc x12, 0x314, x4	// Clear all CSR bits
	csrrw x12, 0x314, x8	// Restore CSR

// Testing CSR 0x315
	csrr x18, 0x315	// Read CSR
	li x17, -1
	csrrw x10, 0x315, x17	// Write all 1s to CSR
	csrrw x10, 0x315, x0	// Write all 0s to CSR
	csrrs x10, 0x315, x17	// Set all CSR bits
	csrrc x10, 0x315, x17	// Clear all CSR bits
	csrrw x10, 0x315, x18	// Restore CSR

// Testing CSR 0x316
	csrr x21, 0x316	// Read CSR
	li x29, -1
	csrrw x14, 0x316, x29	// Write all 1s to CSR
	csrrw x14, 0x316, x0	// Write all 0s to CSR
	csrrs x14, 0x316, x29	// Set all CSR bits
	csrrc x14, 0x316, x29	// Clear all CSR bits
	csrrw x14, 0x316, x21	// Restore CSR

// Testing CSR 0x317
	csrr x22, 0x317	// Read CSR
	li x10, -1
	csrrw x9, 0x317, x10	// Write all 1s to CSR
	csrrw x9, 0x317, x0	// Write all 0s to CSR
	csrrs x9, 0x317, x10	// Set all CSR bits
	csrrc x9, 0x317, x10	// Clear all CSR bits
	csrrw x9, 0x317, x22	// Restore CSR

// Testing CSR 0x318
	csrr x31, 0x318	// Read CSR
	li x23, -1
	csrrw x26, 0x318, x23	// Write all 1s to CSR
	csrrw x26, 0x318, x0	// Write all 0s to CSR
	csrrs x26, 0x318, x23	// Set all CSR bits
	csrrc x26, 0x318, x23	// Clear all CSR bits
	csrrw x26, 0x318, x31	// Restore CSR

// Testing CSR 0x319
	csrr x5, 0x319	// Read CSR
	li x2, -1
	csrrw x14, 0x319, x2	// Write all 1s to CSR
	csrrw x14, 0x319, x0	// Write all 0s to CSR
	csrrs x14, 0x319, x2	// Set all CSR bits
	csrrc x14, 0x319, x2	// Clear all CSR bits
	csrrw x14, 0x319, x5	// Restore CSR

// Testing CSR 0x31a
	csrr x11, 0x31a	// Read CSR
	li x4, -1
	csrrw x19, 0x31a, x4	// Write all 1s to CSR
	csrrw x19, 0x31a, x0	// Write all 0s to CSR
	csrrs x19, 0x31a, x4	// Set all CSR bits
	csrrc x19, 0x31a, x4	// Clear all CSR bits
	csrrw x19, 0x31a, x11	// Restore CSR

// Testing CSR 0x31b
	csrr x11, 0x31b	// Read CSR
	li x31, -1
	csrrw x13, 0x31b, x31	// Write all 1s to CSR
	csrrw x13, 0x31b, x0	// Write all 0s to CSR
	csrrs x13, 0x31b, x31	// Set all CSR bits
	csrrc x13, 0x31b, x31	// Clear all CSR bits
	csrrw x13, 0x31b, x11	// Restore CSR

// Testing CSR 0x31c
	csrr x7, 0x31c	// Read CSR
	li x3, -1
	csrrw x28, 0x31c, x3	// Write all 1s to CSR
	csrrw x28, 0x31c, x0	// Write all 0s to CSR
	csrrs x28, 0x31c, x3	// Set all CSR bits
	csrrc x28, 0x31c, x3	// Clear all CSR bits
	csrrw x28, 0x31c, x7	// Restore CSR

// Testing CSR 0x31d
	csrr x6, 0x31d	// Read CSR
	li x12, -1
	csrrw x31, 0x31d, x12	// Write all 1s to CSR
	csrrw x31, 0x31d, x0	// Write all 0s to CSR
	csrrs x31, 0x31d, x12	// Set all CSR bits
	csrrc x31, 0x31d, x12	// Clear all CSR bits
	csrrw x31, 0x31d, x6	// Restore CSR

// Testing CSR 0x31e
	csrr x18, 0x31e	// Read CSR
	li x20, -1
	csrrw x22, 0x31e, x20	// Write all 1s to CSR
	csrrw x22, 0x31e, x0	// Write all 0s to CSR
	csrrs x22, 0x31e, x20	// Set all CSR bits
	csrrc x22, 0x31e, x20	// Clear all CSR bits
	csrrw x22, 0x31e, x18	// Restore CSR

// Testing CSR 0x31f
	csrr x22, 0x31f	// Read CSR
	li x2, -1
	csrrw x26, 0x31f, x2	// Write all 1s to CSR
	csrrw x26, 0x31f, x0	// Write all 0s to CSR
	csrrs x26, 0x31f, x2	// Set all CSR bits
	csrrc x26, 0x31f, x2	// Clear all CSR bits
	csrrw x26, 0x31f, x22	// Restore CSR

// Testing CSR 0x320
	csrr x11, 0x320	// Read CSR
	li x3, -1
	csrrw x31, 0x320, x3	// Write all 1s to CSR
	csrrw x31, 0x320, x0	// Write all 0s to CSR
	csrrs x31, 0x320, x3	// Set all CSR bits
	csrrc x31, 0x320, x3	// Clear all CSR bits
	csrrw x31, 0x320, x11	// Restore CSR

// Testing CSR 0x321
	csrr x14, 0x321	// Read CSR
	li x24, -1
	csrrw x23, 0x321, x24	// Write all 1s to CSR
	csrrw x23, 0x321, x0	// Write all 0s to CSR
	csrrs x23, 0x321, x24	// Set all CSR bits
	csrrc x23, 0x321, x24	// Clear all CSR bits
	csrrw x23, 0x321, x14	// Restore CSR

// Testing CSR 0x322
	csrr x23, 0x322	// Read CSR
	li x17, -1
	csrrw x18, 0x322, x17	// Write all 1s to CSR
	csrrw x18, 0x322, x0	// Write all 0s to CSR
	csrrs x18, 0x322, x17	// Set all CSR bits
	csrrc x18, 0x322, x17	// Clear all CSR bits
	csrrw x18, 0x322, x23	// Restore CSR

// Testing CSR 0x323
	csrr x22, 0x323	// Read CSR
	li x1, -1
	csrrw x13, 0x323, x1	// Write all 1s to CSR
	csrrw x13, 0x323, x0	// Write all 0s to CSR
	csrrs x13, 0x323, x1	// Set all CSR bits
	csrrc x13, 0x323, x1	// Clear all CSR bits
	csrrw x13, 0x323, x22	// Restore CSR

// Testing CSR 0x324
	csrr x21, 0x324	// Read CSR
	li x29, -1
	csrrw x5, 0x324, x29	// Write all 1s to CSR
	csrrw x5, 0x324, x0	// Write all 0s to CSR
	csrrs x5, 0x324, x29	// Set all CSR bits
	csrrc x5, 0x324, x29	// Clear all CSR bits
	csrrw x5, 0x324, x21	// Restore CSR

// Testing CSR 0x325
	csrr x18, 0x325	// Read CSR
	li x25, -1
	csrrw x4, 0x325, x25	// Write all 1s to CSR
	csrrw x4, 0x325, x0	// Write all 0s to CSR
	csrrs x4, 0x325, x25	// Set all CSR bits
	csrrc x4, 0x325, x25	// Clear all CSR bits
	csrrw x4, 0x325, x18	// Restore CSR

// Testing CSR 0x326
	csrr x25, 0x326	// Read CSR
	li x16, -1
	csrrw x7, 0x326, x16	// Write all 1s to CSR
	csrrw x7, 0x326, x0	// Write all 0s to CSR
	csrrs x7, 0x326, x16	// Set all CSR bits
	csrrc x7, 0x326, x16	// Clear all CSR bits
	csrrw x7, 0x326, x25	// Restore CSR

// Testing CSR 0x327
	csrr x22, 0x327	// Read CSR
	li x12, -1
	csrrw x10, 0x327, x12	// Write all 1s to CSR
	csrrw x10, 0x327, x0	// Write all 0s to CSR
	csrrs x10, 0x327, x12	// Set all CSR bits
	csrrc x10, 0x327, x12	// Clear all CSR bits
	csrrw x10, 0x327, x22	// Restore CSR

// Testing CSR 0x328
	csrr x1, 0x328	// Read CSR
	li x24, -1
	csrrw x16, 0x328, x24	// Write all 1s to CSR
	csrrw x16, 0x328, x0	// Write all 0s to CSR
	csrrs x16, 0x328, x24	// Set all CSR bits
	csrrc x16, 0x328, x24	// Clear all CSR bits
	csrrw x16, 0x328, x1	// Restore CSR

// Testing CSR 0x329
	csrr x31, 0x329	// Read CSR
	li x2, -1
	csrrw x13, 0x329, x2	// Write all 1s to CSR
	csrrw x13, 0x329, x0	// Write all 0s to CSR
	csrrs x13, 0x329, x2	// Set all CSR bits
	csrrc x13, 0x329, x2	// Clear all CSR bits
	csrrw x13, 0x329, x31	// Restore CSR

// Testing CSR 0x32a
	csrr x1, 0x32a	// Read CSR
	li x13, -1
	csrrw x20, 0x32a, x13	// Write all 1s to CSR
	csrrw x20, 0x32a, x0	// Write all 0s to CSR
	csrrs x20, 0x32a, x13	// Set all CSR bits
	csrrc x20, 0x32a, x13	// Clear all CSR bits
	csrrw x20, 0x32a, x1	// Restore CSR

// Testing CSR 0x32b
	csrr x2, 0x32b	// Read CSR
	li x8, -1
	csrrw x28, 0x32b, x8	// Write all 1s to CSR
	csrrw x28, 0x32b, x0	// Write all 0s to CSR
	csrrs x28, 0x32b, x8	// Set all CSR bits
	csrrc x28, 0x32b, x8	// Clear all CSR bits
	csrrw x28, 0x32b, x2	// Restore CSR

// Testing CSR 0x32c
	csrr x9, 0x32c	// Read CSR
	li x30, -1
	csrrw x17, 0x32c, x30	// Write all 1s to CSR
	csrrw x17, 0x32c, x0	// Write all 0s to CSR
	csrrs x17, 0x32c, x30	// Set all CSR bits
	csrrc x17, 0x32c, x30	// Clear all CSR bits
	csrrw x17, 0x32c, x9	// Restore CSR

// Testing CSR 0x32d
	csrr x27, 0x32d	// Read CSR
	li x9, -1
	csrrw x25, 0x32d, x9	// Write all 1s to CSR
	csrrw x25, 0x32d, x0	// Write all 0s to CSR
	csrrs x25, 0x32d, x9	// Set all CSR bits
	csrrc x25, 0x32d, x9	// Clear all CSR bits
	csrrw x25, 0x32d, x27	// Restore CSR

// Testing CSR 0x32e
	csrr x28, 0x32e	// Read CSR
	li x15, -1
	csrrw x10, 0x32e, x15	// Write all 1s to CSR
	csrrw x10, 0x32e, x0	// Write all 0s to CSR
	csrrs x10, 0x32e, x15	// Set all CSR bits
	csrrc x10, 0x32e, x15	// Clear all CSR bits
	csrrw x10, 0x32e, x28	// Restore CSR

// Testing CSR 0x32f
	csrr x30, 0x32f	// Read CSR
	li x4, -1
	csrrw x7, 0x32f, x4	// Write all 1s to CSR
	csrrw x7, 0x32f, x0	// Write all 0s to CSR
	csrrs x7, 0x32f, x4	// Set all CSR bits
	csrrc x7, 0x32f, x4	// Clear all CSR bits
	csrrw x7, 0x32f, x30	// Restore CSR

// Testing CSR 0x330
	csrr x21, 0x330	// Read CSR
	li x14, -1
	csrrw x4, 0x330, x14	// Write all 1s to CSR
	csrrw x4, 0x330, x0	// Write all 0s to CSR
	csrrs x4, 0x330, x14	// Set all CSR bits
	csrrc x4, 0x330, x14	// Clear all CSR bits
	csrrw x4, 0x330, x21	// Restore CSR

// Testing CSR 0x331
	csrr x15, 0x331	// Read CSR
	li x24, -1
	csrrw x23, 0x331, x24	// Write all 1s to CSR
	csrrw x23, 0x331, x0	// Write all 0s to CSR
	csrrs x23, 0x331, x24	// Set all CSR bits
	csrrc x23, 0x331, x24	// Clear all CSR bits
	csrrw x23, 0x331, x15	// Restore CSR

// Testing CSR 0x332
	csrr x23, 0x332	// Read CSR
	li x11, -1
	csrrw x6, 0x332, x11	// Write all 1s to CSR
	csrrw x6, 0x332, x0	// Write all 0s to CSR
	csrrs x6, 0x332, x11	// Set all CSR bits
	csrrc x6, 0x332, x11	// Clear all CSR bits
	csrrw x6, 0x332, x23	// Restore CSR

// Testing CSR 0x333
	csrr x15, 0x333	// Read CSR
	li x9, -1
	csrrw x3, 0x333, x9	// Write all 1s to CSR
	csrrw x3, 0x333, x0	// Write all 0s to CSR
	csrrs x3, 0x333, x9	// Set all CSR bits
	csrrc x3, 0x333, x9	// Clear all CSR bits
	csrrw x3, 0x333, x15	// Restore CSR

// Testing CSR 0x334
	csrr x11, 0x334	// Read CSR
	li x25, -1
	csrrw x29, 0x334, x25	// Write all 1s to CSR
	csrrw x29, 0x334, x0	// Write all 0s to CSR
	csrrs x29, 0x334, x25	// Set all CSR bits
	csrrc x29, 0x334, x25	// Clear all CSR bits
	csrrw x29, 0x334, x11	// Restore CSR

// Testing CSR 0x335
	csrr x16, 0x335	// Read CSR
	li x17, -1
	csrrw x25, 0x335, x17	// Write all 1s to CSR
	csrrw x25, 0x335, x0	// Write all 0s to CSR
	csrrs x25, 0x335, x17	// Set all CSR bits
	csrrc x25, 0x335, x17	// Clear all CSR bits
	csrrw x25, 0x335, x16	// Restore CSR

// Testing CSR 0x336
	csrr x28, 0x336	// Read CSR
	li x8, -1
	csrrw x18, 0x336, x8	// Write all 1s to CSR
	csrrw x18, 0x336, x0	// Write all 0s to CSR
	csrrs x18, 0x336, x8	// Set all CSR bits
	csrrc x18, 0x336, x8	// Clear all CSR bits
	csrrw x18, 0x336, x28	// Restore CSR

// Testing CSR 0x337
	csrr x3, 0x337	// Read CSR
	li x9, -1
	csrrw x23, 0x337, x9	// Write all 1s to CSR
	csrrw x23, 0x337, x0	// Write all 0s to CSR
	csrrs x23, 0x337, x9	// Set all CSR bits
	csrrc x23, 0x337, x9	// Clear all CSR bits
	csrrw x23, 0x337, x3	// Restore CSR

// Testing CSR 0x338
	csrr x7, 0x338	// Read CSR
	li x3, -1
	csrrw x16, 0x338, x3	// Write all 1s to CSR
	csrrw x16, 0x338, x0	// Write all 0s to CSR
	csrrs x16, 0x338, x3	// Set all CSR bits
	csrrc x16, 0x338, x3	// Clear all CSR bits
	csrrw x16, 0x338, x7	// Restore CSR

// Testing CSR 0x339
	csrr x1, 0x339	// Read CSR
	li x2, -1
	csrrw x10, 0x339, x2	// Write all 1s to CSR
	csrrw x10, 0x339, x0	// Write all 0s to CSR
	csrrs x10, 0x339, x2	// Set all CSR bits
	csrrc x10, 0x339, x2	// Clear all CSR bits
	csrrw x10, 0x339, x1	// Restore CSR

// Testing CSR 0x33a
	csrr x29, 0x33a	// Read CSR
	li x26, -1
	csrrw x7, 0x33a, x26	// Write all 1s to CSR
	csrrw x7, 0x33a, x0	// Write all 0s to CSR
	csrrs x7, 0x33a, x26	// Set all CSR bits
	csrrc x7, 0x33a, x26	// Clear all CSR bits
	csrrw x7, 0x33a, x29	// Restore CSR

// Testing CSR 0x33b
	csrr x30, 0x33b	// Read CSR
	li x24, -1
	csrrw x23, 0x33b, x24	// Write all 1s to CSR
	csrrw x23, 0x33b, x0	// Write all 0s to CSR
	csrrs x23, 0x33b, x24	// Set all CSR bits
	csrrc x23, 0x33b, x24	// Clear all CSR bits
	csrrw x23, 0x33b, x30	// Restore CSR

// Testing CSR 0x33c
	csrr x4, 0x33c	// Read CSR
	li x13, -1
	csrrw x10, 0x33c, x13	// Write all 1s to CSR
	csrrw x10, 0x33c, x0	// Write all 0s to CSR
	csrrs x10, 0x33c, x13	// Set all CSR bits
	csrrc x10, 0x33c, x13	// Clear all CSR bits
	csrrw x10, 0x33c, x4	// Restore CSR

// Testing CSR 0x33d
	csrr x16, 0x33d	// Read CSR
	li x4, -1
	csrrw x18, 0x33d, x4	// Write all 1s to CSR
	csrrw x18, 0x33d, x0	// Write all 0s to CSR
	csrrs x18, 0x33d, x4	// Set all CSR bits
	csrrc x18, 0x33d, x4	// Clear all CSR bits
	csrrw x18, 0x33d, x16	// Restore CSR

// Testing CSR 0x33e
	csrr x10, 0x33e	// Read CSR
	li x25, -1
	csrrw x6, 0x33e, x25	// Write all 1s to CSR
	csrrw x6, 0x33e, x0	// Write all 0s to CSR
	csrrs x6, 0x33e, x25	// Set all CSR bits
	csrrc x6, 0x33e, x25	// Clear all CSR bits
	csrrw x6, 0x33e, x10	// Restore CSR

// Testing CSR 0x33f
	csrr x11, 0x33f	// Read CSR
	li x2, -1
	csrrw x4, 0x33f, x2	// Write all 1s to CSR
	csrrw x4, 0x33f, x0	// Write all 0s to CSR
	csrrs x4, 0x33f, x2	// Set all CSR bits
	csrrc x4, 0x33f, x2	// Clear all CSR bits
	csrrw x4, 0x33f, x11	// Restore CSR

// Testing CSR 0x340
	csrr x1, 0x340	// Read CSR
	li x16, -1
	csrrw x7, 0x340, x16	// Write all 1s to CSR
	csrrw x7, 0x340, x0	// Write all 0s to CSR
	csrrs x7, 0x340, x16	// Set all CSR bits
	csrrc x7, 0x340, x16	// Clear all CSR bits
	csrrw x7, 0x340, x1	// Restore CSR

// Testing CSR 0x341
	csrr x20, 0x341	// Read CSR
	li x28, -1
	csrrw x11, 0x341, x28	// Write all 1s to CSR
	csrrw x11, 0x341, x0	// Write all 0s to CSR
	csrrs x11, 0x341, x28	// Set all CSR bits
	csrrc x11, 0x341, x28	// Clear all CSR bits
	csrrw x11, 0x341, x20	// Restore CSR

// Testing CSR 0x342
	csrr x14, 0x342	// Read CSR
	li x8, -1
	csrrw x1, 0x342, x8	// Write all 1s to CSR
	csrrw x1, 0x342, x0	// Write all 0s to CSR
	csrrs x1, 0x342, x8	// Set all CSR bits
	csrrc x1, 0x342, x8	// Clear all CSR bits
	csrrw x1, 0x342, x14	// Restore CSR

// Testing CSR 0x343
	csrr x27, 0x343	// Read CSR
	li x22, -1
	csrrw x24, 0x343, x22	// Write all 1s to CSR
	csrrw x24, 0x343, x0	// Write all 0s to CSR
	csrrs x24, 0x343, x22	// Set all CSR bits
	csrrc x24, 0x343, x22	// Clear all CSR bits
	csrrw x24, 0x343, x27	// Restore CSR

// Testing CSR 0x344
	csrr x8, 0x344	// Read CSR
	li x24, -1
	csrrw x16, 0x344, x24	// Write all 1s to CSR
	csrrw x16, 0x344, x0	// Write all 0s to CSR
	csrrs x16, 0x344, x24	// Set all CSR bits
	csrrc x16, 0x344, x24	// Clear all CSR bits
	csrrw x16, 0x344, x8	// Restore CSR

// Testing CSR 0x345
	csrr x16, 0x345	// Read CSR
	li x25, -1
	csrrw x11, 0x345, x25	// Write all 1s to CSR
	csrrw x11, 0x345, x0	// Write all 0s to CSR
	csrrs x11, 0x345, x25	// Set all CSR bits
	csrrc x11, 0x345, x25	// Clear all CSR bits
	csrrw x11, 0x345, x16	// Restore CSR

// Testing CSR 0x346
	csrr x17, 0x346	// Read CSR
	li x25, -1
	csrrw x19, 0x346, x25	// Write all 1s to CSR
	csrrw x19, 0x346, x0	// Write all 0s to CSR
	csrrs x19, 0x346, x25	// Set all CSR bits
	csrrc x19, 0x346, x25	// Clear all CSR bits
	csrrw x19, 0x346, x17	// Restore CSR

// Testing CSR 0x347
	csrr x6, 0x347	// Read CSR
	li x21, -1
	csrrw x11, 0x347, x21	// Write all 1s to CSR
	csrrw x11, 0x347, x0	// Write all 0s to CSR
	csrrs x11, 0x347, x21	// Set all CSR bits
	csrrc x11, 0x347, x21	// Clear all CSR bits
	csrrw x11, 0x347, x6	// Restore CSR

// Testing CSR 0x348
	csrr x8, 0x348	// Read CSR
	li x16, -1
	csrrw x2, 0x348, x16	// Write all 1s to CSR
	csrrw x2, 0x348, x0	// Write all 0s to CSR
	csrrs x2, 0x348, x16	// Set all CSR bits
	csrrc x2, 0x348, x16	// Clear all CSR bits
	csrrw x2, 0x348, x8	// Restore CSR

// Testing CSR 0x349
	csrr x22, 0x349	// Read CSR
	li x11, -1
	csrrw x20, 0x349, x11	// Write all 1s to CSR
	csrrw x20, 0x349, x0	// Write all 0s to CSR
	csrrs x20, 0x349, x11	// Set all CSR bits
	csrrc x20, 0x349, x11	// Clear all CSR bits
	csrrw x20, 0x349, x22	// Restore CSR

// Testing CSR 0x34a
	csrr x23, 0x34a	// Read CSR
	li x26, -1
	csrrw x27, 0x34a, x26	// Write all 1s to CSR
	csrrw x27, 0x34a, x0	// Write all 0s to CSR
	csrrs x27, 0x34a, x26	// Set all CSR bits
	csrrc x27, 0x34a, x26	// Clear all CSR bits
	csrrw x27, 0x34a, x23	// Restore CSR

// Testing CSR 0x34b
	csrr x30, 0x34b	// Read CSR
	li x19, -1
	csrrw x23, 0x34b, x19	// Write all 1s to CSR
	csrrw x23, 0x34b, x0	// Write all 0s to CSR
	csrrs x23, 0x34b, x19	// Set all CSR bits
	csrrc x23, 0x34b, x19	// Clear all CSR bits
	csrrw x23, 0x34b, x30	// Restore CSR

// Testing CSR 0x34c
	csrr x28, 0x34c	// Read CSR
	li x2, -1
	csrrw x20, 0x34c, x2	// Write all 1s to CSR
	csrrw x20, 0x34c, x0	// Write all 0s to CSR
	csrrs x20, 0x34c, x2	// Set all CSR bits
	csrrc x20, 0x34c, x2	// Clear all CSR bits
	csrrw x20, 0x34c, x28	// Restore CSR

// Testing CSR 0x34d
	csrr x27, 0x34d	// Read CSR
	li x21, -1
	csrrw x25, 0x34d, x21	// Write all 1s to CSR
	csrrw x25, 0x34d, x0	// Write all 0s to CSR
	csrrs x25, 0x34d, x21	// Set all CSR bits
	csrrc x25, 0x34d, x21	// Clear all CSR bits
	csrrw x25, 0x34d, x27	// Restore CSR

// Testing CSR 0x34e
	csrr x1, 0x34e	// Read CSR
	li x25, -1
	csrrw x11, 0x34e, x25	// Write all 1s to CSR
	csrrw x11, 0x34e, x0	// Write all 0s to CSR
	csrrs x11, 0x34e, x25	// Set all CSR bits
	csrrc x11, 0x34e, x25	// Clear all CSR bits
	csrrw x11, 0x34e, x1	// Restore CSR

// Testing CSR 0x34f
	csrr x29, 0x34f	// Read CSR
	li x1, -1
	csrrw x24, 0x34f, x1	// Write all 1s to CSR
	csrrw x24, 0x34f, x0	// Write all 0s to CSR
	csrrs x24, 0x34f, x1	// Set all CSR bits
	csrrc x24, 0x34f, x1	// Clear all CSR bits
	csrrw x24, 0x34f, x29	// Restore CSR

// Testing CSR 0x350
	csrr x16, 0x350	// Read CSR
	li x22, -1
	csrrw x18, 0x350, x22	// Write all 1s to CSR
	csrrw x18, 0x350, x0	// Write all 0s to CSR
	csrrs x18, 0x350, x22	// Set all CSR bits
	csrrc x18, 0x350, x22	// Clear all CSR bits
	csrrw x18, 0x350, x16	// Restore CSR

// Testing CSR 0x351
	csrr x18, 0x351	// Read CSR
	li x29, -1
	csrrw x23, 0x351, x29	// Write all 1s to CSR
	csrrw x23, 0x351, x0	// Write all 0s to CSR
	csrrs x23, 0x351, x29	// Set all CSR bits
	csrrc x23, 0x351, x29	// Clear all CSR bits
	csrrw x23, 0x351, x18	// Restore CSR

// Testing CSR 0x352
	csrr x14, 0x352	// Read CSR
	li x17, -1
	csrrw x10, 0x352, x17	// Write all 1s to CSR
	csrrw x10, 0x352, x0	// Write all 0s to CSR
	csrrs x10, 0x352, x17	// Set all CSR bits
	csrrc x10, 0x352, x17	// Clear all CSR bits
	csrrw x10, 0x352, x14	// Restore CSR

// Testing CSR 0x353
	csrr x25, 0x353	// Read CSR
	li x28, -1
	csrrw x23, 0x353, x28	// Write all 1s to CSR
	csrrw x23, 0x353, x0	// Write all 0s to CSR
	csrrs x23, 0x353, x28	// Set all CSR bits
	csrrc x23, 0x353, x28	// Clear all CSR bits
	csrrw x23, 0x353, x25	// Restore CSR

// Testing CSR 0x354
	csrr x1, 0x354	// Read CSR
	li x3, -1
	csrrw x18, 0x354, x3	// Write all 1s to CSR
	csrrw x18, 0x354, x0	// Write all 0s to CSR
	csrrs x18, 0x354, x3	// Set all CSR bits
	csrrc x18, 0x354, x3	// Clear all CSR bits
	csrrw x18, 0x354, x1	// Restore CSR

// Testing CSR 0x355
	csrr x3, 0x355	// Read CSR
	li x20, -1
	csrrw x14, 0x355, x20	// Write all 1s to CSR
	csrrw x14, 0x355, x0	// Write all 0s to CSR
	csrrs x14, 0x355, x20	// Set all CSR bits
	csrrc x14, 0x355, x20	// Clear all CSR bits
	csrrw x14, 0x355, x3	// Restore CSR

// Testing CSR 0x356
	csrr x29, 0x356	// Read CSR
	li x1, -1
	csrrw x17, 0x356, x1	// Write all 1s to CSR
	csrrw x17, 0x356, x0	// Write all 0s to CSR
	csrrs x17, 0x356, x1	// Set all CSR bits
	csrrc x17, 0x356, x1	// Clear all CSR bits
	csrrw x17, 0x356, x29	// Restore CSR

// Testing CSR 0x357
	csrr x9, 0x357	// Read CSR
	li x28, -1
	csrrw x1, 0x357, x28	// Write all 1s to CSR
	csrrw x1, 0x357, x0	// Write all 0s to CSR
	csrrs x1, 0x357, x28	// Set all CSR bits
	csrrc x1, 0x357, x28	// Clear all CSR bits
	csrrw x1, 0x357, x9	// Restore CSR

// Testing CSR 0x358
	csrr x1, 0x358	// Read CSR
	li x28, -1
	csrrw x8, 0x358, x28	// Write all 1s to CSR
	csrrw x8, 0x358, x0	// Write all 0s to CSR
	csrrs x8, 0x358, x28	// Set all CSR bits
	csrrc x8, 0x358, x28	// Clear all CSR bits
	csrrw x8, 0x358, x1	// Restore CSR

// Testing CSR 0x359
	csrr x2, 0x359	// Read CSR
	li x28, -1
	csrrw x11, 0x359, x28	// Write all 1s to CSR
	csrrw x11, 0x359, x0	// Write all 0s to CSR
	csrrs x11, 0x359, x28	// Set all CSR bits
	csrrc x11, 0x359, x28	// Clear all CSR bits
	csrrw x11, 0x359, x2	// Restore CSR

// Testing CSR 0x35a
	csrr x1, 0x35a	// Read CSR
	li x29, -1
	csrrw x21, 0x35a, x29	// Write all 1s to CSR
	csrrw x21, 0x35a, x0	// Write all 0s to CSR
	csrrs x21, 0x35a, x29	// Set all CSR bits
	csrrc x21, 0x35a, x29	// Clear all CSR bits
	csrrw x21, 0x35a, x1	// Restore CSR

// Testing CSR 0x35b
	csrr x21, 0x35b	// Read CSR
	li x9, -1
	csrrw x16, 0x35b, x9	// Write all 1s to CSR
	csrrw x16, 0x35b, x0	// Write all 0s to CSR
	csrrs x16, 0x35b, x9	// Set all CSR bits
	csrrc x16, 0x35b, x9	// Clear all CSR bits
	csrrw x16, 0x35b, x21	// Restore CSR

// Testing CSR 0x35c
	csrr x20, 0x35c	// Read CSR
	li x22, -1
	csrrw x27, 0x35c, x22	// Write all 1s to CSR
	csrrw x27, 0x35c, x0	// Write all 0s to CSR
	csrrs x27, 0x35c, x22	// Set all CSR bits
	csrrc x27, 0x35c, x22	// Clear all CSR bits
	csrrw x27, 0x35c, x20	// Restore CSR

// Testing CSR 0x35d
	csrr x14, 0x35d	// Read CSR
	li x9, -1
	csrrw x20, 0x35d, x9	// Write all 1s to CSR
	csrrw x20, 0x35d, x0	// Write all 0s to CSR
	csrrs x20, 0x35d, x9	// Set all CSR bits
	csrrc x20, 0x35d, x9	// Clear all CSR bits
	csrrw x20, 0x35d, x14	// Restore CSR

// Testing CSR 0x35e
	csrr x1, 0x35e	// Read CSR
	li x31, -1
	csrrw x25, 0x35e, x31	// Write all 1s to CSR
	csrrw x25, 0x35e, x0	// Write all 0s to CSR
	csrrs x25, 0x35e, x31	// Set all CSR bits
	csrrc x25, 0x35e, x31	// Clear all CSR bits
	csrrw x25, 0x35e, x1	// Restore CSR

// Testing CSR 0x35f
	csrr x28, 0x35f	// Read CSR
	li x16, -1
	csrrw x24, 0x35f, x16	// Write all 1s to CSR
	csrrw x24, 0x35f, x0	// Write all 0s to CSR
	csrrs x24, 0x35f, x16	// Set all CSR bits
	csrrc x24, 0x35f, x16	// Clear all CSR bits
	csrrw x24, 0x35f, x28	// Restore CSR

// Testing CSR 0x360
	csrr x9, 0x360	// Read CSR
	li x31, -1
	csrrw x14, 0x360, x31	// Write all 1s to CSR
	csrrw x14, 0x360, x0	// Write all 0s to CSR
	csrrs x14, 0x360, x31	// Set all CSR bits
	csrrc x14, 0x360, x31	// Clear all CSR bits
	csrrw x14, 0x360, x9	// Restore CSR

// Testing CSR 0x361
	csrr x17, 0x361	// Read CSR
	li x16, -1
	csrrw x13, 0x361, x16	// Write all 1s to CSR
	csrrw x13, 0x361, x0	// Write all 0s to CSR
	csrrs x13, 0x361, x16	// Set all CSR bits
	csrrc x13, 0x361, x16	// Clear all CSR bits
	csrrw x13, 0x361, x17	// Restore CSR

// Testing CSR 0x362
	csrr x14, 0x362	// Read CSR
	li x17, -1
	csrrw x29, 0x362, x17	// Write all 1s to CSR
	csrrw x29, 0x362, x0	// Write all 0s to CSR
	csrrs x29, 0x362, x17	// Set all CSR bits
	csrrc x29, 0x362, x17	// Clear all CSR bits
	csrrw x29, 0x362, x14	// Restore CSR

// Testing CSR 0x363
	csrr x18, 0x363	// Read CSR
	li x7, -1
	csrrw x10, 0x363, x7	// Write all 1s to CSR
	csrrw x10, 0x363, x0	// Write all 0s to CSR
	csrrs x10, 0x363, x7	// Set all CSR bits
	csrrc x10, 0x363, x7	// Clear all CSR bits
	csrrw x10, 0x363, x18	// Restore CSR

// Testing CSR 0x364
	csrr x30, 0x364	// Read CSR
	li x16, -1
	csrrw x12, 0x364, x16	// Write all 1s to CSR
	csrrw x12, 0x364, x0	// Write all 0s to CSR
	csrrs x12, 0x364, x16	// Set all CSR bits
	csrrc x12, 0x364, x16	// Clear all CSR bits
	csrrw x12, 0x364, x30	// Restore CSR

// Testing CSR 0x365
	csrr x12, 0x365	// Read CSR
	li x30, -1
	csrrw x20, 0x365, x30	// Write all 1s to CSR
	csrrw x20, 0x365, x0	// Write all 0s to CSR
	csrrs x20, 0x365, x30	// Set all CSR bits
	csrrc x20, 0x365, x30	// Clear all CSR bits
	csrrw x20, 0x365, x12	// Restore CSR

// Testing CSR 0x366
	csrr x6, 0x366	// Read CSR
	li x28, -1
	csrrw x22, 0x366, x28	// Write all 1s to CSR
	csrrw x22, 0x366, x0	// Write all 0s to CSR
	csrrs x22, 0x366, x28	// Set all CSR bits
	csrrc x22, 0x366, x28	// Clear all CSR bits
	csrrw x22, 0x366, x6	// Restore CSR

// Testing CSR 0x367
	csrr x19, 0x367	// Read CSR
	li x12, -1
	csrrw x8, 0x367, x12	// Write all 1s to CSR
	csrrw x8, 0x367, x0	// Write all 0s to CSR
	csrrs x8, 0x367, x12	// Set all CSR bits
	csrrc x8, 0x367, x12	// Clear all CSR bits
	csrrw x8, 0x367, x19	// Restore CSR

// Testing CSR 0x368
	csrr x31, 0x368	// Read CSR
	li x2, -1
	csrrw x4, 0x368, x2	// Write all 1s to CSR
	csrrw x4, 0x368, x0	// Write all 0s to CSR
	csrrs x4, 0x368, x2	// Set all CSR bits
	csrrc x4, 0x368, x2	// Clear all CSR bits
	csrrw x4, 0x368, x31	// Restore CSR

// Testing CSR 0x369
	csrr x19, 0x369	// Read CSR
	li x1, -1
	csrrw x12, 0x369, x1	// Write all 1s to CSR
	csrrw x12, 0x369, x0	// Write all 0s to CSR
	csrrs x12, 0x369, x1	// Set all CSR bits
	csrrc x12, 0x369, x1	// Clear all CSR bits
	csrrw x12, 0x369, x19	// Restore CSR

// Testing CSR 0x36a
	csrr x5, 0x36a	// Read CSR
	li x12, -1
	csrrw x18, 0x36a, x12	// Write all 1s to CSR
	csrrw x18, 0x36a, x0	// Write all 0s to CSR
	csrrs x18, 0x36a, x12	// Set all CSR bits
	csrrc x18, 0x36a, x12	// Clear all CSR bits
	csrrw x18, 0x36a, x5	// Restore CSR

// Testing CSR 0x36b
	csrr x24, 0x36b	// Read CSR
	li x31, -1
	csrrw x18, 0x36b, x31	// Write all 1s to CSR
	csrrw x18, 0x36b, x0	// Write all 0s to CSR
	csrrs x18, 0x36b, x31	// Set all CSR bits
	csrrc x18, 0x36b, x31	// Clear all CSR bits
	csrrw x18, 0x36b, x24	// Restore CSR

// Testing CSR 0x36c
	csrr x24, 0x36c	// Read CSR
	li x10, -1
	csrrw x13, 0x36c, x10	// Write all 1s to CSR
	csrrw x13, 0x36c, x0	// Write all 0s to CSR
	csrrs x13, 0x36c, x10	// Set all CSR bits
	csrrc x13, 0x36c, x10	// Clear all CSR bits
	csrrw x13, 0x36c, x24	// Restore CSR

// Testing CSR 0x36d
	csrr x23, 0x36d	// Read CSR
	li x21, -1
	csrrw x15, 0x36d, x21	// Write all 1s to CSR
	csrrw x15, 0x36d, x0	// Write all 0s to CSR
	csrrs x15, 0x36d, x21	// Set all CSR bits
	csrrc x15, 0x36d, x21	// Clear all CSR bits
	csrrw x15, 0x36d, x23	// Restore CSR

// Testing CSR 0x36e
	csrr x15, 0x36e	// Read CSR
	li x16, -1
	csrrw x1, 0x36e, x16	// Write all 1s to CSR
	csrrw x1, 0x36e, x0	// Write all 0s to CSR
	csrrs x1, 0x36e, x16	// Set all CSR bits
	csrrc x1, 0x36e, x16	// Clear all CSR bits
	csrrw x1, 0x36e, x15	// Restore CSR

// Testing CSR 0x36f
	csrr x21, 0x36f	// Read CSR
	li x17, -1
	csrrw x23, 0x36f, x17	// Write all 1s to CSR
	csrrw x23, 0x36f, x0	// Write all 0s to CSR
	csrrs x23, 0x36f, x17	// Set all CSR bits
	csrrc x23, 0x36f, x17	// Clear all CSR bits
	csrrw x23, 0x36f, x21	// Restore CSR

// Testing CSR 0x370
	csrr x4, 0x370	// Read CSR
	li x27, -1
	csrrw x21, 0x370, x27	// Write all 1s to CSR
	csrrw x21, 0x370, x0	// Write all 0s to CSR
	csrrs x21, 0x370, x27	// Set all CSR bits
	csrrc x21, 0x370, x27	// Clear all CSR bits
	csrrw x21, 0x370, x4	// Restore CSR

// Testing CSR 0x371
	csrr x14, 0x371	// Read CSR
	li x1, -1
	csrrw x26, 0x371, x1	// Write all 1s to CSR
	csrrw x26, 0x371, x0	// Write all 0s to CSR
	csrrs x26, 0x371, x1	// Set all CSR bits
	csrrc x26, 0x371, x1	// Clear all CSR bits
	csrrw x26, 0x371, x14	// Restore CSR

// Testing CSR 0x372
	csrr x24, 0x372	// Read CSR
	li x19, -1
	csrrw x18, 0x372, x19	// Write all 1s to CSR
	csrrw x18, 0x372, x0	// Write all 0s to CSR
	csrrs x18, 0x372, x19	// Set all CSR bits
	csrrc x18, 0x372, x19	// Clear all CSR bits
	csrrw x18, 0x372, x24	// Restore CSR

// Testing CSR 0x373
	csrr x12, 0x373	// Read CSR
	li x10, -1
	csrrw x11, 0x373, x10	// Write all 1s to CSR
	csrrw x11, 0x373, x0	// Write all 0s to CSR
	csrrs x11, 0x373, x10	// Set all CSR bits
	csrrc x11, 0x373, x10	// Clear all CSR bits
	csrrw x11, 0x373, x12	// Restore CSR

// Testing CSR 0x374
	csrr x14, 0x374	// Read CSR
	li x2, -1
	csrrw x23, 0x374, x2	// Write all 1s to CSR
	csrrw x23, 0x374, x0	// Write all 0s to CSR
	csrrs x23, 0x374, x2	// Set all CSR bits
	csrrc x23, 0x374, x2	// Clear all CSR bits
	csrrw x23, 0x374, x14	// Restore CSR

// Testing CSR 0x375
	csrr x10, 0x375	// Read CSR
	li x26, -1
	csrrw x5, 0x375, x26	// Write all 1s to CSR
	csrrw x5, 0x375, x0	// Write all 0s to CSR
	csrrs x5, 0x375, x26	// Set all CSR bits
	csrrc x5, 0x375, x26	// Clear all CSR bits
	csrrw x5, 0x375, x10	// Restore CSR

// Testing CSR 0x376
	csrr x9, 0x376	// Read CSR
	li x29, -1
	csrrw x25, 0x376, x29	// Write all 1s to CSR
	csrrw x25, 0x376, x0	// Write all 0s to CSR
	csrrs x25, 0x376, x29	// Set all CSR bits
	csrrc x25, 0x376, x29	// Clear all CSR bits
	csrrw x25, 0x376, x9	// Restore CSR

// Testing CSR 0x377
	csrr x16, 0x377	// Read CSR
	li x27, -1
	csrrw x28, 0x377, x27	// Write all 1s to CSR
	csrrw x28, 0x377, x0	// Write all 0s to CSR
	csrrs x28, 0x377, x27	// Set all CSR bits
	csrrc x28, 0x377, x27	// Clear all CSR bits
	csrrw x28, 0x377, x16	// Restore CSR

// Testing CSR 0x378
	csrr x11, 0x378	// Read CSR
	li x7, -1
	csrrw x20, 0x378, x7	// Write all 1s to CSR
	csrrw x20, 0x378, x0	// Write all 0s to CSR
	csrrs x20, 0x378, x7	// Set all CSR bits
	csrrc x20, 0x378, x7	// Clear all CSR bits
	csrrw x20, 0x378, x11	// Restore CSR

// Testing CSR 0x379
	csrr x28, 0x379	// Read CSR
	li x30, -1
	csrrw x29, 0x379, x30	// Write all 1s to CSR
	csrrw x29, 0x379, x0	// Write all 0s to CSR
	csrrs x29, 0x379, x30	// Set all CSR bits
	csrrc x29, 0x379, x30	// Clear all CSR bits
	csrrw x29, 0x379, x28	// Restore CSR

// Testing CSR 0x37a
	csrr x8, 0x37a	// Read CSR
	li x19, -1
	csrrw x2, 0x37a, x19	// Write all 1s to CSR
	csrrw x2, 0x37a, x0	// Write all 0s to CSR
	csrrs x2, 0x37a, x19	// Set all CSR bits
	csrrc x2, 0x37a, x19	// Clear all CSR bits
	csrrw x2, 0x37a, x8	// Restore CSR

// Testing CSR 0x37b
	csrr x1, 0x37b	// Read CSR
	li x30, -1
	csrrw x31, 0x37b, x30	// Write all 1s to CSR
	csrrw x31, 0x37b, x0	// Write all 0s to CSR
	csrrs x31, 0x37b, x30	// Set all CSR bits
	csrrc x31, 0x37b, x30	// Clear all CSR bits
	csrrw x31, 0x37b, x1	// Restore CSR

// Testing CSR 0x37c
	csrr x18, 0x37c	// Read CSR
	li x26, -1
	csrrw x17, 0x37c, x26	// Write all 1s to CSR
	csrrw x17, 0x37c, x0	// Write all 0s to CSR
	csrrs x17, 0x37c, x26	// Set all CSR bits
	csrrc x17, 0x37c, x26	// Clear all CSR bits
	csrrw x17, 0x37c, x18	// Restore CSR

// Testing CSR 0x37d
	csrr x18, 0x37d	// Read CSR
	li x12, -1
	csrrw x5, 0x37d, x12	// Write all 1s to CSR
	csrrw x5, 0x37d, x0	// Write all 0s to CSR
	csrrs x5, 0x37d, x12	// Set all CSR bits
	csrrc x5, 0x37d, x12	// Clear all CSR bits
	csrrw x5, 0x37d, x18	// Restore CSR

// Testing CSR 0x37e
	csrr x21, 0x37e	// Read CSR
	li x30, -1
	csrrw x24, 0x37e, x30	// Write all 1s to CSR
	csrrw x24, 0x37e, x0	// Write all 0s to CSR
	csrrs x24, 0x37e, x30	// Set all CSR bits
	csrrc x24, 0x37e, x30	// Clear all CSR bits
	csrrw x24, 0x37e, x21	// Restore CSR

// Testing CSR 0x37f
	csrr x18, 0x37f	// Read CSR
	li x15, -1
	csrrw x26, 0x37f, x15	// Write all 1s to CSR
	csrrw x26, 0x37f, x0	// Write all 0s to CSR
	csrrs x26, 0x37f, x15	// Set all CSR bits
	csrrc x26, 0x37f, x15	// Clear all CSR bits
	csrrw x26, 0x37f, x18	// Restore CSR

// Testing CSR 0x380
	csrr x14, 0x380	// Read CSR
	li x23, -1
	csrrw x18, 0x380, x23	// Write all 1s to CSR
	csrrw x18, 0x380, x0	// Write all 0s to CSR
	csrrs x18, 0x380, x23	// Set all CSR bits
	csrrc x18, 0x380, x23	// Clear all CSR bits
	csrrw x18, 0x380, x14	// Restore CSR

// Testing CSR 0x381
	csrr x18, 0x381	// Read CSR
	li x17, -1
	csrrw x22, 0x381, x17	// Write all 1s to CSR
	csrrw x22, 0x381, x0	// Write all 0s to CSR
	csrrs x22, 0x381, x17	// Set all CSR bits
	csrrc x22, 0x381, x17	// Clear all CSR bits
	csrrw x22, 0x381, x18	// Restore CSR

// Testing CSR 0x382
	csrr x30, 0x382	// Read CSR
	li x26, -1
	csrrw x27, 0x382, x26	// Write all 1s to CSR
	csrrw x27, 0x382, x0	// Write all 0s to CSR
	csrrs x27, 0x382, x26	// Set all CSR bits
	csrrc x27, 0x382, x26	// Clear all CSR bits
	csrrw x27, 0x382, x30	// Restore CSR

// Testing CSR 0x383
	csrr x7, 0x383	// Read CSR
	li x5, -1
	csrrw x13, 0x383, x5	// Write all 1s to CSR
	csrrw x13, 0x383, x0	// Write all 0s to CSR
	csrrs x13, 0x383, x5	// Set all CSR bits
	csrrc x13, 0x383, x5	// Clear all CSR bits
	csrrw x13, 0x383, x7	// Restore CSR

// Testing CSR 0x384
	csrr x20, 0x384	// Read CSR
	li x27, -1
	csrrw x22, 0x384, x27	// Write all 1s to CSR
	csrrw x22, 0x384, x0	// Write all 0s to CSR
	csrrs x22, 0x384, x27	// Set all CSR bits
	csrrc x22, 0x384, x27	// Clear all CSR bits
	csrrw x22, 0x384, x20	// Restore CSR

// Testing CSR 0x385
	csrr x25, 0x385	// Read CSR
	li x14, -1
	csrrw x23, 0x385, x14	// Write all 1s to CSR
	csrrw x23, 0x385, x0	// Write all 0s to CSR
	csrrs x23, 0x385, x14	// Set all CSR bits
	csrrc x23, 0x385, x14	// Clear all CSR bits
	csrrw x23, 0x385, x25	// Restore CSR

// Testing CSR 0x386
	csrr x1, 0x386	// Read CSR
	li x8, -1
	csrrw x14, 0x386, x8	// Write all 1s to CSR
	csrrw x14, 0x386, x0	// Write all 0s to CSR
	csrrs x14, 0x386, x8	// Set all CSR bits
	csrrc x14, 0x386, x8	// Clear all CSR bits
	csrrw x14, 0x386, x1	// Restore CSR

// Testing CSR 0x387
	csrr x29, 0x387	// Read CSR
	li x1, -1
	csrrw x25, 0x387, x1	// Write all 1s to CSR
	csrrw x25, 0x387, x0	// Write all 0s to CSR
	csrrs x25, 0x387, x1	// Set all CSR bits
	csrrc x25, 0x387, x1	// Clear all CSR bits
	csrrw x25, 0x387, x29	// Restore CSR

// Testing CSR 0x388
	csrr x17, 0x388	// Read CSR
	li x25, -1
	csrrw x8, 0x388, x25	// Write all 1s to CSR
	csrrw x8, 0x388, x0	// Write all 0s to CSR
	csrrs x8, 0x388, x25	// Set all CSR bits
	csrrc x8, 0x388, x25	// Clear all CSR bits
	csrrw x8, 0x388, x17	// Restore CSR

// Testing CSR 0x389
	csrr x25, 0x389	// Read CSR
	li x8, -1
	csrrw x24, 0x389, x8	// Write all 1s to CSR
	csrrw x24, 0x389, x0	// Write all 0s to CSR
	csrrs x24, 0x389, x8	// Set all CSR bits
	csrrc x24, 0x389, x8	// Clear all CSR bits
	csrrw x24, 0x389, x25	// Restore CSR

// Testing CSR 0x38a
	csrr x27, 0x38a	// Read CSR
	li x11, -1
	csrrw x24, 0x38a, x11	// Write all 1s to CSR
	csrrw x24, 0x38a, x0	// Write all 0s to CSR
	csrrs x24, 0x38a, x11	// Set all CSR bits
	csrrc x24, 0x38a, x11	// Clear all CSR bits
	csrrw x24, 0x38a, x27	// Restore CSR

// Testing CSR 0x38b
	csrr x16, 0x38b	// Read CSR
	li x4, -1
	csrrw x31, 0x38b, x4	// Write all 1s to CSR
	csrrw x31, 0x38b, x0	// Write all 0s to CSR
	csrrs x31, 0x38b, x4	// Set all CSR bits
	csrrc x31, 0x38b, x4	// Clear all CSR bits
	csrrw x31, 0x38b, x16	// Restore CSR

// Testing CSR 0x38c
	csrr x18, 0x38c	// Read CSR
	li x31, -1
	csrrw x20, 0x38c, x31	// Write all 1s to CSR
	csrrw x20, 0x38c, x0	// Write all 0s to CSR
	csrrs x20, 0x38c, x31	// Set all CSR bits
	csrrc x20, 0x38c, x31	// Clear all CSR bits
	csrrw x20, 0x38c, x18	// Restore CSR

// Testing CSR 0x38d
	csrr x29, 0x38d	// Read CSR
	li x17, -1
	csrrw x6, 0x38d, x17	// Write all 1s to CSR
	csrrw x6, 0x38d, x0	// Write all 0s to CSR
	csrrs x6, 0x38d, x17	// Set all CSR bits
	csrrc x6, 0x38d, x17	// Clear all CSR bits
	csrrw x6, 0x38d, x29	// Restore CSR

// Testing CSR 0x38e
	csrr x18, 0x38e	// Read CSR
	li x9, -1
	csrrw x21, 0x38e, x9	// Write all 1s to CSR
	csrrw x21, 0x38e, x0	// Write all 0s to CSR
	csrrs x21, 0x38e, x9	// Set all CSR bits
	csrrc x21, 0x38e, x9	// Clear all CSR bits
	csrrw x21, 0x38e, x18	// Restore CSR

// Testing CSR 0x38f
	csrr x16, 0x38f	// Read CSR
	li x10, -1
	csrrw x11, 0x38f, x10	// Write all 1s to CSR
	csrrw x11, 0x38f, x0	// Write all 0s to CSR
	csrrs x11, 0x38f, x10	// Set all CSR bits
	csrrc x11, 0x38f, x10	// Clear all CSR bits
	csrrw x11, 0x38f, x16	// Restore CSR

// Testing CSR 0x390
	csrr x14, 0x390	// Read CSR
	li x22, -1
	csrrw x26, 0x390, x22	// Write all 1s to CSR
	csrrw x26, 0x390, x0	// Write all 0s to CSR
	csrrs x26, 0x390, x22	// Set all CSR bits
	csrrc x26, 0x390, x22	// Clear all CSR bits
	csrrw x26, 0x390, x14	// Restore CSR

// Testing CSR 0x391
	csrr x28, 0x391	// Read CSR
	li x12, -1
	csrrw x5, 0x391, x12	// Write all 1s to CSR
	csrrw x5, 0x391, x0	// Write all 0s to CSR
	csrrs x5, 0x391, x12	// Set all CSR bits
	csrrc x5, 0x391, x12	// Clear all CSR bits
	csrrw x5, 0x391, x28	// Restore CSR

// Testing CSR 0x392
	csrr x23, 0x392	// Read CSR
	li x19, -1
	csrrw x25, 0x392, x19	// Write all 1s to CSR
	csrrw x25, 0x392, x0	// Write all 0s to CSR
	csrrs x25, 0x392, x19	// Set all CSR bits
	csrrc x25, 0x392, x19	// Clear all CSR bits
	csrrw x25, 0x392, x23	// Restore CSR

// Testing CSR 0x393
	csrr x21, 0x393	// Read CSR
	li x31, -1
	csrrw x25, 0x393, x31	// Write all 1s to CSR
	csrrw x25, 0x393, x0	// Write all 0s to CSR
	csrrs x25, 0x393, x31	// Set all CSR bits
	csrrc x25, 0x393, x31	// Clear all CSR bits
	csrrw x25, 0x393, x21	// Restore CSR

// Testing CSR 0x394
	csrr x15, 0x394	// Read CSR
	li x11, -1
	csrrw x4, 0x394, x11	// Write all 1s to CSR
	csrrw x4, 0x394, x0	// Write all 0s to CSR
	csrrs x4, 0x394, x11	// Set all CSR bits
	csrrc x4, 0x394, x11	// Clear all CSR bits
	csrrw x4, 0x394, x15	// Restore CSR

// Testing CSR 0x395
	csrr x30, 0x395	// Read CSR
	li x16, -1
	csrrw x24, 0x395, x16	// Write all 1s to CSR
	csrrw x24, 0x395, x0	// Write all 0s to CSR
	csrrs x24, 0x395, x16	// Set all CSR bits
	csrrc x24, 0x395, x16	// Clear all CSR bits
	csrrw x24, 0x395, x30	// Restore CSR

// Testing CSR 0x396
	csrr x30, 0x396	// Read CSR
	li x5, -1
	csrrw x10, 0x396, x5	// Write all 1s to CSR
	csrrw x10, 0x396, x0	// Write all 0s to CSR
	csrrs x10, 0x396, x5	// Set all CSR bits
	csrrc x10, 0x396, x5	// Clear all CSR bits
	csrrw x10, 0x396, x30	// Restore CSR

// Testing CSR 0x397
	csrr x20, 0x397	// Read CSR
	li x23, -1
	csrrw x10, 0x397, x23	// Write all 1s to CSR
	csrrw x10, 0x397, x0	// Write all 0s to CSR
	csrrs x10, 0x397, x23	// Set all CSR bits
	csrrc x10, 0x397, x23	// Clear all CSR bits
	csrrw x10, 0x397, x20	// Restore CSR

// Testing CSR 0x398
	csrr x13, 0x398	// Read CSR
	li x6, -1
	csrrw x29, 0x398, x6	// Write all 1s to CSR
	csrrw x29, 0x398, x0	// Write all 0s to CSR
	csrrs x29, 0x398, x6	// Set all CSR bits
	csrrc x29, 0x398, x6	// Clear all CSR bits
	csrrw x29, 0x398, x13	// Restore CSR

// Testing CSR 0x399
	csrr x24, 0x399	// Read CSR
	li x26, -1
	csrrw x7, 0x399, x26	// Write all 1s to CSR
	csrrw x7, 0x399, x0	// Write all 0s to CSR
	csrrs x7, 0x399, x26	// Set all CSR bits
	csrrc x7, 0x399, x26	// Clear all CSR bits
	csrrw x7, 0x399, x24	// Restore CSR

// Testing CSR 0x39a
	csrr x7, 0x39a	// Read CSR
	li x14, -1
	csrrw x27, 0x39a, x14	// Write all 1s to CSR
	csrrw x27, 0x39a, x0	// Write all 0s to CSR
	csrrs x27, 0x39a, x14	// Set all CSR bits
	csrrc x27, 0x39a, x14	// Clear all CSR bits
	csrrw x27, 0x39a, x7	// Restore CSR

// Testing CSR 0x39b
	csrr x11, 0x39b	// Read CSR
	li x24, -1
	csrrw x25, 0x39b, x24	// Write all 1s to CSR
	csrrw x25, 0x39b, x0	// Write all 0s to CSR
	csrrs x25, 0x39b, x24	// Set all CSR bits
	csrrc x25, 0x39b, x24	// Clear all CSR bits
	csrrw x25, 0x39b, x11	// Restore CSR

// Testing CSR 0x39c
	csrr x30, 0x39c	// Read CSR
	li x5, -1
	csrrw x27, 0x39c, x5	// Write all 1s to CSR
	csrrw x27, 0x39c, x0	// Write all 0s to CSR
	csrrs x27, 0x39c, x5	// Set all CSR bits
	csrrc x27, 0x39c, x5	// Clear all CSR bits
	csrrw x27, 0x39c, x30	// Restore CSR

// Testing CSR 0x39d
	csrr x20, 0x39d	// Read CSR
	li x2, -1
	csrrw x1, 0x39d, x2	// Write all 1s to CSR
	csrrw x1, 0x39d, x0	// Write all 0s to CSR
	csrrs x1, 0x39d, x2	// Set all CSR bits
	csrrc x1, 0x39d, x2	// Clear all CSR bits
	csrrw x1, 0x39d, x20	// Restore CSR

// Testing CSR 0x39e
	csrr x18, 0x39e	// Read CSR
	li x20, -1
	csrrw x24, 0x39e, x20	// Write all 1s to CSR
	csrrw x24, 0x39e, x0	// Write all 0s to CSR
	csrrs x24, 0x39e, x20	// Set all CSR bits
	csrrc x24, 0x39e, x20	// Clear all CSR bits
	csrrw x24, 0x39e, x18	// Restore CSR

// Testing CSR 0x39f
	csrr x1, 0x39f	// Read CSR
	li x23, -1
	csrrw x13, 0x39f, x23	// Write all 1s to CSR
	csrrw x13, 0x39f, x0	// Write all 0s to CSR
	csrrs x13, 0x39f, x23	// Set all CSR bits
	csrrc x13, 0x39f, x23	// Clear all CSR bits
	csrrw x13, 0x39f, x1	// Restore CSR

// Testing CSR 0x3a0
	csrr x18, 0x3a0	// Read CSR
	li x14, -1
	csrrw x25, 0x3a0, x14	// Write all 1s to CSR
	csrrw x25, 0x3a0, x0	// Write all 0s to CSR
	csrrs x25, 0x3a0, x14	// Set all CSR bits
	csrrc x25, 0x3a0, x14	// Clear all CSR bits
	csrrw x25, 0x3a0, x18	// Restore CSR

// Testing CSR 0x3a1
	csrr x26, 0x3a1	// Read CSR
	li x8, -1
	csrrw x15, 0x3a1, x8	// Write all 1s to CSR
	csrrw x15, 0x3a1, x0	// Write all 0s to CSR
	csrrs x15, 0x3a1, x8	// Set all CSR bits
	csrrc x15, 0x3a1, x8	// Clear all CSR bits
	csrrw x15, 0x3a1, x26	// Restore CSR

// Testing CSR 0x3a2
	csrr x27, 0x3a2	// Read CSR
	li x17, -1
	csrrw x3, 0x3a2, x17	// Write all 1s to CSR
	csrrw x3, 0x3a2, x0	// Write all 0s to CSR
	csrrs x3, 0x3a2, x17	// Set all CSR bits
	csrrc x3, 0x3a2, x17	// Clear all CSR bits
	csrrw x3, 0x3a2, x27	// Restore CSR

// Testing CSR 0x3a3
	csrr x25, 0x3a3	// Read CSR
	li x1, -1
	csrrw x14, 0x3a3, x1	// Write all 1s to CSR
	csrrw x14, 0x3a3, x0	// Write all 0s to CSR
	csrrs x14, 0x3a3, x1	// Set all CSR bits
	csrrc x14, 0x3a3, x1	// Clear all CSR bits
	csrrw x14, 0x3a3, x25	// Restore CSR

// Testing CSR 0x3a4
	csrr x6, 0x3a4	// Read CSR
	li x15, -1
	csrrw x12, 0x3a4, x15	// Write all 1s to CSR
	csrrw x12, 0x3a4, x0	// Write all 0s to CSR
	csrrs x12, 0x3a4, x15	// Set all CSR bits
	csrrc x12, 0x3a4, x15	// Clear all CSR bits
	csrrw x12, 0x3a4, x6	// Restore CSR

// Testing CSR 0x3a5
	csrr x25, 0x3a5	// Read CSR
	li x17, -1
	csrrw x10, 0x3a5, x17	// Write all 1s to CSR
	csrrw x10, 0x3a5, x0	// Write all 0s to CSR
	csrrs x10, 0x3a5, x17	// Set all CSR bits
	csrrc x10, 0x3a5, x17	// Clear all CSR bits
	csrrw x10, 0x3a5, x25	// Restore CSR

// Testing CSR 0x3a6
	csrr x15, 0x3a6	// Read CSR
	li x22, -1
	csrrw x9, 0x3a6, x22	// Write all 1s to CSR
	csrrw x9, 0x3a6, x0	// Write all 0s to CSR
	csrrs x9, 0x3a6, x22	// Set all CSR bits
	csrrc x9, 0x3a6, x22	// Clear all CSR bits
	csrrw x9, 0x3a6, x15	// Restore CSR

// Testing CSR 0x3a7
	csrr x14, 0x3a7	// Read CSR
	li x20, -1
	csrrw x1, 0x3a7, x20	// Write all 1s to CSR
	csrrw x1, 0x3a7, x0	// Write all 0s to CSR
	csrrs x1, 0x3a7, x20	// Set all CSR bits
	csrrc x1, 0x3a7, x20	// Clear all CSR bits
	csrrw x1, 0x3a7, x14	// Restore CSR

// Testing CSR 0x3a8
	csrr x24, 0x3a8	// Read CSR
	li x26, -1
	csrrw x27, 0x3a8, x26	// Write all 1s to CSR
	csrrw x27, 0x3a8, x0	// Write all 0s to CSR
	csrrs x27, 0x3a8, x26	// Set all CSR bits
	csrrc x27, 0x3a8, x26	// Clear all CSR bits
	csrrw x27, 0x3a8, x24	// Restore CSR

// Testing CSR 0x3a9
	csrr x3, 0x3a9	// Read CSR
	li x31, -1
	csrrw x11, 0x3a9, x31	// Write all 1s to CSR
	csrrw x11, 0x3a9, x0	// Write all 0s to CSR
	csrrs x11, 0x3a9, x31	// Set all CSR bits
	csrrc x11, 0x3a9, x31	// Clear all CSR bits
	csrrw x11, 0x3a9, x3	// Restore CSR

// Testing CSR 0x3aa
	csrr x16, 0x3aa	// Read CSR
	li x5, -1
	csrrw x14, 0x3aa, x5	// Write all 1s to CSR
	csrrw x14, 0x3aa, x0	// Write all 0s to CSR
	csrrs x14, 0x3aa, x5	// Set all CSR bits
	csrrc x14, 0x3aa, x5	// Clear all CSR bits
	csrrw x14, 0x3aa, x16	// Restore CSR

// Testing CSR 0x3ab
	csrr x25, 0x3ab	// Read CSR
	li x29, -1
	csrrw x27, 0x3ab, x29	// Write all 1s to CSR
	csrrw x27, 0x3ab, x0	// Write all 0s to CSR
	csrrs x27, 0x3ab, x29	// Set all CSR bits
	csrrc x27, 0x3ab, x29	// Clear all CSR bits
	csrrw x27, 0x3ab, x25	// Restore CSR

// Testing CSR 0x3ac
	csrr x16, 0x3ac	// Read CSR
	li x26, -1
	csrrw x24, 0x3ac, x26	// Write all 1s to CSR
	csrrw x24, 0x3ac, x0	// Write all 0s to CSR
	csrrs x24, 0x3ac, x26	// Set all CSR bits
	csrrc x24, 0x3ac, x26	// Clear all CSR bits
	csrrw x24, 0x3ac, x16	// Restore CSR

// Testing CSR 0x3ad
	csrr x31, 0x3ad	// Read CSR
	li x4, -1
	csrrw x30, 0x3ad, x4	// Write all 1s to CSR
	csrrw x30, 0x3ad, x0	// Write all 0s to CSR
	csrrs x30, 0x3ad, x4	// Set all CSR bits
	csrrc x30, 0x3ad, x4	// Clear all CSR bits
	csrrw x30, 0x3ad, x31	// Restore CSR

// Testing CSR 0x3ae
	csrr x30, 0x3ae	// Read CSR
	li x22, -1
	csrrw x23, 0x3ae, x22	// Write all 1s to CSR
	csrrw x23, 0x3ae, x0	// Write all 0s to CSR
	csrrs x23, 0x3ae, x22	// Set all CSR bits
	csrrc x23, 0x3ae, x22	// Clear all CSR bits
	csrrw x23, 0x3ae, x30	// Restore CSR

// Testing CSR 0x3af
	csrr x7, 0x3af	// Read CSR
	li x4, -1
	csrrw x1, 0x3af, x4	// Write all 1s to CSR
	csrrw x1, 0x3af, x0	// Write all 0s to CSR
	csrrs x1, 0x3af, x4	// Set all CSR bits
	csrrc x1, 0x3af, x4	// Clear all CSR bits
	csrrw x1, 0x3af, x7	// Restore CSR

// Testing CSR 0x3b0
	csrr x17, 0x3b0	// Read CSR
	li x4, -1
	csrrw x5, 0x3b0, x4	// Write all 1s to CSR
	csrrw x5, 0x3b0, x0	// Write all 0s to CSR
	csrrs x5, 0x3b0, x4	// Set all CSR bits
	csrrc x5, 0x3b0, x4	// Clear all CSR bits
	csrrw x5, 0x3b0, x17	// Restore CSR

// Testing CSR 0x3b1
	csrr x4, 0x3b1	// Read CSR
	li x27, -1
	csrrw x22, 0x3b1, x27	// Write all 1s to CSR
	csrrw x22, 0x3b1, x0	// Write all 0s to CSR
	csrrs x22, 0x3b1, x27	// Set all CSR bits
	csrrc x22, 0x3b1, x27	// Clear all CSR bits
	csrrw x22, 0x3b1, x4	// Restore CSR

// Testing CSR 0x3b2
	csrr x2, 0x3b2	// Read CSR
	li x8, -1
	csrrw x4, 0x3b2, x8	// Write all 1s to CSR
	csrrw x4, 0x3b2, x0	// Write all 0s to CSR
	csrrs x4, 0x3b2, x8	// Set all CSR bits
	csrrc x4, 0x3b2, x8	// Clear all CSR bits
	csrrw x4, 0x3b2, x2	// Restore CSR

// Testing CSR 0x3b3
	csrr x4, 0x3b3	// Read CSR
	li x3, -1
	csrrw x30, 0x3b3, x3	// Write all 1s to CSR
	csrrw x30, 0x3b3, x0	// Write all 0s to CSR
	csrrs x30, 0x3b3, x3	// Set all CSR bits
	csrrc x30, 0x3b3, x3	// Clear all CSR bits
	csrrw x30, 0x3b3, x4	// Restore CSR

// Testing CSR 0x3b4
	csrr x5, 0x3b4	// Read CSR
	li x29, -1
	csrrw x3, 0x3b4, x29	// Write all 1s to CSR
	csrrw x3, 0x3b4, x0	// Write all 0s to CSR
	csrrs x3, 0x3b4, x29	// Set all CSR bits
	csrrc x3, 0x3b4, x29	// Clear all CSR bits
	csrrw x3, 0x3b4, x5	// Restore CSR

// Testing CSR 0x3b5
	csrr x7, 0x3b5	// Read CSR
	li x18, -1
	csrrw x10, 0x3b5, x18	// Write all 1s to CSR
	csrrw x10, 0x3b5, x0	// Write all 0s to CSR
	csrrs x10, 0x3b5, x18	// Set all CSR bits
	csrrc x10, 0x3b5, x18	// Clear all CSR bits
	csrrw x10, 0x3b5, x7	// Restore CSR

// Testing CSR 0x3b6
	csrr x28, 0x3b6	// Read CSR
	li x20, -1
	csrrw x26, 0x3b6, x20	// Write all 1s to CSR
	csrrw x26, 0x3b6, x0	// Write all 0s to CSR
	csrrs x26, 0x3b6, x20	// Set all CSR bits
	csrrc x26, 0x3b6, x20	// Clear all CSR bits
	csrrw x26, 0x3b6, x28	// Restore CSR

// Testing CSR 0x3b7
	csrr x19, 0x3b7	// Read CSR
	li x22, -1
	csrrw x28, 0x3b7, x22	// Write all 1s to CSR
	csrrw x28, 0x3b7, x0	// Write all 0s to CSR
	csrrs x28, 0x3b7, x22	// Set all CSR bits
	csrrc x28, 0x3b7, x22	// Clear all CSR bits
	csrrw x28, 0x3b7, x19	// Restore CSR

// Testing CSR 0x3b8
	csrr x16, 0x3b8	// Read CSR
	li x19, -1
	csrrw x6, 0x3b8, x19	// Write all 1s to CSR
	csrrw x6, 0x3b8, x0	// Write all 0s to CSR
	csrrs x6, 0x3b8, x19	// Set all CSR bits
	csrrc x6, 0x3b8, x19	// Clear all CSR bits
	csrrw x6, 0x3b8, x16	// Restore CSR

// Testing CSR 0x3b9
	csrr x20, 0x3b9	// Read CSR
	li x19, -1
	csrrw x30, 0x3b9, x19	// Write all 1s to CSR
	csrrw x30, 0x3b9, x0	// Write all 0s to CSR
	csrrs x30, 0x3b9, x19	// Set all CSR bits
	csrrc x30, 0x3b9, x19	// Clear all CSR bits
	csrrw x30, 0x3b9, x20	// Restore CSR

// Testing CSR 0x3ba
	csrr x9, 0x3ba	// Read CSR
	li x6, -1
	csrrw x7, 0x3ba, x6	// Write all 1s to CSR
	csrrw x7, 0x3ba, x0	// Write all 0s to CSR
	csrrs x7, 0x3ba, x6	// Set all CSR bits
	csrrc x7, 0x3ba, x6	// Clear all CSR bits
	csrrw x7, 0x3ba, x9	// Restore CSR

// Testing CSR 0x3bb
	csrr x1, 0x3bb	// Read CSR
	li x23, -1
	csrrw x11, 0x3bb, x23	// Write all 1s to CSR
	csrrw x11, 0x3bb, x0	// Write all 0s to CSR
	csrrs x11, 0x3bb, x23	// Set all CSR bits
	csrrc x11, 0x3bb, x23	// Clear all CSR bits
	csrrw x11, 0x3bb, x1	// Restore CSR

// Testing CSR 0x3bc
	csrr x23, 0x3bc	// Read CSR
	li x30, -1
	csrrw x19, 0x3bc, x30	// Write all 1s to CSR
	csrrw x19, 0x3bc, x0	// Write all 0s to CSR
	csrrs x19, 0x3bc, x30	// Set all CSR bits
	csrrc x19, 0x3bc, x30	// Clear all CSR bits
	csrrw x19, 0x3bc, x23	// Restore CSR

// Testing CSR 0x3bd
	csrr x22, 0x3bd	// Read CSR
	li x7, -1
	csrrw x15, 0x3bd, x7	// Write all 1s to CSR
	csrrw x15, 0x3bd, x0	// Write all 0s to CSR
	csrrs x15, 0x3bd, x7	// Set all CSR bits
	csrrc x15, 0x3bd, x7	// Clear all CSR bits
	csrrw x15, 0x3bd, x22	// Restore CSR

// Testing CSR 0x3be
	csrr x12, 0x3be	// Read CSR
	li x3, -1
	csrrw x16, 0x3be, x3	// Write all 1s to CSR
	csrrw x16, 0x3be, x0	// Write all 0s to CSR
	csrrs x16, 0x3be, x3	// Set all CSR bits
	csrrc x16, 0x3be, x3	// Clear all CSR bits
	csrrw x16, 0x3be, x12	// Restore CSR

// Testing CSR 0x3bf
	csrr x15, 0x3bf	// Read CSR
	li x12, -1
	csrrw x2, 0x3bf, x12	// Write all 1s to CSR
	csrrw x2, 0x3bf, x0	// Write all 0s to CSR
	csrrs x2, 0x3bf, x12	// Set all CSR bits
	csrrc x2, 0x3bf, x12	// Clear all CSR bits
	csrrw x2, 0x3bf, x15	// Restore CSR

// Testing CSR 0x3c0
	csrr x17, 0x3c0	// Read CSR
	li x7, -1
	csrrw x3, 0x3c0, x7	// Write all 1s to CSR
	csrrw x3, 0x3c0, x0	// Write all 0s to CSR
	csrrs x3, 0x3c0, x7	// Set all CSR bits
	csrrc x3, 0x3c0, x7	// Clear all CSR bits
	csrrw x3, 0x3c0, x17	// Restore CSR

// Testing CSR 0x3c1
	csrr x7, 0x3c1	// Read CSR
	li x24, -1
	csrrw x16, 0x3c1, x24	// Write all 1s to CSR
	csrrw x16, 0x3c1, x0	// Write all 0s to CSR
	csrrs x16, 0x3c1, x24	// Set all CSR bits
	csrrc x16, 0x3c1, x24	// Clear all CSR bits
	csrrw x16, 0x3c1, x7	// Restore CSR

// Testing CSR 0x3c2
	csrr x24, 0x3c2	// Read CSR
	li x25, -1
	csrrw x6, 0x3c2, x25	// Write all 1s to CSR
	csrrw x6, 0x3c2, x0	// Write all 0s to CSR
	csrrs x6, 0x3c2, x25	// Set all CSR bits
	csrrc x6, 0x3c2, x25	// Clear all CSR bits
	csrrw x6, 0x3c2, x24	// Restore CSR

// Testing CSR 0x3c3
	csrr x4, 0x3c3	// Read CSR
	li x29, -1
	csrrw x23, 0x3c3, x29	// Write all 1s to CSR
	csrrw x23, 0x3c3, x0	// Write all 0s to CSR
	csrrs x23, 0x3c3, x29	// Set all CSR bits
	csrrc x23, 0x3c3, x29	// Clear all CSR bits
	csrrw x23, 0x3c3, x4	// Restore CSR

// Testing CSR 0x3c4
	csrr x15, 0x3c4	// Read CSR
	li x17, -1
	csrrw x1, 0x3c4, x17	// Write all 1s to CSR
	csrrw x1, 0x3c4, x0	// Write all 0s to CSR
	csrrs x1, 0x3c4, x17	// Set all CSR bits
	csrrc x1, 0x3c4, x17	// Clear all CSR bits
	csrrw x1, 0x3c4, x15	// Restore CSR

// Testing CSR 0x3c5
	csrr x22, 0x3c5	// Read CSR
	li x30, -1
	csrrw x1, 0x3c5, x30	// Write all 1s to CSR
	csrrw x1, 0x3c5, x0	// Write all 0s to CSR
	csrrs x1, 0x3c5, x30	// Set all CSR bits
	csrrc x1, 0x3c5, x30	// Clear all CSR bits
	csrrw x1, 0x3c5, x22	// Restore CSR

// Testing CSR 0x3c6
	csrr x4, 0x3c6	// Read CSR
	li x31, -1
	csrrw x7, 0x3c6, x31	// Write all 1s to CSR
	csrrw x7, 0x3c6, x0	// Write all 0s to CSR
	csrrs x7, 0x3c6, x31	// Set all CSR bits
	csrrc x7, 0x3c6, x31	// Clear all CSR bits
	csrrw x7, 0x3c6, x4	// Restore CSR

// Testing CSR 0x3c7
	csrr x3, 0x3c7	// Read CSR
	li x13, -1
	csrrw x1, 0x3c7, x13	// Write all 1s to CSR
	csrrw x1, 0x3c7, x0	// Write all 0s to CSR
	csrrs x1, 0x3c7, x13	// Set all CSR bits
	csrrc x1, 0x3c7, x13	// Clear all CSR bits
	csrrw x1, 0x3c7, x3	// Restore CSR

// Testing CSR 0x3c8
	csrr x5, 0x3c8	// Read CSR
	li x12, -1
	csrrw x22, 0x3c8, x12	// Write all 1s to CSR
	csrrw x22, 0x3c8, x0	// Write all 0s to CSR
	csrrs x22, 0x3c8, x12	// Set all CSR bits
	csrrc x22, 0x3c8, x12	// Clear all CSR bits
	csrrw x22, 0x3c8, x5	// Restore CSR

// Testing CSR 0x3c9
	csrr x30, 0x3c9	// Read CSR
	li x10, -1
	csrrw x29, 0x3c9, x10	// Write all 1s to CSR
	csrrw x29, 0x3c9, x0	// Write all 0s to CSR
	csrrs x29, 0x3c9, x10	// Set all CSR bits
	csrrc x29, 0x3c9, x10	// Clear all CSR bits
	csrrw x29, 0x3c9, x30	// Restore CSR

// Testing CSR 0x3ca
	csrr x8, 0x3ca	// Read CSR
	li x2, -1
	csrrw x4, 0x3ca, x2	// Write all 1s to CSR
	csrrw x4, 0x3ca, x0	// Write all 0s to CSR
	csrrs x4, 0x3ca, x2	// Set all CSR bits
	csrrc x4, 0x3ca, x2	// Clear all CSR bits
	csrrw x4, 0x3ca, x8	// Restore CSR

// Testing CSR 0x3cb
	csrr x11, 0x3cb	// Read CSR
	li x27, -1
	csrrw x8, 0x3cb, x27	// Write all 1s to CSR
	csrrw x8, 0x3cb, x0	// Write all 0s to CSR
	csrrs x8, 0x3cb, x27	// Set all CSR bits
	csrrc x8, 0x3cb, x27	// Clear all CSR bits
	csrrw x8, 0x3cb, x11	// Restore CSR

// Testing CSR 0x3cc
	csrr x7, 0x3cc	// Read CSR
	li x29, -1
	csrrw x9, 0x3cc, x29	// Write all 1s to CSR
	csrrw x9, 0x3cc, x0	// Write all 0s to CSR
	csrrs x9, 0x3cc, x29	// Set all CSR bits
	csrrc x9, 0x3cc, x29	// Clear all CSR bits
	csrrw x9, 0x3cc, x7	// Restore CSR

// Testing CSR 0x3cd
	csrr x19, 0x3cd	// Read CSR
	li x26, -1
	csrrw x27, 0x3cd, x26	// Write all 1s to CSR
	csrrw x27, 0x3cd, x0	// Write all 0s to CSR
	csrrs x27, 0x3cd, x26	// Set all CSR bits
	csrrc x27, 0x3cd, x26	// Clear all CSR bits
	csrrw x27, 0x3cd, x19	// Restore CSR

// Testing CSR 0x3ce
	csrr x13, 0x3ce	// Read CSR
	li x20, -1
	csrrw x23, 0x3ce, x20	// Write all 1s to CSR
	csrrw x23, 0x3ce, x0	// Write all 0s to CSR
	csrrs x23, 0x3ce, x20	// Set all CSR bits
	csrrc x23, 0x3ce, x20	// Clear all CSR bits
	csrrw x23, 0x3ce, x13	// Restore CSR

// Testing CSR 0x3cf
	csrr x12, 0x3cf	// Read CSR
	li x26, -1
	csrrw x23, 0x3cf, x26	// Write all 1s to CSR
	csrrw x23, 0x3cf, x0	// Write all 0s to CSR
	csrrs x23, 0x3cf, x26	// Set all CSR bits
	csrrc x23, 0x3cf, x26	// Clear all CSR bits
	csrrw x23, 0x3cf, x12	// Restore CSR

// Testing CSR 0x3d0
	csrr x28, 0x3d0	// Read CSR
	li x29, -1
	csrrw x18, 0x3d0, x29	// Write all 1s to CSR
	csrrw x18, 0x3d0, x0	// Write all 0s to CSR
	csrrs x18, 0x3d0, x29	// Set all CSR bits
	csrrc x18, 0x3d0, x29	// Clear all CSR bits
	csrrw x18, 0x3d0, x28	// Restore CSR

// Testing CSR 0x3d1
	csrr x8, 0x3d1	// Read CSR
	li x27, -1
	csrrw x4, 0x3d1, x27	// Write all 1s to CSR
	csrrw x4, 0x3d1, x0	// Write all 0s to CSR
	csrrs x4, 0x3d1, x27	// Set all CSR bits
	csrrc x4, 0x3d1, x27	// Clear all CSR bits
	csrrw x4, 0x3d1, x8	// Restore CSR

// Testing CSR 0x3d2
	csrr x20, 0x3d2	// Read CSR
	li x19, -1
	csrrw x18, 0x3d2, x19	// Write all 1s to CSR
	csrrw x18, 0x3d2, x0	// Write all 0s to CSR
	csrrs x18, 0x3d2, x19	// Set all CSR bits
	csrrc x18, 0x3d2, x19	// Clear all CSR bits
	csrrw x18, 0x3d2, x20	// Restore CSR

// Testing CSR 0x3d3
	csrr x26, 0x3d3	// Read CSR
	li x5, -1
	csrrw x10, 0x3d3, x5	// Write all 1s to CSR
	csrrw x10, 0x3d3, x0	// Write all 0s to CSR
	csrrs x10, 0x3d3, x5	// Set all CSR bits
	csrrc x10, 0x3d3, x5	// Clear all CSR bits
	csrrw x10, 0x3d3, x26	// Restore CSR

// Testing CSR 0x3d4
	csrr x20, 0x3d4	// Read CSR
	li x27, -1
	csrrw x15, 0x3d4, x27	// Write all 1s to CSR
	csrrw x15, 0x3d4, x0	// Write all 0s to CSR
	csrrs x15, 0x3d4, x27	// Set all CSR bits
	csrrc x15, 0x3d4, x27	// Clear all CSR bits
	csrrw x15, 0x3d4, x20	// Restore CSR

// Testing CSR 0x3d5
	csrr x12, 0x3d5	// Read CSR
	li x21, -1
	csrrw x24, 0x3d5, x21	// Write all 1s to CSR
	csrrw x24, 0x3d5, x0	// Write all 0s to CSR
	csrrs x24, 0x3d5, x21	// Set all CSR bits
	csrrc x24, 0x3d5, x21	// Clear all CSR bits
	csrrw x24, 0x3d5, x12	// Restore CSR

// Testing CSR 0x3d6
	csrr x15, 0x3d6	// Read CSR
	li x28, -1
	csrrw x8, 0x3d6, x28	// Write all 1s to CSR
	csrrw x8, 0x3d6, x0	// Write all 0s to CSR
	csrrs x8, 0x3d6, x28	// Set all CSR bits
	csrrc x8, 0x3d6, x28	// Clear all CSR bits
	csrrw x8, 0x3d6, x15	// Restore CSR

// Testing CSR 0x3d7
	csrr x12, 0x3d7	// Read CSR
	li x22, -1
	csrrw x24, 0x3d7, x22	// Write all 1s to CSR
	csrrw x24, 0x3d7, x0	// Write all 0s to CSR
	csrrs x24, 0x3d7, x22	// Set all CSR bits
	csrrc x24, 0x3d7, x22	// Clear all CSR bits
	csrrw x24, 0x3d7, x12	// Restore CSR

// Testing CSR 0x3d8
	csrr x30, 0x3d8	// Read CSR
	li x27, -1
	csrrw x23, 0x3d8, x27	// Write all 1s to CSR
	csrrw x23, 0x3d8, x0	// Write all 0s to CSR
	csrrs x23, 0x3d8, x27	// Set all CSR bits
	csrrc x23, 0x3d8, x27	// Clear all CSR bits
	csrrw x23, 0x3d8, x30	// Restore CSR

// Testing CSR 0x3d9
	csrr x11, 0x3d9	// Read CSR
	li x13, -1
	csrrw x25, 0x3d9, x13	// Write all 1s to CSR
	csrrw x25, 0x3d9, x0	// Write all 0s to CSR
	csrrs x25, 0x3d9, x13	// Set all CSR bits
	csrrc x25, 0x3d9, x13	// Clear all CSR bits
	csrrw x25, 0x3d9, x11	// Restore CSR

// Testing CSR 0x3da
	csrr x20, 0x3da	// Read CSR
	li x26, -1
	csrrw x31, 0x3da, x26	// Write all 1s to CSR
	csrrw x31, 0x3da, x0	// Write all 0s to CSR
	csrrs x31, 0x3da, x26	// Set all CSR bits
	csrrc x31, 0x3da, x26	// Clear all CSR bits
	csrrw x31, 0x3da, x20	// Restore CSR

// Testing CSR 0x3db
	csrr x1, 0x3db	// Read CSR
	li x24, -1
	csrrw x5, 0x3db, x24	// Write all 1s to CSR
	csrrw x5, 0x3db, x0	// Write all 0s to CSR
	csrrs x5, 0x3db, x24	// Set all CSR bits
	csrrc x5, 0x3db, x24	// Clear all CSR bits
	csrrw x5, 0x3db, x1	// Restore CSR

// Testing CSR 0x3dc
	csrr x23, 0x3dc	// Read CSR
	li x13, -1
	csrrw x21, 0x3dc, x13	// Write all 1s to CSR
	csrrw x21, 0x3dc, x0	// Write all 0s to CSR
	csrrs x21, 0x3dc, x13	// Set all CSR bits
	csrrc x21, 0x3dc, x13	// Clear all CSR bits
	csrrw x21, 0x3dc, x23	// Restore CSR

// Testing CSR 0x3dd
	csrr x23, 0x3dd	// Read CSR
	li x15, -1
	csrrw x12, 0x3dd, x15	// Write all 1s to CSR
	csrrw x12, 0x3dd, x0	// Write all 0s to CSR
	csrrs x12, 0x3dd, x15	// Set all CSR bits
	csrrc x12, 0x3dd, x15	// Clear all CSR bits
	csrrw x12, 0x3dd, x23	// Restore CSR

// Testing CSR 0x3de
	csrr x16, 0x3de	// Read CSR
	li x10, -1
	csrrw x3, 0x3de, x10	// Write all 1s to CSR
	csrrw x3, 0x3de, x0	// Write all 0s to CSR
	csrrs x3, 0x3de, x10	// Set all CSR bits
	csrrc x3, 0x3de, x10	// Clear all CSR bits
	csrrw x3, 0x3de, x16	// Restore CSR

// Testing CSR 0x3df
	csrr x18, 0x3df	// Read CSR
	li x4, -1
	csrrw x31, 0x3df, x4	// Write all 1s to CSR
	csrrw x31, 0x3df, x0	// Write all 0s to CSR
	csrrs x31, 0x3df, x4	// Set all CSR bits
	csrrc x31, 0x3df, x4	// Clear all CSR bits
	csrrw x31, 0x3df, x18	// Restore CSR

// Testing CSR 0x3e0
	csrr x28, 0x3e0	// Read CSR
	li x21, -1
	csrrw x31, 0x3e0, x21	// Write all 1s to CSR
	csrrw x31, 0x3e0, x0	// Write all 0s to CSR
	csrrs x31, 0x3e0, x21	// Set all CSR bits
	csrrc x31, 0x3e0, x21	// Clear all CSR bits
	csrrw x31, 0x3e0, x28	// Restore CSR

// Testing CSR 0x3e1
	csrr x12, 0x3e1	// Read CSR
	li x24, -1
	csrrw x6, 0x3e1, x24	// Write all 1s to CSR
	csrrw x6, 0x3e1, x0	// Write all 0s to CSR
	csrrs x6, 0x3e1, x24	// Set all CSR bits
	csrrc x6, 0x3e1, x24	// Clear all CSR bits
	csrrw x6, 0x3e1, x12	// Restore CSR

// Testing CSR 0x3e2
	csrr x18, 0x3e2	// Read CSR
	li x24, -1
	csrrw x20, 0x3e2, x24	// Write all 1s to CSR
	csrrw x20, 0x3e2, x0	// Write all 0s to CSR
	csrrs x20, 0x3e2, x24	// Set all CSR bits
	csrrc x20, 0x3e2, x24	// Clear all CSR bits
	csrrw x20, 0x3e2, x18	// Restore CSR

// Testing CSR 0x3e3
	csrr x21, 0x3e3	// Read CSR
	li x24, -1
	csrrw x2, 0x3e3, x24	// Write all 1s to CSR
	csrrw x2, 0x3e3, x0	// Write all 0s to CSR
	csrrs x2, 0x3e3, x24	// Set all CSR bits
	csrrc x2, 0x3e3, x24	// Clear all CSR bits
	csrrw x2, 0x3e3, x21	// Restore CSR

// Testing CSR 0x3e4
	csrr x25, 0x3e4	// Read CSR
	li x12, -1
	csrrw x14, 0x3e4, x12	// Write all 1s to CSR
	csrrw x14, 0x3e4, x0	// Write all 0s to CSR
	csrrs x14, 0x3e4, x12	// Set all CSR bits
	csrrc x14, 0x3e4, x12	// Clear all CSR bits
	csrrw x14, 0x3e4, x25	// Restore CSR

// Testing CSR 0x3e5
	csrr x12, 0x3e5	// Read CSR
	li x5, -1
	csrrw x23, 0x3e5, x5	// Write all 1s to CSR
	csrrw x23, 0x3e5, x0	// Write all 0s to CSR
	csrrs x23, 0x3e5, x5	// Set all CSR bits
	csrrc x23, 0x3e5, x5	// Clear all CSR bits
	csrrw x23, 0x3e5, x12	// Restore CSR

// Testing CSR 0x3e6
	csrr x8, 0x3e6	// Read CSR
	li x23, -1
	csrrw x1, 0x3e6, x23	// Write all 1s to CSR
	csrrw x1, 0x3e6, x0	// Write all 0s to CSR
	csrrs x1, 0x3e6, x23	// Set all CSR bits
	csrrc x1, 0x3e6, x23	// Clear all CSR bits
	csrrw x1, 0x3e6, x8	// Restore CSR

// Testing CSR 0x3e7
	csrr x19, 0x3e7	// Read CSR
	li x18, -1
	csrrw x16, 0x3e7, x18	// Write all 1s to CSR
	csrrw x16, 0x3e7, x0	// Write all 0s to CSR
	csrrs x16, 0x3e7, x18	// Set all CSR bits
	csrrc x16, 0x3e7, x18	// Clear all CSR bits
	csrrw x16, 0x3e7, x19	// Restore CSR

// Testing CSR 0x3e8
	csrr x25, 0x3e8	// Read CSR
	li x21, -1
	csrrw x5, 0x3e8, x21	// Write all 1s to CSR
	csrrw x5, 0x3e8, x0	// Write all 0s to CSR
	csrrs x5, 0x3e8, x21	// Set all CSR bits
	csrrc x5, 0x3e8, x21	// Clear all CSR bits
	csrrw x5, 0x3e8, x25	// Restore CSR

// Testing CSR 0x3e9
	csrr x31, 0x3e9	// Read CSR
	li x23, -1
	csrrw x20, 0x3e9, x23	// Write all 1s to CSR
	csrrw x20, 0x3e9, x0	// Write all 0s to CSR
	csrrs x20, 0x3e9, x23	// Set all CSR bits
	csrrc x20, 0x3e9, x23	// Clear all CSR bits
	csrrw x20, 0x3e9, x31	// Restore CSR

// Testing CSR 0x3ea
	csrr x14, 0x3ea	// Read CSR
	li x8, -1
	csrrw x17, 0x3ea, x8	// Write all 1s to CSR
	csrrw x17, 0x3ea, x0	// Write all 0s to CSR
	csrrs x17, 0x3ea, x8	// Set all CSR bits
	csrrc x17, 0x3ea, x8	// Clear all CSR bits
	csrrw x17, 0x3ea, x14	// Restore CSR

// Testing CSR 0x3eb
	csrr x19, 0x3eb	// Read CSR
	li x9, -1
	csrrw x5, 0x3eb, x9	// Write all 1s to CSR
	csrrw x5, 0x3eb, x0	// Write all 0s to CSR
	csrrs x5, 0x3eb, x9	// Set all CSR bits
	csrrc x5, 0x3eb, x9	// Clear all CSR bits
	csrrw x5, 0x3eb, x19	// Restore CSR

// Testing CSR 0x3ec
	csrr x2, 0x3ec	// Read CSR
	li x22, -1
	csrrw x30, 0x3ec, x22	// Write all 1s to CSR
	csrrw x30, 0x3ec, x0	// Write all 0s to CSR
	csrrs x30, 0x3ec, x22	// Set all CSR bits
	csrrc x30, 0x3ec, x22	// Clear all CSR bits
	csrrw x30, 0x3ec, x2	// Restore CSR

// Testing CSR 0x3ed
	csrr x20, 0x3ed	// Read CSR
	li x28, -1
	csrrw x18, 0x3ed, x28	// Write all 1s to CSR
	csrrw x18, 0x3ed, x0	// Write all 0s to CSR
	csrrs x18, 0x3ed, x28	// Set all CSR bits
	csrrc x18, 0x3ed, x28	// Clear all CSR bits
	csrrw x18, 0x3ed, x20	// Restore CSR

// Testing CSR 0x3ee
	csrr x16, 0x3ee	// Read CSR
	li x19, -1
	csrrw x2, 0x3ee, x19	// Write all 1s to CSR
	csrrw x2, 0x3ee, x0	// Write all 0s to CSR
	csrrs x2, 0x3ee, x19	// Set all CSR bits
	csrrc x2, 0x3ee, x19	// Clear all CSR bits
	csrrw x2, 0x3ee, x16	// Restore CSR

// Testing CSR 0x3ef
	csrr x21, 0x3ef	// Read CSR
	li x13, -1
	csrrw x5, 0x3ef, x13	// Write all 1s to CSR
	csrrw x5, 0x3ef, x0	// Write all 0s to CSR
	csrrs x5, 0x3ef, x13	// Set all CSR bits
	csrrc x5, 0x3ef, x13	// Clear all CSR bits
	csrrw x5, 0x3ef, x21	// Restore CSR

// Testing CSR 0x3f0
	csrr x18, 0x3f0	// Read CSR
	li x30, -1
	csrrw x14, 0x3f0, x30	// Write all 1s to CSR
	csrrw x14, 0x3f0, x0	// Write all 0s to CSR
	csrrs x14, 0x3f0, x30	// Set all CSR bits
	csrrc x14, 0x3f0, x30	// Clear all CSR bits
	csrrw x14, 0x3f0, x18	// Restore CSR

// Testing CSR 0x3f1
	csrr x4, 0x3f1	// Read CSR
	li x2, -1
	csrrw x1, 0x3f1, x2	// Write all 1s to CSR
	csrrw x1, 0x3f1, x0	// Write all 0s to CSR
	csrrs x1, 0x3f1, x2	// Set all CSR bits
	csrrc x1, 0x3f1, x2	// Clear all CSR bits
	csrrw x1, 0x3f1, x4	// Restore CSR

// Testing CSR 0x3f2
	csrr x28, 0x3f2	// Read CSR
	li x29, -1
	csrrw x3, 0x3f2, x29	// Write all 1s to CSR
	csrrw x3, 0x3f2, x0	// Write all 0s to CSR
	csrrs x3, 0x3f2, x29	// Set all CSR bits
	csrrc x3, 0x3f2, x29	// Clear all CSR bits
	csrrw x3, 0x3f2, x28	// Restore CSR

// Testing CSR 0x3f3
	csrr x22, 0x3f3	// Read CSR
	li x7, -1
	csrrw x31, 0x3f3, x7	// Write all 1s to CSR
	csrrw x31, 0x3f3, x0	// Write all 0s to CSR
	csrrs x31, 0x3f3, x7	// Set all CSR bits
	csrrc x31, 0x3f3, x7	// Clear all CSR bits
	csrrw x31, 0x3f3, x22	// Restore CSR

// Testing CSR 0x3f4
	csrr x24, 0x3f4	// Read CSR
	li x5, -1
	csrrw x19, 0x3f4, x5	// Write all 1s to CSR
	csrrw x19, 0x3f4, x0	// Write all 0s to CSR
	csrrs x19, 0x3f4, x5	// Set all CSR bits
	csrrc x19, 0x3f4, x5	// Clear all CSR bits
	csrrw x19, 0x3f4, x24	// Restore CSR

// Testing CSR 0x3f5
	csrr x17, 0x3f5	// Read CSR
	li x23, -1
	csrrw x11, 0x3f5, x23	// Write all 1s to CSR
	csrrw x11, 0x3f5, x0	// Write all 0s to CSR
	csrrs x11, 0x3f5, x23	// Set all CSR bits
	csrrc x11, 0x3f5, x23	// Clear all CSR bits
	csrrw x11, 0x3f5, x17	// Restore CSR

// Testing CSR 0x3f6
	csrr x9, 0x3f6	// Read CSR
	li x31, -1
	csrrw x15, 0x3f6, x31	// Write all 1s to CSR
	csrrw x15, 0x3f6, x0	// Write all 0s to CSR
	csrrs x15, 0x3f6, x31	// Set all CSR bits
	csrrc x15, 0x3f6, x31	// Clear all CSR bits
	csrrw x15, 0x3f6, x9	// Restore CSR

// Testing CSR 0x3f7
	csrr x24, 0x3f7	// Read CSR
	li x22, -1
	csrrw x5, 0x3f7, x22	// Write all 1s to CSR
	csrrw x5, 0x3f7, x0	// Write all 0s to CSR
	csrrs x5, 0x3f7, x22	// Set all CSR bits
	csrrc x5, 0x3f7, x22	// Clear all CSR bits
	csrrw x5, 0x3f7, x24	// Restore CSR

// Testing CSR 0x3f8
	csrr x22, 0x3f8	// Read CSR
	li x25, -1
	csrrw x27, 0x3f8, x25	// Write all 1s to CSR
	csrrw x27, 0x3f8, x0	// Write all 0s to CSR
	csrrs x27, 0x3f8, x25	// Set all CSR bits
	csrrc x27, 0x3f8, x25	// Clear all CSR bits
	csrrw x27, 0x3f8, x22	// Restore CSR

// Testing CSR 0x3f9
	csrr x10, 0x3f9	// Read CSR
	li x29, -1
	csrrw x11, 0x3f9, x29	// Write all 1s to CSR
	csrrw x11, 0x3f9, x0	// Write all 0s to CSR
	csrrs x11, 0x3f9, x29	// Set all CSR bits
	csrrc x11, 0x3f9, x29	// Clear all CSR bits
	csrrw x11, 0x3f9, x10	// Restore CSR

// Testing CSR 0x3fa
	csrr x30, 0x3fa	// Read CSR
	li x23, -1
	csrrw x26, 0x3fa, x23	// Write all 1s to CSR
	csrrw x26, 0x3fa, x0	// Write all 0s to CSR
	csrrs x26, 0x3fa, x23	// Set all CSR bits
	csrrc x26, 0x3fa, x23	// Clear all CSR bits
	csrrw x26, 0x3fa, x30	// Restore CSR

// Testing CSR 0x3fb
	csrr x23, 0x3fb	// Read CSR
	li x11, -1
	csrrw x7, 0x3fb, x11	// Write all 1s to CSR
	csrrw x7, 0x3fb, x0	// Write all 0s to CSR
	csrrs x7, 0x3fb, x11	// Set all CSR bits
	csrrc x7, 0x3fb, x11	// Clear all CSR bits
	csrrw x7, 0x3fb, x23	// Restore CSR

// Testing CSR 0x3fc
	csrr x9, 0x3fc	// Read CSR
	li x27, -1
	csrrw x29, 0x3fc, x27	// Write all 1s to CSR
	csrrw x29, 0x3fc, x0	// Write all 0s to CSR
	csrrs x29, 0x3fc, x27	// Set all CSR bits
	csrrc x29, 0x3fc, x27	// Clear all CSR bits
	csrrw x29, 0x3fc, x9	// Restore CSR

// Testing CSR 0x3fd
	csrr x12, 0x3fd	// Read CSR
	li x3, -1
	csrrw x2, 0x3fd, x3	// Write all 1s to CSR
	csrrw x2, 0x3fd, x0	// Write all 0s to CSR
	csrrs x2, 0x3fd, x3	// Set all CSR bits
	csrrc x2, 0x3fd, x3	// Clear all CSR bits
	csrrw x2, 0x3fd, x12	// Restore CSR

// Testing CSR 0x3fe
	csrr x13, 0x3fe	// Read CSR
	li x17, -1
	csrrw x18, 0x3fe, x17	// Write all 1s to CSR
	csrrw x18, 0x3fe, x0	// Write all 0s to CSR
	csrrs x18, 0x3fe, x17	// Set all CSR bits
	csrrc x18, 0x3fe, x17	// Clear all CSR bits
	csrrw x18, 0x3fe, x13	// Restore CSR

// Testing CSR 0x3ff
	csrr x16, 0x3ff	// Read CSR
	li x15, -1
	csrrw x19, 0x3ff, x15	// Write all 1s to CSR
	csrrw x19, 0x3ff, x0	// Write all 0s to CSR
	csrrs x19, 0x3ff, x15	// Set all CSR bits
	csrrc x19, 0x3ff, x15	// Clear all CSR bits
	csrrw x19, 0x3ff, x16	// Restore CSR

// Testing CSR 0x400
	csrr x7, 0x400	// Read CSR
	li x30, -1
	csrrw x20, 0x400, x30	// Write all 1s to CSR
	csrrw x20, 0x400, x0	// Write all 0s to CSR
	csrrs x20, 0x400, x30	// Set all CSR bits
	csrrc x20, 0x400, x30	// Clear all CSR bits
	csrrw x20, 0x400, x7	// Restore CSR

// Testing CSR 0x401
	csrr x3, 0x401	// Read CSR
	li x16, -1
	csrrw x4, 0x401, x16	// Write all 1s to CSR
	csrrw x4, 0x401, x0	// Write all 0s to CSR
	csrrs x4, 0x401, x16	// Set all CSR bits
	csrrc x4, 0x401, x16	// Clear all CSR bits
	csrrw x4, 0x401, x3	// Restore CSR

// Testing CSR 0x402
	csrr x22, 0x402	// Read CSR
	li x9, -1
	csrrw x15, 0x402, x9	// Write all 1s to CSR
	csrrw x15, 0x402, x0	// Write all 0s to CSR
	csrrs x15, 0x402, x9	// Set all CSR bits
	csrrc x15, 0x402, x9	// Clear all CSR bits
	csrrw x15, 0x402, x22	// Restore CSR

// Testing CSR 0x403
	csrr x27, 0x403	// Read CSR
	li x20, -1
	csrrw x8, 0x403, x20	// Write all 1s to CSR
	csrrw x8, 0x403, x0	// Write all 0s to CSR
	csrrs x8, 0x403, x20	// Set all CSR bits
	csrrc x8, 0x403, x20	// Clear all CSR bits
	csrrw x8, 0x403, x27	// Restore CSR

// Testing CSR 0x404
	csrr x26, 0x404	// Read CSR
	li x20, -1
	csrrw x25, 0x404, x20	// Write all 1s to CSR
	csrrw x25, 0x404, x0	// Write all 0s to CSR
	csrrs x25, 0x404, x20	// Set all CSR bits
	csrrc x25, 0x404, x20	// Clear all CSR bits
	csrrw x25, 0x404, x26	// Restore CSR

// Testing CSR 0x405
	csrr x14, 0x405	// Read CSR
	li x18, -1
	csrrw x12, 0x405, x18	// Write all 1s to CSR
	csrrw x12, 0x405, x0	// Write all 0s to CSR
	csrrs x12, 0x405, x18	// Set all CSR bits
	csrrc x12, 0x405, x18	// Clear all CSR bits
	csrrw x12, 0x405, x14	// Restore CSR

// Testing CSR 0x406
	csrr x27, 0x406	// Read CSR
	li x13, -1
	csrrw x19, 0x406, x13	// Write all 1s to CSR
	csrrw x19, 0x406, x0	// Write all 0s to CSR
	csrrs x19, 0x406, x13	// Set all CSR bits
	csrrc x19, 0x406, x13	// Clear all CSR bits
	csrrw x19, 0x406, x27	// Restore CSR

// Testing CSR 0x407
	csrr x22, 0x407	// Read CSR
	li x18, -1
	csrrw x9, 0x407, x18	// Write all 1s to CSR
	csrrw x9, 0x407, x0	// Write all 0s to CSR
	csrrs x9, 0x407, x18	// Set all CSR bits
	csrrc x9, 0x407, x18	// Clear all CSR bits
	csrrw x9, 0x407, x22	// Restore CSR

// Testing CSR 0x408
	csrr x2, 0x408	// Read CSR
	li x4, -1
	csrrw x11, 0x408, x4	// Write all 1s to CSR
	csrrw x11, 0x408, x0	// Write all 0s to CSR
	csrrs x11, 0x408, x4	// Set all CSR bits
	csrrc x11, 0x408, x4	// Clear all CSR bits
	csrrw x11, 0x408, x2	// Restore CSR

// Testing CSR 0x409
	csrr x5, 0x409	// Read CSR
	li x17, -1
	csrrw x22, 0x409, x17	// Write all 1s to CSR
	csrrw x22, 0x409, x0	// Write all 0s to CSR
	csrrs x22, 0x409, x17	// Set all CSR bits
	csrrc x22, 0x409, x17	// Clear all CSR bits
	csrrw x22, 0x409, x5	// Restore CSR

// Testing CSR 0x40a
	csrr x6, 0x40a	// Read CSR
	li x2, -1
	csrrw x3, 0x40a, x2	// Write all 1s to CSR
	csrrw x3, 0x40a, x0	// Write all 0s to CSR
	csrrs x3, 0x40a, x2	// Set all CSR bits
	csrrc x3, 0x40a, x2	// Clear all CSR bits
	csrrw x3, 0x40a, x6	// Restore CSR

// Testing CSR 0x40b
	csrr x8, 0x40b	// Read CSR
	li x23, -1
	csrrw x18, 0x40b, x23	// Write all 1s to CSR
	csrrw x18, 0x40b, x0	// Write all 0s to CSR
	csrrs x18, 0x40b, x23	// Set all CSR bits
	csrrc x18, 0x40b, x23	// Clear all CSR bits
	csrrw x18, 0x40b, x8	// Restore CSR

// Testing CSR 0x40c
	csrr x18, 0x40c	// Read CSR
	li x24, -1
	csrrw x26, 0x40c, x24	// Write all 1s to CSR
	csrrw x26, 0x40c, x0	// Write all 0s to CSR
	csrrs x26, 0x40c, x24	// Set all CSR bits
	csrrc x26, 0x40c, x24	// Clear all CSR bits
	csrrw x26, 0x40c, x18	// Restore CSR

// Testing CSR 0x40d
	csrr x18, 0x40d	// Read CSR
	li x16, -1
	csrrw x17, 0x40d, x16	// Write all 1s to CSR
	csrrw x17, 0x40d, x0	// Write all 0s to CSR
	csrrs x17, 0x40d, x16	// Set all CSR bits
	csrrc x17, 0x40d, x16	// Clear all CSR bits
	csrrw x17, 0x40d, x18	// Restore CSR

// Testing CSR 0x40e
	csrr x4, 0x40e	// Read CSR
	li x12, -1
	csrrw x11, 0x40e, x12	// Write all 1s to CSR
	csrrw x11, 0x40e, x0	// Write all 0s to CSR
	csrrs x11, 0x40e, x12	// Set all CSR bits
	csrrc x11, 0x40e, x12	// Clear all CSR bits
	csrrw x11, 0x40e, x4	// Restore CSR

// Testing CSR 0x40f
	csrr x7, 0x40f	// Read CSR
	li x15, -1
	csrrw x3, 0x40f, x15	// Write all 1s to CSR
	csrrw x3, 0x40f, x0	// Write all 0s to CSR
	csrrs x3, 0x40f, x15	// Set all CSR bits
	csrrc x3, 0x40f, x15	// Clear all CSR bits
	csrrw x3, 0x40f, x7	// Restore CSR

// Testing CSR 0x410
	csrr x3, 0x410	// Read CSR
	li x4, -1
	csrrw x21, 0x410, x4	// Write all 1s to CSR
	csrrw x21, 0x410, x0	// Write all 0s to CSR
	csrrs x21, 0x410, x4	// Set all CSR bits
	csrrc x21, 0x410, x4	// Clear all CSR bits
	csrrw x21, 0x410, x3	// Restore CSR

// Testing CSR 0x411
	csrr x29, 0x411	// Read CSR
	li x16, -1
	csrrw x22, 0x411, x16	// Write all 1s to CSR
	csrrw x22, 0x411, x0	// Write all 0s to CSR
	csrrs x22, 0x411, x16	// Set all CSR bits
	csrrc x22, 0x411, x16	// Clear all CSR bits
	csrrw x22, 0x411, x29	// Restore CSR

// Testing CSR 0x412
	csrr x5, 0x412	// Read CSR
	li x29, -1
	csrrw x16, 0x412, x29	// Write all 1s to CSR
	csrrw x16, 0x412, x0	// Write all 0s to CSR
	csrrs x16, 0x412, x29	// Set all CSR bits
	csrrc x16, 0x412, x29	// Clear all CSR bits
	csrrw x16, 0x412, x5	// Restore CSR

// Testing CSR 0x413
	csrr x10, 0x413	// Read CSR
	li x25, -1
	csrrw x3, 0x413, x25	// Write all 1s to CSR
	csrrw x3, 0x413, x0	// Write all 0s to CSR
	csrrs x3, 0x413, x25	// Set all CSR bits
	csrrc x3, 0x413, x25	// Clear all CSR bits
	csrrw x3, 0x413, x10	// Restore CSR

// Testing CSR 0x414
	csrr x26, 0x414	// Read CSR
	li x15, -1
	csrrw x5, 0x414, x15	// Write all 1s to CSR
	csrrw x5, 0x414, x0	// Write all 0s to CSR
	csrrs x5, 0x414, x15	// Set all CSR bits
	csrrc x5, 0x414, x15	// Clear all CSR bits
	csrrw x5, 0x414, x26	// Restore CSR

// Testing CSR 0x415
	csrr x28, 0x415	// Read CSR
	li x20, -1
	csrrw x17, 0x415, x20	// Write all 1s to CSR
	csrrw x17, 0x415, x0	// Write all 0s to CSR
	csrrs x17, 0x415, x20	// Set all CSR bits
	csrrc x17, 0x415, x20	// Clear all CSR bits
	csrrw x17, 0x415, x28	// Restore CSR

// Testing CSR 0x416
	csrr x9, 0x416	// Read CSR
	li x17, -1
	csrrw x5, 0x416, x17	// Write all 1s to CSR
	csrrw x5, 0x416, x0	// Write all 0s to CSR
	csrrs x5, 0x416, x17	// Set all CSR bits
	csrrc x5, 0x416, x17	// Clear all CSR bits
	csrrw x5, 0x416, x9	// Restore CSR

// Testing CSR 0x417
	csrr x23, 0x417	// Read CSR
	li x19, -1
	csrrw x7, 0x417, x19	// Write all 1s to CSR
	csrrw x7, 0x417, x0	// Write all 0s to CSR
	csrrs x7, 0x417, x19	// Set all CSR bits
	csrrc x7, 0x417, x19	// Clear all CSR bits
	csrrw x7, 0x417, x23	// Restore CSR

// Testing CSR 0x418
	csrr x27, 0x418	// Read CSR
	li x24, -1
	csrrw x3, 0x418, x24	// Write all 1s to CSR
	csrrw x3, 0x418, x0	// Write all 0s to CSR
	csrrs x3, 0x418, x24	// Set all CSR bits
	csrrc x3, 0x418, x24	// Clear all CSR bits
	csrrw x3, 0x418, x27	// Restore CSR

// Testing CSR 0x419
	csrr x2, 0x419	// Read CSR
	li x21, -1
	csrrw x10, 0x419, x21	// Write all 1s to CSR
	csrrw x10, 0x419, x0	// Write all 0s to CSR
	csrrs x10, 0x419, x21	// Set all CSR bits
	csrrc x10, 0x419, x21	// Clear all CSR bits
	csrrw x10, 0x419, x2	// Restore CSR

// Testing CSR 0x41a
	csrr x9, 0x41a	// Read CSR
	li x24, -1
	csrrw x16, 0x41a, x24	// Write all 1s to CSR
	csrrw x16, 0x41a, x0	// Write all 0s to CSR
	csrrs x16, 0x41a, x24	// Set all CSR bits
	csrrc x16, 0x41a, x24	// Clear all CSR bits
	csrrw x16, 0x41a, x9	// Restore CSR

// Testing CSR 0x41b
	csrr x8, 0x41b	// Read CSR
	li x15, -1
	csrrw x17, 0x41b, x15	// Write all 1s to CSR
	csrrw x17, 0x41b, x0	// Write all 0s to CSR
	csrrs x17, 0x41b, x15	// Set all CSR bits
	csrrc x17, 0x41b, x15	// Clear all CSR bits
	csrrw x17, 0x41b, x8	// Restore CSR

// Testing CSR 0x41c
	csrr x20, 0x41c	// Read CSR
	li x7, -1
	csrrw x27, 0x41c, x7	// Write all 1s to CSR
	csrrw x27, 0x41c, x0	// Write all 0s to CSR
	csrrs x27, 0x41c, x7	// Set all CSR bits
	csrrc x27, 0x41c, x7	// Clear all CSR bits
	csrrw x27, 0x41c, x20	// Restore CSR

// Testing CSR 0x41d
	csrr x12, 0x41d	// Read CSR
	li x24, -1
	csrrw x13, 0x41d, x24	// Write all 1s to CSR
	csrrw x13, 0x41d, x0	// Write all 0s to CSR
	csrrs x13, 0x41d, x24	// Set all CSR bits
	csrrc x13, 0x41d, x24	// Clear all CSR bits
	csrrw x13, 0x41d, x12	// Restore CSR

// Testing CSR 0x41e
	csrr x7, 0x41e	// Read CSR
	li x14, -1
	csrrw x27, 0x41e, x14	// Write all 1s to CSR
	csrrw x27, 0x41e, x0	// Write all 0s to CSR
	csrrs x27, 0x41e, x14	// Set all CSR bits
	csrrc x27, 0x41e, x14	// Clear all CSR bits
	csrrw x27, 0x41e, x7	// Restore CSR

// Testing CSR 0x41f
	csrr x17, 0x41f	// Read CSR
	li x20, -1
	csrrw x5, 0x41f, x20	// Write all 1s to CSR
	csrrw x5, 0x41f, x0	// Write all 0s to CSR
	csrrs x5, 0x41f, x20	// Set all CSR bits
	csrrc x5, 0x41f, x20	// Clear all CSR bits
	csrrw x5, 0x41f, x17	// Restore CSR

// Testing CSR 0x420
	csrr x3, 0x420	// Read CSR
	li x19, -1
	csrrw x29, 0x420, x19	// Write all 1s to CSR
	csrrw x29, 0x420, x0	// Write all 0s to CSR
	csrrs x29, 0x420, x19	// Set all CSR bits
	csrrc x29, 0x420, x19	// Clear all CSR bits
	csrrw x29, 0x420, x3	// Restore CSR

// Testing CSR 0x421
	csrr x8, 0x421	// Read CSR
	li x21, -1
	csrrw x14, 0x421, x21	// Write all 1s to CSR
	csrrw x14, 0x421, x0	// Write all 0s to CSR
	csrrs x14, 0x421, x21	// Set all CSR bits
	csrrc x14, 0x421, x21	// Clear all CSR bits
	csrrw x14, 0x421, x8	// Restore CSR

// Testing CSR 0x422
	csrr x10, 0x422	// Read CSR
	li x7, -1
	csrrw x5, 0x422, x7	// Write all 1s to CSR
	csrrw x5, 0x422, x0	// Write all 0s to CSR
	csrrs x5, 0x422, x7	// Set all CSR bits
	csrrc x5, 0x422, x7	// Clear all CSR bits
	csrrw x5, 0x422, x10	// Restore CSR

// Testing CSR 0x423
	csrr x6, 0x423	// Read CSR
	li x9, -1
	csrrw x12, 0x423, x9	// Write all 1s to CSR
	csrrw x12, 0x423, x0	// Write all 0s to CSR
	csrrs x12, 0x423, x9	// Set all CSR bits
	csrrc x12, 0x423, x9	// Clear all CSR bits
	csrrw x12, 0x423, x6	// Restore CSR

// Testing CSR 0x424
	csrr x4, 0x424	// Read CSR
	li x5, -1
	csrrw x12, 0x424, x5	// Write all 1s to CSR
	csrrw x12, 0x424, x0	// Write all 0s to CSR
	csrrs x12, 0x424, x5	// Set all CSR bits
	csrrc x12, 0x424, x5	// Clear all CSR bits
	csrrw x12, 0x424, x4	// Restore CSR

// Testing CSR 0x425
	csrr x22, 0x425	// Read CSR
	li x28, -1
	csrrw x29, 0x425, x28	// Write all 1s to CSR
	csrrw x29, 0x425, x0	// Write all 0s to CSR
	csrrs x29, 0x425, x28	// Set all CSR bits
	csrrc x29, 0x425, x28	// Clear all CSR bits
	csrrw x29, 0x425, x22	// Restore CSR

// Testing CSR 0x426
	csrr x26, 0x426	// Read CSR
	li x23, -1
	csrrw x28, 0x426, x23	// Write all 1s to CSR
	csrrw x28, 0x426, x0	// Write all 0s to CSR
	csrrs x28, 0x426, x23	// Set all CSR bits
	csrrc x28, 0x426, x23	// Clear all CSR bits
	csrrw x28, 0x426, x26	// Restore CSR

// Testing CSR 0x427
	csrr x18, 0x427	// Read CSR
	li x14, -1
	csrrw x4, 0x427, x14	// Write all 1s to CSR
	csrrw x4, 0x427, x0	// Write all 0s to CSR
	csrrs x4, 0x427, x14	// Set all CSR bits
	csrrc x4, 0x427, x14	// Clear all CSR bits
	csrrw x4, 0x427, x18	// Restore CSR

// Testing CSR 0x428
	csrr x31, 0x428	// Read CSR
	li x26, -1
	csrrw x25, 0x428, x26	// Write all 1s to CSR
	csrrw x25, 0x428, x0	// Write all 0s to CSR
	csrrs x25, 0x428, x26	// Set all CSR bits
	csrrc x25, 0x428, x26	// Clear all CSR bits
	csrrw x25, 0x428, x31	// Restore CSR

// Testing CSR 0x429
	csrr x7, 0x429	// Read CSR
	li x30, -1
	csrrw x12, 0x429, x30	// Write all 1s to CSR
	csrrw x12, 0x429, x0	// Write all 0s to CSR
	csrrs x12, 0x429, x30	// Set all CSR bits
	csrrc x12, 0x429, x30	// Clear all CSR bits
	csrrw x12, 0x429, x7	// Restore CSR

// Testing CSR 0x42a
	csrr x13, 0x42a	// Read CSR
	li x6, -1
	csrrw x29, 0x42a, x6	// Write all 1s to CSR
	csrrw x29, 0x42a, x0	// Write all 0s to CSR
	csrrs x29, 0x42a, x6	// Set all CSR bits
	csrrc x29, 0x42a, x6	// Clear all CSR bits
	csrrw x29, 0x42a, x13	// Restore CSR

// Testing CSR 0x42b
	csrr x9, 0x42b	// Read CSR
	li x31, -1
	csrrw x20, 0x42b, x31	// Write all 1s to CSR
	csrrw x20, 0x42b, x0	// Write all 0s to CSR
	csrrs x20, 0x42b, x31	// Set all CSR bits
	csrrc x20, 0x42b, x31	// Clear all CSR bits
	csrrw x20, 0x42b, x9	// Restore CSR

// Testing CSR 0x42c
	csrr x24, 0x42c	// Read CSR
	li x5, -1
	csrrw x2, 0x42c, x5	// Write all 1s to CSR
	csrrw x2, 0x42c, x0	// Write all 0s to CSR
	csrrs x2, 0x42c, x5	// Set all CSR bits
	csrrc x2, 0x42c, x5	// Clear all CSR bits
	csrrw x2, 0x42c, x24	// Restore CSR

// Testing CSR 0x42d
	csrr x28, 0x42d	// Read CSR
	li x12, -1
	csrrw x31, 0x42d, x12	// Write all 1s to CSR
	csrrw x31, 0x42d, x0	// Write all 0s to CSR
	csrrs x31, 0x42d, x12	// Set all CSR bits
	csrrc x31, 0x42d, x12	// Clear all CSR bits
	csrrw x31, 0x42d, x28	// Restore CSR

// Testing CSR 0x42e
	csrr x24, 0x42e	// Read CSR
	li x22, -1
	csrrw x1, 0x42e, x22	// Write all 1s to CSR
	csrrw x1, 0x42e, x0	// Write all 0s to CSR
	csrrs x1, 0x42e, x22	// Set all CSR bits
	csrrc x1, 0x42e, x22	// Clear all CSR bits
	csrrw x1, 0x42e, x24	// Restore CSR

// Testing CSR 0x42f
	csrr x9, 0x42f	// Read CSR
	li x16, -1
	csrrw x11, 0x42f, x16	// Write all 1s to CSR
	csrrw x11, 0x42f, x0	// Write all 0s to CSR
	csrrs x11, 0x42f, x16	// Set all CSR bits
	csrrc x11, 0x42f, x16	// Clear all CSR bits
	csrrw x11, 0x42f, x9	// Restore CSR

// Testing CSR 0x430
	csrr x27, 0x430	// Read CSR
	li x20, -1
	csrrw x25, 0x430, x20	// Write all 1s to CSR
	csrrw x25, 0x430, x0	// Write all 0s to CSR
	csrrs x25, 0x430, x20	// Set all CSR bits
	csrrc x25, 0x430, x20	// Clear all CSR bits
	csrrw x25, 0x430, x27	// Restore CSR

// Testing CSR 0x431
	csrr x29, 0x431	// Read CSR
	li x6, -1
	csrrw x23, 0x431, x6	// Write all 1s to CSR
	csrrw x23, 0x431, x0	// Write all 0s to CSR
	csrrs x23, 0x431, x6	// Set all CSR bits
	csrrc x23, 0x431, x6	// Clear all CSR bits
	csrrw x23, 0x431, x29	// Restore CSR

// Testing CSR 0x432
	csrr x30, 0x432	// Read CSR
	li x20, -1
	csrrw x7, 0x432, x20	// Write all 1s to CSR
	csrrw x7, 0x432, x0	// Write all 0s to CSR
	csrrs x7, 0x432, x20	// Set all CSR bits
	csrrc x7, 0x432, x20	// Clear all CSR bits
	csrrw x7, 0x432, x30	// Restore CSR

// Testing CSR 0x433
	csrr x28, 0x433	// Read CSR
	li x22, -1
	csrrw x26, 0x433, x22	// Write all 1s to CSR
	csrrw x26, 0x433, x0	// Write all 0s to CSR
	csrrs x26, 0x433, x22	// Set all CSR bits
	csrrc x26, 0x433, x22	// Clear all CSR bits
	csrrw x26, 0x433, x28	// Restore CSR

// Testing CSR 0x434
	csrr x3, 0x434	// Read CSR
	li x23, -1
	csrrw x2, 0x434, x23	// Write all 1s to CSR
	csrrw x2, 0x434, x0	// Write all 0s to CSR
	csrrs x2, 0x434, x23	// Set all CSR bits
	csrrc x2, 0x434, x23	// Clear all CSR bits
	csrrw x2, 0x434, x3	// Restore CSR

// Testing CSR 0x435
	csrr x7, 0x435	// Read CSR
	li x1, -1
	csrrw x16, 0x435, x1	// Write all 1s to CSR
	csrrw x16, 0x435, x0	// Write all 0s to CSR
	csrrs x16, 0x435, x1	// Set all CSR bits
	csrrc x16, 0x435, x1	// Clear all CSR bits
	csrrw x16, 0x435, x7	// Restore CSR

// Testing CSR 0x436
	csrr x20, 0x436	// Read CSR
	li x28, -1
	csrrw x12, 0x436, x28	// Write all 1s to CSR
	csrrw x12, 0x436, x0	// Write all 0s to CSR
	csrrs x12, 0x436, x28	// Set all CSR bits
	csrrc x12, 0x436, x28	// Clear all CSR bits
	csrrw x12, 0x436, x20	// Restore CSR

// Testing CSR 0x437
	csrr x13, 0x437	// Read CSR
	li x16, -1
	csrrw x3, 0x437, x16	// Write all 1s to CSR
	csrrw x3, 0x437, x0	// Write all 0s to CSR
	csrrs x3, 0x437, x16	// Set all CSR bits
	csrrc x3, 0x437, x16	// Clear all CSR bits
	csrrw x3, 0x437, x13	// Restore CSR

// Testing CSR 0x438
	csrr x28, 0x438	// Read CSR
	li x3, -1
	csrrw x4, 0x438, x3	// Write all 1s to CSR
	csrrw x4, 0x438, x0	// Write all 0s to CSR
	csrrs x4, 0x438, x3	// Set all CSR bits
	csrrc x4, 0x438, x3	// Clear all CSR bits
	csrrw x4, 0x438, x28	// Restore CSR

// Testing CSR 0x439
	csrr x23, 0x439	// Read CSR
	li x26, -1
	csrrw x24, 0x439, x26	// Write all 1s to CSR
	csrrw x24, 0x439, x0	// Write all 0s to CSR
	csrrs x24, 0x439, x26	// Set all CSR bits
	csrrc x24, 0x439, x26	// Clear all CSR bits
	csrrw x24, 0x439, x23	// Restore CSR

// Testing CSR 0x43a
	csrr x28, 0x43a	// Read CSR
	li x8, -1
	csrrw x4, 0x43a, x8	// Write all 1s to CSR
	csrrw x4, 0x43a, x0	// Write all 0s to CSR
	csrrs x4, 0x43a, x8	// Set all CSR bits
	csrrc x4, 0x43a, x8	// Clear all CSR bits
	csrrw x4, 0x43a, x28	// Restore CSR

// Testing CSR 0x43b
	csrr x8, 0x43b	// Read CSR
	li x14, -1
	csrrw x27, 0x43b, x14	// Write all 1s to CSR
	csrrw x27, 0x43b, x0	// Write all 0s to CSR
	csrrs x27, 0x43b, x14	// Set all CSR bits
	csrrc x27, 0x43b, x14	// Clear all CSR bits
	csrrw x27, 0x43b, x8	// Restore CSR

// Testing CSR 0x43c
	csrr x7, 0x43c	// Read CSR
	li x26, -1
	csrrw x28, 0x43c, x26	// Write all 1s to CSR
	csrrw x28, 0x43c, x0	// Write all 0s to CSR
	csrrs x28, 0x43c, x26	// Set all CSR bits
	csrrc x28, 0x43c, x26	// Clear all CSR bits
	csrrw x28, 0x43c, x7	// Restore CSR

// Testing CSR 0x43d
	csrr x7, 0x43d	// Read CSR
	li x24, -1
	csrrw x15, 0x43d, x24	// Write all 1s to CSR
	csrrw x15, 0x43d, x0	// Write all 0s to CSR
	csrrs x15, 0x43d, x24	// Set all CSR bits
	csrrc x15, 0x43d, x24	// Clear all CSR bits
	csrrw x15, 0x43d, x7	// Restore CSR

// Testing CSR 0x43e
	csrr x29, 0x43e	// Read CSR
	li x7, -1
	csrrw x1, 0x43e, x7	// Write all 1s to CSR
	csrrw x1, 0x43e, x0	// Write all 0s to CSR
	csrrs x1, 0x43e, x7	// Set all CSR bits
	csrrc x1, 0x43e, x7	// Clear all CSR bits
	csrrw x1, 0x43e, x29	// Restore CSR

// Testing CSR 0x43f
	csrr x8, 0x43f	// Read CSR
	li x21, -1
	csrrw x28, 0x43f, x21	// Write all 1s to CSR
	csrrw x28, 0x43f, x0	// Write all 0s to CSR
	csrrs x28, 0x43f, x21	// Set all CSR bits
	csrrc x28, 0x43f, x21	// Clear all CSR bits
	csrrw x28, 0x43f, x8	// Restore CSR

// Testing CSR 0x440
	csrr x6, 0x440	// Read CSR
	li x1, -1
	csrrw x29, 0x440, x1	// Write all 1s to CSR
	csrrw x29, 0x440, x0	// Write all 0s to CSR
	csrrs x29, 0x440, x1	// Set all CSR bits
	csrrc x29, 0x440, x1	// Clear all CSR bits
	csrrw x29, 0x440, x6	// Restore CSR

// Testing CSR 0x441
	csrr x31, 0x441	// Read CSR
	li x13, -1
	csrrw x6, 0x441, x13	// Write all 1s to CSR
	csrrw x6, 0x441, x0	// Write all 0s to CSR
	csrrs x6, 0x441, x13	// Set all CSR bits
	csrrc x6, 0x441, x13	// Clear all CSR bits
	csrrw x6, 0x441, x31	// Restore CSR

// Testing CSR 0x442
	csrr x2, 0x442	// Read CSR
	li x18, -1
	csrrw x12, 0x442, x18	// Write all 1s to CSR
	csrrw x12, 0x442, x0	// Write all 0s to CSR
	csrrs x12, 0x442, x18	// Set all CSR bits
	csrrc x12, 0x442, x18	// Clear all CSR bits
	csrrw x12, 0x442, x2	// Restore CSR

// Testing CSR 0x443
	csrr x14, 0x443	// Read CSR
	li x3, -1
	csrrw x12, 0x443, x3	// Write all 1s to CSR
	csrrw x12, 0x443, x0	// Write all 0s to CSR
	csrrs x12, 0x443, x3	// Set all CSR bits
	csrrc x12, 0x443, x3	// Clear all CSR bits
	csrrw x12, 0x443, x14	// Restore CSR

// Testing CSR 0x444
	csrr x14, 0x444	// Read CSR
	li x31, -1
	csrrw x29, 0x444, x31	// Write all 1s to CSR
	csrrw x29, 0x444, x0	// Write all 0s to CSR
	csrrs x29, 0x444, x31	// Set all CSR bits
	csrrc x29, 0x444, x31	// Clear all CSR bits
	csrrw x29, 0x444, x14	// Restore CSR

// Testing CSR 0x445
	csrr x22, 0x445	// Read CSR
	li x1, -1
	csrrw x14, 0x445, x1	// Write all 1s to CSR
	csrrw x14, 0x445, x0	// Write all 0s to CSR
	csrrs x14, 0x445, x1	// Set all CSR bits
	csrrc x14, 0x445, x1	// Clear all CSR bits
	csrrw x14, 0x445, x22	// Restore CSR

// Testing CSR 0x446
	csrr x14, 0x446	// Read CSR
	li x27, -1
	csrrw x12, 0x446, x27	// Write all 1s to CSR
	csrrw x12, 0x446, x0	// Write all 0s to CSR
	csrrs x12, 0x446, x27	// Set all CSR bits
	csrrc x12, 0x446, x27	// Clear all CSR bits
	csrrw x12, 0x446, x14	// Restore CSR

// Testing CSR 0x447
	csrr x11, 0x447	// Read CSR
	li x27, -1
	csrrw x24, 0x447, x27	// Write all 1s to CSR
	csrrw x24, 0x447, x0	// Write all 0s to CSR
	csrrs x24, 0x447, x27	// Set all CSR bits
	csrrc x24, 0x447, x27	// Clear all CSR bits
	csrrw x24, 0x447, x11	// Restore CSR

// Testing CSR 0x448
	csrr x28, 0x448	// Read CSR
	li x30, -1
	csrrw x14, 0x448, x30	// Write all 1s to CSR
	csrrw x14, 0x448, x0	// Write all 0s to CSR
	csrrs x14, 0x448, x30	// Set all CSR bits
	csrrc x14, 0x448, x30	// Clear all CSR bits
	csrrw x14, 0x448, x28	// Restore CSR

// Testing CSR 0x449
	csrr x12, 0x449	// Read CSR
	li x23, -1
	csrrw x14, 0x449, x23	// Write all 1s to CSR
	csrrw x14, 0x449, x0	// Write all 0s to CSR
	csrrs x14, 0x449, x23	// Set all CSR bits
	csrrc x14, 0x449, x23	// Clear all CSR bits
	csrrw x14, 0x449, x12	// Restore CSR

// Testing CSR 0x44a
	csrr x11, 0x44a	// Read CSR
	li x4, -1
	csrrw x28, 0x44a, x4	// Write all 1s to CSR
	csrrw x28, 0x44a, x0	// Write all 0s to CSR
	csrrs x28, 0x44a, x4	// Set all CSR bits
	csrrc x28, 0x44a, x4	// Clear all CSR bits
	csrrw x28, 0x44a, x11	// Restore CSR

// Testing CSR 0x44b
	csrr x22, 0x44b	// Read CSR
	li x17, -1
	csrrw x18, 0x44b, x17	// Write all 1s to CSR
	csrrw x18, 0x44b, x0	// Write all 0s to CSR
	csrrs x18, 0x44b, x17	// Set all CSR bits
	csrrc x18, 0x44b, x17	// Clear all CSR bits
	csrrw x18, 0x44b, x22	// Restore CSR

// Testing CSR 0x44c
	csrr x18, 0x44c	// Read CSR
	li x4, -1
	csrrw x20, 0x44c, x4	// Write all 1s to CSR
	csrrw x20, 0x44c, x0	// Write all 0s to CSR
	csrrs x20, 0x44c, x4	// Set all CSR bits
	csrrc x20, 0x44c, x4	// Clear all CSR bits
	csrrw x20, 0x44c, x18	// Restore CSR

// Testing CSR 0x44d
	csrr x24, 0x44d	// Read CSR
	li x18, -1
	csrrw x31, 0x44d, x18	// Write all 1s to CSR
	csrrw x31, 0x44d, x0	// Write all 0s to CSR
	csrrs x31, 0x44d, x18	// Set all CSR bits
	csrrc x31, 0x44d, x18	// Clear all CSR bits
	csrrw x31, 0x44d, x24	// Restore CSR

// Testing CSR 0x44e
	csrr x14, 0x44e	// Read CSR
	li x21, -1
	csrrw x3, 0x44e, x21	// Write all 1s to CSR
	csrrw x3, 0x44e, x0	// Write all 0s to CSR
	csrrs x3, 0x44e, x21	// Set all CSR bits
	csrrc x3, 0x44e, x21	// Clear all CSR bits
	csrrw x3, 0x44e, x14	// Restore CSR

// Testing CSR 0x44f
	csrr x8, 0x44f	// Read CSR
	li x4, -1
	csrrw x10, 0x44f, x4	// Write all 1s to CSR
	csrrw x10, 0x44f, x0	// Write all 0s to CSR
	csrrs x10, 0x44f, x4	// Set all CSR bits
	csrrc x10, 0x44f, x4	// Clear all CSR bits
	csrrw x10, 0x44f, x8	// Restore CSR

// Testing CSR 0x450
	csrr x3, 0x450	// Read CSR
	li x9, -1
	csrrw x7, 0x450, x9	// Write all 1s to CSR
	csrrw x7, 0x450, x0	// Write all 0s to CSR
	csrrs x7, 0x450, x9	// Set all CSR bits
	csrrc x7, 0x450, x9	// Clear all CSR bits
	csrrw x7, 0x450, x3	// Restore CSR

// Testing CSR 0x451
	csrr x15, 0x451	// Read CSR
	li x19, -1
	csrrw x5, 0x451, x19	// Write all 1s to CSR
	csrrw x5, 0x451, x0	// Write all 0s to CSR
	csrrs x5, 0x451, x19	// Set all CSR bits
	csrrc x5, 0x451, x19	// Clear all CSR bits
	csrrw x5, 0x451, x15	// Restore CSR

// Testing CSR 0x452
	csrr x9, 0x452	// Read CSR
	li x31, -1
	csrrw x1, 0x452, x31	// Write all 1s to CSR
	csrrw x1, 0x452, x0	// Write all 0s to CSR
	csrrs x1, 0x452, x31	// Set all CSR bits
	csrrc x1, 0x452, x31	// Clear all CSR bits
	csrrw x1, 0x452, x9	// Restore CSR

// Testing CSR 0x453
	csrr x19, 0x453	// Read CSR
	li x26, -1
	csrrw x16, 0x453, x26	// Write all 1s to CSR
	csrrw x16, 0x453, x0	// Write all 0s to CSR
	csrrs x16, 0x453, x26	// Set all CSR bits
	csrrc x16, 0x453, x26	// Clear all CSR bits
	csrrw x16, 0x453, x19	// Restore CSR

// Testing CSR 0x454
	csrr x26, 0x454	// Read CSR
	li x10, -1
	csrrw x21, 0x454, x10	// Write all 1s to CSR
	csrrw x21, 0x454, x0	// Write all 0s to CSR
	csrrs x21, 0x454, x10	// Set all CSR bits
	csrrc x21, 0x454, x10	// Clear all CSR bits
	csrrw x21, 0x454, x26	// Restore CSR

// Testing CSR 0x455
	csrr x18, 0x455	// Read CSR
	li x28, -1
	csrrw x24, 0x455, x28	// Write all 1s to CSR
	csrrw x24, 0x455, x0	// Write all 0s to CSR
	csrrs x24, 0x455, x28	// Set all CSR bits
	csrrc x24, 0x455, x28	// Clear all CSR bits
	csrrw x24, 0x455, x18	// Restore CSR

// Testing CSR 0x456
	csrr x25, 0x456	// Read CSR
	li x4, -1
	csrrw x22, 0x456, x4	// Write all 1s to CSR
	csrrw x22, 0x456, x0	// Write all 0s to CSR
	csrrs x22, 0x456, x4	// Set all CSR bits
	csrrc x22, 0x456, x4	// Clear all CSR bits
	csrrw x22, 0x456, x25	// Restore CSR

// Testing CSR 0x457
	csrr x10, 0x457	// Read CSR
	li x3, -1
	csrrw x7, 0x457, x3	// Write all 1s to CSR
	csrrw x7, 0x457, x0	// Write all 0s to CSR
	csrrs x7, 0x457, x3	// Set all CSR bits
	csrrc x7, 0x457, x3	// Clear all CSR bits
	csrrw x7, 0x457, x10	// Restore CSR

// Testing CSR 0x458
	csrr x9, 0x458	// Read CSR
	li x8, -1
	csrrw x24, 0x458, x8	// Write all 1s to CSR
	csrrw x24, 0x458, x0	// Write all 0s to CSR
	csrrs x24, 0x458, x8	// Set all CSR bits
	csrrc x24, 0x458, x8	// Clear all CSR bits
	csrrw x24, 0x458, x9	// Restore CSR

// Testing CSR 0x459
	csrr x11, 0x459	// Read CSR
	li x19, -1
	csrrw x21, 0x459, x19	// Write all 1s to CSR
	csrrw x21, 0x459, x0	// Write all 0s to CSR
	csrrs x21, 0x459, x19	// Set all CSR bits
	csrrc x21, 0x459, x19	// Clear all CSR bits
	csrrw x21, 0x459, x11	// Restore CSR

// Testing CSR 0x45a
	csrr x10, 0x45a	// Read CSR
	li x22, -1
	csrrw x25, 0x45a, x22	// Write all 1s to CSR
	csrrw x25, 0x45a, x0	// Write all 0s to CSR
	csrrs x25, 0x45a, x22	// Set all CSR bits
	csrrc x25, 0x45a, x22	// Clear all CSR bits
	csrrw x25, 0x45a, x10	// Restore CSR

// Testing CSR 0x45b
	csrr x7, 0x45b	// Read CSR
	li x29, -1
	csrrw x28, 0x45b, x29	// Write all 1s to CSR
	csrrw x28, 0x45b, x0	// Write all 0s to CSR
	csrrs x28, 0x45b, x29	// Set all CSR bits
	csrrc x28, 0x45b, x29	// Clear all CSR bits
	csrrw x28, 0x45b, x7	// Restore CSR

// Testing CSR 0x45c
	csrr x19, 0x45c	// Read CSR
	li x23, -1
	csrrw x26, 0x45c, x23	// Write all 1s to CSR
	csrrw x26, 0x45c, x0	// Write all 0s to CSR
	csrrs x26, 0x45c, x23	// Set all CSR bits
	csrrc x26, 0x45c, x23	// Clear all CSR bits
	csrrw x26, 0x45c, x19	// Restore CSR

// Testing CSR 0x45d
	csrr x4, 0x45d	// Read CSR
	li x17, -1
	csrrw x27, 0x45d, x17	// Write all 1s to CSR
	csrrw x27, 0x45d, x0	// Write all 0s to CSR
	csrrs x27, 0x45d, x17	// Set all CSR bits
	csrrc x27, 0x45d, x17	// Clear all CSR bits
	csrrw x27, 0x45d, x4	// Restore CSR

// Testing CSR 0x45e
	csrr x21, 0x45e	// Read CSR
	li x29, -1
	csrrw x18, 0x45e, x29	// Write all 1s to CSR
	csrrw x18, 0x45e, x0	// Write all 0s to CSR
	csrrs x18, 0x45e, x29	// Set all CSR bits
	csrrc x18, 0x45e, x29	// Clear all CSR bits
	csrrw x18, 0x45e, x21	// Restore CSR

// Testing CSR 0x45f
	csrr x15, 0x45f	// Read CSR
	li x16, -1
	csrrw x12, 0x45f, x16	// Write all 1s to CSR
	csrrw x12, 0x45f, x0	// Write all 0s to CSR
	csrrs x12, 0x45f, x16	// Set all CSR bits
	csrrc x12, 0x45f, x16	// Clear all CSR bits
	csrrw x12, 0x45f, x15	// Restore CSR

// Testing CSR 0x460
	csrr x5, 0x460	// Read CSR
	li x27, -1
	csrrw x14, 0x460, x27	// Write all 1s to CSR
	csrrw x14, 0x460, x0	// Write all 0s to CSR
	csrrs x14, 0x460, x27	// Set all CSR bits
	csrrc x14, 0x460, x27	// Clear all CSR bits
	csrrw x14, 0x460, x5	// Restore CSR

// Testing CSR 0x461
	csrr x4, 0x461	// Read CSR
	li x13, -1
	csrrw x14, 0x461, x13	// Write all 1s to CSR
	csrrw x14, 0x461, x0	// Write all 0s to CSR
	csrrs x14, 0x461, x13	// Set all CSR bits
	csrrc x14, 0x461, x13	// Clear all CSR bits
	csrrw x14, 0x461, x4	// Restore CSR

// Testing CSR 0x462
	csrr x22, 0x462	// Read CSR
	li x4, -1
	csrrw x2, 0x462, x4	// Write all 1s to CSR
	csrrw x2, 0x462, x0	// Write all 0s to CSR
	csrrs x2, 0x462, x4	// Set all CSR bits
	csrrc x2, 0x462, x4	// Clear all CSR bits
	csrrw x2, 0x462, x22	// Restore CSR

// Testing CSR 0x463
	csrr x25, 0x463	// Read CSR
	li x8, -1
	csrrw x29, 0x463, x8	// Write all 1s to CSR
	csrrw x29, 0x463, x0	// Write all 0s to CSR
	csrrs x29, 0x463, x8	// Set all CSR bits
	csrrc x29, 0x463, x8	// Clear all CSR bits
	csrrw x29, 0x463, x25	// Restore CSR

// Testing CSR 0x464
	csrr x15, 0x464	// Read CSR
	li x11, -1
	csrrw x31, 0x464, x11	// Write all 1s to CSR
	csrrw x31, 0x464, x0	// Write all 0s to CSR
	csrrs x31, 0x464, x11	// Set all CSR bits
	csrrc x31, 0x464, x11	// Clear all CSR bits
	csrrw x31, 0x464, x15	// Restore CSR

// Testing CSR 0x465
	csrr x27, 0x465	// Read CSR
	li x18, -1
	csrrw x10, 0x465, x18	// Write all 1s to CSR
	csrrw x10, 0x465, x0	// Write all 0s to CSR
	csrrs x10, 0x465, x18	// Set all CSR bits
	csrrc x10, 0x465, x18	// Clear all CSR bits
	csrrw x10, 0x465, x27	// Restore CSR

// Testing CSR 0x466
	csrr x13, 0x466	// Read CSR
	li x30, -1
	csrrw x24, 0x466, x30	// Write all 1s to CSR
	csrrw x24, 0x466, x0	// Write all 0s to CSR
	csrrs x24, 0x466, x30	// Set all CSR bits
	csrrc x24, 0x466, x30	// Clear all CSR bits
	csrrw x24, 0x466, x13	// Restore CSR

// Testing CSR 0x467
	csrr x23, 0x467	// Read CSR
	li x16, -1
	csrrw x28, 0x467, x16	// Write all 1s to CSR
	csrrw x28, 0x467, x0	// Write all 0s to CSR
	csrrs x28, 0x467, x16	// Set all CSR bits
	csrrc x28, 0x467, x16	// Clear all CSR bits
	csrrw x28, 0x467, x23	// Restore CSR

// Testing CSR 0x468
	csrr x1, 0x468	// Read CSR
	li x24, -1
	csrrw x27, 0x468, x24	// Write all 1s to CSR
	csrrw x27, 0x468, x0	// Write all 0s to CSR
	csrrs x27, 0x468, x24	// Set all CSR bits
	csrrc x27, 0x468, x24	// Clear all CSR bits
	csrrw x27, 0x468, x1	// Restore CSR

// Testing CSR 0x469
	csrr x19, 0x469	// Read CSR
	li x2, -1
	csrrw x8, 0x469, x2	// Write all 1s to CSR
	csrrw x8, 0x469, x0	// Write all 0s to CSR
	csrrs x8, 0x469, x2	// Set all CSR bits
	csrrc x8, 0x469, x2	// Clear all CSR bits
	csrrw x8, 0x469, x19	// Restore CSR

// Testing CSR 0x46a
	csrr x12, 0x46a	// Read CSR
	li x11, -1
	csrrw x8, 0x46a, x11	// Write all 1s to CSR
	csrrw x8, 0x46a, x0	// Write all 0s to CSR
	csrrs x8, 0x46a, x11	// Set all CSR bits
	csrrc x8, 0x46a, x11	// Clear all CSR bits
	csrrw x8, 0x46a, x12	// Restore CSR

// Testing CSR 0x46b
	csrr x22, 0x46b	// Read CSR
	li x5, -1
	csrrw x29, 0x46b, x5	// Write all 1s to CSR
	csrrw x29, 0x46b, x0	// Write all 0s to CSR
	csrrs x29, 0x46b, x5	// Set all CSR bits
	csrrc x29, 0x46b, x5	// Clear all CSR bits
	csrrw x29, 0x46b, x22	// Restore CSR

// Testing CSR 0x46c
	csrr x15, 0x46c	// Read CSR
	li x16, -1
	csrrw x24, 0x46c, x16	// Write all 1s to CSR
	csrrw x24, 0x46c, x0	// Write all 0s to CSR
	csrrs x24, 0x46c, x16	// Set all CSR bits
	csrrc x24, 0x46c, x16	// Clear all CSR bits
	csrrw x24, 0x46c, x15	// Restore CSR

// Testing CSR 0x46d
	csrr x1, 0x46d	// Read CSR
	li x25, -1
	csrrw x18, 0x46d, x25	// Write all 1s to CSR
	csrrw x18, 0x46d, x0	// Write all 0s to CSR
	csrrs x18, 0x46d, x25	// Set all CSR bits
	csrrc x18, 0x46d, x25	// Clear all CSR bits
	csrrw x18, 0x46d, x1	// Restore CSR

// Testing CSR 0x46e
	csrr x31, 0x46e	// Read CSR
	li x26, -1
	csrrw x9, 0x46e, x26	// Write all 1s to CSR
	csrrw x9, 0x46e, x0	// Write all 0s to CSR
	csrrs x9, 0x46e, x26	// Set all CSR bits
	csrrc x9, 0x46e, x26	// Clear all CSR bits
	csrrw x9, 0x46e, x31	// Restore CSR

// Testing CSR 0x46f
	csrr x27, 0x46f	// Read CSR
	li x2, -1
	csrrw x1, 0x46f, x2	// Write all 1s to CSR
	csrrw x1, 0x46f, x0	// Write all 0s to CSR
	csrrs x1, 0x46f, x2	// Set all CSR bits
	csrrc x1, 0x46f, x2	// Clear all CSR bits
	csrrw x1, 0x46f, x27	// Restore CSR

// Testing CSR 0x470
	csrr x27, 0x470	// Read CSR
	li x10, -1
	csrrw x18, 0x470, x10	// Write all 1s to CSR
	csrrw x18, 0x470, x0	// Write all 0s to CSR
	csrrs x18, 0x470, x10	// Set all CSR bits
	csrrc x18, 0x470, x10	// Clear all CSR bits
	csrrw x18, 0x470, x27	// Restore CSR

// Testing CSR 0x471
	csrr x5, 0x471	// Read CSR
	li x27, -1
	csrrw x31, 0x471, x27	// Write all 1s to CSR
	csrrw x31, 0x471, x0	// Write all 0s to CSR
	csrrs x31, 0x471, x27	// Set all CSR bits
	csrrc x31, 0x471, x27	// Clear all CSR bits
	csrrw x31, 0x471, x5	// Restore CSR

// Testing CSR 0x472
	csrr x3, 0x472	// Read CSR
	li x14, -1
	csrrw x21, 0x472, x14	// Write all 1s to CSR
	csrrw x21, 0x472, x0	// Write all 0s to CSR
	csrrs x21, 0x472, x14	// Set all CSR bits
	csrrc x21, 0x472, x14	// Clear all CSR bits
	csrrw x21, 0x472, x3	// Restore CSR

// Testing CSR 0x473
	csrr x18, 0x473	// Read CSR
	li x27, -1
	csrrw x14, 0x473, x27	// Write all 1s to CSR
	csrrw x14, 0x473, x0	// Write all 0s to CSR
	csrrs x14, 0x473, x27	// Set all CSR bits
	csrrc x14, 0x473, x27	// Clear all CSR bits
	csrrw x14, 0x473, x18	// Restore CSR

// Testing CSR 0x474
	csrr x11, 0x474	// Read CSR
	li x13, -1
	csrrw x7, 0x474, x13	// Write all 1s to CSR
	csrrw x7, 0x474, x0	// Write all 0s to CSR
	csrrs x7, 0x474, x13	// Set all CSR bits
	csrrc x7, 0x474, x13	// Clear all CSR bits
	csrrw x7, 0x474, x11	// Restore CSR

// Testing CSR 0x475
	csrr x8, 0x475	// Read CSR
	li x9, -1
	csrrw x3, 0x475, x9	// Write all 1s to CSR
	csrrw x3, 0x475, x0	// Write all 0s to CSR
	csrrs x3, 0x475, x9	// Set all CSR bits
	csrrc x3, 0x475, x9	// Clear all CSR bits
	csrrw x3, 0x475, x8	// Restore CSR

// Testing CSR 0x476
	csrr x21, 0x476	// Read CSR
	li x28, -1
	csrrw x9, 0x476, x28	// Write all 1s to CSR
	csrrw x9, 0x476, x0	// Write all 0s to CSR
	csrrs x9, 0x476, x28	// Set all CSR bits
	csrrc x9, 0x476, x28	// Clear all CSR bits
	csrrw x9, 0x476, x21	// Restore CSR

// Testing CSR 0x477
	csrr x14, 0x477	// Read CSR
	li x25, -1
	csrrw x6, 0x477, x25	// Write all 1s to CSR
	csrrw x6, 0x477, x0	// Write all 0s to CSR
	csrrs x6, 0x477, x25	// Set all CSR bits
	csrrc x6, 0x477, x25	// Clear all CSR bits
	csrrw x6, 0x477, x14	// Restore CSR

// Testing CSR 0x478
	csrr x21, 0x478	// Read CSR
	li x5, -1
	csrrw x6, 0x478, x5	// Write all 1s to CSR
	csrrw x6, 0x478, x0	// Write all 0s to CSR
	csrrs x6, 0x478, x5	// Set all CSR bits
	csrrc x6, 0x478, x5	// Clear all CSR bits
	csrrw x6, 0x478, x21	// Restore CSR

// Testing CSR 0x479
	csrr x29, 0x479	// Read CSR
	li x19, -1
	csrrw x25, 0x479, x19	// Write all 1s to CSR
	csrrw x25, 0x479, x0	// Write all 0s to CSR
	csrrs x25, 0x479, x19	// Set all CSR bits
	csrrc x25, 0x479, x19	// Clear all CSR bits
	csrrw x25, 0x479, x29	// Restore CSR

// Testing CSR 0x47a
	csrr x6, 0x47a	// Read CSR
	li x9, -1
	csrrw x27, 0x47a, x9	// Write all 1s to CSR
	csrrw x27, 0x47a, x0	// Write all 0s to CSR
	csrrs x27, 0x47a, x9	// Set all CSR bits
	csrrc x27, 0x47a, x9	// Clear all CSR bits
	csrrw x27, 0x47a, x6	// Restore CSR

// Testing CSR 0x47b
	csrr x22, 0x47b	// Read CSR
	li x9, -1
	csrrw x24, 0x47b, x9	// Write all 1s to CSR
	csrrw x24, 0x47b, x0	// Write all 0s to CSR
	csrrs x24, 0x47b, x9	// Set all CSR bits
	csrrc x24, 0x47b, x9	// Clear all CSR bits
	csrrw x24, 0x47b, x22	// Restore CSR

// Testing CSR 0x47c
	csrr x11, 0x47c	// Read CSR
	li x25, -1
	csrrw x13, 0x47c, x25	// Write all 1s to CSR
	csrrw x13, 0x47c, x0	// Write all 0s to CSR
	csrrs x13, 0x47c, x25	// Set all CSR bits
	csrrc x13, 0x47c, x25	// Clear all CSR bits
	csrrw x13, 0x47c, x11	// Restore CSR

// Testing CSR 0x47d
	csrr x30, 0x47d	// Read CSR
	li x10, -1
	csrrw x6, 0x47d, x10	// Write all 1s to CSR
	csrrw x6, 0x47d, x0	// Write all 0s to CSR
	csrrs x6, 0x47d, x10	// Set all CSR bits
	csrrc x6, 0x47d, x10	// Clear all CSR bits
	csrrw x6, 0x47d, x30	// Restore CSR

// Testing CSR 0x47e
	csrr x4, 0x47e	// Read CSR
	li x11, -1
	csrrw x15, 0x47e, x11	// Write all 1s to CSR
	csrrw x15, 0x47e, x0	// Write all 0s to CSR
	csrrs x15, 0x47e, x11	// Set all CSR bits
	csrrc x15, 0x47e, x11	// Clear all CSR bits
	csrrw x15, 0x47e, x4	// Restore CSR

// Testing CSR 0x47f
	csrr x7, 0x47f	// Read CSR
	li x5, -1
	csrrw x15, 0x47f, x5	// Write all 1s to CSR
	csrrw x15, 0x47f, x0	// Write all 0s to CSR
	csrrs x15, 0x47f, x5	// Set all CSR bits
	csrrc x15, 0x47f, x5	// Clear all CSR bits
	csrrw x15, 0x47f, x7	// Restore CSR

// Testing CSR 0x480
	csrr x16, 0x480	// Read CSR
	li x26, -1
	csrrw x18, 0x480, x26	// Write all 1s to CSR
	csrrw x18, 0x480, x0	// Write all 0s to CSR
	csrrs x18, 0x480, x26	// Set all CSR bits
	csrrc x18, 0x480, x26	// Clear all CSR bits
	csrrw x18, 0x480, x16	// Restore CSR

// Testing CSR 0x481
	csrr x29, 0x481	// Read CSR
	li x21, -1
	csrrw x8, 0x481, x21	// Write all 1s to CSR
	csrrw x8, 0x481, x0	// Write all 0s to CSR
	csrrs x8, 0x481, x21	// Set all CSR bits
	csrrc x8, 0x481, x21	// Clear all CSR bits
	csrrw x8, 0x481, x29	// Restore CSR

// Testing CSR 0x482
	csrr x10, 0x482	// Read CSR
	li x3, -1
	csrrw x22, 0x482, x3	// Write all 1s to CSR
	csrrw x22, 0x482, x0	// Write all 0s to CSR
	csrrs x22, 0x482, x3	// Set all CSR bits
	csrrc x22, 0x482, x3	// Clear all CSR bits
	csrrw x22, 0x482, x10	// Restore CSR

// Testing CSR 0x483
	csrr x2, 0x483	// Read CSR
	li x11, -1
	csrrw x29, 0x483, x11	// Write all 1s to CSR
	csrrw x29, 0x483, x0	// Write all 0s to CSR
	csrrs x29, 0x483, x11	// Set all CSR bits
	csrrc x29, 0x483, x11	// Clear all CSR bits
	csrrw x29, 0x483, x2	// Restore CSR

// Testing CSR 0x484
	csrr x23, 0x484	// Read CSR
	li x11, -1
	csrrw x31, 0x484, x11	// Write all 1s to CSR
	csrrw x31, 0x484, x0	// Write all 0s to CSR
	csrrs x31, 0x484, x11	// Set all CSR bits
	csrrc x31, 0x484, x11	// Clear all CSR bits
	csrrw x31, 0x484, x23	// Restore CSR

// Testing CSR 0x485
	csrr x20, 0x485	// Read CSR
	li x8, -1
	csrrw x31, 0x485, x8	// Write all 1s to CSR
	csrrw x31, 0x485, x0	// Write all 0s to CSR
	csrrs x31, 0x485, x8	// Set all CSR bits
	csrrc x31, 0x485, x8	// Clear all CSR bits
	csrrw x31, 0x485, x20	// Restore CSR

// Testing CSR 0x486
	csrr x1, 0x486	// Read CSR
	li x6, -1
	csrrw x23, 0x486, x6	// Write all 1s to CSR
	csrrw x23, 0x486, x0	// Write all 0s to CSR
	csrrs x23, 0x486, x6	// Set all CSR bits
	csrrc x23, 0x486, x6	// Clear all CSR bits
	csrrw x23, 0x486, x1	// Restore CSR

// Testing CSR 0x487
	csrr x31, 0x487	// Read CSR
	li x16, -1
	csrrw x24, 0x487, x16	// Write all 1s to CSR
	csrrw x24, 0x487, x0	// Write all 0s to CSR
	csrrs x24, 0x487, x16	// Set all CSR bits
	csrrc x24, 0x487, x16	// Clear all CSR bits
	csrrw x24, 0x487, x31	// Restore CSR

// Testing CSR 0x488
	csrr x21, 0x488	// Read CSR
	li x11, -1
	csrrw x29, 0x488, x11	// Write all 1s to CSR
	csrrw x29, 0x488, x0	// Write all 0s to CSR
	csrrs x29, 0x488, x11	// Set all CSR bits
	csrrc x29, 0x488, x11	// Clear all CSR bits
	csrrw x29, 0x488, x21	// Restore CSR

// Testing CSR 0x489
	csrr x22, 0x489	// Read CSR
	li x18, -1
	csrrw x15, 0x489, x18	// Write all 1s to CSR
	csrrw x15, 0x489, x0	// Write all 0s to CSR
	csrrs x15, 0x489, x18	// Set all CSR bits
	csrrc x15, 0x489, x18	// Clear all CSR bits
	csrrw x15, 0x489, x22	// Restore CSR

// Testing CSR 0x48a
	csrr x8, 0x48a	// Read CSR
	li x12, -1
	csrrw x3, 0x48a, x12	// Write all 1s to CSR
	csrrw x3, 0x48a, x0	// Write all 0s to CSR
	csrrs x3, 0x48a, x12	// Set all CSR bits
	csrrc x3, 0x48a, x12	// Clear all CSR bits
	csrrw x3, 0x48a, x8	// Restore CSR

// Testing CSR 0x48b
	csrr x10, 0x48b	// Read CSR
	li x4, -1
	csrrw x31, 0x48b, x4	// Write all 1s to CSR
	csrrw x31, 0x48b, x0	// Write all 0s to CSR
	csrrs x31, 0x48b, x4	// Set all CSR bits
	csrrc x31, 0x48b, x4	// Clear all CSR bits
	csrrw x31, 0x48b, x10	// Restore CSR

// Testing CSR 0x48c
	csrr x28, 0x48c	// Read CSR
	li x31, -1
	csrrw x20, 0x48c, x31	// Write all 1s to CSR
	csrrw x20, 0x48c, x0	// Write all 0s to CSR
	csrrs x20, 0x48c, x31	// Set all CSR bits
	csrrc x20, 0x48c, x31	// Clear all CSR bits
	csrrw x20, 0x48c, x28	// Restore CSR

// Testing CSR 0x48d
	csrr x10, 0x48d	// Read CSR
	li x4, -1
	csrrw x5, 0x48d, x4	// Write all 1s to CSR
	csrrw x5, 0x48d, x0	// Write all 0s to CSR
	csrrs x5, 0x48d, x4	// Set all CSR bits
	csrrc x5, 0x48d, x4	// Clear all CSR bits
	csrrw x5, 0x48d, x10	// Restore CSR

// Testing CSR 0x48e
	csrr x27, 0x48e	// Read CSR
	li x17, -1
	csrrw x20, 0x48e, x17	// Write all 1s to CSR
	csrrw x20, 0x48e, x0	// Write all 0s to CSR
	csrrs x20, 0x48e, x17	// Set all CSR bits
	csrrc x20, 0x48e, x17	// Clear all CSR bits
	csrrw x20, 0x48e, x27	// Restore CSR

// Testing CSR 0x48f
	csrr x24, 0x48f	// Read CSR
	li x19, -1
	csrrw x1, 0x48f, x19	// Write all 1s to CSR
	csrrw x1, 0x48f, x0	// Write all 0s to CSR
	csrrs x1, 0x48f, x19	// Set all CSR bits
	csrrc x1, 0x48f, x19	// Clear all CSR bits
	csrrw x1, 0x48f, x24	// Restore CSR

// Testing CSR 0x490
	csrr x16, 0x490	// Read CSR
	li x6, -1
	csrrw x15, 0x490, x6	// Write all 1s to CSR
	csrrw x15, 0x490, x0	// Write all 0s to CSR
	csrrs x15, 0x490, x6	// Set all CSR bits
	csrrc x15, 0x490, x6	// Clear all CSR bits
	csrrw x15, 0x490, x16	// Restore CSR

// Testing CSR 0x491
	csrr x8, 0x491	// Read CSR
	li x6, -1
	csrrw x21, 0x491, x6	// Write all 1s to CSR
	csrrw x21, 0x491, x0	// Write all 0s to CSR
	csrrs x21, 0x491, x6	// Set all CSR bits
	csrrc x21, 0x491, x6	// Clear all CSR bits
	csrrw x21, 0x491, x8	// Restore CSR

// Testing CSR 0x492
	csrr x20, 0x492	// Read CSR
	li x31, -1
	csrrw x24, 0x492, x31	// Write all 1s to CSR
	csrrw x24, 0x492, x0	// Write all 0s to CSR
	csrrs x24, 0x492, x31	// Set all CSR bits
	csrrc x24, 0x492, x31	// Clear all CSR bits
	csrrw x24, 0x492, x20	// Restore CSR

// Testing CSR 0x493
	csrr x27, 0x493	// Read CSR
	li x24, -1
	csrrw x8, 0x493, x24	// Write all 1s to CSR
	csrrw x8, 0x493, x0	// Write all 0s to CSR
	csrrs x8, 0x493, x24	// Set all CSR bits
	csrrc x8, 0x493, x24	// Clear all CSR bits
	csrrw x8, 0x493, x27	// Restore CSR

// Testing CSR 0x494
	csrr x30, 0x494	// Read CSR
	li x26, -1
	csrrw x31, 0x494, x26	// Write all 1s to CSR
	csrrw x31, 0x494, x0	// Write all 0s to CSR
	csrrs x31, 0x494, x26	// Set all CSR bits
	csrrc x31, 0x494, x26	// Clear all CSR bits
	csrrw x31, 0x494, x30	// Restore CSR

// Testing CSR 0x495
	csrr x28, 0x495	// Read CSR
	li x20, -1
	csrrw x6, 0x495, x20	// Write all 1s to CSR
	csrrw x6, 0x495, x0	// Write all 0s to CSR
	csrrs x6, 0x495, x20	// Set all CSR bits
	csrrc x6, 0x495, x20	// Clear all CSR bits
	csrrw x6, 0x495, x28	// Restore CSR

// Testing CSR 0x496
	csrr x13, 0x496	// Read CSR
	li x1, -1
	csrrw x19, 0x496, x1	// Write all 1s to CSR
	csrrw x19, 0x496, x0	// Write all 0s to CSR
	csrrs x19, 0x496, x1	// Set all CSR bits
	csrrc x19, 0x496, x1	// Clear all CSR bits
	csrrw x19, 0x496, x13	// Restore CSR

// Testing CSR 0x497
	csrr x25, 0x497	// Read CSR
	li x30, -1
	csrrw x29, 0x497, x30	// Write all 1s to CSR
	csrrw x29, 0x497, x0	// Write all 0s to CSR
	csrrs x29, 0x497, x30	// Set all CSR bits
	csrrc x29, 0x497, x30	// Clear all CSR bits
	csrrw x29, 0x497, x25	// Restore CSR

// Testing CSR 0x498
	csrr x21, 0x498	// Read CSR
	li x5, -1
	csrrw x30, 0x498, x5	// Write all 1s to CSR
	csrrw x30, 0x498, x0	// Write all 0s to CSR
	csrrs x30, 0x498, x5	// Set all CSR bits
	csrrc x30, 0x498, x5	// Clear all CSR bits
	csrrw x30, 0x498, x21	// Restore CSR

// Testing CSR 0x499
	csrr x22, 0x499	// Read CSR
	li x23, -1
	csrrw x29, 0x499, x23	// Write all 1s to CSR
	csrrw x29, 0x499, x0	// Write all 0s to CSR
	csrrs x29, 0x499, x23	// Set all CSR bits
	csrrc x29, 0x499, x23	// Clear all CSR bits
	csrrw x29, 0x499, x22	// Restore CSR

// Testing CSR 0x49a
	csrr x25, 0x49a	// Read CSR
	li x8, -1
	csrrw x15, 0x49a, x8	// Write all 1s to CSR
	csrrw x15, 0x49a, x0	// Write all 0s to CSR
	csrrs x15, 0x49a, x8	// Set all CSR bits
	csrrc x15, 0x49a, x8	// Clear all CSR bits
	csrrw x15, 0x49a, x25	// Restore CSR

// Testing CSR 0x49b
	csrr x13, 0x49b	// Read CSR
	li x28, -1
	csrrw x12, 0x49b, x28	// Write all 1s to CSR
	csrrw x12, 0x49b, x0	// Write all 0s to CSR
	csrrs x12, 0x49b, x28	// Set all CSR bits
	csrrc x12, 0x49b, x28	// Clear all CSR bits
	csrrw x12, 0x49b, x13	// Restore CSR

// Testing CSR 0x49c
	csrr x26, 0x49c	// Read CSR
	li x25, -1
	csrrw x29, 0x49c, x25	// Write all 1s to CSR
	csrrw x29, 0x49c, x0	// Write all 0s to CSR
	csrrs x29, 0x49c, x25	// Set all CSR bits
	csrrc x29, 0x49c, x25	// Clear all CSR bits
	csrrw x29, 0x49c, x26	// Restore CSR

// Testing CSR 0x49d
	csrr x20, 0x49d	// Read CSR
	li x22, -1
	csrrw x16, 0x49d, x22	// Write all 1s to CSR
	csrrw x16, 0x49d, x0	// Write all 0s to CSR
	csrrs x16, 0x49d, x22	// Set all CSR bits
	csrrc x16, 0x49d, x22	// Clear all CSR bits
	csrrw x16, 0x49d, x20	// Restore CSR

// Testing CSR 0x49e
	csrr x3, 0x49e	// Read CSR
	li x17, -1
	csrrw x19, 0x49e, x17	// Write all 1s to CSR
	csrrw x19, 0x49e, x0	// Write all 0s to CSR
	csrrs x19, 0x49e, x17	// Set all CSR bits
	csrrc x19, 0x49e, x17	// Clear all CSR bits
	csrrw x19, 0x49e, x3	// Restore CSR

// Testing CSR 0x49f
	csrr x17, 0x49f	// Read CSR
	li x11, -1
	csrrw x4, 0x49f, x11	// Write all 1s to CSR
	csrrw x4, 0x49f, x0	// Write all 0s to CSR
	csrrs x4, 0x49f, x11	// Set all CSR bits
	csrrc x4, 0x49f, x11	// Clear all CSR bits
	csrrw x4, 0x49f, x17	// Restore CSR

// Testing CSR 0x4a0
	csrr x7, 0x4a0	// Read CSR
	li x15, -1
	csrrw x4, 0x4a0, x15	// Write all 1s to CSR
	csrrw x4, 0x4a0, x0	// Write all 0s to CSR
	csrrs x4, 0x4a0, x15	// Set all CSR bits
	csrrc x4, 0x4a0, x15	// Clear all CSR bits
	csrrw x4, 0x4a0, x7	// Restore CSR

// Testing CSR 0x4a1
	csrr x1, 0x4a1	// Read CSR
	li x24, -1
	csrrw x15, 0x4a1, x24	// Write all 1s to CSR
	csrrw x15, 0x4a1, x0	// Write all 0s to CSR
	csrrs x15, 0x4a1, x24	// Set all CSR bits
	csrrc x15, 0x4a1, x24	// Clear all CSR bits
	csrrw x15, 0x4a1, x1	// Restore CSR

// Testing CSR 0x4a2
	csrr x1, 0x4a2	// Read CSR
	li x5, -1
	csrrw x16, 0x4a2, x5	// Write all 1s to CSR
	csrrw x16, 0x4a2, x0	// Write all 0s to CSR
	csrrs x16, 0x4a2, x5	// Set all CSR bits
	csrrc x16, 0x4a2, x5	// Clear all CSR bits
	csrrw x16, 0x4a2, x1	// Restore CSR

// Testing CSR 0x4a3
	csrr x7, 0x4a3	// Read CSR
	li x3, -1
	csrrw x5, 0x4a3, x3	// Write all 1s to CSR
	csrrw x5, 0x4a3, x0	// Write all 0s to CSR
	csrrs x5, 0x4a3, x3	// Set all CSR bits
	csrrc x5, 0x4a3, x3	// Clear all CSR bits
	csrrw x5, 0x4a3, x7	// Restore CSR

// Testing CSR 0x4a4
	csrr x14, 0x4a4	// Read CSR
	li x26, -1
	csrrw x31, 0x4a4, x26	// Write all 1s to CSR
	csrrw x31, 0x4a4, x0	// Write all 0s to CSR
	csrrs x31, 0x4a4, x26	// Set all CSR bits
	csrrc x31, 0x4a4, x26	// Clear all CSR bits
	csrrw x31, 0x4a4, x14	// Restore CSR

// Testing CSR 0x4a5
	csrr x25, 0x4a5	// Read CSR
	li x20, -1
	csrrw x19, 0x4a5, x20	// Write all 1s to CSR
	csrrw x19, 0x4a5, x0	// Write all 0s to CSR
	csrrs x19, 0x4a5, x20	// Set all CSR bits
	csrrc x19, 0x4a5, x20	// Clear all CSR bits
	csrrw x19, 0x4a5, x25	// Restore CSR

// Testing CSR 0x4a6
	csrr x17, 0x4a6	// Read CSR
	li x1, -1
	csrrw x6, 0x4a6, x1	// Write all 1s to CSR
	csrrw x6, 0x4a6, x0	// Write all 0s to CSR
	csrrs x6, 0x4a6, x1	// Set all CSR bits
	csrrc x6, 0x4a6, x1	// Clear all CSR bits
	csrrw x6, 0x4a6, x17	// Restore CSR

// Testing CSR 0x4a7
	csrr x14, 0x4a7	// Read CSR
	li x12, -1
	csrrw x29, 0x4a7, x12	// Write all 1s to CSR
	csrrw x29, 0x4a7, x0	// Write all 0s to CSR
	csrrs x29, 0x4a7, x12	// Set all CSR bits
	csrrc x29, 0x4a7, x12	// Clear all CSR bits
	csrrw x29, 0x4a7, x14	// Restore CSR

// Testing CSR 0x4a8
	csrr x12, 0x4a8	// Read CSR
	li x29, -1
	csrrw x20, 0x4a8, x29	// Write all 1s to CSR
	csrrw x20, 0x4a8, x0	// Write all 0s to CSR
	csrrs x20, 0x4a8, x29	// Set all CSR bits
	csrrc x20, 0x4a8, x29	// Clear all CSR bits
	csrrw x20, 0x4a8, x12	// Restore CSR

// Testing CSR 0x4a9
	csrr x22, 0x4a9	// Read CSR
	li x20, -1
	csrrw x11, 0x4a9, x20	// Write all 1s to CSR
	csrrw x11, 0x4a9, x0	// Write all 0s to CSR
	csrrs x11, 0x4a9, x20	// Set all CSR bits
	csrrc x11, 0x4a9, x20	// Clear all CSR bits
	csrrw x11, 0x4a9, x22	// Restore CSR

// Testing CSR 0x4aa
	csrr x24, 0x4aa	// Read CSR
	li x29, -1
	csrrw x2, 0x4aa, x29	// Write all 1s to CSR
	csrrw x2, 0x4aa, x0	// Write all 0s to CSR
	csrrs x2, 0x4aa, x29	// Set all CSR bits
	csrrc x2, 0x4aa, x29	// Clear all CSR bits
	csrrw x2, 0x4aa, x24	// Restore CSR

// Testing CSR 0x4ab
	csrr x29, 0x4ab	// Read CSR
	li x25, -1
	csrrw x26, 0x4ab, x25	// Write all 1s to CSR
	csrrw x26, 0x4ab, x0	// Write all 0s to CSR
	csrrs x26, 0x4ab, x25	// Set all CSR bits
	csrrc x26, 0x4ab, x25	// Clear all CSR bits
	csrrw x26, 0x4ab, x29	// Restore CSR

// Testing CSR 0x4ac
	csrr x26, 0x4ac	// Read CSR
	li x24, -1
	csrrw x8, 0x4ac, x24	// Write all 1s to CSR
	csrrw x8, 0x4ac, x0	// Write all 0s to CSR
	csrrs x8, 0x4ac, x24	// Set all CSR bits
	csrrc x8, 0x4ac, x24	// Clear all CSR bits
	csrrw x8, 0x4ac, x26	// Restore CSR

// Testing CSR 0x4ad
	csrr x24, 0x4ad	// Read CSR
	li x30, -1
	csrrw x9, 0x4ad, x30	// Write all 1s to CSR
	csrrw x9, 0x4ad, x0	// Write all 0s to CSR
	csrrs x9, 0x4ad, x30	// Set all CSR bits
	csrrc x9, 0x4ad, x30	// Clear all CSR bits
	csrrw x9, 0x4ad, x24	// Restore CSR

// Testing CSR 0x4ae
	csrr x5, 0x4ae	// Read CSR
	li x28, -1
	csrrw x10, 0x4ae, x28	// Write all 1s to CSR
	csrrw x10, 0x4ae, x0	// Write all 0s to CSR
	csrrs x10, 0x4ae, x28	// Set all CSR bits
	csrrc x10, 0x4ae, x28	// Clear all CSR bits
	csrrw x10, 0x4ae, x5	// Restore CSR

// Testing CSR 0x4af
	csrr x6, 0x4af	// Read CSR
	li x10, -1
	csrrw x15, 0x4af, x10	// Write all 1s to CSR
	csrrw x15, 0x4af, x0	// Write all 0s to CSR
	csrrs x15, 0x4af, x10	// Set all CSR bits
	csrrc x15, 0x4af, x10	// Clear all CSR bits
	csrrw x15, 0x4af, x6	// Restore CSR

// Testing CSR 0x4b0
	csrr x17, 0x4b0	// Read CSR
	li x1, -1
	csrrw x30, 0x4b0, x1	// Write all 1s to CSR
	csrrw x30, 0x4b0, x0	// Write all 0s to CSR
	csrrs x30, 0x4b0, x1	// Set all CSR bits
	csrrc x30, 0x4b0, x1	// Clear all CSR bits
	csrrw x30, 0x4b0, x17	// Restore CSR

// Testing CSR 0x4b1
	csrr x11, 0x4b1	// Read CSR
	li x14, -1
	csrrw x22, 0x4b1, x14	// Write all 1s to CSR
	csrrw x22, 0x4b1, x0	// Write all 0s to CSR
	csrrs x22, 0x4b1, x14	// Set all CSR bits
	csrrc x22, 0x4b1, x14	// Clear all CSR bits
	csrrw x22, 0x4b1, x11	// Restore CSR

// Testing CSR 0x4b2
	csrr x19, 0x4b2	// Read CSR
	li x2, -1
	csrrw x20, 0x4b2, x2	// Write all 1s to CSR
	csrrw x20, 0x4b2, x0	// Write all 0s to CSR
	csrrs x20, 0x4b2, x2	// Set all CSR bits
	csrrc x20, 0x4b2, x2	// Clear all CSR bits
	csrrw x20, 0x4b2, x19	// Restore CSR

// Testing CSR 0x4b3
	csrr x7, 0x4b3	// Read CSR
	li x30, -1
	csrrw x11, 0x4b3, x30	// Write all 1s to CSR
	csrrw x11, 0x4b3, x0	// Write all 0s to CSR
	csrrs x11, 0x4b3, x30	// Set all CSR bits
	csrrc x11, 0x4b3, x30	// Clear all CSR bits
	csrrw x11, 0x4b3, x7	// Restore CSR

// Testing CSR 0x4b4
	csrr x8, 0x4b4	// Read CSR
	li x11, -1
	csrrw x24, 0x4b4, x11	// Write all 1s to CSR
	csrrw x24, 0x4b4, x0	// Write all 0s to CSR
	csrrs x24, 0x4b4, x11	// Set all CSR bits
	csrrc x24, 0x4b4, x11	// Clear all CSR bits
	csrrw x24, 0x4b4, x8	// Restore CSR

// Testing CSR 0x4b5
	csrr x30, 0x4b5	// Read CSR
	li x17, -1
	csrrw x18, 0x4b5, x17	// Write all 1s to CSR
	csrrw x18, 0x4b5, x0	// Write all 0s to CSR
	csrrs x18, 0x4b5, x17	// Set all CSR bits
	csrrc x18, 0x4b5, x17	// Clear all CSR bits
	csrrw x18, 0x4b5, x30	// Restore CSR

// Testing CSR 0x4b6
	csrr x31, 0x4b6	// Read CSR
	li x9, -1
	csrrw x3, 0x4b6, x9	// Write all 1s to CSR
	csrrw x3, 0x4b6, x0	// Write all 0s to CSR
	csrrs x3, 0x4b6, x9	// Set all CSR bits
	csrrc x3, 0x4b6, x9	// Clear all CSR bits
	csrrw x3, 0x4b6, x31	// Restore CSR

// Testing CSR 0x4b7
	csrr x19, 0x4b7	// Read CSR
	li x3, -1
	csrrw x5, 0x4b7, x3	// Write all 1s to CSR
	csrrw x5, 0x4b7, x0	// Write all 0s to CSR
	csrrs x5, 0x4b7, x3	// Set all CSR bits
	csrrc x5, 0x4b7, x3	// Clear all CSR bits
	csrrw x5, 0x4b7, x19	// Restore CSR

// Testing CSR 0x4b8
	csrr x11, 0x4b8	// Read CSR
	li x17, -1
	csrrw x31, 0x4b8, x17	// Write all 1s to CSR
	csrrw x31, 0x4b8, x0	// Write all 0s to CSR
	csrrs x31, 0x4b8, x17	// Set all CSR bits
	csrrc x31, 0x4b8, x17	// Clear all CSR bits
	csrrw x31, 0x4b8, x11	// Restore CSR

// Testing CSR 0x4b9
	csrr x6, 0x4b9	// Read CSR
	li x12, -1
	csrrw x19, 0x4b9, x12	// Write all 1s to CSR
	csrrw x19, 0x4b9, x0	// Write all 0s to CSR
	csrrs x19, 0x4b9, x12	// Set all CSR bits
	csrrc x19, 0x4b9, x12	// Clear all CSR bits
	csrrw x19, 0x4b9, x6	// Restore CSR

// Testing CSR 0x4ba
	csrr x5, 0x4ba	// Read CSR
	li x1, -1
	csrrw x9, 0x4ba, x1	// Write all 1s to CSR
	csrrw x9, 0x4ba, x0	// Write all 0s to CSR
	csrrs x9, 0x4ba, x1	// Set all CSR bits
	csrrc x9, 0x4ba, x1	// Clear all CSR bits
	csrrw x9, 0x4ba, x5	// Restore CSR

// Testing CSR 0x4bb
	csrr x20, 0x4bb	// Read CSR
	li x11, -1
	csrrw x30, 0x4bb, x11	// Write all 1s to CSR
	csrrw x30, 0x4bb, x0	// Write all 0s to CSR
	csrrs x30, 0x4bb, x11	// Set all CSR bits
	csrrc x30, 0x4bb, x11	// Clear all CSR bits
	csrrw x30, 0x4bb, x20	// Restore CSR

// Testing CSR 0x4bc
	csrr x11, 0x4bc	// Read CSR
	li x26, -1
	csrrw x5, 0x4bc, x26	// Write all 1s to CSR
	csrrw x5, 0x4bc, x0	// Write all 0s to CSR
	csrrs x5, 0x4bc, x26	// Set all CSR bits
	csrrc x5, 0x4bc, x26	// Clear all CSR bits
	csrrw x5, 0x4bc, x11	// Restore CSR

// Testing CSR 0x4bd
	csrr x28, 0x4bd	// Read CSR
	li x3, -1
	csrrw x26, 0x4bd, x3	// Write all 1s to CSR
	csrrw x26, 0x4bd, x0	// Write all 0s to CSR
	csrrs x26, 0x4bd, x3	// Set all CSR bits
	csrrc x26, 0x4bd, x3	// Clear all CSR bits
	csrrw x26, 0x4bd, x28	// Restore CSR

// Testing CSR 0x4be
	csrr x11, 0x4be	// Read CSR
	li x4, -1
	csrrw x10, 0x4be, x4	// Write all 1s to CSR
	csrrw x10, 0x4be, x0	// Write all 0s to CSR
	csrrs x10, 0x4be, x4	// Set all CSR bits
	csrrc x10, 0x4be, x4	// Clear all CSR bits
	csrrw x10, 0x4be, x11	// Restore CSR

// Testing CSR 0x4bf
	csrr x2, 0x4bf	// Read CSR
	li x22, -1
	csrrw x3, 0x4bf, x22	// Write all 1s to CSR
	csrrw x3, 0x4bf, x0	// Write all 0s to CSR
	csrrs x3, 0x4bf, x22	// Set all CSR bits
	csrrc x3, 0x4bf, x22	// Clear all CSR bits
	csrrw x3, 0x4bf, x2	// Restore CSR

// Testing CSR 0x4c0
	csrr x17, 0x4c0	// Read CSR
	li x21, -1
	csrrw x16, 0x4c0, x21	// Write all 1s to CSR
	csrrw x16, 0x4c0, x0	// Write all 0s to CSR
	csrrs x16, 0x4c0, x21	// Set all CSR bits
	csrrc x16, 0x4c0, x21	// Clear all CSR bits
	csrrw x16, 0x4c0, x17	// Restore CSR

// Testing CSR 0x4c1
	csrr x1, 0x4c1	// Read CSR
	li x12, -1
	csrrw x19, 0x4c1, x12	// Write all 1s to CSR
	csrrw x19, 0x4c1, x0	// Write all 0s to CSR
	csrrs x19, 0x4c1, x12	// Set all CSR bits
	csrrc x19, 0x4c1, x12	// Clear all CSR bits
	csrrw x19, 0x4c1, x1	// Restore CSR

// Testing CSR 0x4c2
	csrr x19, 0x4c2	// Read CSR
	li x4, -1
	csrrw x28, 0x4c2, x4	// Write all 1s to CSR
	csrrw x28, 0x4c2, x0	// Write all 0s to CSR
	csrrs x28, 0x4c2, x4	// Set all CSR bits
	csrrc x28, 0x4c2, x4	// Clear all CSR bits
	csrrw x28, 0x4c2, x19	// Restore CSR

// Testing CSR 0x4c3
	csrr x25, 0x4c3	// Read CSR
	li x27, -1
	csrrw x9, 0x4c3, x27	// Write all 1s to CSR
	csrrw x9, 0x4c3, x0	// Write all 0s to CSR
	csrrs x9, 0x4c3, x27	// Set all CSR bits
	csrrc x9, 0x4c3, x27	// Clear all CSR bits
	csrrw x9, 0x4c3, x25	// Restore CSR

// Testing CSR 0x4c4
	csrr x13, 0x4c4	// Read CSR
	li x12, -1
	csrrw x19, 0x4c4, x12	// Write all 1s to CSR
	csrrw x19, 0x4c4, x0	// Write all 0s to CSR
	csrrs x19, 0x4c4, x12	// Set all CSR bits
	csrrc x19, 0x4c4, x12	// Clear all CSR bits
	csrrw x19, 0x4c4, x13	// Restore CSR

// Testing CSR 0x4c5
	csrr x31, 0x4c5	// Read CSR
	li x19, -1
	csrrw x23, 0x4c5, x19	// Write all 1s to CSR
	csrrw x23, 0x4c5, x0	// Write all 0s to CSR
	csrrs x23, 0x4c5, x19	// Set all CSR bits
	csrrc x23, 0x4c5, x19	// Clear all CSR bits
	csrrw x23, 0x4c5, x31	// Restore CSR

// Testing CSR 0x4c6
	csrr x6, 0x4c6	// Read CSR
	li x4, -1
	csrrw x21, 0x4c6, x4	// Write all 1s to CSR
	csrrw x21, 0x4c6, x0	// Write all 0s to CSR
	csrrs x21, 0x4c6, x4	// Set all CSR bits
	csrrc x21, 0x4c6, x4	// Clear all CSR bits
	csrrw x21, 0x4c6, x6	// Restore CSR

// Testing CSR 0x4c7
	csrr x29, 0x4c7	// Read CSR
	li x5, -1
	csrrw x23, 0x4c7, x5	// Write all 1s to CSR
	csrrw x23, 0x4c7, x0	// Write all 0s to CSR
	csrrs x23, 0x4c7, x5	// Set all CSR bits
	csrrc x23, 0x4c7, x5	// Clear all CSR bits
	csrrw x23, 0x4c7, x29	// Restore CSR

// Testing CSR 0x4c8
	csrr x30, 0x4c8	// Read CSR
	li x10, -1
	csrrw x9, 0x4c8, x10	// Write all 1s to CSR
	csrrw x9, 0x4c8, x0	// Write all 0s to CSR
	csrrs x9, 0x4c8, x10	// Set all CSR bits
	csrrc x9, 0x4c8, x10	// Clear all CSR bits
	csrrw x9, 0x4c8, x30	// Restore CSR

// Testing CSR 0x4c9
	csrr x25, 0x4c9	// Read CSR
	li x26, -1
	csrrw x1, 0x4c9, x26	// Write all 1s to CSR
	csrrw x1, 0x4c9, x0	// Write all 0s to CSR
	csrrs x1, 0x4c9, x26	// Set all CSR bits
	csrrc x1, 0x4c9, x26	// Clear all CSR bits
	csrrw x1, 0x4c9, x25	// Restore CSR

// Testing CSR 0x4ca
	csrr x21, 0x4ca	// Read CSR
	li x25, -1
	csrrw x30, 0x4ca, x25	// Write all 1s to CSR
	csrrw x30, 0x4ca, x0	// Write all 0s to CSR
	csrrs x30, 0x4ca, x25	// Set all CSR bits
	csrrc x30, 0x4ca, x25	// Clear all CSR bits
	csrrw x30, 0x4ca, x21	// Restore CSR

// Testing CSR 0x4cb
	csrr x12, 0x4cb	// Read CSR
	li x23, -1
	csrrw x11, 0x4cb, x23	// Write all 1s to CSR
	csrrw x11, 0x4cb, x0	// Write all 0s to CSR
	csrrs x11, 0x4cb, x23	// Set all CSR bits
	csrrc x11, 0x4cb, x23	// Clear all CSR bits
	csrrw x11, 0x4cb, x12	// Restore CSR

// Testing CSR 0x4cc
	csrr x16, 0x4cc	// Read CSR
	li x18, -1
	csrrw x22, 0x4cc, x18	// Write all 1s to CSR
	csrrw x22, 0x4cc, x0	// Write all 0s to CSR
	csrrs x22, 0x4cc, x18	// Set all CSR bits
	csrrc x22, 0x4cc, x18	// Clear all CSR bits
	csrrw x22, 0x4cc, x16	// Restore CSR

// Testing CSR 0x4cd
	csrr x29, 0x4cd	// Read CSR
	li x11, -1
	csrrw x17, 0x4cd, x11	// Write all 1s to CSR
	csrrw x17, 0x4cd, x0	// Write all 0s to CSR
	csrrs x17, 0x4cd, x11	// Set all CSR bits
	csrrc x17, 0x4cd, x11	// Clear all CSR bits
	csrrw x17, 0x4cd, x29	// Restore CSR

// Testing CSR 0x4ce
	csrr x6, 0x4ce	// Read CSR
	li x8, -1
	csrrw x26, 0x4ce, x8	// Write all 1s to CSR
	csrrw x26, 0x4ce, x0	// Write all 0s to CSR
	csrrs x26, 0x4ce, x8	// Set all CSR bits
	csrrc x26, 0x4ce, x8	// Clear all CSR bits
	csrrw x26, 0x4ce, x6	// Restore CSR

// Testing CSR 0x4cf
	csrr x15, 0x4cf	// Read CSR
	li x7, -1
	csrrw x12, 0x4cf, x7	// Write all 1s to CSR
	csrrw x12, 0x4cf, x0	// Write all 0s to CSR
	csrrs x12, 0x4cf, x7	// Set all CSR bits
	csrrc x12, 0x4cf, x7	// Clear all CSR bits
	csrrw x12, 0x4cf, x15	// Restore CSR

// Testing CSR 0x4d0
	csrr x25, 0x4d0	// Read CSR
	li x26, -1
	csrrw x20, 0x4d0, x26	// Write all 1s to CSR
	csrrw x20, 0x4d0, x0	// Write all 0s to CSR
	csrrs x20, 0x4d0, x26	// Set all CSR bits
	csrrc x20, 0x4d0, x26	// Clear all CSR bits
	csrrw x20, 0x4d0, x25	// Restore CSR

// Testing CSR 0x4d1
	csrr x1, 0x4d1	// Read CSR
	li x3, -1
	csrrw x4, 0x4d1, x3	// Write all 1s to CSR
	csrrw x4, 0x4d1, x0	// Write all 0s to CSR
	csrrs x4, 0x4d1, x3	// Set all CSR bits
	csrrc x4, 0x4d1, x3	// Clear all CSR bits
	csrrw x4, 0x4d1, x1	// Restore CSR

// Testing CSR 0x4d2
	csrr x3, 0x4d2	// Read CSR
	li x30, -1
	csrrw x13, 0x4d2, x30	// Write all 1s to CSR
	csrrw x13, 0x4d2, x0	// Write all 0s to CSR
	csrrs x13, 0x4d2, x30	// Set all CSR bits
	csrrc x13, 0x4d2, x30	// Clear all CSR bits
	csrrw x13, 0x4d2, x3	// Restore CSR

// Testing CSR 0x4d3
	csrr x18, 0x4d3	// Read CSR
	li x26, -1
	csrrw x8, 0x4d3, x26	// Write all 1s to CSR
	csrrw x8, 0x4d3, x0	// Write all 0s to CSR
	csrrs x8, 0x4d3, x26	// Set all CSR bits
	csrrc x8, 0x4d3, x26	// Clear all CSR bits
	csrrw x8, 0x4d3, x18	// Restore CSR

// Testing CSR 0x4d4
	csrr x21, 0x4d4	// Read CSR
	li x16, -1
	csrrw x3, 0x4d4, x16	// Write all 1s to CSR
	csrrw x3, 0x4d4, x0	// Write all 0s to CSR
	csrrs x3, 0x4d4, x16	// Set all CSR bits
	csrrc x3, 0x4d4, x16	// Clear all CSR bits
	csrrw x3, 0x4d4, x21	// Restore CSR

// Testing CSR 0x4d5
	csrr x13, 0x4d5	// Read CSR
	li x30, -1
	csrrw x16, 0x4d5, x30	// Write all 1s to CSR
	csrrw x16, 0x4d5, x0	// Write all 0s to CSR
	csrrs x16, 0x4d5, x30	// Set all CSR bits
	csrrc x16, 0x4d5, x30	// Clear all CSR bits
	csrrw x16, 0x4d5, x13	// Restore CSR

// Testing CSR 0x4d6
	csrr x8, 0x4d6	// Read CSR
	li x1, -1
	csrrw x24, 0x4d6, x1	// Write all 1s to CSR
	csrrw x24, 0x4d6, x0	// Write all 0s to CSR
	csrrs x24, 0x4d6, x1	// Set all CSR bits
	csrrc x24, 0x4d6, x1	// Clear all CSR bits
	csrrw x24, 0x4d6, x8	// Restore CSR

// Testing CSR 0x4d7
	csrr x20, 0x4d7	// Read CSR
	li x13, -1
	csrrw x6, 0x4d7, x13	// Write all 1s to CSR
	csrrw x6, 0x4d7, x0	// Write all 0s to CSR
	csrrs x6, 0x4d7, x13	// Set all CSR bits
	csrrc x6, 0x4d7, x13	// Clear all CSR bits
	csrrw x6, 0x4d7, x20	// Restore CSR

// Testing CSR 0x4d8
	csrr x28, 0x4d8	// Read CSR
	li x17, -1
	csrrw x27, 0x4d8, x17	// Write all 1s to CSR
	csrrw x27, 0x4d8, x0	// Write all 0s to CSR
	csrrs x27, 0x4d8, x17	// Set all CSR bits
	csrrc x27, 0x4d8, x17	// Clear all CSR bits
	csrrw x27, 0x4d8, x28	// Restore CSR

// Testing CSR 0x4d9
	csrr x27, 0x4d9	// Read CSR
	li x19, -1
	csrrw x12, 0x4d9, x19	// Write all 1s to CSR
	csrrw x12, 0x4d9, x0	// Write all 0s to CSR
	csrrs x12, 0x4d9, x19	// Set all CSR bits
	csrrc x12, 0x4d9, x19	// Clear all CSR bits
	csrrw x12, 0x4d9, x27	// Restore CSR

// Testing CSR 0x4da
	csrr x26, 0x4da	// Read CSR
	li x28, -1
	csrrw x25, 0x4da, x28	// Write all 1s to CSR
	csrrw x25, 0x4da, x0	// Write all 0s to CSR
	csrrs x25, 0x4da, x28	// Set all CSR bits
	csrrc x25, 0x4da, x28	// Clear all CSR bits
	csrrw x25, 0x4da, x26	// Restore CSR

// Testing CSR 0x4db
	csrr x11, 0x4db	// Read CSR
	li x21, -1
	csrrw x7, 0x4db, x21	// Write all 1s to CSR
	csrrw x7, 0x4db, x0	// Write all 0s to CSR
	csrrs x7, 0x4db, x21	// Set all CSR bits
	csrrc x7, 0x4db, x21	// Clear all CSR bits
	csrrw x7, 0x4db, x11	// Restore CSR

// Testing CSR 0x4dc
	csrr x28, 0x4dc	// Read CSR
	li x23, -1
	csrrw x10, 0x4dc, x23	// Write all 1s to CSR
	csrrw x10, 0x4dc, x0	// Write all 0s to CSR
	csrrs x10, 0x4dc, x23	// Set all CSR bits
	csrrc x10, 0x4dc, x23	// Clear all CSR bits
	csrrw x10, 0x4dc, x28	// Restore CSR

// Testing CSR 0x4dd
	csrr x28, 0x4dd	// Read CSR
	li x2, -1
	csrrw x26, 0x4dd, x2	// Write all 1s to CSR
	csrrw x26, 0x4dd, x0	// Write all 0s to CSR
	csrrs x26, 0x4dd, x2	// Set all CSR bits
	csrrc x26, 0x4dd, x2	// Clear all CSR bits
	csrrw x26, 0x4dd, x28	// Restore CSR

// Testing CSR 0x4de
	csrr x25, 0x4de	// Read CSR
	li x27, -1
	csrrw x21, 0x4de, x27	// Write all 1s to CSR
	csrrw x21, 0x4de, x0	// Write all 0s to CSR
	csrrs x21, 0x4de, x27	// Set all CSR bits
	csrrc x21, 0x4de, x27	// Clear all CSR bits
	csrrw x21, 0x4de, x25	// Restore CSR

// Testing CSR 0x4df
	csrr x28, 0x4df	// Read CSR
	li x27, -1
	csrrw x20, 0x4df, x27	// Write all 1s to CSR
	csrrw x20, 0x4df, x0	// Write all 0s to CSR
	csrrs x20, 0x4df, x27	// Set all CSR bits
	csrrc x20, 0x4df, x27	// Clear all CSR bits
	csrrw x20, 0x4df, x28	// Restore CSR

// Testing CSR 0x4e0
	csrr x21, 0x4e0	// Read CSR
	li x3, -1
	csrrw x11, 0x4e0, x3	// Write all 1s to CSR
	csrrw x11, 0x4e0, x0	// Write all 0s to CSR
	csrrs x11, 0x4e0, x3	// Set all CSR bits
	csrrc x11, 0x4e0, x3	// Clear all CSR bits
	csrrw x11, 0x4e0, x21	// Restore CSR

// Testing CSR 0x4e1
	csrr x16, 0x4e1	// Read CSR
	li x2, -1
	csrrw x6, 0x4e1, x2	// Write all 1s to CSR
	csrrw x6, 0x4e1, x0	// Write all 0s to CSR
	csrrs x6, 0x4e1, x2	// Set all CSR bits
	csrrc x6, 0x4e1, x2	// Clear all CSR bits
	csrrw x6, 0x4e1, x16	// Restore CSR

// Testing CSR 0x4e2
	csrr x9, 0x4e2	// Read CSR
	li x3, -1
	csrrw x26, 0x4e2, x3	// Write all 1s to CSR
	csrrw x26, 0x4e2, x0	// Write all 0s to CSR
	csrrs x26, 0x4e2, x3	// Set all CSR bits
	csrrc x26, 0x4e2, x3	// Clear all CSR bits
	csrrw x26, 0x4e2, x9	// Restore CSR

// Testing CSR 0x4e3
	csrr x13, 0x4e3	// Read CSR
	li x25, -1
	csrrw x16, 0x4e3, x25	// Write all 1s to CSR
	csrrw x16, 0x4e3, x0	// Write all 0s to CSR
	csrrs x16, 0x4e3, x25	// Set all CSR bits
	csrrc x16, 0x4e3, x25	// Clear all CSR bits
	csrrw x16, 0x4e3, x13	// Restore CSR

// Testing CSR 0x4e4
	csrr x5, 0x4e4	// Read CSR
	li x19, -1
	csrrw x9, 0x4e4, x19	// Write all 1s to CSR
	csrrw x9, 0x4e4, x0	// Write all 0s to CSR
	csrrs x9, 0x4e4, x19	// Set all CSR bits
	csrrc x9, 0x4e4, x19	// Clear all CSR bits
	csrrw x9, 0x4e4, x5	// Restore CSR

// Testing CSR 0x4e5
	csrr x13, 0x4e5	// Read CSR
	li x5, -1
	csrrw x28, 0x4e5, x5	// Write all 1s to CSR
	csrrw x28, 0x4e5, x0	// Write all 0s to CSR
	csrrs x28, 0x4e5, x5	// Set all CSR bits
	csrrc x28, 0x4e5, x5	// Clear all CSR bits
	csrrw x28, 0x4e5, x13	// Restore CSR

// Testing CSR 0x4e6
	csrr x18, 0x4e6	// Read CSR
	li x30, -1
	csrrw x2, 0x4e6, x30	// Write all 1s to CSR
	csrrw x2, 0x4e6, x0	// Write all 0s to CSR
	csrrs x2, 0x4e6, x30	// Set all CSR bits
	csrrc x2, 0x4e6, x30	// Clear all CSR bits
	csrrw x2, 0x4e6, x18	// Restore CSR

// Testing CSR 0x4e7
	csrr x27, 0x4e7	// Read CSR
	li x6, -1
	csrrw x29, 0x4e7, x6	// Write all 1s to CSR
	csrrw x29, 0x4e7, x0	// Write all 0s to CSR
	csrrs x29, 0x4e7, x6	// Set all CSR bits
	csrrc x29, 0x4e7, x6	// Clear all CSR bits
	csrrw x29, 0x4e7, x27	// Restore CSR

// Testing CSR 0x4e8
	csrr x9, 0x4e8	// Read CSR
	li x23, -1
	csrrw x30, 0x4e8, x23	// Write all 1s to CSR
	csrrw x30, 0x4e8, x0	// Write all 0s to CSR
	csrrs x30, 0x4e8, x23	// Set all CSR bits
	csrrc x30, 0x4e8, x23	// Clear all CSR bits
	csrrw x30, 0x4e8, x9	// Restore CSR

// Testing CSR 0x4e9
	csrr x5, 0x4e9	// Read CSR
	li x31, -1
	csrrw x25, 0x4e9, x31	// Write all 1s to CSR
	csrrw x25, 0x4e9, x0	// Write all 0s to CSR
	csrrs x25, 0x4e9, x31	// Set all CSR bits
	csrrc x25, 0x4e9, x31	// Clear all CSR bits
	csrrw x25, 0x4e9, x5	// Restore CSR

// Testing CSR 0x4ea
	csrr x24, 0x4ea	// Read CSR
	li x26, -1
	csrrw x13, 0x4ea, x26	// Write all 1s to CSR
	csrrw x13, 0x4ea, x0	// Write all 0s to CSR
	csrrs x13, 0x4ea, x26	// Set all CSR bits
	csrrc x13, 0x4ea, x26	// Clear all CSR bits
	csrrw x13, 0x4ea, x24	// Restore CSR

// Testing CSR 0x4eb
	csrr x1, 0x4eb	// Read CSR
	li x8, -1
	csrrw x25, 0x4eb, x8	// Write all 1s to CSR
	csrrw x25, 0x4eb, x0	// Write all 0s to CSR
	csrrs x25, 0x4eb, x8	// Set all CSR bits
	csrrc x25, 0x4eb, x8	// Clear all CSR bits
	csrrw x25, 0x4eb, x1	// Restore CSR

// Testing CSR 0x4ec
	csrr x15, 0x4ec	// Read CSR
	li x19, -1
	csrrw x3, 0x4ec, x19	// Write all 1s to CSR
	csrrw x3, 0x4ec, x0	// Write all 0s to CSR
	csrrs x3, 0x4ec, x19	// Set all CSR bits
	csrrc x3, 0x4ec, x19	// Clear all CSR bits
	csrrw x3, 0x4ec, x15	// Restore CSR

// Testing CSR 0x4ed
	csrr x2, 0x4ed	// Read CSR
	li x19, -1
	csrrw x7, 0x4ed, x19	// Write all 1s to CSR
	csrrw x7, 0x4ed, x0	// Write all 0s to CSR
	csrrs x7, 0x4ed, x19	// Set all CSR bits
	csrrc x7, 0x4ed, x19	// Clear all CSR bits
	csrrw x7, 0x4ed, x2	// Restore CSR

// Testing CSR 0x4ee
	csrr x19, 0x4ee	// Read CSR
	li x6, -1
	csrrw x26, 0x4ee, x6	// Write all 1s to CSR
	csrrw x26, 0x4ee, x0	// Write all 0s to CSR
	csrrs x26, 0x4ee, x6	// Set all CSR bits
	csrrc x26, 0x4ee, x6	// Clear all CSR bits
	csrrw x26, 0x4ee, x19	// Restore CSR

// Testing CSR 0x4ef
	csrr x20, 0x4ef	// Read CSR
	li x18, -1
	csrrw x12, 0x4ef, x18	// Write all 1s to CSR
	csrrw x12, 0x4ef, x0	// Write all 0s to CSR
	csrrs x12, 0x4ef, x18	// Set all CSR bits
	csrrc x12, 0x4ef, x18	// Clear all CSR bits
	csrrw x12, 0x4ef, x20	// Restore CSR

// Testing CSR 0x4f0
	csrr x29, 0x4f0	// Read CSR
	li x6, -1
	csrrw x24, 0x4f0, x6	// Write all 1s to CSR
	csrrw x24, 0x4f0, x0	// Write all 0s to CSR
	csrrs x24, 0x4f0, x6	// Set all CSR bits
	csrrc x24, 0x4f0, x6	// Clear all CSR bits
	csrrw x24, 0x4f0, x29	// Restore CSR

// Testing CSR 0x4f1
	csrr x27, 0x4f1	// Read CSR
	li x7, -1
	csrrw x1, 0x4f1, x7	// Write all 1s to CSR
	csrrw x1, 0x4f1, x0	// Write all 0s to CSR
	csrrs x1, 0x4f1, x7	// Set all CSR bits
	csrrc x1, 0x4f1, x7	// Clear all CSR bits
	csrrw x1, 0x4f1, x27	// Restore CSR

// Testing CSR 0x4f2
	csrr x27, 0x4f2	// Read CSR
	li x16, -1
	csrrw x18, 0x4f2, x16	// Write all 1s to CSR
	csrrw x18, 0x4f2, x0	// Write all 0s to CSR
	csrrs x18, 0x4f2, x16	// Set all CSR bits
	csrrc x18, 0x4f2, x16	// Clear all CSR bits
	csrrw x18, 0x4f2, x27	// Restore CSR

// Testing CSR 0x4f3
	csrr x26, 0x4f3	// Read CSR
	li x20, -1
	csrrw x15, 0x4f3, x20	// Write all 1s to CSR
	csrrw x15, 0x4f3, x0	// Write all 0s to CSR
	csrrs x15, 0x4f3, x20	// Set all CSR bits
	csrrc x15, 0x4f3, x20	// Clear all CSR bits
	csrrw x15, 0x4f3, x26	// Restore CSR

// Testing CSR 0x4f4
	csrr x18, 0x4f4	// Read CSR
	li x12, -1
	csrrw x19, 0x4f4, x12	// Write all 1s to CSR
	csrrw x19, 0x4f4, x0	// Write all 0s to CSR
	csrrs x19, 0x4f4, x12	// Set all CSR bits
	csrrc x19, 0x4f4, x12	// Clear all CSR bits
	csrrw x19, 0x4f4, x18	// Restore CSR

// Testing CSR 0x4f5
	csrr x30, 0x4f5	// Read CSR
	li x12, -1
	csrrw x26, 0x4f5, x12	// Write all 1s to CSR
	csrrw x26, 0x4f5, x0	// Write all 0s to CSR
	csrrs x26, 0x4f5, x12	// Set all CSR bits
	csrrc x26, 0x4f5, x12	// Clear all CSR bits
	csrrw x26, 0x4f5, x30	// Restore CSR

// Testing CSR 0x4f6
	csrr x29, 0x4f6	// Read CSR
	li x26, -1
	csrrw x22, 0x4f6, x26	// Write all 1s to CSR
	csrrw x22, 0x4f6, x0	// Write all 0s to CSR
	csrrs x22, 0x4f6, x26	// Set all CSR bits
	csrrc x22, 0x4f6, x26	// Clear all CSR bits
	csrrw x22, 0x4f6, x29	// Restore CSR

// Testing CSR 0x4f7
	csrr x31, 0x4f7	// Read CSR
	li x16, -1
	csrrw x3, 0x4f7, x16	// Write all 1s to CSR
	csrrw x3, 0x4f7, x0	// Write all 0s to CSR
	csrrs x3, 0x4f7, x16	// Set all CSR bits
	csrrc x3, 0x4f7, x16	// Clear all CSR bits
	csrrw x3, 0x4f7, x31	// Restore CSR

// Testing CSR 0x4f8
	csrr x14, 0x4f8	// Read CSR
	li x30, -1
	csrrw x28, 0x4f8, x30	// Write all 1s to CSR
	csrrw x28, 0x4f8, x0	// Write all 0s to CSR
	csrrs x28, 0x4f8, x30	// Set all CSR bits
	csrrc x28, 0x4f8, x30	// Clear all CSR bits
	csrrw x28, 0x4f8, x14	// Restore CSR

// Testing CSR 0x4f9
	csrr x31, 0x4f9	// Read CSR
	li x16, -1
	csrrw x26, 0x4f9, x16	// Write all 1s to CSR
	csrrw x26, 0x4f9, x0	// Write all 0s to CSR
	csrrs x26, 0x4f9, x16	// Set all CSR bits
	csrrc x26, 0x4f9, x16	// Clear all CSR bits
	csrrw x26, 0x4f9, x31	// Restore CSR

// Testing CSR 0x4fa
	csrr x15, 0x4fa	// Read CSR
	li x29, -1
	csrrw x26, 0x4fa, x29	// Write all 1s to CSR
	csrrw x26, 0x4fa, x0	// Write all 0s to CSR
	csrrs x26, 0x4fa, x29	// Set all CSR bits
	csrrc x26, 0x4fa, x29	// Clear all CSR bits
	csrrw x26, 0x4fa, x15	// Restore CSR

// Testing CSR 0x4fb
	csrr x20, 0x4fb	// Read CSR
	li x27, -1
	csrrw x15, 0x4fb, x27	// Write all 1s to CSR
	csrrw x15, 0x4fb, x0	// Write all 0s to CSR
	csrrs x15, 0x4fb, x27	// Set all CSR bits
	csrrc x15, 0x4fb, x27	// Clear all CSR bits
	csrrw x15, 0x4fb, x20	// Restore CSR

// Testing CSR 0x4fc
	csrr x13, 0x4fc	// Read CSR
	li x31, -1
	csrrw x20, 0x4fc, x31	// Write all 1s to CSR
	csrrw x20, 0x4fc, x0	// Write all 0s to CSR
	csrrs x20, 0x4fc, x31	// Set all CSR bits
	csrrc x20, 0x4fc, x31	// Clear all CSR bits
	csrrw x20, 0x4fc, x13	// Restore CSR

// Testing CSR 0x4fd
	csrr x5, 0x4fd	// Read CSR
	li x28, -1
	csrrw x13, 0x4fd, x28	// Write all 1s to CSR
	csrrw x13, 0x4fd, x0	// Write all 0s to CSR
	csrrs x13, 0x4fd, x28	// Set all CSR bits
	csrrc x13, 0x4fd, x28	// Clear all CSR bits
	csrrw x13, 0x4fd, x5	// Restore CSR

// Testing CSR 0x4fe
	csrr x22, 0x4fe	// Read CSR
	li x14, -1
	csrrw x2, 0x4fe, x14	// Write all 1s to CSR
	csrrw x2, 0x4fe, x0	// Write all 0s to CSR
	csrrs x2, 0x4fe, x14	// Set all CSR bits
	csrrc x2, 0x4fe, x14	// Clear all CSR bits
	csrrw x2, 0x4fe, x22	// Restore CSR

// Testing CSR 0x4ff
	csrr x13, 0x4ff	// Read CSR
	li x9, -1
	csrrw x2, 0x4ff, x9	// Write all 1s to CSR
	csrrw x2, 0x4ff, x0	// Write all 0s to CSR
	csrrs x2, 0x4ff, x9	// Set all CSR bits
	csrrc x2, 0x4ff, x9	// Clear all CSR bits
	csrrw x2, 0x4ff, x13	// Restore CSR

// Testing CSR 0x500
	csrr x28, 0x500	// Read CSR
	li x25, -1
	csrrw x30, 0x500, x25	// Write all 1s to CSR
	csrrw x30, 0x500, x0	// Write all 0s to CSR
	csrrs x30, 0x500, x25	// Set all CSR bits
	csrrc x30, 0x500, x25	// Clear all CSR bits
	csrrw x30, 0x500, x28	// Restore CSR

// Testing CSR 0x501
	csrr x26, 0x501	// Read CSR
	li x10, -1
	csrrw x7, 0x501, x10	// Write all 1s to CSR
	csrrw x7, 0x501, x0	// Write all 0s to CSR
	csrrs x7, 0x501, x10	// Set all CSR bits
	csrrc x7, 0x501, x10	// Clear all CSR bits
	csrrw x7, 0x501, x26	// Restore CSR

// Testing CSR 0x502
	csrr x2, 0x502	// Read CSR
	li x20, -1
	csrrw x10, 0x502, x20	// Write all 1s to CSR
	csrrw x10, 0x502, x0	// Write all 0s to CSR
	csrrs x10, 0x502, x20	// Set all CSR bits
	csrrc x10, 0x502, x20	// Clear all CSR bits
	csrrw x10, 0x502, x2	// Restore CSR

// Testing CSR 0x503
	csrr x10, 0x503	// Read CSR
	li x12, -1
	csrrw x30, 0x503, x12	// Write all 1s to CSR
	csrrw x30, 0x503, x0	// Write all 0s to CSR
	csrrs x30, 0x503, x12	// Set all CSR bits
	csrrc x30, 0x503, x12	// Clear all CSR bits
	csrrw x30, 0x503, x10	// Restore CSR

// Testing CSR 0x504
	csrr x20, 0x504	// Read CSR
	li x11, -1
	csrrw x30, 0x504, x11	// Write all 1s to CSR
	csrrw x30, 0x504, x0	// Write all 0s to CSR
	csrrs x30, 0x504, x11	// Set all CSR bits
	csrrc x30, 0x504, x11	// Clear all CSR bits
	csrrw x30, 0x504, x20	// Restore CSR

// Testing CSR 0x505
	csrr x27, 0x505	// Read CSR
	li x7, -1
	csrrw x8, 0x505, x7	// Write all 1s to CSR
	csrrw x8, 0x505, x0	// Write all 0s to CSR
	csrrs x8, 0x505, x7	// Set all CSR bits
	csrrc x8, 0x505, x7	// Clear all CSR bits
	csrrw x8, 0x505, x27	// Restore CSR

// Testing CSR 0x506
	csrr x5, 0x506	// Read CSR
	li x31, -1
	csrrw x9, 0x506, x31	// Write all 1s to CSR
	csrrw x9, 0x506, x0	// Write all 0s to CSR
	csrrs x9, 0x506, x31	// Set all CSR bits
	csrrc x9, 0x506, x31	// Clear all CSR bits
	csrrw x9, 0x506, x5	// Restore CSR

// Testing CSR 0x507
	csrr x19, 0x507	// Read CSR
	li x3, -1
	csrrw x14, 0x507, x3	// Write all 1s to CSR
	csrrw x14, 0x507, x0	// Write all 0s to CSR
	csrrs x14, 0x507, x3	// Set all CSR bits
	csrrc x14, 0x507, x3	// Clear all CSR bits
	csrrw x14, 0x507, x19	// Restore CSR

// Testing CSR 0x508
	csrr x3, 0x508	// Read CSR
	li x13, -1
	csrrw x6, 0x508, x13	// Write all 1s to CSR
	csrrw x6, 0x508, x0	// Write all 0s to CSR
	csrrs x6, 0x508, x13	// Set all CSR bits
	csrrc x6, 0x508, x13	// Clear all CSR bits
	csrrw x6, 0x508, x3	// Restore CSR

// Testing CSR 0x509
	csrr x23, 0x509	// Read CSR
	li x15, -1
	csrrw x9, 0x509, x15	// Write all 1s to CSR
	csrrw x9, 0x509, x0	// Write all 0s to CSR
	csrrs x9, 0x509, x15	// Set all CSR bits
	csrrc x9, 0x509, x15	// Clear all CSR bits
	csrrw x9, 0x509, x23	// Restore CSR

// Testing CSR 0x50a
	csrr x23, 0x50a	// Read CSR
	li x18, -1
	csrrw x2, 0x50a, x18	// Write all 1s to CSR
	csrrw x2, 0x50a, x0	// Write all 0s to CSR
	csrrs x2, 0x50a, x18	// Set all CSR bits
	csrrc x2, 0x50a, x18	// Clear all CSR bits
	csrrw x2, 0x50a, x23	// Restore CSR

// Testing CSR 0x50b
	csrr x11, 0x50b	// Read CSR
	li x1, -1
	csrrw x15, 0x50b, x1	// Write all 1s to CSR
	csrrw x15, 0x50b, x0	// Write all 0s to CSR
	csrrs x15, 0x50b, x1	// Set all CSR bits
	csrrc x15, 0x50b, x1	// Clear all CSR bits
	csrrw x15, 0x50b, x11	// Restore CSR

// Testing CSR 0x50c
	csrr x2, 0x50c	// Read CSR
	li x26, -1
	csrrw x1, 0x50c, x26	// Write all 1s to CSR
	csrrw x1, 0x50c, x0	// Write all 0s to CSR
	csrrs x1, 0x50c, x26	// Set all CSR bits
	csrrc x1, 0x50c, x26	// Clear all CSR bits
	csrrw x1, 0x50c, x2	// Restore CSR

// Testing CSR 0x50d
	csrr x12, 0x50d	// Read CSR
	li x9, -1
	csrrw x2, 0x50d, x9	// Write all 1s to CSR
	csrrw x2, 0x50d, x0	// Write all 0s to CSR
	csrrs x2, 0x50d, x9	// Set all CSR bits
	csrrc x2, 0x50d, x9	// Clear all CSR bits
	csrrw x2, 0x50d, x12	// Restore CSR

// Testing CSR 0x50e
	csrr x5, 0x50e	// Read CSR
	li x30, -1
	csrrw x25, 0x50e, x30	// Write all 1s to CSR
	csrrw x25, 0x50e, x0	// Write all 0s to CSR
	csrrs x25, 0x50e, x30	// Set all CSR bits
	csrrc x25, 0x50e, x30	// Clear all CSR bits
	csrrw x25, 0x50e, x5	// Restore CSR

// Testing CSR 0x50f
	csrr x26, 0x50f	// Read CSR
	li x9, -1
	csrrw x27, 0x50f, x9	// Write all 1s to CSR
	csrrw x27, 0x50f, x0	// Write all 0s to CSR
	csrrs x27, 0x50f, x9	// Set all CSR bits
	csrrc x27, 0x50f, x9	// Clear all CSR bits
	csrrw x27, 0x50f, x26	// Restore CSR

// Testing CSR 0x510
	csrr x25, 0x510	// Read CSR
	li x4, -1
	csrrw x9, 0x510, x4	// Write all 1s to CSR
	csrrw x9, 0x510, x0	// Write all 0s to CSR
	csrrs x9, 0x510, x4	// Set all CSR bits
	csrrc x9, 0x510, x4	// Clear all CSR bits
	csrrw x9, 0x510, x25	// Restore CSR

// Testing CSR 0x511
	csrr x30, 0x511	// Read CSR
	li x9, -1
	csrrw x11, 0x511, x9	// Write all 1s to CSR
	csrrw x11, 0x511, x0	// Write all 0s to CSR
	csrrs x11, 0x511, x9	// Set all CSR bits
	csrrc x11, 0x511, x9	// Clear all CSR bits
	csrrw x11, 0x511, x30	// Restore CSR

// Testing CSR 0x512
	csrr x26, 0x512	// Read CSR
	li x22, -1
	csrrw x13, 0x512, x22	// Write all 1s to CSR
	csrrw x13, 0x512, x0	// Write all 0s to CSR
	csrrs x13, 0x512, x22	// Set all CSR bits
	csrrc x13, 0x512, x22	// Clear all CSR bits
	csrrw x13, 0x512, x26	// Restore CSR

// Testing CSR 0x513
	csrr x15, 0x513	// Read CSR
	li x5, -1
	csrrw x11, 0x513, x5	// Write all 1s to CSR
	csrrw x11, 0x513, x0	// Write all 0s to CSR
	csrrs x11, 0x513, x5	// Set all CSR bits
	csrrc x11, 0x513, x5	// Clear all CSR bits
	csrrw x11, 0x513, x15	// Restore CSR

// Testing CSR 0x514
	csrr x8, 0x514	// Read CSR
	li x5, -1
	csrrw x21, 0x514, x5	// Write all 1s to CSR
	csrrw x21, 0x514, x0	// Write all 0s to CSR
	csrrs x21, 0x514, x5	// Set all CSR bits
	csrrc x21, 0x514, x5	// Clear all CSR bits
	csrrw x21, 0x514, x8	// Restore CSR

// Testing CSR 0x515
	csrr x19, 0x515	// Read CSR
	li x17, -1
	csrrw x10, 0x515, x17	// Write all 1s to CSR
	csrrw x10, 0x515, x0	// Write all 0s to CSR
	csrrs x10, 0x515, x17	// Set all CSR bits
	csrrc x10, 0x515, x17	// Clear all CSR bits
	csrrw x10, 0x515, x19	// Restore CSR

// Testing CSR 0x516
	csrr x22, 0x516	// Read CSR
	li x25, -1
	csrrw x2, 0x516, x25	// Write all 1s to CSR
	csrrw x2, 0x516, x0	// Write all 0s to CSR
	csrrs x2, 0x516, x25	// Set all CSR bits
	csrrc x2, 0x516, x25	// Clear all CSR bits
	csrrw x2, 0x516, x22	// Restore CSR

// Testing CSR 0x517
	csrr x25, 0x517	// Read CSR
	li x7, -1
	csrrw x9, 0x517, x7	// Write all 1s to CSR
	csrrw x9, 0x517, x0	// Write all 0s to CSR
	csrrs x9, 0x517, x7	// Set all CSR bits
	csrrc x9, 0x517, x7	// Clear all CSR bits
	csrrw x9, 0x517, x25	// Restore CSR

// Testing CSR 0x518
	csrr x14, 0x518	// Read CSR
	li x9, -1
	csrrw x27, 0x518, x9	// Write all 1s to CSR
	csrrw x27, 0x518, x0	// Write all 0s to CSR
	csrrs x27, 0x518, x9	// Set all CSR bits
	csrrc x27, 0x518, x9	// Clear all CSR bits
	csrrw x27, 0x518, x14	// Restore CSR

// Testing CSR 0x519
	csrr x24, 0x519	// Read CSR
	li x2, -1
	csrrw x11, 0x519, x2	// Write all 1s to CSR
	csrrw x11, 0x519, x0	// Write all 0s to CSR
	csrrs x11, 0x519, x2	// Set all CSR bits
	csrrc x11, 0x519, x2	// Clear all CSR bits
	csrrw x11, 0x519, x24	// Restore CSR

// Testing CSR 0x51a
	csrr x8, 0x51a	// Read CSR
	li x15, -1
	csrrw x26, 0x51a, x15	// Write all 1s to CSR
	csrrw x26, 0x51a, x0	// Write all 0s to CSR
	csrrs x26, 0x51a, x15	// Set all CSR bits
	csrrc x26, 0x51a, x15	// Clear all CSR bits
	csrrw x26, 0x51a, x8	// Restore CSR

// Testing CSR 0x51b
	csrr x3, 0x51b	// Read CSR
	li x21, -1
	csrrw x15, 0x51b, x21	// Write all 1s to CSR
	csrrw x15, 0x51b, x0	// Write all 0s to CSR
	csrrs x15, 0x51b, x21	// Set all CSR bits
	csrrc x15, 0x51b, x21	// Clear all CSR bits
	csrrw x15, 0x51b, x3	// Restore CSR

// Testing CSR 0x51c
	csrr x4, 0x51c	// Read CSR
	li x18, -1
	csrrw x11, 0x51c, x18	// Write all 1s to CSR
	csrrw x11, 0x51c, x0	// Write all 0s to CSR
	csrrs x11, 0x51c, x18	// Set all CSR bits
	csrrc x11, 0x51c, x18	// Clear all CSR bits
	csrrw x11, 0x51c, x4	// Restore CSR

// Testing CSR 0x51d
	csrr x30, 0x51d	// Read CSR
	li x19, -1
	csrrw x7, 0x51d, x19	// Write all 1s to CSR
	csrrw x7, 0x51d, x0	// Write all 0s to CSR
	csrrs x7, 0x51d, x19	// Set all CSR bits
	csrrc x7, 0x51d, x19	// Clear all CSR bits
	csrrw x7, 0x51d, x30	// Restore CSR

// Testing CSR 0x51e
	csrr x5, 0x51e	// Read CSR
	li x30, -1
	csrrw x3, 0x51e, x30	// Write all 1s to CSR
	csrrw x3, 0x51e, x0	// Write all 0s to CSR
	csrrs x3, 0x51e, x30	// Set all CSR bits
	csrrc x3, 0x51e, x30	// Clear all CSR bits
	csrrw x3, 0x51e, x5	// Restore CSR

// Testing CSR 0x51f
	csrr x11, 0x51f	// Read CSR
	li x2, -1
	csrrw x28, 0x51f, x2	// Write all 1s to CSR
	csrrw x28, 0x51f, x0	// Write all 0s to CSR
	csrrs x28, 0x51f, x2	// Set all CSR bits
	csrrc x28, 0x51f, x2	// Clear all CSR bits
	csrrw x28, 0x51f, x11	// Restore CSR

// Testing CSR 0x520
	csrr x30, 0x520	// Read CSR
	li x9, -1
	csrrw x19, 0x520, x9	// Write all 1s to CSR
	csrrw x19, 0x520, x0	// Write all 0s to CSR
	csrrs x19, 0x520, x9	// Set all CSR bits
	csrrc x19, 0x520, x9	// Clear all CSR bits
	csrrw x19, 0x520, x30	// Restore CSR

// Testing CSR 0x521
	csrr x25, 0x521	// Read CSR
	li x2, -1
	csrrw x23, 0x521, x2	// Write all 1s to CSR
	csrrw x23, 0x521, x0	// Write all 0s to CSR
	csrrs x23, 0x521, x2	// Set all CSR bits
	csrrc x23, 0x521, x2	// Clear all CSR bits
	csrrw x23, 0x521, x25	// Restore CSR

// Testing CSR 0x522
	csrr x3, 0x522	// Read CSR
	li x1, -1
	csrrw x29, 0x522, x1	// Write all 1s to CSR
	csrrw x29, 0x522, x0	// Write all 0s to CSR
	csrrs x29, 0x522, x1	// Set all CSR bits
	csrrc x29, 0x522, x1	// Clear all CSR bits
	csrrw x29, 0x522, x3	// Restore CSR

// Testing CSR 0x523
	csrr x21, 0x523	// Read CSR
	li x7, -1
	csrrw x26, 0x523, x7	// Write all 1s to CSR
	csrrw x26, 0x523, x0	// Write all 0s to CSR
	csrrs x26, 0x523, x7	// Set all CSR bits
	csrrc x26, 0x523, x7	// Clear all CSR bits
	csrrw x26, 0x523, x21	// Restore CSR

// Testing CSR 0x524
	csrr x4, 0x524	// Read CSR
	li x5, -1
	csrrw x24, 0x524, x5	// Write all 1s to CSR
	csrrw x24, 0x524, x0	// Write all 0s to CSR
	csrrs x24, 0x524, x5	// Set all CSR bits
	csrrc x24, 0x524, x5	// Clear all CSR bits
	csrrw x24, 0x524, x4	// Restore CSR

// Testing CSR 0x525
	csrr x3, 0x525	// Read CSR
	li x20, -1
	csrrw x2, 0x525, x20	// Write all 1s to CSR
	csrrw x2, 0x525, x0	// Write all 0s to CSR
	csrrs x2, 0x525, x20	// Set all CSR bits
	csrrc x2, 0x525, x20	// Clear all CSR bits
	csrrw x2, 0x525, x3	// Restore CSR

// Testing CSR 0x526
	csrr x29, 0x526	// Read CSR
	li x19, -1
	csrrw x16, 0x526, x19	// Write all 1s to CSR
	csrrw x16, 0x526, x0	// Write all 0s to CSR
	csrrs x16, 0x526, x19	// Set all CSR bits
	csrrc x16, 0x526, x19	// Clear all CSR bits
	csrrw x16, 0x526, x29	// Restore CSR

// Testing CSR 0x527
	csrr x1, 0x527	// Read CSR
	li x18, -1
	csrrw x4, 0x527, x18	// Write all 1s to CSR
	csrrw x4, 0x527, x0	// Write all 0s to CSR
	csrrs x4, 0x527, x18	// Set all CSR bits
	csrrc x4, 0x527, x18	// Clear all CSR bits
	csrrw x4, 0x527, x1	// Restore CSR

// Testing CSR 0x528
	csrr x14, 0x528	// Read CSR
	li x29, -1
	csrrw x15, 0x528, x29	// Write all 1s to CSR
	csrrw x15, 0x528, x0	// Write all 0s to CSR
	csrrs x15, 0x528, x29	// Set all CSR bits
	csrrc x15, 0x528, x29	// Clear all CSR bits
	csrrw x15, 0x528, x14	// Restore CSR

// Testing CSR 0x529
	csrr x26, 0x529	// Read CSR
	li x15, -1
	csrrw x3, 0x529, x15	// Write all 1s to CSR
	csrrw x3, 0x529, x0	// Write all 0s to CSR
	csrrs x3, 0x529, x15	// Set all CSR bits
	csrrc x3, 0x529, x15	// Clear all CSR bits
	csrrw x3, 0x529, x26	// Restore CSR

// Testing CSR 0x52a
	csrr x13, 0x52a	// Read CSR
	li x8, -1
	csrrw x15, 0x52a, x8	// Write all 1s to CSR
	csrrw x15, 0x52a, x0	// Write all 0s to CSR
	csrrs x15, 0x52a, x8	// Set all CSR bits
	csrrc x15, 0x52a, x8	// Clear all CSR bits
	csrrw x15, 0x52a, x13	// Restore CSR

// Testing CSR 0x52b
	csrr x22, 0x52b	// Read CSR
	li x2, -1
	csrrw x21, 0x52b, x2	// Write all 1s to CSR
	csrrw x21, 0x52b, x0	// Write all 0s to CSR
	csrrs x21, 0x52b, x2	// Set all CSR bits
	csrrc x21, 0x52b, x2	// Clear all CSR bits
	csrrw x21, 0x52b, x22	// Restore CSR

// Testing CSR 0x52c
	csrr x17, 0x52c	// Read CSR
	li x28, -1
	csrrw x15, 0x52c, x28	// Write all 1s to CSR
	csrrw x15, 0x52c, x0	// Write all 0s to CSR
	csrrs x15, 0x52c, x28	// Set all CSR bits
	csrrc x15, 0x52c, x28	// Clear all CSR bits
	csrrw x15, 0x52c, x17	// Restore CSR

// Testing CSR 0x52d
	csrr x11, 0x52d	// Read CSR
	li x27, -1
	csrrw x10, 0x52d, x27	// Write all 1s to CSR
	csrrw x10, 0x52d, x0	// Write all 0s to CSR
	csrrs x10, 0x52d, x27	// Set all CSR bits
	csrrc x10, 0x52d, x27	// Clear all CSR bits
	csrrw x10, 0x52d, x11	// Restore CSR

// Testing CSR 0x52e
	csrr x15, 0x52e	// Read CSR
	li x17, -1
	csrrw x2, 0x52e, x17	// Write all 1s to CSR
	csrrw x2, 0x52e, x0	// Write all 0s to CSR
	csrrs x2, 0x52e, x17	// Set all CSR bits
	csrrc x2, 0x52e, x17	// Clear all CSR bits
	csrrw x2, 0x52e, x15	// Restore CSR

// Testing CSR 0x52f
	csrr x29, 0x52f	// Read CSR
	li x28, -1
	csrrw x31, 0x52f, x28	// Write all 1s to CSR
	csrrw x31, 0x52f, x0	// Write all 0s to CSR
	csrrs x31, 0x52f, x28	// Set all CSR bits
	csrrc x31, 0x52f, x28	// Clear all CSR bits
	csrrw x31, 0x52f, x29	// Restore CSR

// Testing CSR 0x530
	csrr x19, 0x530	// Read CSR
	li x12, -1
	csrrw x20, 0x530, x12	// Write all 1s to CSR
	csrrw x20, 0x530, x0	// Write all 0s to CSR
	csrrs x20, 0x530, x12	// Set all CSR bits
	csrrc x20, 0x530, x12	// Clear all CSR bits
	csrrw x20, 0x530, x19	// Restore CSR

// Testing CSR 0x531
	csrr x16, 0x531	// Read CSR
	li x31, -1
	csrrw x8, 0x531, x31	// Write all 1s to CSR
	csrrw x8, 0x531, x0	// Write all 0s to CSR
	csrrs x8, 0x531, x31	// Set all CSR bits
	csrrc x8, 0x531, x31	// Clear all CSR bits
	csrrw x8, 0x531, x16	// Restore CSR

// Testing CSR 0x532
	csrr x20, 0x532	// Read CSR
	li x21, -1
	csrrw x7, 0x532, x21	// Write all 1s to CSR
	csrrw x7, 0x532, x0	// Write all 0s to CSR
	csrrs x7, 0x532, x21	// Set all CSR bits
	csrrc x7, 0x532, x21	// Clear all CSR bits
	csrrw x7, 0x532, x20	// Restore CSR

// Testing CSR 0x533
	csrr x24, 0x533	// Read CSR
	li x30, -1
	csrrw x3, 0x533, x30	// Write all 1s to CSR
	csrrw x3, 0x533, x0	// Write all 0s to CSR
	csrrs x3, 0x533, x30	// Set all CSR bits
	csrrc x3, 0x533, x30	// Clear all CSR bits
	csrrw x3, 0x533, x24	// Restore CSR

// Testing CSR 0x534
	csrr x10, 0x534	// Read CSR
	li x9, -1
	csrrw x2, 0x534, x9	// Write all 1s to CSR
	csrrw x2, 0x534, x0	// Write all 0s to CSR
	csrrs x2, 0x534, x9	// Set all CSR bits
	csrrc x2, 0x534, x9	// Clear all CSR bits
	csrrw x2, 0x534, x10	// Restore CSR

// Testing CSR 0x535
	csrr x11, 0x535	// Read CSR
	li x26, -1
	csrrw x3, 0x535, x26	// Write all 1s to CSR
	csrrw x3, 0x535, x0	// Write all 0s to CSR
	csrrs x3, 0x535, x26	// Set all CSR bits
	csrrc x3, 0x535, x26	// Clear all CSR bits
	csrrw x3, 0x535, x11	// Restore CSR

// Testing CSR 0x536
	csrr x11, 0x536	// Read CSR
	li x30, -1
	csrrw x16, 0x536, x30	// Write all 1s to CSR
	csrrw x16, 0x536, x0	// Write all 0s to CSR
	csrrs x16, 0x536, x30	// Set all CSR bits
	csrrc x16, 0x536, x30	// Clear all CSR bits
	csrrw x16, 0x536, x11	// Restore CSR

// Testing CSR 0x537
	csrr x30, 0x537	// Read CSR
	li x17, -1
	csrrw x6, 0x537, x17	// Write all 1s to CSR
	csrrw x6, 0x537, x0	// Write all 0s to CSR
	csrrs x6, 0x537, x17	// Set all CSR bits
	csrrc x6, 0x537, x17	// Clear all CSR bits
	csrrw x6, 0x537, x30	// Restore CSR

// Testing CSR 0x538
	csrr x6, 0x538	// Read CSR
	li x21, -1
	csrrw x30, 0x538, x21	// Write all 1s to CSR
	csrrw x30, 0x538, x0	// Write all 0s to CSR
	csrrs x30, 0x538, x21	// Set all CSR bits
	csrrc x30, 0x538, x21	// Clear all CSR bits
	csrrw x30, 0x538, x6	// Restore CSR

// Testing CSR 0x539
	csrr x26, 0x539	// Read CSR
	li x8, -1
	csrrw x25, 0x539, x8	// Write all 1s to CSR
	csrrw x25, 0x539, x0	// Write all 0s to CSR
	csrrs x25, 0x539, x8	// Set all CSR bits
	csrrc x25, 0x539, x8	// Clear all CSR bits
	csrrw x25, 0x539, x26	// Restore CSR

// Testing CSR 0x53a
	csrr x18, 0x53a	// Read CSR
	li x2, -1
	csrrw x13, 0x53a, x2	// Write all 1s to CSR
	csrrw x13, 0x53a, x0	// Write all 0s to CSR
	csrrs x13, 0x53a, x2	// Set all CSR bits
	csrrc x13, 0x53a, x2	// Clear all CSR bits
	csrrw x13, 0x53a, x18	// Restore CSR

// Testing CSR 0x53b
	csrr x22, 0x53b	// Read CSR
	li x6, -1
	csrrw x9, 0x53b, x6	// Write all 1s to CSR
	csrrw x9, 0x53b, x0	// Write all 0s to CSR
	csrrs x9, 0x53b, x6	// Set all CSR bits
	csrrc x9, 0x53b, x6	// Clear all CSR bits
	csrrw x9, 0x53b, x22	// Restore CSR

// Testing CSR 0x53c
	csrr x31, 0x53c	// Read CSR
	li x12, -1
	csrrw x17, 0x53c, x12	// Write all 1s to CSR
	csrrw x17, 0x53c, x0	// Write all 0s to CSR
	csrrs x17, 0x53c, x12	// Set all CSR bits
	csrrc x17, 0x53c, x12	// Clear all CSR bits
	csrrw x17, 0x53c, x31	// Restore CSR

// Testing CSR 0x53d
	csrr x7, 0x53d	// Read CSR
	li x8, -1
	csrrw x23, 0x53d, x8	// Write all 1s to CSR
	csrrw x23, 0x53d, x0	// Write all 0s to CSR
	csrrs x23, 0x53d, x8	// Set all CSR bits
	csrrc x23, 0x53d, x8	// Clear all CSR bits
	csrrw x23, 0x53d, x7	// Restore CSR

// Testing CSR 0x53e
	csrr x17, 0x53e	// Read CSR
	li x28, -1
	csrrw x18, 0x53e, x28	// Write all 1s to CSR
	csrrw x18, 0x53e, x0	// Write all 0s to CSR
	csrrs x18, 0x53e, x28	// Set all CSR bits
	csrrc x18, 0x53e, x28	// Clear all CSR bits
	csrrw x18, 0x53e, x17	// Restore CSR

// Testing CSR 0x53f
	csrr x3, 0x53f	// Read CSR
	li x7, -1
	csrrw x19, 0x53f, x7	// Write all 1s to CSR
	csrrw x19, 0x53f, x0	// Write all 0s to CSR
	csrrs x19, 0x53f, x7	// Set all CSR bits
	csrrc x19, 0x53f, x7	// Clear all CSR bits
	csrrw x19, 0x53f, x3	// Restore CSR

// Testing CSR 0x540
	csrr x7, 0x540	// Read CSR
	li x27, -1
	csrrw x31, 0x540, x27	// Write all 1s to CSR
	csrrw x31, 0x540, x0	// Write all 0s to CSR
	csrrs x31, 0x540, x27	// Set all CSR bits
	csrrc x31, 0x540, x27	// Clear all CSR bits
	csrrw x31, 0x540, x7	// Restore CSR

// Testing CSR 0x541
	csrr x24, 0x541	// Read CSR
	li x20, -1
	csrrw x5, 0x541, x20	// Write all 1s to CSR
	csrrw x5, 0x541, x0	// Write all 0s to CSR
	csrrs x5, 0x541, x20	// Set all CSR bits
	csrrc x5, 0x541, x20	// Clear all CSR bits
	csrrw x5, 0x541, x24	// Restore CSR

// Testing CSR 0x542
	csrr x5, 0x542	// Read CSR
	li x17, -1
	csrrw x26, 0x542, x17	// Write all 1s to CSR
	csrrw x26, 0x542, x0	// Write all 0s to CSR
	csrrs x26, 0x542, x17	// Set all CSR bits
	csrrc x26, 0x542, x17	// Clear all CSR bits
	csrrw x26, 0x542, x5	// Restore CSR

// Testing CSR 0x543
	csrr x7, 0x543	// Read CSR
	li x13, -1
	csrrw x17, 0x543, x13	// Write all 1s to CSR
	csrrw x17, 0x543, x0	// Write all 0s to CSR
	csrrs x17, 0x543, x13	// Set all CSR bits
	csrrc x17, 0x543, x13	// Clear all CSR bits
	csrrw x17, 0x543, x7	// Restore CSR

// Testing CSR 0x544
	csrr x14, 0x544	// Read CSR
	li x17, -1
	csrrw x31, 0x544, x17	// Write all 1s to CSR
	csrrw x31, 0x544, x0	// Write all 0s to CSR
	csrrs x31, 0x544, x17	// Set all CSR bits
	csrrc x31, 0x544, x17	// Clear all CSR bits
	csrrw x31, 0x544, x14	// Restore CSR

// Testing CSR 0x545
	csrr x10, 0x545	// Read CSR
	li x30, -1
	csrrw x8, 0x545, x30	// Write all 1s to CSR
	csrrw x8, 0x545, x0	// Write all 0s to CSR
	csrrs x8, 0x545, x30	// Set all CSR bits
	csrrc x8, 0x545, x30	// Clear all CSR bits
	csrrw x8, 0x545, x10	// Restore CSR

// Testing CSR 0x546
	csrr x2, 0x546	// Read CSR
	li x31, -1
	csrrw x16, 0x546, x31	// Write all 1s to CSR
	csrrw x16, 0x546, x0	// Write all 0s to CSR
	csrrs x16, 0x546, x31	// Set all CSR bits
	csrrc x16, 0x546, x31	// Clear all CSR bits
	csrrw x16, 0x546, x2	// Restore CSR

// Testing CSR 0x547
	csrr x5, 0x547	// Read CSR
	li x12, -1
	csrrw x22, 0x547, x12	// Write all 1s to CSR
	csrrw x22, 0x547, x0	// Write all 0s to CSR
	csrrs x22, 0x547, x12	// Set all CSR bits
	csrrc x22, 0x547, x12	// Clear all CSR bits
	csrrw x22, 0x547, x5	// Restore CSR

// Testing CSR 0x548
	csrr x4, 0x548	// Read CSR
	li x14, -1
	csrrw x18, 0x548, x14	// Write all 1s to CSR
	csrrw x18, 0x548, x0	// Write all 0s to CSR
	csrrs x18, 0x548, x14	// Set all CSR bits
	csrrc x18, 0x548, x14	// Clear all CSR bits
	csrrw x18, 0x548, x4	// Restore CSR

// Testing CSR 0x549
	csrr x20, 0x549	// Read CSR
	li x3, -1
	csrrw x19, 0x549, x3	// Write all 1s to CSR
	csrrw x19, 0x549, x0	// Write all 0s to CSR
	csrrs x19, 0x549, x3	// Set all CSR bits
	csrrc x19, 0x549, x3	// Clear all CSR bits
	csrrw x19, 0x549, x20	// Restore CSR

// Testing CSR 0x54a
	csrr x21, 0x54a	// Read CSR
	li x29, -1
	csrrw x20, 0x54a, x29	// Write all 1s to CSR
	csrrw x20, 0x54a, x0	// Write all 0s to CSR
	csrrs x20, 0x54a, x29	// Set all CSR bits
	csrrc x20, 0x54a, x29	// Clear all CSR bits
	csrrw x20, 0x54a, x21	// Restore CSR

// Testing CSR 0x54b
	csrr x11, 0x54b	// Read CSR
	li x23, -1
	csrrw x8, 0x54b, x23	// Write all 1s to CSR
	csrrw x8, 0x54b, x0	// Write all 0s to CSR
	csrrs x8, 0x54b, x23	// Set all CSR bits
	csrrc x8, 0x54b, x23	// Clear all CSR bits
	csrrw x8, 0x54b, x11	// Restore CSR

// Testing CSR 0x54c
	csrr x17, 0x54c	// Read CSR
	li x25, -1
	csrrw x28, 0x54c, x25	// Write all 1s to CSR
	csrrw x28, 0x54c, x0	// Write all 0s to CSR
	csrrs x28, 0x54c, x25	// Set all CSR bits
	csrrc x28, 0x54c, x25	// Clear all CSR bits
	csrrw x28, 0x54c, x17	// Restore CSR

// Testing CSR 0x54d
	csrr x27, 0x54d	// Read CSR
	li x8, -1
	csrrw x22, 0x54d, x8	// Write all 1s to CSR
	csrrw x22, 0x54d, x0	// Write all 0s to CSR
	csrrs x22, 0x54d, x8	// Set all CSR bits
	csrrc x22, 0x54d, x8	// Clear all CSR bits
	csrrw x22, 0x54d, x27	// Restore CSR

// Testing CSR 0x54e
	csrr x4, 0x54e	// Read CSR
	li x2, -1
	csrrw x29, 0x54e, x2	// Write all 1s to CSR
	csrrw x29, 0x54e, x0	// Write all 0s to CSR
	csrrs x29, 0x54e, x2	// Set all CSR bits
	csrrc x29, 0x54e, x2	// Clear all CSR bits
	csrrw x29, 0x54e, x4	// Restore CSR

// Testing CSR 0x54f
	csrr x22, 0x54f	// Read CSR
	li x23, -1
	csrrw x26, 0x54f, x23	// Write all 1s to CSR
	csrrw x26, 0x54f, x0	// Write all 0s to CSR
	csrrs x26, 0x54f, x23	// Set all CSR bits
	csrrc x26, 0x54f, x23	// Clear all CSR bits
	csrrw x26, 0x54f, x22	// Restore CSR

// Testing CSR 0x550
	csrr x10, 0x550	// Read CSR
	li x14, -1
	csrrw x30, 0x550, x14	// Write all 1s to CSR
	csrrw x30, 0x550, x0	// Write all 0s to CSR
	csrrs x30, 0x550, x14	// Set all CSR bits
	csrrc x30, 0x550, x14	// Clear all CSR bits
	csrrw x30, 0x550, x10	// Restore CSR

// Testing CSR 0x551
	csrr x12, 0x551	// Read CSR
	li x4, -1
	csrrw x27, 0x551, x4	// Write all 1s to CSR
	csrrw x27, 0x551, x0	// Write all 0s to CSR
	csrrs x27, 0x551, x4	// Set all CSR bits
	csrrc x27, 0x551, x4	// Clear all CSR bits
	csrrw x27, 0x551, x12	// Restore CSR

// Testing CSR 0x552
	csrr x1, 0x552	// Read CSR
	li x4, -1
	csrrw x26, 0x552, x4	// Write all 1s to CSR
	csrrw x26, 0x552, x0	// Write all 0s to CSR
	csrrs x26, 0x552, x4	// Set all CSR bits
	csrrc x26, 0x552, x4	// Clear all CSR bits
	csrrw x26, 0x552, x1	// Restore CSR

// Testing CSR 0x553
	csrr x27, 0x553	// Read CSR
	li x2, -1
	csrrw x9, 0x553, x2	// Write all 1s to CSR
	csrrw x9, 0x553, x0	// Write all 0s to CSR
	csrrs x9, 0x553, x2	// Set all CSR bits
	csrrc x9, 0x553, x2	// Clear all CSR bits
	csrrw x9, 0x553, x27	// Restore CSR

// Testing CSR 0x554
	csrr x12, 0x554	// Read CSR
	li x29, -1
	csrrw x17, 0x554, x29	// Write all 1s to CSR
	csrrw x17, 0x554, x0	// Write all 0s to CSR
	csrrs x17, 0x554, x29	// Set all CSR bits
	csrrc x17, 0x554, x29	// Clear all CSR bits
	csrrw x17, 0x554, x12	// Restore CSR

// Testing CSR 0x555
	csrr x20, 0x555	// Read CSR
	li x22, -1
	csrrw x12, 0x555, x22	// Write all 1s to CSR
	csrrw x12, 0x555, x0	// Write all 0s to CSR
	csrrs x12, 0x555, x22	// Set all CSR bits
	csrrc x12, 0x555, x22	// Clear all CSR bits
	csrrw x12, 0x555, x20	// Restore CSR

// Testing CSR 0x556
	csrr x9, 0x556	// Read CSR
	li x4, -1
	csrrw x3, 0x556, x4	// Write all 1s to CSR
	csrrw x3, 0x556, x0	// Write all 0s to CSR
	csrrs x3, 0x556, x4	// Set all CSR bits
	csrrc x3, 0x556, x4	// Clear all CSR bits
	csrrw x3, 0x556, x9	// Restore CSR

// Testing CSR 0x557
	csrr x26, 0x557	// Read CSR
	li x8, -1
	csrrw x10, 0x557, x8	// Write all 1s to CSR
	csrrw x10, 0x557, x0	// Write all 0s to CSR
	csrrs x10, 0x557, x8	// Set all CSR bits
	csrrc x10, 0x557, x8	// Clear all CSR bits
	csrrw x10, 0x557, x26	// Restore CSR

// Testing CSR 0x558
	csrr x9, 0x558	// Read CSR
	li x19, -1
	csrrw x21, 0x558, x19	// Write all 1s to CSR
	csrrw x21, 0x558, x0	// Write all 0s to CSR
	csrrs x21, 0x558, x19	// Set all CSR bits
	csrrc x21, 0x558, x19	// Clear all CSR bits
	csrrw x21, 0x558, x9	// Restore CSR

// Testing CSR 0x559
	csrr x26, 0x559	// Read CSR
	li x17, -1
	csrrw x2, 0x559, x17	// Write all 1s to CSR
	csrrw x2, 0x559, x0	// Write all 0s to CSR
	csrrs x2, 0x559, x17	// Set all CSR bits
	csrrc x2, 0x559, x17	// Clear all CSR bits
	csrrw x2, 0x559, x26	// Restore CSR

// Testing CSR 0x55a
	csrr x13, 0x55a	// Read CSR
	li x21, -1
	csrrw x17, 0x55a, x21	// Write all 1s to CSR
	csrrw x17, 0x55a, x0	// Write all 0s to CSR
	csrrs x17, 0x55a, x21	// Set all CSR bits
	csrrc x17, 0x55a, x21	// Clear all CSR bits
	csrrw x17, 0x55a, x13	// Restore CSR

// Testing CSR 0x55b
	csrr x5, 0x55b	// Read CSR
	li x4, -1
	csrrw x10, 0x55b, x4	// Write all 1s to CSR
	csrrw x10, 0x55b, x0	// Write all 0s to CSR
	csrrs x10, 0x55b, x4	// Set all CSR bits
	csrrc x10, 0x55b, x4	// Clear all CSR bits
	csrrw x10, 0x55b, x5	// Restore CSR

// Testing CSR 0x55c
	csrr x23, 0x55c	// Read CSR
	li x2, -1
	csrrw x22, 0x55c, x2	// Write all 1s to CSR
	csrrw x22, 0x55c, x0	// Write all 0s to CSR
	csrrs x22, 0x55c, x2	// Set all CSR bits
	csrrc x22, 0x55c, x2	// Clear all CSR bits
	csrrw x22, 0x55c, x23	// Restore CSR

// Testing CSR 0x55d
	csrr x4, 0x55d	// Read CSR
	li x21, -1
	csrrw x8, 0x55d, x21	// Write all 1s to CSR
	csrrw x8, 0x55d, x0	// Write all 0s to CSR
	csrrs x8, 0x55d, x21	// Set all CSR bits
	csrrc x8, 0x55d, x21	// Clear all CSR bits
	csrrw x8, 0x55d, x4	// Restore CSR

// Testing CSR 0x55e
	csrr x21, 0x55e	// Read CSR
	li x19, -1
	csrrw x5, 0x55e, x19	// Write all 1s to CSR
	csrrw x5, 0x55e, x0	// Write all 0s to CSR
	csrrs x5, 0x55e, x19	// Set all CSR bits
	csrrc x5, 0x55e, x19	// Clear all CSR bits
	csrrw x5, 0x55e, x21	// Restore CSR

// Testing CSR 0x55f
	csrr x2, 0x55f	// Read CSR
	li x30, -1
	csrrw x28, 0x55f, x30	// Write all 1s to CSR
	csrrw x28, 0x55f, x0	// Write all 0s to CSR
	csrrs x28, 0x55f, x30	// Set all CSR bits
	csrrc x28, 0x55f, x30	// Clear all CSR bits
	csrrw x28, 0x55f, x2	// Restore CSR

// Testing CSR 0x560
	csrr x15, 0x560	// Read CSR
	li x3, -1
	csrrw x11, 0x560, x3	// Write all 1s to CSR
	csrrw x11, 0x560, x0	// Write all 0s to CSR
	csrrs x11, 0x560, x3	// Set all CSR bits
	csrrc x11, 0x560, x3	// Clear all CSR bits
	csrrw x11, 0x560, x15	// Restore CSR

// Testing CSR 0x561
	csrr x4, 0x561	// Read CSR
	li x12, -1
	csrrw x23, 0x561, x12	// Write all 1s to CSR
	csrrw x23, 0x561, x0	// Write all 0s to CSR
	csrrs x23, 0x561, x12	// Set all CSR bits
	csrrc x23, 0x561, x12	// Clear all CSR bits
	csrrw x23, 0x561, x4	// Restore CSR

// Testing CSR 0x562
	csrr x11, 0x562	// Read CSR
	li x2, -1
	csrrw x29, 0x562, x2	// Write all 1s to CSR
	csrrw x29, 0x562, x0	// Write all 0s to CSR
	csrrs x29, 0x562, x2	// Set all CSR bits
	csrrc x29, 0x562, x2	// Clear all CSR bits
	csrrw x29, 0x562, x11	// Restore CSR

// Testing CSR 0x563
	csrr x14, 0x563	// Read CSR
	li x3, -1
	csrrw x9, 0x563, x3	// Write all 1s to CSR
	csrrw x9, 0x563, x0	// Write all 0s to CSR
	csrrs x9, 0x563, x3	// Set all CSR bits
	csrrc x9, 0x563, x3	// Clear all CSR bits
	csrrw x9, 0x563, x14	// Restore CSR

// Testing CSR 0x564
	csrr x18, 0x564	// Read CSR
	li x13, -1
	csrrw x9, 0x564, x13	// Write all 1s to CSR
	csrrw x9, 0x564, x0	// Write all 0s to CSR
	csrrs x9, 0x564, x13	// Set all CSR bits
	csrrc x9, 0x564, x13	// Clear all CSR bits
	csrrw x9, 0x564, x18	// Restore CSR

// Testing CSR 0x565
	csrr x14, 0x565	// Read CSR
	li x4, -1
	csrrw x28, 0x565, x4	// Write all 1s to CSR
	csrrw x28, 0x565, x0	// Write all 0s to CSR
	csrrs x28, 0x565, x4	// Set all CSR bits
	csrrc x28, 0x565, x4	// Clear all CSR bits
	csrrw x28, 0x565, x14	// Restore CSR

// Testing CSR 0x566
	csrr x17, 0x566	// Read CSR
	li x29, -1
	csrrw x18, 0x566, x29	// Write all 1s to CSR
	csrrw x18, 0x566, x0	// Write all 0s to CSR
	csrrs x18, 0x566, x29	// Set all CSR bits
	csrrc x18, 0x566, x29	// Clear all CSR bits
	csrrw x18, 0x566, x17	// Restore CSR

// Testing CSR 0x567
	csrr x12, 0x567	// Read CSR
	li x13, -1
	csrrw x11, 0x567, x13	// Write all 1s to CSR
	csrrw x11, 0x567, x0	// Write all 0s to CSR
	csrrs x11, 0x567, x13	// Set all CSR bits
	csrrc x11, 0x567, x13	// Clear all CSR bits
	csrrw x11, 0x567, x12	// Restore CSR

// Testing CSR 0x568
	csrr x30, 0x568	// Read CSR
	li x26, -1
	csrrw x18, 0x568, x26	// Write all 1s to CSR
	csrrw x18, 0x568, x0	// Write all 0s to CSR
	csrrs x18, 0x568, x26	// Set all CSR bits
	csrrc x18, 0x568, x26	// Clear all CSR bits
	csrrw x18, 0x568, x30	// Restore CSR

// Testing CSR 0x569
	csrr x22, 0x569	// Read CSR
	li x26, -1
	csrrw x7, 0x569, x26	// Write all 1s to CSR
	csrrw x7, 0x569, x0	// Write all 0s to CSR
	csrrs x7, 0x569, x26	// Set all CSR bits
	csrrc x7, 0x569, x26	// Clear all CSR bits
	csrrw x7, 0x569, x22	// Restore CSR

// Testing CSR 0x56a
	csrr x23, 0x56a	// Read CSR
	li x5, -1
	csrrw x22, 0x56a, x5	// Write all 1s to CSR
	csrrw x22, 0x56a, x0	// Write all 0s to CSR
	csrrs x22, 0x56a, x5	// Set all CSR bits
	csrrc x22, 0x56a, x5	// Clear all CSR bits
	csrrw x22, 0x56a, x23	// Restore CSR

// Testing CSR 0x56b
	csrr x27, 0x56b	// Read CSR
	li x30, -1
	csrrw x24, 0x56b, x30	// Write all 1s to CSR
	csrrw x24, 0x56b, x0	// Write all 0s to CSR
	csrrs x24, 0x56b, x30	// Set all CSR bits
	csrrc x24, 0x56b, x30	// Clear all CSR bits
	csrrw x24, 0x56b, x27	// Restore CSR

// Testing CSR 0x56c
	csrr x14, 0x56c	// Read CSR
	li x15, -1
	csrrw x16, 0x56c, x15	// Write all 1s to CSR
	csrrw x16, 0x56c, x0	// Write all 0s to CSR
	csrrs x16, 0x56c, x15	// Set all CSR bits
	csrrc x16, 0x56c, x15	// Clear all CSR bits
	csrrw x16, 0x56c, x14	// Restore CSR

// Testing CSR 0x56d
	csrr x15, 0x56d	// Read CSR
	li x28, -1
	csrrw x26, 0x56d, x28	// Write all 1s to CSR
	csrrw x26, 0x56d, x0	// Write all 0s to CSR
	csrrs x26, 0x56d, x28	// Set all CSR bits
	csrrc x26, 0x56d, x28	// Clear all CSR bits
	csrrw x26, 0x56d, x15	// Restore CSR

// Testing CSR 0x56e
	csrr x16, 0x56e	// Read CSR
	li x28, -1
	csrrw x17, 0x56e, x28	// Write all 1s to CSR
	csrrw x17, 0x56e, x0	// Write all 0s to CSR
	csrrs x17, 0x56e, x28	// Set all CSR bits
	csrrc x17, 0x56e, x28	// Clear all CSR bits
	csrrw x17, 0x56e, x16	// Restore CSR

// Testing CSR 0x56f
	csrr x26, 0x56f	// Read CSR
	li x21, -1
	csrrw x25, 0x56f, x21	// Write all 1s to CSR
	csrrw x25, 0x56f, x0	// Write all 0s to CSR
	csrrs x25, 0x56f, x21	// Set all CSR bits
	csrrc x25, 0x56f, x21	// Clear all CSR bits
	csrrw x25, 0x56f, x26	// Restore CSR

// Testing CSR 0x570
	csrr x27, 0x570	// Read CSR
	li x11, -1
	csrrw x28, 0x570, x11	// Write all 1s to CSR
	csrrw x28, 0x570, x0	// Write all 0s to CSR
	csrrs x28, 0x570, x11	// Set all CSR bits
	csrrc x28, 0x570, x11	// Clear all CSR bits
	csrrw x28, 0x570, x27	// Restore CSR

// Testing CSR 0x571
	csrr x20, 0x571	// Read CSR
	li x27, -1
	csrrw x2, 0x571, x27	// Write all 1s to CSR
	csrrw x2, 0x571, x0	// Write all 0s to CSR
	csrrs x2, 0x571, x27	// Set all CSR bits
	csrrc x2, 0x571, x27	// Clear all CSR bits
	csrrw x2, 0x571, x20	// Restore CSR

// Testing CSR 0x572
	csrr x28, 0x572	// Read CSR
	li x6, -1
	csrrw x5, 0x572, x6	// Write all 1s to CSR
	csrrw x5, 0x572, x0	// Write all 0s to CSR
	csrrs x5, 0x572, x6	// Set all CSR bits
	csrrc x5, 0x572, x6	// Clear all CSR bits
	csrrw x5, 0x572, x28	// Restore CSR

// Testing CSR 0x573
	csrr x5, 0x573	// Read CSR
	li x17, -1
	csrrw x27, 0x573, x17	// Write all 1s to CSR
	csrrw x27, 0x573, x0	// Write all 0s to CSR
	csrrs x27, 0x573, x17	// Set all CSR bits
	csrrc x27, 0x573, x17	// Clear all CSR bits
	csrrw x27, 0x573, x5	// Restore CSR

// Testing CSR 0x574
	csrr x2, 0x574	// Read CSR
	li x18, -1
	csrrw x23, 0x574, x18	// Write all 1s to CSR
	csrrw x23, 0x574, x0	// Write all 0s to CSR
	csrrs x23, 0x574, x18	// Set all CSR bits
	csrrc x23, 0x574, x18	// Clear all CSR bits
	csrrw x23, 0x574, x2	// Restore CSR

// Testing CSR 0x575
	csrr x15, 0x575	// Read CSR
	li x27, -1
	csrrw x12, 0x575, x27	// Write all 1s to CSR
	csrrw x12, 0x575, x0	// Write all 0s to CSR
	csrrs x12, 0x575, x27	// Set all CSR bits
	csrrc x12, 0x575, x27	// Clear all CSR bits
	csrrw x12, 0x575, x15	// Restore CSR

// Testing CSR 0x576
	csrr x19, 0x576	// Read CSR
	li x14, -1
	csrrw x7, 0x576, x14	// Write all 1s to CSR
	csrrw x7, 0x576, x0	// Write all 0s to CSR
	csrrs x7, 0x576, x14	// Set all CSR bits
	csrrc x7, 0x576, x14	// Clear all CSR bits
	csrrw x7, 0x576, x19	// Restore CSR

// Testing CSR 0x577
	csrr x21, 0x577	// Read CSR
	li x8, -1
	csrrw x25, 0x577, x8	// Write all 1s to CSR
	csrrw x25, 0x577, x0	// Write all 0s to CSR
	csrrs x25, 0x577, x8	// Set all CSR bits
	csrrc x25, 0x577, x8	// Clear all CSR bits
	csrrw x25, 0x577, x21	// Restore CSR

// Testing CSR 0x578
	csrr x10, 0x578	// Read CSR
	li x11, -1
	csrrw x21, 0x578, x11	// Write all 1s to CSR
	csrrw x21, 0x578, x0	// Write all 0s to CSR
	csrrs x21, 0x578, x11	// Set all CSR bits
	csrrc x21, 0x578, x11	// Clear all CSR bits
	csrrw x21, 0x578, x10	// Restore CSR

// Testing CSR 0x579
	csrr x30, 0x579	// Read CSR
	li x5, -1
	csrrw x16, 0x579, x5	// Write all 1s to CSR
	csrrw x16, 0x579, x0	// Write all 0s to CSR
	csrrs x16, 0x579, x5	// Set all CSR bits
	csrrc x16, 0x579, x5	// Clear all CSR bits
	csrrw x16, 0x579, x30	// Restore CSR

// Testing CSR 0x57a
	csrr x17, 0x57a	// Read CSR
	li x10, -1
	csrrw x11, 0x57a, x10	// Write all 1s to CSR
	csrrw x11, 0x57a, x0	// Write all 0s to CSR
	csrrs x11, 0x57a, x10	// Set all CSR bits
	csrrc x11, 0x57a, x10	// Clear all CSR bits
	csrrw x11, 0x57a, x17	// Restore CSR

// Testing CSR 0x57b
	csrr x6, 0x57b	// Read CSR
	li x29, -1
	csrrw x4, 0x57b, x29	// Write all 1s to CSR
	csrrw x4, 0x57b, x0	// Write all 0s to CSR
	csrrs x4, 0x57b, x29	// Set all CSR bits
	csrrc x4, 0x57b, x29	// Clear all CSR bits
	csrrw x4, 0x57b, x6	// Restore CSR

// Testing CSR 0x57c
	csrr x31, 0x57c	// Read CSR
	li x9, -1
	csrrw x4, 0x57c, x9	// Write all 1s to CSR
	csrrw x4, 0x57c, x0	// Write all 0s to CSR
	csrrs x4, 0x57c, x9	// Set all CSR bits
	csrrc x4, 0x57c, x9	// Clear all CSR bits
	csrrw x4, 0x57c, x31	// Restore CSR

// Testing CSR 0x57d
	csrr x2, 0x57d	// Read CSR
	li x5, -1
	csrrw x9, 0x57d, x5	// Write all 1s to CSR
	csrrw x9, 0x57d, x0	// Write all 0s to CSR
	csrrs x9, 0x57d, x5	// Set all CSR bits
	csrrc x9, 0x57d, x5	// Clear all CSR bits
	csrrw x9, 0x57d, x2	// Restore CSR

// Testing CSR 0x57e
	csrr x19, 0x57e	// Read CSR
	li x24, -1
	csrrw x18, 0x57e, x24	// Write all 1s to CSR
	csrrw x18, 0x57e, x0	// Write all 0s to CSR
	csrrs x18, 0x57e, x24	// Set all CSR bits
	csrrc x18, 0x57e, x24	// Clear all CSR bits
	csrrw x18, 0x57e, x19	// Restore CSR

// Testing CSR 0x57f
	csrr x5, 0x57f	// Read CSR
	li x22, -1
	csrrw x30, 0x57f, x22	// Write all 1s to CSR
	csrrw x30, 0x57f, x0	// Write all 0s to CSR
	csrrs x30, 0x57f, x22	// Set all CSR bits
	csrrc x30, 0x57f, x22	// Clear all CSR bits
	csrrw x30, 0x57f, x5	// Restore CSR

// Testing CSR 0x580
	csrr x2, 0x580	// Read CSR
	li x1, -1
	csrrw x25, 0x580, x1	// Write all 1s to CSR
	csrrw x25, 0x580, x0	// Write all 0s to CSR
	csrrs x25, 0x580, x1	// Set all CSR bits
	csrrc x25, 0x580, x1	// Clear all CSR bits
	csrrw x25, 0x580, x2	// Restore CSR

// Testing CSR 0x581
	csrr x24, 0x581	// Read CSR
	li x6, -1
	csrrw x27, 0x581, x6	// Write all 1s to CSR
	csrrw x27, 0x581, x0	// Write all 0s to CSR
	csrrs x27, 0x581, x6	// Set all CSR bits
	csrrc x27, 0x581, x6	// Clear all CSR bits
	csrrw x27, 0x581, x24	// Restore CSR

// Testing CSR 0x582
	csrr x2, 0x582	// Read CSR
	li x23, -1
	csrrw x4, 0x582, x23	// Write all 1s to CSR
	csrrw x4, 0x582, x0	// Write all 0s to CSR
	csrrs x4, 0x582, x23	// Set all CSR bits
	csrrc x4, 0x582, x23	// Clear all CSR bits
	csrrw x4, 0x582, x2	// Restore CSR

// Testing CSR 0x583
	csrr x2, 0x583	// Read CSR
	li x15, -1
	csrrw x12, 0x583, x15	// Write all 1s to CSR
	csrrw x12, 0x583, x0	// Write all 0s to CSR
	csrrs x12, 0x583, x15	// Set all CSR bits
	csrrc x12, 0x583, x15	// Clear all CSR bits
	csrrw x12, 0x583, x2	// Restore CSR

// Testing CSR 0x584
	csrr x2, 0x584	// Read CSR
	li x1, -1
	csrrw x25, 0x584, x1	// Write all 1s to CSR
	csrrw x25, 0x584, x0	// Write all 0s to CSR
	csrrs x25, 0x584, x1	// Set all CSR bits
	csrrc x25, 0x584, x1	// Clear all CSR bits
	csrrw x25, 0x584, x2	// Restore CSR

// Testing CSR 0x585
	csrr x9, 0x585	// Read CSR
	li x13, -1
	csrrw x23, 0x585, x13	// Write all 1s to CSR
	csrrw x23, 0x585, x0	// Write all 0s to CSR
	csrrs x23, 0x585, x13	// Set all CSR bits
	csrrc x23, 0x585, x13	// Clear all CSR bits
	csrrw x23, 0x585, x9	// Restore CSR

// Testing CSR 0x586
	csrr x2, 0x586	// Read CSR
	li x6, -1
	csrrw x19, 0x586, x6	// Write all 1s to CSR
	csrrw x19, 0x586, x0	// Write all 0s to CSR
	csrrs x19, 0x586, x6	// Set all CSR bits
	csrrc x19, 0x586, x6	// Clear all CSR bits
	csrrw x19, 0x586, x2	// Restore CSR

// Testing CSR 0x587
	csrr x24, 0x587	// Read CSR
	li x13, -1
	csrrw x26, 0x587, x13	// Write all 1s to CSR
	csrrw x26, 0x587, x0	// Write all 0s to CSR
	csrrs x26, 0x587, x13	// Set all CSR bits
	csrrc x26, 0x587, x13	// Clear all CSR bits
	csrrw x26, 0x587, x24	// Restore CSR

// Testing CSR 0x588
	csrr x29, 0x588	// Read CSR
	li x9, -1
	csrrw x3, 0x588, x9	// Write all 1s to CSR
	csrrw x3, 0x588, x0	// Write all 0s to CSR
	csrrs x3, 0x588, x9	// Set all CSR bits
	csrrc x3, 0x588, x9	// Clear all CSR bits
	csrrw x3, 0x588, x29	// Restore CSR

// Testing CSR 0x589
	csrr x19, 0x589	// Read CSR
	li x9, -1
	csrrw x3, 0x589, x9	// Write all 1s to CSR
	csrrw x3, 0x589, x0	// Write all 0s to CSR
	csrrs x3, 0x589, x9	// Set all CSR bits
	csrrc x3, 0x589, x9	// Clear all CSR bits
	csrrw x3, 0x589, x19	// Restore CSR

// Testing CSR 0x58a
	csrr x15, 0x58a	// Read CSR
	li x28, -1
	csrrw x27, 0x58a, x28	// Write all 1s to CSR
	csrrw x27, 0x58a, x0	// Write all 0s to CSR
	csrrs x27, 0x58a, x28	// Set all CSR bits
	csrrc x27, 0x58a, x28	// Clear all CSR bits
	csrrw x27, 0x58a, x15	// Restore CSR

// Testing CSR 0x58b
	csrr x6, 0x58b	// Read CSR
	li x26, -1
	csrrw x8, 0x58b, x26	// Write all 1s to CSR
	csrrw x8, 0x58b, x0	// Write all 0s to CSR
	csrrs x8, 0x58b, x26	// Set all CSR bits
	csrrc x8, 0x58b, x26	// Clear all CSR bits
	csrrw x8, 0x58b, x6	// Restore CSR

// Testing CSR 0x58c
	csrr x27, 0x58c	// Read CSR
	li x16, -1
	csrrw x9, 0x58c, x16	// Write all 1s to CSR
	csrrw x9, 0x58c, x0	// Write all 0s to CSR
	csrrs x9, 0x58c, x16	// Set all CSR bits
	csrrc x9, 0x58c, x16	// Clear all CSR bits
	csrrw x9, 0x58c, x27	// Restore CSR

// Testing CSR 0x58d
	csrr x26, 0x58d	// Read CSR
	li x16, -1
	csrrw x22, 0x58d, x16	// Write all 1s to CSR
	csrrw x22, 0x58d, x0	// Write all 0s to CSR
	csrrs x22, 0x58d, x16	// Set all CSR bits
	csrrc x22, 0x58d, x16	// Clear all CSR bits
	csrrw x22, 0x58d, x26	// Restore CSR

// Testing CSR 0x58e
	csrr x21, 0x58e	// Read CSR
	li x15, -1
	csrrw x4, 0x58e, x15	// Write all 1s to CSR
	csrrw x4, 0x58e, x0	// Write all 0s to CSR
	csrrs x4, 0x58e, x15	// Set all CSR bits
	csrrc x4, 0x58e, x15	// Clear all CSR bits
	csrrw x4, 0x58e, x21	// Restore CSR

// Testing CSR 0x58f
	csrr x27, 0x58f	// Read CSR
	li x13, -1
	csrrw x6, 0x58f, x13	// Write all 1s to CSR
	csrrw x6, 0x58f, x0	// Write all 0s to CSR
	csrrs x6, 0x58f, x13	// Set all CSR bits
	csrrc x6, 0x58f, x13	// Clear all CSR bits
	csrrw x6, 0x58f, x27	// Restore CSR

// Testing CSR 0x590
	csrr x11, 0x590	// Read CSR
	li x19, -1
	csrrw x23, 0x590, x19	// Write all 1s to CSR
	csrrw x23, 0x590, x0	// Write all 0s to CSR
	csrrs x23, 0x590, x19	// Set all CSR bits
	csrrc x23, 0x590, x19	// Clear all CSR bits
	csrrw x23, 0x590, x11	// Restore CSR

// Testing CSR 0x591
	csrr x26, 0x591	// Read CSR
	li x13, -1
	csrrw x7, 0x591, x13	// Write all 1s to CSR
	csrrw x7, 0x591, x0	// Write all 0s to CSR
	csrrs x7, 0x591, x13	// Set all CSR bits
	csrrc x7, 0x591, x13	// Clear all CSR bits
	csrrw x7, 0x591, x26	// Restore CSR

// Testing CSR 0x592
	csrr x26, 0x592	// Read CSR
	li x12, -1
	csrrw x21, 0x592, x12	// Write all 1s to CSR
	csrrw x21, 0x592, x0	// Write all 0s to CSR
	csrrs x21, 0x592, x12	// Set all CSR bits
	csrrc x21, 0x592, x12	// Clear all CSR bits
	csrrw x21, 0x592, x26	// Restore CSR

// Testing CSR 0x593
	csrr x12, 0x593	// Read CSR
	li x2, -1
	csrrw x31, 0x593, x2	// Write all 1s to CSR
	csrrw x31, 0x593, x0	// Write all 0s to CSR
	csrrs x31, 0x593, x2	// Set all CSR bits
	csrrc x31, 0x593, x2	// Clear all CSR bits
	csrrw x31, 0x593, x12	// Restore CSR

// Testing CSR 0x594
	csrr x16, 0x594	// Read CSR
	li x7, -1
	csrrw x17, 0x594, x7	// Write all 1s to CSR
	csrrw x17, 0x594, x0	// Write all 0s to CSR
	csrrs x17, 0x594, x7	// Set all CSR bits
	csrrc x17, 0x594, x7	// Clear all CSR bits
	csrrw x17, 0x594, x16	// Restore CSR

// Testing CSR 0x595
	csrr x9, 0x595	// Read CSR
	li x2, -1
	csrrw x4, 0x595, x2	// Write all 1s to CSR
	csrrw x4, 0x595, x0	// Write all 0s to CSR
	csrrs x4, 0x595, x2	// Set all CSR bits
	csrrc x4, 0x595, x2	// Clear all CSR bits
	csrrw x4, 0x595, x9	// Restore CSR

// Testing CSR 0x596
	csrr x2, 0x596	// Read CSR
	li x25, -1
	csrrw x26, 0x596, x25	// Write all 1s to CSR
	csrrw x26, 0x596, x0	// Write all 0s to CSR
	csrrs x26, 0x596, x25	// Set all CSR bits
	csrrc x26, 0x596, x25	// Clear all CSR bits
	csrrw x26, 0x596, x2	// Restore CSR

// Testing CSR 0x597
	csrr x31, 0x597	// Read CSR
	li x9, -1
	csrrw x3, 0x597, x9	// Write all 1s to CSR
	csrrw x3, 0x597, x0	// Write all 0s to CSR
	csrrs x3, 0x597, x9	// Set all CSR bits
	csrrc x3, 0x597, x9	// Clear all CSR bits
	csrrw x3, 0x597, x31	// Restore CSR

// Testing CSR 0x598
	csrr x27, 0x598	// Read CSR
	li x29, -1
	csrrw x10, 0x598, x29	// Write all 1s to CSR
	csrrw x10, 0x598, x0	// Write all 0s to CSR
	csrrs x10, 0x598, x29	// Set all CSR bits
	csrrc x10, 0x598, x29	// Clear all CSR bits
	csrrw x10, 0x598, x27	// Restore CSR

// Testing CSR 0x599
	csrr x17, 0x599	// Read CSR
	li x29, -1
	csrrw x14, 0x599, x29	// Write all 1s to CSR
	csrrw x14, 0x599, x0	// Write all 0s to CSR
	csrrs x14, 0x599, x29	// Set all CSR bits
	csrrc x14, 0x599, x29	// Clear all CSR bits
	csrrw x14, 0x599, x17	// Restore CSR

// Testing CSR 0x59a
	csrr x25, 0x59a	// Read CSR
	li x13, -1
	csrrw x4, 0x59a, x13	// Write all 1s to CSR
	csrrw x4, 0x59a, x0	// Write all 0s to CSR
	csrrs x4, 0x59a, x13	// Set all CSR bits
	csrrc x4, 0x59a, x13	// Clear all CSR bits
	csrrw x4, 0x59a, x25	// Restore CSR

// Testing CSR 0x59b
	csrr x30, 0x59b	// Read CSR
	li x18, -1
	csrrw x17, 0x59b, x18	// Write all 1s to CSR
	csrrw x17, 0x59b, x0	// Write all 0s to CSR
	csrrs x17, 0x59b, x18	// Set all CSR bits
	csrrc x17, 0x59b, x18	// Clear all CSR bits
	csrrw x17, 0x59b, x30	// Restore CSR

// Testing CSR 0x59c
	csrr x29, 0x59c	// Read CSR
	li x25, -1
	csrrw x1, 0x59c, x25	// Write all 1s to CSR
	csrrw x1, 0x59c, x0	// Write all 0s to CSR
	csrrs x1, 0x59c, x25	// Set all CSR bits
	csrrc x1, 0x59c, x25	// Clear all CSR bits
	csrrw x1, 0x59c, x29	// Restore CSR

// Testing CSR 0x59d
	csrr x1, 0x59d	// Read CSR
	li x29, -1
	csrrw x21, 0x59d, x29	// Write all 1s to CSR
	csrrw x21, 0x59d, x0	// Write all 0s to CSR
	csrrs x21, 0x59d, x29	// Set all CSR bits
	csrrc x21, 0x59d, x29	// Clear all CSR bits
	csrrw x21, 0x59d, x1	// Restore CSR

// Testing CSR 0x59e
	csrr x24, 0x59e	// Read CSR
	li x20, -1
	csrrw x30, 0x59e, x20	// Write all 1s to CSR
	csrrw x30, 0x59e, x0	// Write all 0s to CSR
	csrrs x30, 0x59e, x20	// Set all CSR bits
	csrrc x30, 0x59e, x20	// Clear all CSR bits
	csrrw x30, 0x59e, x24	// Restore CSR

// Testing CSR 0x59f
	csrr x21, 0x59f	// Read CSR
	li x16, -1
	csrrw x1, 0x59f, x16	// Write all 1s to CSR
	csrrw x1, 0x59f, x0	// Write all 0s to CSR
	csrrs x1, 0x59f, x16	// Set all CSR bits
	csrrc x1, 0x59f, x16	// Clear all CSR bits
	csrrw x1, 0x59f, x21	// Restore CSR

// Testing CSR 0x5a0
	csrr x25, 0x5a0	// Read CSR
	li x13, -1
	csrrw x24, 0x5a0, x13	// Write all 1s to CSR
	csrrw x24, 0x5a0, x0	// Write all 0s to CSR
	csrrs x24, 0x5a0, x13	// Set all CSR bits
	csrrc x24, 0x5a0, x13	// Clear all CSR bits
	csrrw x24, 0x5a0, x25	// Restore CSR

// Testing CSR 0x5a1
	csrr x18, 0x5a1	// Read CSR
	li x26, -1
	csrrw x6, 0x5a1, x26	// Write all 1s to CSR
	csrrw x6, 0x5a1, x0	// Write all 0s to CSR
	csrrs x6, 0x5a1, x26	// Set all CSR bits
	csrrc x6, 0x5a1, x26	// Clear all CSR bits
	csrrw x6, 0x5a1, x18	// Restore CSR

// Testing CSR 0x5a2
	csrr x20, 0x5a2	// Read CSR
	li x7, -1
	csrrw x25, 0x5a2, x7	// Write all 1s to CSR
	csrrw x25, 0x5a2, x0	// Write all 0s to CSR
	csrrs x25, 0x5a2, x7	// Set all CSR bits
	csrrc x25, 0x5a2, x7	// Clear all CSR bits
	csrrw x25, 0x5a2, x20	// Restore CSR

// Testing CSR 0x5a3
	csrr x29, 0x5a3	// Read CSR
	li x2, -1
	csrrw x3, 0x5a3, x2	// Write all 1s to CSR
	csrrw x3, 0x5a3, x0	// Write all 0s to CSR
	csrrs x3, 0x5a3, x2	// Set all CSR bits
	csrrc x3, 0x5a3, x2	// Clear all CSR bits
	csrrw x3, 0x5a3, x29	// Restore CSR

// Testing CSR 0x5a4
	csrr x20, 0x5a4	// Read CSR
	li x23, -1
	csrrw x16, 0x5a4, x23	// Write all 1s to CSR
	csrrw x16, 0x5a4, x0	// Write all 0s to CSR
	csrrs x16, 0x5a4, x23	// Set all CSR bits
	csrrc x16, 0x5a4, x23	// Clear all CSR bits
	csrrw x16, 0x5a4, x20	// Restore CSR

// Testing CSR 0x5a5
	csrr x8, 0x5a5	// Read CSR
	li x13, -1
	csrrw x12, 0x5a5, x13	// Write all 1s to CSR
	csrrw x12, 0x5a5, x0	// Write all 0s to CSR
	csrrs x12, 0x5a5, x13	// Set all CSR bits
	csrrc x12, 0x5a5, x13	// Clear all CSR bits
	csrrw x12, 0x5a5, x8	// Restore CSR

// Testing CSR 0x5a6
	csrr x9, 0x5a6	// Read CSR
	li x10, -1
	csrrw x23, 0x5a6, x10	// Write all 1s to CSR
	csrrw x23, 0x5a6, x0	// Write all 0s to CSR
	csrrs x23, 0x5a6, x10	// Set all CSR bits
	csrrc x23, 0x5a6, x10	// Clear all CSR bits
	csrrw x23, 0x5a6, x9	// Restore CSR

// Testing CSR 0x5a7
	csrr x27, 0x5a7	// Read CSR
	li x28, -1
	csrrw x29, 0x5a7, x28	// Write all 1s to CSR
	csrrw x29, 0x5a7, x0	// Write all 0s to CSR
	csrrs x29, 0x5a7, x28	// Set all CSR bits
	csrrc x29, 0x5a7, x28	// Clear all CSR bits
	csrrw x29, 0x5a7, x27	// Restore CSR

// Testing CSR 0x5a8
	csrr x29, 0x5a8	// Read CSR
	li x17, -1
	csrrw x28, 0x5a8, x17	// Write all 1s to CSR
	csrrw x28, 0x5a8, x0	// Write all 0s to CSR
	csrrs x28, 0x5a8, x17	// Set all CSR bits
	csrrc x28, 0x5a8, x17	// Clear all CSR bits
	csrrw x28, 0x5a8, x29	// Restore CSR

// Testing CSR 0x5a9
	csrr x2, 0x5a9	// Read CSR
	li x15, -1
	csrrw x6, 0x5a9, x15	// Write all 1s to CSR
	csrrw x6, 0x5a9, x0	// Write all 0s to CSR
	csrrs x6, 0x5a9, x15	// Set all CSR bits
	csrrc x6, 0x5a9, x15	// Clear all CSR bits
	csrrw x6, 0x5a9, x2	// Restore CSR

// Testing CSR 0x5aa
	csrr x12, 0x5aa	// Read CSR
	li x28, -1
	csrrw x3, 0x5aa, x28	// Write all 1s to CSR
	csrrw x3, 0x5aa, x0	// Write all 0s to CSR
	csrrs x3, 0x5aa, x28	// Set all CSR bits
	csrrc x3, 0x5aa, x28	// Clear all CSR bits
	csrrw x3, 0x5aa, x12	// Restore CSR

// Testing CSR 0x5ab
	csrr x24, 0x5ab	// Read CSR
	li x7, -1
	csrrw x20, 0x5ab, x7	// Write all 1s to CSR
	csrrw x20, 0x5ab, x0	// Write all 0s to CSR
	csrrs x20, 0x5ab, x7	// Set all CSR bits
	csrrc x20, 0x5ab, x7	// Clear all CSR bits
	csrrw x20, 0x5ab, x24	// Restore CSR

// Testing CSR 0x5ac
	csrr x31, 0x5ac	// Read CSR
	li x17, -1
	csrrw x6, 0x5ac, x17	// Write all 1s to CSR
	csrrw x6, 0x5ac, x0	// Write all 0s to CSR
	csrrs x6, 0x5ac, x17	// Set all CSR bits
	csrrc x6, 0x5ac, x17	// Clear all CSR bits
	csrrw x6, 0x5ac, x31	// Restore CSR

// Testing CSR 0x5ad
	csrr x12, 0x5ad	// Read CSR
	li x4, -1
	csrrw x16, 0x5ad, x4	// Write all 1s to CSR
	csrrw x16, 0x5ad, x0	// Write all 0s to CSR
	csrrs x16, 0x5ad, x4	// Set all CSR bits
	csrrc x16, 0x5ad, x4	// Clear all CSR bits
	csrrw x16, 0x5ad, x12	// Restore CSR

// Testing CSR 0x5ae
	csrr x22, 0x5ae	// Read CSR
	li x16, -1
	csrrw x4, 0x5ae, x16	// Write all 1s to CSR
	csrrw x4, 0x5ae, x0	// Write all 0s to CSR
	csrrs x4, 0x5ae, x16	// Set all CSR bits
	csrrc x4, 0x5ae, x16	// Clear all CSR bits
	csrrw x4, 0x5ae, x22	// Restore CSR

// Testing CSR 0x5af
	csrr x17, 0x5af	// Read CSR
	li x21, -1
	csrrw x3, 0x5af, x21	// Write all 1s to CSR
	csrrw x3, 0x5af, x0	// Write all 0s to CSR
	csrrs x3, 0x5af, x21	// Set all CSR bits
	csrrc x3, 0x5af, x21	// Clear all CSR bits
	csrrw x3, 0x5af, x17	// Restore CSR

// Testing CSR 0x5b0
	csrr x22, 0x5b0	// Read CSR
	li x9, -1
	csrrw x12, 0x5b0, x9	// Write all 1s to CSR
	csrrw x12, 0x5b0, x0	// Write all 0s to CSR
	csrrs x12, 0x5b0, x9	// Set all CSR bits
	csrrc x12, 0x5b0, x9	// Clear all CSR bits
	csrrw x12, 0x5b0, x22	// Restore CSR

// Testing CSR 0x5b1
	csrr x4, 0x5b1	// Read CSR
	li x6, -1
	csrrw x10, 0x5b1, x6	// Write all 1s to CSR
	csrrw x10, 0x5b1, x0	// Write all 0s to CSR
	csrrs x10, 0x5b1, x6	// Set all CSR bits
	csrrc x10, 0x5b1, x6	// Clear all CSR bits
	csrrw x10, 0x5b1, x4	// Restore CSR

// Testing CSR 0x5b2
	csrr x8, 0x5b2	// Read CSR
	li x17, -1
	csrrw x20, 0x5b2, x17	// Write all 1s to CSR
	csrrw x20, 0x5b2, x0	// Write all 0s to CSR
	csrrs x20, 0x5b2, x17	// Set all CSR bits
	csrrc x20, 0x5b2, x17	// Clear all CSR bits
	csrrw x20, 0x5b2, x8	// Restore CSR

// Testing CSR 0x5b3
	csrr x2, 0x5b3	// Read CSR
	li x24, -1
	csrrw x13, 0x5b3, x24	// Write all 1s to CSR
	csrrw x13, 0x5b3, x0	// Write all 0s to CSR
	csrrs x13, 0x5b3, x24	// Set all CSR bits
	csrrc x13, 0x5b3, x24	// Clear all CSR bits
	csrrw x13, 0x5b3, x2	// Restore CSR

// Testing CSR 0x5b4
	csrr x18, 0x5b4	// Read CSR
	li x4, -1
	csrrw x23, 0x5b4, x4	// Write all 1s to CSR
	csrrw x23, 0x5b4, x0	// Write all 0s to CSR
	csrrs x23, 0x5b4, x4	// Set all CSR bits
	csrrc x23, 0x5b4, x4	// Clear all CSR bits
	csrrw x23, 0x5b4, x18	// Restore CSR

// Testing CSR 0x5b5
	csrr x12, 0x5b5	// Read CSR
	li x21, -1
	csrrw x5, 0x5b5, x21	// Write all 1s to CSR
	csrrw x5, 0x5b5, x0	// Write all 0s to CSR
	csrrs x5, 0x5b5, x21	// Set all CSR bits
	csrrc x5, 0x5b5, x21	// Clear all CSR bits
	csrrw x5, 0x5b5, x12	// Restore CSR

// Testing CSR 0x5b6
	csrr x6, 0x5b6	// Read CSR
	li x12, -1
	csrrw x19, 0x5b6, x12	// Write all 1s to CSR
	csrrw x19, 0x5b6, x0	// Write all 0s to CSR
	csrrs x19, 0x5b6, x12	// Set all CSR bits
	csrrc x19, 0x5b6, x12	// Clear all CSR bits
	csrrw x19, 0x5b6, x6	// Restore CSR

// Testing CSR 0x5b7
	csrr x17, 0x5b7	// Read CSR
	li x12, -1
	csrrw x14, 0x5b7, x12	// Write all 1s to CSR
	csrrw x14, 0x5b7, x0	// Write all 0s to CSR
	csrrs x14, 0x5b7, x12	// Set all CSR bits
	csrrc x14, 0x5b7, x12	// Clear all CSR bits
	csrrw x14, 0x5b7, x17	// Restore CSR

// Testing CSR 0x5b8
	csrr x12, 0x5b8	// Read CSR
	li x1, -1
	csrrw x23, 0x5b8, x1	// Write all 1s to CSR
	csrrw x23, 0x5b8, x0	// Write all 0s to CSR
	csrrs x23, 0x5b8, x1	// Set all CSR bits
	csrrc x23, 0x5b8, x1	// Clear all CSR bits
	csrrw x23, 0x5b8, x12	// Restore CSR

// Testing CSR 0x5b9
	csrr x21, 0x5b9	// Read CSR
	li x6, -1
	csrrw x26, 0x5b9, x6	// Write all 1s to CSR
	csrrw x26, 0x5b9, x0	// Write all 0s to CSR
	csrrs x26, 0x5b9, x6	// Set all CSR bits
	csrrc x26, 0x5b9, x6	// Clear all CSR bits
	csrrw x26, 0x5b9, x21	// Restore CSR

// Testing CSR 0x5ba
	csrr x15, 0x5ba	// Read CSR
	li x7, -1
	csrrw x2, 0x5ba, x7	// Write all 1s to CSR
	csrrw x2, 0x5ba, x0	// Write all 0s to CSR
	csrrs x2, 0x5ba, x7	// Set all CSR bits
	csrrc x2, 0x5ba, x7	// Clear all CSR bits
	csrrw x2, 0x5ba, x15	// Restore CSR

// Testing CSR 0x5bb
	csrr x31, 0x5bb	// Read CSR
	li x21, -1
	csrrw x10, 0x5bb, x21	// Write all 1s to CSR
	csrrw x10, 0x5bb, x0	// Write all 0s to CSR
	csrrs x10, 0x5bb, x21	// Set all CSR bits
	csrrc x10, 0x5bb, x21	// Clear all CSR bits
	csrrw x10, 0x5bb, x31	// Restore CSR

// Testing CSR 0x5bc
	csrr x23, 0x5bc	// Read CSR
	li x27, -1
	csrrw x22, 0x5bc, x27	// Write all 1s to CSR
	csrrw x22, 0x5bc, x0	// Write all 0s to CSR
	csrrs x22, 0x5bc, x27	// Set all CSR bits
	csrrc x22, 0x5bc, x27	// Clear all CSR bits
	csrrw x22, 0x5bc, x23	// Restore CSR

// Testing CSR 0x5bd
	csrr x6, 0x5bd	// Read CSR
	li x5, -1
	csrrw x26, 0x5bd, x5	// Write all 1s to CSR
	csrrw x26, 0x5bd, x0	// Write all 0s to CSR
	csrrs x26, 0x5bd, x5	// Set all CSR bits
	csrrc x26, 0x5bd, x5	// Clear all CSR bits
	csrrw x26, 0x5bd, x6	// Restore CSR

// Testing CSR 0x5be
	csrr x29, 0x5be	// Read CSR
	li x28, -1
	csrrw x1, 0x5be, x28	// Write all 1s to CSR
	csrrw x1, 0x5be, x0	// Write all 0s to CSR
	csrrs x1, 0x5be, x28	// Set all CSR bits
	csrrc x1, 0x5be, x28	// Clear all CSR bits
	csrrw x1, 0x5be, x29	// Restore CSR

// Testing CSR 0x5bf
	csrr x8, 0x5bf	// Read CSR
	li x7, -1
	csrrw x18, 0x5bf, x7	// Write all 1s to CSR
	csrrw x18, 0x5bf, x0	// Write all 0s to CSR
	csrrs x18, 0x5bf, x7	// Set all CSR bits
	csrrc x18, 0x5bf, x7	// Clear all CSR bits
	csrrw x18, 0x5bf, x8	// Restore CSR

// Testing CSR 0x5c0
	csrr x30, 0x5c0	// Read CSR
	li x3, -1
	csrrw x23, 0x5c0, x3	// Write all 1s to CSR
	csrrw x23, 0x5c0, x0	// Write all 0s to CSR
	csrrs x23, 0x5c0, x3	// Set all CSR bits
	csrrc x23, 0x5c0, x3	// Clear all CSR bits
	csrrw x23, 0x5c0, x30	// Restore CSR

// Testing CSR 0x5c1
	csrr x15, 0x5c1	// Read CSR
	li x16, -1
	csrrw x31, 0x5c1, x16	// Write all 1s to CSR
	csrrw x31, 0x5c1, x0	// Write all 0s to CSR
	csrrs x31, 0x5c1, x16	// Set all CSR bits
	csrrc x31, 0x5c1, x16	// Clear all CSR bits
	csrrw x31, 0x5c1, x15	// Restore CSR

// Testing CSR 0x5c2
	csrr x17, 0x5c2	// Read CSR
	li x21, -1
	csrrw x25, 0x5c2, x21	// Write all 1s to CSR
	csrrw x25, 0x5c2, x0	// Write all 0s to CSR
	csrrs x25, 0x5c2, x21	// Set all CSR bits
	csrrc x25, 0x5c2, x21	// Clear all CSR bits
	csrrw x25, 0x5c2, x17	// Restore CSR

// Testing CSR 0x5c3
	csrr x4, 0x5c3	// Read CSR
	li x19, -1
	csrrw x21, 0x5c3, x19	// Write all 1s to CSR
	csrrw x21, 0x5c3, x0	// Write all 0s to CSR
	csrrs x21, 0x5c3, x19	// Set all CSR bits
	csrrc x21, 0x5c3, x19	// Clear all CSR bits
	csrrw x21, 0x5c3, x4	// Restore CSR

// Testing CSR 0x5c4
	csrr x3, 0x5c4	// Read CSR
	li x19, -1
	csrrw x13, 0x5c4, x19	// Write all 1s to CSR
	csrrw x13, 0x5c4, x0	// Write all 0s to CSR
	csrrs x13, 0x5c4, x19	// Set all CSR bits
	csrrc x13, 0x5c4, x19	// Clear all CSR bits
	csrrw x13, 0x5c4, x3	// Restore CSR

// Testing CSR 0x5c5
	csrr x14, 0x5c5	// Read CSR
	li x19, -1
	csrrw x24, 0x5c5, x19	// Write all 1s to CSR
	csrrw x24, 0x5c5, x0	// Write all 0s to CSR
	csrrs x24, 0x5c5, x19	// Set all CSR bits
	csrrc x24, 0x5c5, x19	// Clear all CSR bits
	csrrw x24, 0x5c5, x14	// Restore CSR

// Testing CSR 0x5c6
	csrr x6, 0x5c6	// Read CSR
	li x21, -1
	csrrw x5, 0x5c6, x21	// Write all 1s to CSR
	csrrw x5, 0x5c6, x0	// Write all 0s to CSR
	csrrs x5, 0x5c6, x21	// Set all CSR bits
	csrrc x5, 0x5c6, x21	// Clear all CSR bits
	csrrw x5, 0x5c6, x6	// Restore CSR

// Testing CSR 0x5c7
	csrr x7, 0x5c7	// Read CSR
	li x19, -1
	csrrw x24, 0x5c7, x19	// Write all 1s to CSR
	csrrw x24, 0x5c7, x0	// Write all 0s to CSR
	csrrs x24, 0x5c7, x19	// Set all CSR bits
	csrrc x24, 0x5c7, x19	// Clear all CSR bits
	csrrw x24, 0x5c7, x7	// Restore CSR

// Testing CSR 0x5c8
	csrr x13, 0x5c8	// Read CSR
	li x18, -1
	csrrw x26, 0x5c8, x18	// Write all 1s to CSR
	csrrw x26, 0x5c8, x0	// Write all 0s to CSR
	csrrs x26, 0x5c8, x18	// Set all CSR bits
	csrrc x26, 0x5c8, x18	// Clear all CSR bits
	csrrw x26, 0x5c8, x13	// Restore CSR

// Testing CSR 0x5c9
	csrr x7, 0x5c9	// Read CSR
	li x1, -1
	csrrw x20, 0x5c9, x1	// Write all 1s to CSR
	csrrw x20, 0x5c9, x0	// Write all 0s to CSR
	csrrs x20, 0x5c9, x1	// Set all CSR bits
	csrrc x20, 0x5c9, x1	// Clear all CSR bits
	csrrw x20, 0x5c9, x7	// Restore CSR

// Testing CSR 0x5ca
	csrr x19, 0x5ca	// Read CSR
	li x15, -1
	csrrw x13, 0x5ca, x15	// Write all 1s to CSR
	csrrw x13, 0x5ca, x0	// Write all 0s to CSR
	csrrs x13, 0x5ca, x15	// Set all CSR bits
	csrrc x13, 0x5ca, x15	// Clear all CSR bits
	csrrw x13, 0x5ca, x19	// Restore CSR

// Testing CSR 0x5cb
	csrr x27, 0x5cb	// Read CSR
	li x12, -1
	csrrw x29, 0x5cb, x12	// Write all 1s to CSR
	csrrw x29, 0x5cb, x0	// Write all 0s to CSR
	csrrs x29, 0x5cb, x12	// Set all CSR bits
	csrrc x29, 0x5cb, x12	// Clear all CSR bits
	csrrw x29, 0x5cb, x27	// Restore CSR

// Testing CSR 0x5cc
	csrr x22, 0x5cc	// Read CSR
	li x25, -1
	csrrw x20, 0x5cc, x25	// Write all 1s to CSR
	csrrw x20, 0x5cc, x0	// Write all 0s to CSR
	csrrs x20, 0x5cc, x25	// Set all CSR bits
	csrrc x20, 0x5cc, x25	// Clear all CSR bits
	csrrw x20, 0x5cc, x22	// Restore CSR

// Testing CSR 0x5cd
	csrr x13, 0x5cd	// Read CSR
	li x3, -1
	csrrw x7, 0x5cd, x3	// Write all 1s to CSR
	csrrw x7, 0x5cd, x0	// Write all 0s to CSR
	csrrs x7, 0x5cd, x3	// Set all CSR bits
	csrrc x7, 0x5cd, x3	// Clear all CSR bits
	csrrw x7, 0x5cd, x13	// Restore CSR

// Testing CSR 0x5ce
	csrr x18, 0x5ce	// Read CSR
	li x26, -1
	csrrw x28, 0x5ce, x26	// Write all 1s to CSR
	csrrw x28, 0x5ce, x0	// Write all 0s to CSR
	csrrs x28, 0x5ce, x26	// Set all CSR bits
	csrrc x28, 0x5ce, x26	// Clear all CSR bits
	csrrw x28, 0x5ce, x18	// Restore CSR

// Testing CSR 0x5cf
	csrr x2, 0x5cf	// Read CSR
	li x11, -1
	csrrw x3, 0x5cf, x11	// Write all 1s to CSR
	csrrw x3, 0x5cf, x0	// Write all 0s to CSR
	csrrs x3, 0x5cf, x11	// Set all CSR bits
	csrrc x3, 0x5cf, x11	// Clear all CSR bits
	csrrw x3, 0x5cf, x2	// Restore CSR

// Testing CSR 0x5d0
	csrr x4, 0x5d0	// Read CSR
	li x11, -1
	csrrw x21, 0x5d0, x11	// Write all 1s to CSR
	csrrw x21, 0x5d0, x0	// Write all 0s to CSR
	csrrs x21, 0x5d0, x11	// Set all CSR bits
	csrrc x21, 0x5d0, x11	// Clear all CSR bits
	csrrw x21, 0x5d0, x4	// Restore CSR

// Testing CSR 0x5d1
	csrr x12, 0x5d1	// Read CSR
	li x27, -1
	csrrw x9, 0x5d1, x27	// Write all 1s to CSR
	csrrw x9, 0x5d1, x0	// Write all 0s to CSR
	csrrs x9, 0x5d1, x27	// Set all CSR bits
	csrrc x9, 0x5d1, x27	// Clear all CSR bits
	csrrw x9, 0x5d1, x12	// Restore CSR

// Testing CSR 0x5d2
	csrr x6, 0x5d2	// Read CSR
	li x3, -1
	csrrw x22, 0x5d2, x3	// Write all 1s to CSR
	csrrw x22, 0x5d2, x0	// Write all 0s to CSR
	csrrs x22, 0x5d2, x3	// Set all CSR bits
	csrrc x22, 0x5d2, x3	// Clear all CSR bits
	csrrw x22, 0x5d2, x6	// Restore CSR

// Testing CSR 0x5d3
	csrr x21, 0x5d3	// Read CSR
	li x12, -1
	csrrw x2, 0x5d3, x12	// Write all 1s to CSR
	csrrw x2, 0x5d3, x0	// Write all 0s to CSR
	csrrs x2, 0x5d3, x12	// Set all CSR bits
	csrrc x2, 0x5d3, x12	// Clear all CSR bits
	csrrw x2, 0x5d3, x21	// Restore CSR

// Testing CSR 0x5d4
	csrr x20, 0x5d4	// Read CSR
	li x22, -1
	csrrw x19, 0x5d4, x22	// Write all 1s to CSR
	csrrw x19, 0x5d4, x0	// Write all 0s to CSR
	csrrs x19, 0x5d4, x22	// Set all CSR bits
	csrrc x19, 0x5d4, x22	// Clear all CSR bits
	csrrw x19, 0x5d4, x20	// Restore CSR

// Testing CSR 0x5d5
	csrr x14, 0x5d5	// Read CSR
	li x23, -1
	csrrw x26, 0x5d5, x23	// Write all 1s to CSR
	csrrw x26, 0x5d5, x0	// Write all 0s to CSR
	csrrs x26, 0x5d5, x23	// Set all CSR bits
	csrrc x26, 0x5d5, x23	// Clear all CSR bits
	csrrw x26, 0x5d5, x14	// Restore CSR

// Testing CSR 0x5d6
	csrr x30, 0x5d6	// Read CSR
	li x8, -1
	csrrw x14, 0x5d6, x8	// Write all 1s to CSR
	csrrw x14, 0x5d6, x0	// Write all 0s to CSR
	csrrs x14, 0x5d6, x8	// Set all CSR bits
	csrrc x14, 0x5d6, x8	// Clear all CSR bits
	csrrw x14, 0x5d6, x30	// Restore CSR

// Testing CSR 0x5d7
	csrr x6, 0x5d7	// Read CSR
	li x24, -1
	csrrw x12, 0x5d7, x24	// Write all 1s to CSR
	csrrw x12, 0x5d7, x0	// Write all 0s to CSR
	csrrs x12, 0x5d7, x24	// Set all CSR bits
	csrrc x12, 0x5d7, x24	// Clear all CSR bits
	csrrw x12, 0x5d7, x6	// Restore CSR

// Testing CSR 0x5d8
	csrr x9, 0x5d8	// Read CSR
	li x25, -1
	csrrw x18, 0x5d8, x25	// Write all 1s to CSR
	csrrw x18, 0x5d8, x0	// Write all 0s to CSR
	csrrs x18, 0x5d8, x25	// Set all CSR bits
	csrrc x18, 0x5d8, x25	// Clear all CSR bits
	csrrw x18, 0x5d8, x9	// Restore CSR

// Testing CSR 0x5d9
	csrr x29, 0x5d9	// Read CSR
	li x23, -1
	csrrw x19, 0x5d9, x23	// Write all 1s to CSR
	csrrw x19, 0x5d9, x0	// Write all 0s to CSR
	csrrs x19, 0x5d9, x23	// Set all CSR bits
	csrrc x19, 0x5d9, x23	// Clear all CSR bits
	csrrw x19, 0x5d9, x29	// Restore CSR

// Testing CSR 0x5da
	csrr x19, 0x5da	// Read CSR
	li x20, -1
	csrrw x6, 0x5da, x20	// Write all 1s to CSR
	csrrw x6, 0x5da, x0	// Write all 0s to CSR
	csrrs x6, 0x5da, x20	// Set all CSR bits
	csrrc x6, 0x5da, x20	// Clear all CSR bits
	csrrw x6, 0x5da, x19	// Restore CSR

// Testing CSR 0x5db
	csrr x17, 0x5db	// Read CSR
	li x6, -1
	csrrw x8, 0x5db, x6	// Write all 1s to CSR
	csrrw x8, 0x5db, x0	// Write all 0s to CSR
	csrrs x8, 0x5db, x6	// Set all CSR bits
	csrrc x8, 0x5db, x6	// Clear all CSR bits
	csrrw x8, 0x5db, x17	// Restore CSR

// Testing CSR 0x5dc
	csrr x1, 0x5dc	// Read CSR
	li x8, -1
	csrrw x27, 0x5dc, x8	// Write all 1s to CSR
	csrrw x27, 0x5dc, x0	// Write all 0s to CSR
	csrrs x27, 0x5dc, x8	// Set all CSR bits
	csrrc x27, 0x5dc, x8	// Clear all CSR bits
	csrrw x27, 0x5dc, x1	// Restore CSR

// Testing CSR 0x5dd
	csrr x25, 0x5dd	// Read CSR
	li x26, -1
	csrrw x28, 0x5dd, x26	// Write all 1s to CSR
	csrrw x28, 0x5dd, x0	// Write all 0s to CSR
	csrrs x28, 0x5dd, x26	// Set all CSR bits
	csrrc x28, 0x5dd, x26	// Clear all CSR bits
	csrrw x28, 0x5dd, x25	// Restore CSR

// Testing CSR 0x5de
	csrr x18, 0x5de	// Read CSR
	li x7, -1
	csrrw x15, 0x5de, x7	// Write all 1s to CSR
	csrrw x15, 0x5de, x0	// Write all 0s to CSR
	csrrs x15, 0x5de, x7	// Set all CSR bits
	csrrc x15, 0x5de, x7	// Clear all CSR bits
	csrrw x15, 0x5de, x18	// Restore CSR

// Testing CSR 0x5df
	csrr x5, 0x5df	// Read CSR
	li x12, -1
	csrrw x13, 0x5df, x12	// Write all 1s to CSR
	csrrw x13, 0x5df, x0	// Write all 0s to CSR
	csrrs x13, 0x5df, x12	// Set all CSR bits
	csrrc x13, 0x5df, x12	// Clear all CSR bits
	csrrw x13, 0x5df, x5	// Restore CSR

// Testing CSR 0x5e0
	csrr x14, 0x5e0	// Read CSR
	li x26, -1
	csrrw x1, 0x5e0, x26	// Write all 1s to CSR
	csrrw x1, 0x5e0, x0	// Write all 0s to CSR
	csrrs x1, 0x5e0, x26	// Set all CSR bits
	csrrc x1, 0x5e0, x26	// Clear all CSR bits
	csrrw x1, 0x5e0, x14	// Restore CSR

// Testing CSR 0x5e1
	csrr x19, 0x5e1	// Read CSR
	li x4, -1
	csrrw x26, 0x5e1, x4	// Write all 1s to CSR
	csrrw x26, 0x5e1, x0	// Write all 0s to CSR
	csrrs x26, 0x5e1, x4	// Set all CSR bits
	csrrc x26, 0x5e1, x4	// Clear all CSR bits
	csrrw x26, 0x5e1, x19	// Restore CSR

// Testing CSR 0x5e2
	csrr x18, 0x5e2	// Read CSR
	li x3, -1
	csrrw x17, 0x5e2, x3	// Write all 1s to CSR
	csrrw x17, 0x5e2, x0	// Write all 0s to CSR
	csrrs x17, 0x5e2, x3	// Set all CSR bits
	csrrc x17, 0x5e2, x3	// Clear all CSR bits
	csrrw x17, 0x5e2, x18	// Restore CSR

// Testing CSR 0x5e3
	csrr x9, 0x5e3	// Read CSR
	li x5, -1
	csrrw x1, 0x5e3, x5	// Write all 1s to CSR
	csrrw x1, 0x5e3, x0	// Write all 0s to CSR
	csrrs x1, 0x5e3, x5	// Set all CSR bits
	csrrc x1, 0x5e3, x5	// Clear all CSR bits
	csrrw x1, 0x5e3, x9	// Restore CSR

// Testing CSR 0x5e4
	csrr x15, 0x5e4	// Read CSR
	li x10, -1
	csrrw x24, 0x5e4, x10	// Write all 1s to CSR
	csrrw x24, 0x5e4, x0	// Write all 0s to CSR
	csrrs x24, 0x5e4, x10	// Set all CSR bits
	csrrc x24, 0x5e4, x10	// Clear all CSR bits
	csrrw x24, 0x5e4, x15	// Restore CSR

// Testing CSR 0x5e5
	csrr x15, 0x5e5	// Read CSR
	li x1, -1
	csrrw x4, 0x5e5, x1	// Write all 1s to CSR
	csrrw x4, 0x5e5, x0	// Write all 0s to CSR
	csrrs x4, 0x5e5, x1	// Set all CSR bits
	csrrc x4, 0x5e5, x1	// Clear all CSR bits
	csrrw x4, 0x5e5, x15	// Restore CSR

// Testing CSR 0x5e6
	csrr x5, 0x5e6	// Read CSR
	li x3, -1
	csrrw x19, 0x5e6, x3	// Write all 1s to CSR
	csrrw x19, 0x5e6, x0	// Write all 0s to CSR
	csrrs x19, 0x5e6, x3	// Set all CSR bits
	csrrc x19, 0x5e6, x3	// Clear all CSR bits
	csrrw x19, 0x5e6, x5	// Restore CSR

// Testing CSR 0x5e7
	csrr x28, 0x5e7	// Read CSR
	li x7, -1
	csrrw x3, 0x5e7, x7	// Write all 1s to CSR
	csrrw x3, 0x5e7, x0	// Write all 0s to CSR
	csrrs x3, 0x5e7, x7	// Set all CSR bits
	csrrc x3, 0x5e7, x7	// Clear all CSR bits
	csrrw x3, 0x5e7, x28	// Restore CSR

// Testing CSR 0x5e8
	csrr x2, 0x5e8	// Read CSR
	li x8, -1
	csrrw x4, 0x5e8, x8	// Write all 1s to CSR
	csrrw x4, 0x5e8, x0	// Write all 0s to CSR
	csrrs x4, 0x5e8, x8	// Set all CSR bits
	csrrc x4, 0x5e8, x8	// Clear all CSR bits
	csrrw x4, 0x5e8, x2	// Restore CSR

// Testing CSR 0x5e9
	csrr x2, 0x5e9	// Read CSR
	li x15, -1
	csrrw x21, 0x5e9, x15	// Write all 1s to CSR
	csrrw x21, 0x5e9, x0	// Write all 0s to CSR
	csrrs x21, 0x5e9, x15	// Set all CSR bits
	csrrc x21, 0x5e9, x15	// Clear all CSR bits
	csrrw x21, 0x5e9, x2	// Restore CSR

// Testing CSR 0x5ea
	csrr x3, 0x5ea	// Read CSR
	li x15, -1
	csrrw x25, 0x5ea, x15	// Write all 1s to CSR
	csrrw x25, 0x5ea, x0	// Write all 0s to CSR
	csrrs x25, 0x5ea, x15	// Set all CSR bits
	csrrc x25, 0x5ea, x15	// Clear all CSR bits
	csrrw x25, 0x5ea, x3	// Restore CSR

// Testing CSR 0x5eb
	csrr x9, 0x5eb	// Read CSR
	li x24, -1
	csrrw x4, 0x5eb, x24	// Write all 1s to CSR
	csrrw x4, 0x5eb, x0	// Write all 0s to CSR
	csrrs x4, 0x5eb, x24	// Set all CSR bits
	csrrc x4, 0x5eb, x24	// Clear all CSR bits
	csrrw x4, 0x5eb, x9	// Restore CSR

// Testing CSR 0x5ec
	csrr x9, 0x5ec	// Read CSR
	li x31, -1
	csrrw x6, 0x5ec, x31	// Write all 1s to CSR
	csrrw x6, 0x5ec, x0	// Write all 0s to CSR
	csrrs x6, 0x5ec, x31	// Set all CSR bits
	csrrc x6, 0x5ec, x31	// Clear all CSR bits
	csrrw x6, 0x5ec, x9	// Restore CSR

// Testing CSR 0x5ed
	csrr x5, 0x5ed	// Read CSR
	li x31, -1
	csrrw x12, 0x5ed, x31	// Write all 1s to CSR
	csrrw x12, 0x5ed, x0	// Write all 0s to CSR
	csrrs x12, 0x5ed, x31	// Set all CSR bits
	csrrc x12, 0x5ed, x31	// Clear all CSR bits
	csrrw x12, 0x5ed, x5	// Restore CSR

// Testing CSR 0x5ee
	csrr x23, 0x5ee	// Read CSR
	li x13, -1
	csrrw x2, 0x5ee, x13	// Write all 1s to CSR
	csrrw x2, 0x5ee, x0	// Write all 0s to CSR
	csrrs x2, 0x5ee, x13	// Set all CSR bits
	csrrc x2, 0x5ee, x13	// Clear all CSR bits
	csrrw x2, 0x5ee, x23	// Restore CSR

// Testing CSR 0x5ef
	csrr x1, 0x5ef	// Read CSR
	li x12, -1
	csrrw x7, 0x5ef, x12	// Write all 1s to CSR
	csrrw x7, 0x5ef, x0	// Write all 0s to CSR
	csrrs x7, 0x5ef, x12	// Set all CSR bits
	csrrc x7, 0x5ef, x12	// Clear all CSR bits
	csrrw x7, 0x5ef, x1	// Restore CSR

// Testing CSR 0x5f0
	csrr x18, 0x5f0	// Read CSR
	li x16, -1
	csrrw x25, 0x5f0, x16	// Write all 1s to CSR
	csrrw x25, 0x5f0, x0	// Write all 0s to CSR
	csrrs x25, 0x5f0, x16	// Set all CSR bits
	csrrc x25, 0x5f0, x16	// Clear all CSR bits
	csrrw x25, 0x5f0, x18	// Restore CSR

// Testing CSR 0x5f1
	csrr x17, 0x5f1	// Read CSR
	li x15, -1
	csrrw x9, 0x5f1, x15	// Write all 1s to CSR
	csrrw x9, 0x5f1, x0	// Write all 0s to CSR
	csrrs x9, 0x5f1, x15	// Set all CSR bits
	csrrc x9, 0x5f1, x15	// Clear all CSR bits
	csrrw x9, 0x5f1, x17	// Restore CSR

// Testing CSR 0x5f2
	csrr x22, 0x5f2	// Read CSR
	li x30, -1
	csrrw x23, 0x5f2, x30	// Write all 1s to CSR
	csrrw x23, 0x5f2, x0	// Write all 0s to CSR
	csrrs x23, 0x5f2, x30	// Set all CSR bits
	csrrc x23, 0x5f2, x30	// Clear all CSR bits
	csrrw x23, 0x5f2, x22	// Restore CSR

// Testing CSR 0x5f3
	csrr x7, 0x5f3	// Read CSR
	li x4, -1
	csrrw x5, 0x5f3, x4	// Write all 1s to CSR
	csrrw x5, 0x5f3, x0	// Write all 0s to CSR
	csrrs x5, 0x5f3, x4	// Set all CSR bits
	csrrc x5, 0x5f3, x4	// Clear all CSR bits
	csrrw x5, 0x5f3, x7	// Restore CSR

// Testing CSR 0x5f4
	csrr x29, 0x5f4	// Read CSR
	li x21, -1
	csrrw x3, 0x5f4, x21	// Write all 1s to CSR
	csrrw x3, 0x5f4, x0	// Write all 0s to CSR
	csrrs x3, 0x5f4, x21	// Set all CSR bits
	csrrc x3, 0x5f4, x21	// Clear all CSR bits
	csrrw x3, 0x5f4, x29	// Restore CSR

// Testing CSR 0x5f5
	csrr x10, 0x5f5	// Read CSR
	li x17, -1
	csrrw x30, 0x5f5, x17	// Write all 1s to CSR
	csrrw x30, 0x5f5, x0	// Write all 0s to CSR
	csrrs x30, 0x5f5, x17	// Set all CSR bits
	csrrc x30, 0x5f5, x17	// Clear all CSR bits
	csrrw x30, 0x5f5, x10	// Restore CSR

// Testing CSR 0x5f6
	csrr x28, 0x5f6	// Read CSR
	li x14, -1
	csrrw x15, 0x5f6, x14	// Write all 1s to CSR
	csrrw x15, 0x5f6, x0	// Write all 0s to CSR
	csrrs x15, 0x5f6, x14	// Set all CSR bits
	csrrc x15, 0x5f6, x14	// Clear all CSR bits
	csrrw x15, 0x5f6, x28	// Restore CSR

// Testing CSR 0x5f7
	csrr x19, 0x5f7	// Read CSR
	li x16, -1
	csrrw x7, 0x5f7, x16	// Write all 1s to CSR
	csrrw x7, 0x5f7, x0	// Write all 0s to CSR
	csrrs x7, 0x5f7, x16	// Set all CSR bits
	csrrc x7, 0x5f7, x16	// Clear all CSR bits
	csrrw x7, 0x5f7, x19	// Restore CSR

// Testing CSR 0x5f8
	csrr x10, 0x5f8	// Read CSR
	li x13, -1
	csrrw x17, 0x5f8, x13	// Write all 1s to CSR
	csrrw x17, 0x5f8, x0	// Write all 0s to CSR
	csrrs x17, 0x5f8, x13	// Set all CSR bits
	csrrc x17, 0x5f8, x13	// Clear all CSR bits
	csrrw x17, 0x5f8, x10	// Restore CSR

// Testing CSR 0x5f9
	csrr x20, 0x5f9	// Read CSR
	li x12, -1
	csrrw x26, 0x5f9, x12	// Write all 1s to CSR
	csrrw x26, 0x5f9, x0	// Write all 0s to CSR
	csrrs x26, 0x5f9, x12	// Set all CSR bits
	csrrc x26, 0x5f9, x12	// Clear all CSR bits
	csrrw x26, 0x5f9, x20	// Restore CSR

// Testing CSR 0x5fa
	csrr x22, 0x5fa	// Read CSR
	li x18, -1
	csrrw x17, 0x5fa, x18	// Write all 1s to CSR
	csrrw x17, 0x5fa, x0	// Write all 0s to CSR
	csrrs x17, 0x5fa, x18	// Set all CSR bits
	csrrc x17, 0x5fa, x18	// Clear all CSR bits
	csrrw x17, 0x5fa, x22	// Restore CSR

// Testing CSR 0x5fb
	csrr x4, 0x5fb	// Read CSR
	li x8, -1
	csrrw x15, 0x5fb, x8	// Write all 1s to CSR
	csrrw x15, 0x5fb, x0	// Write all 0s to CSR
	csrrs x15, 0x5fb, x8	// Set all CSR bits
	csrrc x15, 0x5fb, x8	// Clear all CSR bits
	csrrw x15, 0x5fb, x4	// Restore CSR

// Testing CSR 0x5fc
	csrr x14, 0x5fc	// Read CSR
	li x17, -1
	csrrw x30, 0x5fc, x17	// Write all 1s to CSR
	csrrw x30, 0x5fc, x0	// Write all 0s to CSR
	csrrs x30, 0x5fc, x17	// Set all CSR bits
	csrrc x30, 0x5fc, x17	// Clear all CSR bits
	csrrw x30, 0x5fc, x14	// Restore CSR

// Testing CSR 0x5fd
	csrr x20, 0x5fd	// Read CSR
	li x10, -1
	csrrw x9, 0x5fd, x10	// Write all 1s to CSR
	csrrw x9, 0x5fd, x0	// Write all 0s to CSR
	csrrs x9, 0x5fd, x10	// Set all CSR bits
	csrrc x9, 0x5fd, x10	// Clear all CSR bits
	csrrw x9, 0x5fd, x20	// Restore CSR

// Testing CSR 0x5fe
	csrr x17, 0x5fe	// Read CSR
	li x9, -1
	csrrw x21, 0x5fe, x9	// Write all 1s to CSR
	csrrw x21, 0x5fe, x0	// Write all 0s to CSR
	csrrs x21, 0x5fe, x9	// Set all CSR bits
	csrrc x21, 0x5fe, x9	// Clear all CSR bits
	csrrw x21, 0x5fe, x17	// Restore CSR

// Testing CSR 0x5ff
	csrr x8, 0x5ff	// Read CSR
	li x27, -1
	csrrw x1, 0x5ff, x27	// Write all 1s to CSR
	csrrw x1, 0x5ff, x0	// Write all 0s to CSR
	csrrs x1, 0x5ff, x27	// Set all CSR bits
	csrrc x1, 0x5ff, x27	// Clear all CSR bits
	csrrw x1, 0x5ff, x8	// Restore CSR

// Testing CSR 0x600
	csrr x10, 0x600	// Read CSR
	li x7, -1
	csrrw x23, 0x600, x7	// Write all 1s to CSR
	csrrw x23, 0x600, x0	// Write all 0s to CSR
	csrrs x23, 0x600, x7	// Set all CSR bits
	csrrc x23, 0x600, x7	// Clear all CSR bits
	csrrw x23, 0x600, x10	// Restore CSR

// Testing CSR 0x601
	csrr x11, 0x601	// Read CSR
	li x26, -1
	csrrw x16, 0x601, x26	// Write all 1s to CSR
	csrrw x16, 0x601, x0	// Write all 0s to CSR
	csrrs x16, 0x601, x26	// Set all CSR bits
	csrrc x16, 0x601, x26	// Clear all CSR bits
	csrrw x16, 0x601, x11	// Restore CSR

// Testing CSR 0x602
	csrr x9, 0x602	// Read CSR
	li x7, -1
	csrrw x26, 0x602, x7	// Write all 1s to CSR
	csrrw x26, 0x602, x0	// Write all 0s to CSR
	csrrs x26, 0x602, x7	// Set all CSR bits
	csrrc x26, 0x602, x7	// Clear all CSR bits
	csrrw x26, 0x602, x9	// Restore CSR

// Testing CSR 0x603
	csrr x19, 0x603	// Read CSR
	li x31, -1
	csrrw x8, 0x603, x31	// Write all 1s to CSR
	csrrw x8, 0x603, x0	// Write all 0s to CSR
	csrrs x8, 0x603, x31	// Set all CSR bits
	csrrc x8, 0x603, x31	// Clear all CSR bits
	csrrw x8, 0x603, x19	// Restore CSR

// Testing CSR 0x604
	csrr x22, 0x604	// Read CSR
	li x30, -1
	csrrw x6, 0x604, x30	// Write all 1s to CSR
	csrrw x6, 0x604, x0	// Write all 0s to CSR
	csrrs x6, 0x604, x30	// Set all CSR bits
	csrrc x6, 0x604, x30	// Clear all CSR bits
	csrrw x6, 0x604, x22	// Restore CSR

// Testing CSR 0x605
	csrr x14, 0x605	// Read CSR
	li x30, -1
	csrrw x18, 0x605, x30	// Write all 1s to CSR
	csrrw x18, 0x605, x0	// Write all 0s to CSR
	csrrs x18, 0x605, x30	// Set all CSR bits
	csrrc x18, 0x605, x30	// Clear all CSR bits
	csrrw x18, 0x605, x14	// Restore CSR

// Testing CSR 0x606
	csrr x10, 0x606	// Read CSR
	li x2, -1
	csrrw x3, 0x606, x2	// Write all 1s to CSR
	csrrw x3, 0x606, x0	// Write all 0s to CSR
	csrrs x3, 0x606, x2	// Set all CSR bits
	csrrc x3, 0x606, x2	// Clear all CSR bits
	csrrw x3, 0x606, x10	// Restore CSR

// Testing CSR 0x607
	csrr x22, 0x607	// Read CSR
	li x28, -1
	csrrw x17, 0x607, x28	// Write all 1s to CSR
	csrrw x17, 0x607, x0	// Write all 0s to CSR
	csrrs x17, 0x607, x28	// Set all CSR bits
	csrrc x17, 0x607, x28	// Clear all CSR bits
	csrrw x17, 0x607, x22	// Restore CSR

// Testing CSR 0x608
	csrr x1, 0x608	// Read CSR
	li x8, -1
	csrrw x12, 0x608, x8	// Write all 1s to CSR
	csrrw x12, 0x608, x0	// Write all 0s to CSR
	csrrs x12, 0x608, x8	// Set all CSR bits
	csrrc x12, 0x608, x8	// Clear all CSR bits
	csrrw x12, 0x608, x1	// Restore CSR

// Testing CSR 0x609
	csrr x14, 0x609	// Read CSR
	li x7, -1
	csrrw x3, 0x609, x7	// Write all 1s to CSR
	csrrw x3, 0x609, x0	// Write all 0s to CSR
	csrrs x3, 0x609, x7	// Set all CSR bits
	csrrc x3, 0x609, x7	// Clear all CSR bits
	csrrw x3, 0x609, x14	// Restore CSR

// Testing CSR 0x60a
	csrr x13, 0x60a	// Read CSR
	li x6, -1
	csrrw x19, 0x60a, x6	// Write all 1s to CSR
	csrrw x19, 0x60a, x0	// Write all 0s to CSR
	csrrs x19, 0x60a, x6	// Set all CSR bits
	csrrc x19, 0x60a, x6	// Clear all CSR bits
	csrrw x19, 0x60a, x13	// Restore CSR

// Testing CSR 0x60b
	csrr x21, 0x60b	// Read CSR
	li x5, -1
	csrrw x28, 0x60b, x5	// Write all 1s to CSR
	csrrw x28, 0x60b, x0	// Write all 0s to CSR
	csrrs x28, 0x60b, x5	// Set all CSR bits
	csrrc x28, 0x60b, x5	// Clear all CSR bits
	csrrw x28, 0x60b, x21	// Restore CSR

// Testing CSR 0x60c
	csrr x17, 0x60c	// Read CSR
	li x28, -1
	csrrw x26, 0x60c, x28	// Write all 1s to CSR
	csrrw x26, 0x60c, x0	// Write all 0s to CSR
	csrrs x26, 0x60c, x28	// Set all CSR bits
	csrrc x26, 0x60c, x28	// Clear all CSR bits
	csrrw x26, 0x60c, x17	// Restore CSR

// Testing CSR 0x60d
	csrr x7, 0x60d	// Read CSR
	li x9, -1
	csrrw x28, 0x60d, x9	// Write all 1s to CSR
	csrrw x28, 0x60d, x0	// Write all 0s to CSR
	csrrs x28, 0x60d, x9	// Set all CSR bits
	csrrc x28, 0x60d, x9	// Clear all CSR bits
	csrrw x28, 0x60d, x7	// Restore CSR

// Testing CSR 0x60e
	csrr x17, 0x60e	// Read CSR
	li x14, -1
	csrrw x25, 0x60e, x14	// Write all 1s to CSR
	csrrw x25, 0x60e, x0	// Write all 0s to CSR
	csrrs x25, 0x60e, x14	// Set all CSR bits
	csrrc x25, 0x60e, x14	// Clear all CSR bits
	csrrw x25, 0x60e, x17	// Restore CSR

// Testing CSR 0x60f
	csrr x22, 0x60f	// Read CSR
	li x16, -1
	csrrw x14, 0x60f, x16	// Write all 1s to CSR
	csrrw x14, 0x60f, x0	// Write all 0s to CSR
	csrrs x14, 0x60f, x16	// Set all CSR bits
	csrrc x14, 0x60f, x16	// Clear all CSR bits
	csrrw x14, 0x60f, x22	// Restore CSR

// Testing CSR 0x610
	csrr x23, 0x610	// Read CSR
	li x29, -1
	csrrw x7, 0x610, x29	// Write all 1s to CSR
	csrrw x7, 0x610, x0	// Write all 0s to CSR
	csrrs x7, 0x610, x29	// Set all CSR bits
	csrrc x7, 0x610, x29	// Clear all CSR bits
	csrrw x7, 0x610, x23	// Restore CSR

// Testing CSR 0x611
	csrr x25, 0x611	// Read CSR
	li x3, -1
	csrrw x26, 0x611, x3	// Write all 1s to CSR
	csrrw x26, 0x611, x0	// Write all 0s to CSR
	csrrs x26, 0x611, x3	// Set all CSR bits
	csrrc x26, 0x611, x3	// Clear all CSR bits
	csrrw x26, 0x611, x25	// Restore CSR

// Testing CSR 0x612
	csrr x2, 0x612	// Read CSR
	li x26, -1
	csrrw x18, 0x612, x26	// Write all 1s to CSR
	csrrw x18, 0x612, x0	// Write all 0s to CSR
	csrrs x18, 0x612, x26	// Set all CSR bits
	csrrc x18, 0x612, x26	// Clear all CSR bits
	csrrw x18, 0x612, x2	// Restore CSR

// Testing CSR 0x613
	csrr x11, 0x613	// Read CSR
	li x19, -1
	csrrw x1, 0x613, x19	// Write all 1s to CSR
	csrrw x1, 0x613, x0	// Write all 0s to CSR
	csrrs x1, 0x613, x19	// Set all CSR bits
	csrrc x1, 0x613, x19	// Clear all CSR bits
	csrrw x1, 0x613, x11	// Restore CSR

// Testing CSR 0x614
	csrr x27, 0x614	// Read CSR
	li x23, -1
	csrrw x16, 0x614, x23	// Write all 1s to CSR
	csrrw x16, 0x614, x0	// Write all 0s to CSR
	csrrs x16, 0x614, x23	// Set all CSR bits
	csrrc x16, 0x614, x23	// Clear all CSR bits
	csrrw x16, 0x614, x27	// Restore CSR

// Testing CSR 0x615
	csrr x29, 0x615	// Read CSR
	li x13, -1
	csrrw x20, 0x615, x13	// Write all 1s to CSR
	csrrw x20, 0x615, x0	// Write all 0s to CSR
	csrrs x20, 0x615, x13	// Set all CSR bits
	csrrc x20, 0x615, x13	// Clear all CSR bits
	csrrw x20, 0x615, x29	// Restore CSR

// Testing CSR 0x616
	csrr x10, 0x616	// Read CSR
	li x20, -1
	csrrw x8, 0x616, x20	// Write all 1s to CSR
	csrrw x8, 0x616, x0	// Write all 0s to CSR
	csrrs x8, 0x616, x20	// Set all CSR bits
	csrrc x8, 0x616, x20	// Clear all CSR bits
	csrrw x8, 0x616, x10	// Restore CSR

// Testing CSR 0x617
	csrr x22, 0x617	// Read CSR
	li x16, -1
	csrrw x23, 0x617, x16	// Write all 1s to CSR
	csrrw x23, 0x617, x0	// Write all 0s to CSR
	csrrs x23, 0x617, x16	// Set all CSR bits
	csrrc x23, 0x617, x16	// Clear all CSR bits
	csrrw x23, 0x617, x22	// Restore CSR

// Testing CSR 0x618
	csrr x30, 0x618	// Read CSR
	li x20, -1
	csrrw x6, 0x618, x20	// Write all 1s to CSR
	csrrw x6, 0x618, x0	// Write all 0s to CSR
	csrrs x6, 0x618, x20	// Set all CSR bits
	csrrc x6, 0x618, x20	// Clear all CSR bits
	csrrw x6, 0x618, x30	// Restore CSR

// Testing CSR 0x619
	csrr x8, 0x619	// Read CSR
	li x20, -1
	csrrw x27, 0x619, x20	// Write all 1s to CSR
	csrrw x27, 0x619, x0	// Write all 0s to CSR
	csrrs x27, 0x619, x20	// Set all CSR bits
	csrrc x27, 0x619, x20	// Clear all CSR bits
	csrrw x27, 0x619, x8	// Restore CSR

// Testing CSR 0x61a
	csrr x20, 0x61a	// Read CSR
	li x12, -1
	csrrw x29, 0x61a, x12	// Write all 1s to CSR
	csrrw x29, 0x61a, x0	// Write all 0s to CSR
	csrrs x29, 0x61a, x12	// Set all CSR bits
	csrrc x29, 0x61a, x12	// Clear all CSR bits
	csrrw x29, 0x61a, x20	// Restore CSR

// Testing CSR 0x61b
	csrr x18, 0x61b	// Read CSR
	li x6, -1
	csrrw x3, 0x61b, x6	// Write all 1s to CSR
	csrrw x3, 0x61b, x0	// Write all 0s to CSR
	csrrs x3, 0x61b, x6	// Set all CSR bits
	csrrc x3, 0x61b, x6	// Clear all CSR bits
	csrrw x3, 0x61b, x18	// Restore CSR

// Testing CSR 0x61c
	csrr x28, 0x61c	// Read CSR
	li x24, -1
	csrrw x11, 0x61c, x24	// Write all 1s to CSR
	csrrw x11, 0x61c, x0	// Write all 0s to CSR
	csrrs x11, 0x61c, x24	// Set all CSR bits
	csrrc x11, 0x61c, x24	// Clear all CSR bits
	csrrw x11, 0x61c, x28	// Restore CSR

// Testing CSR 0x61d
	csrr x18, 0x61d	// Read CSR
	li x13, -1
	csrrw x11, 0x61d, x13	// Write all 1s to CSR
	csrrw x11, 0x61d, x0	// Write all 0s to CSR
	csrrs x11, 0x61d, x13	// Set all CSR bits
	csrrc x11, 0x61d, x13	// Clear all CSR bits
	csrrw x11, 0x61d, x18	// Restore CSR

// Testing CSR 0x61e
	csrr x14, 0x61e	// Read CSR
	li x19, -1
	csrrw x15, 0x61e, x19	// Write all 1s to CSR
	csrrw x15, 0x61e, x0	// Write all 0s to CSR
	csrrs x15, 0x61e, x19	// Set all CSR bits
	csrrc x15, 0x61e, x19	// Clear all CSR bits
	csrrw x15, 0x61e, x14	// Restore CSR

// Testing CSR 0x61f
	csrr x12, 0x61f	// Read CSR
	li x1, -1
	csrrw x3, 0x61f, x1	// Write all 1s to CSR
	csrrw x3, 0x61f, x0	// Write all 0s to CSR
	csrrs x3, 0x61f, x1	// Set all CSR bits
	csrrc x3, 0x61f, x1	// Clear all CSR bits
	csrrw x3, 0x61f, x12	// Restore CSR

// Testing CSR 0x620
	csrr x3, 0x620	// Read CSR
	li x26, -1
	csrrw x15, 0x620, x26	// Write all 1s to CSR
	csrrw x15, 0x620, x0	// Write all 0s to CSR
	csrrs x15, 0x620, x26	// Set all CSR bits
	csrrc x15, 0x620, x26	// Clear all CSR bits
	csrrw x15, 0x620, x3	// Restore CSR

// Testing CSR 0x621
	csrr x21, 0x621	// Read CSR
	li x26, -1
	csrrw x9, 0x621, x26	// Write all 1s to CSR
	csrrw x9, 0x621, x0	// Write all 0s to CSR
	csrrs x9, 0x621, x26	// Set all CSR bits
	csrrc x9, 0x621, x26	// Clear all CSR bits
	csrrw x9, 0x621, x21	// Restore CSR

// Testing CSR 0x622
	csrr x7, 0x622	// Read CSR
	li x24, -1
	csrrw x29, 0x622, x24	// Write all 1s to CSR
	csrrw x29, 0x622, x0	// Write all 0s to CSR
	csrrs x29, 0x622, x24	// Set all CSR bits
	csrrc x29, 0x622, x24	// Clear all CSR bits
	csrrw x29, 0x622, x7	// Restore CSR

// Testing CSR 0x623
	csrr x28, 0x623	// Read CSR
	li x17, -1
	csrrw x29, 0x623, x17	// Write all 1s to CSR
	csrrw x29, 0x623, x0	// Write all 0s to CSR
	csrrs x29, 0x623, x17	// Set all CSR bits
	csrrc x29, 0x623, x17	// Clear all CSR bits
	csrrw x29, 0x623, x28	// Restore CSR

// Testing CSR 0x624
	csrr x16, 0x624	// Read CSR
	li x30, -1
	csrrw x20, 0x624, x30	// Write all 1s to CSR
	csrrw x20, 0x624, x0	// Write all 0s to CSR
	csrrs x20, 0x624, x30	// Set all CSR bits
	csrrc x20, 0x624, x30	// Clear all CSR bits
	csrrw x20, 0x624, x16	// Restore CSR

// Testing CSR 0x625
	csrr x18, 0x625	// Read CSR
	li x16, -1
	csrrw x17, 0x625, x16	// Write all 1s to CSR
	csrrw x17, 0x625, x0	// Write all 0s to CSR
	csrrs x17, 0x625, x16	// Set all CSR bits
	csrrc x17, 0x625, x16	// Clear all CSR bits
	csrrw x17, 0x625, x18	// Restore CSR

// Testing CSR 0x626
	csrr x21, 0x626	// Read CSR
	li x10, -1
	csrrw x17, 0x626, x10	// Write all 1s to CSR
	csrrw x17, 0x626, x0	// Write all 0s to CSR
	csrrs x17, 0x626, x10	// Set all CSR bits
	csrrc x17, 0x626, x10	// Clear all CSR bits
	csrrw x17, 0x626, x21	// Restore CSR

// Testing CSR 0x627
	csrr x24, 0x627	// Read CSR
	li x7, -1
	csrrw x14, 0x627, x7	// Write all 1s to CSR
	csrrw x14, 0x627, x0	// Write all 0s to CSR
	csrrs x14, 0x627, x7	// Set all CSR bits
	csrrc x14, 0x627, x7	// Clear all CSR bits
	csrrw x14, 0x627, x24	// Restore CSR

// Testing CSR 0x628
	csrr x3, 0x628	// Read CSR
	li x20, -1
	csrrw x31, 0x628, x20	// Write all 1s to CSR
	csrrw x31, 0x628, x0	// Write all 0s to CSR
	csrrs x31, 0x628, x20	// Set all CSR bits
	csrrc x31, 0x628, x20	// Clear all CSR bits
	csrrw x31, 0x628, x3	// Restore CSR

// Testing CSR 0x629
	csrr x20, 0x629	// Read CSR
	li x7, -1
	csrrw x15, 0x629, x7	// Write all 1s to CSR
	csrrw x15, 0x629, x0	// Write all 0s to CSR
	csrrs x15, 0x629, x7	// Set all CSR bits
	csrrc x15, 0x629, x7	// Clear all CSR bits
	csrrw x15, 0x629, x20	// Restore CSR

// Testing CSR 0x62a
	csrr x23, 0x62a	// Read CSR
	li x30, -1
	csrrw x2, 0x62a, x30	// Write all 1s to CSR
	csrrw x2, 0x62a, x0	// Write all 0s to CSR
	csrrs x2, 0x62a, x30	// Set all CSR bits
	csrrc x2, 0x62a, x30	// Clear all CSR bits
	csrrw x2, 0x62a, x23	// Restore CSR

// Testing CSR 0x62b
	csrr x8, 0x62b	// Read CSR
	li x4, -1
	csrrw x30, 0x62b, x4	// Write all 1s to CSR
	csrrw x30, 0x62b, x0	// Write all 0s to CSR
	csrrs x30, 0x62b, x4	// Set all CSR bits
	csrrc x30, 0x62b, x4	// Clear all CSR bits
	csrrw x30, 0x62b, x8	// Restore CSR

// Testing CSR 0x62c
	csrr x23, 0x62c	// Read CSR
	li x28, -1
	csrrw x12, 0x62c, x28	// Write all 1s to CSR
	csrrw x12, 0x62c, x0	// Write all 0s to CSR
	csrrs x12, 0x62c, x28	// Set all CSR bits
	csrrc x12, 0x62c, x28	// Clear all CSR bits
	csrrw x12, 0x62c, x23	// Restore CSR

// Testing CSR 0x62d
	csrr x11, 0x62d	// Read CSR
	li x9, -1
	csrrw x19, 0x62d, x9	// Write all 1s to CSR
	csrrw x19, 0x62d, x0	// Write all 0s to CSR
	csrrs x19, 0x62d, x9	// Set all CSR bits
	csrrc x19, 0x62d, x9	// Clear all CSR bits
	csrrw x19, 0x62d, x11	// Restore CSR

// Testing CSR 0x62e
	csrr x21, 0x62e	// Read CSR
	li x13, -1
	csrrw x11, 0x62e, x13	// Write all 1s to CSR
	csrrw x11, 0x62e, x0	// Write all 0s to CSR
	csrrs x11, 0x62e, x13	// Set all CSR bits
	csrrc x11, 0x62e, x13	// Clear all CSR bits
	csrrw x11, 0x62e, x21	// Restore CSR

// Testing CSR 0x62f
	csrr x7, 0x62f	// Read CSR
	li x15, -1
	csrrw x5, 0x62f, x15	// Write all 1s to CSR
	csrrw x5, 0x62f, x0	// Write all 0s to CSR
	csrrs x5, 0x62f, x15	// Set all CSR bits
	csrrc x5, 0x62f, x15	// Clear all CSR bits
	csrrw x5, 0x62f, x7	// Restore CSR

// Testing CSR 0x630
	csrr x3, 0x630	// Read CSR
	li x7, -1
	csrrw x24, 0x630, x7	// Write all 1s to CSR
	csrrw x24, 0x630, x0	// Write all 0s to CSR
	csrrs x24, 0x630, x7	// Set all CSR bits
	csrrc x24, 0x630, x7	// Clear all CSR bits
	csrrw x24, 0x630, x3	// Restore CSR

// Testing CSR 0x631
	csrr x18, 0x631	// Read CSR
	li x7, -1
	csrrw x15, 0x631, x7	// Write all 1s to CSR
	csrrw x15, 0x631, x0	// Write all 0s to CSR
	csrrs x15, 0x631, x7	// Set all CSR bits
	csrrc x15, 0x631, x7	// Clear all CSR bits
	csrrw x15, 0x631, x18	// Restore CSR

// Testing CSR 0x632
	csrr x28, 0x632	// Read CSR
	li x26, -1
	csrrw x8, 0x632, x26	// Write all 1s to CSR
	csrrw x8, 0x632, x0	// Write all 0s to CSR
	csrrs x8, 0x632, x26	// Set all CSR bits
	csrrc x8, 0x632, x26	// Clear all CSR bits
	csrrw x8, 0x632, x28	// Restore CSR

// Testing CSR 0x633
	csrr x8, 0x633	// Read CSR
	li x6, -1
	csrrw x18, 0x633, x6	// Write all 1s to CSR
	csrrw x18, 0x633, x0	// Write all 0s to CSR
	csrrs x18, 0x633, x6	// Set all CSR bits
	csrrc x18, 0x633, x6	// Clear all CSR bits
	csrrw x18, 0x633, x8	// Restore CSR

// Testing CSR 0x634
	csrr x27, 0x634	// Read CSR
	li x14, -1
	csrrw x3, 0x634, x14	// Write all 1s to CSR
	csrrw x3, 0x634, x0	// Write all 0s to CSR
	csrrs x3, 0x634, x14	// Set all CSR bits
	csrrc x3, 0x634, x14	// Clear all CSR bits
	csrrw x3, 0x634, x27	// Restore CSR

// Testing CSR 0x635
	csrr x8, 0x635	// Read CSR
	li x6, -1
	csrrw x15, 0x635, x6	// Write all 1s to CSR
	csrrw x15, 0x635, x0	// Write all 0s to CSR
	csrrs x15, 0x635, x6	// Set all CSR bits
	csrrc x15, 0x635, x6	// Clear all CSR bits
	csrrw x15, 0x635, x8	// Restore CSR

// Testing CSR 0x636
	csrr x3, 0x636	// Read CSR
	li x8, -1
	csrrw x21, 0x636, x8	// Write all 1s to CSR
	csrrw x21, 0x636, x0	// Write all 0s to CSR
	csrrs x21, 0x636, x8	// Set all CSR bits
	csrrc x21, 0x636, x8	// Clear all CSR bits
	csrrw x21, 0x636, x3	// Restore CSR

// Testing CSR 0x637
	csrr x11, 0x637	// Read CSR
	li x21, -1
	csrrw x9, 0x637, x21	// Write all 1s to CSR
	csrrw x9, 0x637, x0	// Write all 0s to CSR
	csrrs x9, 0x637, x21	// Set all CSR bits
	csrrc x9, 0x637, x21	// Clear all CSR bits
	csrrw x9, 0x637, x11	// Restore CSR

// Testing CSR 0x638
	csrr x27, 0x638	// Read CSR
	li x17, -1
	csrrw x1, 0x638, x17	// Write all 1s to CSR
	csrrw x1, 0x638, x0	// Write all 0s to CSR
	csrrs x1, 0x638, x17	// Set all CSR bits
	csrrc x1, 0x638, x17	// Clear all CSR bits
	csrrw x1, 0x638, x27	// Restore CSR

// Testing CSR 0x639
	csrr x22, 0x639	// Read CSR
	li x5, -1
	csrrw x24, 0x639, x5	// Write all 1s to CSR
	csrrw x24, 0x639, x0	// Write all 0s to CSR
	csrrs x24, 0x639, x5	// Set all CSR bits
	csrrc x24, 0x639, x5	// Clear all CSR bits
	csrrw x24, 0x639, x22	// Restore CSR

// Testing CSR 0x63a
	csrr x25, 0x63a	// Read CSR
	li x6, -1
	csrrw x1, 0x63a, x6	// Write all 1s to CSR
	csrrw x1, 0x63a, x0	// Write all 0s to CSR
	csrrs x1, 0x63a, x6	// Set all CSR bits
	csrrc x1, 0x63a, x6	// Clear all CSR bits
	csrrw x1, 0x63a, x25	// Restore CSR

// Testing CSR 0x63b
	csrr x3, 0x63b	// Read CSR
	li x28, -1
	csrrw x30, 0x63b, x28	// Write all 1s to CSR
	csrrw x30, 0x63b, x0	// Write all 0s to CSR
	csrrs x30, 0x63b, x28	// Set all CSR bits
	csrrc x30, 0x63b, x28	// Clear all CSR bits
	csrrw x30, 0x63b, x3	// Restore CSR

// Testing CSR 0x63c
	csrr x20, 0x63c	// Read CSR
	li x2, -1
	csrrw x8, 0x63c, x2	// Write all 1s to CSR
	csrrw x8, 0x63c, x0	// Write all 0s to CSR
	csrrs x8, 0x63c, x2	// Set all CSR bits
	csrrc x8, 0x63c, x2	// Clear all CSR bits
	csrrw x8, 0x63c, x20	// Restore CSR

// Testing CSR 0x63d
	csrr x12, 0x63d	// Read CSR
	li x15, -1
	csrrw x25, 0x63d, x15	// Write all 1s to CSR
	csrrw x25, 0x63d, x0	// Write all 0s to CSR
	csrrs x25, 0x63d, x15	// Set all CSR bits
	csrrc x25, 0x63d, x15	// Clear all CSR bits
	csrrw x25, 0x63d, x12	// Restore CSR

// Testing CSR 0x63e
	csrr x24, 0x63e	// Read CSR
	li x14, -1
	csrrw x22, 0x63e, x14	// Write all 1s to CSR
	csrrw x22, 0x63e, x0	// Write all 0s to CSR
	csrrs x22, 0x63e, x14	// Set all CSR bits
	csrrc x22, 0x63e, x14	// Clear all CSR bits
	csrrw x22, 0x63e, x24	// Restore CSR

// Testing CSR 0x63f
	csrr x16, 0x63f	// Read CSR
	li x9, -1
	csrrw x6, 0x63f, x9	// Write all 1s to CSR
	csrrw x6, 0x63f, x0	// Write all 0s to CSR
	csrrs x6, 0x63f, x9	// Set all CSR bits
	csrrc x6, 0x63f, x9	// Clear all CSR bits
	csrrw x6, 0x63f, x16	// Restore CSR

// Testing CSR 0x640
	csrr x29, 0x640	// Read CSR
	li x24, -1
	csrrw x27, 0x640, x24	// Write all 1s to CSR
	csrrw x27, 0x640, x0	// Write all 0s to CSR
	csrrs x27, 0x640, x24	// Set all CSR bits
	csrrc x27, 0x640, x24	// Clear all CSR bits
	csrrw x27, 0x640, x29	// Restore CSR

// Testing CSR 0x641
	csrr x10, 0x641	// Read CSR
	li x16, -1
	csrrw x25, 0x641, x16	// Write all 1s to CSR
	csrrw x25, 0x641, x0	// Write all 0s to CSR
	csrrs x25, 0x641, x16	// Set all CSR bits
	csrrc x25, 0x641, x16	// Clear all CSR bits
	csrrw x25, 0x641, x10	// Restore CSR

// Testing CSR 0x642
	csrr x15, 0x642	// Read CSR
	li x16, -1
	csrrw x28, 0x642, x16	// Write all 1s to CSR
	csrrw x28, 0x642, x0	// Write all 0s to CSR
	csrrs x28, 0x642, x16	// Set all CSR bits
	csrrc x28, 0x642, x16	// Clear all CSR bits
	csrrw x28, 0x642, x15	// Restore CSR

// Testing CSR 0x643
	csrr x20, 0x643	// Read CSR
	li x7, -1
	csrrw x10, 0x643, x7	// Write all 1s to CSR
	csrrw x10, 0x643, x0	// Write all 0s to CSR
	csrrs x10, 0x643, x7	// Set all CSR bits
	csrrc x10, 0x643, x7	// Clear all CSR bits
	csrrw x10, 0x643, x20	// Restore CSR

// Testing CSR 0x644
	csrr x18, 0x644	// Read CSR
	li x17, -1
	csrrw x19, 0x644, x17	// Write all 1s to CSR
	csrrw x19, 0x644, x0	// Write all 0s to CSR
	csrrs x19, 0x644, x17	// Set all CSR bits
	csrrc x19, 0x644, x17	// Clear all CSR bits
	csrrw x19, 0x644, x18	// Restore CSR

// Testing CSR 0x645
	csrr x18, 0x645	// Read CSR
	li x21, -1
	csrrw x6, 0x645, x21	// Write all 1s to CSR
	csrrw x6, 0x645, x0	// Write all 0s to CSR
	csrrs x6, 0x645, x21	// Set all CSR bits
	csrrc x6, 0x645, x21	// Clear all CSR bits
	csrrw x6, 0x645, x18	// Restore CSR

// Testing CSR 0x646
	csrr x20, 0x646	// Read CSR
	li x7, -1
	csrrw x6, 0x646, x7	// Write all 1s to CSR
	csrrw x6, 0x646, x0	// Write all 0s to CSR
	csrrs x6, 0x646, x7	// Set all CSR bits
	csrrc x6, 0x646, x7	// Clear all CSR bits
	csrrw x6, 0x646, x20	// Restore CSR

// Testing CSR 0x647
	csrr x9, 0x647	// Read CSR
	li x4, -1
	csrrw x14, 0x647, x4	// Write all 1s to CSR
	csrrw x14, 0x647, x0	// Write all 0s to CSR
	csrrs x14, 0x647, x4	// Set all CSR bits
	csrrc x14, 0x647, x4	// Clear all CSR bits
	csrrw x14, 0x647, x9	// Restore CSR

// Testing CSR 0x648
	csrr x12, 0x648	// Read CSR
	li x19, -1
	csrrw x29, 0x648, x19	// Write all 1s to CSR
	csrrw x29, 0x648, x0	// Write all 0s to CSR
	csrrs x29, 0x648, x19	// Set all CSR bits
	csrrc x29, 0x648, x19	// Clear all CSR bits
	csrrw x29, 0x648, x12	// Restore CSR

// Testing CSR 0x649
	csrr x16, 0x649	// Read CSR
	li x14, -1
	csrrw x25, 0x649, x14	// Write all 1s to CSR
	csrrw x25, 0x649, x0	// Write all 0s to CSR
	csrrs x25, 0x649, x14	// Set all CSR bits
	csrrc x25, 0x649, x14	// Clear all CSR bits
	csrrw x25, 0x649, x16	// Restore CSR

// Testing CSR 0x64a
	csrr x19, 0x64a	// Read CSR
	li x25, -1
	csrrw x22, 0x64a, x25	// Write all 1s to CSR
	csrrw x22, 0x64a, x0	// Write all 0s to CSR
	csrrs x22, 0x64a, x25	// Set all CSR bits
	csrrc x22, 0x64a, x25	// Clear all CSR bits
	csrrw x22, 0x64a, x19	// Restore CSR

// Testing CSR 0x64b
	csrr x19, 0x64b	// Read CSR
	li x18, -1
	csrrw x4, 0x64b, x18	// Write all 1s to CSR
	csrrw x4, 0x64b, x0	// Write all 0s to CSR
	csrrs x4, 0x64b, x18	// Set all CSR bits
	csrrc x4, 0x64b, x18	// Clear all CSR bits
	csrrw x4, 0x64b, x19	// Restore CSR

// Testing CSR 0x64c
	csrr x14, 0x64c	// Read CSR
	li x9, -1
	csrrw x20, 0x64c, x9	// Write all 1s to CSR
	csrrw x20, 0x64c, x0	// Write all 0s to CSR
	csrrs x20, 0x64c, x9	// Set all CSR bits
	csrrc x20, 0x64c, x9	// Clear all CSR bits
	csrrw x20, 0x64c, x14	// Restore CSR

// Testing CSR 0x64d
	csrr x6, 0x64d	// Read CSR
	li x13, -1
	csrrw x2, 0x64d, x13	// Write all 1s to CSR
	csrrw x2, 0x64d, x0	// Write all 0s to CSR
	csrrs x2, 0x64d, x13	// Set all CSR bits
	csrrc x2, 0x64d, x13	// Clear all CSR bits
	csrrw x2, 0x64d, x6	// Restore CSR

// Testing CSR 0x64e
	csrr x23, 0x64e	// Read CSR
	li x1, -1
	csrrw x9, 0x64e, x1	// Write all 1s to CSR
	csrrw x9, 0x64e, x0	// Write all 0s to CSR
	csrrs x9, 0x64e, x1	// Set all CSR bits
	csrrc x9, 0x64e, x1	// Clear all CSR bits
	csrrw x9, 0x64e, x23	// Restore CSR

// Testing CSR 0x64f
	csrr x17, 0x64f	// Read CSR
	li x11, -1
	csrrw x6, 0x64f, x11	// Write all 1s to CSR
	csrrw x6, 0x64f, x0	// Write all 0s to CSR
	csrrs x6, 0x64f, x11	// Set all CSR bits
	csrrc x6, 0x64f, x11	// Clear all CSR bits
	csrrw x6, 0x64f, x17	// Restore CSR

// Testing CSR 0x650
	csrr x22, 0x650	// Read CSR
	li x10, -1
	csrrw x19, 0x650, x10	// Write all 1s to CSR
	csrrw x19, 0x650, x0	// Write all 0s to CSR
	csrrs x19, 0x650, x10	// Set all CSR bits
	csrrc x19, 0x650, x10	// Clear all CSR bits
	csrrw x19, 0x650, x22	// Restore CSR

// Testing CSR 0x651
	csrr x29, 0x651	// Read CSR
	li x21, -1
	csrrw x15, 0x651, x21	// Write all 1s to CSR
	csrrw x15, 0x651, x0	// Write all 0s to CSR
	csrrs x15, 0x651, x21	// Set all CSR bits
	csrrc x15, 0x651, x21	// Clear all CSR bits
	csrrw x15, 0x651, x29	// Restore CSR

// Testing CSR 0x652
	csrr x29, 0x652	// Read CSR
	li x20, -1
	csrrw x28, 0x652, x20	// Write all 1s to CSR
	csrrw x28, 0x652, x0	// Write all 0s to CSR
	csrrs x28, 0x652, x20	// Set all CSR bits
	csrrc x28, 0x652, x20	// Clear all CSR bits
	csrrw x28, 0x652, x29	// Restore CSR

// Testing CSR 0x653
	csrr x24, 0x653	// Read CSR
	li x27, -1
	csrrw x30, 0x653, x27	// Write all 1s to CSR
	csrrw x30, 0x653, x0	// Write all 0s to CSR
	csrrs x30, 0x653, x27	// Set all CSR bits
	csrrc x30, 0x653, x27	// Clear all CSR bits
	csrrw x30, 0x653, x24	// Restore CSR

// Testing CSR 0x654
	csrr x12, 0x654	// Read CSR
	li x11, -1
	csrrw x18, 0x654, x11	// Write all 1s to CSR
	csrrw x18, 0x654, x0	// Write all 0s to CSR
	csrrs x18, 0x654, x11	// Set all CSR bits
	csrrc x18, 0x654, x11	// Clear all CSR bits
	csrrw x18, 0x654, x12	// Restore CSR

// Testing CSR 0x655
	csrr x18, 0x655	// Read CSR
	li x14, -1
	csrrw x28, 0x655, x14	// Write all 1s to CSR
	csrrw x28, 0x655, x0	// Write all 0s to CSR
	csrrs x28, 0x655, x14	// Set all CSR bits
	csrrc x28, 0x655, x14	// Clear all CSR bits
	csrrw x28, 0x655, x18	// Restore CSR

// Testing CSR 0x656
	csrr x20, 0x656	// Read CSR
	li x28, -1
	csrrw x13, 0x656, x28	// Write all 1s to CSR
	csrrw x13, 0x656, x0	// Write all 0s to CSR
	csrrs x13, 0x656, x28	// Set all CSR bits
	csrrc x13, 0x656, x28	// Clear all CSR bits
	csrrw x13, 0x656, x20	// Restore CSR

// Testing CSR 0x657
	csrr x3, 0x657	// Read CSR
	li x25, -1
	csrrw x6, 0x657, x25	// Write all 1s to CSR
	csrrw x6, 0x657, x0	// Write all 0s to CSR
	csrrs x6, 0x657, x25	// Set all CSR bits
	csrrc x6, 0x657, x25	// Clear all CSR bits
	csrrw x6, 0x657, x3	// Restore CSR

// Testing CSR 0x658
	csrr x23, 0x658	// Read CSR
	li x5, -1
	csrrw x8, 0x658, x5	// Write all 1s to CSR
	csrrw x8, 0x658, x0	// Write all 0s to CSR
	csrrs x8, 0x658, x5	// Set all CSR bits
	csrrc x8, 0x658, x5	// Clear all CSR bits
	csrrw x8, 0x658, x23	// Restore CSR

// Testing CSR 0x659
	csrr x30, 0x659	// Read CSR
	li x4, -1
	csrrw x24, 0x659, x4	// Write all 1s to CSR
	csrrw x24, 0x659, x0	// Write all 0s to CSR
	csrrs x24, 0x659, x4	// Set all CSR bits
	csrrc x24, 0x659, x4	// Clear all CSR bits
	csrrw x24, 0x659, x30	// Restore CSR

// Testing CSR 0x65a
	csrr x18, 0x65a	// Read CSR
	li x17, -1
	csrrw x22, 0x65a, x17	// Write all 1s to CSR
	csrrw x22, 0x65a, x0	// Write all 0s to CSR
	csrrs x22, 0x65a, x17	// Set all CSR bits
	csrrc x22, 0x65a, x17	// Clear all CSR bits
	csrrw x22, 0x65a, x18	// Restore CSR

// Testing CSR 0x65b
	csrr x10, 0x65b	// Read CSR
	li x8, -1
	csrrw x3, 0x65b, x8	// Write all 1s to CSR
	csrrw x3, 0x65b, x0	// Write all 0s to CSR
	csrrs x3, 0x65b, x8	// Set all CSR bits
	csrrc x3, 0x65b, x8	// Clear all CSR bits
	csrrw x3, 0x65b, x10	// Restore CSR

// Testing CSR 0x65c
	csrr x16, 0x65c	// Read CSR
	li x24, -1
	csrrw x1, 0x65c, x24	// Write all 1s to CSR
	csrrw x1, 0x65c, x0	// Write all 0s to CSR
	csrrs x1, 0x65c, x24	// Set all CSR bits
	csrrc x1, 0x65c, x24	// Clear all CSR bits
	csrrw x1, 0x65c, x16	// Restore CSR

// Testing CSR 0x65d
	csrr x10, 0x65d	// Read CSR
	li x6, -1
	csrrw x1, 0x65d, x6	// Write all 1s to CSR
	csrrw x1, 0x65d, x0	// Write all 0s to CSR
	csrrs x1, 0x65d, x6	// Set all CSR bits
	csrrc x1, 0x65d, x6	// Clear all CSR bits
	csrrw x1, 0x65d, x10	// Restore CSR

// Testing CSR 0x65e
	csrr x16, 0x65e	// Read CSR
	li x18, -1
	csrrw x30, 0x65e, x18	// Write all 1s to CSR
	csrrw x30, 0x65e, x0	// Write all 0s to CSR
	csrrs x30, 0x65e, x18	// Set all CSR bits
	csrrc x30, 0x65e, x18	// Clear all CSR bits
	csrrw x30, 0x65e, x16	// Restore CSR

// Testing CSR 0x65f
	csrr x31, 0x65f	// Read CSR
	li x30, -1
	csrrw x4, 0x65f, x30	// Write all 1s to CSR
	csrrw x4, 0x65f, x0	// Write all 0s to CSR
	csrrs x4, 0x65f, x30	// Set all CSR bits
	csrrc x4, 0x65f, x30	// Clear all CSR bits
	csrrw x4, 0x65f, x31	// Restore CSR

// Testing CSR 0x660
	csrr x5, 0x660	// Read CSR
	li x13, -1
	csrrw x10, 0x660, x13	// Write all 1s to CSR
	csrrw x10, 0x660, x0	// Write all 0s to CSR
	csrrs x10, 0x660, x13	// Set all CSR bits
	csrrc x10, 0x660, x13	// Clear all CSR bits
	csrrw x10, 0x660, x5	// Restore CSR

// Testing CSR 0x661
	csrr x20, 0x661	// Read CSR
	li x8, -1
	csrrw x23, 0x661, x8	// Write all 1s to CSR
	csrrw x23, 0x661, x0	// Write all 0s to CSR
	csrrs x23, 0x661, x8	// Set all CSR bits
	csrrc x23, 0x661, x8	// Clear all CSR bits
	csrrw x23, 0x661, x20	// Restore CSR

// Testing CSR 0x662
	csrr x16, 0x662	// Read CSR
	li x12, -1
	csrrw x19, 0x662, x12	// Write all 1s to CSR
	csrrw x19, 0x662, x0	// Write all 0s to CSR
	csrrs x19, 0x662, x12	// Set all CSR bits
	csrrc x19, 0x662, x12	// Clear all CSR bits
	csrrw x19, 0x662, x16	// Restore CSR

// Testing CSR 0x663
	csrr x13, 0x663	// Read CSR
	li x10, -1
	csrrw x17, 0x663, x10	// Write all 1s to CSR
	csrrw x17, 0x663, x0	// Write all 0s to CSR
	csrrs x17, 0x663, x10	// Set all CSR bits
	csrrc x17, 0x663, x10	// Clear all CSR bits
	csrrw x17, 0x663, x13	// Restore CSR

// Testing CSR 0x664
	csrr x2, 0x664	// Read CSR
	li x12, -1
	csrrw x1, 0x664, x12	// Write all 1s to CSR
	csrrw x1, 0x664, x0	// Write all 0s to CSR
	csrrs x1, 0x664, x12	// Set all CSR bits
	csrrc x1, 0x664, x12	// Clear all CSR bits
	csrrw x1, 0x664, x2	// Restore CSR

// Testing CSR 0x665
	csrr x4, 0x665	// Read CSR
	li x6, -1
	csrrw x24, 0x665, x6	// Write all 1s to CSR
	csrrw x24, 0x665, x0	// Write all 0s to CSR
	csrrs x24, 0x665, x6	// Set all CSR bits
	csrrc x24, 0x665, x6	// Clear all CSR bits
	csrrw x24, 0x665, x4	// Restore CSR

// Testing CSR 0x666
	csrr x22, 0x666	// Read CSR
	li x31, -1
	csrrw x9, 0x666, x31	// Write all 1s to CSR
	csrrw x9, 0x666, x0	// Write all 0s to CSR
	csrrs x9, 0x666, x31	// Set all CSR bits
	csrrc x9, 0x666, x31	// Clear all CSR bits
	csrrw x9, 0x666, x22	// Restore CSR

// Testing CSR 0x667
	csrr x25, 0x667	// Read CSR
	li x1, -1
	csrrw x29, 0x667, x1	// Write all 1s to CSR
	csrrw x29, 0x667, x0	// Write all 0s to CSR
	csrrs x29, 0x667, x1	// Set all CSR bits
	csrrc x29, 0x667, x1	// Clear all CSR bits
	csrrw x29, 0x667, x25	// Restore CSR

// Testing CSR 0x668
	csrr x26, 0x668	// Read CSR
	li x9, -1
	csrrw x2, 0x668, x9	// Write all 1s to CSR
	csrrw x2, 0x668, x0	// Write all 0s to CSR
	csrrs x2, 0x668, x9	// Set all CSR bits
	csrrc x2, 0x668, x9	// Clear all CSR bits
	csrrw x2, 0x668, x26	// Restore CSR

// Testing CSR 0x669
	csrr x14, 0x669	// Read CSR
	li x3, -1
	csrrw x1, 0x669, x3	// Write all 1s to CSR
	csrrw x1, 0x669, x0	// Write all 0s to CSR
	csrrs x1, 0x669, x3	// Set all CSR bits
	csrrc x1, 0x669, x3	// Clear all CSR bits
	csrrw x1, 0x669, x14	// Restore CSR

// Testing CSR 0x66a
	csrr x28, 0x66a	// Read CSR
	li x14, -1
	csrrw x17, 0x66a, x14	// Write all 1s to CSR
	csrrw x17, 0x66a, x0	// Write all 0s to CSR
	csrrs x17, 0x66a, x14	// Set all CSR bits
	csrrc x17, 0x66a, x14	// Clear all CSR bits
	csrrw x17, 0x66a, x28	// Restore CSR

// Testing CSR 0x66b
	csrr x9, 0x66b	// Read CSR
	li x7, -1
	csrrw x22, 0x66b, x7	// Write all 1s to CSR
	csrrw x22, 0x66b, x0	// Write all 0s to CSR
	csrrs x22, 0x66b, x7	// Set all CSR bits
	csrrc x22, 0x66b, x7	// Clear all CSR bits
	csrrw x22, 0x66b, x9	// Restore CSR

// Testing CSR 0x66c
	csrr x8, 0x66c	// Read CSR
	li x10, -1
	csrrw x31, 0x66c, x10	// Write all 1s to CSR
	csrrw x31, 0x66c, x0	// Write all 0s to CSR
	csrrs x31, 0x66c, x10	// Set all CSR bits
	csrrc x31, 0x66c, x10	// Clear all CSR bits
	csrrw x31, 0x66c, x8	// Restore CSR

// Testing CSR 0x66d
	csrr x28, 0x66d	// Read CSR
	li x4, -1
	csrrw x3, 0x66d, x4	// Write all 1s to CSR
	csrrw x3, 0x66d, x0	// Write all 0s to CSR
	csrrs x3, 0x66d, x4	// Set all CSR bits
	csrrc x3, 0x66d, x4	// Clear all CSR bits
	csrrw x3, 0x66d, x28	// Restore CSR

// Testing CSR 0x66e
	csrr x12, 0x66e	// Read CSR
	li x13, -1
	csrrw x17, 0x66e, x13	// Write all 1s to CSR
	csrrw x17, 0x66e, x0	// Write all 0s to CSR
	csrrs x17, 0x66e, x13	// Set all CSR bits
	csrrc x17, 0x66e, x13	// Clear all CSR bits
	csrrw x17, 0x66e, x12	// Restore CSR

// Testing CSR 0x66f
	csrr x2, 0x66f	// Read CSR
	li x12, -1
	csrrw x8, 0x66f, x12	// Write all 1s to CSR
	csrrw x8, 0x66f, x0	// Write all 0s to CSR
	csrrs x8, 0x66f, x12	// Set all CSR bits
	csrrc x8, 0x66f, x12	// Clear all CSR bits
	csrrw x8, 0x66f, x2	// Restore CSR

// Testing CSR 0x670
	csrr x10, 0x670	// Read CSR
	li x22, -1
	csrrw x13, 0x670, x22	// Write all 1s to CSR
	csrrw x13, 0x670, x0	// Write all 0s to CSR
	csrrs x13, 0x670, x22	// Set all CSR bits
	csrrc x13, 0x670, x22	// Clear all CSR bits
	csrrw x13, 0x670, x10	// Restore CSR

// Testing CSR 0x671
	csrr x1, 0x671	// Read CSR
	li x15, -1
	csrrw x19, 0x671, x15	// Write all 1s to CSR
	csrrw x19, 0x671, x0	// Write all 0s to CSR
	csrrs x19, 0x671, x15	// Set all CSR bits
	csrrc x19, 0x671, x15	// Clear all CSR bits
	csrrw x19, 0x671, x1	// Restore CSR

// Testing CSR 0x672
	csrr x11, 0x672	// Read CSR
	li x24, -1
	csrrw x5, 0x672, x24	// Write all 1s to CSR
	csrrw x5, 0x672, x0	// Write all 0s to CSR
	csrrs x5, 0x672, x24	// Set all CSR bits
	csrrc x5, 0x672, x24	// Clear all CSR bits
	csrrw x5, 0x672, x11	// Restore CSR

// Testing CSR 0x673
	csrr x4, 0x673	// Read CSR
	li x23, -1
	csrrw x1, 0x673, x23	// Write all 1s to CSR
	csrrw x1, 0x673, x0	// Write all 0s to CSR
	csrrs x1, 0x673, x23	// Set all CSR bits
	csrrc x1, 0x673, x23	// Clear all CSR bits
	csrrw x1, 0x673, x4	// Restore CSR

// Testing CSR 0x674
	csrr x29, 0x674	// Read CSR
	li x13, -1
	csrrw x30, 0x674, x13	// Write all 1s to CSR
	csrrw x30, 0x674, x0	// Write all 0s to CSR
	csrrs x30, 0x674, x13	// Set all CSR bits
	csrrc x30, 0x674, x13	// Clear all CSR bits
	csrrw x30, 0x674, x29	// Restore CSR

// Testing CSR 0x675
	csrr x17, 0x675	// Read CSR
	li x22, -1
	csrrw x5, 0x675, x22	// Write all 1s to CSR
	csrrw x5, 0x675, x0	// Write all 0s to CSR
	csrrs x5, 0x675, x22	// Set all CSR bits
	csrrc x5, 0x675, x22	// Clear all CSR bits
	csrrw x5, 0x675, x17	// Restore CSR

// Testing CSR 0x676
	csrr x21, 0x676	// Read CSR
	li x23, -1
	csrrw x10, 0x676, x23	// Write all 1s to CSR
	csrrw x10, 0x676, x0	// Write all 0s to CSR
	csrrs x10, 0x676, x23	// Set all CSR bits
	csrrc x10, 0x676, x23	// Clear all CSR bits
	csrrw x10, 0x676, x21	// Restore CSR

// Testing CSR 0x677
	csrr x1, 0x677	// Read CSR
	li x12, -1
	csrrw x10, 0x677, x12	// Write all 1s to CSR
	csrrw x10, 0x677, x0	// Write all 0s to CSR
	csrrs x10, 0x677, x12	// Set all CSR bits
	csrrc x10, 0x677, x12	// Clear all CSR bits
	csrrw x10, 0x677, x1	// Restore CSR

// Testing CSR 0x678
	csrr x20, 0x678	// Read CSR
	li x21, -1
	csrrw x26, 0x678, x21	// Write all 1s to CSR
	csrrw x26, 0x678, x0	// Write all 0s to CSR
	csrrs x26, 0x678, x21	// Set all CSR bits
	csrrc x26, 0x678, x21	// Clear all CSR bits
	csrrw x26, 0x678, x20	// Restore CSR

// Testing CSR 0x679
	csrr x15, 0x679	// Read CSR
	li x6, -1
	csrrw x29, 0x679, x6	// Write all 1s to CSR
	csrrw x29, 0x679, x0	// Write all 0s to CSR
	csrrs x29, 0x679, x6	// Set all CSR bits
	csrrc x29, 0x679, x6	// Clear all CSR bits
	csrrw x29, 0x679, x15	// Restore CSR

// Testing CSR 0x67a
	csrr x4, 0x67a	// Read CSR
	li x9, -1
	csrrw x20, 0x67a, x9	// Write all 1s to CSR
	csrrw x20, 0x67a, x0	// Write all 0s to CSR
	csrrs x20, 0x67a, x9	// Set all CSR bits
	csrrc x20, 0x67a, x9	// Clear all CSR bits
	csrrw x20, 0x67a, x4	// Restore CSR

// Testing CSR 0x67b
	csrr x23, 0x67b	// Read CSR
	li x17, -1
	csrrw x5, 0x67b, x17	// Write all 1s to CSR
	csrrw x5, 0x67b, x0	// Write all 0s to CSR
	csrrs x5, 0x67b, x17	// Set all CSR bits
	csrrc x5, 0x67b, x17	// Clear all CSR bits
	csrrw x5, 0x67b, x23	// Restore CSR

// Testing CSR 0x67c
	csrr x18, 0x67c	// Read CSR
	li x27, -1
	csrrw x16, 0x67c, x27	// Write all 1s to CSR
	csrrw x16, 0x67c, x0	// Write all 0s to CSR
	csrrs x16, 0x67c, x27	// Set all CSR bits
	csrrc x16, 0x67c, x27	// Clear all CSR bits
	csrrw x16, 0x67c, x18	// Restore CSR

// Testing CSR 0x67d
	csrr x10, 0x67d	// Read CSR
	li x27, -1
	csrrw x1, 0x67d, x27	// Write all 1s to CSR
	csrrw x1, 0x67d, x0	// Write all 0s to CSR
	csrrs x1, 0x67d, x27	// Set all CSR bits
	csrrc x1, 0x67d, x27	// Clear all CSR bits
	csrrw x1, 0x67d, x10	// Restore CSR

// Testing CSR 0x67e
	csrr x4, 0x67e	// Read CSR
	li x18, -1
	csrrw x8, 0x67e, x18	// Write all 1s to CSR
	csrrw x8, 0x67e, x0	// Write all 0s to CSR
	csrrs x8, 0x67e, x18	// Set all CSR bits
	csrrc x8, 0x67e, x18	// Clear all CSR bits
	csrrw x8, 0x67e, x4	// Restore CSR

// Testing CSR 0x67f
	csrr x14, 0x67f	// Read CSR
	li x19, -1
	csrrw x21, 0x67f, x19	// Write all 1s to CSR
	csrrw x21, 0x67f, x0	// Write all 0s to CSR
	csrrs x21, 0x67f, x19	// Set all CSR bits
	csrrc x21, 0x67f, x19	// Clear all CSR bits
	csrrw x21, 0x67f, x14	// Restore CSR

// Testing CSR 0x680
	csrr x27, 0x680	// Read CSR
	li x14, -1
	csrrw x19, 0x680, x14	// Write all 1s to CSR
	csrrw x19, 0x680, x0	// Write all 0s to CSR
	csrrs x19, 0x680, x14	// Set all CSR bits
	csrrc x19, 0x680, x14	// Clear all CSR bits
	csrrw x19, 0x680, x27	// Restore CSR

// Testing CSR 0x681
	csrr x2, 0x681	// Read CSR
	li x13, -1
	csrrw x21, 0x681, x13	// Write all 1s to CSR
	csrrw x21, 0x681, x0	// Write all 0s to CSR
	csrrs x21, 0x681, x13	// Set all CSR bits
	csrrc x21, 0x681, x13	// Clear all CSR bits
	csrrw x21, 0x681, x2	// Restore CSR

// Testing CSR 0x682
	csrr x12, 0x682	// Read CSR
	li x3, -1
	csrrw x18, 0x682, x3	// Write all 1s to CSR
	csrrw x18, 0x682, x0	// Write all 0s to CSR
	csrrs x18, 0x682, x3	// Set all CSR bits
	csrrc x18, 0x682, x3	// Clear all CSR bits
	csrrw x18, 0x682, x12	// Restore CSR

// Testing CSR 0x683
	csrr x7, 0x683	// Read CSR
	li x18, -1
	csrrw x30, 0x683, x18	// Write all 1s to CSR
	csrrw x30, 0x683, x0	// Write all 0s to CSR
	csrrs x30, 0x683, x18	// Set all CSR bits
	csrrc x30, 0x683, x18	// Clear all CSR bits
	csrrw x30, 0x683, x7	// Restore CSR

// Testing CSR 0x684
	csrr x17, 0x684	// Read CSR
	li x25, -1
	csrrw x23, 0x684, x25	// Write all 1s to CSR
	csrrw x23, 0x684, x0	// Write all 0s to CSR
	csrrs x23, 0x684, x25	// Set all CSR bits
	csrrc x23, 0x684, x25	// Clear all CSR bits
	csrrw x23, 0x684, x17	// Restore CSR

// Testing CSR 0x685
	csrr x29, 0x685	// Read CSR
	li x5, -1
	csrrw x12, 0x685, x5	// Write all 1s to CSR
	csrrw x12, 0x685, x0	// Write all 0s to CSR
	csrrs x12, 0x685, x5	// Set all CSR bits
	csrrc x12, 0x685, x5	// Clear all CSR bits
	csrrw x12, 0x685, x29	// Restore CSR

// Testing CSR 0x686
	csrr x13, 0x686	// Read CSR
	li x16, -1
	csrrw x7, 0x686, x16	// Write all 1s to CSR
	csrrw x7, 0x686, x0	// Write all 0s to CSR
	csrrs x7, 0x686, x16	// Set all CSR bits
	csrrc x7, 0x686, x16	// Clear all CSR bits
	csrrw x7, 0x686, x13	// Restore CSR

// Testing CSR 0x687
	csrr x26, 0x687	// Read CSR
	li x1, -1
	csrrw x27, 0x687, x1	// Write all 1s to CSR
	csrrw x27, 0x687, x0	// Write all 0s to CSR
	csrrs x27, 0x687, x1	// Set all CSR bits
	csrrc x27, 0x687, x1	// Clear all CSR bits
	csrrw x27, 0x687, x26	// Restore CSR

// Testing CSR 0x688
	csrr x15, 0x688	// Read CSR
	li x28, -1
	csrrw x21, 0x688, x28	// Write all 1s to CSR
	csrrw x21, 0x688, x0	// Write all 0s to CSR
	csrrs x21, 0x688, x28	// Set all CSR bits
	csrrc x21, 0x688, x28	// Clear all CSR bits
	csrrw x21, 0x688, x15	// Restore CSR

// Testing CSR 0x689
	csrr x9, 0x689	// Read CSR
	li x31, -1
	csrrw x16, 0x689, x31	// Write all 1s to CSR
	csrrw x16, 0x689, x0	// Write all 0s to CSR
	csrrs x16, 0x689, x31	// Set all CSR bits
	csrrc x16, 0x689, x31	// Clear all CSR bits
	csrrw x16, 0x689, x9	// Restore CSR

// Testing CSR 0x68a
	csrr x28, 0x68a	// Read CSR
	li x8, -1
	csrrw x10, 0x68a, x8	// Write all 1s to CSR
	csrrw x10, 0x68a, x0	// Write all 0s to CSR
	csrrs x10, 0x68a, x8	// Set all CSR bits
	csrrc x10, 0x68a, x8	// Clear all CSR bits
	csrrw x10, 0x68a, x28	// Restore CSR

// Testing CSR 0x68b
	csrr x25, 0x68b	// Read CSR
	li x10, -1
	csrrw x6, 0x68b, x10	// Write all 1s to CSR
	csrrw x6, 0x68b, x0	// Write all 0s to CSR
	csrrs x6, 0x68b, x10	// Set all CSR bits
	csrrc x6, 0x68b, x10	// Clear all CSR bits
	csrrw x6, 0x68b, x25	// Restore CSR

// Testing CSR 0x68c
	csrr x2, 0x68c	// Read CSR
	li x6, -1
	csrrw x19, 0x68c, x6	// Write all 1s to CSR
	csrrw x19, 0x68c, x0	// Write all 0s to CSR
	csrrs x19, 0x68c, x6	// Set all CSR bits
	csrrc x19, 0x68c, x6	// Clear all CSR bits
	csrrw x19, 0x68c, x2	// Restore CSR

// Testing CSR 0x68d
	csrr x6, 0x68d	// Read CSR
	li x29, -1
	csrrw x26, 0x68d, x29	// Write all 1s to CSR
	csrrw x26, 0x68d, x0	// Write all 0s to CSR
	csrrs x26, 0x68d, x29	// Set all CSR bits
	csrrc x26, 0x68d, x29	// Clear all CSR bits
	csrrw x26, 0x68d, x6	// Restore CSR

// Testing CSR 0x68e
	csrr x11, 0x68e	// Read CSR
	li x29, -1
	csrrw x20, 0x68e, x29	// Write all 1s to CSR
	csrrw x20, 0x68e, x0	// Write all 0s to CSR
	csrrs x20, 0x68e, x29	// Set all CSR bits
	csrrc x20, 0x68e, x29	// Clear all CSR bits
	csrrw x20, 0x68e, x11	// Restore CSR

// Testing CSR 0x68f
	csrr x28, 0x68f	// Read CSR
	li x23, -1
	csrrw x8, 0x68f, x23	// Write all 1s to CSR
	csrrw x8, 0x68f, x0	// Write all 0s to CSR
	csrrs x8, 0x68f, x23	// Set all CSR bits
	csrrc x8, 0x68f, x23	// Clear all CSR bits
	csrrw x8, 0x68f, x28	// Restore CSR

// Testing CSR 0x690
	csrr x10, 0x690	// Read CSR
	li x29, -1
	csrrw x2, 0x690, x29	// Write all 1s to CSR
	csrrw x2, 0x690, x0	// Write all 0s to CSR
	csrrs x2, 0x690, x29	// Set all CSR bits
	csrrc x2, 0x690, x29	// Clear all CSR bits
	csrrw x2, 0x690, x10	// Restore CSR

// Testing CSR 0x691
	csrr x15, 0x691	// Read CSR
	li x19, -1
	csrrw x5, 0x691, x19	// Write all 1s to CSR
	csrrw x5, 0x691, x0	// Write all 0s to CSR
	csrrs x5, 0x691, x19	// Set all CSR bits
	csrrc x5, 0x691, x19	// Clear all CSR bits
	csrrw x5, 0x691, x15	// Restore CSR

// Testing CSR 0x692
	csrr x21, 0x692	// Read CSR
	li x11, -1
	csrrw x19, 0x692, x11	// Write all 1s to CSR
	csrrw x19, 0x692, x0	// Write all 0s to CSR
	csrrs x19, 0x692, x11	// Set all CSR bits
	csrrc x19, 0x692, x11	// Clear all CSR bits
	csrrw x19, 0x692, x21	// Restore CSR

// Testing CSR 0x693
	csrr x2, 0x693	// Read CSR
	li x24, -1
	csrrw x18, 0x693, x24	// Write all 1s to CSR
	csrrw x18, 0x693, x0	// Write all 0s to CSR
	csrrs x18, 0x693, x24	// Set all CSR bits
	csrrc x18, 0x693, x24	// Clear all CSR bits
	csrrw x18, 0x693, x2	// Restore CSR

// Testing CSR 0x694
	csrr x17, 0x694	// Read CSR
	li x24, -1
	csrrw x5, 0x694, x24	// Write all 1s to CSR
	csrrw x5, 0x694, x0	// Write all 0s to CSR
	csrrs x5, 0x694, x24	// Set all CSR bits
	csrrc x5, 0x694, x24	// Clear all CSR bits
	csrrw x5, 0x694, x17	// Restore CSR

// Testing CSR 0x695
	csrr x14, 0x695	// Read CSR
	li x18, -1
	csrrw x7, 0x695, x18	// Write all 1s to CSR
	csrrw x7, 0x695, x0	// Write all 0s to CSR
	csrrs x7, 0x695, x18	// Set all CSR bits
	csrrc x7, 0x695, x18	// Clear all CSR bits
	csrrw x7, 0x695, x14	// Restore CSR

// Testing CSR 0x696
	csrr x15, 0x696	// Read CSR
	li x23, -1
	csrrw x29, 0x696, x23	// Write all 1s to CSR
	csrrw x29, 0x696, x0	// Write all 0s to CSR
	csrrs x29, 0x696, x23	// Set all CSR bits
	csrrc x29, 0x696, x23	// Clear all CSR bits
	csrrw x29, 0x696, x15	// Restore CSR

// Testing CSR 0x697
	csrr x20, 0x697	// Read CSR
	li x30, -1
	csrrw x14, 0x697, x30	// Write all 1s to CSR
	csrrw x14, 0x697, x0	// Write all 0s to CSR
	csrrs x14, 0x697, x30	// Set all CSR bits
	csrrc x14, 0x697, x30	// Clear all CSR bits
	csrrw x14, 0x697, x20	// Restore CSR

// Testing CSR 0x698
	csrr x7, 0x698	// Read CSR
	li x29, -1
	csrrw x12, 0x698, x29	// Write all 1s to CSR
	csrrw x12, 0x698, x0	// Write all 0s to CSR
	csrrs x12, 0x698, x29	// Set all CSR bits
	csrrc x12, 0x698, x29	// Clear all CSR bits
	csrrw x12, 0x698, x7	// Restore CSR

// Testing CSR 0x699
	csrr x11, 0x699	// Read CSR
	li x20, -1
	csrrw x10, 0x699, x20	// Write all 1s to CSR
	csrrw x10, 0x699, x0	// Write all 0s to CSR
	csrrs x10, 0x699, x20	// Set all CSR bits
	csrrc x10, 0x699, x20	// Clear all CSR bits
	csrrw x10, 0x699, x11	// Restore CSR

// Testing CSR 0x69a
	csrr x19, 0x69a	// Read CSR
	li x13, -1
	csrrw x29, 0x69a, x13	// Write all 1s to CSR
	csrrw x29, 0x69a, x0	// Write all 0s to CSR
	csrrs x29, 0x69a, x13	// Set all CSR bits
	csrrc x29, 0x69a, x13	// Clear all CSR bits
	csrrw x29, 0x69a, x19	// Restore CSR

// Testing CSR 0x69b
	csrr x31, 0x69b	// Read CSR
	li x18, -1
	csrrw x28, 0x69b, x18	// Write all 1s to CSR
	csrrw x28, 0x69b, x0	// Write all 0s to CSR
	csrrs x28, 0x69b, x18	// Set all CSR bits
	csrrc x28, 0x69b, x18	// Clear all CSR bits
	csrrw x28, 0x69b, x31	// Restore CSR

// Testing CSR 0x69c
	csrr x20, 0x69c	// Read CSR
	li x8, -1
	csrrw x25, 0x69c, x8	// Write all 1s to CSR
	csrrw x25, 0x69c, x0	// Write all 0s to CSR
	csrrs x25, 0x69c, x8	// Set all CSR bits
	csrrc x25, 0x69c, x8	// Clear all CSR bits
	csrrw x25, 0x69c, x20	// Restore CSR

// Testing CSR 0x69d
	csrr x14, 0x69d	// Read CSR
	li x11, -1
	csrrw x2, 0x69d, x11	// Write all 1s to CSR
	csrrw x2, 0x69d, x0	// Write all 0s to CSR
	csrrs x2, 0x69d, x11	// Set all CSR bits
	csrrc x2, 0x69d, x11	// Clear all CSR bits
	csrrw x2, 0x69d, x14	// Restore CSR

// Testing CSR 0x69e
	csrr x30, 0x69e	// Read CSR
	li x5, -1
	csrrw x6, 0x69e, x5	// Write all 1s to CSR
	csrrw x6, 0x69e, x0	// Write all 0s to CSR
	csrrs x6, 0x69e, x5	// Set all CSR bits
	csrrc x6, 0x69e, x5	// Clear all CSR bits
	csrrw x6, 0x69e, x30	// Restore CSR

// Testing CSR 0x69f
	csrr x25, 0x69f	// Read CSR
	li x4, -1
	csrrw x13, 0x69f, x4	// Write all 1s to CSR
	csrrw x13, 0x69f, x0	// Write all 0s to CSR
	csrrs x13, 0x69f, x4	// Set all CSR bits
	csrrc x13, 0x69f, x4	// Clear all CSR bits
	csrrw x13, 0x69f, x25	// Restore CSR

// Testing CSR 0x6a0
	csrr x31, 0x6a0	// Read CSR
	li x16, -1
	csrrw x8, 0x6a0, x16	// Write all 1s to CSR
	csrrw x8, 0x6a0, x0	// Write all 0s to CSR
	csrrs x8, 0x6a0, x16	// Set all CSR bits
	csrrc x8, 0x6a0, x16	// Clear all CSR bits
	csrrw x8, 0x6a0, x31	// Restore CSR

// Testing CSR 0x6a1
	csrr x26, 0x6a1	// Read CSR
	li x1, -1
	csrrw x23, 0x6a1, x1	// Write all 1s to CSR
	csrrw x23, 0x6a1, x0	// Write all 0s to CSR
	csrrs x23, 0x6a1, x1	// Set all CSR bits
	csrrc x23, 0x6a1, x1	// Clear all CSR bits
	csrrw x23, 0x6a1, x26	// Restore CSR

// Testing CSR 0x6a2
	csrr x20, 0x6a2	// Read CSR
	li x14, -1
	csrrw x24, 0x6a2, x14	// Write all 1s to CSR
	csrrw x24, 0x6a2, x0	// Write all 0s to CSR
	csrrs x24, 0x6a2, x14	// Set all CSR bits
	csrrc x24, 0x6a2, x14	// Clear all CSR bits
	csrrw x24, 0x6a2, x20	// Restore CSR

// Testing CSR 0x6a3
	csrr x23, 0x6a3	// Read CSR
	li x25, -1
	csrrw x26, 0x6a3, x25	// Write all 1s to CSR
	csrrw x26, 0x6a3, x0	// Write all 0s to CSR
	csrrs x26, 0x6a3, x25	// Set all CSR bits
	csrrc x26, 0x6a3, x25	// Clear all CSR bits
	csrrw x26, 0x6a3, x23	// Restore CSR

// Testing CSR 0x6a4
	csrr x26, 0x6a4	// Read CSR
	li x3, -1
	csrrw x20, 0x6a4, x3	// Write all 1s to CSR
	csrrw x20, 0x6a4, x0	// Write all 0s to CSR
	csrrs x20, 0x6a4, x3	// Set all CSR bits
	csrrc x20, 0x6a4, x3	// Clear all CSR bits
	csrrw x20, 0x6a4, x26	// Restore CSR

// Testing CSR 0x6a5
	csrr x16, 0x6a5	// Read CSR
	li x10, -1
	csrrw x6, 0x6a5, x10	// Write all 1s to CSR
	csrrw x6, 0x6a5, x0	// Write all 0s to CSR
	csrrs x6, 0x6a5, x10	// Set all CSR bits
	csrrc x6, 0x6a5, x10	// Clear all CSR bits
	csrrw x6, 0x6a5, x16	// Restore CSR

// Testing CSR 0x6a6
	csrr x28, 0x6a6	// Read CSR
	li x23, -1
	csrrw x21, 0x6a6, x23	// Write all 1s to CSR
	csrrw x21, 0x6a6, x0	// Write all 0s to CSR
	csrrs x21, 0x6a6, x23	// Set all CSR bits
	csrrc x21, 0x6a6, x23	// Clear all CSR bits
	csrrw x21, 0x6a6, x28	// Restore CSR

// Testing CSR 0x6a7
	csrr x3, 0x6a7	// Read CSR
	li x15, -1
	csrrw x12, 0x6a7, x15	// Write all 1s to CSR
	csrrw x12, 0x6a7, x0	// Write all 0s to CSR
	csrrs x12, 0x6a7, x15	// Set all CSR bits
	csrrc x12, 0x6a7, x15	// Clear all CSR bits
	csrrw x12, 0x6a7, x3	// Restore CSR

// Testing CSR 0x6a8
	csrr x14, 0x6a8	// Read CSR
	li x22, -1
	csrrw x12, 0x6a8, x22	// Write all 1s to CSR
	csrrw x12, 0x6a8, x0	// Write all 0s to CSR
	csrrs x12, 0x6a8, x22	// Set all CSR bits
	csrrc x12, 0x6a8, x22	// Clear all CSR bits
	csrrw x12, 0x6a8, x14	// Restore CSR

// Testing CSR 0x6a9
	csrr x21, 0x6a9	// Read CSR
	li x11, -1
	csrrw x4, 0x6a9, x11	// Write all 1s to CSR
	csrrw x4, 0x6a9, x0	// Write all 0s to CSR
	csrrs x4, 0x6a9, x11	// Set all CSR bits
	csrrc x4, 0x6a9, x11	// Clear all CSR bits
	csrrw x4, 0x6a9, x21	// Restore CSR

// Testing CSR 0x6aa
	csrr x21, 0x6aa	// Read CSR
	li x1, -1
	csrrw x17, 0x6aa, x1	// Write all 1s to CSR
	csrrw x17, 0x6aa, x0	// Write all 0s to CSR
	csrrs x17, 0x6aa, x1	// Set all CSR bits
	csrrc x17, 0x6aa, x1	// Clear all CSR bits
	csrrw x17, 0x6aa, x21	// Restore CSR

// Testing CSR 0x6ab
	csrr x17, 0x6ab	// Read CSR
	li x15, -1
	csrrw x27, 0x6ab, x15	// Write all 1s to CSR
	csrrw x27, 0x6ab, x0	// Write all 0s to CSR
	csrrs x27, 0x6ab, x15	// Set all CSR bits
	csrrc x27, 0x6ab, x15	// Clear all CSR bits
	csrrw x27, 0x6ab, x17	// Restore CSR

// Testing CSR 0x6ac
	csrr x2, 0x6ac	// Read CSR
	li x3, -1
	csrrw x1, 0x6ac, x3	// Write all 1s to CSR
	csrrw x1, 0x6ac, x0	// Write all 0s to CSR
	csrrs x1, 0x6ac, x3	// Set all CSR bits
	csrrc x1, 0x6ac, x3	// Clear all CSR bits
	csrrw x1, 0x6ac, x2	// Restore CSR

// Testing CSR 0x6ad
	csrr x5, 0x6ad	// Read CSR
	li x31, -1
	csrrw x27, 0x6ad, x31	// Write all 1s to CSR
	csrrw x27, 0x6ad, x0	// Write all 0s to CSR
	csrrs x27, 0x6ad, x31	// Set all CSR bits
	csrrc x27, 0x6ad, x31	// Clear all CSR bits
	csrrw x27, 0x6ad, x5	// Restore CSR

// Testing CSR 0x6ae
	csrr x26, 0x6ae	// Read CSR
	li x22, -1
	csrrw x27, 0x6ae, x22	// Write all 1s to CSR
	csrrw x27, 0x6ae, x0	// Write all 0s to CSR
	csrrs x27, 0x6ae, x22	// Set all CSR bits
	csrrc x27, 0x6ae, x22	// Clear all CSR bits
	csrrw x27, 0x6ae, x26	// Restore CSR

// Testing CSR 0x6af
	csrr x19, 0x6af	// Read CSR
	li x3, -1
	csrrw x13, 0x6af, x3	// Write all 1s to CSR
	csrrw x13, 0x6af, x0	// Write all 0s to CSR
	csrrs x13, 0x6af, x3	// Set all CSR bits
	csrrc x13, 0x6af, x3	// Clear all CSR bits
	csrrw x13, 0x6af, x19	// Restore CSR

// Testing CSR 0x6b0
	csrr x9, 0x6b0	// Read CSR
	li x26, -1
	csrrw x10, 0x6b0, x26	// Write all 1s to CSR
	csrrw x10, 0x6b0, x0	// Write all 0s to CSR
	csrrs x10, 0x6b0, x26	// Set all CSR bits
	csrrc x10, 0x6b0, x26	// Clear all CSR bits
	csrrw x10, 0x6b0, x9	// Restore CSR

// Testing CSR 0x6b1
	csrr x5, 0x6b1	// Read CSR
	li x9, -1
	csrrw x16, 0x6b1, x9	// Write all 1s to CSR
	csrrw x16, 0x6b1, x0	// Write all 0s to CSR
	csrrs x16, 0x6b1, x9	// Set all CSR bits
	csrrc x16, 0x6b1, x9	// Clear all CSR bits
	csrrw x16, 0x6b1, x5	// Restore CSR

// Testing CSR 0x6b2
	csrr x16, 0x6b2	// Read CSR
	li x28, -1
	csrrw x15, 0x6b2, x28	// Write all 1s to CSR
	csrrw x15, 0x6b2, x0	// Write all 0s to CSR
	csrrs x15, 0x6b2, x28	// Set all CSR bits
	csrrc x15, 0x6b2, x28	// Clear all CSR bits
	csrrw x15, 0x6b2, x16	// Restore CSR

// Testing CSR 0x6b3
	csrr x13, 0x6b3	// Read CSR
	li x26, -1
	csrrw x12, 0x6b3, x26	// Write all 1s to CSR
	csrrw x12, 0x6b3, x0	// Write all 0s to CSR
	csrrs x12, 0x6b3, x26	// Set all CSR bits
	csrrc x12, 0x6b3, x26	// Clear all CSR bits
	csrrw x12, 0x6b3, x13	// Restore CSR

// Testing CSR 0x6b4
	csrr x19, 0x6b4	// Read CSR
	li x11, -1
	csrrw x12, 0x6b4, x11	// Write all 1s to CSR
	csrrw x12, 0x6b4, x0	// Write all 0s to CSR
	csrrs x12, 0x6b4, x11	// Set all CSR bits
	csrrc x12, 0x6b4, x11	// Clear all CSR bits
	csrrw x12, 0x6b4, x19	// Restore CSR

// Testing CSR 0x6b5
	csrr x17, 0x6b5	// Read CSR
	li x6, -1
	csrrw x13, 0x6b5, x6	// Write all 1s to CSR
	csrrw x13, 0x6b5, x0	// Write all 0s to CSR
	csrrs x13, 0x6b5, x6	// Set all CSR bits
	csrrc x13, 0x6b5, x6	// Clear all CSR bits
	csrrw x13, 0x6b5, x17	// Restore CSR

// Testing CSR 0x6b6
	csrr x3, 0x6b6	// Read CSR
	li x22, -1
	csrrw x17, 0x6b6, x22	// Write all 1s to CSR
	csrrw x17, 0x6b6, x0	// Write all 0s to CSR
	csrrs x17, 0x6b6, x22	// Set all CSR bits
	csrrc x17, 0x6b6, x22	// Clear all CSR bits
	csrrw x17, 0x6b6, x3	// Restore CSR

// Testing CSR 0x6b7
	csrr x16, 0x6b7	// Read CSR
	li x25, -1
	csrrw x28, 0x6b7, x25	// Write all 1s to CSR
	csrrw x28, 0x6b7, x0	// Write all 0s to CSR
	csrrs x28, 0x6b7, x25	// Set all CSR bits
	csrrc x28, 0x6b7, x25	// Clear all CSR bits
	csrrw x28, 0x6b7, x16	// Restore CSR

// Testing CSR 0x6b8
	csrr x11, 0x6b8	// Read CSR
	li x16, -1
	csrrw x3, 0x6b8, x16	// Write all 1s to CSR
	csrrw x3, 0x6b8, x0	// Write all 0s to CSR
	csrrs x3, 0x6b8, x16	// Set all CSR bits
	csrrc x3, 0x6b8, x16	// Clear all CSR bits
	csrrw x3, 0x6b8, x11	// Restore CSR

// Testing CSR 0x6b9
	csrr x21, 0x6b9	// Read CSR
	li x3, -1
	csrrw x8, 0x6b9, x3	// Write all 1s to CSR
	csrrw x8, 0x6b9, x0	// Write all 0s to CSR
	csrrs x8, 0x6b9, x3	// Set all CSR bits
	csrrc x8, 0x6b9, x3	// Clear all CSR bits
	csrrw x8, 0x6b9, x21	// Restore CSR

// Testing CSR 0x6ba
	csrr x21, 0x6ba	// Read CSR
	li x3, -1
	csrrw x30, 0x6ba, x3	// Write all 1s to CSR
	csrrw x30, 0x6ba, x0	// Write all 0s to CSR
	csrrs x30, 0x6ba, x3	// Set all CSR bits
	csrrc x30, 0x6ba, x3	// Clear all CSR bits
	csrrw x30, 0x6ba, x21	// Restore CSR

// Testing CSR 0x6bb
	csrr x12, 0x6bb	// Read CSR
	li x25, -1
	csrrw x30, 0x6bb, x25	// Write all 1s to CSR
	csrrw x30, 0x6bb, x0	// Write all 0s to CSR
	csrrs x30, 0x6bb, x25	// Set all CSR bits
	csrrc x30, 0x6bb, x25	// Clear all CSR bits
	csrrw x30, 0x6bb, x12	// Restore CSR

// Testing CSR 0x6bc
	csrr x9, 0x6bc	// Read CSR
	li x29, -1
	csrrw x6, 0x6bc, x29	// Write all 1s to CSR
	csrrw x6, 0x6bc, x0	// Write all 0s to CSR
	csrrs x6, 0x6bc, x29	// Set all CSR bits
	csrrc x6, 0x6bc, x29	// Clear all CSR bits
	csrrw x6, 0x6bc, x9	// Restore CSR

// Testing CSR 0x6bd
	csrr x19, 0x6bd	// Read CSR
	li x7, -1
	csrrw x21, 0x6bd, x7	// Write all 1s to CSR
	csrrw x21, 0x6bd, x0	// Write all 0s to CSR
	csrrs x21, 0x6bd, x7	// Set all CSR bits
	csrrc x21, 0x6bd, x7	// Clear all CSR bits
	csrrw x21, 0x6bd, x19	// Restore CSR

// Testing CSR 0x6be
	csrr x23, 0x6be	// Read CSR
	li x28, -1
	csrrw x22, 0x6be, x28	// Write all 1s to CSR
	csrrw x22, 0x6be, x0	// Write all 0s to CSR
	csrrs x22, 0x6be, x28	// Set all CSR bits
	csrrc x22, 0x6be, x28	// Clear all CSR bits
	csrrw x22, 0x6be, x23	// Restore CSR

// Testing CSR 0x6bf
	csrr x22, 0x6bf	// Read CSR
	li x14, -1
	csrrw x3, 0x6bf, x14	// Write all 1s to CSR
	csrrw x3, 0x6bf, x0	// Write all 0s to CSR
	csrrs x3, 0x6bf, x14	// Set all CSR bits
	csrrc x3, 0x6bf, x14	// Clear all CSR bits
	csrrw x3, 0x6bf, x22	// Restore CSR

// Testing CSR 0x6c0
	csrr x21, 0x6c0	// Read CSR
	li x31, -1
	csrrw x28, 0x6c0, x31	// Write all 1s to CSR
	csrrw x28, 0x6c0, x0	// Write all 0s to CSR
	csrrs x28, 0x6c0, x31	// Set all CSR bits
	csrrc x28, 0x6c0, x31	// Clear all CSR bits
	csrrw x28, 0x6c0, x21	// Restore CSR

// Testing CSR 0x6c1
	csrr x20, 0x6c1	// Read CSR
	li x30, -1
	csrrw x13, 0x6c1, x30	// Write all 1s to CSR
	csrrw x13, 0x6c1, x0	// Write all 0s to CSR
	csrrs x13, 0x6c1, x30	// Set all CSR bits
	csrrc x13, 0x6c1, x30	// Clear all CSR bits
	csrrw x13, 0x6c1, x20	// Restore CSR

// Testing CSR 0x6c2
	csrr x18, 0x6c2	// Read CSR
	li x1, -1
	csrrw x7, 0x6c2, x1	// Write all 1s to CSR
	csrrw x7, 0x6c2, x0	// Write all 0s to CSR
	csrrs x7, 0x6c2, x1	// Set all CSR bits
	csrrc x7, 0x6c2, x1	// Clear all CSR bits
	csrrw x7, 0x6c2, x18	// Restore CSR

// Testing CSR 0x6c3
	csrr x11, 0x6c3	// Read CSR
	li x6, -1
	csrrw x23, 0x6c3, x6	// Write all 1s to CSR
	csrrw x23, 0x6c3, x0	// Write all 0s to CSR
	csrrs x23, 0x6c3, x6	// Set all CSR bits
	csrrc x23, 0x6c3, x6	// Clear all CSR bits
	csrrw x23, 0x6c3, x11	// Restore CSR

// Testing CSR 0x6c4
	csrr x31, 0x6c4	// Read CSR
	li x2, -1
	csrrw x30, 0x6c4, x2	// Write all 1s to CSR
	csrrw x30, 0x6c4, x0	// Write all 0s to CSR
	csrrs x30, 0x6c4, x2	// Set all CSR bits
	csrrc x30, 0x6c4, x2	// Clear all CSR bits
	csrrw x30, 0x6c4, x31	// Restore CSR

// Testing CSR 0x6c5
	csrr x24, 0x6c5	// Read CSR
	li x25, -1
	csrrw x3, 0x6c5, x25	// Write all 1s to CSR
	csrrw x3, 0x6c5, x0	// Write all 0s to CSR
	csrrs x3, 0x6c5, x25	// Set all CSR bits
	csrrc x3, 0x6c5, x25	// Clear all CSR bits
	csrrw x3, 0x6c5, x24	// Restore CSR

// Testing CSR 0x6c6
	csrr x23, 0x6c6	// Read CSR
	li x13, -1
	csrrw x21, 0x6c6, x13	// Write all 1s to CSR
	csrrw x21, 0x6c6, x0	// Write all 0s to CSR
	csrrs x21, 0x6c6, x13	// Set all CSR bits
	csrrc x21, 0x6c6, x13	// Clear all CSR bits
	csrrw x21, 0x6c6, x23	// Restore CSR

// Testing CSR 0x6c7
	csrr x17, 0x6c7	// Read CSR
	li x26, -1
	csrrw x2, 0x6c7, x26	// Write all 1s to CSR
	csrrw x2, 0x6c7, x0	// Write all 0s to CSR
	csrrs x2, 0x6c7, x26	// Set all CSR bits
	csrrc x2, 0x6c7, x26	// Clear all CSR bits
	csrrw x2, 0x6c7, x17	// Restore CSR

// Testing CSR 0x6c8
	csrr x12, 0x6c8	// Read CSR
	li x3, -1
	csrrw x19, 0x6c8, x3	// Write all 1s to CSR
	csrrw x19, 0x6c8, x0	// Write all 0s to CSR
	csrrs x19, 0x6c8, x3	// Set all CSR bits
	csrrc x19, 0x6c8, x3	// Clear all CSR bits
	csrrw x19, 0x6c8, x12	// Restore CSR

// Testing CSR 0x6c9
	csrr x28, 0x6c9	// Read CSR
	li x20, -1
	csrrw x31, 0x6c9, x20	// Write all 1s to CSR
	csrrw x31, 0x6c9, x0	// Write all 0s to CSR
	csrrs x31, 0x6c9, x20	// Set all CSR bits
	csrrc x31, 0x6c9, x20	// Clear all CSR bits
	csrrw x31, 0x6c9, x28	// Restore CSR

// Testing CSR 0x6ca
	csrr x6, 0x6ca	// Read CSR
	li x3, -1
	csrrw x27, 0x6ca, x3	// Write all 1s to CSR
	csrrw x27, 0x6ca, x0	// Write all 0s to CSR
	csrrs x27, 0x6ca, x3	// Set all CSR bits
	csrrc x27, 0x6ca, x3	// Clear all CSR bits
	csrrw x27, 0x6ca, x6	// Restore CSR

// Testing CSR 0x6cb
	csrr x10, 0x6cb	// Read CSR
	li x20, -1
	csrrw x18, 0x6cb, x20	// Write all 1s to CSR
	csrrw x18, 0x6cb, x0	// Write all 0s to CSR
	csrrs x18, 0x6cb, x20	// Set all CSR bits
	csrrc x18, 0x6cb, x20	// Clear all CSR bits
	csrrw x18, 0x6cb, x10	// Restore CSR

// Testing CSR 0x6cc
	csrr x30, 0x6cc	// Read CSR
	li x15, -1
	csrrw x1, 0x6cc, x15	// Write all 1s to CSR
	csrrw x1, 0x6cc, x0	// Write all 0s to CSR
	csrrs x1, 0x6cc, x15	// Set all CSR bits
	csrrc x1, 0x6cc, x15	// Clear all CSR bits
	csrrw x1, 0x6cc, x30	// Restore CSR

// Testing CSR 0x6cd
	csrr x17, 0x6cd	// Read CSR
	li x20, -1
	csrrw x29, 0x6cd, x20	// Write all 1s to CSR
	csrrw x29, 0x6cd, x0	// Write all 0s to CSR
	csrrs x29, 0x6cd, x20	// Set all CSR bits
	csrrc x29, 0x6cd, x20	// Clear all CSR bits
	csrrw x29, 0x6cd, x17	// Restore CSR

// Testing CSR 0x6ce
	csrr x4, 0x6ce	// Read CSR
	li x6, -1
	csrrw x28, 0x6ce, x6	// Write all 1s to CSR
	csrrw x28, 0x6ce, x0	// Write all 0s to CSR
	csrrs x28, 0x6ce, x6	// Set all CSR bits
	csrrc x28, 0x6ce, x6	// Clear all CSR bits
	csrrw x28, 0x6ce, x4	// Restore CSR

// Testing CSR 0x6cf
	csrr x21, 0x6cf	// Read CSR
	li x30, -1
	csrrw x1, 0x6cf, x30	// Write all 1s to CSR
	csrrw x1, 0x6cf, x0	// Write all 0s to CSR
	csrrs x1, 0x6cf, x30	// Set all CSR bits
	csrrc x1, 0x6cf, x30	// Clear all CSR bits
	csrrw x1, 0x6cf, x21	// Restore CSR

// Testing CSR 0x6d0
	csrr x7, 0x6d0	// Read CSR
	li x10, -1
	csrrw x4, 0x6d0, x10	// Write all 1s to CSR
	csrrw x4, 0x6d0, x0	// Write all 0s to CSR
	csrrs x4, 0x6d0, x10	// Set all CSR bits
	csrrc x4, 0x6d0, x10	// Clear all CSR bits
	csrrw x4, 0x6d0, x7	// Restore CSR

// Testing CSR 0x6d1
	csrr x22, 0x6d1	// Read CSR
	li x11, -1
	csrrw x6, 0x6d1, x11	// Write all 1s to CSR
	csrrw x6, 0x6d1, x0	// Write all 0s to CSR
	csrrs x6, 0x6d1, x11	// Set all CSR bits
	csrrc x6, 0x6d1, x11	// Clear all CSR bits
	csrrw x6, 0x6d1, x22	// Restore CSR

// Testing CSR 0x6d2
	csrr x29, 0x6d2	// Read CSR
	li x12, -1
	csrrw x8, 0x6d2, x12	// Write all 1s to CSR
	csrrw x8, 0x6d2, x0	// Write all 0s to CSR
	csrrs x8, 0x6d2, x12	// Set all CSR bits
	csrrc x8, 0x6d2, x12	// Clear all CSR bits
	csrrw x8, 0x6d2, x29	// Restore CSR

// Testing CSR 0x6d3
	csrr x2, 0x6d3	// Read CSR
	li x19, -1
	csrrw x24, 0x6d3, x19	// Write all 1s to CSR
	csrrw x24, 0x6d3, x0	// Write all 0s to CSR
	csrrs x24, 0x6d3, x19	// Set all CSR bits
	csrrc x24, 0x6d3, x19	// Clear all CSR bits
	csrrw x24, 0x6d3, x2	// Restore CSR

// Testing CSR 0x6d4
	csrr x30, 0x6d4	// Read CSR
	li x13, -1
	csrrw x22, 0x6d4, x13	// Write all 1s to CSR
	csrrw x22, 0x6d4, x0	// Write all 0s to CSR
	csrrs x22, 0x6d4, x13	// Set all CSR bits
	csrrc x22, 0x6d4, x13	// Clear all CSR bits
	csrrw x22, 0x6d4, x30	// Restore CSR

// Testing CSR 0x6d5
	csrr x28, 0x6d5	// Read CSR
	li x24, -1
	csrrw x23, 0x6d5, x24	// Write all 1s to CSR
	csrrw x23, 0x6d5, x0	// Write all 0s to CSR
	csrrs x23, 0x6d5, x24	// Set all CSR bits
	csrrc x23, 0x6d5, x24	// Clear all CSR bits
	csrrw x23, 0x6d5, x28	// Restore CSR

// Testing CSR 0x6d6
	csrr x13, 0x6d6	// Read CSR
	li x4, -1
	csrrw x29, 0x6d6, x4	// Write all 1s to CSR
	csrrw x29, 0x6d6, x0	// Write all 0s to CSR
	csrrs x29, 0x6d6, x4	// Set all CSR bits
	csrrc x29, 0x6d6, x4	// Clear all CSR bits
	csrrw x29, 0x6d6, x13	// Restore CSR

// Testing CSR 0x6d7
	csrr x19, 0x6d7	// Read CSR
	li x17, -1
	csrrw x27, 0x6d7, x17	// Write all 1s to CSR
	csrrw x27, 0x6d7, x0	// Write all 0s to CSR
	csrrs x27, 0x6d7, x17	// Set all CSR bits
	csrrc x27, 0x6d7, x17	// Clear all CSR bits
	csrrw x27, 0x6d7, x19	// Restore CSR

// Testing CSR 0x6d8
	csrr x17, 0x6d8	// Read CSR
	li x4, -1
	csrrw x2, 0x6d8, x4	// Write all 1s to CSR
	csrrw x2, 0x6d8, x0	// Write all 0s to CSR
	csrrs x2, 0x6d8, x4	// Set all CSR bits
	csrrc x2, 0x6d8, x4	// Clear all CSR bits
	csrrw x2, 0x6d8, x17	// Restore CSR

// Testing CSR 0x6d9
	csrr x17, 0x6d9	// Read CSR
	li x20, -1
	csrrw x15, 0x6d9, x20	// Write all 1s to CSR
	csrrw x15, 0x6d9, x0	// Write all 0s to CSR
	csrrs x15, 0x6d9, x20	// Set all CSR bits
	csrrc x15, 0x6d9, x20	// Clear all CSR bits
	csrrw x15, 0x6d9, x17	// Restore CSR

// Testing CSR 0x6da
	csrr x23, 0x6da	// Read CSR
	li x25, -1
	csrrw x3, 0x6da, x25	// Write all 1s to CSR
	csrrw x3, 0x6da, x0	// Write all 0s to CSR
	csrrs x3, 0x6da, x25	// Set all CSR bits
	csrrc x3, 0x6da, x25	// Clear all CSR bits
	csrrw x3, 0x6da, x23	// Restore CSR

// Testing CSR 0x6db
	csrr x12, 0x6db	// Read CSR
	li x8, -1
	csrrw x14, 0x6db, x8	// Write all 1s to CSR
	csrrw x14, 0x6db, x0	// Write all 0s to CSR
	csrrs x14, 0x6db, x8	// Set all CSR bits
	csrrc x14, 0x6db, x8	// Clear all CSR bits
	csrrw x14, 0x6db, x12	// Restore CSR

// Testing CSR 0x6dc
	csrr x24, 0x6dc	// Read CSR
	li x20, -1
	csrrw x9, 0x6dc, x20	// Write all 1s to CSR
	csrrw x9, 0x6dc, x0	// Write all 0s to CSR
	csrrs x9, 0x6dc, x20	// Set all CSR bits
	csrrc x9, 0x6dc, x20	// Clear all CSR bits
	csrrw x9, 0x6dc, x24	// Restore CSR

// Testing CSR 0x6dd
	csrr x17, 0x6dd	// Read CSR
	li x20, -1
	csrrw x2, 0x6dd, x20	// Write all 1s to CSR
	csrrw x2, 0x6dd, x0	// Write all 0s to CSR
	csrrs x2, 0x6dd, x20	// Set all CSR bits
	csrrc x2, 0x6dd, x20	// Clear all CSR bits
	csrrw x2, 0x6dd, x17	// Restore CSR

// Testing CSR 0x6de
	csrr x29, 0x6de	// Read CSR
	li x1, -1
	csrrw x5, 0x6de, x1	// Write all 1s to CSR
	csrrw x5, 0x6de, x0	// Write all 0s to CSR
	csrrs x5, 0x6de, x1	// Set all CSR bits
	csrrc x5, 0x6de, x1	// Clear all CSR bits
	csrrw x5, 0x6de, x29	// Restore CSR

// Testing CSR 0x6df
	csrr x13, 0x6df	// Read CSR
	li x20, -1
	csrrw x7, 0x6df, x20	// Write all 1s to CSR
	csrrw x7, 0x6df, x0	// Write all 0s to CSR
	csrrs x7, 0x6df, x20	// Set all CSR bits
	csrrc x7, 0x6df, x20	// Clear all CSR bits
	csrrw x7, 0x6df, x13	// Restore CSR

// Testing CSR 0x6e0
	csrr x28, 0x6e0	// Read CSR
	li x22, -1
	csrrw x15, 0x6e0, x22	// Write all 1s to CSR
	csrrw x15, 0x6e0, x0	// Write all 0s to CSR
	csrrs x15, 0x6e0, x22	// Set all CSR bits
	csrrc x15, 0x6e0, x22	// Clear all CSR bits
	csrrw x15, 0x6e0, x28	// Restore CSR

// Testing CSR 0x6e1
	csrr x29, 0x6e1	// Read CSR
	li x5, -1
	csrrw x21, 0x6e1, x5	// Write all 1s to CSR
	csrrw x21, 0x6e1, x0	// Write all 0s to CSR
	csrrs x21, 0x6e1, x5	// Set all CSR bits
	csrrc x21, 0x6e1, x5	// Clear all CSR bits
	csrrw x21, 0x6e1, x29	// Restore CSR

// Testing CSR 0x6e2
	csrr x29, 0x6e2	// Read CSR
	li x21, -1
	csrrw x19, 0x6e2, x21	// Write all 1s to CSR
	csrrw x19, 0x6e2, x0	// Write all 0s to CSR
	csrrs x19, 0x6e2, x21	// Set all CSR bits
	csrrc x19, 0x6e2, x21	// Clear all CSR bits
	csrrw x19, 0x6e2, x29	// Restore CSR

// Testing CSR 0x6e3
	csrr x11, 0x6e3	// Read CSR
	li x10, -1
	csrrw x23, 0x6e3, x10	// Write all 1s to CSR
	csrrw x23, 0x6e3, x0	// Write all 0s to CSR
	csrrs x23, 0x6e3, x10	// Set all CSR bits
	csrrc x23, 0x6e3, x10	// Clear all CSR bits
	csrrw x23, 0x6e3, x11	// Restore CSR

// Testing CSR 0x6e4
	csrr x28, 0x6e4	// Read CSR
	li x2, -1
	csrrw x1, 0x6e4, x2	// Write all 1s to CSR
	csrrw x1, 0x6e4, x0	// Write all 0s to CSR
	csrrs x1, 0x6e4, x2	// Set all CSR bits
	csrrc x1, 0x6e4, x2	// Clear all CSR bits
	csrrw x1, 0x6e4, x28	// Restore CSR

// Testing CSR 0x6e5
	csrr x3, 0x6e5	// Read CSR
	li x8, -1
	csrrw x19, 0x6e5, x8	// Write all 1s to CSR
	csrrw x19, 0x6e5, x0	// Write all 0s to CSR
	csrrs x19, 0x6e5, x8	// Set all CSR bits
	csrrc x19, 0x6e5, x8	// Clear all CSR bits
	csrrw x19, 0x6e5, x3	// Restore CSR

// Testing CSR 0x6e6
	csrr x17, 0x6e6	// Read CSR
	li x4, -1
	csrrw x14, 0x6e6, x4	// Write all 1s to CSR
	csrrw x14, 0x6e6, x0	// Write all 0s to CSR
	csrrs x14, 0x6e6, x4	// Set all CSR bits
	csrrc x14, 0x6e6, x4	// Clear all CSR bits
	csrrw x14, 0x6e6, x17	// Restore CSR

// Testing CSR 0x6e7
	csrr x2, 0x6e7	// Read CSR
	li x11, -1
	csrrw x4, 0x6e7, x11	// Write all 1s to CSR
	csrrw x4, 0x6e7, x0	// Write all 0s to CSR
	csrrs x4, 0x6e7, x11	// Set all CSR bits
	csrrc x4, 0x6e7, x11	// Clear all CSR bits
	csrrw x4, 0x6e7, x2	// Restore CSR

// Testing CSR 0x6e8
	csrr x11, 0x6e8	// Read CSR
	li x9, -1
	csrrw x15, 0x6e8, x9	// Write all 1s to CSR
	csrrw x15, 0x6e8, x0	// Write all 0s to CSR
	csrrs x15, 0x6e8, x9	// Set all CSR bits
	csrrc x15, 0x6e8, x9	// Clear all CSR bits
	csrrw x15, 0x6e8, x11	// Restore CSR

// Testing CSR 0x6e9
	csrr x22, 0x6e9	// Read CSR
	li x1, -1
	csrrw x18, 0x6e9, x1	// Write all 1s to CSR
	csrrw x18, 0x6e9, x0	// Write all 0s to CSR
	csrrs x18, 0x6e9, x1	// Set all CSR bits
	csrrc x18, 0x6e9, x1	// Clear all CSR bits
	csrrw x18, 0x6e9, x22	// Restore CSR

// Testing CSR 0x6ea
	csrr x9, 0x6ea	// Read CSR
	li x24, -1
	csrrw x19, 0x6ea, x24	// Write all 1s to CSR
	csrrw x19, 0x6ea, x0	// Write all 0s to CSR
	csrrs x19, 0x6ea, x24	// Set all CSR bits
	csrrc x19, 0x6ea, x24	// Clear all CSR bits
	csrrw x19, 0x6ea, x9	// Restore CSR

// Testing CSR 0x6eb
	csrr x11, 0x6eb	// Read CSR
	li x17, -1
	csrrw x3, 0x6eb, x17	// Write all 1s to CSR
	csrrw x3, 0x6eb, x0	// Write all 0s to CSR
	csrrs x3, 0x6eb, x17	// Set all CSR bits
	csrrc x3, 0x6eb, x17	// Clear all CSR bits
	csrrw x3, 0x6eb, x11	// Restore CSR

// Testing CSR 0x6ec
	csrr x20, 0x6ec	// Read CSR
	li x29, -1
	csrrw x5, 0x6ec, x29	// Write all 1s to CSR
	csrrw x5, 0x6ec, x0	// Write all 0s to CSR
	csrrs x5, 0x6ec, x29	// Set all CSR bits
	csrrc x5, 0x6ec, x29	// Clear all CSR bits
	csrrw x5, 0x6ec, x20	// Restore CSR

// Testing CSR 0x6ed
	csrr x30, 0x6ed	// Read CSR
	li x5, -1
	csrrw x23, 0x6ed, x5	// Write all 1s to CSR
	csrrw x23, 0x6ed, x0	// Write all 0s to CSR
	csrrs x23, 0x6ed, x5	// Set all CSR bits
	csrrc x23, 0x6ed, x5	// Clear all CSR bits
	csrrw x23, 0x6ed, x30	// Restore CSR

// Testing CSR 0x6ee
	csrr x9, 0x6ee	// Read CSR
	li x11, -1
	csrrw x31, 0x6ee, x11	// Write all 1s to CSR
	csrrw x31, 0x6ee, x0	// Write all 0s to CSR
	csrrs x31, 0x6ee, x11	// Set all CSR bits
	csrrc x31, 0x6ee, x11	// Clear all CSR bits
	csrrw x31, 0x6ee, x9	// Restore CSR

// Testing CSR 0x6ef
	csrr x5, 0x6ef	// Read CSR
	li x14, -1
	csrrw x29, 0x6ef, x14	// Write all 1s to CSR
	csrrw x29, 0x6ef, x0	// Write all 0s to CSR
	csrrs x29, 0x6ef, x14	// Set all CSR bits
	csrrc x29, 0x6ef, x14	// Clear all CSR bits
	csrrw x29, 0x6ef, x5	// Restore CSR

// Testing CSR 0x6f0
	csrr x11, 0x6f0	// Read CSR
	li x4, -1
	csrrw x24, 0x6f0, x4	// Write all 1s to CSR
	csrrw x24, 0x6f0, x0	// Write all 0s to CSR
	csrrs x24, 0x6f0, x4	// Set all CSR bits
	csrrc x24, 0x6f0, x4	// Clear all CSR bits
	csrrw x24, 0x6f0, x11	// Restore CSR

// Testing CSR 0x6f1
	csrr x15, 0x6f1	// Read CSR
	li x27, -1
	csrrw x13, 0x6f1, x27	// Write all 1s to CSR
	csrrw x13, 0x6f1, x0	// Write all 0s to CSR
	csrrs x13, 0x6f1, x27	// Set all CSR bits
	csrrc x13, 0x6f1, x27	// Clear all CSR bits
	csrrw x13, 0x6f1, x15	// Restore CSR

// Testing CSR 0x6f2
	csrr x5, 0x6f2	// Read CSR
	li x25, -1
	csrrw x30, 0x6f2, x25	// Write all 1s to CSR
	csrrw x30, 0x6f2, x0	// Write all 0s to CSR
	csrrs x30, 0x6f2, x25	// Set all CSR bits
	csrrc x30, 0x6f2, x25	// Clear all CSR bits
	csrrw x30, 0x6f2, x5	// Restore CSR

// Testing CSR 0x6f3
	csrr x28, 0x6f3	// Read CSR
	li x27, -1
	csrrw x17, 0x6f3, x27	// Write all 1s to CSR
	csrrw x17, 0x6f3, x0	// Write all 0s to CSR
	csrrs x17, 0x6f3, x27	// Set all CSR bits
	csrrc x17, 0x6f3, x27	// Clear all CSR bits
	csrrw x17, 0x6f3, x28	// Restore CSR

// Testing CSR 0x6f4
	csrr x12, 0x6f4	// Read CSR
	li x17, -1
	csrrw x7, 0x6f4, x17	// Write all 1s to CSR
	csrrw x7, 0x6f4, x0	// Write all 0s to CSR
	csrrs x7, 0x6f4, x17	// Set all CSR bits
	csrrc x7, 0x6f4, x17	// Clear all CSR bits
	csrrw x7, 0x6f4, x12	// Restore CSR

// Testing CSR 0x6f5
	csrr x24, 0x6f5	// Read CSR
	li x16, -1
	csrrw x13, 0x6f5, x16	// Write all 1s to CSR
	csrrw x13, 0x6f5, x0	// Write all 0s to CSR
	csrrs x13, 0x6f5, x16	// Set all CSR bits
	csrrc x13, 0x6f5, x16	// Clear all CSR bits
	csrrw x13, 0x6f5, x24	// Restore CSR

// Testing CSR 0x6f6
	csrr x27, 0x6f6	// Read CSR
	li x7, -1
	csrrw x22, 0x6f6, x7	// Write all 1s to CSR
	csrrw x22, 0x6f6, x0	// Write all 0s to CSR
	csrrs x22, 0x6f6, x7	// Set all CSR bits
	csrrc x22, 0x6f6, x7	// Clear all CSR bits
	csrrw x22, 0x6f6, x27	// Restore CSR

// Testing CSR 0x6f7
	csrr x6, 0x6f7	// Read CSR
	li x11, -1
	csrrw x26, 0x6f7, x11	// Write all 1s to CSR
	csrrw x26, 0x6f7, x0	// Write all 0s to CSR
	csrrs x26, 0x6f7, x11	// Set all CSR bits
	csrrc x26, 0x6f7, x11	// Clear all CSR bits
	csrrw x26, 0x6f7, x6	// Restore CSR

// Testing CSR 0x6f8
	csrr x26, 0x6f8	// Read CSR
	li x20, -1
	csrrw x24, 0x6f8, x20	// Write all 1s to CSR
	csrrw x24, 0x6f8, x0	// Write all 0s to CSR
	csrrs x24, 0x6f8, x20	// Set all CSR bits
	csrrc x24, 0x6f8, x20	// Clear all CSR bits
	csrrw x24, 0x6f8, x26	// Restore CSR

// Testing CSR 0x6f9
	csrr x24, 0x6f9	// Read CSR
	li x18, -1
	csrrw x16, 0x6f9, x18	// Write all 1s to CSR
	csrrw x16, 0x6f9, x0	// Write all 0s to CSR
	csrrs x16, 0x6f9, x18	// Set all CSR bits
	csrrc x16, 0x6f9, x18	// Clear all CSR bits
	csrrw x16, 0x6f9, x24	// Restore CSR

// Testing CSR 0x6fa
	csrr x19, 0x6fa	// Read CSR
	li x10, -1
	csrrw x3, 0x6fa, x10	// Write all 1s to CSR
	csrrw x3, 0x6fa, x0	// Write all 0s to CSR
	csrrs x3, 0x6fa, x10	// Set all CSR bits
	csrrc x3, 0x6fa, x10	// Clear all CSR bits
	csrrw x3, 0x6fa, x19	// Restore CSR

// Testing CSR 0x6fb
	csrr x31, 0x6fb	// Read CSR
	li x6, -1
	csrrw x8, 0x6fb, x6	// Write all 1s to CSR
	csrrw x8, 0x6fb, x0	// Write all 0s to CSR
	csrrs x8, 0x6fb, x6	// Set all CSR bits
	csrrc x8, 0x6fb, x6	// Clear all CSR bits
	csrrw x8, 0x6fb, x31	// Restore CSR

// Testing CSR 0x6fc
	csrr x30, 0x6fc	// Read CSR
	li x22, -1
	csrrw x11, 0x6fc, x22	// Write all 1s to CSR
	csrrw x11, 0x6fc, x0	// Write all 0s to CSR
	csrrs x11, 0x6fc, x22	// Set all CSR bits
	csrrc x11, 0x6fc, x22	// Clear all CSR bits
	csrrw x11, 0x6fc, x30	// Restore CSR

// Testing CSR 0x6fd
	csrr x8, 0x6fd	// Read CSR
	li x1, -1
	csrrw x17, 0x6fd, x1	// Write all 1s to CSR
	csrrw x17, 0x6fd, x0	// Write all 0s to CSR
	csrrs x17, 0x6fd, x1	// Set all CSR bits
	csrrc x17, 0x6fd, x1	// Clear all CSR bits
	csrrw x17, 0x6fd, x8	// Restore CSR

// Testing CSR 0x6fe
	csrr x4, 0x6fe	// Read CSR
	li x23, -1
	csrrw x24, 0x6fe, x23	// Write all 1s to CSR
	csrrw x24, 0x6fe, x0	// Write all 0s to CSR
	csrrs x24, 0x6fe, x23	// Set all CSR bits
	csrrc x24, 0x6fe, x23	// Clear all CSR bits
	csrrw x24, 0x6fe, x4	// Restore CSR

// Testing CSR 0x6ff
	csrr x27, 0x6ff	// Read CSR
	li x19, -1
	csrrw x17, 0x6ff, x19	// Write all 1s to CSR
	csrrw x17, 0x6ff, x0	// Write all 0s to CSR
	csrrs x17, 0x6ff, x19	// Set all CSR bits
	csrrc x17, 0x6ff, x19	// Clear all CSR bits
	csrrw x17, 0x6ff, x27	// Restore CSR

// Testing CSR 0x700
	csrr x4, 0x700	// Read CSR
	li x1, -1
	csrrw x24, 0x700, x1	// Write all 1s to CSR
	csrrw x24, 0x700, x0	// Write all 0s to CSR
	csrrs x24, 0x700, x1	// Set all CSR bits
	csrrc x24, 0x700, x1	// Clear all CSR bits
	csrrw x24, 0x700, x4	// Restore CSR

// Testing CSR 0x701
	csrr x11, 0x701	// Read CSR
	li x16, -1
	csrrw x19, 0x701, x16	// Write all 1s to CSR
	csrrw x19, 0x701, x0	// Write all 0s to CSR
	csrrs x19, 0x701, x16	// Set all CSR bits
	csrrc x19, 0x701, x16	// Clear all CSR bits
	csrrw x19, 0x701, x11	// Restore CSR

// Testing CSR 0x702
	csrr x1, 0x702	// Read CSR
	li x19, -1
	csrrw x31, 0x702, x19	// Write all 1s to CSR
	csrrw x31, 0x702, x0	// Write all 0s to CSR
	csrrs x31, 0x702, x19	// Set all CSR bits
	csrrc x31, 0x702, x19	// Clear all CSR bits
	csrrw x31, 0x702, x1	// Restore CSR

// Testing CSR 0x703
	csrr x30, 0x703	// Read CSR
	li x5, -1
	csrrw x31, 0x703, x5	// Write all 1s to CSR
	csrrw x31, 0x703, x0	// Write all 0s to CSR
	csrrs x31, 0x703, x5	// Set all CSR bits
	csrrc x31, 0x703, x5	// Clear all CSR bits
	csrrw x31, 0x703, x30	// Restore CSR

// Testing CSR 0x704
	csrr x29, 0x704	// Read CSR
	li x23, -1
	csrrw x26, 0x704, x23	// Write all 1s to CSR
	csrrw x26, 0x704, x0	// Write all 0s to CSR
	csrrs x26, 0x704, x23	// Set all CSR bits
	csrrc x26, 0x704, x23	// Clear all CSR bits
	csrrw x26, 0x704, x29	// Restore CSR

// Testing CSR 0x705
	csrr x6, 0x705	// Read CSR
	li x16, -1
	csrrw x24, 0x705, x16	// Write all 1s to CSR
	csrrw x24, 0x705, x0	// Write all 0s to CSR
	csrrs x24, 0x705, x16	// Set all CSR bits
	csrrc x24, 0x705, x16	// Clear all CSR bits
	csrrw x24, 0x705, x6	// Restore CSR

// Testing CSR 0x706
	csrr x21, 0x706	// Read CSR
	li x4, -1
	csrrw x19, 0x706, x4	// Write all 1s to CSR
	csrrw x19, 0x706, x0	// Write all 0s to CSR
	csrrs x19, 0x706, x4	// Set all CSR bits
	csrrc x19, 0x706, x4	// Clear all CSR bits
	csrrw x19, 0x706, x21	// Restore CSR

// Testing CSR 0x707
	csrr x11, 0x707	// Read CSR
	li x18, -1
	csrrw x3, 0x707, x18	// Write all 1s to CSR
	csrrw x3, 0x707, x0	// Write all 0s to CSR
	csrrs x3, 0x707, x18	// Set all CSR bits
	csrrc x3, 0x707, x18	// Clear all CSR bits
	csrrw x3, 0x707, x11	// Restore CSR

// Testing CSR 0x708
	csrr x9, 0x708	// Read CSR
	li x26, -1
	csrrw x8, 0x708, x26	// Write all 1s to CSR
	csrrw x8, 0x708, x0	// Write all 0s to CSR
	csrrs x8, 0x708, x26	// Set all CSR bits
	csrrc x8, 0x708, x26	// Clear all CSR bits
	csrrw x8, 0x708, x9	// Restore CSR

// Testing CSR 0x709
	csrr x17, 0x709	// Read CSR
	li x2, -1
	csrrw x23, 0x709, x2	// Write all 1s to CSR
	csrrw x23, 0x709, x0	// Write all 0s to CSR
	csrrs x23, 0x709, x2	// Set all CSR bits
	csrrc x23, 0x709, x2	// Clear all CSR bits
	csrrw x23, 0x709, x17	// Restore CSR

// Testing CSR 0x70a
	csrr x2, 0x70a	// Read CSR
	li x24, -1
	csrrw x6, 0x70a, x24	// Write all 1s to CSR
	csrrw x6, 0x70a, x0	// Write all 0s to CSR
	csrrs x6, 0x70a, x24	// Set all CSR bits
	csrrc x6, 0x70a, x24	// Clear all CSR bits
	csrrw x6, 0x70a, x2	// Restore CSR

// Testing CSR 0x70b
	csrr x3, 0x70b	// Read CSR
	li x7, -1
	csrrw x27, 0x70b, x7	// Write all 1s to CSR
	csrrw x27, 0x70b, x0	// Write all 0s to CSR
	csrrs x27, 0x70b, x7	// Set all CSR bits
	csrrc x27, 0x70b, x7	// Clear all CSR bits
	csrrw x27, 0x70b, x3	// Restore CSR

// Testing CSR 0x70c
	csrr x16, 0x70c	// Read CSR
	li x6, -1
	csrrw x21, 0x70c, x6	// Write all 1s to CSR
	csrrw x21, 0x70c, x0	// Write all 0s to CSR
	csrrs x21, 0x70c, x6	// Set all CSR bits
	csrrc x21, 0x70c, x6	// Clear all CSR bits
	csrrw x21, 0x70c, x16	// Restore CSR

// Testing CSR 0x70d
	csrr x4, 0x70d	// Read CSR
	li x5, -1
	csrrw x18, 0x70d, x5	// Write all 1s to CSR
	csrrw x18, 0x70d, x0	// Write all 0s to CSR
	csrrs x18, 0x70d, x5	// Set all CSR bits
	csrrc x18, 0x70d, x5	// Clear all CSR bits
	csrrw x18, 0x70d, x4	// Restore CSR

// Testing CSR 0x70e
	csrr x20, 0x70e	// Read CSR
	li x4, -1
	csrrw x29, 0x70e, x4	// Write all 1s to CSR
	csrrw x29, 0x70e, x0	// Write all 0s to CSR
	csrrs x29, 0x70e, x4	// Set all CSR bits
	csrrc x29, 0x70e, x4	// Clear all CSR bits
	csrrw x29, 0x70e, x20	// Restore CSR

// Testing CSR 0x70f
	csrr x31, 0x70f	// Read CSR
	li x1, -1
	csrrw x25, 0x70f, x1	// Write all 1s to CSR
	csrrw x25, 0x70f, x0	// Write all 0s to CSR
	csrrs x25, 0x70f, x1	// Set all CSR bits
	csrrc x25, 0x70f, x1	// Clear all CSR bits
	csrrw x25, 0x70f, x31	// Restore CSR

// Testing CSR 0x710
	csrr x19, 0x710	// Read CSR
	li x10, -1
	csrrw x25, 0x710, x10	// Write all 1s to CSR
	csrrw x25, 0x710, x0	// Write all 0s to CSR
	csrrs x25, 0x710, x10	// Set all CSR bits
	csrrc x25, 0x710, x10	// Clear all CSR bits
	csrrw x25, 0x710, x19	// Restore CSR

// Testing CSR 0x711
	csrr x20, 0x711	// Read CSR
	li x10, -1
	csrrw x7, 0x711, x10	// Write all 1s to CSR
	csrrw x7, 0x711, x0	// Write all 0s to CSR
	csrrs x7, 0x711, x10	// Set all CSR bits
	csrrc x7, 0x711, x10	// Clear all CSR bits
	csrrw x7, 0x711, x20	// Restore CSR

// Testing CSR 0x712
	csrr x26, 0x712	// Read CSR
	li x20, -1
	csrrw x7, 0x712, x20	// Write all 1s to CSR
	csrrw x7, 0x712, x0	// Write all 0s to CSR
	csrrs x7, 0x712, x20	// Set all CSR bits
	csrrc x7, 0x712, x20	// Clear all CSR bits
	csrrw x7, 0x712, x26	// Restore CSR

// Testing CSR 0x713
	csrr x20, 0x713	// Read CSR
	li x4, -1
	csrrw x5, 0x713, x4	// Write all 1s to CSR
	csrrw x5, 0x713, x0	// Write all 0s to CSR
	csrrs x5, 0x713, x4	// Set all CSR bits
	csrrc x5, 0x713, x4	// Clear all CSR bits
	csrrw x5, 0x713, x20	// Restore CSR

// Testing CSR 0x714
	csrr x11, 0x714	// Read CSR
	li x3, -1
	csrrw x23, 0x714, x3	// Write all 1s to CSR
	csrrw x23, 0x714, x0	// Write all 0s to CSR
	csrrs x23, 0x714, x3	// Set all CSR bits
	csrrc x23, 0x714, x3	// Clear all CSR bits
	csrrw x23, 0x714, x11	// Restore CSR

// Testing CSR 0x715
	csrr x20, 0x715	// Read CSR
	li x4, -1
	csrrw x28, 0x715, x4	// Write all 1s to CSR
	csrrw x28, 0x715, x0	// Write all 0s to CSR
	csrrs x28, 0x715, x4	// Set all CSR bits
	csrrc x28, 0x715, x4	// Clear all CSR bits
	csrrw x28, 0x715, x20	// Restore CSR

// Testing CSR 0x716
	csrr x29, 0x716	// Read CSR
	li x14, -1
	csrrw x3, 0x716, x14	// Write all 1s to CSR
	csrrw x3, 0x716, x0	// Write all 0s to CSR
	csrrs x3, 0x716, x14	// Set all CSR bits
	csrrc x3, 0x716, x14	// Clear all CSR bits
	csrrw x3, 0x716, x29	// Restore CSR

// Testing CSR 0x717
	csrr x6, 0x717	// Read CSR
	li x25, -1
	csrrw x23, 0x717, x25	// Write all 1s to CSR
	csrrw x23, 0x717, x0	// Write all 0s to CSR
	csrrs x23, 0x717, x25	// Set all CSR bits
	csrrc x23, 0x717, x25	// Clear all CSR bits
	csrrw x23, 0x717, x6	// Restore CSR

// Testing CSR 0x718
	csrr x6, 0x718	// Read CSR
	li x17, -1
	csrrw x19, 0x718, x17	// Write all 1s to CSR
	csrrw x19, 0x718, x0	// Write all 0s to CSR
	csrrs x19, 0x718, x17	// Set all CSR bits
	csrrc x19, 0x718, x17	// Clear all CSR bits
	csrrw x19, 0x718, x6	// Restore CSR

// Testing CSR 0x719
	csrr x25, 0x719	// Read CSR
	li x31, -1
	csrrw x18, 0x719, x31	// Write all 1s to CSR
	csrrw x18, 0x719, x0	// Write all 0s to CSR
	csrrs x18, 0x719, x31	// Set all CSR bits
	csrrc x18, 0x719, x31	// Clear all CSR bits
	csrrw x18, 0x719, x25	// Restore CSR

// Testing CSR 0x71a
	csrr x3, 0x71a	// Read CSR
	li x23, -1
	csrrw x8, 0x71a, x23	// Write all 1s to CSR
	csrrw x8, 0x71a, x0	// Write all 0s to CSR
	csrrs x8, 0x71a, x23	// Set all CSR bits
	csrrc x8, 0x71a, x23	// Clear all CSR bits
	csrrw x8, 0x71a, x3	// Restore CSR

// Testing CSR 0x71b
	csrr x5, 0x71b	// Read CSR
	li x6, -1
	csrrw x10, 0x71b, x6	// Write all 1s to CSR
	csrrw x10, 0x71b, x0	// Write all 0s to CSR
	csrrs x10, 0x71b, x6	// Set all CSR bits
	csrrc x10, 0x71b, x6	// Clear all CSR bits
	csrrw x10, 0x71b, x5	// Restore CSR

// Testing CSR 0x71c
	csrr x26, 0x71c	// Read CSR
	li x18, -1
	csrrw x12, 0x71c, x18	// Write all 1s to CSR
	csrrw x12, 0x71c, x0	// Write all 0s to CSR
	csrrs x12, 0x71c, x18	// Set all CSR bits
	csrrc x12, 0x71c, x18	// Clear all CSR bits
	csrrw x12, 0x71c, x26	// Restore CSR

// Testing CSR 0x71d
	csrr x21, 0x71d	// Read CSR
	li x20, -1
	csrrw x19, 0x71d, x20	// Write all 1s to CSR
	csrrw x19, 0x71d, x0	// Write all 0s to CSR
	csrrs x19, 0x71d, x20	// Set all CSR bits
	csrrc x19, 0x71d, x20	// Clear all CSR bits
	csrrw x19, 0x71d, x21	// Restore CSR

// Testing CSR 0x71e
	csrr x7, 0x71e	// Read CSR
	li x24, -1
	csrrw x31, 0x71e, x24	// Write all 1s to CSR
	csrrw x31, 0x71e, x0	// Write all 0s to CSR
	csrrs x31, 0x71e, x24	// Set all CSR bits
	csrrc x31, 0x71e, x24	// Clear all CSR bits
	csrrw x31, 0x71e, x7	// Restore CSR

// Testing CSR 0x71f
	csrr x23, 0x71f	// Read CSR
	li x1, -1
	csrrw x11, 0x71f, x1	// Write all 1s to CSR
	csrrw x11, 0x71f, x0	// Write all 0s to CSR
	csrrs x11, 0x71f, x1	// Set all CSR bits
	csrrc x11, 0x71f, x1	// Clear all CSR bits
	csrrw x11, 0x71f, x23	// Restore CSR

// Testing CSR 0x720
	csrr x23, 0x720	// Read CSR
	li x31, -1
	csrrw x14, 0x720, x31	// Write all 1s to CSR
	csrrw x14, 0x720, x0	// Write all 0s to CSR
	csrrs x14, 0x720, x31	// Set all CSR bits
	csrrc x14, 0x720, x31	// Clear all CSR bits
	csrrw x14, 0x720, x23	// Restore CSR

// Testing CSR 0x721
	csrr x31, 0x721	// Read CSR
	li x9, -1
	csrrw x10, 0x721, x9	// Write all 1s to CSR
	csrrw x10, 0x721, x0	// Write all 0s to CSR
	csrrs x10, 0x721, x9	// Set all CSR bits
	csrrc x10, 0x721, x9	// Clear all CSR bits
	csrrw x10, 0x721, x31	// Restore CSR

// Testing CSR 0x722
	csrr x16, 0x722	// Read CSR
	li x15, -1
	csrrw x13, 0x722, x15	// Write all 1s to CSR
	csrrw x13, 0x722, x0	// Write all 0s to CSR
	csrrs x13, 0x722, x15	// Set all CSR bits
	csrrc x13, 0x722, x15	// Clear all CSR bits
	csrrw x13, 0x722, x16	// Restore CSR

// Testing CSR 0x723
	csrr x3, 0x723	// Read CSR
	li x18, -1
	csrrw x5, 0x723, x18	// Write all 1s to CSR
	csrrw x5, 0x723, x0	// Write all 0s to CSR
	csrrs x5, 0x723, x18	// Set all CSR bits
	csrrc x5, 0x723, x18	// Clear all CSR bits
	csrrw x5, 0x723, x3	// Restore CSR

// Testing CSR 0x724
	csrr x25, 0x724	// Read CSR
	li x27, -1
	csrrw x4, 0x724, x27	// Write all 1s to CSR
	csrrw x4, 0x724, x0	// Write all 0s to CSR
	csrrs x4, 0x724, x27	// Set all CSR bits
	csrrc x4, 0x724, x27	// Clear all CSR bits
	csrrw x4, 0x724, x25	// Restore CSR

// Testing CSR 0x725
	csrr x8, 0x725	// Read CSR
	li x2, -1
	csrrw x17, 0x725, x2	// Write all 1s to CSR
	csrrw x17, 0x725, x0	// Write all 0s to CSR
	csrrs x17, 0x725, x2	// Set all CSR bits
	csrrc x17, 0x725, x2	// Clear all CSR bits
	csrrw x17, 0x725, x8	// Restore CSR

// Testing CSR 0x726
	csrr x23, 0x726	// Read CSR
	li x20, -1
	csrrw x29, 0x726, x20	// Write all 1s to CSR
	csrrw x29, 0x726, x0	// Write all 0s to CSR
	csrrs x29, 0x726, x20	// Set all CSR bits
	csrrc x29, 0x726, x20	// Clear all CSR bits
	csrrw x29, 0x726, x23	// Restore CSR

// Testing CSR 0x727
	csrr x4, 0x727	// Read CSR
	li x3, -1
	csrrw x14, 0x727, x3	// Write all 1s to CSR
	csrrw x14, 0x727, x0	// Write all 0s to CSR
	csrrs x14, 0x727, x3	// Set all CSR bits
	csrrc x14, 0x727, x3	// Clear all CSR bits
	csrrw x14, 0x727, x4	// Restore CSR

// Testing CSR 0x728
	csrr x12, 0x728	// Read CSR
	li x4, -1
	csrrw x28, 0x728, x4	// Write all 1s to CSR
	csrrw x28, 0x728, x0	// Write all 0s to CSR
	csrrs x28, 0x728, x4	// Set all CSR bits
	csrrc x28, 0x728, x4	// Clear all CSR bits
	csrrw x28, 0x728, x12	// Restore CSR

// Testing CSR 0x729
	csrr x12, 0x729	// Read CSR
	li x16, -1
	csrrw x20, 0x729, x16	// Write all 1s to CSR
	csrrw x20, 0x729, x0	// Write all 0s to CSR
	csrrs x20, 0x729, x16	// Set all CSR bits
	csrrc x20, 0x729, x16	// Clear all CSR bits
	csrrw x20, 0x729, x12	// Restore CSR

// Testing CSR 0x72a
	csrr x13, 0x72a	// Read CSR
	li x22, -1
	csrrw x16, 0x72a, x22	// Write all 1s to CSR
	csrrw x16, 0x72a, x0	// Write all 0s to CSR
	csrrs x16, 0x72a, x22	// Set all CSR bits
	csrrc x16, 0x72a, x22	// Clear all CSR bits
	csrrw x16, 0x72a, x13	// Restore CSR

// Testing CSR 0x72b
	csrr x7, 0x72b	// Read CSR
	li x23, -1
	csrrw x17, 0x72b, x23	// Write all 1s to CSR
	csrrw x17, 0x72b, x0	// Write all 0s to CSR
	csrrs x17, 0x72b, x23	// Set all CSR bits
	csrrc x17, 0x72b, x23	// Clear all CSR bits
	csrrw x17, 0x72b, x7	// Restore CSR

// Testing CSR 0x72c
	csrr x29, 0x72c	// Read CSR
	li x2, -1
	csrrw x23, 0x72c, x2	// Write all 1s to CSR
	csrrw x23, 0x72c, x0	// Write all 0s to CSR
	csrrs x23, 0x72c, x2	// Set all CSR bits
	csrrc x23, 0x72c, x2	// Clear all CSR bits
	csrrw x23, 0x72c, x29	// Restore CSR

// Testing CSR 0x72d
	csrr x24, 0x72d	// Read CSR
	li x28, -1
	csrrw x13, 0x72d, x28	// Write all 1s to CSR
	csrrw x13, 0x72d, x0	// Write all 0s to CSR
	csrrs x13, 0x72d, x28	// Set all CSR bits
	csrrc x13, 0x72d, x28	// Clear all CSR bits
	csrrw x13, 0x72d, x24	// Restore CSR

// Testing CSR 0x72e
	csrr x15, 0x72e	// Read CSR
	li x27, -1
	csrrw x7, 0x72e, x27	// Write all 1s to CSR
	csrrw x7, 0x72e, x0	// Write all 0s to CSR
	csrrs x7, 0x72e, x27	// Set all CSR bits
	csrrc x7, 0x72e, x27	// Clear all CSR bits
	csrrw x7, 0x72e, x15	// Restore CSR

// Testing CSR 0x72f
	csrr x10, 0x72f	// Read CSR
	li x16, -1
	csrrw x21, 0x72f, x16	// Write all 1s to CSR
	csrrw x21, 0x72f, x0	// Write all 0s to CSR
	csrrs x21, 0x72f, x16	// Set all CSR bits
	csrrc x21, 0x72f, x16	// Clear all CSR bits
	csrrw x21, 0x72f, x10	// Restore CSR

// Testing CSR 0x730
	csrr x26, 0x730	// Read CSR
	li x17, -1
	csrrw x15, 0x730, x17	// Write all 1s to CSR
	csrrw x15, 0x730, x0	// Write all 0s to CSR
	csrrs x15, 0x730, x17	// Set all CSR bits
	csrrc x15, 0x730, x17	// Clear all CSR bits
	csrrw x15, 0x730, x26	// Restore CSR

// Testing CSR 0x731
	csrr x12, 0x731	// Read CSR
	li x2, -1
	csrrw x13, 0x731, x2	// Write all 1s to CSR
	csrrw x13, 0x731, x0	// Write all 0s to CSR
	csrrs x13, 0x731, x2	// Set all CSR bits
	csrrc x13, 0x731, x2	// Clear all CSR bits
	csrrw x13, 0x731, x12	// Restore CSR

// Testing CSR 0x732
	csrr x8, 0x732	// Read CSR
	li x1, -1
	csrrw x26, 0x732, x1	// Write all 1s to CSR
	csrrw x26, 0x732, x0	// Write all 0s to CSR
	csrrs x26, 0x732, x1	// Set all CSR bits
	csrrc x26, 0x732, x1	// Clear all CSR bits
	csrrw x26, 0x732, x8	// Restore CSR

// Testing CSR 0x733
	csrr x9, 0x733	// Read CSR
	li x24, -1
	csrrw x17, 0x733, x24	// Write all 1s to CSR
	csrrw x17, 0x733, x0	// Write all 0s to CSR
	csrrs x17, 0x733, x24	// Set all CSR bits
	csrrc x17, 0x733, x24	// Clear all CSR bits
	csrrw x17, 0x733, x9	// Restore CSR

// Testing CSR 0x734
	csrr x6, 0x734	// Read CSR
	li x20, -1
	csrrw x13, 0x734, x20	// Write all 1s to CSR
	csrrw x13, 0x734, x0	// Write all 0s to CSR
	csrrs x13, 0x734, x20	// Set all CSR bits
	csrrc x13, 0x734, x20	// Clear all CSR bits
	csrrw x13, 0x734, x6	// Restore CSR

// Testing CSR 0x735
	csrr x14, 0x735	// Read CSR
	li x12, -1
	csrrw x31, 0x735, x12	// Write all 1s to CSR
	csrrw x31, 0x735, x0	// Write all 0s to CSR
	csrrs x31, 0x735, x12	// Set all CSR bits
	csrrc x31, 0x735, x12	// Clear all CSR bits
	csrrw x31, 0x735, x14	// Restore CSR

// Testing CSR 0x736
	csrr x10, 0x736	// Read CSR
	li x31, -1
	csrrw x4, 0x736, x31	// Write all 1s to CSR
	csrrw x4, 0x736, x0	// Write all 0s to CSR
	csrrs x4, 0x736, x31	// Set all CSR bits
	csrrc x4, 0x736, x31	// Clear all CSR bits
	csrrw x4, 0x736, x10	// Restore CSR

// Testing CSR 0x737
	csrr x10, 0x737	// Read CSR
	li x9, -1
	csrrw x8, 0x737, x9	// Write all 1s to CSR
	csrrw x8, 0x737, x0	// Write all 0s to CSR
	csrrs x8, 0x737, x9	// Set all CSR bits
	csrrc x8, 0x737, x9	// Clear all CSR bits
	csrrw x8, 0x737, x10	// Restore CSR

// Testing CSR 0x738
	csrr x14, 0x738	// Read CSR
	li x12, -1
	csrrw x29, 0x738, x12	// Write all 1s to CSR
	csrrw x29, 0x738, x0	// Write all 0s to CSR
	csrrs x29, 0x738, x12	// Set all CSR bits
	csrrc x29, 0x738, x12	// Clear all CSR bits
	csrrw x29, 0x738, x14	// Restore CSR

// Testing CSR 0x739
	csrr x12, 0x739	// Read CSR
	li x25, -1
	csrrw x15, 0x739, x25	// Write all 1s to CSR
	csrrw x15, 0x739, x0	// Write all 0s to CSR
	csrrs x15, 0x739, x25	// Set all CSR bits
	csrrc x15, 0x739, x25	// Clear all CSR bits
	csrrw x15, 0x739, x12	// Restore CSR

// Testing CSR 0x73a
	csrr x16, 0x73a	// Read CSR
	li x8, -1
	csrrw x6, 0x73a, x8	// Write all 1s to CSR
	csrrw x6, 0x73a, x0	// Write all 0s to CSR
	csrrs x6, 0x73a, x8	// Set all CSR bits
	csrrc x6, 0x73a, x8	// Clear all CSR bits
	csrrw x6, 0x73a, x16	// Restore CSR

// Testing CSR 0x73b
	csrr x14, 0x73b	// Read CSR
	li x10, -1
	csrrw x3, 0x73b, x10	// Write all 1s to CSR
	csrrw x3, 0x73b, x0	// Write all 0s to CSR
	csrrs x3, 0x73b, x10	// Set all CSR bits
	csrrc x3, 0x73b, x10	// Clear all CSR bits
	csrrw x3, 0x73b, x14	// Restore CSR

// Testing CSR 0x73c
	csrr x15, 0x73c	// Read CSR
	li x11, -1
	csrrw x5, 0x73c, x11	// Write all 1s to CSR
	csrrw x5, 0x73c, x0	// Write all 0s to CSR
	csrrs x5, 0x73c, x11	// Set all CSR bits
	csrrc x5, 0x73c, x11	// Clear all CSR bits
	csrrw x5, 0x73c, x15	// Restore CSR

// Testing CSR 0x73d
	csrr x29, 0x73d	// Read CSR
	li x23, -1
	csrrw x18, 0x73d, x23	// Write all 1s to CSR
	csrrw x18, 0x73d, x0	// Write all 0s to CSR
	csrrs x18, 0x73d, x23	// Set all CSR bits
	csrrc x18, 0x73d, x23	// Clear all CSR bits
	csrrw x18, 0x73d, x29	// Restore CSR

// Testing CSR 0x73e
	csrr x25, 0x73e	// Read CSR
	li x4, -1
	csrrw x9, 0x73e, x4	// Write all 1s to CSR
	csrrw x9, 0x73e, x0	// Write all 0s to CSR
	csrrs x9, 0x73e, x4	// Set all CSR bits
	csrrc x9, 0x73e, x4	// Clear all CSR bits
	csrrw x9, 0x73e, x25	// Restore CSR

// Testing CSR 0x73f
	csrr x28, 0x73f	// Read CSR
	li x5, -1
	csrrw x25, 0x73f, x5	// Write all 1s to CSR
	csrrw x25, 0x73f, x0	// Write all 0s to CSR
	csrrs x25, 0x73f, x5	// Set all CSR bits
	csrrc x25, 0x73f, x5	// Clear all CSR bits
	csrrw x25, 0x73f, x28	// Restore CSR

// Testing CSR 0x740
	csrr x19, 0x740	// Read CSR
	li x26, -1
	csrrw x9, 0x740, x26	// Write all 1s to CSR
	csrrw x9, 0x740, x0	// Write all 0s to CSR
	csrrs x9, 0x740, x26	// Set all CSR bits
	csrrc x9, 0x740, x26	// Clear all CSR bits
	csrrw x9, 0x740, x19	// Restore CSR

// Testing CSR 0x741
	csrr x6, 0x741	// Read CSR
	li x11, -1
	csrrw x8, 0x741, x11	// Write all 1s to CSR
	csrrw x8, 0x741, x0	// Write all 0s to CSR
	csrrs x8, 0x741, x11	// Set all CSR bits
	csrrc x8, 0x741, x11	// Clear all CSR bits
	csrrw x8, 0x741, x6	// Restore CSR

// Testing CSR 0x742
	csrr x15, 0x742	// Read CSR
	li x30, -1
	csrrw x24, 0x742, x30	// Write all 1s to CSR
	csrrw x24, 0x742, x0	// Write all 0s to CSR
	csrrs x24, 0x742, x30	// Set all CSR bits
	csrrc x24, 0x742, x30	// Clear all CSR bits
	csrrw x24, 0x742, x15	// Restore CSR

// Testing CSR 0x743
	csrr x27, 0x743	// Read CSR
	li x9, -1
	csrrw x4, 0x743, x9	// Write all 1s to CSR
	csrrw x4, 0x743, x0	// Write all 0s to CSR
	csrrs x4, 0x743, x9	// Set all CSR bits
	csrrc x4, 0x743, x9	// Clear all CSR bits
	csrrw x4, 0x743, x27	// Restore CSR

// Testing CSR 0x744
	csrr x10, 0x744	// Read CSR
	li x11, -1
	csrrw x1, 0x744, x11	// Write all 1s to CSR
	csrrw x1, 0x744, x0	// Write all 0s to CSR
	csrrs x1, 0x744, x11	// Set all CSR bits
	csrrc x1, 0x744, x11	// Clear all CSR bits
	csrrw x1, 0x744, x10	// Restore CSR

// Testing CSR 0x745
	csrr x1, 0x745	// Read CSR
	li x19, -1
	csrrw x31, 0x745, x19	// Write all 1s to CSR
	csrrw x31, 0x745, x0	// Write all 0s to CSR
	csrrs x31, 0x745, x19	// Set all CSR bits
	csrrc x31, 0x745, x19	// Clear all CSR bits
	csrrw x31, 0x745, x1	// Restore CSR

// Testing CSR 0x746
	csrr x24, 0x746	// Read CSR
	li x16, -1
	csrrw x11, 0x746, x16	// Write all 1s to CSR
	csrrw x11, 0x746, x0	// Write all 0s to CSR
	csrrs x11, 0x746, x16	// Set all CSR bits
	csrrc x11, 0x746, x16	// Clear all CSR bits
	csrrw x11, 0x746, x24	// Restore CSR

// Testing CSR 0x747
	csrr x2, 0x747	// Read CSR
	li x21, -1
	csrrw x13, 0x747, x21	// Write all 1s to CSR
	csrrw x13, 0x747, x0	// Write all 0s to CSR
	csrrs x13, 0x747, x21	// Set all CSR bits
	csrrc x13, 0x747, x21	// Clear all CSR bits
	csrrw x13, 0x747, x2	// Restore CSR

// Testing CSR 0x748
	csrr x30, 0x748	// Read CSR
	li x1, -1
	csrrw x2, 0x748, x1	// Write all 1s to CSR
	csrrw x2, 0x748, x0	// Write all 0s to CSR
	csrrs x2, 0x748, x1	// Set all CSR bits
	csrrc x2, 0x748, x1	// Clear all CSR bits
	csrrw x2, 0x748, x30	// Restore CSR

// Testing CSR 0x749
	csrr x2, 0x749	// Read CSR
	li x3, -1
	csrrw x24, 0x749, x3	// Write all 1s to CSR
	csrrw x24, 0x749, x0	// Write all 0s to CSR
	csrrs x24, 0x749, x3	// Set all CSR bits
	csrrc x24, 0x749, x3	// Clear all CSR bits
	csrrw x24, 0x749, x2	// Restore CSR

// Testing CSR 0x74a
	csrr x10, 0x74a	// Read CSR
	li x11, -1
	csrrw x2, 0x74a, x11	// Write all 1s to CSR
	csrrw x2, 0x74a, x0	// Write all 0s to CSR
	csrrs x2, 0x74a, x11	// Set all CSR bits
	csrrc x2, 0x74a, x11	// Clear all CSR bits
	csrrw x2, 0x74a, x10	// Restore CSR

// Testing CSR 0x74b
	csrr x27, 0x74b	// Read CSR
	li x11, -1
	csrrw x23, 0x74b, x11	// Write all 1s to CSR
	csrrw x23, 0x74b, x0	// Write all 0s to CSR
	csrrs x23, 0x74b, x11	// Set all CSR bits
	csrrc x23, 0x74b, x11	// Clear all CSR bits
	csrrw x23, 0x74b, x27	// Restore CSR

// Testing CSR 0x74c
	csrr x28, 0x74c	// Read CSR
	li x29, -1
	csrrw x31, 0x74c, x29	// Write all 1s to CSR
	csrrw x31, 0x74c, x0	// Write all 0s to CSR
	csrrs x31, 0x74c, x29	// Set all CSR bits
	csrrc x31, 0x74c, x29	// Clear all CSR bits
	csrrw x31, 0x74c, x28	// Restore CSR

// Testing CSR 0x74d
	csrr x18, 0x74d	// Read CSR
	li x2, -1
	csrrw x24, 0x74d, x2	// Write all 1s to CSR
	csrrw x24, 0x74d, x0	// Write all 0s to CSR
	csrrs x24, 0x74d, x2	// Set all CSR bits
	csrrc x24, 0x74d, x2	// Clear all CSR bits
	csrrw x24, 0x74d, x18	// Restore CSR

// Testing CSR 0x74e
	csrr x23, 0x74e	// Read CSR
	li x11, -1
	csrrw x8, 0x74e, x11	// Write all 1s to CSR
	csrrw x8, 0x74e, x0	// Write all 0s to CSR
	csrrs x8, 0x74e, x11	// Set all CSR bits
	csrrc x8, 0x74e, x11	// Clear all CSR bits
	csrrw x8, 0x74e, x23	// Restore CSR

// Testing CSR 0x74f
	csrr x23, 0x74f	// Read CSR
	li x13, -1
	csrrw x30, 0x74f, x13	// Write all 1s to CSR
	csrrw x30, 0x74f, x0	// Write all 0s to CSR
	csrrs x30, 0x74f, x13	// Set all CSR bits
	csrrc x30, 0x74f, x13	// Clear all CSR bits
	csrrw x30, 0x74f, x23	// Restore CSR

// Testing CSR 0x750
	csrr x30, 0x750	// Read CSR
	li x5, -1
	csrrw x12, 0x750, x5	// Write all 1s to CSR
	csrrw x12, 0x750, x0	// Write all 0s to CSR
	csrrs x12, 0x750, x5	// Set all CSR bits
	csrrc x12, 0x750, x5	// Clear all CSR bits
	csrrw x12, 0x750, x30	// Restore CSR

// Testing CSR 0x751
	csrr x17, 0x751	// Read CSR
	li x18, -1
	csrrw x26, 0x751, x18	// Write all 1s to CSR
	csrrw x26, 0x751, x0	// Write all 0s to CSR
	csrrs x26, 0x751, x18	// Set all CSR bits
	csrrc x26, 0x751, x18	// Clear all CSR bits
	csrrw x26, 0x751, x17	// Restore CSR

// Testing CSR 0x752
	csrr x23, 0x752	// Read CSR
	li x24, -1
	csrrw x25, 0x752, x24	// Write all 1s to CSR
	csrrw x25, 0x752, x0	// Write all 0s to CSR
	csrrs x25, 0x752, x24	// Set all CSR bits
	csrrc x25, 0x752, x24	// Clear all CSR bits
	csrrw x25, 0x752, x23	// Restore CSR

// Testing CSR 0x753
	csrr x24, 0x753	// Read CSR
	li x18, -1
	csrrw x27, 0x753, x18	// Write all 1s to CSR
	csrrw x27, 0x753, x0	// Write all 0s to CSR
	csrrs x27, 0x753, x18	// Set all CSR bits
	csrrc x27, 0x753, x18	// Clear all CSR bits
	csrrw x27, 0x753, x24	// Restore CSR

// Testing CSR 0x754
	csrr x2, 0x754	// Read CSR
	li x25, -1
	csrrw x9, 0x754, x25	// Write all 1s to CSR
	csrrw x9, 0x754, x0	// Write all 0s to CSR
	csrrs x9, 0x754, x25	// Set all CSR bits
	csrrc x9, 0x754, x25	// Clear all CSR bits
	csrrw x9, 0x754, x2	// Restore CSR

// Testing CSR 0x755
	csrr x23, 0x755	// Read CSR
	li x22, -1
	csrrw x3, 0x755, x22	// Write all 1s to CSR
	csrrw x3, 0x755, x0	// Write all 0s to CSR
	csrrs x3, 0x755, x22	// Set all CSR bits
	csrrc x3, 0x755, x22	// Clear all CSR bits
	csrrw x3, 0x755, x23	// Restore CSR

// Testing CSR 0x756
	csrr x12, 0x756	// Read CSR
	li x15, -1
	csrrw x17, 0x756, x15	// Write all 1s to CSR
	csrrw x17, 0x756, x0	// Write all 0s to CSR
	csrrs x17, 0x756, x15	// Set all CSR bits
	csrrc x17, 0x756, x15	// Clear all CSR bits
	csrrw x17, 0x756, x12	// Restore CSR

// Testing CSR 0x757
	csrr x3, 0x757	// Read CSR
	li x23, -1
	csrrw x20, 0x757, x23	// Write all 1s to CSR
	csrrw x20, 0x757, x0	// Write all 0s to CSR
	csrrs x20, 0x757, x23	// Set all CSR bits
	csrrc x20, 0x757, x23	// Clear all CSR bits
	csrrw x20, 0x757, x3	// Restore CSR

// Testing CSR 0x758
	csrr x15, 0x758	// Read CSR
	li x29, -1
	csrrw x8, 0x758, x29	// Write all 1s to CSR
	csrrw x8, 0x758, x0	// Write all 0s to CSR
	csrrs x8, 0x758, x29	// Set all CSR bits
	csrrc x8, 0x758, x29	// Clear all CSR bits
	csrrw x8, 0x758, x15	// Restore CSR

// Testing CSR 0x759
	csrr x11, 0x759	// Read CSR
	li x24, -1
	csrrw x8, 0x759, x24	// Write all 1s to CSR
	csrrw x8, 0x759, x0	// Write all 0s to CSR
	csrrs x8, 0x759, x24	// Set all CSR bits
	csrrc x8, 0x759, x24	// Clear all CSR bits
	csrrw x8, 0x759, x11	// Restore CSR

// Testing CSR 0x75a
	csrr x14, 0x75a	// Read CSR
	li x30, -1
	csrrw x24, 0x75a, x30	// Write all 1s to CSR
	csrrw x24, 0x75a, x0	// Write all 0s to CSR
	csrrs x24, 0x75a, x30	// Set all CSR bits
	csrrc x24, 0x75a, x30	// Clear all CSR bits
	csrrw x24, 0x75a, x14	// Restore CSR

// Testing CSR 0x75b
	csrr x26, 0x75b	// Read CSR
	li x13, -1
	csrrw x28, 0x75b, x13	// Write all 1s to CSR
	csrrw x28, 0x75b, x0	// Write all 0s to CSR
	csrrs x28, 0x75b, x13	// Set all CSR bits
	csrrc x28, 0x75b, x13	// Clear all CSR bits
	csrrw x28, 0x75b, x26	// Restore CSR

// Testing CSR 0x75c
	csrr x9, 0x75c	// Read CSR
	li x4, -1
	csrrw x28, 0x75c, x4	// Write all 1s to CSR
	csrrw x28, 0x75c, x0	// Write all 0s to CSR
	csrrs x28, 0x75c, x4	// Set all CSR bits
	csrrc x28, 0x75c, x4	// Clear all CSR bits
	csrrw x28, 0x75c, x9	// Restore CSR

// Testing CSR 0x75d
	csrr x11, 0x75d	// Read CSR
	li x26, -1
	csrrw x19, 0x75d, x26	// Write all 1s to CSR
	csrrw x19, 0x75d, x0	// Write all 0s to CSR
	csrrs x19, 0x75d, x26	// Set all CSR bits
	csrrc x19, 0x75d, x26	// Clear all CSR bits
	csrrw x19, 0x75d, x11	// Restore CSR

// Testing CSR 0x75e
	csrr x15, 0x75e	// Read CSR
	li x28, -1
	csrrw x12, 0x75e, x28	// Write all 1s to CSR
	csrrw x12, 0x75e, x0	// Write all 0s to CSR
	csrrs x12, 0x75e, x28	// Set all CSR bits
	csrrc x12, 0x75e, x28	// Clear all CSR bits
	csrrw x12, 0x75e, x15	// Restore CSR

// Testing CSR 0x75f
	csrr x21, 0x75f	// Read CSR
	li x15, -1
	csrrw x18, 0x75f, x15	// Write all 1s to CSR
	csrrw x18, 0x75f, x0	// Write all 0s to CSR
	csrrs x18, 0x75f, x15	// Set all CSR bits
	csrrc x18, 0x75f, x15	// Clear all CSR bits
	csrrw x18, 0x75f, x21	// Restore CSR

// Testing CSR 0x760
	csrr x20, 0x760	// Read CSR
	li x21, -1
	csrrw x8, 0x760, x21	// Write all 1s to CSR
	csrrw x8, 0x760, x0	// Write all 0s to CSR
	csrrs x8, 0x760, x21	// Set all CSR bits
	csrrc x8, 0x760, x21	// Clear all CSR bits
	csrrw x8, 0x760, x20	// Restore CSR

// Testing CSR 0x761
	csrr x29, 0x761	// Read CSR
	li x20, -1
	csrrw x1, 0x761, x20	// Write all 1s to CSR
	csrrw x1, 0x761, x0	// Write all 0s to CSR
	csrrs x1, 0x761, x20	// Set all CSR bits
	csrrc x1, 0x761, x20	// Clear all CSR bits
	csrrw x1, 0x761, x29	// Restore CSR

// Testing CSR 0x762
	csrr x14, 0x762	// Read CSR
	li x9, -1
	csrrw x15, 0x762, x9	// Write all 1s to CSR
	csrrw x15, 0x762, x0	// Write all 0s to CSR
	csrrs x15, 0x762, x9	// Set all CSR bits
	csrrc x15, 0x762, x9	// Clear all CSR bits
	csrrw x15, 0x762, x14	// Restore CSR

// Testing CSR 0x763
	csrr x12, 0x763	// Read CSR
	li x13, -1
	csrrw x9, 0x763, x13	// Write all 1s to CSR
	csrrw x9, 0x763, x0	// Write all 0s to CSR
	csrrs x9, 0x763, x13	// Set all CSR bits
	csrrc x9, 0x763, x13	// Clear all CSR bits
	csrrw x9, 0x763, x12	// Restore CSR

// Testing CSR 0x764
	csrr x8, 0x764	// Read CSR
	li x16, -1
	csrrw x1, 0x764, x16	// Write all 1s to CSR
	csrrw x1, 0x764, x0	// Write all 0s to CSR
	csrrs x1, 0x764, x16	// Set all CSR bits
	csrrc x1, 0x764, x16	// Clear all CSR bits
	csrrw x1, 0x764, x8	// Restore CSR

// Testing CSR 0x765
	csrr x9, 0x765	// Read CSR
	li x11, -1
	csrrw x8, 0x765, x11	// Write all 1s to CSR
	csrrw x8, 0x765, x0	// Write all 0s to CSR
	csrrs x8, 0x765, x11	// Set all CSR bits
	csrrc x8, 0x765, x11	// Clear all CSR bits
	csrrw x8, 0x765, x9	// Restore CSR

// Testing CSR 0x766
	csrr x31, 0x766	// Read CSR
	li x6, -1
	csrrw x21, 0x766, x6	// Write all 1s to CSR
	csrrw x21, 0x766, x0	// Write all 0s to CSR
	csrrs x21, 0x766, x6	// Set all CSR bits
	csrrc x21, 0x766, x6	// Clear all CSR bits
	csrrw x21, 0x766, x31	// Restore CSR

// Testing CSR 0x767
	csrr x11, 0x767	// Read CSR
	li x5, -1
	csrrw x31, 0x767, x5	// Write all 1s to CSR
	csrrw x31, 0x767, x0	// Write all 0s to CSR
	csrrs x31, 0x767, x5	// Set all CSR bits
	csrrc x31, 0x767, x5	// Clear all CSR bits
	csrrw x31, 0x767, x11	// Restore CSR

// Testing CSR 0x768
	csrr x21, 0x768	// Read CSR
	li x28, -1
	csrrw x14, 0x768, x28	// Write all 1s to CSR
	csrrw x14, 0x768, x0	// Write all 0s to CSR
	csrrs x14, 0x768, x28	// Set all CSR bits
	csrrc x14, 0x768, x28	// Clear all CSR bits
	csrrw x14, 0x768, x21	// Restore CSR

// Testing CSR 0x769
	csrr x11, 0x769	// Read CSR
	li x7, -1
	csrrw x3, 0x769, x7	// Write all 1s to CSR
	csrrw x3, 0x769, x0	// Write all 0s to CSR
	csrrs x3, 0x769, x7	// Set all CSR bits
	csrrc x3, 0x769, x7	// Clear all CSR bits
	csrrw x3, 0x769, x11	// Restore CSR

// Testing CSR 0x76a
	csrr x15, 0x76a	// Read CSR
	li x16, -1
	csrrw x28, 0x76a, x16	// Write all 1s to CSR
	csrrw x28, 0x76a, x0	// Write all 0s to CSR
	csrrs x28, 0x76a, x16	// Set all CSR bits
	csrrc x28, 0x76a, x16	// Clear all CSR bits
	csrrw x28, 0x76a, x15	// Restore CSR

// Testing CSR 0x76b
	csrr x24, 0x76b	// Read CSR
	li x31, -1
	csrrw x14, 0x76b, x31	// Write all 1s to CSR
	csrrw x14, 0x76b, x0	// Write all 0s to CSR
	csrrs x14, 0x76b, x31	// Set all CSR bits
	csrrc x14, 0x76b, x31	// Clear all CSR bits
	csrrw x14, 0x76b, x24	// Restore CSR

// Testing CSR 0x76c
	csrr x16, 0x76c	// Read CSR
	li x2, -1
	csrrw x23, 0x76c, x2	// Write all 1s to CSR
	csrrw x23, 0x76c, x0	// Write all 0s to CSR
	csrrs x23, 0x76c, x2	// Set all CSR bits
	csrrc x23, 0x76c, x2	// Clear all CSR bits
	csrrw x23, 0x76c, x16	// Restore CSR

// Testing CSR 0x76d
	csrr x9, 0x76d	// Read CSR
	li x25, -1
	csrrw x18, 0x76d, x25	// Write all 1s to CSR
	csrrw x18, 0x76d, x0	// Write all 0s to CSR
	csrrs x18, 0x76d, x25	// Set all CSR bits
	csrrc x18, 0x76d, x25	// Clear all CSR bits
	csrrw x18, 0x76d, x9	// Restore CSR

// Testing CSR 0x76e
	csrr x21, 0x76e	// Read CSR
	li x18, -1
	csrrw x7, 0x76e, x18	// Write all 1s to CSR
	csrrw x7, 0x76e, x0	// Write all 0s to CSR
	csrrs x7, 0x76e, x18	// Set all CSR bits
	csrrc x7, 0x76e, x18	// Clear all CSR bits
	csrrw x7, 0x76e, x21	// Restore CSR

// Testing CSR 0x76f
	csrr x1, 0x76f	// Read CSR
	li x2, -1
	csrrw x10, 0x76f, x2	// Write all 1s to CSR
	csrrw x10, 0x76f, x0	// Write all 0s to CSR
	csrrs x10, 0x76f, x2	// Set all CSR bits
	csrrc x10, 0x76f, x2	// Clear all CSR bits
	csrrw x10, 0x76f, x1	// Restore CSR

// Testing CSR 0x770
	csrr x27, 0x770	// Read CSR
	li x6, -1
	csrrw x10, 0x770, x6	// Write all 1s to CSR
	csrrw x10, 0x770, x0	// Write all 0s to CSR
	csrrs x10, 0x770, x6	// Set all CSR bits
	csrrc x10, 0x770, x6	// Clear all CSR bits
	csrrw x10, 0x770, x27	// Restore CSR

// Testing CSR 0x771
	csrr x24, 0x771	// Read CSR
	li x5, -1
	csrrw x23, 0x771, x5	// Write all 1s to CSR
	csrrw x23, 0x771, x0	// Write all 0s to CSR
	csrrs x23, 0x771, x5	// Set all CSR bits
	csrrc x23, 0x771, x5	// Clear all CSR bits
	csrrw x23, 0x771, x24	// Restore CSR

// Testing CSR 0x772
	csrr x22, 0x772	// Read CSR
	li x29, -1
	csrrw x17, 0x772, x29	// Write all 1s to CSR
	csrrw x17, 0x772, x0	// Write all 0s to CSR
	csrrs x17, 0x772, x29	// Set all CSR bits
	csrrc x17, 0x772, x29	// Clear all CSR bits
	csrrw x17, 0x772, x22	// Restore CSR

// Testing CSR 0x773
	csrr x5, 0x773	// Read CSR
	li x18, -1
	csrrw x16, 0x773, x18	// Write all 1s to CSR
	csrrw x16, 0x773, x0	// Write all 0s to CSR
	csrrs x16, 0x773, x18	// Set all CSR bits
	csrrc x16, 0x773, x18	// Clear all CSR bits
	csrrw x16, 0x773, x5	// Restore CSR

// Testing CSR 0x774
	csrr x17, 0x774	// Read CSR
	li x12, -1
	csrrw x7, 0x774, x12	// Write all 1s to CSR
	csrrw x7, 0x774, x0	// Write all 0s to CSR
	csrrs x7, 0x774, x12	// Set all CSR bits
	csrrc x7, 0x774, x12	// Clear all CSR bits
	csrrw x7, 0x774, x17	// Restore CSR

// Testing CSR 0x775
	csrr x19, 0x775	// Read CSR
	li x17, -1
	csrrw x1, 0x775, x17	// Write all 1s to CSR
	csrrw x1, 0x775, x0	// Write all 0s to CSR
	csrrs x1, 0x775, x17	// Set all CSR bits
	csrrc x1, 0x775, x17	// Clear all CSR bits
	csrrw x1, 0x775, x19	// Restore CSR

// Testing CSR 0x776
	csrr x5, 0x776	// Read CSR
	li x16, -1
	csrrw x27, 0x776, x16	// Write all 1s to CSR
	csrrw x27, 0x776, x0	// Write all 0s to CSR
	csrrs x27, 0x776, x16	// Set all CSR bits
	csrrc x27, 0x776, x16	// Clear all CSR bits
	csrrw x27, 0x776, x5	// Restore CSR

// Testing CSR 0x777
	csrr x1, 0x777	// Read CSR
	li x18, -1
	csrrw x8, 0x777, x18	// Write all 1s to CSR
	csrrw x8, 0x777, x0	// Write all 0s to CSR
	csrrs x8, 0x777, x18	// Set all CSR bits
	csrrc x8, 0x777, x18	// Clear all CSR bits
	csrrw x8, 0x777, x1	// Restore CSR

// Testing CSR 0x778
	csrr x26, 0x778	// Read CSR
	li x27, -1
	csrrw x29, 0x778, x27	// Write all 1s to CSR
	csrrw x29, 0x778, x0	// Write all 0s to CSR
	csrrs x29, 0x778, x27	// Set all CSR bits
	csrrc x29, 0x778, x27	// Clear all CSR bits
	csrrw x29, 0x778, x26	// Restore CSR

// Testing CSR 0x779
	csrr x29, 0x779	// Read CSR
	li x23, -1
	csrrw x2, 0x779, x23	// Write all 1s to CSR
	csrrw x2, 0x779, x0	// Write all 0s to CSR
	csrrs x2, 0x779, x23	// Set all CSR bits
	csrrc x2, 0x779, x23	// Clear all CSR bits
	csrrw x2, 0x779, x29	// Restore CSR

// Testing CSR 0x77a
	csrr x19, 0x77a	// Read CSR
	li x22, -1
	csrrw x30, 0x77a, x22	// Write all 1s to CSR
	csrrw x30, 0x77a, x0	// Write all 0s to CSR
	csrrs x30, 0x77a, x22	// Set all CSR bits
	csrrc x30, 0x77a, x22	// Clear all CSR bits
	csrrw x30, 0x77a, x19	// Restore CSR

// Testing CSR 0x77b
	csrr x14, 0x77b	// Read CSR
	li x22, -1
	csrrw x18, 0x77b, x22	// Write all 1s to CSR
	csrrw x18, 0x77b, x0	// Write all 0s to CSR
	csrrs x18, 0x77b, x22	// Set all CSR bits
	csrrc x18, 0x77b, x22	// Clear all CSR bits
	csrrw x18, 0x77b, x14	// Restore CSR

// Testing CSR 0x77c
	csrr x3, 0x77c	// Read CSR
	li x11, -1
	csrrw x21, 0x77c, x11	// Write all 1s to CSR
	csrrw x21, 0x77c, x0	// Write all 0s to CSR
	csrrs x21, 0x77c, x11	// Set all CSR bits
	csrrc x21, 0x77c, x11	// Clear all CSR bits
	csrrw x21, 0x77c, x3	// Restore CSR

// Testing CSR 0x77d
	csrr x13, 0x77d	// Read CSR
	li x3, -1
	csrrw x11, 0x77d, x3	// Write all 1s to CSR
	csrrw x11, 0x77d, x0	// Write all 0s to CSR
	csrrs x11, 0x77d, x3	// Set all CSR bits
	csrrc x11, 0x77d, x3	// Clear all CSR bits
	csrrw x11, 0x77d, x13	// Restore CSR

// Testing CSR 0x77e
	csrr x5, 0x77e	// Read CSR
	li x19, -1
	csrrw x21, 0x77e, x19	// Write all 1s to CSR
	csrrw x21, 0x77e, x0	// Write all 0s to CSR
	csrrs x21, 0x77e, x19	// Set all CSR bits
	csrrc x21, 0x77e, x19	// Clear all CSR bits
	csrrw x21, 0x77e, x5	// Restore CSR

// Testing CSR 0x77f
	csrr x22, 0x77f	// Read CSR
	li x28, -1
	csrrw x2, 0x77f, x28	// Write all 1s to CSR
	csrrw x2, 0x77f, x0	// Write all 0s to CSR
	csrrs x2, 0x77f, x28	// Set all CSR bits
	csrrc x2, 0x77f, x28	// Clear all CSR bits
	csrrw x2, 0x77f, x22	// Restore CSR

// Testing CSR 0x780
	csrr x14, 0x780	// Read CSR
	li x19, -1
	csrrw x29, 0x780, x19	// Write all 1s to CSR
	csrrw x29, 0x780, x0	// Write all 0s to CSR
	csrrs x29, 0x780, x19	// Set all CSR bits
	csrrc x29, 0x780, x19	// Clear all CSR bits
	csrrw x29, 0x780, x14	// Restore CSR

// Testing CSR 0x781
	csrr x29, 0x781	// Read CSR
	li x21, -1
	csrrw x17, 0x781, x21	// Write all 1s to CSR
	csrrw x17, 0x781, x0	// Write all 0s to CSR
	csrrs x17, 0x781, x21	// Set all CSR bits
	csrrc x17, 0x781, x21	// Clear all CSR bits
	csrrw x17, 0x781, x29	// Restore CSR

// Testing CSR 0x782
	csrr x21, 0x782	// Read CSR
	li x29, -1
	csrrw x7, 0x782, x29	// Write all 1s to CSR
	csrrw x7, 0x782, x0	// Write all 0s to CSR
	csrrs x7, 0x782, x29	// Set all CSR bits
	csrrc x7, 0x782, x29	// Clear all CSR bits
	csrrw x7, 0x782, x21	// Restore CSR

// Testing CSR 0x783
	csrr x25, 0x783	// Read CSR
	li x21, -1
	csrrw x1, 0x783, x21	// Write all 1s to CSR
	csrrw x1, 0x783, x0	// Write all 0s to CSR
	csrrs x1, 0x783, x21	// Set all CSR bits
	csrrc x1, 0x783, x21	// Clear all CSR bits
	csrrw x1, 0x783, x25	// Restore CSR

// Testing CSR 0x784
	csrr x5, 0x784	// Read CSR
	li x18, -1
	csrrw x27, 0x784, x18	// Write all 1s to CSR
	csrrw x27, 0x784, x0	// Write all 0s to CSR
	csrrs x27, 0x784, x18	// Set all CSR bits
	csrrc x27, 0x784, x18	// Clear all CSR bits
	csrrw x27, 0x784, x5	// Restore CSR

// Testing CSR 0x785
	csrr x15, 0x785	// Read CSR
	li x8, -1
	csrrw x27, 0x785, x8	// Write all 1s to CSR
	csrrw x27, 0x785, x0	// Write all 0s to CSR
	csrrs x27, 0x785, x8	// Set all CSR bits
	csrrc x27, 0x785, x8	// Clear all CSR bits
	csrrw x27, 0x785, x15	// Restore CSR

// Testing CSR 0x786
	csrr x9, 0x786	// Read CSR
	li x1, -1
	csrrw x27, 0x786, x1	// Write all 1s to CSR
	csrrw x27, 0x786, x0	// Write all 0s to CSR
	csrrs x27, 0x786, x1	// Set all CSR bits
	csrrc x27, 0x786, x1	// Clear all CSR bits
	csrrw x27, 0x786, x9	// Restore CSR

// Testing CSR 0x787
	csrr x20, 0x787	// Read CSR
	li x22, -1
	csrrw x11, 0x787, x22	// Write all 1s to CSR
	csrrw x11, 0x787, x0	// Write all 0s to CSR
	csrrs x11, 0x787, x22	// Set all CSR bits
	csrrc x11, 0x787, x22	// Clear all CSR bits
	csrrw x11, 0x787, x20	// Restore CSR

// Testing CSR 0x788
	csrr x12, 0x788	// Read CSR
	li x10, -1
	csrrw x17, 0x788, x10	// Write all 1s to CSR
	csrrw x17, 0x788, x0	// Write all 0s to CSR
	csrrs x17, 0x788, x10	// Set all CSR bits
	csrrc x17, 0x788, x10	// Clear all CSR bits
	csrrw x17, 0x788, x12	// Restore CSR

// Testing CSR 0x789
	csrr x20, 0x789	// Read CSR
	li x19, -1
	csrrw x8, 0x789, x19	// Write all 1s to CSR
	csrrw x8, 0x789, x0	// Write all 0s to CSR
	csrrs x8, 0x789, x19	// Set all CSR bits
	csrrc x8, 0x789, x19	// Clear all CSR bits
	csrrw x8, 0x789, x20	// Restore CSR

// Testing CSR 0x78a
	csrr x11, 0x78a	// Read CSR
	li x16, -1
	csrrw x6, 0x78a, x16	// Write all 1s to CSR
	csrrw x6, 0x78a, x0	// Write all 0s to CSR
	csrrs x6, 0x78a, x16	// Set all CSR bits
	csrrc x6, 0x78a, x16	// Clear all CSR bits
	csrrw x6, 0x78a, x11	// Restore CSR

// Testing CSR 0x78b
	csrr x3, 0x78b	// Read CSR
	li x18, -1
	csrrw x2, 0x78b, x18	// Write all 1s to CSR
	csrrw x2, 0x78b, x0	// Write all 0s to CSR
	csrrs x2, 0x78b, x18	// Set all CSR bits
	csrrc x2, 0x78b, x18	// Clear all CSR bits
	csrrw x2, 0x78b, x3	// Restore CSR

// Testing CSR 0x78c
	csrr x22, 0x78c	// Read CSR
	li x25, -1
	csrrw x10, 0x78c, x25	// Write all 1s to CSR
	csrrw x10, 0x78c, x0	// Write all 0s to CSR
	csrrs x10, 0x78c, x25	// Set all CSR bits
	csrrc x10, 0x78c, x25	// Clear all CSR bits
	csrrw x10, 0x78c, x22	// Restore CSR

// Testing CSR 0x78d
	csrr x23, 0x78d	// Read CSR
	li x30, -1
	csrrw x17, 0x78d, x30	// Write all 1s to CSR
	csrrw x17, 0x78d, x0	// Write all 0s to CSR
	csrrs x17, 0x78d, x30	// Set all CSR bits
	csrrc x17, 0x78d, x30	// Clear all CSR bits
	csrrw x17, 0x78d, x23	// Restore CSR

// Testing CSR 0x78e
	csrr x25, 0x78e	// Read CSR
	li x19, -1
	csrrw x7, 0x78e, x19	// Write all 1s to CSR
	csrrw x7, 0x78e, x0	// Write all 0s to CSR
	csrrs x7, 0x78e, x19	// Set all CSR bits
	csrrc x7, 0x78e, x19	// Clear all CSR bits
	csrrw x7, 0x78e, x25	// Restore CSR

// Testing CSR 0x78f
	csrr x15, 0x78f	// Read CSR
	li x18, -1
	csrrw x5, 0x78f, x18	// Write all 1s to CSR
	csrrw x5, 0x78f, x0	// Write all 0s to CSR
	csrrs x5, 0x78f, x18	// Set all CSR bits
	csrrc x5, 0x78f, x18	// Clear all CSR bits
	csrrw x5, 0x78f, x15	// Restore CSR

// Testing CSR 0x790
	csrr x1, 0x790	// Read CSR
	li x17, -1
	csrrw x25, 0x790, x17	// Write all 1s to CSR
	csrrw x25, 0x790, x0	// Write all 0s to CSR
	csrrs x25, 0x790, x17	// Set all CSR bits
	csrrc x25, 0x790, x17	// Clear all CSR bits
	csrrw x25, 0x790, x1	// Restore CSR

// Testing CSR 0x791
	csrr x28, 0x791	// Read CSR
	li x24, -1
	csrrw x30, 0x791, x24	// Write all 1s to CSR
	csrrw x30, 0x791, x0	// Write all 0s to CSR
	csrrs x30, 0x791, x24	// Set all CSR bits
	csrrc x30, 0x791, x24	// Clear all CSR bits
	csrrw x30, 0x791, x28	// Restore CSR

// Testing CSR 0x792
	csrr x10, 0x792	// Read CSR
	li x6, -1
	csrrw x26, 0x792, x6	// Write all 1s to CSR
	csrrw x26, 0x792, x0	// Write all 0s to CSR
	csrrs x26, 0x792, x6	// Set all CSR bits
	csrrc x26, 0x792, x6	// Clear all CSR bits
	csrrw x26, 0x792, x10	// Restore CSR

// Testing CSR 0x793
	csrr x4, 0x793	// Read CSR
	li x14, -1
	csrrw x15, 0x793, x14	// Write all 1s to CSR
	csrrw x15, 0x793, x0	// Write all 0s to CSR
	csrrs x15, 0x793, x14	// Set all CSR bits
	csrrc x15, 0x793, x14	// Clear all CSR bits
	csrrw x15, 0x793, x4	// Restore CSR

// Testing CSR 0x794
	csrr x14, 0x794	// Read CSR
	li x3, -1
	csrrw x28, 0x794, x3	// Write all 1s to CSR
	csrrw x28, 0x794, x0	// Write all 0s to CSR
	csrrs x28, 0x794, x3	// Set all CSR bits
	csrrc x28, 0x794, x3	// Clear all CSR bits
	csrrw x28, 0x794, x14	// Restore CSR

// Testing CSR 0x795
	csrr x4, 0x795	// Read CSR
	li x10, -1
	csrrw x5, 0x795, x10	// Write all 1s to CSR
	csrrw x5, 0x795, x0	// Write all 0s to CSR
	csrrs x5, 0x795, x10	// Set all CSR bits
	csrrc x5, 0x795, x10	// Clear all CSR bits
	csrrw x5, 0x795, x4	// Restore CSR

// Testing CSR 0x796
	csrr x25, 0x796	// Read CSR
	li x3, -1
	csrrw x24, 0x796, x3	// Write all 1s to CSR
	csrrw x24, 0x796, x0	// Write all 0s to CSR
	csrrs x24, 0x796, x3	// Set all CSR bits
	csrrc x24, 0x796, x3	// Clear all CSR bits
	csrrw x24, 0x796, x25	// Restore CSR

// Testing CSR 0x797
	csrr x21, 0x797	// Read CSR
	li x7, -1
	csrrw x20, 0x797, x7	// Write all 1s to CSR
	csrrw x20, 0x797, x0	// Write all 0s to CSR
	csrrs x20, 0x797, x7	// Set all CSR bits
	csrrc x20, 0x797, x7	// Clear all CSR bits
	csrrw x20, 0x797, x21	// Restore CSR

// Testing CSR 0x798
	csrr x10, 0x798	// Read CSR
	li x30, -1
	csrrw x6, 0x798, x30	// Write all 1s to CSR
	csrrw x6, 0x798, x0	// Write all 0s to CSR
	csrrs x6, 0x798, x30	// Set all CSR bits
	csrrc x6, 0x798, x30	// Clear all CSR bits
	csrrw x6, 0x798, x10	// Restore CSR

// Testing CSR 0x799
	csrr x2, 0x799	// Read CSR
	li x20, -1
	csrrw x23, 0x799, x20	// Write all 1s to CSR
	csrrw x23, 0x799, x0	// Write all 0s to CSR
	csrrs x23, 0x799, x20	// Set all CSR bits
	csrrc x23, 0x799, x20	// Clear all CSR bits
	csrrw x23, 0x799, x2	// Restore CSR

// Testing CSR 0x79a
	csrr x9, 0x79a	// Read CSR
	li x5, -1
	csrrw x30, 0x79a, x5	// Write all 1s to CSR
	csrrw x30, 0x79a, x0	// Write all 0s to CSR
	csrrs x30, 0x79a, x5	// Set all CSR bits
	csrrc x30, 0x79a, x5	// Clear all CSR bits
	csrrw x30, 0x79a, x9	// Restore CSR

// Testing CSR 0x79b
	csrr x15, 0x79b	// Read CSR
	li x7, -1
	csrrw x23, 0x79b, x7	// Write all 1s to CSR
	csrrw x23, 0x79b, x0	// Write all 0s to CSR
	csrrs x23, 0x79b, x7	// Set all CSR bits
	csrrc x23, 0x79b, x7	// Clear all CSR bits
	csrrw x23, 0x79b, x15	// Restore CSR

// Testing CSR 0x79c
	csrr x8, 0x79c	// Read CSR
	li x21, -1
	csrrw x25, 0x79c, x21	// Write all 1s to CSR
	csrrw x25, 0x79c, x0	// Write all 0s to CSR
	csrrs x25, 0x79c, x21	// Set all CSR bits
	csrrc x25, 0x79c, x21	// Clear all CSR bits
	csrrw x25, 0x79c, x8	// Restore CSR

// Testing CSR 0x79d
	csrr x23, 0x79d	// Read CSR
	li x20, -1
	csrrw x3, 0x79d, x20	// Write all 1s to CSR
	csrrw x3, 0x79d, x0	// Write all 0s to CSR
	csrrs x3, 0x79d, x20	// Set all CSR bits
	csrrc x3, 0x79d, x20	// Clear all CSR bits
	csrrw x3, 0x79d, x23	// Restore CSR

// Testing CSR 0x79e
	csrr x14, 0x79e	// Read CSR
	li x17, -1
	csrrw x1, 0x79e, x17	// Write all 1s to CSR
	csrrw x1, 0x79e, x0	// Write all 0s to CSR
	csrrs x1, 0x79e, x17	// Set all CSR bits
	csrrc x1, 0x79e, x17	// Clear all CSR bits
	csrrw x1, 0x79e, x14	// Restore CSR

// Testing CSR 0x79f
	csrr x24, 0x79f	// Read CSR
	li x26, -1
	csrrw x11, 0x79f, x26	// Write all 1s to CSR
	csrrw x11, 0x79f, x0	// Write all 0s to CSR
	csrrs x11, 0x79f, x26	// Set all CSR bits
	csrrc x11, 0x79f, x26	// Clear all CSR bits
	csrrw x11, 0x79f, x24	// Restore CSR

// Testing CSR 0x7a0
	csrr x1, 0x7a0	// Read CSR
	li x18, -1
	csrrw x9, 0x7a0, x18	// Write all 1s to CSR
	csrrw x9, 0x7a0, x0	// Write all 0s to CSR
	csrrs x9, 0x7a0, x18	// Set all CSR bits
	csrrc x9, 0x7a0, x18	// Clear all CSR bits
	csrrw x9, 0x7a0, x1	// Restore CSR

// Testing CSR 0x7a1
	csrr x11, 0x7a1	// Read CSR
	li x7, -1
	csrrw x23, 0x7a1, x7	// Write all 1s to CSR
	csrrw x23, 0x7a1, x0	// Write all 0s to CSR
	csrrs x23, 0x7a1, x7	// Set all CSR bits
	csrrc x23, 0x7a1, x7	// Clear all CSR bits
	csrrw x23, 0x7a1, x11	// Restore CSR

// Testing CSR 0x7a2
	csrr x29, 0x7a2	// Read CSR
	li x30, -1
	csrrw x16, 0x7a2, x30	// Write all 1s to CSR
	csrrw x16, 0x7a2, x0	// Write all 0s to CSR
	csrrs x16, 0x7a2, x30	// Set all CSR bits
	csrrc x16, 0x7a2, x30	// Clear all CSR bits
	csrrw x16, 0x7a2, x29	// Restore CSR

// Testing CSR 0x7a3
	csrr x10, 0x7a3	// Read CSR
	li x23, -1
	csrrw x29, 0x7a3, x23	// Write all 1s to CSR
	csrrw x29, 0x7a3, x0	// Write all 0s to CSR
	csrrs x29, 0x7a3, x23	// Set all CSR bits
	csrrc x29, 0x7a3, x23	// Clear all CSR bits
	csrrw x29, 0x7a3, x10	// Restore CSR

// Testing CSR 0x7a4
	csrr x27, 0x7a4	// Read CSR
	li x30, -1
	csrrw x13, 0x7a4, x30	// Write all 1s to CSR
	csrrw x13, 0x7a4, x0	// Write all 0s to CSR
	csrrs x13, 0x7a4, x30	// Set all CSR bits
	csrrc x13, 0x7a4, x30	// Clear all CSR bits
	csrrw x13, 0x7a4, x27	// Restore CSR

// Testing CSR 0x7a5
	csrr x13, 0x7a5	// Read CSR
	li x21, -1
	csrrw x15, 0x7a5, x21	// Write all 1s to CSR
	csrrw x15, 0x7a5, x0	// Write all 0s to CSR
	csrrs x15, 0x7a5, x21	// Set all CSR bits
	csrrc x15, 0x7a5, x21	// Clear all CSR bits
	csrrw x15, 0x7a5, x13	// Restore CSR

// Testing CSR 0x7a6
	csrr x3, 0x7a6	// Read CSR
	li x22, -1
	csrrw x2, 0x7a6, x22	// Write all 1s to CSR
	csrrw x2, 0x7a6, x0	// Write all 0s to CSR
	csrrs x2, 0x7a6, x22	// Set all CSR bits
	csrrc x2, 0x7a6, x22	// Clear all CSR bits
	csrrw x2, 0x7a6, x3	// Restore CSR

// Testing CSR 0x7a7
	csrr x24, 0x7a7	// Read CSR
	li x20, -1
	csrrw x9, 0x7a7, x20	// Write all 1s to CSR
	csrrw x9, 0x7a7, x0	// Write all 0s to CSR
	csrrs x9, 0x7a7, x20	// Set all CSR bits
	csrrc x9, 0x7a7, x20	// Clear all CSR bits
	csrrw x9, 0x7a7, x24	// Restore CSR

// Testing CSR 0x7a8
	csrr x23, 0x7a8	// Read CSR
	li x31, -1
	csrrw x24, 0x7a8, x31	// Write all 1s to CSR
	csrrw x24, 0x7a8, x0	// Write all 0s to CSR
	csrrs x24, 0x7a8, x31	// Set all CSR bits
	csrrc x24, 0x7a8, x31	// Clear all CSR bits
	csrrw x24, 0x7a8, x23	// Restore CSR

// Testing CSR 0x7a9
	csrr x31, 0x7a9	// Read CSR
	li x1, -1
	csrrw x19, 0x7a9, x1	// Write all 1s to CSR
	csrrw x19, 0x7a9, x0	// Write all 0s to CSR
	csrrs x19, 0x7a9, x1	// Set all CSR bits
	csrrc x19, 0x7a9, x1	// Clear all CSR bits
	csrrw x19, 0x7a9, x31	// Restore CSR

// Testing CSR 0x7aa
	csrr x29, 0x7aa	// Read CSR
	li x15, -1
	csrrw x8, 0x7aa, x15	// Write all 1s to CSR
	csrrw x8, 0x7aa, x0	// Write all 0s to CSR
	csrrs x8, 0x7aa, x15	// Set all CSR bits
	csrrc x8, 0x7aa, x15	// Clear all CSR bits
	csrrw x8, 0x7aa, x29	// Restore CSR

// Testing CSR 0x7ab
	csrr x23, 0x7ab	// Read CSR
	li x22, -1
	csrrw x20, 0x7ab, x22	// Write all 1s to CSR
	csrrw x20, 0x7ab, x0	// Write all 0s to CSR
	csrrs x20, 0x7ab, x22	// Set all CSR bits
	csrrc x20, 0x7ab, x22	// Clear all CSR bits
	csrrw x20, 0x7ab, x23	// Restore CSR

// Testing CSR 0x7ac
	csrr x15, 0x7ac	// Read CSR
	li x28, -1
	csrrw x22, 0x7ac, x28	// Write all 1s to CSR
	csrrw x22, 0x7ac, x0	// Write all 0s to CSR
	csrrs x22, 0x7ac, x28	// Set all CSR bits
	csrrc x22, 0x7ac, x28	// Clear all CSR bits
	csrrw x22, 0x7ac, x15	// Restore CSR

// Testing CSR 0x7ad
	csrr x30, 0x7ad	// Read CSR
	li x8, -1
	csrrw x22, 0x7ad, x8	// Write all 1s to CSR
	csrrw x22, 0x7ad, x0	// Write all 0s to CSR
	csrrs x22, 0x7ad, x8	// Set all CSR bits
	csrrc x22, 0x7ad, x8	// Clear all CSR bits
	csrrw x22, 0x7ad, x30	// Restore CSR

// Testing CSR 0x7ae
	csrr x12, 0x7ae	// Read CSR
	li x10, -1
	csrrw x16, 0x7ae, x10	// Write all 1s to CSR
	csrrw x16, 0x7ae, x0	// Write all 0s to CSR
	csrrs x16, 0x7ae, x10	// Set all CSR bits
	csrrc x16, 0x7ae, x10	// Clear all CSR bits
	csrrw x16, 0x7ae, x12	// Restore CSR

// Testing CSR 0x7af
	csrr x29, 0x7af	// Read CSR
	li x24, -1
	csrrw x8, 0x7af, x24	// Write all 1s to CSR
	csrrw x8, 0x7af, x0	// Write all 0s to CSR
	csrrs x8, 0x7af, x24	// Set all CSR bits
	csrrc x8, 0x7af, x24	// Clear all CSR bits
	csrrw x8, 0x7af, x29	// Restore CSR

// Testing CSR 0x7b0
	csrr x24, 0x7b0	// Read CSR
	li x20, -1
	csrrw x3, 0x7b0, x20	// Write all 1s to CSR
	csrrw x3, 0x7b0, x0	// Write all 0s to CSR
	csrrs x3, 0x7b0, x20	// Set all CSR bits
	csrrc x3, 0x7b0, x20	// Clear all CSR bits
	csrrw x3, 0x7b0, x24	// Restore CSR

// Testing CSR 0x7b1
	csrr x2, 0x7b1	// Read CSR
	li x7, -1
	csrrw x11, 0x7b1, x7	// Write all 1s to CSR
	csrrw x11, 0x7b1, x0	// Write all 0s to CSR
	csrrs x11, 0x7b1, x7	// Set all CSR bits
	csrrc x11, 0x7b1, x7	// Clear all CSR bits
	csrrw x11, 0x7b1, x2	// Restore CSR

// Testing CSR 0x7b2
	csrr x2, 0x7b2	// Read CSR
	li x7, -1
	csrrw x25, 0x7b2, x7	// Write all 1s to CSR
	csrrw x25, 0x7b2, x0	// Write all 0s to CSR
	csrrs x25, 0x7b2, x7	// Set all CSR bits
	csrrc x25, 0x7b2, x7	// Clear all CSR bits
	csrrw x25, 0x7b2, x2	// Restore CSR

// Testing CSR 0x7b3
	csrr x14, 0x7b3	// Read CSR
	li x16, -1
	csrrw x2, 0x7b3, x16	// Write all 1s to CSR
	csrrw x2, 0x7b3, x0	// Write all 0s to CSR
	csrrs x2, 0x7b3, x16	// Set all CSR bits
	csrrc x2, 0x7b3, x16	// Clear all CSR bits
	csrrw x2, 0x7b3, x14	// Restore CSR

// Testing CSR 0x7b4
	csrr x3, 0x7b4	// Read CSR
	li x23, -1
	csrrw x1, 0x7b4, x23	// Write all 1s to CSR
	csrrw x1, 0x7b4, x0	// Write all 0s to CSR
	csrrs x1, 0x7b4, x23	// Set all CSR bits
	csrrc x1, 0x7b4, x23	// Clear all CSR bits
	csrrw x1, 0x7b4, x3	// Restore CSR

// Testing CSR 0x7b5
	csrr x10, 0x7b5	// Read CSR
	li x5, -1
	csrrw x12, 0x7b5, x5	// Write all 1s to CSR
	csrrw x12, 0x7b5, x0	// Write all 0s to CSR
	csrrs x12, 0x7b5, x5	// Set all CSR bits
	csrrc x12, 0x7b5, x5	// Clear all CSR bits
	csrrw x12, 0x7b5, x10	// Restore CSR

// Testing CSR 0x7b6
	csrr x23, 0x7b6	// Read CSR
	li x22, -1
	csrrw x26, 0x7b6, x22	// Write all 1s to CSR
	csrrw x26, 0x7b6, x0	// Write all 0s to CSR
	csrrs x26, 0x7b6, x22	// Set all CSR bits
	csrrc x26, 0x7b6, x22	// Clear all CSR bits
	csrrw x26, 0x7b6, x23	// Restore CSR

// Testing CSR 0x7b7
	csrr x20, 0x7b7	// Read CSR
	li x21, -1
	csrrw x26, 0x7b7, x21	// Write all 1s to CSR
	csrrw x26, 0x7b7, x0	// Write all 0s to CSR
	csrrs x26, 0x7b7, x21	// Set all CSR bits
	csrrc x26, 0x7b7, x21	// Clear all CSR bits
	csrrw x26, 0x7b7, x20	// Restore CSR

// Testing CSR 0x7b8
	csrr x31, 0x7b8	// Read CSR
	li x3, -1
	csrrw x25, 0x7b8, x3	// Write all 1s to CSR
	csrrw x25, 0x7b8, x0	// Write all 0s to CSR
	csrrs x25, 0x7b8, x3	// Set all CSR bits
	csrrc x25, 0x7b8, x3	// Clear all CSR bits
	csrrw x25, 0x7b8, x31	// Restore CSR

// Testing CSR 0x7b9
	csrr x4, 0x7b9	// Read CSR
	li x11, -1
	csrrw x21, 0x7b9, x11	// Write all 1s to CSR
	csrrw x21, 0x7b9, x0	// Write all 0s to CSR
	csrrs x21, 0x7b9, x11	// Set all CSR bits
	csrrc x21, 0x7b9, x11	// Clear all CSR bits
	csrrw x21, 0x7b9, x4	// Restore CSR

// Testing CSR 0x7ba
	csrr x24, 0x7ba	// Read CSR
	li x19, -1
	csrrw x12, 0x7ba, x19	// Write all 1s to CSR
	csrrw x12, 0x7ba, x0	// Write all 0s to CSR
	csrrs x12, 0x7ba, x19	// Set all CSR bits
	csrrc x12, 0x7ba, x19	// Clear all CSR bits
	csrrw x12, 0x7ba, x24	// Restore CSR

// Testing CSR 0x7bb
	csrr x22, 0x7bb	// Read CSR
	li x24, -1
	csrrw x3, 0x7bb, x24	// Write all 1s to CSR
	csrrw x3, 0x7bb, x0	// Write all 0s to CSR
	csrrs x3, 0x7bb, x24	// Set all CSR bits
	csrrc x3, 0x7bb, x24	// Clear all CSR bits
	csrrw x3, 0x7bb, x22	// Restore CSR

// Testing CSR 0x7bc
	csrr x3, 0x7bc	// Read CSR
	li x18, -1
	csrrw x28, 0x7bc, x18	// Write all 1s to CSR
	csrrw x28, 0x7bc, x0	// Write all 0s to CSR
	csrrs x28, 0x7bc, x18	// Set all CSR bits
	csrrc x28, 0x7bc, x18	// Clear all CSR bits
	csrrw x28, 0x7bc, x3	// Restore CSR

// Testing CSR 0x7bd
	csrr x21, 0x7bd	// Read CSR
	li x7, -1
	csrrw x22, 0x7bd, x7	// Write all 1s to CSR
	csrrw x22, 0x7bd, x0	// Write all 0s to CSR
	csrrs x22, 0x7bd, x7	// Set all CSR bits
	csrrc x22, 0x7bd, x7	// Clear all CSR bits
	csrrw x22, 0x7bd, x21	// Restore CSR

// Testing CSR 0x7be
	csrr x28, 0x7be	// Read CSR
	li x8, -1
	csrrw x26, 0x7be, x8	// Write all 1s to CSR
	csrrw x26, 0x7be, x0	// Write all 0s to CSR
	csrrs x26, 0x7be, x8	// Set all CSR bits
	csrrc x26, 0x7be, x8	// Clear all CSR bits
	csrrw x26, 0x7be, x28	// Restore CSR

// Testing CSR 0x7bf
	csrr x14, 0x7bf	// Read CSR
	li x5, -1
	csrrw x2, 0x7bf, x5	// Write all 1s to CSR
	csrrw x2, 0x7bf, x0	// Write all 0s to CSR
	csrrs x2, 0x7bf, x5	// Set all CSR bits
	csrrc x2, 0x7bf, x5	// Clear all CSR bits
	csrrw x2, 0x7bf, x14	// Restore CSR

// Testing CSR 0x7c0
	csrr x10, 0x7c0	// Read CSR
	li x1, -1
	csrrw x29, 0x7c0, x1	// Write all 1s to CSR
	csrrw x29, 0x7c0, x0	// Write all 0s to CSR
	csrrs x29, 0x7c0, x1	// Set all CSR bits
	csrrc x29, 0x7c0, x1	// Clear all CSR bits
	csrrw x29, 0x7c0, x10	// Restore CSR

// Testing CSR 0x7c1
	csrr x15, 0x7c1	// Read CSR
	li x3, -1
	csrrw x14, 0x7c1, x3	// Write all 1s to CSR
	csrrw x14, 0x7c1, x0	// Write all 0s to CSR
	csrrs x14, 0x7c1, x3	// Set all CSR bits
	csrrc x14, 0x7c1, x3	// Clear all CSR bits
	csrrw x14, 0x7c1, x15	// Restore CSR

// Testing CSR 0x7c2
	csrr x29, 0x7c2	// Read CSR
	li x23, -1
	csrrw x19, 0x7c2, x23	// Write all 1s to CSR
	csrrw x19, 0x7c2, x0	// Write all 0s to CSR
	csrrs x19, 0x7c2, x23	// Set all CSR bits
	csrrc x19, 0x7c2, x23	// Clear all CSR bits
	csrrw x19, 0x7c2, x29	// Restore CSR

// Testing CSR 0x7c3
	csrr x17, 0x7c3	// Read CSR
	li x1, -1
	csrrw x14, 0x7c3, x1	// Write all 1s to CSR
	csrrw x14, 0x7c3, x0	// Write all 0s to CSR
	csrrs x14, 0x7c3, x1	// Set all CSR bits
	csrrc x14, 0x7c3, x1	// Clear all CSR bits
	csrrw x14, 0x7c3, x17	// Restore CSR

// Testing CSR 0x7c4
	csrr x28, 0x7c4	// Read CSR
	li x30, -1
	csrrw x21, 0x7c4, x30	// Write all 1s to CSR
	csrrw x21, 0x7c4, x0	// Write all 0s to CSR
	csrrs x21, 0x7c4, x30	// Set all CSR bits
	csrrc x21, 0x7c4, x30	// Clear all CSR bits
	csrrw x21, 0x7c4, x28	// Restore CSR

// Testing CSR 0x7c5
	csrr x6, 0x7c5	// Read CSR
	li x8, -1
	csrrw x29, 0x7c5, x8	// Write all 1s to CSR
	csrrw x29, 0x7c5, x0	// Write all 0s to CSR
	csrrs x29, 0x7c5, x8	// Set all CSR bits
	csrrc x29, 0x7c5, x8	// Clear all CSR bits
	csrrw x29, 0x7c5, x6	// Restore CSR

// Testing CSR 0x7c6
	csrr x11, 0x7c6	// Read CSR
	li x2, -1
	csrrw x12, 0x7c6, x2	// Write all 1s to CSR
	csrrw x12, 0x7c6, x0	// Write all 0s to CSR
	csrrs x12, 0x7c6, x2	// Set all CSR bits
	csrrc x12, 0x7c6, x2	// Clear all CSR bits
	csrrw x12, 0x7c6, x11	// Restore CSR

// Testing CSR 0x7c7
	csrr x4, 0x7c7	// Read CSR
	li x18, -1
	csrrw x7, 0x7c7, x18	// Write all 1s to CSR
	csrrw x7, 0x7c7, x0	// Write all 0s to CSR
	csrrs x7, 0x7c7, x18	// Set all CSR bits
	csrrc x7, 0x7c7, x18	// Clear all CSR bits
	csrrw x7, 0x7c7, x4	// Restore CSR

// Testing CSR 0x7c8
	csrr x11, 0x7c8	// Read CSR
	li x16, -1
	csrrw x1, 0x7c8, x16	// Write all 1s to CSR
	csrrw x1, 0x7c8, x0	// Write all 0s to CSR
	csrrs x1, 0x7c8, x16	// Set all CSR bits
	csrrc x1, 0x7c8, x16	// Clear all CSR bits
	csrrw x1, 0x7c8, x11	// Restore CSR

// Testing CSR 0x7c9
	csrr x5, 0x7c9	// Read CSR
	li x7, -1
	csrrw x10, 0x7c9, x7	// Write all 1s to CSR
	csrrw x10, 0x7c9, x0	// Write all 0s to CSR
	csrrs x10, 0x7c9, x7	// Set all CSR bits
	csrrc x10, 0x7c9, x7	// Clear all CSR bits
	csrrw x10, 0x7c9, x5	// Restore CSR

// Testing CSR 0x7ca
	csrr x18, 0x7ca	// Read CSR
	li x17, -1
	csrrw x12, 0x7ca, x17	// Write all 1s to CSR
	csrrw x12, 0x7ca, x0	// Write all 0s to CSR
	csrrs x12, 0x7ca, x17	// Set all CSR bits
	csrrc x12, 0x7ca, x17	// Clear all CSR bits
	csrrw x12, 0x7ca, x18	// Restore CSR

// Testing CSR 0x7cb
	csrr x29, 0x7cb	// Read CSR
	li x22, -1
	csrrw x9, 0x7cb, x22	// Write all 1s to CSR
	csrrw x9, 0x7cb, x0	// Write all 0s to CSR
	csrrs x9, 0x7cb, x22	// Set all CSR bits
	csrrc x9, 0x7cb, x22	// Clear all CSR bits
	csrrw x9, 0x7cb, x29	// Restore CSR

// Testing CSR 0x7cc
	csrr x29, 0x7cc	// Read CSR
	li x4, -1
	csrrw x12, 0x7cc, x4	// Write all 1s to CSR
	csrrw x12, 0x7cc, x0	// Write all 0s to CSR
	csrrs x12, 0x7cc, x4	// Set all CSR bits
	csrrc x12, 0x7cc, x4	// Clear all CSR bits
	csrrw x12, 0x7cc, x29	// Restore CSR

// Testing CSR 0x7cd
	csrr x1, 0x7cd	// Read CSR
	li x24, -1
	csrrw x15, 0x7cd, x24	// Write all 1s to CSR
	csrrw x15, 0x7cd, x0	// Write all 0s to CSR
	csrrs x15, 0x7cd, x24	// Set all CSR bits
	csrrc x15, 0x7cd, x24	// Clear all CSR bits
	csrrw x15, 0x7cd, x1	// Restore CSR

// Testing CSR 0x7ce
	csrr x18, 0x7ce	// Read CSR
	li x25, -1
	csrrw x30, 0x7ce, x25	// Write all 1s to CSR
	csrrw x30, 0x7ce, x0	// Write all 0s to CSR
	csrrs x30, 0x7ce, x25	// Set all CSR bits
	csrrc x30, 0x7ce, x25	// Clear all CSR bits
	csrrw x30, 0x7ce, x18	// Restore CSR

// Testing CSR 0x7cf
	csrr x9, 0x7cf	// Read CSR
	li x21, -1
	csrrw x3, 0x7cf, x21	// Write all 1s to CSR
	csrrw x3, 0x7cf, x0	// Write all 0s to CSR
	csrrs x3, 0x7cf, x21	// Set all CSR bits
	csrrc x3, 0x7cf, x21	// Clear all CSR bits
	csrrw x3, 0x7cf, x9	// Restore CSR

// Testing CSR 0x7d0
	csrr x25, 0x7d0	// Read CSR
	li x10, -1
	csrrw x23, 0x7d0, x10	// Write all 1s to CSR
	csrrw x23, 0x7d0, x0	// Write all 0s to CSR
	csrrs x23, 0x7d0, x10	// Set all CSR bits
	csrrc x23, 0x7d0, x10	// Clear all CSR bits
	csrrw x23, 0x7d0, x25	// Restore CSR

// Testing CSR 0x7d1
	csrr x4, 0x7d1	// Read CSR
	li x12, -1
	csrrw x30, 0x7d1, x12	// Write all 1s to CSR
	csrrw x30, 0x7d1, x0	// Write all 0s to CSR
	csrrs x30, 0x7d1, x12	// Set all CSR bits
	csrrc x30, 0x7d1, x12	// Clear all CSR bits
	csrrw x30, 0x7d1, x4	// Restore CSR

// Testing CSR 0x7d2
	csrr x15, 0x7d2	// Read CSR
	li x1, -1
	csrrw x28, 0x7d2, x1	// Write all 1s to CSR
	csrrw x28, 0x7d2, x0	// Write all 0s to CSR
	csrrs x28, 0x7d2, x1	// Set all CSR bits
	csrrc x28, 0x7d2, x1	// Clear all CSR bits
	csrrw x28, 0x7d2, x15	// Restore CSR

// Testing CSR 0x7d3
	csrr x3, 0x7d3	// Read CSR
	li x14, -1
	csrrw x28, 0x7d3, x14	// Write all 1s to CSR
	csrrw x28, 0x7d3, x0	// Write all 0s to CSR
	csrrs x28, 0x7d3, x14	// Set all CSR bits
	csrrc x28, 0x7d3, x14	// Clear all CSR bits
	csrrw x28, 0x7d3, x3	// Restore CSR

// Testing CSR 0x7d4
	csrr x24, 0x7d4	// Read CSR
	li x6, -1
	csrrw x1, 0x7d4, x6	// Write all 1s to CSR
	csrrw x1, 0x7d4, x0	// Write all 0s to CSR
	csrrs x1, 0x7d4, x6	// Set all CSR bits
	csrrc x1, 0x7d4, x6	// Clear all CSR bits
	csrrw x1, 0x7d4, x24	// Restore CSR

// Testing CSR 0x7d5
	csrr x23, 0x7d5	// Read CSR
	li x22, -1
	csrrw x11, 0x7d5, x22	// Write all 1s to CSR
	csrrw x11, 0x7d5, x0	// Write all 0s to CSR
	csrrs x11, 0x7d5, x22	// Set all CSR bits
	csrrc x11, 0x7d5, x22	// Clear all CSR bits
	csrrw x11, 0x7d5, x23	// Restore CSR

// Testing CSR 0x7d6
	csrr x15, 0x7d6	// Read CSR
	li x16, -1
	csrrw x18, 0x7d6, x16	// Write all 1s to CSR
	csrrw x18, 0x7d6, x0	// Write all 0s to CSR
	csrrs x18, 0x7d6, x16	// Set all CSR bits
	csrrc x18, 0x7d6, x16	// Clear all CSR bits
	csrrw x18, 0x7d6, x15	// Restore CSR

// Testing CSR 0x7d7
	csrr x22, 0x7d7	// Read CSR
	li x9, -1
	csrrw x28, 0x7d7, x9	// Write all 1s to CSR
	csrrw x28, 0x7d7, x0	// Write all 0s to CSR
	csrrs x28, 0x7d7, x9	// Set all CSR bits
	csrrc x28, 0x7d7, x9	// Clear all CSR bits
	csrrw x28, 0x7d7, x22	// Restore CSR

// Testing CSR 0x7d8
	csrr x6, 0x7d8	// Read CSR
	li x19, -1
	csrrw x21, 0x7d8, x19	// Write all 1s to CSR
	csrrw x21, 0x7d8, x0	// Write all 0s to CSR
	csrrs x21, 0x7d8, x19	// Set all CSR bits
	csrrc x21, 0x7d8, x19	// Clear all CSR bits
	csrrw x21, 0x7d8, x6	// Restore CSR

// Testing CSR 0x7d9
	csrr x23, 0x7d9	// Read CSR
	li x28, -1
	csrrw x25, 0x7d9, x28	// Write all 1s to CSR
	csrrw x25, 0x7d9, x0	// Write all 0s to CSR
	csrrs x25, 0x7d9, x28	// Set all CSR bits
	csrrc x25, 0x7d9, x28	// Clear all CSR bits
	csrrw x25, 0x7d9, x23	// Restore CSR

// Testing CSR 0x7da
	csrr x2, 0x7da	// Read CSR
	li x5, -1
	csrrw x12, 0x7da, x5	// Write all 1s to CSR
	csrrw x12, 0x7da, x0	// Write all 0s to CSR
	csrrs x12, 0x7da, x5	// Set all CSR bits
	csrrc x12, 0x7da, x5	// Clear all CSR bits
	csrrw x12, 0x7da, x2	// Restore CSR

// Testing CSR 0x7db
	csrr x31, 0x7db	// Read CSR
	li x3, -1
	csrrw x12, 0x7db, x3	// Write all 1s to CSR
	csrrw x12, 0x7db, x0	// Write all 0s to CSR
	csrrs x12, 0x7db, x3	// Set all CSR bits
	csrrc x12, 0x7db, x3	// Clear all CSR bits
	csrrw x12, 0x7db, x31	// Restore CSR

// Testing CSR 0x7dc
	csrr x7, 0x7dc	// Read CSR
	li x25, -1
	csrrw x17, 0x7dc, x25	// Write all 1s to CSR
	csrrw x17, 0x7dc, x0	// Write all 0s to CSR
	csrrs x17, 0x7dc, x25	// Set all CSR bits
	csrrc x17, 0x7dc, x25	// Clear all CSR bits
	csrrw x17, 0x7dc, x7	// Restore CSR

// Testing CSR 0x7dd
	csrr x10, 0x7dd	// Read CSR
	li x6, -1
	csrrw x21, 0x7dd, x6	// Write all 1s to CSR
	csrrw x21, 0x7dd, x0	// Write all 0s to CSR
	csrrs x21, 0x7dd, x6	// Set all CSR bits
	csrrc x21, 0x7dd, x6	// Clear all CSR bits
	csrrw x21, 0x7dd, x10	// Restore CSR

// Testing CSR 0x7de
	csrr x22, 0x7de	// Read CSR
	li x4, -1
	csrrw x26, 0x7de, x4	// Write all 1s to CSR
	csrrw x26, 0x7de, x0	// Write all 0s to CSR
	csrrs x26, 0x7de, x4	// Set all CSR bits
	csrrc x26, 0x7de, x4	// Clear all CSR bits
	csrrw x26, 0x7de, x22	// Restore CSR

// Testing CSR 0x7df
	csrr x24, 0x7df	// Read CSR
	li x28, -1
	csrrw x27, 0x7df, x28	// Write all 1s to CSR
	csrrw x27, 0x7df, x0	// Write all 0s to CSR
	csrrs x27, 0x7df, x28	// Set all CSR bits
	csrrc x27, 0x7df, x28	// Clear all CSR bits
	csrrw x27, 0x7df, x24	// Restore CSR

// Testing CSR 0x7e0
	csrr x11, 0x7e0	// Read CSR
	li x6, -1
	csrrw x19, 0x7e0, x6	// Write all 1s to CSR
	csrrw x19, 0x7e0, x0	// Write all 0s to CSR
	csrrs x19, 0x7e0, x6	// Set all CSR bits
	csrrc x19, 0x7e0, x6	// Clear all CSR bits
	csrrw x19, 0x7e0, x11	// Restore CSR

// Testing CSR 0x7e1
	csrr x5, 0x7e1	// Read CSR
	li x28, -1
	csrrw x16, 0x7e1, x28	// Write all 1s to CSR
	csrrw x16, 0x7e1, x0	// Write all 0s to CSR
	csrrs x16, 0x7e1, x28	// Set all CSR bits
	csrrc x16, 0x7e1, x28	// Clear all CSR bits
	csrrw x16, 0x7e1, x5	// Restore CSR

// Testing CSR 0x7e2
	csrr x20, 0x7e2	// Read CSR
	li x19, -1
	csrrw x21, 0x7e2, x19	// Write all 1s to CSR
	csrrw x21, 0x7e2, x0	// Write all 0s to CSR
	csrrs x21, 0x7e2, x19	// Set all CSR bits
	csrrc x21, 0x7e2, x19	// Clear all CSR bits
	csrrw x21, 0x7e2, x20	// Restore CSR

// Testing CSR 0x7e3
	csrr x24, 0x7e3	// Read CSR
	li x5, -1
	csrrw x9, 0x7e3, x5	// Write all 1s to CSR
	csrrw x9, 0x7e3, x0	// Write all 0s to CSR
	csrrs x9, 0x7e3, x5	// Set all CSR bits
	csrrc x9, 0x7e3, x5	// Clear all CSR bits
	csrrw x9, 0x7e3, x24	// Restore CSR

// Testing CSR 0x7e4
	csrr x15, 0x7e4	// Read CSR
	li x10, -1
	csrrw x27, 0x7e4, x10	// Write all 1s to CSR
	csrrw x27, 0x7e4, x0	// Write all 0s to CSR
	csrrs x27, 0x7e4, x10	// Set all CSR bits
	csrrc x27, 0x7e4, x10	// Clear all CSR bits
	csrrw x27, 0x7e4, x15	// Restore CSR

// Testing CSR 0x7e5
	csrr x20, 0x7e5	// Read CSR
	li x15, -1
	csrrw x27, 0x7e5, x15	// Write all 1s to CSR
	csrrw x27, 0x7e5, x0	// Write all 0s to CSR
	csrrs x27, 0x7e5, x15	// Set all CSR bits
	csrrc x27, 0x7e5, x15	// Clear all CSR bits
	csrrw x27, 0x7e5, x20	// Restore CSR

// Testing CSR 0x7e6
	csrr x31, 0x7e6	// Read CSR
	li x14, -1
	csrrw x23, 0x7e6, x14	// Write all 1s to CSR
	csrrw x23, 0x7e6, x0	// Write all 0s to CSR
	csrrs x23, 0x7e6, x14	// Set all CSR bits
	csrrc x23, 0x7e6, x14	// Clear all CSR bits
	csrrw x23, 0x7e6, x31	// Restore CSR

// Testing CSR 0x7e7
	csrr x6, 0x7e7	// Read CSR
	li x14, -1
	csrrw x25, 0x7e7, x14	// Write all 1s to CSR
	csrrw x25, 0x7e7, x0	// Write all 0s to CSR
	csrrs x25, 0x7e7, x14	// Set all CSR bits
	csrrc x25, 0x7e7, x14	// Clear all CSR bits
	csrrw x25, 0x7e7, x6	// Restore CSR

// Testing CSR 0x7e8
	csrr x29, 0x7e8	// Read CSR
	li x20, -1
	csrrw x25, 0x7e8, x20	// Write all 1s to CSR
	csrrw x25, 0x7e8, x0	// Write all 0s to CSR
	csrrs x25, 0x7e8, x20	// Set all CSR bits
	csrrc x25, 0x7e8, x20	// Clear all CSR bits
	csrrw x25, 0x7e8, x29	// Restore CSR

// Testing CSR 0x7e9
	csrr x26, 0x7e9	// Read CSR
	li x13, -1
	csrrw x30, 0x7e9, x13	// Write all 1s to CSR
	csrrw x30, 0x7e9, x0	// Write all 0s to CSR
	csrrs x30, 0x7e9, x13	// Set all CSR bits
	csrrc x30, 0x7e9, x13	// Clear all CSR bits
	csrrw x30, 0x7e9, x26	// Restore CSR

// Testing CSR 0x7ea
	csrr x7, 0x7ea	// Read CSR
	li x3, -1
	csrrw x26, 0x7ea, x3	// Write all 1s to CSR
	csrrw x26, 0x7ea, x0	// Write all 0s to CSR
	csrrs x26, 0x7ea, x3	// Set all CSR bits
	csrrc x26, 0x7ea, x3	// Clear all CSR bits
	csrrw x26, 0x7ea, x7	// Restore CSR

// Testing CSR 0x7eb
	csrr x18, 0x7eb	// Read CSR
	li x20, -1
	csrrw x6, 0x7eb, x20	// Write all 1s to CSR
	csrrw x6, 0x7eb, x0	// Write all 0s to CSR
	csrrs x6, 0x7eb, x20	// Set all CSR bits
	csrrc x6, 0x7eb, x20	// Clear all CSR bits
	csrrw x6, 0x7eb, x18	// Restore CSR

// Testing CSR 0x7ec
	csrr x27, 0x7ec	// Read CSR
	li x22, -1
	csrrw x1, 0x7ec, x22	// Write all 1s to CSR
	csrrw x1, 0x7ec, x0	// Write all 0s to CSR
	csrrs x1, 0x7ec, x22	// Set all CSR bits
	csrrc x1, 0x7ec, x22	// Clear all CSR bits
	csrrw x1, 0x7ec, x27	// Restore CSR

// Testing CSR 0x7ed
	csrr x13, 0x7ed	// Read CSR
	li x27, -1
	csrrw x9, 0x7ed, x27	// Write all 1s to CSR
	csrrw x9, 0x7ed, x0	// Write all 0s to CSR
	csrrs x9, 0x7ed, x27	// Set all CSR bits
	csrrc x9, 0x7ed, x27	// Clear all CSR bits
	csrrw x9, 0x7ed, x13	// Restore CSR

// Testing CSR 0x7ee
	csrr x31, 0x7ee	// Read CSR
	li x29, -1
	csrrw x8, 0x7ee, x29	// Write all 1s to CSR
	csrrw x8, 0x7ee, x0	// Write all 0s to CSR
	csrrs x8, 0x7ee, x29	// Set all CSR bits
	csrrc x8, 0x7ee, x29	// Clear all CSR bits
	csrrw x8, 0x7ee, x31	// Restore CSR

// Testing CSR 0x7ef
	csrr x11, 0x7ef	// Read CSR
	li x16, -1
	csrrw x24, 0x7ef, x16	// Write all 1s to CSR
	csrrw x24, 0x7ef, x0	// Write all 0s to CSR
	csrrs x24, 0x7ef, x16	// Set all CSR bits
	csrrc x24, 0x7ef, x16	// Clear all CSR bits
	csrrw x24, 0x7ef, x11	// Restore CSR

// Testing CSR 0x7f0
	csrr x6, 0x7f0	// Read CSR
	li x1, -1
	csrrw x11, 0x7f0, x1	// Write all 1s to CSR
	csrrw x11, 0x7f0, x0	// Write all 0s to CSR
	csrrs x11, 0x7f0, x1	// Set all CSR bits
	csrrc x11, 0x7f0, x1	// Clear all CSR bits
	csrrw x11, 0x7f0, x6	// Restore CSR

// Testing CSR 0x7f1
	csrr x11, 0x7f1	// Read CSR
	li x7, -1
	csrrw x1, 0x7f1, x7	// Write all 1s to CSR
	csrrw x1, 0x7f1, x0	// Write all 0s to CSR
	csrrs x1, 0x7f1, x7	// Set all CSR bits
	csrrc x1, 0x7f1, x7	// Clear all CSR bits
	csrrw x1, 0x7f1, x11	// Restore CSR

// Testing CSR 0x7f2
	csrr x25, 0x7f2	// Read CSR
	li x18, -1
	csrrw x14, 0x7f2, x18	// Write all 1s to CSR
	csrrw x14, 0x7f2, x0	// Write all 0s to CSR
	csrrs x14, 0x7f2, x18	// Set all CSR bits
	csrrc x14, 0x7f2, x18	// Clear all CSR bits
	csrrw x14, 0x7f2, x25	// Restore CSR

// Testing CSR 0x7f3
	csrr x18, 0x7f3	// Read CSR
	li x1, -1
	csrrw x19, 0x7f3, x1	// Write all 1s to CSR
	csrrw x19, 0x7f3, x0	// Write all 0s to CSR
	csrrs x19, 0x7f3, x1	// Set all CSR bits
	csrrc x19, 0x7f3, x1	// Clear all CSR bits
	csrrw x19, 0x7f3, x18	// Restore CSR

// Testing CSR 0x7f4
	csrr x17, 0x7f4	// Read CSR
	li x30, -1
	csrrw x27, 0x7f4, x30	// Write all 1s to CSR
	csrrw x27, 0x7f4, x0	// Write all 0s to CSR
	csrrs x27, 0x7f4, x30	// Set all CSR bits
	csrrc x27, 0x7f4, x30	// Clear all CSR bits
	csrrw x27, 0x7f4, x17	// Restore CSR

// Testing CSR 0x7f5
	csrr x27, 0x7f5	// Read CSR
	li x29, -1
	csrrw x7, 0x7f5, x29	// Write all 1s to CSR
	csrrw x7, 0x7f5, x0	// Write all 0s to CSR
	csrrs x7, 0x7f5, x29	// Set all CSR bits
	csrrc x7, 0x7f5, x29	// Clear all CSR bits
	csrrw x7, 0x7f5, x27	// Restore CSR

// Testing CSR 0x7f6
	csrr x7, 0x7f6	// Read CSR
	li x24, -1
	csrrw x2, 0x7f6, x24	// Write all 1s to CSR
	csrrw x2, 0x7f6, x0	// Write all 0s to CSR
	csrrs x2, 0x7f6, x24	// Set all CSR bits
	csrrc x2, 0x7f6, x24	// Clear all CSR bits
	csrrw x2, 0x7f6, x7	// Restore CSR

// Testing CSR 0x7f7
	csrr x17, 0x7f7	// Read CSR
	li x26, -1
	csrrw x13, 0x7f7, x26	// Write all 1s to CSR
	csrrw x13, 0x7f7, x0	// Write all 0s to CSR
	csrrs x13, 0x7f7, x26	// Set all CSR bits
	csrrc x13, 0x7f7, x26	// Clear all CSR bits
	csrrw x13, 0x7f7, x17	// Restore CSR

// Testing CSR 0x7f8
	csrr x16, 0x7f8	// Read CSR
	li x24, -1
	csrrw x10, 0x7f8, x24	// Write all 1s to CSR
	csrrw x10, 0x7f8, x0	// Write all 0s to CSR
	csrrs x10, 0x7f8, x24	// Set all CSR bits
	csrrc x10, 0x7f8, x24	// Clear all CSR bits
	csrrw x10, 0x7f8, x16	// Restore CSR

// Testing CSR 0x7f9
	csrr x31, 0x7f9	// Read CSR
	li x2, -1
	csrrw x30, 0x7f9, x2	// Write all 1s to CSR
	csrrw x30, 0x7f9, x0	// Write all 0s to CSR
	csrrs x30, 0x7f9, x2	// Set all CSR bits
	csrrc x30, 0x7f9, x2	// Clear all CSR bits
	csrrw x30, 0x7f9, x31	// Restore CSR

// Testing CSR 0x7fa
	csrr x30, 0x7fa	// Read CSR
	li x31, -1
	csrrw x4, 0x7fa, x31	// Write all 1s to CSR
	csrrw x4, 0x7fa, x0	// Write all 0s to CSR
	csrrs x4, 0x7fa, x31	// Set all CSR bits
	csrrc x4, 0x7fa, x31	// Clear all CSR bits
	csrrw x4, 0x7fa, x30	// Restore CSR

// Testing CSR 0x7fb
	csrr x16, 0x7fb	// Read CSR
	li x19, -1
	csrrw x30, 0x7fb, x19	// Write all 1s to CSR
	csrrw x30, 0x7fb, x0	// Write all 0s to CSR
	csrrs x30, 0x7fb, x19	// Set all CSR bits
	csrrc x30, 0x7fb, x19	// Clear all CSR bits
	csrrw x30, 0x7fb, x16	// Restore CSR

// Testing CSR 0x7fc
	csrr x26, 0x7fc	// Read CSR
	li x2, -1
	csrrw x31, 0x7fc, x2	// Write all 1s to CSR
	csrrw x31, 0x7fc, x0	// Write all 0s to CSR
	csrrs x31, 0x7fc, x2	// Set all CSR bits
	csrrc x31, 0x7fc, x2	// Clear all CSR bits
	csrrw x31, 0x7fc, x26	// Restore CSR

// Testing CSR 0x7fd
	csrr x6, 0x7fd	// Read CSR
	li x4, -1
	csrrw x17, 0x7fd, x4	// Write all 1s to CSR
	csrrw x17, 0x7fd, x0	// Write all 0s to CSR
	csrrs x17, 0x7fd, x4	// Set all CSR bits
	csrrc x17, 0x7fd, x4	// Clear all CSR bits
	csrrw x17, 0x7fd, x6	// Restore CSR

// Testing CSR 0x7fe
	csrr x17, 0x7fe	// Read CSR
	li x27, -1
	csrrw x31, 0x7fe, x27	// Write all 1s to CSR
	csrrw x31, 0x7fe, x0	// Write all 0s to CSR
	csrrs x31, 0x7fe, x27	// Set all CSR bits
	csrrc x31, 0x7fe, x27	// Clear all CSR bits
	csrrw x31, 0x7fe, x17	// Restore CSR

// Testing CSR 0x7ff
	csrr x24, 0x7ff	// Read CSR
	li x15, -1
	csrrw x25, 0x7ff, x15	// Write all 1s to CSR
	csrrw x25, 0x7ff, x0	// Write all 0s to CSR
	csrrs x25, 0x7ff, x15	// Set all CSR bits
	csrrc x25, 0x7ff, x15	// Clear all CSR bits
	csrrw x25, 0x7ff, x24	// Restore CSR

// Testing CSR 0x900
	csrr x10, 0x900	// Read CSR
	li x27, -1
	csrrw x2, 0x900, x27	// Write all 1s to CSR
	csrrw x2, 0x900, x0	// Write all 0s to CSR
	csrrs x2, 0x900, x27	// Set all CSR bits
	csrrc x2, 0x900, x27	// Clear all CSR bits
	csrrw x2, 0x900, x10	// Restore CSR

// Testing CSR 0x901
	csrr x22, 0x901	// Read CSR
	li x13, -1
	csrrw x24, 0x901, x13	// Write all 1s to CSR
	csrrw x24, 0x901, x0	// Write all 0s to CSR
	csrrs x24, 0x901, x13	// Set all CSR bits
	csrrc x24, 0x901, x13	// Clear all CSR bits
	csrrw x24, 0x901, x22	// Restore CSR

// Testing CSR 0x902
	csrr x12, 0x902	// Read CSR
	li x9, -1
	csrrw x13, 0x902, x9	// Write all 1s to CSR
	csrrw x13, 0x902, x0	// Write all 0s to CSR
	csrrs x13, 0x902, x9	// Set all CSR bits
	csrrc x13, 0x902, x9	// Clear all CSR bits
	csrrw x13, 0x902, x12	// Restore CSR

// Testing CSR 0x903
	csrr x13, 0x903	// Read CSR
	li x15, -1
	csrrw x8, 0x903, x15	// Write all 1s to CSR
	csrrw x8, 0x903, x0	// Write all 0s to CSR
	csrrs x8, 0x903, x15	// Set all CSR bits
	csrrc x8, 0x903, x15	// Clear all CSR bits
	csrrw x8, 0x903, x13	// Restore CSR

// Testing CSR 0x904
	csrr x29, 0x904	// Read CSR
	li x16, -1
	csrrw x8, 0x904, x16	// Write all 1s to CSR
	csrrw x8, 0x904, x0	// Write all 0s to CSR
	csrrs x8, 0x904, x16	// Set all CSR bits
	csrrc x8, 0x904, x16	// Clear all CSR bits
	csrrw x8, 0x904, x29	// Restore CSR

// Testing CSR 0x905
	csrr x20, 0x905	// Read CSR
	li x29, -1
	csrrw x10, 0x905, x29	// Write all 1s to CSR
	csrrw x10, 0x905, x0	// Write all 0s to CSR
	csrrs x10, 0x905, x29	// Set all CSR bits
	csrrc x10, 0x905, x29	// Clear all CSR bits
	csrrw x10, 0x905, x20	// Restore CSR

// Testing CSR 0x906
	csrr x18, 0x906	// Read CSR
	li x24, -1
	csrrw x30, 0x906, x24	// Write all 1s to CSR
	csrrw x30, 0x906, x0	// Write all 0s to CSR
	csrrs x30, 0x906, x24	// Set all CSR bits
	csrrc x30, 0x906, x24	// Clear all CSR bits
	csrrw x30, 0x906, x18	// Restore CSR

// Testing CSR 0x907
	csrr x22, 0x907	// Read CSR
	li x23, -1
	csrrw x14, 0x907, x23	// Write all 1s to CSR
	csrrw x14, 0x907, x0	// Write all 0s to CSR
	csrrs x14, 0x907, x23	// Set all CSR bits
	csrrc x14, 0x907, x23	// Clear all CSR bits
	csrrw x14, 0x907, x22	// Restore CSR

// Testing CSR 0x908
	csrr x13, 0x908	// Read CSR
	li x6, -1
	csrrw x29, 0x908, x6	// Write all 1s to CSR
	csrrw x29, 0x908, x0	// Write all 0s to CSR
	csrrs x29, 0x908, x6	// Set all CSR bits
	csrrc x29, 0x908, x6	// Clear all CSR bits
	csrrw x29, 0x908, x13	// Restore CSR

// Testing CSR 0x909
	csrr x4, 0x909	// Read CSR
	li x3, -1
	csrrw x21, 0x909, x3	// Write all 1s to CSR
	csrrw x21, 0x909, x0	// Write all 0s to CSR
	csrrs x21, 0x909, x3	// Set all CSR bits
	csrrc x21, 0x909, x3	// Clear all CSR bits
	csrrw x21, 0x909, x4	// Restore CSR

// Testing CSR 0x90a
	csrr x1, 0x90a	// Read CSR
	li x2, -1
	csrrw x29, 0x90a, x2	// Write all 1s to CSR
	csrrw x29, 0x90a, x0	// Write all 0s to CSR
	csrrs x29, 0x90a, x2	// Set all CSR bits
	csrrc x29, 0x90a, x2	// Clear all CSR bits
	csrrw x29, 0x90a, x1	// Restore CSR

// Testing CSR 0x90b
	csrr x31, 0x90b	// Read CSR
	li x17, -1
	csrrw x22, 0x90b, x17	// Write all 1s to CSR
	csrrw x22, 0x90b, x0	// Write all 0s to CSR
	csrrs x22, 0x90b, x17	// Set all CSR bits
	csrrc x22, 0x90b, x17	// Clear all CSR bits
	csrrw x22, 0x90b, x31	// Restore CSR

// Testing CSR 0x90c
	csrr x7, 0x90c	// Read CSR
	li x20, -1
	csrrw x18, 0x90c, x20	// Write all 1s to CSR
	csrrw x18, 0x90c, x0	// Write all 0s to CSR
	csrrs x18, 0x90c, x20	// Set all CSR bits
	csrrc x18, 0x90c, x20	// Clear all CSR bits
	csrrw x18, 0x90c, x7	// Restore CSR

// Testing CSR 0x90d
	csrr x10, 0x90d	// Read CSR
	li x4, -1
	csrrw x19, 0x90d, x4	// Write all 1s to CSR
	csrrw x19, 0x90d, x0	// Write all 0s to CSR
	csrrs x19, 0x90d, x4	// Set all CSR bits
	csrrc x19, 0x90d, x4	// Clear all CSR bits
	csrrw x19, 0x90d, x10	// Restore CSR

// Testing CSR 0x90e
	csrr x28, 0x90e	// Read CSR
	li x8, -1
	csrrw x23, 0x90e, x8	// Write all 1s to CSR
	csrrw x23, 0x90e, x0	// Write all 0s to CSR
	csrrs x23, 0x90e, x8	// Set all CSR bits
	csrrc x23, 0x90e, x8	// Clear all CSR bits
	csrrw x23, 0x90e, x28	// Restore CSR

// Testing CSR 0x90f
	csrr x19, 0x90f	// Read CSR
	li x6, -1
	csrrw x13, 0x90f, x6	// Write all 1s to CSR
	csrrw x13, 0x90f, x0	// Write all 0s to CSR
	csrrs x13, 0x90f, x6	// Set all CSR bits
	csrrc x13, 0x90f, x6	// Clear all CSR bits
	csrrw x13, 0x90f, x19	// Restore CSR

// Testing CSR 0x910
	csrr x3, 0x910	// Read CSR
	li x29, -1
	csrrw x22, 0x910, x29	// Write all 1s to CSR
	csrrw x22, 0x910, x0	// Write all 0s to CSR
	csrrs x22, 0x910, x29	// Set all CSR bits
	csrrc x22, 0x910, x29	// Clear all CSR bits
	csrrw x22, 0x910, x3	// Restore CSR

// Testing CSR 0x911
	csrr x31, 0x911	// Read CSR
	li x10, -1
	csrrw x25, 0x911, x10	// Write all 1s to CSR
	csrrw x25, 0x911, x0	// Write all 0s to CSR
	csrrs x25, 0x911, x10	// Set all CSR bits
	csrrc x25, 0x911, x10	// Clear all CSR bits
	csrrw x25, 0x911, x31	// Restore CSR

// Testing CSR 0x912
	csrr x27, 0x912	// Read CSR
	li x7, -1
	csrrw x26, 0x912, x7	// Write all 1s to CSR
	csrrw x26, 0x912, x0	// Write all 0s to CSR
	csrrs x26, 0x912, x7	// Set all CSR bits
	csrrc x26, 0x912, x7	// Clear all CSR bits
	csrrw x26, 0x912, x27	// Restore CSR

// Testing CSR 0x913
	csrr x4, 0x913	// Read CSR
	li x26, -1
	csrrw x14, 0x913, x26	// Write all 1s to CSR
	csrrw x14, 0x913, x0	// Write all 0s to CSR
	csrrs x14, 0x913, x26	// Set all CSR bits
	csrrc x14, 0x913, x26	// Clear all CSR bits
	csrrw x14, 0x913, x4	// Restore CSR

// Testing CSR 0x914
	csrr x27, 0x914	// Read CSR
	li x30, -1
	csrrw x22, 0x914, x30	// Write all 1s to CSR
	csrrw x22, 0x914, x0	// Write all 0s to CSR
	csrrs x22, 0x914, x30	// Set all CSR bits
	csrrc x22, 0x914, x30	// Clear all CSR bits
	csrrw x22, 0x914, x27	// Restore CSR

// Testing CSR 0x915
	csrr x17, 0x915	// Read CSR
	li x23, -1
	csrrw x28, 0x915, x23	// Write all 1s to CSR
	csrrw x28, 0x915, x0	// Write all 0s to CSR
	csrrs x28, 0x915, x23	// Set all CSR bits
	csrrc x28, 0x915, x23	// Clear all CSR bits
	csrrw x28, 0x915, x17	// Restore CSR

// Testing CSR 0x916
	csrr x26, 0x916	// Read CSR
	li x23, -1
	csrrw x1, 0x916, x23	// Write all 1s to CSR
	csrrw x1, 0x916, x0	// Write all 0s to CSR
	csrrs x1, 0x916, x23	// Set all CSR bits
	csrrc x1, 0x916, x23	// Clear all CSR bits
	csrrw x1, 0x916, x26	// Restore CSR

// Testing CSR 0x917
	csrr x25, 0x917	// Read CSR
	li x26, -1
	csrrw x6, 0x917, x26	// Write all 1s to CSR
	csrrw x6, 0x917, x0	// Write all 0s to CSR
	csrrs x6, 0x917, x26	// Set all CSR bits
	csrrc x6, 0x917, x26	// Clear all CSR bits
	csrrw x6, 0x917, x25	// Restore CSR

// Testing CSR 0x918
	csrr x16, 0x918	// Read CSR
	li x9, -1
	csrrw x20, 0x918, x9	// Write all 1s to CSR
	csrrw x20, 0x918, x0	// Write all 0s to CSR
	csrrs x20, 0x918, x9	// Set all CSR bits
	csrrc x20, 0x918, x9	// Clear all CSR bits
	csrrw x20, 0x918, x16	// Restore CSR

// Testing CSR 0x919
	csrr x11, 0x919	// Read CSR
	li x20, -1
	csrrw x13, 0x919, x20	// Write all 1s to CSR
	csrrw x13, 0x919, x0	// Write all 0s to CSR
	csrrs x13, 0x919, x20	// Set all CSR bits
	csrrc x13, 0x919, x20	// Clear all CSR bits
	csrrw x13, 0x919, x11	// Restore CSR

// Testing CSR 0x91a
	csrr x23, 0x91a	// Read CSR
	li x19, -1
	csrrw x11, 0x91a, x19	// Write all 1s to CSR
	csrrw x11, 0x91a, x0	// Write all 0s to CSR
	csrrs x11, 0x91a, x19	// Set all CSR bits
	csrrc x11, 0x91a, x19	// Clear all CSR bits
	csrrw x11, 0x91a, x23	// Restore CSR

// Testing CSR 0x91b
	csrr x5, 0x91b	// Read CSR
	li x19, -1
	csrrw x17, 0x91b, x19	// Write all 1s to CSR
	csrrw x17, 0x91b, x0	// Write all 0s to CSR
	csrrs x17, 0x91b, x19	// Set all CSR bits
	csrrc x17, 0x91b, x19	// Clear all CSR bits
	csrrw x17, 0x91b, x5	// Restore CSR

// Testing CSR 0x91c
	csrr x11, 0x91c	// Read CSR
	li x6, -1
	csrrw x23, 0x91c, x6	// Write all 1s to CSR
	csrrw x23, 0x91c, x0	// Write all 0s to CSR
	csrrs x23, 0x91c, x6	// Set all CSR bits
	csrrc x23, 0x91c, x6	// Clear all CSR bits
	csrrw x23, 0x91c, x11	// Restore CSR

// Testing CSR 0x91d
	csrr x22, 0x91d	// Read CSR
	li x25, -1
	csrrw x2, 0x91d, x25	// Write all 1s to CSR
	csrrw x2, 0x91d, x0	// Write all 0s to CSR
	csrrs x2, 0x91d, x25	// Set all CSR bits
	csrrc x2, 0x91d, x25	// Clear all CSR bits
	csrrw x2, 0x91d, x22	// Restore CSR

// Testing CSR 0x91e
	csrr x24, 0x91e	// Read CSR
	li x12, -1
	csrrw x27, 0x91e, x12	// Write all 1s to CSR
	csrrw x27, 0x91e, x0	// Write all 0s to CSR
	csrrs x27, 0x91e, x12	// Set all CSR bits
	csrrc x27, 0x91e, x12	// Clear all CSR bits
	csrrw x27, 0x91e, x24	// Restore CSR

// Testing CSR 0x91f
	csrr x19, 0x91f	// Read CSR
	li x16, -1
	csrrw x31, 0x91f, x16	// Write all 1s to CSR
	csrrw x31, 0x91f, x0	// Write all 0s to CSR
	csrrs x31, 0x91f, x16	// Set all CSR bits
	csrrc x31, 0x91f, x16	// Clear all CSR bits
	csrrw x31, 0x91f, x19	// Restore CSR

// Testing CSR 0x920
	csrr x10, 0x920	// Read CSR
	li x4, -1
	csrrw x23, 0x920, x4	// Write all 1s to CSR
	csrrw x23, 0x920, x0	// Write all 0s to CSR
	csrrs x23, 0x920, x4	// Set all CSR bits
	csrrc x23, 0x920, x4	// Clear all CSR bits
	csrrw x23, 0x920, x10	// Restore CSR

// Testing CSR 0x921
	csrr x5, 0x921	// Read CSR
	li x31, -1
	csrrw x13, 0x921, x31	// Write all 1s to CSR
	csrrw x13, 0x921, x0	// Write all 0s to CSR
	csrrs x13, 0x921, x31	// Set all CSR bits
	csrrc x13, 0x921, x31	// Clear all CSR bits
	csrrw x13, 0x921, x5	// Restore CSR

// Testing CSR 0x922
	csrr x8, 0x922	// Read CSR
	li x11, -1
	csrrw x28, 0x922, x11	// Write all 1s to CSR
	csrrw x28, 0x922, x0	// Write all 0s to CSR
	csrrs x28, 0x922, x11	// Set all CSR bits
	csrrc x28, 0x922, x11	// Clear all CSR bits
	csrrw x28, 0x922, x8	// Restore CSR

// Testing CSR 0x923
	csrr x23, 0x923	// Read CSR
	li x17, -1
	csrrw x27, 0x923, x17	// Write all 1s to CSR
	csrrw x27, 0x923, x0	// Write all 0s to CSR
	csrrs x27, 0x923, x17	// Set all CSR bits
	csrrc x27, 0x923, x17	// Clear all CSR bits
	csrrw x27, 0x923, x23	// Restore CSR

// Testing CSR 0x924
	csrr x30, 0x924	// Read CSR
	li x5, -1
	csrrw x26, 0x924, x5	// Write all 1s to CSR
	csrrw x26, 0x924, x0	// Write all 0s to CSR
	csrrs x26, 0x924, x5	// Set all CSR bits
	csrrc x26, 0x924, x5	// Clear all CSR bits
	csrrw x26, 0x924, x30	// Restore CSR

// Testing CSR 0x925
	csrr x22, 0x925	// Read CSR
	li x1, -1
	csrrw x26, 0x925, x1	// Write all 1s to CSR
	csrrw x26, 0x925, x0	// Write all 0s to CSR
	csrrs x26, 0x925, x1	// Set all CSR bits
	csrrc x26, 0x925, x1	// Clear all CSR bits
	csrrw x26, 0x925, x22	// Restore CSR

// Testing CSR 0x926
	csrr x6, 0x926	// Read CSR
	li x30, -1
	csrrw x17, 0x926, x30	// Write all 1s to CSR
	csrrw x17, 0x926, x0	// Write all 0s to CSR
	csrrs x17, 0x926, x30	// Set all CSR bits
	csrrc x17, 0x926, x30	// Clear all CSR bits
	csrrw x17, 0x926, x6	// Restore CSR

// Testing CSR 0x927
	csrr x11, 0x927	// Read CSR
	li x2, -1
	csrrw x9, 0x927, x2	// Write all 1s to CSR
	csrrw x9, 0x927, x0	// Write all 0s to CSR
	csrrs x9, 0x927, x2	// Set all CSR bits
	csrrc x9, 0x927, x2	// Clear all CSR bits
	csrrw x9, 0x927, x11	// Restore CSR

// Testing CSR 0x928
	csrr x18, 0x928	// Read CSR
	li x24, -1
	csrrw x13, 0x928, x24	// Write all 1s to CSR
	csrrw x13, 0x928, x0	// Write all 0s to CSR
	csrrs x13, 0x928, x24	// Set all CSR bits
	csrrc x13, 0x928, x24	// Clear all CSR bits
	csrrw x13, 0x928, x18	// Restore CSR

// Testing CSR 0x929
	csrr x13, 0x929	// Read CSR
	li x22, -1
	csrrw x19, 0x929, x22	// Write all 1s to CSR
	csrrw x19, 0x929, x0	// Write all 0s to CSR
	csrrs x19, 0x929, x22	// Set all CSR bits
	csrrc x19, 0x929, x22	// Clear all CSR bits
	csrrw x19, 0x929, x13	// Restore CSR

// Testing CSR 0x92a
	csrr x18, 0x92a	// Read CSR
	li x23, -1
	csrrw x25, 0x92a, x23	// Write all 1s to CSR
	csrrw x25, 0x92a, x0	// Write all 0s to CSR
	csrrs x25, 0x92a, x23	// Set all CSR bits
	csrrc x25, 0x92a, x23	// Clear all CSR bits
	csrrw x25, 0x92a, x18	// Restore CSR

// Testing CSR 0x92b
	csrr x10, 0x92b	// Read CSR
	li x26, -1
	csrrw x24, 0x92b, x26	// Write all 1s to CSR
	csrrw x24, 0x92b, x0	// Write all 0s to CSR
	csrrs x24, 0x92b, x26	// Set all CSR bits
	csrrc x24, 0x92b, x26	// Clear all CSR bits
	csrrw x24, 0x92b, x10	// Restore CSR

// Testing CSR 0x92c
	csrr x16, 0x92c	// Read CSR
	li x30, -1
	csrrw x6, 0x92c, x30	// Write all 1s to CSR
	csrrw x6, 0x92c, x0	// Write all 0s to CSR
	csrrs x6, 0x92c, x30	// Set all CSR bits
	csrrc x6, 0x92c, x30	// Clear all CSR bits
	csrrw x6, 0x92c, x16	// Restore CSR

// Testing CSR 0x92d
	csrr x24, 0x92d	// Read CSR
	li x15, -1
	csrrw x11, 0x92d, x15	// Write all 1s to CSR
	csrrw x11, 0x92d, x0	// Write all 0s to CSR
	csrrs x11, 0x92d, x15	// Set all CSR bits
	csrrc x11, 0x92d, x15	// Clear all CSR bits
	csrrw x11, 0x92d, x24	// Restore CSR

// Testing CSR 0x92e
	csrr x28, 0x92e	// Read CSR
	li x16, -1
	csrrw x8, 0x92e, x16	// Write all 1s to CSR
	csrrw x8, 0x92e, x0	// Write all 0s to CSR
	csrrs x8, 0x92e, x16	// Set all CSR bits
	csrrc x8, 0x92e, x16	// Clear all CSR bits
	csrrw x8, 0x92e, x28	// Restore CSR

// Testing CSR 0x92f
	csrr x30, 0x92f	// Read CSR
	li x7, -1
	csrrw x19, 0x92f, x7	// Write all 1s to CSR
	csrrw x19, 0x92f, x0	// Write all 0s to CSR
	csrrs x19, 0x92f, x7	// Set all CSR bits
	csrrc x19, 0x92f, x7	// Clear all CSR bits
	csrrw x19, 0x92f, x30	// Restore CSR

// Testing CSR 0x930
	csrr x6, 0x930	// Read CSR
	li x4, -1
	csrrw x25, 0x930, x4	// Write all 1s to CSR
	csrrw x25, 0x930, x0	// Write all 0s to CSR
	csrrs x25, 0x930, x4	// Set all CSR bits
	csrrc x25, 0x930, x4	// Clear all CSR bits
	csrrw x25, 0x930, x6	// Restore CSR

// Testing CSR 0x931
	csrr x17, 0x931	// Read CSR
	li x1, -1
	csrrw x12, 0x931, x1	// Write all 1s to CSR
	csrrw x12, 0x931, x0	// Write all 0s to CSR
	csrrs x12, 0x931, x1	// Set all CSR bits
	csrrc x12, 0x931, x1	// Clear all CSR bits
	csrrw x12, 0x931, x17	// Restore CSR

// Testing CSR 0x932
	csrr x8, 0x932	// Read CSR
	li x30, -1
	csrrw x9, 0x932, x30	// Write all 1s to CSR
	csrrw x9, 0x932, x0	// Write all 0s to CSR
	csrrs x9, 0x932, x30	// Set all CSR bits
	csrrc x9, 0x932, x30	// Clear all CSR bits
	csrrw x9, 0x932, x8	// Restore CSR

// Testing CSR 0x933
	csrr x18, 0x933	// Read CSR
	li x20, -1
	csrrw x6, 0x933, x20	// Write all 1s to CSR
	csrrw x6, 0x933, x0	// Write all 0s to CSR
	csrrs x6, 0x933, x20	// Set all CSR bits
	csrrc x6, 0x933, x20	// Clear all CSR bits
	csrrw x6, 0x933, x18	// Restore CSR

// Testing CSR 0x934
	csrr x9, 0x934	// Read CSR
	li x2, -1
	csrrw x13, 0x934, x2	// Write all 1s to CSR
	csrrw x13, 0x934, x0	// Write all 0s to CSR
	csrrs x13, 0x934, x2	// Set all CSR bits
	csrrc x13, 0x934, x2	// Clear all CSR bits
	csrrw x13, 0x934, x9	// Restore CSR

// Testing CSR 0x935
	csrr x30, 0x935	// Read CSR
	li x6, -1
	csrrw x5, 0x935, x6	// Write all 1s to CSR
	csrrw x5, 0x935, x0	// Write all 0s to CSR
	csrrs x5, 0x935, x6	// Set all CSR bits
	csrrc x5, 0x935, x6	// Clear all CSR bits
	csrrw x5, 0x935, x30	// Restore CSR

// Testing CSR 0x936
	csrr x22, 0x936	// Read CSR
	li x21, -1
	csrrw x14, 0x936, x21	// Write all 1s to CSR
	csrrw x14, 0x936, x0	// Write all 0s to CSR
	csrrs x14, 0x936, x21	// Set all CSR bits
	csrrc x14, 0x936, x21	// Clear all CSR bits
	csrrw x14, 0x936, x22	// Restore CSR

// Testing CSR 0x937
	csrr x27, 0x937	// Read CSR
	li x12, -1
	csrrw x16, 0x937, x12	// Write all 1s to CSR
	csrrw x16, 0x937, x0	// Write all 0s to CSR
	csrrs x16, 0x937, x12	// Set all CSR bits
	csrrc x16, 0x937, x12	// Clear all CSR bits
	csrrw x16, 0x937, x27	// Restore CSR

// Testing CSR 0x938
	csrr x24, 0x938	// Read CSR
	li x3, -1
	csrrw x14, 0x938, x3	// Write all 1s to CSR
	csrrw x14, 0x938, x0	// Write all 0s to CSR
	csrrs x14, 0x938, x3	// Set all CSR bits
	csrrc x14, 0x938, x3	// Clear all CSR bits
	csrrw x14, 0x938, x24	// Restore CSR

// Testing CSR 0x939
	csrr x7, 0x939	// Read CSR
	li x6, -1
	csrrw x10, 0x939, x6	// Write all 1s to CSR
	csrrw x10, 0x939, x0	// Write all 0s to CSR
	csrrs x10, 0x939, x6	// Set all CSR bits
	csrrc x10, 0x939, x6	// Clear all CSR bits
	csrrw x10, 0x939, x7	// Restore CSR

// Testing CSR 0x93a
	csrr x20, 0x93a	// Read CSR
	li x13, -1
	csrrw x16, 0x93a, x13	// Write all 1s to CSR
	csrrw x16, 0x93a, x0	// Write all 0s to CSR
	csrrs x16, 0x93a, x13	// Set all CSR bits
	csrrc x16, 0x93a, x13	// Clear all CSR bits
	csrrw x16, 0x93a, x20	// Restore CSR

// Testing CSR 0x93b
	csrr x19, 0x93b	// Read CSR
	li x29, -1
	csrrw x27, 0x93b, x29	// Write all 1s to CSR
	csrrw x27, 0x93b, x0	// Write all 0s to CSR
	csrrs x27, 0x93b, x29	// Set all CSR bits
	csrrc x27, 0x93b, x29	// Clear all CSR bits
	csrrw x27, 0x93b, x19	// Restore CSR

// Testing CSR 0x93c
	csrr x18, 0x93c	// Read CSR
	li x15, -1
	csrrw x26, 0x93c, x15	// Write all 1s to CSR
	csrrw x26, 0x93c, x0	// Write all 0s to CSR
	csrrs x26, 0x93c, x15	// Set all CSR bits
	csrrc x26, 0x93c, x15	// Clear all CSR bits
	csrrw x26, 0x93c, x18	// Restore CSR

// Testing CSR 0x93d
	csrr x31, 0x93d	// Read CSR
	li x6, -1
	csrrw x11, 0x93d, x6	// Write all 1s to CSR
	csrrw x11, 0x93d, x0	// Write all 0s to CSR
	csrrs x11, 0x93d, x6	// Set all CSR bits
	csrrc x11, 0x93d, x6	// Clear all CSR bits
	csrrw x11, 0x93d, x31	// Restore CSR

// Testing CSR 0x93e
	csrr x25, 0x93e	// Read CSR
	li x13, -1
	csrrw x15, 0x93e, x13	// Write all 1s to CSR
	csrrw x15, 0x93e, x0	// Write all 0s to CSR
	csrrs x15, 0x93e, x13	// Set all CSR bits
	csrrc x15, 0x93e, x13	// Clear all CSR bits
	csrrw x15, 0x93e, x25	// Restore CSR

// Testing CSR 0x93f
	csrr x26, 0x93f	// Read CSR
	li x24, -1
	csrrw x12, 0x93f, x24	// Write all 1s to CSR
	csrrw x12, 0x93f, x0	// Write all 0s to CSR
	csrrs x12, 0x93f, x24	// Set all CSR bits
	csrrc x12, 0x93f, x24	// Clear all CSR bits
	csrrw x12, 0x93f, x26	// Restore CSR

// Testing CSR 0x940
	csrr x1, 0x940	// Read CSR
	li x30, -1
	csrrw x19, 0x940, x30	// Write all 1s to CSR
	csrrw x19, 0x940, x0	// Write all 0s to CSR
	csrrs x19, 0x940, x30	// Set all CSR bits
	csrrc x19, 0x940, x30	// Clear all CSR bits
	csrrw x19, 0x940, x1	// Restore CSR

// Testing CSR 0x941
	csrr x18, 0x941	// Read CSR
	li x21, -1
	csrrw x27, 0x941, x21	// Write all 1s to CSR
	csrrw x27, 0x941, x0	// Write all 0s to CSR
	csrrs x27, 0x941, x21	// Set all CSR bits
	csrrc x27, 0x941, x21	// Clear all CSR bits
	csrrw x27, 0x941, x18	// Restore CSR

// Testing CSR 0x942
	csrr x18, 0x942	// Read CSR
	li x15, -1
	csrrw x11, 0x942, x15	// Write all 1s to CSR
	csrrw x11, 0x942, x0	// Write all 0s to CSR
	csrrs x11, 0x942, x15	// Set all CSR bits
	csrrc x11, 0x942, x15	// Clear all CSR bits
	csrrw x11, 0x942, x18	// Restore CSR

// Testing CSR 0x943
	csrr x18, 0x943	// Read CSR
	li x23, -1
	csrrw x24, 0x943, x23	// Write all 1s to CSR
	csrrw x24, 0x943, x0	// Write all 0s to CSR
	csrrs x24, 0x943, x23	// Set all CSR bits
	csrrc x24, 0x943, x23	// Clear all CSR bits
	csrrw x24, 0x943, x18	// Restore CSR

// Testing CSR 0x944
	csrr x12, 0x944	// Read CSR
	li x9, -1
	csrrw x3, 0x944, x9	// Write all 1s to CSR
	csrrw x3, 0x944, x0	// Write all 0s to CSR
	csrrs x3, 0x944, x9	// Set all CSR bits
	csrrc x3, 0x944, x9	// Clear all CSR bits
	csrrw x3, 0x944, x12	// Restore CSR

// Testing CSR 0x945
	csrr x3, 0x945	// Read CSR
	li x5, -1
	csrrw x20, 0x945, x5	// Write all 1s to CSR
	csrrw x20, 0x945, x0	// Write all 0s to CSR
	csrrs x20, 0x945, x5	// Set all CSR bits
	csrrc x20, 0x945, x5	// Clear all CSR bits
	csrrw x20, 0x945, x3	// Restore CSR

// Testing CSR 0x946
	csrr x12, 0x946	// Read CSR
	li x4, -1
	csrrw x21, 0x946, x4	// Write all 1s to CSR
	csrrw x21, 0x946, x0	// Write all 0s to CSR
	csrrs x21, 0x946, x4	// Set all CSR bits
	csrrc x21, 0x946, x4	// Clear all CSR bits
	csrrw x21, 0x946, x12	// Restore CSR

// Testing CSR 0x947
	csrr x23, 0x947	// Read CSR
	li x9, -1
	csrrw x30, 0x947, x9	// Write all 1s to CSR
	csrrw x30, 0x947, x0	// Write all 0s to CSR
	csrrs x30, 0x947, x9	// Set all CSR bits
	csrrc x30, 0x947, x9	// Clear all CSR bits
	csrrw x30, 0x947, x23	// Restore CSR

// Testing CSR 0x948
	csrr x11, 0x948	// Read CSR
	li x28, -1
	csrrw x5, 0x948, x28	// Write all 1s to CSR
	csrrw x5, 0x948, x0	// Write all 0s to CSR
	csrrs x5, 0x948, x28	// Set all CSR bits
	csrrc x5, 0x948, x28	// Clear all CSR bits
	csrrw x5, 0x948, x11	// Restore CSR

// Testing CSR 0x949
	csrr x13, 0x949	// Read CSR
	li x23, -1
	csrrw x6, 0x949, x23	// Write all 1s to CSR
	csrrw x6, 0x949, x0	// Write all 0s to CSR
	csrrs x6, 0x949, x23	// Set all CSR bits
	csrrc x6, 0x949, x23	// Clear all CSR bits
	csrrw x6, 0x949, x13	// Restore CSR

// Testing CSR 0x94a
	csrr x2, 0x94a	// Read CSR
	li x15, -1
	csrrw x30, 0x94a, x15	// Write all 1s to CSR
	csrrw x30, 0x94a, x0	// Write all 0s to CSR
	csrrs x30, 0x94a, x15	// Set all CSR bits
	csrrc x30, 0x94a, x15	// Clear all CSR bits
	csrrw x30, 0x94a, x2	// Restore CSR

// Testing CSR 0x94b
	csrr x9, 0x94b	// Read CSR
	li x8, -1
	csrrw x28, 0x94b, x8	// Write all 1s to CSR
	csrrw x28, 0x94b, x0	// Write all 0s to CSR
	csrrs x28, 0x94b, x8	// Set all CSR bits
	csrrc x28, 0x94b, x8	// Clear all CSR bits
	csrrw x28, 0x94b, x9	// Restore CSR

// Testing CSR 0x94c
	csrr x27, 0x94c	// Read CSR
	li x23, -1
	csrrw x13, 0x94c, x23	// Write all 1s to CSR
	csrrw x13, 0x94c, x0	// Write all 0s to CSR
	csrrs x13, 0x94c, x23	// Set all CSR bits
	csrrc x13, 0x94c, x23	// Clear all CSR bits
	csrrw x13, 0x94c, x27	// Restore CSR

// Testing CSR 0x94d
	csrr x20, 0x94d	// Read CSR
	li x7, -1
	csrrw x17, 0x94d, x7	// Write all 1s to CSR
	csrrw x17, 0x94d, x0	// Write all 0s to CSR
	csrrs x17, 0x94d, x7	// Set all CSR bits
	csrrc x17, 0x94d, x7	// Clear all CSR bits
	csrrw x17, 0x94d, x20	// Restore CSR

// Testing CSR 0x94e
	csrr x6, 0x94e	// Read CSR
	li x18, -1
	csrrw x10, 0x94e, x18	// Write all 1s to CSR
	csrrw x10, 0x94e, x0	// Write all 0s to CSR
	csrrs x10, 0x94e, x18	// Set all CSR bits
	csrrc x10, 0x94e, x18	// Clear all CSR bits
	csrrw x10, 0x94e, x6	// Restore CSR

// Testing CSR 0x94f
	csrr x27, 0x94f	// Read CSR
	li x21, -1
	csrrw x6, 0x94f, x21	// Write all 1s to CSR
	csrrw x6, 0x94f, x0	// Write all 0s to CSR
	csrrs x6, 0x94f, x21	// Set all CSR bits
	csrrc x6, 0x94f, x21	// Clear all CSR bits
	csrrw x6, 0x94f, x27	// Restore CSR

// Testing CSR 0x950
	csrr x15, 0x950	// Read CSR
	li x8, -1
	csrrw x21, 0x950, x8	// Write all 1s to CSR
	csrrw x21, 0x950, x0	// Write all 0s to CSR
	csrrs x21, 0x950, x8	// Set all CSR bits
	csrrc x21, 0x950, x8	// Clear all CSR bits
	csrrw x21, 0x950, x15	// Restore CSR

// Testing CSR 0x951
	csrr x3, 0x951	// Read CSR
	li x28, -1
	csrrw x22, 0x951, x28	// Write all 1s to CSR
	csrrw x22, 0x951, x0	// Write all 0s to CSR
	csrrs x22, 0x951, x28	// Set all CSR bits
	csrrc x22, 0x951, x28	// Clear all CSR bits
	csrrw x22, 0x951, x3	// Restore CSR

// Testing CSR 0x952
	csrr x12, 0x952	// Read CSR
	li x7, -1
	csrrw x18, 0x952, x7	// Write all 1s to CSR
	csrrw x18, 0x952, x0	// Write all 0s to CSR
	csrrs x18, 0x952, x7	// Set all CSR bits
	csrrc x18, 0x952, x7	// Clear all CSR bits
	csrrw x18, 0x952, x12	// Restore CSR

// Testing CSR 0x953
	csrr x24, 0x953	// Read CSR
	li x7, -1
	csrrw x27, 0x953, x7	// Write all 1s to CSR
	csrrw x27, 0x953, x0	// Write all 0s to CSR
	csrrs x27, 0x953, x7	// Set all CSR bits
	csrrc x27, 0x953, x7	// Clear all CSR bits
	csrrw x27, 0x953, x24	// Restore CSR

// Testing CSR 0x954
	csrr x16, 0x954	// Read CSR
	li x13, -1
	csrrw x2, 0x954, x13	// Write all 1s to CSR
	csrrw x2, 0x954, x0	// Write all 0s to CSR
	csrrs x2, 0x954, x13	// Set all CSR bits
	csrrc x2, 0x954, x13	// Clear all CSR bits
	csrrw x2, 0x954, x16	// Restore CSR

// Testing CSR 0x955
	csrr x24, 0x955	// Read CSR
	li x29, -1
	csrrw x1, 0x955, x29	// Write all 1s to CSR
	csrrw x1, 0x955, x0	// Write all 0s to CSR
	csrrs x1, 0x955, x29	// Set all CSR bits
	csrrc x1, 0x955, x29	// Clear all CSR bits
	csrrw x1, 0x955, x24	// Restore CSR

// Testing CSR 0x956
	csrr x3, 0x956	// Read CSR
	li x27, -1
	csrrw x8, 0x956, x27	// Write all 1s to CSR
	csrrw x8, 0x956, x0	// Write all 0s to CSR
	csrrs x8, 0x956, x27	// Set all CSR bits
	csrrc x8, 0x956, x27	// Clear all CSR bits
	csrrw x8, 0x956, x3	// Restore CSR

// Testing CSR 0x957
	csrr x30, 0x957	// Read CSR
	li x23, -1
	csrrw x2, 0x957, x23	// Write all 1s to CSR
	csrrw x2, 0x957, x0	// Write all 0s to CSR
	csrrs x2, 0x957, x23	// Set all CSR bits
	csrrc x2, 0x957, x23	// Clear all CSR bits
	csrrw x2, 0x957, x30	// Restore CSR

// Testing CSR 0x958
	csrr x6, 0x958	// Read CSR
	li x15, -1
	csrrw x18, 0x958, x15	// Write all 1s to CSR
	csrrw x18, 0x958, x0	// Write all 0s to CSR
	csrrs x18, 0x958, x15	// Set all CSR bits
	csrrc x18, 0x958, x15	// Clear all CSR bits
	csrrw x18, 0x958, x6	// Restore CSR

// Testing CSR 0x959
	csrr x17, 0x959	// Read CSR
	li x1, -1
	csrrw x13, 0x959, x1	// Write all 1s to CSR
	csrrw x13, 0x959, x0	// Write all 0s to CSR
	csrrs x13, 0x959, x1	// Set all CSR bits
	csrrc x13, 0x959, x1	// Clear all CSR bits
	csrrw x13, 0x959, x17	// Restore CSR

// Testing CSR 0x95a
	csrr x3, 0x95a	// Read CSR
	li x6, -1
	csrrw x21, 0x95a, x6	// Write all 1s to CSR
	csrrw x21, 0x95a, x0	// Write all 0s to CSR
	csrrs x21, 0x95a, x6	// Set all CSR bits
	csrrc x21, 0x95a, x6	// Clear all CSR bits
	csrrw x21, 0x95a, x3	// Restore CSR

// Testing CSR 0x95b
	csrr x29, 0x95b	// Read CSR
	li x24, -1
	csrrw x23, 0x95b, x24	// Write all 1s to CSR
	csrrw x23, 0x95b, x0	// Write all 0s to CSR
	csrrs x23, 0x95b, x24	// Set all CSR bits
	csrrc x23, 0x95b, x24	// Clear all CSR bits
	csrrw x23, 0x95b, x29	// Restore CSR

// Testing CSR 0x95c
	csrr x30, 0x95c	// Read CSR
	li x8, -1
	csrrw x5, 0x95c, x8	// Write all 1s to CSR
	csrrw x5, 0x95c, x0	// Write all 0s to CSR
	csrrs x5, 0x95c, x8	// Set all CSR bits
	csrrc x5, 0x95c, x8	// Clear all CSR bits
	csrrw x5, 0x95c, x30	// Restore CSR

// Testing CSR 0x95d
	csrr x9, 0x95d	// Read CSR
	li x30, -1
	csrrw x11, 0x95d, x30	// Write all 1s to CSR
	csrrw x11, 0x95d, x0	// Write all 0s to CSR
	csrrs x11, 0x95d, x30	// Set all CSR bits
	csrrc x11, 0x95d, x30	// Clear all CSR bits
	csrrw x11, 0x95d, x9	// Restore CSR

// Testing CSR 0x95e
	csrr x27, 0x95e	// Read CSR
	li x5, -1
	csrrw x3, 0x95e, x5	// Write all 1s to CSR
	csrrw x3, 0x95e, x0	// Write all 0s to CSR
	csrrs x3, 0x95e, x5	// Set all CSR bits
	csrrc x3, 0x95e, x5	// Clear all CSR bits
	csrrw x3, 0x95e, x27	// Restore CSR

// Testing CSR 0x95f
	csrr x22, 0x95f	// Read CSR
	li x19, -1
	csrrw x6, 0x95f, x19	// Write all 1s to CSR
	csrrw x6, 0x95f, x0	// Write all 0s to CSR
	csrrs x6, 0x95f, x19	// Set all CSR bits
	csrrc x6, 0x95f, x19	// Clear all CSR bits
	csrrw x6, 0x95f, x22	// Restore CSR

// Testing CSR 0x960
	csrr x4, 0x960	// Read CSR
	li x22, -1
	csrrw x28, 0x960, x22	// Write all 1s to CSR
	csrrw x28, 0x960, x0	// Write all 0s to CSR
	csrrs x28, 0x960, x22	// Set all CSR bits
	csrrc x28, 0x960, x22	// Clear all CSR bits
	csrrw x28, 0x960, x4	// Restore CSR

// Testing CSR 0x961
	csrr x1, 0x961	// Read CSR
	li x28, -1
	csrrw x26, 0x961, x28	// Write all 1s to CSR
	csrrw x26, 0x961, x0	// Write all 0s to CSR
	csrrs x26, 0x961, x28	// Set all CSR bits
	csrrc x26, 0x961, x28	// Clear all CSR bits
	csrrw x26, 0x961, x1	// Restore CSR

// Testing CSR 0x962
	csrr x28, 0x962	// Read CSR
	li x8, -1
	csrrw x9, 0x962, x8	// Write all 1s to CSR
	csrrw x9, 0x962, x0	// Write all 0s to CSR
	csrrs x9, 0x962, x8	// Set all CSR bits
	csrrc x9, 0x962, x8	// Clear all CSR bits
	csrrw x9, 0x962, x28	// Restore CSR

// Testing CSR 0x963
	csrr x21, 0x963	// Read CSR
	li x8, -1
	csrrw x9, 0x963, x8	// Write all 1s to CSR
	csrrw x9, 0x963, x0	// Write all 0s to CSR
	csrrs x9, 0x963, x8	// Set all CSR bits
	csrrc x9, 0x963, x8	// Clear all CSR bits
	csrrw x9, 0x963, x21	// Restore CSR

// Testing CSR 0x964
	csrr x22, 0x964	// Read CSR
	li x31, -1
	csrrw x29, 0x964, x31	// Write all 1s to CSR
	csrrw x29, 0x964, x0	// Write all 0s to CSR
	csrrs x29, 0x964, x31	// Set all CSR bits
	csrrc x29, 0x964, x31	// Clear all CSR bits
	csrrw x29, 0x964, x22	// Restore CSR

// Testing CSR 0x965
	csrr x14, 0x965	// Read CSR
	li x22, -1
	csrrw x6, 0x965, x22	// Write all 1s to CSR
	csrrw x6, 0x965, x0	// Write all 0s to CSR
	csrrs x6, 0x965, x22	// Set all CSR bits
	csrrc x6, 0x965, x22	// Clear all CSR bits
	csrrw x6, 0x965, x14	// Restore CSR

// Testing CSR 0x966
	csrr x25, 0x966	// Read CSR
	li x9, -1
	csrrw x10, 0x966, x9	// Write all 1s to CSR
	csrrw x10, 0x966, x0	// Write all 0s to CSR
	csrrs x10, 0x966, x9	// Set all CSR bits
	csrrc x10, 0x966, x9	// Clear all CSR bits
	csrrw x10, 0x966, x25	// Restore CSR

// Testing CSR 0x967
	csrr x27, 0x967	// Read CSR
	li x9, -1
	csrrw x7, 0x967, x9	// Write all 1s to CSR
	csrrw x7, 0x967, x0	// Write all 0s to CSR
	csrrs x7, 0x967, x9	// Set all CSR bits
	csrrc x7, 0x967, x9	// Clear all CSR bits
	csrrw x7, 0x967, x27	// Restore CSR

// Testing CSR 0x968
	csrr x31, 0x968	// Read CSR
	li x2, -1
	csrrw x19, 0x968, x2	// Write all 1s to CSR
	csrrw x19, 0x968, x0	// Write all 0s to CSR
	csrrs x19, 0x968, x2	// Set all CSR bits
	csrrc x19, 0x968, x2	// Clear all CSR bits
	csrrw x19, 0x968, x31	// Restore CSR

// Testing CSR 0x969
	csrr x17, 0x969	// Read CSR
	li x23, -1
	csrrw x16, 0x969, x23	// Write all 1s to CSR
	csrrw x16, 0x969, x0	// Write all 0s to CSR
	csrrs x16, 0x969, x23	// Set all CSR bits
	csrrc x16, 0x969, x23	// Clear all CSR bits
	csrrw x16, 0x969, x17	// Restore CSR

// Testing CSR 0x96a
	csrr x4, 0x96a	// Read CSR
	li x29, -1
	csrrw x1, 0x96a, x29	// Write all 1s to CSR
	csrrw x1, 0x96a, x0	// Write all 0s to CSR
	csrrs x1, 0x96a, x29	// Set all CSR bits
	csrrc x1, 0x96a, x29	// Clear all CSR bits
	csrrw x1, 0x96a, x4	// Restore CSR

// Testing CSR 0x96b
	csrr x29, 0x96b	// Read CSR
	li x19, -1
	csrrw x31, 0x96b, x19	// Write all 1s to CSR
	csrrw x31, 0x96b, x0	// Write all 0s to CSR
	csrrs x31, 0x96b, x19	// Set all CSR bits
	csrrc x31, 0x96b, x19	// Clear all CSR bits
	csrrw x31, 0x96b, x29	// Restore CSR

// Testing CSR 0x96c
	csrr x29, 0x96c	// Read CSR
	li x31, -1
	csrrw x22, 0x96c, x31	// Write all 1s to CSR
	csrrw x22, 0x96c, x0	// Write all 0s to CSR
	csrrs x22, 0x96c, x31	// Set all CSR bits
	csrrc x22, 0x96c, x31	// Clear all CSR bits
	csrrw x22, 0x96c, x29	// Restore CSR

// Testing CSR 0x96d
	csrr x25, 0x96d	// Read CSR
	li x28, -1
	csrrw x12, 0x96d, x28	// Write all 1s to CSR
	csrrw x12, 0x96d, x0	// Write all 0s to CSR
	csrrs x12, 0x96d, x28	// Set all CSR bits
	csrrc x12, 0x96d, x28	// Clear all CSR bits
	csrrw x12, 0x96d, x25	// Restore CSR

// Testing CSR 0x96e
	csrr x21, 0x96e	// Read CSR
	li x17, -1
	csrrw x4, 0x96e, x17	// Write all 1s to CSR
	csrrw x4, 0x96e, x0	// Write all 0s to CSR
	csrrs x4, 0x96e, x17	// Set all CSR bits
	csrrc x4, 0x96e, x17	// Clear all CSR bits
	csrrw x4, 0x96e, x21	// Restore CSR

// Testing CSR 0x96f
	csrr x15, 0x96f	// Read CSR
	li x2, -1
	csrrw x20, 0x96f, x2	// Write all 1s to CSR
	csrrw x20, 0x96f, x0	// Write all 0s to CSR
	csrrs x20, 0x96f, x2	// Set all CSR bits
	csrrc x20, 0x96f, x2	// Clear all CSR bits
	csrrw x20, 0x96f, x15	// Restore CSR

// Testing CSR 0x970
	csrr x25, 0x970	// Read CSR
	li x18, -1
	csrrw x20, 0x970, x18	// Write all 1s to CSR
	csrrw x20, 0x970, x0	// Write all 0s to CSR
	csrrs x20, 0x970, x18	// Set all CSR bits
	csrrc x20, 0x970, x18	// Clear all CSR bits
	csrrw x20, 0x970, x25	// Restore CSR

// Testing CSR 0x971
	csrr x12, 0x971	// Read CSR
	li x22, -1
	csrrw x30, 0x971, x22	// Write all 1s to CSR
	csrrw x30, 0x971, x0	// Write all 0s to CSR
	csrrs x30, 0x971, x22	// Set all CSR bits
	csrrc x30, 0x971, x22	// Clear all CSR bits
	csrrw x30, 0x971, x12	// Restore CSR

// Testing CSR 0x972
	csrr x27, 0x972	// Read CSR
	li x24, -1
	csrrw x8, 0x972, x24	// Write all 1s to CSR
	csrrw x8, 0x972, x0	// Write all 0s to CSR
	csrrs x8, 0x972, x24	// Set all CSR bits
	csrrc x8, 0x972, x24	// Clear all CSR bits
	csrrw x8, 0x972, x27	// Restore CSR

// Testing CSR 0x973
	csrr x10, 0x973	// Read CSR
	li x30, -1
	csrrw x6, 0x973, x30	// Write all 1s to CSR
	csrrw x6, 0x973, x0	// Write all 0s to CSR
	csrrs x6, 0x973, x30	// Set all CSR bits
	csrrc x6, 0x973, x30	// Clear all CSR bits
	csrrw x6, 0x973, x10	// Restore CSR

// Testing CSR 0x974
	csrr x16, 0x974	// Read CSR
	li x5, -1
	csrrw x1, 0x974, x5	// Write all 1s to CSR
	csrrw x1, 0x974, x0	// Write all 0s to CSR
	csrrs x1, 0x974, x5	// Set all CSR bits
	csrrc x1, 0x974, x5	// Clear all CSR bits
	csrrw x1, 0x974, x16	// Restore CSR

// Testing CSR 0x975
	csrr x31, 0x975	// Read CSR
	li x26, -1
	csrrw x7, 0x975, x26	// Write all 1s to CSR
	csrrw x7, 0x975, x0	// Write all 0s to CSR
	csrrs x7, 0x975, x26	// Set all CSR bits
	csrrc x7, 0x975, x26	// Clear all CSR bits
	csrrw x7, 0x975, x31	// Restore CSR

// Testing CSR 0x976
	csrr x8, 0x976	// Read CSR
	li x31, -1
	csrrw x27, 0x976, x31	// Write all 1s to CSR
	csrrw x27, 0x976, x0	// Write all 0s to CSR
	csrrs x27, 0x976, x31	// Set all CSR bits
	csrrc x27, 0x976, x31	// Clear all CSR bits
	csrrw x27, 0x976, x8	// Restore CSR

// Testing CSR 0x977
	csrr x29, 0x977	// Read CSR
	li x7, -1
	csrrw x14, 0x977, x7	// Write all 1s to CSR
	csrrw x14, 0x977, x0	// Write all 0s to CSR
	csrrs x14, 0x977, x7	// Set all CSR bits
	csrrc x14, 0x977, x7	// Clear all CSR bits
	csrrw x14, 0x977, x29	// Restore CSR

// Testing CSR 0x978
	csrr x5, 0x978	// Read CSR
	li x12, -1
	csrrw x18, 0x978, x12	// Write all 1s to CSR
	csrrw x18, 0x978, x0	// Write all 0s to CSR
	csrrs x18, 0x978, x12	// Set all CSR bits
	csrrc x18, 0x978, x12	// Clear all CSR bits
	csrrw x18, 0x978, x5	// Restore CSR

// Testing CSR 0x979
	csrr x25, 0x979	// Read CSR
	li x1, -1
	csrrw x4, 0x979, x1	// Write all 1s to CSR
	csrrw x4, 0x979, x0	// Write all 0s to CSR
	csrrs x4, 0x979, x1	// Set all CSR bits
	csrrc x4, 0x979, x1	// Clear all CSR bits
	csrrw x4, 0x979, x25	// Restore CSR

// Testing CSR 0x97a
	csrr x7, 0x97a	// Read CSR
	li x30, -1
	csrrw x25, 0x97a, x30	// Write all 1s to CSR
	csrrw x25, 0x97a, x0	// Write all 0s to CSR
	csrrs x25, 0x97a, x30	// Set all CSR bits
	csrrc x25, 0x97a, x30	// Clear all CSR bits
	csrrw x25, 0x97a, x7	// Restore CSR

// Testing CSR 0x97b
	csrr x20, 0x97b	// Read CSR
	li x19, -1
	csrrw x2, 0x97b, x19	// Write all 1s to CSR
	csrrw x2, 0x97b, x0	// Write all 0s to CSR
	csrrs x2, 0x97b, x19	// Set all CSR bits
	csrrc x2, 0x97b, x19	// Clear all CSR bits
	csrrw x2, 0x97b, x20	// Restore CSR

// Testing CSR 0x97c
	csrr x31, 0x97c	// Read CSR
	li x26, -1
	csrrw x29, 0x97c, x26	// Write all 1s to CSR
	csrrw x29, 0x97c, x0	// Write all 0s to CSR
	csrrs x29, 0x97c, x26	// Set all CSR bits
	csrrc x29, 0x97c, x26	// Clear all CSR bits
	csrrw x29, 0x97c, x31	// Restore CSR

// Testing CSR 0x97d
	csrr x27, 0x97d	// Read CSR
	li x11, -1
	csrrw x18, 0x97d, x11	// Write all 1s to CSR
	csrrw x18, 0x97d, x0	// Write all 0s to CSR
	csrrs x18, 0x97d, x11	// Set all CSR bits
	csrrc x18, 0x97d, x11	// Clear all CSR bits
	csrrw x18, 0x97d, x27	// Restore CSR

// Testing CSR 0x97e
	csrr x13, 0x97e	// Read CSR
	li x10, -1
	csrrw x31, 0x97e, x10	// Write all 1s to CSR
	csrrw x31, 0x97e, x0	// Write all 0s to CSR
	csrrs x31, 0x97e, x10	// Set all CSR bits
	csrrc x31, 0x97e, x10	// Clear all CSR bits
	csrrw x31, 0x97e, x13	// Restore CSR

// Testing CSR 0x97f
	csrr x28, 0x97f	// Read CSR
	li x23, -1
	csrrw x25, 0x97f, x23	// Write all 1s to CSR
	csrrw x25, 0x97f, x0	// Write all 0s to CSR
	csrrs x25, 0x97f, x23	// Set all CSR bits
	csrrc x25, 0x97f, x23	// Clear all CSR bits
	csrrw x25, 0x97f, x28	// Restore CSR

// Testing CSR 0x980
	csrr x1, 0x980	// Read CSR
	li x27, -1
	csrrw x23, 0x980, x27	// Write all 1s to CSR
	csrrw x23, 0x980, x0	// Write all 0s to CSR
	csrrs x23, 0x980, x27	// Set all CSR bits
	csrrc x23, 0x980, x27	// Clear all CSR bits
	csrrw x23, 0x980, x1	// Restore CSR

// Testing CSR 0x981
	csrr x20, 0x981	// Read CSR
	li x14, -1
	csrrw x15, 0x981, x14	// Write all 1s to CSR
	csrrw x15, 0x981, x0	// Write all 0s to CSR
	csrrs x15, 0x981, x14	// Set all CSR bits
	csrrc x15, 0x981, x14	// Clear all CSR bits
	csrrw x15, 0x981, x20	// Restore CSR

// Testing CSR 0x982
	csrr x3, 0x982	// Read CSR
	li x31, -1
	csrrw x30, 0x982, x31	// Write all 1s to CSR
	csrrw x30, 0x982, x0	// Write all 0s to CSR
	csrrs x30, 0x982, x31	// Set all CSR bits
	csrrc x30, 0x982, x31	// Clear all CSR bits
	csrrw x30, 0x982, x3	// Restore CSR

// Testing CSR 0x983
	csrr x4, 0x983	// Read CSR
	li x19, -1
	csrrw x29, 0x983, x19	// Write all 1s to CSR
	csrrw x29, 0x983, x0	// Write all 0s to CSR
	csrrs x29, 0x983, x19	// Set all CSR bits
	csrrc x29, 0x983, x19	// Clear all CSR bits
	csrrw x29, 0x983, x4	// Restore CSR

// Testing CSR 0x984
	csrr x1, 0x984	// Read CSR
	li x29, -1
	csrrw x28, 0x984, x29	// Write all 1s to CSR
	csrrw x28, 0x984, x0	// Write all 0s to CSR
	csrrs x28, 0x984, x29	// Set all CSR bits
	csrrc x28, 0x984, x29	// Clear all CSR bits
	csrrw x28, 0x984, x1	// Restore CSR

// Testing CSR 0x985
	csrr x28, 0x985	// Read CSR
	li x19, -1
	csrrw x27, 0x985, x19	// Write all 1s to CSR
	csrrw x27, 0x985, x0	// Write all 0s to CSR
	csrrs x27, 0x985, x19	// Set all CSR bits
	csrrc x27, 0x985, x19	// Clear all CSR bits
	csrrw x27, 0x985, x28	// Restore CSR

// Testing CSR 0x986
	csrr x2, 0x986	// Read CSR
	li x15, -1
	csrrw x8, 0x986, x15	// Write all 1s to CSR
	csrrw x8, 0x986, x0	// Write all 0s to CSR
	csrrs x8, 0x986, x15	// Set all CSR bits
	csrrc x8, 0x986, x15	// Clear all CSR bits
	csrrw x8, 0x986, x2	// Restore CSR

// Testing CSR 0x987
	csrr x28, 0x987	// Read CSR
	li x9, -1
	csrrw x29, 0x987, x9	// Write all 1s to CSR
	csrrw x29, 0x987, x0	// Write all 0s to CSR
	csrrs x29, 0x987, x9	// Set all CSR bits
	csrrc x29, 0x987, x9	// Clear all CSR bits
	csrrw x29, 0x987, x28	// Restore CSR

// Testing CSR 0x988
	csrr x16, 0x988	// Read CSR
	li x8, -1
	csrrw x3, 0x988, x8	// Write all 1s to CSR
	csrrw x3, 0x988, x0	// Write all 0s to CSR
	csrrs x3, 0x988, x8	// Set all CSR bits
	csrrc x3, 0x988, x8	// Clear all CSR bits
	csrrw x3, 0x988, x16	// Restore CSR

// Testing CSR 0x989
	csrr x26, 0x989	// Read CSR
	li x31, -1
	csrrw x6, 0x989, x31	// Write all 1s to CSR
	csrrw x6, 0x989, x0	// Write all 0s to CSR
	csrrs x6, 0x989, x31	// Set all CSR bits
	csrrc x6, 0x989, x31	// Clear all CSR bits
	csrrw x6, 0x989, x26	// Restore CSR

// Testing CSR 0x98a
	csrr x4, 0x98a	// Read CSR
	li x14, -1
	csrrw x16, 0x98a, x14	// Write all 1s to CSR
	csrrw x16, 0x98a, x0	// Write all 0s to CSR
	csrrs x16, 0x98a, x14	// Set all CSR bits
	csrrc x16, 0x98a, x14	// Clear all CSR bits
	csrrw x16, 0x98a, x4	// Restore CSR

// Testing CSR 0x98b
	csrr x22, 0x98b	// Read CSR
	li x13, -1
	csrrw x16, 0x98b, x13	// Write all 1s to CSR
	csrrw x16, 0x98b, x0	// Write all 0s to CSR
	csrrs x16, 0x98b, x13	// Set all CSR bits
	csrrc x16, 0x98b, x13	// Clear all CSR bits
	csrrw x16, 0x98b, x22	// Restore CSR

// Testing CSR 0x98c
	csrr x23, 0x98c	// Read CSR
	li x28, -1
	csrrw x4, 0x98c, x28	// Write all 1s to CSR
	csrrw x4, 0x98c, x0	// Write all 0s to CSR
	csrrs x4, 0x98c, x28	// Set all CSR bits
	csrrc x4, 0x98c, x28	// Clear all CSR bits
	csrrw x4, 0x98c, x23	// Restore CSR

// Testing CSR 0x98d
	csrr x19, 0x98d	// Read CSR
	li x14, -1
	csrrw x10, 0x98d, x14	// Write all 1s to CSR
	csrrw x10, 0x98d, x0	// Write all 0s to CSR
	csrrs x10, 0x98d, x14	// Set all CSR bits
	csrrc x10, 0x98d, x14	// Clear all CSR bits
	csrrw x10, 0x98d, x19	// Restore CSR

// Testing CSR 0x98e
	csrr x9, 0x98e	// Read CSR
	li x2, -1
	csrrw x7, 0x98e, x2	// Write all 1s to CSR
	csrrw x7, 0x98e, x0	// Write all 0s to CSR
	csrrs x7, 0x98e, x2	// Set all CSR bits
	csrrc x7, 0x98e, x2	// Clear all CSR bits
	csrrw x7, 0x98e, x9	// Restore CSR

// Testing CSR 0x98f
	csrr x22, 0x98f	// Read CSR
	li x8, -1
	csrrw x10, 0x98f, x8	// Write all 1s to CSR
	csrrw x10, 0x98f, x0	// Write all 0s to CSR
	csrrs x10, 0x98f, x8	// Set all CSR bits
	csrrc x10, 0x98f, x8	// Clear all CSR bits
	csrrw x10, 0x98f, x22	// Restore CSR

// Testing CSR 0x990
	csrr x26, 0x990	// Read CSR
	li x19, -1
	csrrw x11, 0x990, x19	// Write all 1s to CSR
	csrrw x11, 0x990, x0	// Write all 0s to CSR
	csrrs x11, 0x990, x19	// Set all CSR bits
	csrrc x11, 0x990, x19	// Clear all CSR bits
	csrrw x11, 0x990, x26	// Restore CSR

// Testing CSR 0x991
	csrr x15, 0x991	// Read CSR
	li x9, -1
	csrrw x4, 0x991, x9	// Write all 1s to CSR
	csrrw x4, 0x991, x0	// Write all 0s to CSR
	csrrs x4, 0x991, x9	// Set all CSR bits
	csrrc x4, 0x991, x9	// Clear all CSR bits
	csrrw x4, 0x991, x15	// Restore CSR

// Testing CSR 0x992
	csrr x14, 0x992	// Read CSR
	li x27, -1
	csrrw x31, 0x992, x27	// Write all 1s to CSR
	csrrw x31, 0x992, x0	// Write all 0s to CSR
	csrrs x31, 0x992, x27	// Set all CSR bits
	csrrc x31, 0x992, x27	// Clear all CSR bits
	csrrw x31, 0x992, x14	// Restore CSR

// Testing CSR 0x993
	csrr x10, 0x993	// Read CSR
	li x3, -1
	csrrw x31, 0x993, x3	// Write all 1s to CSR
	csrrw x31, 0x993, x0	// Write all 0s to CSR
	csrrs x31, 0x993, x3	// Set all CSR bits
	csrrc x31, 0x993, x3	// Clear all CSR bits
	csrrw x31, 0x993, x10	// Restore CSR

// Testing CSR 0x994
	csrr x28, 0x994	// Read CSR
	li x9, -1
	csrrw x2, 0x994, x9	// Write all 1s to CSR
	csrrw x2, 0x994, x0	// Write all 0s to CSR
	csrrs x2, 0x994, x9	// Set all CSR bits
	csrrc x2, 0x994, x9	// Clear all CSR bits
	csrrw x2, 0x994, x28	// Restore CSR

// Testing CSR 0x995
	csrr x17, 0x995	// Read CSR
	li x8, -1
	csrrw x12, 0x995, x8	// Write all 1s to CSR
	csrrw x12, 0x995, x0	// Write all 0s to CSR
	csrrs x12, 0x995, x8	// Set all CSR bits
	csrrc x12, 0x995, x8	// Clear all CSR bits
	csrrw x12, 0x995, x17	// Restore CSR

// Testing CSR 0x996
	csrr x20, 0x996	// Read CSR
	li x1, -1
	csrrw x13, 0x996, x1	// Write all 1s to CSR
	csrrw x13, 0x996, x0	// Write all 0s to CSR
	csrrs x13, 0x996, x1	// Set all CSR bits
	csrrc x13, 0x996, x1	// Clear all CSR bits
	csrrw x13, 0x996, x20	// Restore CSR

// Testing CSR 0x997
	csrr x24, 0x997	// Read CSR
	li x25, -1
	csrrw x26, 0x997, x25	// Write all 1s to CSR
	csrrw x26, 0x997, x0	// Write all 0s to CSR
	csrrs x26, 0x997, x25	// Set all CSR bits
	csrrc x26, 0x997, x25	// Clear all CSR bits
	csrrw x26, 0x997, x24	// Restore CSR

// Testing CSR 0x998
	csrr x24, 0x998	// Read CSR
	li x15, -1
	csrrw x7, 0x998, x15	// Write all 1s to CSR
	csrrw x7, 0x998, x0	// Write all 0s to CSR
	csrrs x7, 0x998, x15	// Set all CSR bits
	csrrc x7, 0x998, x15	// Clear all CSR bits
	csrrw x7, 0x998, x24	// Restore CSR

// Testing CSR 0x999
	csrr x12, 0x999	// Read CSR
	li x9, -1
	csrrw x23, 0x999, x9	// Write all 1s to CSR
	csrrw x23, 0x999, x0	// Write all 0s to CSR
	csrrs x23, 0x999, x9	// Set all CSR bits
	csrrc x23, 0x999, x9	// Clear all CSR bits
	csrrw x23, 0x999, x12	// Restore CSR

// Testing CSR 0x99a
	csrr x9, 0x99a	// Read CSR
	li x1, -1
	csrrw x31, 0x99a, x1	// Write all 1s to CSR
	csrrw x31, 0x99a, x0	// Write all 0s to CSR
	csrrs x31, 0x99a, x1	// Set all CSR bits
	csrrc x31, 0x99a, x1	// Clear all CSR bits
	csrrw x31, 0x99a, x9	// Restore CSR

// Testing CSR 0x99b
	csrr x3, 0x99b	// Read CSR
	li x23, -1
	csrrw x26, 0x99b, x23	// Write all 1s to CSR
	csrrw x26, 0x99b, x0	// Write all 0s to CSR
	csrrs x26, 0x99b, x23	// Set all CSR bits
	csrrc x26, 0x99b, x23	// Clear all CSR bits
	csrrw x26, 0x99b, x3	// Restore CSR

// Testing CSR 0x99c
	csrr x12, 0x99c	// Read CSR
	li x2, -1
	csrrw x16, 0x99c, x2	// Write all 1s to CSR
	csrrw x16, 0x99c, x0	// Write all 0s to CSR
	csrrs x16, 0x99c, x2	// Set all CSR bits
	csrrc x16, 0x99c, x2	// Clear all CSR bits
	csrrw x16, 0x99c, x12	// Restore CSR

// Testing CSR 0x99d
	csrr x13, 0x99d	// Read CSR
	li x31, -1
	csrrw x25, 0x99d, x31	// Write all 1s to CSR
	csrrw x25, 0x99d, x0	// Write all 0s to CSR
	csrrs x25, 0x99d, x31	// Set all CSR bits
	csrrc x25, 0x99d, x31	// Clear all CSR bits
	csrrw x25, 0x99d, x13	// Restore CSR

// Testing CSR 0x99e
	csrr x1, 0x99e	// Read CSR
	li x3, -1
	csrrw x23, 0x99e, x3	// Write all 1s to CSR
	csrrw x23, 0x99e, x0	// Write all 0s to CSR
	csrrs x23, 0x99e, x3	// Set all CSR bits
	csrrc x23, 0x99e, x3	// Clear all CSR bits
	csrrw x23, 0x99e, x1	// Restore CSR

// Testing CSR 0x99f
	csrr x27, 0x99f	// Read CSR
	li x4, -1
	csrrw x11, 0x99f, x4	// Write all 1s to CSR
	csrrw x11, 0x99f, x0	// Write all 0s to CSR
	csrrs x11, 0x99f, x4	// Set all CSR bits
	csrrc x11, 0x99f, x4	// Clear all CSR bits
	csrrw x11, 0x99f, x27	// Restore CSR

// Testing CSR 0x9a0
	csrr x22, 0x9a0	// Read CSR
	li x25, -1
	csrrw x17, 0x9a0, x25	// Write all 1s to CSR
	csrrw x17, 0x9a0, x0	// Write all 0s to CSR
	csrrs x17, 0x9a0, x25	// Set all CSR bits
	csrrc x17, 0x9a0, x25	// Clear all CSR bits
	csrrw x17, 0x9a0, x22	// Restore CSR

// Testing CSR 0x9a1
	csrr x1, 0x9a1	// Read CSR
	li x22, -1
	csrrw x2, 0x9a1, x22	// Write all 1s to CSR
	csrrw x2, 0x9a1, x0	// Write all 0s to CSR
	csrrs x2, 0x9a1, x22	// Set all CSR bits
	csrrc x2, 0x9a1, x22	// Clear all CSR bits
	csrrw x2, 0x9a1, x1	// Restore CSR

// Testing CSR 0x9a2
	csrr x13, 0x9a2	// Read CSR
	li x27, -1
	csrrw x29, 0x9a2, x27	// Write all 1s to CSR
	csrrw x29, 0x9a2, x0	// Write all 0s to CSR
	csrrs x29, 0x9a2, x27	// Set all CSR bits
	csrrc x29, 0x9a2, x27	// Clear all CSR bits
	csrrw x29, 0x9a2, x13	// Restore CSR

// Testing CSR 0x9a3
	csrr x12, 0x9a3	// Read CSR
	li x9, -1
	csrrw x21, 0x9a3, x9	// Write all 1s to CSR
	csrrw x21, 0x9a3, x0	// Write all 0s to CSR
	csrrs x21, 0x9a3, x9	// Set all CSR bits
	csrrc x21, 0x9a3, x9	// Clear all CSR bits
	csrrw x21, 0x9a3, x12	// Restore CSR

// Testing CSR 0x9a4
	csrr x8, 0x9a4	// Read CSR
	li x1, -1
	csrrw x16, 0x9a4, x1	// Write all 1s to CSR
	csrrw x16, 0x9a4, x0	// Write all 0s to CSR
	csrrs x16, 0x9a4, x1	// Set all CSR bits
	csrrc x16, 0x9a4, x1	// Clear all CSR bits
	csrrw x16, 0x9a4, x8	// Restore CSR

// Testing CSR 0x9a5
	csrr x26, 0x9a5	// Read CSR
	li x11, -1
	csrrw x1, 0x9a5, x11	// Write all 1s to CSR
	csrrw x1, 0x9a5, x0	// Write all 0s to CSR
	csrrs x1, 0x9a5, x11	// Set all CSR bits
	csrrc x1, 0x9a5, x11	// Clear all CSR bits
	csrrw x1, 0x9a5, x26	// Restore CSR

// Testing CSR 0x9a6
	csrr x29, 0x9a6	// Read CSR
	li x30, -1
	csrrw x12, 0x9a6, x30	// Write all 1s to CSR
	csrrw x12, 0x9a6, x0	// Write all 0s to CSR
	csrrs x12, 0x9a6, x30	// Set all CSR bits
	csrrc x12, 0x9a6, x30	// Clear all CSR bits
	csrrw x12, 0x9a6, x29	// Restore CSR

// Testing CSR 0x9a7
	csrr x31, 0x9a7	// Read CSR
	li x23, -1
	csrrw x19, 0x9a7, x23	// Write all 1s to CSR
	csrrw x19, 0x9a7, x0	// Write all 0s to CSR
	csrrs x19, 0x9a7, x23	// Set all CSR bits
	csrrc x19, 0x9a7, x23	// Clear all CSR bits
	csrrw x19, 0x9a7, x31	// Restore CSR

// Testing CSR 0x9a8
	csrr x27, 0x9a8	// Read CSR
	li x2, -1
	csrrw x3, 0x9a8, x2	// Write all 1s to CSR
	csrrw x3, 0x9a8, x0	// Write all 0s to CSR
	csrrs x3, 0x9a8, x2	// Set all CSR bits
	csrrc x3, 0x9a8, x2	// Clear all CSR bits
	csrrw x3, 0x9a8, x27	// Restore CSR

// Testing CSR 0x9a9
	csrr x14, 0x9a9	// Read CSR
	li x29, -1
	csrrw x4, 0x9a9, x29	// Write all 1s to CSR
	csrrw x4, 0x9a9, x0	// Write all 0s to CSR
	csrrs x4, 0x9a9, x29	// Set all CSR bits
	csrrc x4, 0x9a9, x29	// Clear all CSR bits
	csrrw x4, 0x9a9, x14	// Restore CSR

// Testing CSR 0x9aa
	csrr x30, 0x9aa	// Read CSR
	li x21, -1
	csrrw x28, 0x9aa, x21	// Write all 1s to CSR
	csrrw x28, 0x9aa, x0	// Write all 0s to CSR
	csrrs x28, 0x9aa, x21	// Set all CSR bits
	csrrc x28, 0x9aa, x21	// Clear all CSR bits
	csrrw x28, 0x9aa, x30	// Restore CSR

// Testing CSR 0x9ab
	csrr x17, 0x9ab	// Read CSR
	li x6, -1
	csrrw x19, 0x9ab, x6	// Write all 1s to CSR
	csrrw x19, 0x9ab, x0	// Write all 0s to CSR
	csrrs x19, 0x9ab, x6	// Set all CSR bits
	csrrc x19, 0x9ab, x6	// Clear all CSR bits
	csrrw x19, 0x9ab, x17	// Restore CSR

// Testing CSR 0x9ac
	csrr x29, 0x9ac	// Read CSR
	li x20, -1
	csrrw x28, 0x9ac, x20	// Write all 1s to CSR
	csrrw x28, 0x9ac, x0	// Write all 0s to CSR
	csrrs x28, 0x9ac, x20	// Set all CSR bits
	csrrc x28, 0x9ac, x20	// Clear all CSR bits
	csrrw x28, 0x9ac, x29	// Restore CSR

// Testing CSR 0x9ad
	csrr x6, 0x9ad	// Read CSR
	li x15, -1
	csrrw x7, 0x9ad, x15	// Write all 1s to CSR
	csrrw x7, 0x9ad, x0	// Write all 0s to CSR
	csrrs x7, 0x9ad, x15	// Set all CSR bits
	csrrc x7, 0x9ad, x15	// Clear all CSR bits
	csrrw x7, 0x9ad, x6	// Restore CSR

// Testing CSR 0x9ae
	csrr x17, 0x9ae	// Read CSR
	li x23, -1
	csrrw x3, 0x9ae, x23	// Write all 1s to CSR
	csrrw x3, 0x9ae, x0	// Write all 0s to CSR
	csrrs x3, 0x9ae, x23	// Set all CSR bits
	csrrc x3, 0x9ae, x23	// Clear all CSR bits
	csrrw x3, 0x9ae, x17	// Restore CSR

// Testing CSR 0x9af
	csrr x16, 0x9af	// Read CSR
	li x29, -1
	csrrw x19, 0x9af, x29	// Write all 1s to CSR
	csrrw x19, 0x9af, x0	// Write all 0s to CSR
	csrrs x19, 0x9af, x29	// Set all CSR bits
	csrrc x19, 0x9af, x29	// Clear all CSR bits
	csrrw x19, 0x9af, x16	// Restore CSR

// Testing CSR 0x9b0
	csrr x12, 0x9b0	// Read CSR
	li x30, -1
	csrrw x9, 0x9b0, x30	// Write all 1s to CSR
	csrrw x9, 0x9b0, x0	// Write all 0s to CSR
	csrrs x9, 0x9b0, x30	// Set all CSR bits
	csrrc x9, 0x9b0, x30	// Clear all CSR bits
	csrrw x9, 0x9b0, x12	// Restore CSR

// Testing CSR 0x9b1
	csrr x14, 0x9b1	// Read CSR
	li x18, -1
	csrrw x19, 0x9b1, x18	// Write all 1s to CSR
	csrrw x19, 0x9b1, x0	// Write all 0s to CSR
	csrrs x19, 0x9b1, x18	// Set all CSR bits
	csrrc x19, 0x9b1, x18	// Clear all CSR bits
	csrrw x19, 0x9b1, x14	// Restore CSR

// Testing CSR 0x9b2
	csrr x22, 0x9b2	// Read CSR
	li x16, -1
	csrrw x11, 0x9b2, x16	// Write all 1s to CSR
	csrrw x11, 0x9b2, x0	// Write all 0s to CSR
	csrrs x11, 0x9b2, x16	// Set all CSR bits
	csrrc x11, 0x9b2, x16	// Clear all CSR bits
	csrrw x11, 0x9b2, x22	// Restore CSR

// Testing CSR 0x9b3
	csrr x18, 0x9b3	// Read CSR
	li x16, -1
	csrrw x11, 0x9b3, x16	// Write all 1s to CSR
	csrrw x11, 0x9b3, x0	// Write all 0s to CSR
	csrrs x11, 0x9b3, x16	// Set all CSR bits
	csrrc x11, 0x9b3, x16	// Clear all CSR bits
	csrrw x11, 0x9b3, x18	// Restore CSR

// Testing CSR 0x9b4
	csrr x25, 0x9b4	// Read CSR
	li x16, -1
	csrrw x30, 0x9b4, x16	// Write all 1s to CSR
	csrrw x30, 0x9b4, x0	// Write all 0s to CSR
	csrrs x30, 0x9b4, x16	// Set all CSR bits
	csrrc x30, 0x9b4, x16	// Clear all CSR bits
	csrrw x30, 0x9b4, x25	// Restore CSR

// Testing CSR 0x9b5
	csrr x18, 0x9b5	// Read CSR
	li x21, -1
	csrrw x2, 0x9b5, x21	// Write all 1s to CSR
	csrrw x2, 0x9b5, x0	// Write all 0s to CSR
	csrrs x2, 0x9b5, x21	// Set all CSR bits
	csrrc x2, 0x9b5, x21	// Clear all CSR bits
	csrrw x2, 0x9b5, x18	// Restore CSR

// Testing CSR 0x9b6
	csrr x12, 0x9b6	// Read CSR
	li x25, -1
	csrrw x21, 0x9b6, x25	// Write all 1s to CSR
	csrrw x21, 0x9b6, x0	// Write all 0s to CSR
	csrrs x21, 0x9b6, x25	// Set all CSR bits
	csrrc x21, 0x9b6, x25	// Clear all CSR bits
	csrrw x21, 0x9b6, x12	// Restore CSR

// Testing CSR 0x9b7
	csrr x31, 0x9b7	// Read CSR
	li x17, -1
	csrrw x12, 0x9b7, x17	// Write all 1s to CSR
	csrrw x12, 0x9b7, x0	// Write all 0s to CSR
	csrrs x12, 0x9b7, x17	// Set all CSR bits
	csrrc x12, 0x9b7, x17	// Clear all CSR bits
	csrrw x12, 0x9b7, x31	// Restore CSR

// Testing CSR 0x9b8
	csrr x15, 0x9b8	// Read CSR
	li x4, -1
	csrrw x18, 0x9b8, x4	// Write all 1s to CSR
	csrrw x18, 0x9b8, x0	// Write all 0s to CSR
	csrrs x18, 0x9b8, x4	// Set all CSR bits
	csrrc x18, 0x9b8, x4	// Clear all CSR bits
	csrrw x18, 0x9b8, x15	// Restore CSR

// Testing CSR 0x9b9
	csrr x22, 0x9b9	// Read CSR
	li x24, -1
	csrrw x5, 0x9b9, x24	// Write all 1s to CSR
	csrrw x5, 0x9b9, x0	// Write all 0s to CSR
	csrrs x5, 0x9b9, x24	// Set all CSR bits
	csrrc x5, 0x9b9, x24	// Clear all CSR bits
	csrrw x5, 0x9b9, x22	// Restore CSR

// Testing CSR 0x9ba
	csrr x20, 0x9ba	// Read CSR
	li x13, -1
	csrrw x31, 0x9ba, x13	// Write all 1s to CSR
	csrrw x31, 0x9ba, x0	// Write all 0s to CSR
	csrrs x31, 0x9ba, x13	// Set all CSR bits
	csrrc x31, 0x9ba, x13	// Clear all CSR bits
	csrrw x31, 0x9ba, x20	// Restore CSR

// Testing CSR 0x9bb
	csrr x17, 0x9bb	// Read CSR
	li x4, -1
	csrrw x8, 0x9bb, x4	// Write all 1s to CSR
	csrrw x8, 0x9bb, x0	// Write all 0s to CSR
	csrrs x8, 0x9bb, x4	// Set all CSR bits
	csrrc x8, 0x9bb, x4	// Clear all CSR bits
	csrrw x8, 0x9bb, x17	// Restore CSR

// Testing CSR 0x9bc
	csrr x17, 0x9bc	// Read CSR
	li x13, -1
	csrrw x9, 0x9bc, x13	// Write all 1s to CSR
	csrrw x9, 0x9bc, x0	// Write all 0s to CSR
	csrrs x9, 0x9bc, x13	// Set all CSR bits
	csrrc x9, 0x9bc, x13	// Clear all CSR bits
	csrrw x9, 0x9bc, x17	// Restore CSR

// Testing CSR 0x9bd
	csrr x19, 0x9bd	// Read CSR
	li x15, -1
	csrrw x20, 0x9bd, x15	// Write all 1s to CSR
	csrrw x20, 0x9bd, x0	// Write all 0s to CSR
	csrrs x20, 0x9bd, x15	// Set all CSR bits
	csrrc x20, 0x9bd, x15	// Clear all CSR bits
	csrrw x20, 0x9bd, x19	// Restore CSR

// Testing CSR 0x9be
	csrr x21, 0x9be	// Read CSR
	li x11, -1
	csrrw x4, 0x9be, x11	// Write all 1s to CSR
	csrrw x4, 0x9be, x0	// Write all 0s to CSR
	csrrs x4, 0x9be, x11	// Set all CSR bits
	csrrc x4, 0x9be, x11	// Clear all CSR bits
	csrrw x4, 0x9be, x21	// Restore CSR

// Testing CSR 0x9bf
	csrr x24, 0x9bf	// Read CSR
	li x26, -1
	csrrw x7, 0x9bf, x26	// Write all 1s to CSR
	csrrw x7, 0x9bf, x0	// Write all 0s to CSR
	csrrs x7, 0x9bf, x26	// Set all CSR bits
	csrrc x7, 0x9bf, x26	// Clear all CSR bits
	csrrw x7, 0x9bf, x24	// Restore CSR

// Testing CSR 0x9c0
	csrr x16, 0x9c0	// Read CSR
	li x4, -1
	csrrw x29, 0x9c0, x4	// Write all 1s to CSR
	csrrw x29, 0x9c0, x0	// Write all 0s to CSR
	csrrs x29, 0x9c0, x4	// Set all CSR bits
	csrrc x29, 0x9c0, x4	// Clear all CSR bits
	csrrw x29, 0x9c0, x16	// Restore CSR

// Testing CSR 0x9c1
	csrr x17, 0x9c1	// Read CSR
	li x9, -1
	csrrw x7, 0x9c1, x9	// Write all 1s to CSR
	csrrw x7, 0x9c1, x0	// Write all 0s to CSR
	csrrs x7, 0x9c1, x9	// Set all CSR bits
	csrrc x7, 0x9c1, x9	// Clear all CSR bits
	csrrw x7, 0x9c1, x17	// Restore CSR

// Testing CSR 0x9c2
	csrr x10, 0x9c2	// Read CSR
	li x6, -1
	csrrw x31, 0x9c2, x6	// Write all 1s to CSR
	csrrw x31, 0x9c2, x0	// Write all 0s to CSR
	csrrs x31, 0x9c2, x6	// Set all CSR bits
	csrrc x31, 0x9c2, x6	// Clear all CSR bits
	csrrw x31, 0x9c2, x10	// Restore CSR

// Testing CSR 0x9c3
	csrr x4, 0x9c3	// Read CSR
	li x28, -1
	csrrw x19, 0x9c3, x28	// Write all 1s to CSR
	csrrw x19, 0x9c3, x0	// Write all 0s to CSR
	csrrs x19, 0x9c3, x28	// Set all CSR bits
	csrrc x19, 0x9c3, x28	// Clear all CSR bits
	csrrw x19, 0x9c3, x4	// Restore CSR

// Testing CSR 0x9c4
	csrr x2, 0x9c4	// Read CSR
	li x3, -1
	csrrw x11, 0x9c4, x3	// Write all 1s to CSR
	csrrw x11, 0x9c4, x0	// Write all 0s to CSR
	csrrs x11, 0x9c4, x3	// Set all CSR bits
	csrrc x11, 0x9c4, x3	// Clear all CSR bits
	csrrw x11, 0x9c4, x2	// Restore CSR

// Testing CSR 0x9c5
	csrr x17, 0x9c5	// Read CSR
	li x19, -1
	csrrw x8, 0x9c5, x19	// Write all 1s to CSR
	csrrw x8, 0x9c5, x0	// Write all 0s to CSR
	csrrs x8, 0x9c5, x19	// Set all CSR bits
	csrrc x8, 0x9c5, x19	// Clear all CSR bits
	csrrw x8, 0x9c5, x17	// Restore CSR

// Testing CSR 0x9c6
	csrr x4, 0x9c6	// Read CSR
	li x22, -1
	csrrw x11, 0x9c6, x22	// Write all 1s to CSR
	csrrw x11, 0x9c6, x0	// Write all 0s to CSR
	csrrs x11, 0x9c6, x22	// Set all CSR bits
	csrrc x11, 0x9c6, x22	// Clear all CSR bits
	csrrw x11, 0x9c6, x4	// Restore CSR

// Testing CSR 0x9c7
	csrr x13, 0x9c7	// Read CSR
	li x24, -1
	csrrw x22, 0x9c7, x24	// Write all 1s to CSR
	csrrw x22, 0x9c7, x0	// Write all 0s to CSR
	csrrs x22, 0x9c7, x24	// Set all CSR bits
	csrrc x22, 0x9c7, x24	// Clear all CSR bits
	csrrw x22, 0x9c7, x13	// Restore CSR

// Testing CSR 0x9c8
	csrr x8, 0x9c8	// Read CSR
	li x19, -1
	csrrw x13, 0x9c8, x19	// Write all 1s to CSR
	csrrw x13, 0x9c8, x0	// Write all 0s to CSR
	csrrs x13, 0x9c8, x19	// Set all CSR bits
	csrrc x13, 0x9c8, x19	// Clear all CSR bits
	csrrw x13, 0x9c8, x8	// Restore CSR

// Testing CSR 0x9c9
	csrr x2, 0x9c9	// Read CSR
	li x1, -1
	csrrw x30, 0x9c9, x1	// Write all 1s to CSR
	csrrw x30, 0x9c9, x0	// Write all 0s to CSR
	csrrs x30, 0x9c9, x1	// Set all CSR bits
	csrrc x30, 0x9c9, x1	// Clear all CSR bits
	csrrw x30, 0x9c9, x2	// Restore CSR

// Testing CSR 0x9ca
	csrr x1, 0x9ca	// Read CSR
	li x12, -1
	csrrw x30, 0x9ca, x12	// Write all 1s to CSR
	csrrw x30, 0x9ca, x0	// Write all 0s to CSR
	csrrs x30, 0x9ca, x12	// Set all CSR bits
	csrrc x30, 0x9ca, x12	// Clear all CSR bits
	csrrw x30, 0x9ca, x1	// Restore CSR

// Testing CSR 0x9cb
	csrr x1, 0x9cb	// Read CSR
	li x13, -1
	csrrw x30, 0x9cb, x13	// Write all 1s to CSR
	csrrw x30, 0x9cb, x0	// Write all 0s to CSR
	csrrs x30, 0x9cb, x13	// Set all CSR bits
	csrrc x30, 0x9cb, x13	// Clear all CSR bits
	csrrw x30, 0x9cb, x1	// Restore CSR

// Testing CSR 0x9cc
	csrr x18, 0x9cc	// Read CSR
	li x30, -1
	csrrw x26, 0x9cc, x30	// Write all 1s to CSR
	csrrw x26, 0x9cc, x0	// Write all 0s to CSR
	csrrs x26, 0x9cc, x30	// Set all CSR bits
	csrrc x26, 0x9cc, x30	// Clear all CSR bits
	csrrw x26, 0x9cc, x18	// Restore CSR

// Testing CSR 0x9cd
	csrr x2, 0x9cd	// Read CSR
	li x14, -1
	csrrw x31, 0x9cd, x14	// Write all 1s to CSR
	csrrw x31, 0x9cd, x0	// Write all 0s to CSR
	csrrs x31, 0x9cd, x14	// Set all CSR bits
	csrrc x31, 0x9cd, x14	// Clear all CSR bits
	csrrw x31, 0x9cd, x2	// Restore CSR

// Testing CSR 0x9ce
	csrr x6, 0x9ce	// Read CSR
	li x8, -1
	csrrw x24, 0x9ce, x8	// Write all 1s to CSR
	csrrw x24, 0x9ce, x0	// Write all 0s to CSR
	csrrs x24, 0x9ce, x8	// Set all CSR bits
	csrrc x24, 0x9ce, x8	// Clear all CSR bits
	csrrw x24, 0x9ce, x6	// Restore CSR

// Testing CSR 0x9cf
	csrr x1, 0x9cf	// Read CSR
	li x26, -1
	csrrw x7, 0x9cf, x26	// Write all 1s to CSR
	csrrw x7, 0x9cf, x0	// Write all 0s to CSR
	csrrs x7, 0x9cf, x26	// Set all CSR bits
	csrrc x7, 0x9cf, x26	// Clear all CSR bits
	csrrw x7, 0x9cf, x1	// Restore CSR

// Testing CSR 0x9d0
	csrr x4, 0x9d0	// Read CSR
	li x11, -1
	csrrw x2, 0x9d0, x11	// Write all 1s to CSR
	csrrw x2, 0x9d0, x0	// Write all 0s to CSR
	csrrs x2, 0x9d0, x11	// Set all CSR bits
	csrrc x2, 0x9d0, x11	// Clear all CSR bits
	csrrw x2, 0x9d0, x4	// Restore CSR

// Testing CSR 0x9d1
	csrr x11, 0x9d1	// Read CSR
	li x19, -1
	csrrw x10, 0x9d1, x19	// Write all 1s to CSR
	csrrw x10, 0x9d1, x0	// Write all 0s to CSR
	csrrs x10, 0x9d1, x19	// Set all CSR bits
	csrrc x10, 0x9d1, x19	// Clear all CSR bits
	csrrw x10, 0x9d1, x11	// Restore CSR

// Testing CSR 0x9d2
	csrr x31, 0x9d2	// Read CSR
	li x3, -1
	csrrw x13, 0x9d2, x3	// Write all 1s to CSR
	csrrw x13, 0x9d2, x0	// Write all 0s to CSR
	csrrs x13, 0x9d2, x3	// Set all CSR bits
	csrrc x13, 0x9d2, x3	// Clear all CSR bits
	csrrw x13, 0x9d2, x31	// Restore CSR

// Testing CSR 0x9d3
	csrr x6, 0x9d3	// Read CSR
	li x1, -1
	csrrw x21, 0x9d3, x1	// Write all 1s to CSR
	csrrw x21, 0x9d3, x0	// Write all 0s to CSR
	csrrs x21, 0x9d3, x1	// Set all CSR bits
	csrrc x21, 0x9d3, x1	// Clear all CSR bits
	csrrw x21, 0x9d3, x6	// Restore CSR

// Testing CSR 0x9d4
	csrr x23, 0x9d4	// Read CSR
	li x8, -1
	csrrw x14, 0x9d4, x8	// Write all 1s to CSR
	csrrw x14, 0x9d4, x0	// Write all 0s to CSR
	csrrs x14, 0x9d4, x8	// Set all CSR bits
	csrrc x14, 0x9d4, x8	// Clear all CSR bits
	csrrw x14, 0x9d4, x23	// Restore CSR

// Testing CSR 0x9d5
	csrr x11, 0x9d5	// Read CSR
	li x31, -1
	csrrw x25, 0x9d5, x31	// Write all 1s to CSR
	csrrw x25, 0x9d5, x0	// Write all 0s to CSR
	csrrs x25, 0x9d5, x31	// Set all CSR bits
	csrrc x25, 0x9d5, x31	// Clear all CSR bits
	csrrw x25, 0x9d5, x11	// Restore CSR

// Testing CSR 0x9d6
	csrr x10, 0x9d6	// Read CSR
	li x28, -1
	csrrw x27, 0x9d6, x28	// Write all 1s to CSR
	csrrw x27, 0x9d6, x0	// Write all 0s to CSR
	csrrs x27, 0x9d6, x28	// Set all CSR bits
	csrrc x27, 0x9d6, x28	// Clear all CSR bits
	csrrw x27, 0x9d6, x10	// Restore CSR

// Testing CSR 0x9d7
	csrr x7, 0x9d7	// Read CSR
	li x20, -1
	csrrw x13, 0x9d7, x20	// Write all 1s to CSR
	csrrw x13, 0x9d7, x0	// Write all 0s to CSR
	csrrs x13, 0x9d7, x20	// Set all CSR bits
	csrrc x13, 0x9d7, x20	// Clear all CSR bits
	csrrw x13, 0x9d7, x7	// Restore CSR

// Testing CSR 0x9d8
	csrr x4, 0x9d8	// Read CSR
	li x26, -1
	csrrw x19, 0x9d8, x26	// Write all 1s to CSR
	csrrw x19, 0x9d8, x0	// Write all 0s to CSR
	csrrs x19, 0x9d8, x26	// Set all CSR bits
	csrrc x19, 0x9d8, x26	// Clear all CSR bits
	csrrw x19, 0x9d8, x4	// Restore CSR

// Testing CSR 0x9d9
	csrr x20, 0x9d9	// Read CSR
	li x26, -1
	csrrw x22, 0x9d9, x26	// Write all 1s to CSR
	csrrw x22, 0x9d9, x0	// Write all 0s to CSR
	csrrs x22, 0x9d9, x26	// Set all CSR bits
	csrrc x22, 0x9d9, x26	// Clear all CSR bits
	csrrw x22, 0x9d9, x20	// Restore CSR

// Testing CSR 0x9da
	csrr x19, 0x9da	// Read CSR
	li x25, -1
	csrrw x18, 0x9da, x25	// Write all 1s to CSR
	csrrw x18, 0x9da, x0	// Write all 0s to CSR
	csrrs x18, 0x9da, x25	// Set all CSR bits
	csrrc x18, 0x9da, x25	// Clear all CSR bits
	csrrw x18, 0x9da, x19	// Restore CSR

// Testing CSR 0x9db
	csrr x10, 0x9db	// Read CSR
	li x27, -1
	csrrw x16, 0x9db, x27	// Write all 1s to CSR
	csrrw x16, 0x9db, x0	// Write all 0s to CSR
	csrrs x16, 0x9db, x27	// Set all CSR bits
	csrrc x16, 0x9db, x27	// Clear all CSR bits
	csrrw x16, 0x9db, x10	// Restore CSR

// Testing CSR 0x9dc
	csrr x28, 0x9dc	// Read CSR
	li x3, -1
	csrrw x16, 0x9dc, x3	// Write all 1s to CSR
	csrrw x16, 0x9dc, x0	// Write all 0s to CSR
	csrrs x16, 0x9dc, x3	// Set all CSR bits
	csrrc x16, 0x9dc, x3	// Clear all CSR bits
	csrrw x16, 0x9dc, x28	// Restore CSR

// Testing CSR 0x9dd
	csrr x11, 0x9dd	// Read CSR
	li x27, -1
	csrrw x8, 0x9dd, x27	// Write all 1s to CSR
	csrrw x8, 0x9dd, x0	// Write all 0s to CSR
	csrrs x8, 0x9dd, x27	// Set all CSR bits
	csrrc x8, 0x9dd, x27	// Clear all CSR bits
	csrrw x8, 0x9dd, x11	// Restore CSR

// Testing CSR 0x9de
	csrr x19, 0x9de	// Read CSR
	li x12, -1
	csrrw x26, 0x9de, x12	// Write all 1s to CSR
	csrrw x26, 0x9de, x0	// Write all 0s to CSR
	csrrs x26, 0x9de, x12	// Set all CSR bits
	csrrc x26, 0x9de, x12	// Clear all CSR bits
	csrrw x26, 0x9de, x19	// Restore CSR

// Testing CSR 0x9df
	csrr x8, 0x9df	// Read CSR
	li x1, -1
	csrrw x3, 0x9df, x1	// Write all 1s to CSR
	csrrw x3, 0x9df, x0	// Write all 0s to CSR
	csrrs x3, 0x9df, x1	// Set all CSR bits
	csrrc x3, 0x9df, x1	// Clear all CSR bits
	csrrw x3, 0x9df, x8	// Restore CSR

// Testing CSR 0x9e0
	csrr x1, 0x9e0	// Read CSR
	li x27, -1
	csrrw x6, 0x9e0, x27	// Write all 1s to CSR
	csrrw x6, 0x9e0, x0	// Write all 0s to CSR
	csrrs x6, 0x9e0, x27	// Set all CSR bits
	csrrc x6, 0x9e0, x27	// Clear all CSR bits
	csrrw x6, 0x9e0, x1	// Restore CSR

// Testing CSR 0x9e1
	csrr x11, 0x9e1	// Read CSR
	li x20, -1
	csrrw x18, 0x9e1, x20	// Write all 1s to CSR
	csrrw x18, 0x9e1, x0	// Write all 0s to CSR
	csrrs x18, 0x9e1, x20	// Set all CSR bits
	csrrc x18, 0x9e1, x20	// Clear all CSR bits
	csrrw x18, 0x9e1, x11	// Restore CSR

// Testing CSR 0x9e2
	csrr x5, 0x9e2	// Read CSR
	li x17, -1
	csrrw x6, 0x9e2, x17	// Write all 1s to CSR
	csrrw x6, 0x9e2, x0	// Write all 0s to CSR
	csrrs x6, 0x9e2, x17	// Set all CSR bits
	csrrc x6, 0x9e2, x17	// Clear all CSR bits
	csrrw x6, 0x9e2, x5	// Restore CSR

// Testing CSR 0x9e3
	csrr x25, 0x9e3	// Read CSR
	li x29, -1
	csrrw x16, 0x9e3, x29	// Write all 1s to CSR
	csrrw x16, 0x9e3, x0	// Write all 0s to CSR
	csrrs x16, 0x9e3, x29	// Set all CSR bits
	csrrc x16, 0x9e3, x29	// Clear all CSR bits
	csrrw x16, 0x9e3, x25	// Restore CSR

// Testing CSR 0x9e4
	csrr x6, 0x9e4	// Read CSR
	li x12, -1
	csrrw x29, 0x9e4, x12	// Write all 1s to CSR
	csrrw x29, 0x9e4, x0	// Write all 0s to CSR
	csrrs x29, 0x9e4, x12	// Set all CSR bits
	csrrc x29, 0x9e4, x12	// Clear all CSR bits
	csrrw x29, 0x9e4, x6	// Restore CSR

// Testing CSR 0x9e5
	csrr x25, 0x9e5	// Read CSR
	li x17, -1
	csrrw x8, 0x9e5, x17	// Write all 1s to CSR
	csrrw x8, 0x9e5, x0	// Write all 0s to CSR
	csrrs x8, 0x9e5, x17	// Set all CSR bits
	csrrc x8, 0x9e5, x17	// Clear all CSR bits
	csrrw x8, 0x9e5, x25	// Restore CSR

// Testing CSR 0x9e6
	csrr x12, 0x9e6	// Read CSR
	li x5, -1
	csrrw x23, 0x9e6, x5	// Write all 1s to CSR
	csrrw x23, 0x9e6, x0	// Write all 0s to CSR
	csrrs x23, 0x9e6, x5	// Set all CSR bits
	csrrc x23, 0x9e6, x5	// Clear all CSR bits
	csrrw x23, 0x9e6, x12	// Restore CSR

// Testing CSR 0x9e7
	csrr x23, 0x9e7	// Read CSR
	li x20, -1
	csrrw x21, 0x9e7, x20	// Write all 1s to CSR
	csrrw x21, 0x9e7, x0	// Write all 0s to CSR
	csrrs x21, 0x9e7, x20	// Set all CSR bits
	csrrc x21, 0x9e7, x20	// Clear all CSR bits
	csrrw x21, 0x9e7, x23	// Restore CSR

// Testing CSR 0x9e8
	csrr x3, 0x9e8	// Read CSR
	li x12, -1
	csrrw x26, 0x9e8, x12	// Write all 1s to CSR
	csrrw x26, 0x9e8, x0	// Write all 0s to CSR
	csrrs x26, 0x9e8, x12	// Set all CSR bits
	csrrc x26, 0x9e8, x12	// Clear all CSR bits
	csrrw x26, 0x9e8, x3	// Restore CSR

// Testing CSR 0x9e9
	csrr x31, 0x9e9	// Read CSR
	li x19, -1
	csrrw x11, 0x9e9, x19	// Write all 1s to CSR
	csrrw x11, 0x9e9, x0	// Write all 0s to CSR
	csrrs x11, 0x9e9, x19	// Set all CSR bits
	csrrc x11, 0x9e9, x19	// Clear all CSR bits
	csrrw x11, 0x9e9, x31	// Restore CSR

// Testing CSR 0x9ea
	csrr x26, 0x9ea	// Read CSR
	li x5, -1
	csrrw x17, 0x9ea, x5	// Write all 1s to CSR
	csrrw x17, 0x9ea, x0	// Write all 0s to CSR
	csrrs x17, 0x9ea, x5	// Set all CSR bits
	csrrc x17, 0x9ea, x5	// Clear all CSR bits
	csrrw x17, 0x9ea, x26	// Restore CSR

// Testing CSR 0x9eb
	csrr x9, 0x9eb	// Read CSR
	li x7, -1
	csrrw x23, 0x9eb, x7	// Write all 1s to CSR
	csrrw x23, 0x9eb, x0	// Write all 0s to CSR
	csrrs x23, 0x9eb, x7	// Set all CSR bits
	csrrc x23, 0x9eb, x7	// Clear all CSR bits
	csrrw x23, 0x9eb, x9	// Restore CSR

// Testing CSR 0x9ec
	csrr x11, 0x9ec	// Read CSR
	li x13, -1
	csrrw x7, 0x9ec, x13	// Write all 1s to CSR
	csrrw x7, 0x9ec, x0	// Write all 0s to CSR
	csrrs x7, 0x9ec, x13	// Set all CSR bits
	csrrc x7, 0x9ec, x13	// Clear all CSR bits
	csrrw x7, 0x9ec, x11	// Restore CSR

// Testing CSR 0x9ed
	csrr x3, 0x9ed	// Read CSR
	li x31, -1
	csrrw x29, 0x9ed, x31	// Write all 1s to CSR
	csrrw x29, 0x9ed, x0	// Write all 0s to CSR
	csrrs x29, 0x9ed, x31	// Set all CSR bits
	csrrc x29, 0x9ed, x31	// Clear all CSR bits
	csrrw x29, 0x9ed, x3	// Restore CSR

// Testing CSR 0x9ee
	csrr x14, 0x9ee	// Read CSR
	li x29, -1
	csrrw x24, 0x9ee, x29	// Write all 1s to CSR
	csrrw x24, 0x9ee, x0	// Write all 0s to CSR
	csrrs x24, 0x9ee, x29	// Set all CSR bits
	csrrc x24, 0x9ee, x29	// Clear all CSR bits
	csrrw x24, 0x9ee, x14	// Restore CSR

// Testing CSR 0x9ef
	csrr x12, 0x9ef	// Read CSR
	li x1, -1
	csrrw x6, 0x9ef, x1	// Write all 1s to CSR
	csrrw x6, 0x9ef, x0	// Write all 0s to CSR
	csrrs x6, 0x9ef, x1	// Set all CSR bits
	csrrc x6, 0x9ef, x1	// Clear all CSR bits
	csrrw x6, 0x9ef, x12	// Restore CSR

// Testing CSR 0x9f0
	csrr x31, 0x9f0	// Read CSR
	li x20, -1
	csrrw x14, 0x9f0, x20	// Write all 1s to CSR
	csrrw x14, 0x9f0, x0	// Write all 0s to CSR
	csrrs x14, 0x9f0, x20	// Set all CSR bits
	csrrc x14, 0x9f0, x20	// Clear all CSR bits
	csrrw x14, 0x9f0, x31	// Restore CSR

// Testing CSR 0x9f1
	csrr x6, 0x9f1	// Read CSR
	li x2, -1
	csrrw x24, 0x9f1, x2	// Write all 1s to CSR
	csrrw x24, 0x9f1, x0	// Write all 0s to CSR
	csrrs x24, 0x9f1, x2	// Set all CSR bits
	csrrc x24, 0x9f1, x2	// Clear all CSR bits
	csrrw x24, 0x9f1, x6	// Restore CSR

// Testing CSR 0x9f2
	csrr x7, 0x9f2	// Read CSR
	li x21, -1
	csrrw x2, 0x9f2, x21	// Write all 1s to CSR
	csrrw x2, 0x9f2, x0	// Write all 0s to CSR
	csrrs x2, 0x9f2, x21	// Set all CSR bits
	csrrc x2, 0x9f2, x21	// Clear all CSR bits
	csrrw x2, 0x9f2, x7	// Restore CSR

// Testing CSR 0x9f3
	csrr x8, 0x9f3	// Read CSR
	li x15, -1
	csrrw x6, 0x9f3, x15	// Write all 1s to CSR
	csrrw x6, 0x9f3, x0	// Write all 0s to CSR
	csrrs x6, 0x9f3, x15	// Set all CSR bits
	csrrc x6, 0x9f3, x15	// Clear all CSR bits
	csrrw x6, 0x9f3, x8	// Restore CSR

// Testing CSR 0x9f4
	csrr x2, 0x9f4	// Read CSR
	li x30, -1
	csrrw x19, 0x9f4, x30	// Write all 1s to CSR
	csrrw x19, 0x9f4, x0	// Write all 0s to CSR
	csrrs x19, 0x9f4, x30	// Set all CSR bits
	csrrc x19, 0x9f4, x30	// Clear all CSR bits
	csrrw x19, 0x9f4, x2	// Restore CSR

// Testing CSR 0x9f5
	csrr x17, 0x9f5	// Read CSR
	li x12, -1
	csrrw x2, 0x9f5, x12	// Write all 1s to CSR
	csrrw x2, 0x9f5, x0	// Write all 0s to CSR
	csrrs x2, 0x9f5, x12	// Set all CSR bits
	csrrc x2, 0x9f5, x12	// Clear all CSR bits
	csrrw x2, 0x9f5, x17	// Restore CSR

// Testing CSR 0x9f6
	csrr x18, 0x9f6	// Read CSR
	li x19, -1
	csrrw x5, 0x9f6, x19	// Write all 1s to CSR
	csrrw x5, 0x9f6, x0	// Write all 0s to CSR
	csrrs x5, 0x9f6, x19	// Set all CSR bits
	csrrc x5, 0x9f6, x19	// Clear all CSR bits
	csrrw x5, 0x9f6, x18	// Restore CSR

// Testing CSR 0x9f7
	csrr x24, 0x9f7	// Read CSR
	li x30, -1
	csrrw x17, 0x9f7, x30	// Write all 1s to CSR
	csrrw x17, 0x9f7, x0	// Write all 0s to CSR
	csrrs x17, 0x9f7, x30	// Set all CSR bits
	csrrc x17, 0x9f7, x30	// Clear all CSR bits
	csrrw x17, 0x9f7, x24	// Restore CSR

// Testing CSR 0x9f8
	csrr x30, 0x9f8	// Read CSR
	li x9, -1
	csrrw x1, 0x9f8, x9	// Write all 1s to CSR
	csrrw x1, 0x9f8, x0	// Write all 0s to CSR
	csrrs x1, 0x9f8, x9	// Set all CSR bits
	csrrc x1, 0x9f8, x9	// Clear all CSR bits
	csrrw x1, 0x9f8, x30	// Restore CSR

// Testing CSR 0x9f9
	csrr x16, 0x9f9	// Read CSR
	li x25, -1
	csrrw x31, 0x9f9, x25	// Write all 1s to CSR
	csrrw x31, 0x9f9, x0	// Write all 0s to CSR
	csrrs x31, 0x9f9, x25	// Set all CSR bits
	csrrc x31, 0x9f9, x25	// Clear all CSR bits
	csrrw x31, 0x9f9, x16	// Restore CSR

// Testing CSR 0x9fa
	csrr x11, 0x9fa	// Read CSR
	li x8, -1
	csrrw x26, 0x9fa, x8	// Write all 1s to CSR
	csrrw x26, 0x9fa, x0	// Write all 0s to CSR
	csrrs x26, 0x9fa, x8	// Set all CSR bits
	csrrc x26, 0x9fa, x8	// Clear all CSR bits
	csrrw x26, 0x9fa, x11	// Restore CSR

// Testing CSR 0x9fb
	csrr x30, 0x9fb	// Read CSR
	li x21, -1
	csrrw x12, 0x9fb, x21	// Write all 1s to CSR
	csrrw x12, 0x9fb, x0	// Write all 0s to CSR
	csrrs x12, 0x9fb, x21	// Set all CSR bits
	csrrc x12, 0x9fb, x21	// Clear all CSR bits
	csrrw x12, 0x9fb, x30	// Restore CSR

// Testing CSR 0x9fc
	csrr x24, 0x9fc	// Read CSR
	li x1, -1
	csrrw x17, 0x9fc, x1	// Write all 1s to CSR
	csrrw x17, 0x9fc, x0	// Write all 0s to CSR
	csrrs x17, 0x9fc, x1	// Set all CSR bits
	csrrc x17, 0x9fc, x1	// Clear all CSR bits
	csrrw x17, 0x9fc, x24	// Restore CSR

// Testing CSR 0x9fd
	csrr x22, 0x9fd	// Read CSR
	li x16, -1
	csrrw x1, 0x9fd, x16	// Write all 1s to CSR
	csrrw x1, 0x9fd, x0	// Write all 0s to CSR
	csrrs x1, 0x9fd, x16	// Set all CSR bits
	csrrc x1, 0x9fd, x16	// Clear all CSR bits
	csrrw x1, 0x9fd, x22	// Restore CSR

// Testing CSR 0x9fe
	csrr x12, 0x9fe	// Read CSR
	li x25, -1
	csrrw x10, 0x9fe, x25	// Write all 1s to CSR
	csrrw x10, 0x9fe, x0	// Write all 0s to CSR
	csrrs x10, 0x9fe, x25	// Set all CSR bits
	csrrc x10, 0x9fe, x25	// Clear all CSR bits
	csrrw x10, 0x9fe, x12	// Restore CSR

// Testing CSR 0x9ff
	csrr x7, 0x9ff	// Read CSR
	li x6, -1
	csrrw x1, 0x9ff, x6	// Write all 1s to CSR
	csrrw x1, 0x9ff, x0	// Write all 0s to CSR
	csrrs x1, 0x9ff, x6	// Set all CSR bits
	csrrc x1, 0x9ff, x6	// Clear all CSR bits
	csrrw x1, 0x9ff, x7	// Restore CSR

// Testing CSR 0xa00
	csrr x20, 0xa00	// Read CSR
	li x6, -1
	csrrw x23, 0xa00, x6	// Write all 1s to CSR
	csrrw x23, 0xa00, x0	// Write all 0s to CSR
	csrrs x23, 0xa00, x6	// Set all CSR bits
	csrrc x23, 0xa00, x6	// Clear all CSR bits
	csrrw x23, 0xa00, x20	// Restore CSR

// Testing CSR 0xa01
	csrr x10, 0xa01	// Read CSR
	li x9, -1
	csrrw x6, 0xa01, x9	// Write all 1s to CSR
	csrrw x6, 0xa01, x0	// Write all 0s to CSR
	csrrs x6, 0xa01, x9	// Set all CSR bits
	csrrc x6, 0xa01, x9	// Clear all CSR bits
	csrrw x6, 0xa01, x10	// Restore CSR

// Testing CSR 0xa02
	csrr x6, 0xa02	// Read CSR
	li x14, -1
	csrrw x20, 0xa02, x14	// Write all 1s to CSR
	csrrw x20, 0xa02, x0	// Write all 0s to CSR
	csrrs x20, 0xa02, x14	// Set all CSR bits
	csrrc x20, 0xa02, x14	// Clear all CSR bits
	csrrw x20, 0xa02, x6	// Restore CSR

// Testing CSR 0xa03
	csrr x15, 0xa03	// Read CSR
	li x3, -1
	csrrw x13, 0xa03, x3	// Write all 1s to CSR
	csrrw x13, 0xa03, x0	// Write all 0s to CSR
	csrrs x13, 0xa03, x3	// Set all CSR bits
	csrrc x13, 0xa03, x3	// Clear all CSR bits
	csrrw x13, 0xa03, x15	// Restore CSR

// Testing CSR 0xa04
	csrr x19, 0xa04	// Read CSR
	li x30, -1
	csrrw x29, 0xa04, x30	// Write all 1s to CSR
	csrrw x29, 0xa04, x0	// Write all 0s to CSR
	csrrs x29, 0xa04, x30	// Set all CSR bits
	csrrc x29, 0xa04, x30	// Clear all CSR bits
	csrrw x29, 0xa04, x19	// Restore CSR

// Testing CSR 0xa05
	csrr x6, 0xa05	// Read CSR
	li x11, -1
	csrrw x15, 0xa05, x11	// Write all 1s to CSR
	csrrw x15, 0xa05, x0	// Write all 0s to CSR
	csrrs x15, 0xa05, x11	// Set all CSR bits
	csrrc x15, 0xa05, x11	// Clear all CSR bits
	csrrw x15, 0xa05, x6	// Restore CSR

// Testing CSR 0xa06
	csrr x18, 0xa06	// Read CSR
	li x25, -1
	csrrw x8, 0xa06, x25	// Write all 1s to CSR
	csrrw x8, 0xa06, x0	// Write all 0s to CSR
	csrrs x8, 0xa06, x25	// Set all CSR bits
	csrrc x8, 0xa06, x25	// Clear all CSR bits
	csrrw x8, 0xa06, x18	// Restore CSR

// Testing CSR 0xa07
	csrr x11, 0xa07	// Read CSR
	li x13, -1
	csrrw x27, 0xa07, x13	// Write all 1s to CSR
	csrrw x27, 0xa07, x0	// Write all 0s to CSR
	csrrs x27, 0xa07, x13	// Set all CSR bits
	csrrc x27, 0xa07, x13	// Clear all CSR bits
	csrrw x27, 0xa07, x11	// Restore CSR

// Testing CSR 0xa08
	csrr x24, 0xa08	// Read CSR
	li x17, -1
	csrrw x8, 0xa08, x17	// Write all 1s to CSR
	csrrw x8, 0xa08, x0	// Write all 0s to CSR
	csrrs x8, 0xa08, x17	// Set all CSR bits
	csrrc x8, 0xa08, x17	// Clear all CSR bits
	csrrw x8, 0xa08, x24	// Restore CSR

// Testing CSR 0xa09
	csrr x2, 0xa09	// Read CSR
	li x13, -1
	csrrw x10, 0xa09, x13	// Write all 1s to CSR
	csrrw x10, 0xa09, x0	// Write all 0s to CSR
	csrrs x10, 0xa09, x13	// Set all CSR bits
	csrrc x10, 0xa09, x13	// Clear all CSR bits
	csrrw x10, 0xa09, x2	// Restore CSR

// Testing CSR 0xa0a
	csrr x31, 0xa0a	// Read CSR
	li x23, -1
	csrrw x26, 0xa0a, x23	// Write all 1s to CSR
	csrrw x26, 0xa0a, x0	// Write all 0s to CSR
	csrrs x26, 0xa0a, x23	// Set all CSR bits
	csrrc x26, 0xa0a, x23	// Clear all CSR bits
	csrrw x26, 0xa0a, x31	// Restore CSR

// Testing CSR 0xa0b
	csrr x12, 0xa0b	// Read CSR
	li x15, -1
	csrrw x27, 0xa0b, x15	// Write all 1s to CSR
	csrrw x27, 0xa0b, x0	// Write all 0s to CSR
	csrrs x27, 0xa0b, x15	// Set all CSR bits
	csrrc x27, 0xa0b, x15	// Clear all CSR bits
	csrrw x27, 0xa0b, x12	// Restore CSR

// Testing CSR 0xa0c
	csrr x21, 0xa0c	// Read CSR
	li x4, -1
	csrrw x24, 0xa0c, x4	// Write all 1s to CSR
	csrrw x24, 0xa0c, x0	// Write all 0s to CSR
	csrrs x24, 0xa0c, x4	// Set all CSR bits
	csrrc x24, 0xa0c, x4	// Clear all CSR bits
	csrrw x24, 0xa0c, x21	// Restore CSR

// Testing CSR 0xa0d
	csrr x21, 0xa0d	// Read CSR
	li x17, -1
	csrrw x10, 0xa0d, x17	// Write all 1s to CSR
	csrrw x10, 0xa0d, x0	// Write all 0s to CSR
	csrrs x10, 0xa0d, x17	// Set all CSR bits
	csrrc x10, 0xa0d, x17	// Clear all CSR bits
	csrrw x10, 0xa0d, x21	// Restore CSR

// Testing CSR 0xa0e
	csrr x26, 0xa0e	// Read CSR
	li x28, -1
	csrrw x18, 0xa0e, x28	// Write all 1s to CSR
	csrrw x18, 0xa0e, x0	// Write all 0s to CSR
	csrrs x18, 0xa0e, x28	// Set all CSR bits
	csrrc x18, 0xa0e, x28	// Clear all CSR bits
	csrrw x18, 0xa0e, x26	// Restore CSR

// Testing CSR 0xa0f
	csrr x31, 0xa0f	// Read CSR
	li x21, -1
	csrrw x28, 0xa0f, x21	// Write all 1s to CSR
	csrrw x28, 0xa0f, x0	// Write all 0s to CSR
	csrrs x28, 0xa0f, x21	// Set all CSR bits
	csrrc x28, 0xa0f, x21	// Clear all CSR bits
	csrrw x28, 0xa0f, x31	// Restore CSR

// Testing CSR 0xa10
	csrr x28, 0xa10	// Read CSR
	li x10, -1
	csrrw x15, 0xa10, x10	// Write all 1s to CSR
	csrrw x15, 0xa10, x0	// Write all 0s to CSR
	csrrs x15, 0xa10, x10	// Set all CSR bits
	csrrc x15, 0xa10, x10	// Clear all CSR bits
	csrrw x15, 0xa10, x28	// Restore CSR

// Testing CSR 0xa11
	csrr x18, 0xa11	// Read CSR
	li x11, -1
	csrrw x22, 0xa11, x11	// Write all 1s to CSR
	csrrw x22, 0xa11, x0	// Write all 0s to CSR
	csrrs x22, 0xa11, x11	// Set all CSR bits
	csrrc x22, 0xa11, x11	// Clear all CSR bits
	csrrw x22, 0xa11, x18	// Restore CSR

// Testing CSR 0xa12
	csrr x10, 0xa12	// Read CSR
	li x28, -1
	csrrw x26, 0xa12, x28	// Write all 1s to CSR
	csrrw x26, 0xa12, x0	// Write all 0s to CSR
	csrrs x26, 0xa12, x28	// Set all CSR bits
	csrrc x26, 0xa12, x28	// Clear all CSR bits
	csrrw x26, 0xa12, x10	// Restore CSR

// Testing CSR 0xa13
	csrr x9, 0xa13	// Read CSR
	li x12, -1
	csrrw x7, 0xa13, x12	// Write all 1s to CSR
	csrrw x7, 0xa13, x0	// Write all 0s to CSR
	csrrs x7, 0xa13, x12	// Set all CSR bits
	csrrc x7, 0xa13, x12	// Clear all CSR bits
	csrrw x7, 0xa13, x9	// Restore CSR

// Testing CSR 0xa14
	csrr x11, 0xa14	// Read CSR
	li x26, -1
	csrrw x1, 0xa14, x26	// Write all 1s to CSR
	csrrw x1, 0xa14, x0	// Write all 0s to CSR
	csrrs x1, 0xa14, x26	// Set all CSR bits
	csrrc x1, 0xa14, x26	// Clear all CSR bits
	csrrw x1, 0xa14, x11	// Restore CSR

// Testing CSR 0xa15
	csrr x16, 0xa15	// Read CSR
	li x27, -1
	csrrw x15, 0xa15, x27	// Write all 1s to CSR
	csrrw x15, 0xa15, x0	// Write all 0s to CSR
	csrrs x15, 0xa15, x27	// Set all CSR bits
	csrrc x15, 0xa15, x27	// Clear all CSR bits
	csrrw x15, 0xa15, x16	// Restore CSR

// Testing CSR 0xa16
	csrr x23, 0xa16	// Read CSR
	li x12, -1
	csrrw x17, 0xa16, x12	// Write all 1s to CSR
	csrrw x17, 0xa16, x0	// Write all 0s to CSR
	csrrs x17, 0xa16, x12	// Set all CSR bits
	csrrc x17, 0xa16, x12	// Clear all CSR bits
	csrrw x17, 0xa16, x23	// Restore CSR

// Testing CSR 0xa17
	csrr x26, 0xa17	// Read CSR
	li x7, -1
	csrrw x6, 0xa17, x7	// Write all 1s to CSR
	csrrw x6, 0xa17, x0	// Write all 0s to CSR
	csrrs x6, 0xa17, x7	// Set all CSR bits
	csrrc x6, 0xa17, x7	// Clear all CSR bits
	csrrw x6, 0xa17, x26	// Restore CSR

// Testing CSR 0xa18
	csrr x11, 0xa18	// Read CSR
	li x18, -1
	csrrw x16, 0xa18, x18	// Write all 1s to CSR
	csrrw x16, 0xa18, x0	// Write all 0s to CSR
	csrrs x16, 0xa18, x18	// Set all CSR bits
	csrrc x16, 0xa18, x18	// Clear all CSR bits
	csrrw x16, 0xa18, x11	// Restore CSR

// Testing CSR 0xa19
	csrr x17, 0xa19	// Read CSR
	li x19, -1
	csrrw x24, 0xa19, x19	// Write all 1s to CSR
	csrrw x24, 0xa19, x0	// Write all 0s to CSR
	csrrs x24, 0xa19, x19	// Set all CSR bits
	csrrc x24, 0xa19, x19	// Clear all CSR bits
	csrrw x24, 0xa19, x17	// Restore CSR

// Testing CSR 0xa1a
	csrr x16, 0xa1a	// Read CSR
	li x11, -1
	csrrw x25, 0xa1a, x11	// Write all 1s to CSR
	csrrw x25, 0xa1a, x0	// Write all 0s to CSR
	csrrs x25, 0xa1a, x11	// Set all CSR bits
	csrrc x25, 0xa1a, x11	// Clear all CSR bits
	csrrw x25, 0xa1a, x16	// Restore CSR

// Testing CSR 0xa1b
	csrr x28, 0xa1b	// Read CSR
	li x30, -1
	csrrw x7, 0xa1b, x30	// Write all 1s to CSR
	csrrw x7, 0xa1b, x0	// Write all 0s to CSR
	csrrs x7, 0xa1b, x30	// Set all CSR bits
	csrrc x7, 0xa1b, x30	// Clear all CSR bits
	csrrw x7, 0xa1b, x28	// Restore CSR

// Testing CSR 0xa1c
	csrr x1, 0xa1c	// Read CSR
	li x2, -1
	csrrw x8, 0xa1c, x2	// Write all 1s to CSR
	csrrw x8, 0xa1c, x0	// Write all 0s to CSR
	csrrs x8, 0xa1c, x2	// Set all CSR bits
	csrrc x8, 0xa1c, x2	// Clear all CSR bits
	csrrw x8, 0xa1c, x1	// Restore CSR

// Testing CSR 0xa1d
	csrr x15, 0xa1d	// Read CSR
	li x27, -1
	csrrw x13, 0xa1d, x27	// Write all 1s to CSR
	csrrw x13, 0xa1d, x0	// Write all 0s to CSR
	csrrs x13, 0xa1d, x27	// Set all CSR bits
	csrrc x13, 0xa1d, x27	// Clear all CSR bits
	csrrw x13, 0xa1d, x15	// Restore CSR

// Testing CSR 0xa1e
	csrr x21, 0xa1e	// Read CSR
	li x30, -1
	csrrw x13, 0xa1e, x30	// Write all 1s to CSR
	csrrw x13, 0xa1e, x0	// Write all 0s to CSR
	csrrs x13, 0xa1e, x30	// Set all CSR bits
	csrrc x13, 0xa1e, x30	// Clear all CSR bits
	csrrw x13, 0xa1e, x21	// Restore CSR

// Testing CSR 0xa1f
	csrr x29, 0xa1f	// Read CSR
	li x4, -1
	csrrw x27, 0xa1f, x4	// Write all 1s to CSR
	csrrw x27, 0xa1f, x0	// Write all 0s to CSR
	csrrs x27, 0xa1f, x4	// Set all CSR bits
	csrrc x27, 0xa1f, x4	// Clear all CSR bits
	csrrw x27, 0xa1f, x29	// Restore CSR

// Testing CSR 0xa20
	csrr x6, 0xa20	// Read CSR
	li x11, -1
	csrrw x9, 0xa20, x11	// Write all 1s to CSR
	csrrw x9, 0xa20, x0	// Write all 0s to CSR
	csrrs x9, 0xa20, x11	// Set all CSR bits
	csrrc x9, 0xa20, x11	// Clear all CSR bits
	csrrw x9, 0xa20, x6	// Restore CSR

// Testing CSR 0xa21
	csrr x4, 0xa21	// Read CSR
	li x6, -1
	csrrw x3, 0xa21, x6	// Write all 1s to CSR
	csrrw x3, 0xa21, x0	// Write all 0s to CSR
	csrrs x3, 0xa21, x6	// Set all CSR bits
	csrrc x3, 0xa21, x6	// Clear all CSR bits
	csrrw x3, 0xa21, x4	// Restore CSR

// Testing CSR 0xa22
	csrr x19, 0xa22	// Read CSR
	li x1, -1
	csrrw x13, 0xa22, x1	// Write all 1s to CSR
	csrrw x13, 0xa22, x0	// Write all 0s to CSR
	csrrs x13, 0xa22, x1	// Set all CSR bits
	csrrc x13, 0xa22, x1	// Clear all CSR bits
	csrrw x13, 0xa22, x19	// Restore CSR

// Testing CSR 0xa23
	csrr x14, 0xa23	// Read CSR
	li x26, -1
	csrrw x18, 0xa23, x26	// Write all 1s to CSR
	csrrw x18, 0xa23, x0	// Write all 0s to CSR
	csrrs x18, 0xa23, x26	// Set all CSR bits
	csrrc x18, 0xa23, x26	// Clear all CSR bits
	csrrw x18, 0xa23, x14	// Restore CSR

// Testing CSR 0xa24
	csrr x7, 0xa24	// Read CSR
	li x4, -1
	csrrw x2, 0xa24, x4	// Write all 1s to CSR
	csrrw x2, 0xa24, x0	// Write all 0s to CSR
	csrrs x2, 0xa24, x4	// Set all CSR bits
	csrrc x2, 0xa24, x4	// Clear all CSR bits
	csrrw x2, 0xa24, x7	// Restore CSR

// Testing CSR 0xa25
	csrr x12, 0xa25	// Read CSR
	li x18, -1
	csrrw x26, 0xa25, x18	// Write all 1s to CSR
	csrrw x26, 0xa25, x0	// Write all 0s to CSR
	csrrs x26, 0xa25, x18	// Set all CSR bits
	csrrc x26, 0xa25, x18	// Clear all CSR bits
	csrrw x26, 0xa25, x12	// Restore CSR

// Testing CSR 0xa26
	csrr x30, 0xa26	// Read CSR
	li x1, -1
	csrrw x3, 0xa26, x1	// Write all 1s to CSR
	csrrw x3, 0xa26, x0	// Write all 0s to CSR
	csrrs x3, 0xa26, x1	// Set all CSR bits
	csrrc x3, 0xa26, x1	// Clear all CSR bits
	csrrw x3, 0xa26, x30	// Restore CSR

// Testing CSR 0xa27
	csrr x1, 0xa27	// Read CSR
	li x30, -1
	csrrw x7, 0xa27, x30	// Write all 1s to CSR
	csrrw x7, 0xa27, x0	// Write all 0s to CSR
	csrrs x7, 0xa27, x30	// Set all CSR bits
	csrrc x7, 0xa27, x30	// Clear all CSR bits
	csrrw x7, 0xa27, x1	// Restore CSR

// Testing CSR 0xa28
	csrr x4, 0xa28	// Read CSR
	li x28, -1
	csrrw x31, 0xa28, x28	// Write all 1s to CSR
	csrrw x31, 0xa28, x0	// Write all 0s to CSR
	csrrs x31, 0xa28, x28	// Set all CSR bits
	csrrc x31, 0xa28, x28	// Clear all CSR bits
	csrrw x31, 0xa28, x4	// Restore CSR

// Testing CSR 0xa29
	csrr x20, 0xa29	// Read CSR
	li x4, -1
	csrrw x28, 0xa29, x4	// Write all 1s to CSR
	csrrw x28, 0xa29, x0	// Write all 0s to CSR
	csrrs x28, 0xa29, x4	// Set all CSR bits
	csrrc x28, 0xa29, x4	// Clear all CSR bits
	csrrw x28, 0xa29, x20	// Restore CSR

// Testing CSR 0xa2a
	csrr x26, 0xa2a	// Read CSR
	li x25, -1
	csrrw x31, 0xa2a, x25	// Write all 1s to CSR
	csrrw x31, 0xa2a, x0	// Write all 0s to CSR
	csrrs x31, 0xa2a, x25	// Set all CSR bits
	csrrc x31, 0xa2a, x25	// Clear all CSR bits
	csrrw x31, 0xa2a, x26	// Restore CSR

// Testing CSR 0xa2b
	csrr x15, 0xa2b	// Read CSR
	li x14, -1
	csrrw x7, 0xa2b, x14	// Write all 1s to CSR
	csrrw x7, 0xa2b, x0	// Write all 0s to CSR
	csrrs x7, 0xa2b, x14	// Set all CSR bits
	csrrc x7, 0xa2b, x14	// Clear all CSR bits
	csrrw x7, 0xa2b, x15	// Restore CSR

// Testing CSR 0xa2c
	csrr x7, 0xa2c	// Read CSR
	li x27, -1
	csrrw x15, 0xa2c, x27	// Write all 1s to CSR
	csrrw x15, 0xa2c, x0	// Write all 0s to CSR
	csrrs x15, 0xa2c, x27	// Set all CSR bits
	csrrc x15, 0xa2c, x27	// Clear all CSR bits
	csrrw x15, 0xa2c, x7	// Restore CSR

// Testing CSR 0xa2d
	csrr x18, 0xa2d	// Read CSR
	li x11, -1
	csrrw x2, 0xa2d, x11	// Write all 1s to CSR
	csrrw x2, 0xa2d, x0	// Write all 0s to CSR
	csrrs x2, 0xa2d, x11	// Set all CSR bits
	csrrc x2, 0xa2d, x11	// Clear all CSR bits
	csrrw x2, 0xa2d, x18	// Restore CSR

// Testing CSR 0xa2e
	csrr x18, 0xa2e	// Read CSR
	li x6, -1
	csrrw x28, 0xa2e, x6	// Write all 1s to CSR
	csrrw x28, 0xa2e, x0	// Write all 0s to CSR
	csrrs x28, 0xa2e, x6	// Set all CSR bits
	csrrc x28, 0xa2e, x6	// Clear all CSR bits
	csrrw x28, 0xa2e, x18	// Restore CSR

// Testing CSR 0xa2f
	csrr x9, 0xa2f	// Read CSR
	li x2, -1
	csrrw x24, 0xa2f, x2	// Write all 1s to CSR
	csrrw x24, 0xa2f, x0	// Write all 0s to CSR
	csrrs x24, 0xa2f, x2	// Set all CSR bits
	csrrc x24, 0xa2f, x2	// Clear all CSR bits
	csrrw x24, 0xa2f, x9	// Restore CSR

// Testing CSR 0xa30
	csrr x1, 0xa30	// Read CSR
	li x15, -1
	csrrw x25, 0xa30, x15	// Write all 1s to CSR
	csrrw x25, 0xa30, x0	// Write all 0s to CSR
	csrrs x25, 0xa30, x15	// Set all CSR bits
	csrrc x25, 0xa30, x15	// Clear all CSR bits
	csrrw x25, 0xa30, x1	// Restore CSR

// Testing CSR 0xa31
	csrr x24, 0xa31	// Read CSR
	li x28, -1
	csrrw x31, 0xa31, x28	// Write all 1s to CSR
	csrrw x31, 0xa31, x0	// Write all 0s to CSR
	csrrs x31, 0xa31, x28	// Set all CSR bits
	csrrc x31, 0xa31, x28	// Clear all CSR bits
	csrrw x31, 0xa31, x24	// Restore CSR

// Testing CSR 0xa32
	csrr x19, 0xa32	// Read CSR
	li x26, -1
	csrrw x11, 0xa32, x26	// Write all 1s to CSR
	csrrw x11, 0xa32, x0	// Write all 0s to CSR
	csrrs x11, 0xa32, x26	// Set all CSR bits
	csrrc x11, 0xa32, x26	// Clear all CSR bits
	csrrw x11, 0xa32, x19	// Restore CSR

// Testing CSR 0xa33
	csrr x16, 0xa33	// Read CSR
	li x28, -1
	csrrw x17, 0xa33, x28	// Write all 1s to CSR
	csrrw x17, 0xa33, x0	// Write all 0s to CSR
	csrrs x17, 0xa33, x28	// Set all CSR bits
	csrrc x17, 0xa33, x28	// Clear all CSR bits
	csrrw x17, 0xa33, x16	// Restore CSR

// Testing CSR 0xa34
	csrr x26, 0xa34	// Read CSR
	li x10, -1
	csrrw x29, 0xa34, x10	// Write all 1s to CSR
	csrrw x29, 0xa34, x0	// Write all 0s to CSR
	csrrs x29, 0xa34, x10	// Set all CSR bits
	csrrc x29, 0xa34, x10	// Clear all CSR bits
	csrrw x29, 0xa34, x26	// Restore CSR

// Testing CSR 0xa35
	csrr x2, 0xa35	// Read CSR
	li x24, -1
	csrrw x19, 0xa35, x24	// Write all 1s to CSR
	csrrw x19, 0xa35, x0	// Write all 0s to CSR
	csrrs x19, 0xa35, x24	// Set all CSR bits
	csrrc x19, 0xa35, x24	// Clear all CSR bits
	csrrw x19, 0xa35, x2	// Restore CSR

// Testing CSR 0xa36
	csrr x30, 0xa36	// Read CSR
	li x27, -1
	csrrw x25, 0xa36, x27	// Write all 1s to CSR
	csrrw x25, 0xa36, x0	// Write all 0s to CSR
	csrrs x25, 0xa36, x27	// Set all CSR bits
	csrrc x25, 0xa36, x27	// Clear all CSR bits
	csrrw x25, 0xa36, x30	// Restore CSR

// Testing CSR 0xa37
	csrr x20, 0xa37	// Read CSR
	li x9, -1
	csrrw x15, 0xa37, x9	// Write all 1s to CSR
	csrrw x15, 0xa37, x0	// Write all 0s to CSR
	csrrs x15, 0xa37, x9	// Set all CSR bits
	csrrc x15, 0xa37, x9	// Clear all CSR bits
	csrrw x15, 0xa37, x20	// Restore CSR

// Testing CSR 0xa38
	csrr x12, 0xa38	// Read CSR
	li x23, -1
	csrrw x24, 0xa38, x23	// Write all 1s to CSR
	csrrw x24, 0xa38, x0	// Write all 0s to CSR
	csrrs x24, 0xa38, x23	// Set all CSR bits
	csrrc x24, 0xa38, x23	// Clear all CSR bits
	csrrw x24, 0xa38, x12	// Restore CSR

// Testing CSR 0xa39
	csrr x31, 0xa39	// Read CSR
	li x7, -1
	csrrw x15, 0xa39, x7	// Write all 1s to CSR
	csrrw x15, 0xa39, x0	// Write all 0s to CSR
	csrrs x15, 0xa39, x7	// Set all CSR bits
	csrrc x15, 0xa39, x7	// Clear all CSR bits
	csrrw x15, 0xa39, x31	// Restore CSR

// Testing CSR 0xa3a
	csrr x30, 0xa3a	// Read CSR
	li x28, -1
	csrrw x16, 0xa3a, x28	// Write all 1s to CSR
	csrrw x16, 0xa3a, x0	// Write all 0s to CSR
	csrrs x16, 0xa3a, x28	// Set all CSR bits
	csrrc x16, 0xa3a, x28	// Clear all CSR bits
	csrrw x16, 0xa3a, x30	// Restore CSR

// Testing CSR 0xa3b
	csrr x2, 0xa3b	// Read CSR
	li x23, -1
	csrrw x3, 0xa3b, x23	// Write all 1s to CSR
	csrrw x3, 0xa3b, x0	// Write all 0s to CSR
	csrrs x3, 0xa3b, x23	// Set all CSR bits
	csrrc x3, 0xa3b, x23	// Clear all CSR bits
	csrrw x3, 0xa3b, x2	// Restore CSR

// Testing CSR 0xa3c
	csrr x29, 0xa3c	// Read CSR
	li x22, -1
	csrrw x24, 0xa3c, x22	// Write all 1s to CSR
	csrrw x24, 0xa3c, x0	// Write all 0s to CSR
	csrrs x24, 0xa3c, x22	// Set all CSR bits
	csrrc x24, 0xa3c, x22	// Clear all CSR bits
	csrrw x24, 0xa3c, x29	// Restore CSR

// Testing CSR 0xa3d
	csrr x17, 0xa3d	// Read CSR
	li x22, -1
	csrrw x6, 0xa3d, x22	// Write all 1s to CSR
	csrrw x6, 0xa3d, x0	// Write all 0s to CSR
	csrrs x6, 0xa3d, x22	// Set all CSR bits
	csrrc x6, 0xa3d, x22	// Clear all CSR bits
	csrrw x6, 0xa3d, x17	// Restore CSR

// Testing CSR 0xa3e
	csrr x25, 0xa3e	// Read CSR
	li x28, -1
	csrrw x1, 0xa3e, x28	// Write all 1s to CSR
	csrrw x1, 0xa3e, x0	// Write all 0s to CSR
	csrrs x1, 0xa3e, x28	// Set all CSR bits
	csrrc x1, 0xa3e, x28	// Clear all CSR bits
	csrrw x1, 0xa3e, x25	// Restore CSR

// Testing CSR 0xa3f
	csrr x3, 0xa3f	// Read CSR
	li x19, -1
	csrrw x17, 0xa3f, x19	// Write all 1s to CSR
	csrrw x17, 0xa3f, x0	// Write all 0s to CSR
	csrrs x17, 0xa3f, x19	// Set all CSR bits
	csrrc x17, 0xa3f, x19	// Clear all CSR bits
	csrrw x17, 0xa3f, x3	// Restore CSR

// Testing CSR 0xa40
	csrr x7, 0xa40	// Read CSR
	li x21, -1
	csrrw x18, 0xa40, x21	// Write all 1s to CSR
	csrrw x18, 0xa40, x0	// Write all 0s to CSR
	csrrs x18, 0xa40, x21	// Set all CSR bits
	csrrc x18, 0xa40, x21	// Clear all CSR bits
	csrrw x18, 0xa40, x7	// Restore CSR

// Testing CSR 0xa41
	csrr x6, 0xa41	// Read CSR
	li x31, -1
	csrrw x11, 0xa41, x31	// Write all 1s to CSR
	csrrw x11, 0xa41, x0	// Write all 0s to CSR
	csrrs x11, 0xa41, x31	// Set all CSR bits
	csrrc x11, 0xa41, x31	// Clear all CSR bits
	csrrw x11, 0xa41, x6	// Restore CSR

// Testing CSR 0xa42
	csrr x1, 0xa42	// Read CSR
	li x20, -1
	csrrw x4, 0xa42, x20	// Write all 1s to CSR
	csrrw x4, 0xa42, x0	// Write all 0s to CSR
	csrrs x4, 0xa42, x20	// Set all CSR bits
	csrrc x4, 0xa42, x20	// Clear all CSR bits
	csrrw x4, 0xa42, x1	// Restore CSR

// Testing CSR 0xa43
	csrr x25, 0xa43	// Read CSR
	li x23, -1
	csrrw x8, 0xa43, x23	// Write all 1s to CSR
	csrrw x8, 0xa43, x0	// Write all 0s to CSR
	csrrs x8, 0xa43, x23	// Set all CSR bits
	csrrc x8, 0xa43, x23	// Clear all CSR bits
	csrrw x8, 0xa43, x25	// Restore CSR

// Testing CSR 0xa44
	csrr x26, 0xa44	// Read CSR
	li x3, -1
	csrrw x11, 0xa44, x3	// Write all 1s to CSR
	csrrw x11, 0xa44, x0	// Write all 0s to CSR
	csrrs x11, 0xa44, x3	// Set all CSR bits
	csrrc x11, 0xa44, x3	// Clear all CSR bits
	csrrw x11, 0xa44, x26	// Restore CSR

// Testing CSR 0xa45
	csrr x13, 0xa45	// Read CSR
	li x5, -1
	csrrw x17, 0xa45, x5	// Write all 1s to CSR
	csrrw x17, 0xa45, x0	// Write all 0s to CSR
	csrrs x17, 0xa45, x5	// Set all CSR bits
	csrrc x17, 0xa45, x5	// Clear all CSR bits
	csrrw x17, 0xa45, x13	// Restore CSR

// Testing CSR 0xa46
	csrr x12, 0xa46	// Read CSR
	li x14, -1
	csrrw x15, 0xa46, x14	// Write all 1s to CSR
	csrrw x15, 0xa46, x0	// Write all 0s to CSR
	csrrs x15, 0xa46, x14	// Set all CSR bits
	csrrc x15, 0xa46, x14	// Clear all CSR bits
	csrrw x15, 0xa46, x12	// Restore CSR

// Testing CSR 0xa47
	csrr x10, 0xa47	// Read CSR
	li x12, -1
	csrrw x1, 0xa47, x12	// Write all 1s to CSR
	csrrw x1, 0xa47, x0	// Write all 0s to CSR
	csrrs x1, 0xa47, x12	// Set all CSR bits
	csrrc x1, 0xa47, x12	// Clear all CSR bits
	csrrw x1, 0xa47, x10	// Restore CSR

// Testing CSR 0xa48
	csrr x1, 0xa48	// Read CSR
	li x23, -1
	csrrw x30, 0xa48, x23	// Write all 1s to CSR
	csrrw x30, 0xa48, x0	// Write all 0s to CSR
	csrrs x30, 0xa48, x23	// Set all CSR bits
	csrrc x30, 0xa48, x23	// Clear all CSR bits
	csrrw x30, 0xa48, x1	// Restore CSR

// Testing CSR 0xa49
	csrr x23, 0xa49	// Read CSR
	li x2, -1
	csrrw x3, 0xa49, x2	// Write all 1s to CSR
	csrrw x3, 0xa49, x0	// Write all 0s to CSR
	csrrs x3, 0xa49, x2	// Set all CSR bits
	csrrc x3, 0xa49, x2	// Clear all CSR bits
	csrrw x3, 0xa49, x23	// Restore CSR

// Testing CSR 0xa4a
	csrr x23, 0xa4a	// Read CSR
	li x18, -1
	csrrw x8, 0xa4a, x18	// Write all 1s to CSR
	csrrw x8, 0xa4a, x0	// Write all 0s to CSR
	csrrs x8, 0xa4a, x18	// Set all CSR bits
	csrrc x8, 0xa4a, x18	// Clear all CSR bits
	csrrw x8, 0xa4a, x23	// Restore CSR

// Testing CSR 0xa4b
	csrr x25, 0xa4b	// Read CSR
	li x4, -1
	csrrw x6, 0xa4b, x4	// Write all 1s to CSR
	csrrw x6, 0xa4b, x0	// Write all 0s to CSR
	csrrs x6, 0xa4b, x4	// Set all CSR bits
	csrrc x6, 0xa4b, x4	// Clear all CSR bits
	csrrw x6, 0xa4b, x25	// Restore CSR

// Testing CSR 0xa4c
	csrr x26, 0xa4c	// Read CSR
	li x6, -1
	csrrw x24, 0xa4c, x6	// Write all 1s to CSR
	csrrw x24, 0xa4c, x0	// Write all 0s to CSR
	csrrs x24, 0xa4c, x6	// Set all CSR bits
	csrrc x24, 0xa4c, x6	// Clear all CSR bits
	csrrw x24, 0xa4c, x26	// Restore CSR

// Testing CSR 0xa4d
	csrr x5, 0xa4d	// Read CSR
	li x18, -1
	csrrw x23, 0xa4d, x18	// Write all 1s to CSR
	csrrw x23, 0xa4d, x0	// Write all 0s to CSR
	csrrs x23, 0xa4d, x18	// Set all CSR bits
	csrrc x23, 0xa4d, x18	// Clear all CSR bits
	csrrw x23, 0xa4d, x5	// Restore CSR

// Testing CSR 0xa4e
	csrr x16, 0xa4e	// Read CSR
	li x28, -1
	csrrw x13, 0xa4e, x28	// Write all 1s to CSR
	csrrw x13, 0xa4e, x0	// Write all 0s to CSR
	csrrs x13, 0xa4e, x28	// Set all CSR bits
	csrrc x13, 0xa4e, x28	// Clear all CSR bits
	csrrw x13, 0xa4e, x16	// Restore CSR

// Testing CSR 0xa4f
	csrr x13, 0xa4f	// Read CSR
	li x5, -1
	csrrw x7, 0xa4f, x5	// Write all 1s to CSR
	csrrw x7, 0xa4f, x0	// Write all 0s to CSR
	csrrs x7, 0xa4f, x5	// Set all CSR bits
	csrrc x7, 0xa4f, x5	// Clear all CSR bits
	csrrw x7, 0xa4f, x13	// Restore CSR

// Testing CSR 0xa50
	csrr x10, 0xa50	// Read CSR
	li x26, -1
	csrrw x3, 0xa50, x26	// Write all 1s to CSR
	csrrw x3, 0xa50, x0	// Write all 0s to CSR
	csrrs x3, 0xa50, x26	// Set all CSR bits
	csrrc x3, 0xa50, x26	// Clear all CSR bits
	csrrw x3, 0xa50, x10	// Restore CSR

// Testing CSR 0xa51
	csrr x25, 0xa51	// Read CSR
	li x3, -1
	csrrw x17, 0xa51, x3	// Write all 1s to CSR
	csrrw x17, 0xa51, x0	// Write all 0s to CSR
	csrrs x17, 0xa51, x3	// Set all CSR bits
	csrrc x17, 0xa51, x3	// Clear all CSR bits
	csrrw x17, 0xa51, x25	// Restore CSR

// Testing CSR 0xa52
	csrr x4, 0xa52	// Read CSR
	li x23, -1
	csrrw x6, 0xa52, x23	// Write all 1s to CSR
	csrrw x6, 0xa52, x0	// Write all 0s to CSR
	csrrs x6, 0xa52, x23	// Set all CSR bits
	csrrc x6, 0xa52, x23	// Clear all CSR bits
	csrrw x6, 0xa52, x4	// Restore CSR

// Testing CSR 0xa53
	csrr x31, 0xa53	// Read CSR
	li x4, -1
	csrrw x18, 0xa53, x4	// Write all 1s to CSR
	csrrw x18, 0xa53, x0	// Write all 0s to CSR
	csrrs x18, 0xa53, x4	// Set all CSR bits
	csrrc x18, 0xa53, x4	// Clear all CSR bits
	csrrw x18, 0xa53, x31	// Restore CSR

// Testing CSR 0xa54
	csrr x30, 0xa54	// Read CSR
	li x14, -1
	csrrw x18, 0xa54, x14	// Write all 1s to CSR
	csrrw x18, 0xa54, x0	// Write all 0s to CSR
	csrrs x18, 0xa54, x14	// Set all CSR bits
	csrrc x18, 0xa54, x14	// Clear all CSR bits
	csrrw x18, 0xa54, x30	// Restore CSR

// Testing CSR 0xa55
	csrr x9, 0xa55	// Read CSR
	li x12, -1
	csrrw x2, 0xa55, x12	// Write all 1s to CSR
	csrrw x2, 0xa55, x0	// Write all 0s to CSR
	csrrs x2, 0xa55, x12	// Set all CSR bits
	csrrc x2, 0xa55, x12	// Clear all CSR bits
	csrrw x2, 0xa55, x9	// Restore CSR

// Testing CSR 0xa56
	csrr x17, 0xa56	// Read CSR
	li x8, -1
	csrrw x15, 0xa56, x8	// Write all 1s to CSR
	csrrw x15, 0xa56, x0	// Write all 0s to CSR
	csrrs x15, 0xa56, x8	// Set all CSR bits
	csrrc x15, 0xa56, x8	// Clear all CSR bits
	csrrw x15, 0xa56, x17	// Restore CSR

// Testing CSR 0xa57
	csrr x18, 0xa57	// Read CSR
	li x17, -1
	csrrw x21, 0xa57, x17	// Write all 1s to CSR
	csrrw x21, 0xa57, x0	// Write all 0s to CSR
	csrrs x21, 0xa57, x17	// Set all CSR bits
	csrrc x21, 0xa57, x17	// Clear all CSR bits
	csrrw x21, 0xa57, x18	// Restore CSR

// Testing CSR 0xa58
	csrr x26, 0xa58	// Read CSR
	li x17, -1
	csrrw x29, 0xa58, x17	// Write all 1s to CSR
	csrrw x29, 0xa58, x0	// Write all 0s to CSR
	csrrs x29, 0xa58, x17	// Set all CSR bits
	csrrc x29, 0xa58, x17	// Clear all CSR bits
	csrrw x29, 0xa58, x26	// Restore CSR

// Testing CSR 0xa59
	csrr x30, 0xa59	// Read CSR
	li x27, -1
	csrrw x15, 0xa59, x27	// Write all 1s to CSR
	csrrw x15, 0xa59, x0	// Write all 0s to CSR
	csrrs x15, 0xa59, x27	// Set all CSR bits
	csrrc x15, 0xa59, x27	// Clear all CSR bits
	csrrw x15, 0xa59, x30	// Restore CSR

// Testing CSR 0xa5a
	csrr x11, 0xa5a	// Read CSR
	li x23, -1
	csrrw x5, 0xa5a, x23	// Write all 1s to CSR
	csrrw x5, 0xa5a, x0	// Write all 0s to CSR
	csrrs x5, 0xa5a, x23	// Set all CSR bits
	csrrc x5, 0xa5a, x23	// Clear all CSR bits
	csrrw x5, 0xa5a, x11	// Restore CSR

// Testing CSR 0xa5b
	csrr x8, 0xa5b	// Read CSR
	li x23, -1
	csrrw x1, 0xa5b, x23	// Write all 1s to CSR
	csrrw x1, 0xa5b, x0	// Write all 0s to CSR
	csrrs x1, 0xa5b, x23	// Set all CSR bits
	csrrc x1, 0xa5b, x23	// Clear all CSR bits
	csrrw x1, 0xa5b, x8	// Restore CSR

// Testing CSR 0xa5c
	csrr x24, 0xa5c	// Read CSR
	li x23, -1
	csrrw x20, 0xa5c, x23	// Write all 1s to CSR
	csrrw x20, 0xa5c, x0	// Write all 0s to CSR
	csrrs x20, 0xa5c, x23	// Set all CSR bits
	csrrc x20, 0xa5c, x23	// Clear all CSR bits
	csrrw x20, 0xa5c, x24	// Restore CSR

// Testing CSR 0xa5d
	csrr x1, 0xa5d	// Read CSR
	li x22, -1
	csrrw x28, 0xa5d, x22	// Write all 1s to CSR
	csrrw x28, 0xa5d, x0	// Write all 0s to CSR
	csrrs x28, 0xa5d, x22	// Set all CSR bits
	csrrc x28, 0xa5d, x22	// Clear all CSR bits
	csrrw x28, 0xa5d, x1	// Restore CSR

// Testing CSR 0xa5e
	csrr x21, 0xa5e	// Read CSR
	li x14, -1
	csrrw x16, 0xa5e, x14	// Write all 1s to CSR
	csrrw x16, 0xa5e, x0	// Write all 0s to CSR
	csrrs x16, 0xa5e, x14	// Set all CSR bits
	csrrc x16, 0xa5e, x14	// Clear all CSR bits
	csrrw x16, 0xa5e, x21	// Restore CSR

// Testing CSR 0xa5f
	csrr x22, 0xa5f	// Read CSR
	li x6, -1
	csrrw x28, 0xa5f, x6	// Write all 1s to CSR
	csrrw x28, 0xa5f, x0	// Write all 0s to CSR
	csrrs x28, 0xa5f, x6	// Set all CSR bits
	csrrc x28, 0xa5f, x6	// Clear all CSR bits
	csrrw x28, 0xa5f, x22	// Restore CSR

// Testing CSR 0xa60
	csrr x15, 0xa60	// Read CSR
	li x11, -1
	csrrw x27, 0xa60, x11	// Write all 1s to CSR
	csrrw x27, 0xa60, x0	// Write all 0s to CSR
	csrrs x27, 0xa60, x11	// Set all CSR bits
	csrrc x27, 0xa60, x11	// Clear all CSR bits
	csrrw x27, 0xa60, x15	// Restore CSR

// Testing CSR 0xa61
	csrr x7, 0xa61	// Read CSR
	li x19, -1
	csrrw x22, 0xa61, x19	// Write all 1s to CSR
	csrrw x22, 0xa61, x0	// Write all 0s to CSR
	csrrs x22, 0xa61, x19	// Set all CSR bits
	csrrc x22, 0xa61, x19	// Clear all CSR bits
	csrrw x22, 0xa61, x7	// Restore CSR

// Testing CSR 0xa62
	csrr x6, 0xa62	// Read CSR
	li x3, -1
	csrrw x21, 0xa62, x3	// Write all 1s to CSR
	csrrw x21, 0xa62, x0	// Write all 0s to CSR
	csrrs x21, 0xa62, x3	// Set all CSR bits
	csrrc x21, 0xa62, x3	// Clear all CSR bits
	csrrw x21, 0xa62, x6	// Restore CSR

// Testing CSR 0xa63
	csrr x3, 0xa63	// Read CSR
	li x15, -1
	csrrw x11, 0xa63, x15	// Write all 1s to CSR
	csrrw x11, 0xa63, x0	// Write all 0s to CSR
	csrrs x11, 0xa63, x15	// Set all CSR bits
	csrrc x11, 0xa63, x15	// Clear all CSR bits
	csrrw x11, 0xa63, x3	// Restore CSR

// Testing CSR 0xa64
	csrr x25, 0xa64	// Read CSR
	li x31, -1
	csrrw x12, 0xa64, x31	// Write all 1s to CSR
	csrrw x12, 0xa64, x0	// Write all 0s to CSR
	csrrs x12, 0xa64, x31	// Set all CSR bits
	csrrc x12, 0xa64, x31	// Clear all CSR bits
	csrrw x12, 0xa64, x25	// Restore CSR

// Testing CSR 0xa65
	csrr x13, 0xa65	// Read CSR
	li x30, -1
	csrrw x8, 0xa65, x30	// Write all 1s to CSR
	csrrw x8, 0xa65, x0	// Write all 0s to CSR
	csrrs x8, 0xa65, x30	// Set all CSR bits
	csrrc x8, 0xa65, x30	// Clear all CSR bits
	csrrw x8, 0xa65, x13	// Restore CSR

// Testing CSR 0xa66
	csrr x24, 0xa66	// Read CSR
	li x8, -1
	csrrw x12, 0xa66, x8	// Write all 1s to CSR
	csrrw x12, 0xa66, x0	// Write all 0s to CSR
	csrrs x12, 0xa66, x8	// Set all CSR bits
	csrrc x12, 0xa66, x8	// Clear all CSR bits
	csrrw x12, 0xa66, x24	// Restore CSR

// Testing CSR 0xa67
	csrr x14, 0xa67	// Read CSR
	li x29, -1
	csrrw x2, 0xa67, x29	// Write all 1s to CSR
	csrrw x2, 0xa67, x0	// Write all 0s to CSR
	csrrs x2, 0xa67, x29	// Set all CSR bits
	csrrc x2, 0xa67, x29	// Clear all CSR bits
	csrrw x2, 0xa67, x14	// Restore CSR

// Testing CSR 0xa68
	csrr x23, 0xa68	// Read CSR
	li x20, -1
	csrrw x13, 0xa68, x20	// Write all 1s to CSR
	csrrw x13, 0xa68, x0	// Write all 0s to CSR
	csrrs x13, 0xa68, x20	// Set all CSR bits
	csrrc x13, 0xa68, x20	// Clear all CSR bits
	csrrw x13, 0xa68, x23	// Restore CSR

// Testing CSR 0xa69
	csrr x1, 0xa69	// Read CSR
	li x20, -1
	csrrw x3, 0xa69, x20	// Write all 1s to CSR
	csrrw x3, 0xa69, x0	// Write all 0s to CSR
	csrrs x3, 0xa69, x20	// Set all CSR bits
	csrrc x3, 0xa69, x20	// Clear all CSR bits
	csrrw x3, 0xa69, x1	// Restore CSR

// Testing CSR 0xa6a
	csrr x19, 0xa6a	// Read CSR
	li x6, -1
	csrrw x14, 0xa6a, x6	// Write all 1s to CSR
	csrrw x14, 0xa6a, x0	// Write all 0s to CSR
	csrrs x14, 0xa6a, x6	// Set all CSR bits
	csrrc x14, 0xa6a, x6	// Clear all CSR bits
	csrrw x14, 0xa6a, x19	// Restore CSR

// Testing CSR 0xa6b
	csrr x14, 0xa6b	// Read CSR
	li x6, -1
	csrrw x15, 0xa6b, x6	// Write all 1s to CSR
	csrrw x15, 0xa6b, x0	// Write all 0s to CSR
	csrrs x15, 0xa6b, x6	// Set all CSR bits
	csrrc x15, 0xa6b, x6	// Clear all CSR bits
	csrrw x15, 0xa6b, x14	// Restore CSR

// Testing CSR 0xa6c
	csrr x7, 0xa6c	// Read CSR
	li x31, -1
	csrrw x6, 0xa6c, x31	// Write all 1s to CSR
	csrrw x6, 0xa6c, x0	// Write all 0s to CSR
	csrrs x6, 0xa6c, x31	// Set all CSR bits
	csrrc x6, 0xa6c, x31	// Clear all CSR bits
	csrrw x6, 0xa6c, x7	// Restore CSR

// Testing CSR 0xa6d
	csrr x22, 0xa6d	// Read CSR
	li x19, -1
	csrrw x31, 0xa6d, x19	// Write all 1s to CSR
	csrrw x31, 0xa6d, x0	// Write all 0s to CSR
	csrrs x31, 0xa6d, x19	// Set all CSR bits
	csrrc x31, 0xa6d, x19	// Clear all CSR bits
	csrrw x31, 0xa6d, x22	// Restore CSR

// Testing CSR 0xa6e
	csrr x4, 0xa6e	// Read CSR
	li x5, -1
	csrrw x27, 0xa6e, x5	// Write all 1s to CSR
	csrrw x27, 0xa6e, x0	// Write all 0s to CSR
	csrrs x27, 0xa6e, x5	// Set all CSR bits
	csrrc x27, 0xa6e, x5	// Clear all CSR bits
	csrrw x27, 0xa6e, x4	// Restore CSR

// Testing CSR 0xa6f
	csrr x14, 0xa6f	// Read CSR
	li x27, -1
	csrrw x19, 0xa6f, x27	// Write all 1s to CSR
	csrrw x19, 0xa6f, x0	// Write all 0s to CSR
	csrrs x19, 0xa6f, x27	// Set all CSR bits
	csrrc x19, 0xa6f, x27	// Clear all CSR bits
	csrrw x19, 0xa6f, x14	// Restore CSR

// Testing CSR 0xa70
	csrr x28, 0xa70	// Read CSR
	li x9, -1
	csrrw x4, 0xa70, x9	// Write all 1s to CSR
	csrrw x4, 0xa70, x0	// Write all 0s to CSR
	csrrs x4, 0xa70, x9	// Set all CSR bits
	csrrc x4, 0xa70, x9	// Clear all CSR bits
	csrrw x4, 0xa70, x28	// Restore CSR

// Testing CSR 0xa71
	csrr x4, 0xa71	// Read CSR
	li x21, -1
	csrrw x25, 0xa71, x21	// Write all 1s to CSR
	csrrw x25, 0xa71, x0	// Write all 0s to CSR
	csrrs x25, 0xa71, x21	// Set all CSR bits
	csrrc x25, 0xa71, x21	// Clear all CSR bits
	csrrw x25, 0xa71, x4	// Restore CSR

// Testing CSR 0xa72
	csrr x16, 0xa72	// Read CSR
	li x21, -1
	csrrw x17, 0xa72, x21	// Write all 1s to CSR
	csrrw x17, 0xa72, x0	// Write all 0s to CSR
	csrrs x17, 0xa72, x21	// Set all CSR bits
	csrrc x17, 0xa72, x21	// Clear all CSR bits
	csrrw x17, 0xa72, x16	// Restore CSR

// Testing CSR 0xa73
	csrr x4, 0xa73	// Read CSR
	li x29, -1
	csrrw x1, 0xa73, x29	// Write all 1s to CSR
	csrrw x1, 0xa73, x0	// Write all 0s to CSR
	csrrs x1, 0xa73, x29	// Set all CSR bits
	csrrc x1, 0xa73, x29	// Clear all CSR bits
	csrrw x1, 0xa73, x4	// Restore CSR

// Testing CSR 0xa74
	csrr x23, 0xa74	// Read CSR
	li x29, -1
	csrrw x14, 0xa74, x29	// Write all 1s to CSR
	csrrw x14, 0xa74, x0	// Write all 0s to CSR
	csrrs x14, 0xa74, x29	// Set all CSR bits
	csrrc x14, 0xa74, x29	// Clear all CSR bits
	csrrw x14, 0xa74, x23	// Restore CSR

// Testing CSR 0xa75
	csrr x31, 0xa75	// Read CSR
	li x5, -1
	csrrw x29, 0xa75, x5	// Write all 1s to CSR
	csrrw x29, 0xa75, x0	// Write all 0s to CSR
	csrrs x29, 0xa75, x5	// Set all CSR bits
	csrrc x29, 0xa75, x5	// Clear all CSR bits
	csrrw x29, 0xa75, x31	// Restore CSR

// Testing CSR 0xa76
	csrr x6, 0xa76	// Read CSR
	li x26, -1
	csrrw x8, 0xa76, x26	// Write all 1s to CSR
	csrrw x8, 0xa76, x0	// Write all 0s to CSR
	csrrs x8, 0xa76, x26	// Set all CSR bits
	csrrc x8, 0xa76, x26	// Clear all CSR bits
	csrrw x8, 0xa76, x6	// Restore CSR

// Testing CSR 0xa77
	csrr x13, 0xa77	// Read CSR
	li x15, -1
	csrrw x23, 0xa77, x15	// Write all 1s to CSR
	csrrw x23, 0xa77, x0	// Write all 0s to CSR
	csrrs x23, 0xa77, x15	// Set all CSR bits
	csrrc x23, 0xa77, x15	// Clear all CSR bits
	csrrw x23, 0xa77, x13	// Restore CSR

// Testing CSR 0xa78
	csrr x19, 0xa78	// Read CSR
	li x23, -1
	csrrw x8, 0xa78, x23	// Write all 1s to CSR
	csrrw x8, 0xa78, x0	// Write all 0s to CSR
	csrrs x8, 0xa78, x23	// Set all CSR bits
	csrrc x8, 0xa78, x23	// Clear all CSR bits
	csrrw x8, 0xa78, x19	// Restore CSR

// Testing CSR 0xa79
	csrr x5, 0xa79	// Read CSR
	li x20, -1
	csrrw x19, 0xa79, x20	// Write all 1s to CSR
	csrrw x19, 0xa79, x0	// Write all 0s to CSR
	csrrs x19, 0xa79, x20	// Set all CSR bits
	csrrc x19, 0xa79, x20	// Clear all CSR bits
	csrrw x19, 0xa79, x5	// Restore CSR

// Testing CSR 0xa7a
	csrr x13, 0xa7a	// Read CSR
	li x10, -1
	csrrw x22, 0xa7a, x10	// Write all 1s to CSR
	csrrw x22, 0xa7a, x0	// Write all 0s to CSR
	csrrs x22, 0xa7a, x10	// Set all CSR bits
	csrrc x22, 0xa7a, x10	// Clear all CSR bits
	csrrw x22, 0xa7a, x13	// Restore CSR

// Testing CSR 0xa7b
	csrr x11, 0xa7b	// Read CSR
	li x29, -1
	csrrw x21, 0xa7b, x29	// Write all 1s to CSR
	csrrw x21, 0xa7b, x0	// Write all 0s to CSR
	csrrs x21, 0xa7b, x29	// Set all CSR bits
	csrrc x21, 0xa7b, x29	// Clear all CSR bits
	csrrw x21, 0xa7b, x11	// Restore CSR

// Testing CSR 0xa7c
	csrr x5, 0xa7c	// Read CSR
	li x15, -1
	csrrw x20, 0xa7c, x15	// Write all 1s to CSR
	csrrw x20, 0xa7c, x0	// Write all 0s to CSR
	csrrs x20, 0xa7c, x15	// Set all CSR bits
	csrrc x20, 0xa7c, x15	// Clear all CSR bits
	csrrw x20, 0xa7c, x5	// Restore CSR

// Testing CSR 0xa7d
	csrr x8, 0xa7d	// Read CSR
	li x17, -1
	csrrw x25, 0xa7d, x17	// Write all 1s to CSR
	csrrw x25, 0xa7d, x0	// Write all 0s to CSR
	csrrs x25, 0xa7d, x17	// Set all CSR bits
	csrrc x25, 0xa7d, x17	// Clear all CSR bits
	csrrw x25, 0xa7d, x8	// Restore CSR

// Testing CSR 0xa7e
	csrr x6, 0xa7e	// Read CSR
	li x23, -1
	csrrw x18, 0xa7e, x23	// Write all 1s to CSR
	csrrw x18, 0xa7e, x0	// Write all 0s to CSR
	csrrs x18, 0xa7e, x23	// Set all CSR bits
	csrrc x18, 0xa7e, x23	// Clear all CSR bits
	csrrw x18, 0xa7e, x6	// Restore CSR

// Testing CSR 0xa7f
	csrr x7, 0xa7f	// Read CSR
	li x3, -1
	csrrw x25, 0xa7f, x3	// Write all 1s to CSR
	csrrw x25, 0xa7f, x0	// Write all 0s to CSR
	csrrs x25, 0xa7f, x3	// Set all CSR bits
	csrrc x25, 0xa7f, x3	// Clear all CSR bits
	csrrw x25, 0xa7f, x7	// Restore CSR

// Testing CSR 0xa80
	csrr x18, 0xa80	// Read CSR
	li x6, -1
	csrrw x2, 0xa80, x6	// Write all 1s to CSR
	csrrw x2, 0xa80, x0	// Write all 0s to CSR
	csrrs x2, 0xa80, x6	// Set all CSR bits
	csrrc x2, 0xa80, x6	// Clear all CSR bits
	csrrw x2, 0xa80, x18	// Restore CSR

// Testing CSR 0xa81
	csrr x13, 0xa81	// Read CSR
	li x25, -1
	csrrw x27, 0xa81, x25	// Write all 1s to CSR
	csrrw x27, 0xa81, x0	// Write all 0s to CSR
	csrrs x27, 0xa81, x25	// Set all CSR bits
	csrrc x27, 0xa81, x25	// Clear all CSR bits
	csrrw x27, 0xa81, x13	// Restore CSR

// Testing CSR 0xa82
	csrr x16, 0xa82	// Read CSR
	li x10, -1
	csrrw x8, 0xa82, x10	// Write all 1s to CSR
	csrrw x8, 0xa82, x0	// Write all 0s to CSR
	csrrs x8, 0xa82, x10	// Set all CSR bits
	csrrc x8, 0xa82, x10	// Clear all CSR bits
	csrrw x8, 0xa82, x16	// Restore CSR

// Testing CSR 0xa83
	csrr x22, 0xa83	// Read CSR
	li x30, -1
	csrrw x5, 0xa83, x30	// Write all 1s to CSR
	csrrw x5, 0xa83, x0	// Write all 0s to CSR
	csrrs x5, 0xa83, x30	// Set all CSR bits
	csrrc x5, 0xa83, x30	// Clear all CSR bits
	csrrw x5, 0xa83, x22	// Restore CSR

// Testing CSR 0xa84
	csrr x9, 0xa84	// Read CSR
	li x13, -1
	csrrw x3, 0xa84, x13	// Write all 1s to CSR
	csrrw x3, 0xa84, x0	// Write all 0s to CSR
	csrrs x3, 0xa84, x13	// Set all CSR bits
	csrrc x3, 0xa84, x13	// Clear all CSR bits
	csrrw x3, 0xa84, x9	// Restore CSR

// Testing CSR 0xa85
	csrr x18, 0xa85	// Read CSR
	li x21, -1
	csrrw x27, 0xa85, x21	// Write all 1s to CSR
	csrrw x27, 0xa85, x0	// Write all 0s to CSR
	csrrs x27, 0xa85, x21	// Set all CSR bits
	csrrc x27, 0xa85, x21	// Clear all CSR bits
	csrrw x27, 0xa85, x18	// Restore CSR

// Testing CSR 0xa86
	csrr x9, 0xa86	// Read CSR
	li x13, -1
	csrrw x16, 0xa86, x13	// Write all 1s to CSR
	csrrw x16, 0xa86, x0	// Write all 0s to CSR
	csrrs x16, 0xa86, x13	// Set all CSR bits
	csrrc x16, 0xa86, x13	// Clear all CSR bits
	csrrw x16, 0xa86, x9	// Restore CSR

// Testing CSR 0xa87
	csrr x4, 0xa87	// Read CSR
	li x3, -1
	csrrw x27, 0xa87, x3	// Write all 1s to CSR
	csrrw x27, 0xa87, x0	// Write all 0s to CSR
	csrrs x27, 0xa87, x3	// Set all CSR bits
	csrrc x27, 0xa87, x3	// Clear all CSR bits
	csrrw x27, 0xa87, x4	// Restore CSR

// Testing CSR 0xa88
	csrr x27, 0xa88	// Read CSR
	li x1, -1
	csrrw x3, 0xa88, x1	// Write all 1s to CSR
	csrrw x3, 0xa88, x0	// Write all 0s to CSR
	csrrs x3, 0xa88, x1	// Set all CSR bits
	csrrc x3, 0xa88, x1	// Clear all CSR bits
	csrrw x3, 0xa88, x27	// Restore CSR

// Testing CSR 0xa89
	csrr x19, 0xa89	// Read CSR
	li x12, -1
	csrrw x18, 0xa89, x12	// Write all 1s to CSR
	csrrw x18, 0xa89, x0	// Write all 0s to CSR
	csrrs x18, 0xa89, x12	// Set all CSR bits
	csrrc x18, 0xa89, x12	// Clear all CSR bits
	csrrw x18, 0xa89, x19	// Restore CSR

// Testing CSR 0xa8a
	csrr x25, 0xa8a	// Read CSR
	li x11, -1
	csrrw x2, 0xa8a, x11	// Write all 1s to CSR
	csrrw x2, 0xa8a, x0	// Write all 0s to CSR
	csrrs x2, 0xa8a, x11	// Set all CSR bits
	csrrc x2, 0xa8a, x11	// Clear all CSR bits
	csrrw x2, 0xa8a, x25	// Restore CSR

// Testing CSR 0xa8b
	csrr x14, 0xa8b	// Read CSR
	li x22, -1
	csrrw x20, 0xa8b, x22	// Write all 1s to CSR
	csrrw x20, 0xa8b, x0	// Write all 0s to CSR
	csrrs x20, 0xa8b, x22	// Set all CSR bits
	csrrc x20, 0xa8b, x22	// Clear all CSR bits
	csrrw x20, 0xa8b, x14	// Restore CSR

// Testing CSR 0xa8c
	csrr x18, 0xa8c	// Read CSR
	li x25, -1
	csrrw x7, 0xa8c, x25	// Write all 1s to CSR
	csrrw x7, 0xa8c, x0	// Write all 0s to CSR
	csrrs x7, 0xa8c, x25	// Set all CSR bits
	csrrc x7, 0xa8c, x25	// Clear all CSR bits
	csrrw x7, 0xa8c, x18	// Restore CSR

// Testing CSR 0xa8d
	csrr x17, 0xa8d	// Read CSR
	li x28, -1
	csrrw x7, 0xa8d, x28	// Write all 1s to CSR
	csrrw x7, 0xa8d, x0	// Write all 0s to CSR
	csrrs x7, 0xa8d, x28	// Set all CSR bits
	csrrc x7, 0xa8d, x28	// Clear all CSR bits
	csrrw x7, 0xa8d, x17	// Restore CSR

// Testing CSR 0xa8e
	csrr x27, 0xa8e	// Read CSR
	li x1, -1
	csrrw x23, 0xa8e, x1	// Write all 1s to CSR
	csrrw x23, 0xa8e, x0	// Write all 0s to CSR
	csrrs x23, 0xa8e, x1	// Set all CSR bits
	csrrc x23, 0xa8e, x1	// Clear all CSR bits
	csrrw x23, 0xa8e, x27	// Restore CSR

// Testing CSR 0xa8f
	csrr x20, 0xa8f	// Read CSR
	li x8, -1
	csrrw x2, 0xa8f, x8	// Write all 1s to CSR
	csrrw x2, 0xa8f, x0	// Write all 0s to CSR
	csrrs x2, 0xa8f, x8	// Set all CSR bits
	csrrc x2, 0xa8f, x8	// Clear all CSR bits
	csrrw x2, 0xa8f, x20	// Restore CSR

// Testing CSR 0xa90
	csrr x14, 0xa90	// Read CSR
	li x21, -1
	csrrw x20, 0xa90, x21	// Write all 1s to CSR
	csrrw x20, 0xa90, x0	// Write all 0s to CSR
	csrrs x20, 0xa90, x21	// Set all CSR bits
	csrrc x20, 0xa90, x21	// Clear all CSR bits
	csrrw x20, 0xa90, x14	// Restore CSR

// Testing CSR 0xa91
	csrr x13, 0xa91	// Read CSR
	li x19, -1
	csrrw x8, 0xa91, x19	// Write all 1s to CSR
	csrrw x8, 0xa91, x0	// Write all 0s to CSR
	csrrs x8, 0xa91, x19	// Set all CSR bits
	csrrc x8, 0xa91, x19	// Clear all CSR bits
	csrrw x8, 0xa91, x13	// Restore CSR

// Testing CSR 0xa92
	csrr x7, 0xa92	// Read CSR
	li x31, -1
	csrrw x1, 0xa92, x31	// Write all 1s to CSR
	csrrw x1, 0xa92, x0	// Write all 0s to CSR
	csrrs x1, 0xa92, x31	// Set all CSR bits
	csrrc x1, 0xa92, x31	// Clear all CSR bits
	csrrw x1, 0xa92, x7	// Restore CSR

// Testing CSR 0xa93
	csrr x29, 0xa93	// Read CSR
	li x5, -1
	csrrw x22, 0xa93, x5	// Write all 1s to CSR
	csrrw x22, 0xa93, x0	// Write all 0s to CSR
	csrrs x22, 0xa93, x5	// Set all CSR bits
	csrrc x22, 0xa93, x5	// Clear all CSR bits
	csrrw x22, 0xa93, x29	// Restore CSR

// Testing CSR 0xa94
	csrr x4, 0xa94	// Read CSR
	li x12, -1
	csrrw x8, 0xa94, x12	// Write all 1s to CSR
	csrrw x8, 0xa94, x0	// Write all 0s to CSR
	csrrs x8, 0xa94, x12	// Set all CSR bits
	csrrc x8, 0xa94, x12	// Clear all CSR bits
	csrrw x8, 0xa94, x4	// Restore CSR

// Testing CSR 0xa95
	csrr x13, 0xa95	// Read CSR
	li x28, -1
	csrrw x10, 0xa95, x28	// Write all 1s to CSR
	csrrw x10, 0xa95, x0	// Write all 0s to CSR
	csrrs x10, 0xa95, x28	// Set all CSR bits
	csrrc x10, 0xa95, x28	// Clear all CSR bits
	csrrw x10, 0xa95, x13	// Restore CSR

// Testing CSR 0xa96
	csrr x17, 0xa96	// Read CSR
	li x29, -1
	csrrw x23, 0xa96, x29	// Write all 1s to CSR
	csrrw x23, 0xa96, x0	// Write all 0s to CSR
	csrrs x23, 0xa96, x29	// Set all CSR bits
	csrrc x23, 0xa96, x29	// Clear all CSR bits
	csrrw x23, 0xa96, x17	// Restore CSR

// Testing CSR 0xa97
	csrr x27, 0xa97	// Read CSR
	li x3, -1
	csrrw x1, 0xa97, x3	// Write all 1s to CSR
	csrrw x1, 0xa97, x0	// Write all 0s to CSR
	csrrs x1, 0xa97, x3	// Set all CSR bits
	csrrc x1, 0xa97, x3	// Clear all CSR bits
	csrrw x1, 0xa97, x27	// Restore CSR

// Testing CSR 0xa98
	csrr x31, 0xa98	// Read CSR
	li x1, -1
	csrrw x4, 0xa98, x1	// Write all 1s to CSR
	csrrw x4, 0xa98, x0	// Write all 0s to CSR
	csrrs x4, 0xa98, x1	// Set all CSR bits
	csrrc x4, 0xa98, x1	// Clear all CSR bits
	csrrw x4, 0xa98, x31	// Restore CSR

// Testing CSR 0xa99
	csrr x29, 0xa99	// Read CSR
	li x23, -1
	csrrw x1, 0xa99, x23	// Write all 1s to CSR
	csrrw x1, 0xa99, x0	// Write all 0s to CSR
	csrrs x1, 0xa99, x23	// Set all CSR bits
	csrrc x1, 0xa99, x23	// Clear all CSR bits
	csrrw x1, 0xa99, x29	// Restore CSR

// Testing CSR 0xa9a
	csrr x13, 0xa9a	// Read CSR
	li x18, -1
	csrrw x25, 0xa9a, x18	// Write all 1s to CSR
	csrrw x25, 0xa9a, x0	// Write all 0s to CSR
	csrrs x25, 0xa9a, x18	// Set all CSR bits
	csrrc x25, 0xa9a, x18	// Clear all CSR bits
	csrrw x25, 0xa9a, x13	// Restore CSR

// Testing CSR 0xa9b
	csrr x18, 0xa9b	// Read CSR
	li x17, -1
	csrrw x1, 0xa9b, x17	// Write all 1s to CSR
	csrrw x1, 0xa9b, x0	// Write all 0s to CSR
	csrrs x1, 0xa9b, x17	// Set all CSR bits
	csrrc x1, 0xa9b, x17	// Clear all CSR bits
	csrrw x1, 0xa9b, x18	// Restore CSR

// Testing CSR 0xa9c
	csrr x19, 0xa9c	// Read CSR
	li x10, -1
	csrrw x21, 0xa9c, x10	// Write all 1s to CSR
	csrrw x21, 0xa9c, x0	// Write all 0s to CSR
	csrrs x21, 0xa9c, x10	// Set all CSR bits
	csrrc x21, 0xa9c, x10	// Clear all CSR bits
	csrrw x21, 0xa9c, x19	// Restore CSR

// Testing CSR 0xa9d
	csrr x28, 0xa9d	// Read CSR
	li x19, -1
	csrrw x10, 0xa9d, x19	// Write all 1s to CSR
	csrrw x10, 0xa9d, x0	// Write all 0s to CSR
	csrrs x10, 0xa9d, x19	// Set all CSR bits
	csrrc x10, 0xa9d, x19	// Clear all CSR bits
	csrrw x10, 0xa9d, x28	// Restore CSR

// Testing CSR 0xa9e
	csrr x20, 0xa9e	// Read CSR
	li x8, -1
	csrrw x5, 0xa9e, x8	// Write all 1s to CSR
	csrrw x5, 0xa9e, x0	// Write all 0s to CSR
	csrrs x5, 0xa9e, x8	// Set all CSR bits
	csrrc x5, 0xa9e, x8	// Clear all CSR bits
	csrrw x5, 0xa9e, x20	// Restore CSR

// Testing CSR 0xa9f
	csrr x17, 0xa9f	// Read CSR
	li x11, -1
	csrrw x7, 0xa9f, x11	// Write all 1s to CSR
	csrrw x7, 0xa9f, x0	// Write all 0s to CSR
	csrrs x7, 0xa9f, x11	// Set all CSR bits
	csrrc x7, 0xa9f, x11	// Clear all CSR bits
	csrrw x7, 0xa9f, x17	// Restore CSR

// Testing CSR 0xaa0
	csrr x1, 0xaa0	// Read CSR
	li x30, -1
	csrrw x5, 0xaa0, x30	// Write all 1s to CSR
	csrrw x5, 0xaa0, x0	// Write all 0s to CSR
	csrrs x5, 0xaa0, x30	// Set all CSR bits
	csrrc x5, 0xaa0, x30	// Clear all CSR bits
	csrrw x5, 0xaa0, x1	// Restore CSR

// Testing CSR 0xaa1
	csrr x8, 0xaa1	// Read CSR
	li x11, -1
	csrrw x27, 0xaa1, x11	// Write all 1s to CSR
	csrrw x27, 0xaa1, x0	// Write all 0s to CSR
	csrrs x27, 0xaa1, x11	// Set all CSR bits
	csrrc x27, 0xaa1, x11	// Clear all CSR bits
	csrrw x27, 0xaa1, x8	// Restore CSR

// Testing CSR 0xaa2
	csrr x10, 0xaa2	// Read CSR
	li x20, -1
	csrrw x4, 0xaa2, x20	// Write all 1s to CSR
	csrrw x4, 0xaa2, x0	// Write all 0s to CSR
	csrrs x4, 0xaa2, x20	// Set all CSR bits
	csrrc x4, 0xaa2, x20	// Clear all CSR bits
	csrrw x4, 0xaa2, x10	// Restore CSR

// Testing CSR 0xaa3
	csrr x18, 0xaa3	// Read CSR
	li x13, -1
	csrrw x1, 0xaa3, x13	// Write all 1s to CSR
	csrrw x1, 0xaa3, x0	// Write all 0s to CSR
	csrrs x1, 0xaa3, x13	// Set all CSR bits
	csrrc x1, 0xaa3, x13	// Clear all CSR bits
	csrrw x1, 0xaa3, x18	// Restore CSR

// Testing CSR 0xaa4
	csrr x21, 0xaa4	// Read CSR
	li x12, -1
	csrrw x4, 0xaa4, x12	// Write all 1s to CSR
	csrrw x4, 0xaa4, x0	// Write all 0s to CSR
	csrrs x4, 0xaa4, x12	// Set all CSR bits
	csrrc x4, 0xaa4, x12	// Clear all CSR bits
	csrrw x4, 0xaa4, x21	// Restore CSR

// Testing CSR 0xaa5
	csrr x24, 0xaa5	// Read CSR
	li x19, -1
	csrrw x7, 0xaa5, x19	// Write all 1s to CSR
	csrrw x7, 0xaa5, x0	// Write all 0s to CSR
	csrrs x7, 0xaa5, x19	// Set all CSR bits
	csrrc x7, 0xaa5, x19	// Clear all CSR bits
	csrrw x7, 0xaa5, x24	// Restore CSR

// Testing CSR 0xaa6
	csrr x13, 0xaa6	// Read CSR
	li x20, -1
	csrrw x6, 0xaa6, x20	// Write all 1s to CSR
	csrrw x6, 0xaa6, x0	// Write all 0s to CSR
	csrrs x6, 0xaa6, x20	// Set all CSR bits
	csrrc x6, 0xaa6, x20	// Clear all CSR bits
	csrrw x6, 0xaa6, x13	// Restore CSR

// Testing CSR 0xaa7
	csrr x7, 0xaa7	// Read CSR
	li x9, -1
	csrrw x29, 0xaa7, x9	// Write all 1s to CSR
	csrrw x29, 0xaa7, x0	// Write all 0s to CSR
	csrrs x29, 0xaa7, x9	// Set all CSR bits
	csrrc x29, 0xaa7, x9	// Clear all CSR bits
	csrrw x29, 0xaa7, x7	// Restore CSR

// Testing CSR 0xaa8
	csrr x9, 0xaa8	// Read CSR
	li x24, -1
	csrrw x6, 0xaa8, x24	// Write all 1s to CSR
	csrrw x6, 0xaa8, x0	// Write all 0s to CSR
	csrrs x6, 0xaa8, x24	// Set all CSR bits
	csrrc x6, 0xaa8, x24	// Clear all CSR bits
	csrrw x6, 0xaa8, x9	// Restore CSR

// Testing CSR 0xaa9
	csrr x9, 0xaa9	// Read CSR
	li x8, -1
	csrrw x26, 0xaa9, x8	// Write all 1s to CSR
	csrrw x26, 0xaa9, x0	// Write all 0s to CSR
	csrrs x26, 0xaa9, x8	// Set all CSR bits
	csrrc x26, 0xaa9, x8	// Clear all CSR bits
	csrrw x26, 0xaa9, x9	// Restore CSR

// Testing CSR 0xaaa
	csrr x29, 0xaaa	// Read CSR
	li x11, -1
	csrrw x24, 0xaaa, x11	// Write all 1s to CSR
	csrrw x24, 0xaaa, x0	// Write all 0s to CSR
	csrrs x24, 0xaaa, x11	// Set all CSR bits
	csrrc x24, 0xaaa, x11	// Clear all CSR bits
	csrrw x24, 0xaaa, x29	// Restore CSR

// Testing CSR 0xaab
	csrr x22, 0xaab	// Read CSR
	li x20, -1
	csrrw x17, 0xaab, x20	// Write all 1s to CSR
	csrrw x17, 0xaab, x0	// Write all 0s to CSR
	csrrs x17, 0xaab, x20	// Set all CSR bits
	csrrc x17, 0xaab, x20	// Clear all CSR bits
	csrrw x17, 0xaab, x22	// Restore CSR

// Testing CSR 0xaac
	csrr x27, 0xaac	// Read CSR
	li x10, -1
	csrrw x19, 0xaac, x10	// Write all 1s to CSR
	csrrw x19, 0xaac, x0	// Write all 0s to CSR
	csrrs x19, 0xaac, x10	// Set all CSR bits
	csrrc x19, 0xaac, x10	// Clear all CSR bits
	csrrw x19, 0xaac, x27	// Restore CSR

// Testing CSR 0xaad
	csrr x21, 0xaad	// Read CSR
	li x3, -1
	csrrw x15, 0xaad, x3	// Write all 1s to CSR
	csrrw x15, 0xaad, x0	// Write all 0s to CSR
	csrrs x15, 0xaad, x3	// Set all CSR bits
	csrrc x15, 0xaad, x3	// Clear all CSR bits
	csrrw x15, 0xaad, x21	// Restore CSR

// Testing CSR 0xaae
	csrr x27, 0xaae	// Read CSR
	li x1, -1
	csrrw x8, 0xaae, x1	// Write all 1s to CSR
	csrrw x8, 0xaae, x0	// Write all 0s to CSR
	csrrs x8, 0xaae, x1	// Set all CSR bits
	csrrc x8, 0xaae, x1	// Clear all CSR bits
	csrrw x8, 0xaae, x27	// Restore CSR

// Testing CSR 0xaaf
	csrr x27, 0xaaf	// Read CSR
	li x23, -1
	csrrw x15, 0xaaf, x23	// Write all 1s to CSR
	csrrw x15, 0xaaf, x0	// Write all 0s to CSR
	csrrs x15, 0xaaf, x23	// Set all CSR bits
	csrrc x15, 0xaaf, x23	// Clear all CSR bits
	csrrw x15, 0xaaf, x27	// Restore CSR

// Testing CSR 0xab0
	csrr x25, 0xab0	// Read CSR
	li x30, -1
	csrrw x5, 0xab0, x30	// Write all 1s to CSR
	csrrw x5, 0xab0, x0	// Write all 0s to CSR
	csrrs x5, 0xab0, x30	// Set all CSR bits
	csrrc x5, 0xab0, x30	// Clear all CSR bits
	csrrw x5, 0xab0, x25	// Restore CSR

// Testing CSR 0xab1
	csrr x23, 0xab1	// Read CSR
	li x31, -1
	csrrw x6, 0xab1, x31	// Write all 1s to CSR
	csrrw x6, 0xab1, x0	// Write all 0s to CSR
	csrrs x6, 0xab1, x31	// Set all CSR bits
	csrrc x6, 0xab1, x31	// Clear all CSR bits
	csrrw x6, 0xab1, x23	// Restore CSR

// Testing CSR 0xab2
	csrr x12, 0xab2	// Read CSR
	li x18, -1
	csrrw x24, 0xab2, x18	// Write all 1s to CSR
	csrrw x24, 0xab2, x0	// Write all 0s to CSR
	csrrs x24, 0xab2, x18	// Set all CSR bits
	csrrc x24, 0xab2, x18	// Clear all CSR bits
	csrrw x24, 0xab2, x12	// Restore CSR

// Testing CSR 0xab3
	csrr x31, 0xab3	// Read CSR
	li x15, -1
	csrrw x6, 0xab3, x15	// Write all 1s to CSR
	csrrw x6, 0xab3, x0	// Write all 0s to CSR
	csrrs x6, 0xab3, x15	// Set all CSR bits
	csrrc x6, 0xab3, x15	// Clear all CSR bits
	csrrw x6, 0xab3, x31	// Restore CSR

// Testing CSR 0xab4
	csrr x2, 0xab4	// Read CSR
	li x14, -1
	csrrw x29, 0xab4, x14	// Write all 1s to CSR
	csrrw x29, 0xab4, x0	// Write all 0s to CSR
	csrrs x29, 0xab4, x14	// Set all CSR bits
	csrrc x29, 0xab4, x14	// Clear all CSR bits
	csrrw x29, 0xab4, x2	// Restore CSR

// Testing CSR 0xab5
	csrr x16, 0xab5	// Read CSR
	li x23, -1
	csrrw x20, 0xab5, x23	// Write all 1s to CSR
	csrrw x20, 0xab5, x0	// Write all 0s to CSR
	csrrs x20, 0xab5, x23	// Set all CSR bits
	csrrc x20, 0xab5, x23	// Clear all CSR bits
	csrrw x20, 0xab5, x16	// Restore CSR

// Testing CSR 0xab6
	csrr x20, 0xab6	// Read CSR
	li x10, -1
	csrrw x18, 0xab6, x10	// Write all 1s to CSR
	csrrw x18, 0xab6, x0	// Write all 0s to CSR
	csrrs x18, 0xab6, x10	// Set all CSR bits
	csrrc x18, 0xab6, x10	// Clear all CSR bits
	csrrw x18, 0xab6, x20	// Restore CSR

// Testing CSR 0xab7
	csrr x6, 0xab7	// Read CSR
	li x24, -1
	csrrw x9, 0xab7, x24	// Write all 1s to CSR
	csrrw x9, 0xab7, x0	// Write all 0s to CSR
	csrrs x9, 0xab7, x24	// Set all CSR bits
	csrrc x9, 0xab7, x24	// Clear all CSR bits
	csrrw x9, 0xab7, x6	// Restore CSR

// Testing CSR 0xab8
	csrr x6, 0xab8	// Read CSR
	li x11, -1
	csrrw x26, 0xab8, x11	// Write all 1s to CSR
	csrrw x26, 0xab8, x0	// Write all 0s to CSR
	csrrs x26, 0xab8, x11	// Set all CSR bits
	csrrc x26, 0xab8, x11	// Clear all CSR bits
	csrrw x26, 0xab8, x6	// Restore CSR

// Testing CSR 0xab9
	csrr x28, 0xab9	// Read CSR
	li x23, -1
	csrrw x22, 0xab9, x23	// Write all 1s to CSR
	csrrw x22, 0xab9, x0	// Write all 0s to CSR
	csrrs x22, 0xab9, x23	// Set all CSR bits
	csrrc x22, 0xab9, x23	// Clear all CSR bits
	csrrw x22, 0xab9, x28	// Restore CSR

// Testing CSR 0xaba
	csrr x17, 0xaba	// Read CSR
	li x25, -1
	csrrw x12, 0xaba, x25	// Write all 1s to CSR
	csrrw x12, 0xaba, x0	// Write all 0s to CSR
	csrrs x12, 0xaba, x25	// Set all CSR bits
	csrrc x12, 0xaba, x25	// Clear all CSR bits
	csrrw x12, 0xaba, x17	// Restore CSR

// Testing CSR 0xabb
	csrr x16, 0xabb	// Read CSR
	li x27, -1
	csrrw x4, 0xabb, x27	// Write all 1s to CSR
	csrrw x4, 0xabb, x0	// Write all 0s to CSR
	csrrs x4, 0xabb, x27	// Set all CSR bits
	csrrc x4, 0xabb, x27	// Clear all CSR bits
	csrrw x4, 0xabb, x16	// Restore CSR

// Testing CSR 0xabc
	csrr x21, 0xabc	// Read CSR
	li x7, -1
	csrrw x22, 0xabc, x7	// Write all 1s to CSR
	csrrw x22, 0xabc, x0	// Write all 0s to CSR
	csrrs x22, 0xabc, x7	// Set all CSR bits
	csrrc x22, 0xabc, x7	// Clear all CSR bits
	csrrw x22, 0xabc, x21	// Restore CSR

// Testing CSR 0xabd
	csrr x10, 0xabd	// Read CSR
	li x20, -1
	csrrw x27, 0xabd, x20	// Write all 1s to CSR
	csrrw x27, 0xabd, x0	// Write all 0s to CSR
	csrrs x27, 0xabd, x20	// Set all CSR bits
	csrrc x27, 0xabd, x20	// Clear all CSR bits
	csrrw x27, 0xabd, x10	// Restore CSR

// Testing CSR 0xabe
	csrr x7, 0xabe	// Read CSR
	li x30, -1
	csrrw x15, 0xabe, x30	// Write all 1s to CSR
	csrrw x15, 0xabe, x0	// Write all 0s to CSR
	csrrs x15, 0xabe, x30	// Set all CSR bits
	csrrc x15, 0xabe, x30	// Clear all CSR bits
	csrrw x15, 0xabe, x7	// Restore CSR

// Testing CSR 0xabf
	csrr x17, 0xabf	// Read CSR
	li x2, -1
	csrrw x12, 0xabf, x2	// Write all 1s to CSR
	csrrw x12, 0xabf, x0	// Write all 0s to CSR
	csrrs x12, 0xabf, x2	// Set all CSR bits
	csrrc x12, 0xabf, x2	// Clear all CSR bits
	csrrw x12, 0xabf, x17	// Restore CSR

// Testing CSR 0xac0
	csrr x30, 0xac0	// Read CSR
	li x29, -1
	csrrw x9, 0xac0, x29	// Write all 1s to CSR
	csrrw x9, 0xac0, x0	// Write all 0s to CSR
	csrrs x9, 0xac0, x29	// Set all CSR bits
	csrrc x9, 0xac0, x29	// Clear all CSR bits
	csrrw x9, 0xac0, x30	// Restore CSR

// Testing CSR 0xac1
	csrr x13, 0xac1	// Read CSR
	li x7, -1
	csrrw x24, 0xac1, x7	// Write all 1s to CSR
	csrrw x24, 0xac1, x0	// Write all 0s to CSR
	csrrs x24, 0xac1, x7	// Set all CSR bits
	csrrc x24, 0xac1, x7	// Clear all CSR bits
	csrrw x24, 0xac1, x13	// Restore CSR

// Testing CSR 0xac2
	csrr x28, 0xac2	// Read CSR
	li x4, -1
	csrrw x1, 0xac2, x4	// Write all 1s to CSR
	csrrw x1, 0xac2, x0	// Write all 0s to CSR
	csrrs x1, 0xac2, x4	// Set all CSR bits
	csrrc x1, 0xac2, x4	// Clear all CSR bits
	csrrw x1, 0xac2, x28	// Restore CSR

// Testing CSR 0xac3
	csrr x24, 0xac3	// Read CSR
	li x20, -1
	csrrw x23, 0xac3, x20	// Write all 1s to CSR
	csrrw x23, 0xac3, x0	// Write all 0s to CSR
	csrrs x23, 0xac3, x20	// Set all CSR bits
	csrrc x23, 0xac3, x20	// Clear all CSR bits
	csrrw x23, 0xac3, x24	// Restore CSR

// Testing CSR 0xac4
	csrr x31, 0xac4	// Read CSR
	li x6, -1
	csrrw x4, 0xac4, x6	// Write all 1s to CSR
	csrrw x4, 0xac4, x0	// Write all 0s to CSR
	csrrs x4, 0xac4, x6	// Set all CSR bits
	csrrc x4, 0xac4, x6	// Clear all CSR bits
	csrrw x4, 0xac4, x31	// Restore CSR

// Testing CSR 0xac5
	csrr x17, 0xac5	// Read CSR
	li x23, -1
	csrrw x3, 0xac5, x23	// Write all 1s to CSR
	csrrw x3, 0xac5, x0	// Write all 0s to CSR
	csrrs x3, 0xac5, x23	// Set all CSR bits
	csrrc x3, 0xac5, x23	// Clear all CSR bits
	csrrw x3, 0xac5, x17	// Restore CSR

// Testing CSR 0xac6
	csrr x16, 0xac6	// Read CSR
	li x18, -1
	csrrw x3, 0xac6, x18	// Write all 1s to CSR
	csrrw x3, 0xac6, x0	// Write all 0s to CSR
	csrrs x3, 0xac6, x18	// Set all CSR bits
	csrrc x3, 0xac6, x18	// Clear all CSR bits
	csrrw x3, 0xac6, x16	// Restore CSR

// Testing CSR 0xac7
	csrr x24, 0xac7	// Read CSR
	li x28, -1
	csrrw x23, 0xac7, x28	// Write all 1s to CSR
	csrrw x23, 0xac7, x0	// Write all 0s to CSR
	csrrs x23, 0xac7, x28	// Set all CSR bits
	csrrc x23, 0xac7, x28	// Clear all CSR bits
	csrrw x23, 0xac7, x24	// Restore CSR

// Testing CSR 0xac8
	csrr x3, 0xac8	// Read CSR
	li x9, -1
	csrrw x31, 0xac8, x9	// Write all 1s to CSR
	csrrw x31, 0xac8, x0	// Write all 0s to CSR
	csrrs x31, 0xac8, x9	// Set all CSR bits
	csrrc x31, 0xac8, x9	// Clear all CSR bits
	csrrw x31, 0xac8, x3	// Restore CSR

// Testing CSR 0xac9
	csrr x5, 0xac9	// Read CSR
	li x10, -1
	csrrw x18, 0xac9, x10	// Write all 1s to CSR
	csrrw x18, 0xac9, x0	// Write all 0s to CSR
	csrrs x18, 0xac9, x10	// Set all CSR bits
	csrrc x18, 0xac9, x10	// Clear all CSR bits
	csrrw x18, 0xac9, x5	// Restore CSR

// Testing CSR 0xaca
	csrr x4, 0xaca	// Read CSR
	li x2, -1
	csrrw x31, 0xaca, x2	// Write all 1s to CSR
	csrrw x31, 0xaca, x0	// Write all 0s to CSR
	csrrs x31, 0xaca, x2	// Set all CSR bits
	csrrc x31, 0xaca, x2	// Clear all CSR bits
	csrrw x31, 0xaca, x4	// Restore CSR

// Testing CSR 0xacb
	csrr x9, 0xacb	// Read CSR
	li x24, -1
	csrrw x27, 0xacb, x24	// Write all 1s to CSR
	csrrw x27, 0xacb, x0	// Write all 0s to CSR
	csrrs x27, 0xacb, x24	// Set all CSR bits
	csrrc x27, 0xacb, x24	// Clear all CSR bits
	csrrw x27, 0xacb, x9	// Restore CSR

// Testing CSR 0xacc
	csrr x3, 0xacc	// Read CSR
	li x6, -1
	csrrw x30, 0xacc, x6	// Write all 1s to CSR
	csrrw x30, 0xacc, x0	// Write all 0s to CSR
	csrrs x30, 0xacc, x6	// Set all CSR bits
	csrrc x30, 0xacc, x6	// Clear all CSR bits
	csrrw x30, 0xacc, x3	// Restore CSR

// Testing CSR 0xacd
	csrr x8, 0xacd	// Read CSR
	li x21, -1
	csrrw x30, 0xacd, x21	// Write all 1s to CSR
	csrrw x30, 0xacd, x0	// Write all 0s to CSR
	csrrs x30, 0xacd, x21	// Set all CSR bits
	csrrc x30, 0xacd, x21	// Clear all CSR bits
	csrrw x30, 0xacd, x8	// Restore CSR

// Testing CSR 0xace
	csrr x16, 0xace	// Read CSR
	li x27, -1
	csrrw x26, 0xace, x27	// Write all 1s to CSR
	csrrw x26, 0xace, x0	// Write all 0s to CSR
	csrrs x26, 0xace, x27	// Set all CSR bits
	csrrc x26, 0xace, x27	// Clear all CSR bits
	csrrw x26, 0xace, x16	// Restore CSR

// Testing CSR 0xacf
	csrr x8, 0xacf	// Read CSR
	li x26, -1
	csrrw x5, 0xacf, x26	// Write all 1s to CSR
	csrrw x5, 0xacf, x0	// Write all 0s to CSR
	csrrs x5, 0xacf, x26	// Set all CSR bits
	csrrc x5, 0xacf, x26	// Clear all CSR bits
	csrrw x5, 0xacf, x8	// Restore CSR

// Testing CSR 0xad0
	csrr x24, 0xad0	// Read CSR
	li x30, -1
	csrrw x20, 0xad0, x30	// Write all 1s to CSR
	csrrw x20, 0xad0, x0	// Write all 0s to CSR
	csrrs x20, 0xad0, x30	// Set all CSR bits
	csrrc x20, 0xad0, x30	// Clear all CSR bits
	csrrw x20, 0xad0, x24	// Restore CSR

// Testing CSR 0xad1
	csrr x12, 0xad1	// Read CSR
	li x1, -1
	csrrw x11, 0xad1, x1	// Write all 1s to CSR
	csrrw x11, 0xad1, x0	// Write all 0s to CSR
	csrrs x11, 0xad1, x1	// Set all CSR bits
	csrrc x11, 0xad1, x1	// Clear all CSR bits
	csrrw x11, 0xad1, x12	// Restore CSR

// Testing CSR 0xad2
	csrr x15, 0xad2	// Read CSR
	li x24, -1
	csrrw x8, 0xad2, x24	// Write all 1s to CSR
	csrrw x8, 0xad2, x0	// Write all 0s to CSR
	csrrs x8, 0xad2, x24	// Set all CSR bits
	csrrc x8, 0xad2, x24	// Clear all CSR bits
	csrrw x8, 0xad2, x15	// Restore CSR

// Testing CSR 0xad3
	csrr x16, 0xad3	// Read CSR
	li x31, -1
	csrrw x26, 0xad3, x31	// Write all 1s to CSR
	csrrw x26, 0xad3, x0	// Write all 0s to CSR
	csrrs x26, 0xad3, x31	// Set all CSR bits
	csrrc x26, 0xad3, x31	// Clear all CSR bits
	csrrw x26, 0xad3, x16	// Restore CSR

// Testing CSR 0xad4
	csrr x30, 0xad4	// Read CSR
	li x8, -1
	csrrw x24, 0xad4, x8	// Write all 1s to CSR
	csrrw x24, 0xad4, x0	// Write all 0s to CSR
	csrrs x24, 0xad4, x8	// Set all CSR bits
	csrrc x24, 0xad4, x8	// Clear all CSR bits
	csrrw x24, 0xad4, x30	// Restore CSR

// Testing CSR 0xad5
	csrr x29, 0xad5	// Read CSR
	li x21, -1
	csrrw x17, 0xad5, x21	// Write all 1s to CSR
	csrrw x17, 0xad5, x0	// Write all 0s to CSR
	csrrs x17, 0xad5, x21	// Set all CSR bits
	csrrc x17, 0xad5, x21	// Clear all CSR bits
	csrrw x17, 0xad5, x29	// Restore CSR

// Testing CSR 0xad6
	csrr x16, 0xad6	// Read CSR
	li x6, -1
	csrrw x17, 0xad6, x6	// Write all 1s to CSR
	csrrw x17, 0xad6, x0	// Write all 0s to CSR
	csrrs x17, 0xad6, x6	// Set all CSR bits
	csrrc x17, 0xad6, x6	// Clear all CSR bits
	csrrw x17, 0xad6, x16	// Restore CSR

// Testing CSR 0xad7
	csrr x7, 0xad7	// Read CSR
	li x1, -1
	csrrw x22, 0xad7, x1	// Write all 1s to CSR
	csrrw x22, 0xad7, x0	// Write all 0s to CSR
	csrrs x22, 0xad7, x1	// Set all CSR bits
	csrrc x22, 0xad7, x1	// Clear all CSR bits
	csrrw x22, 0xad7, x7	// Restore CSR

// Testing CSR 0xad8
	csrr x18, 0xad8	// Read CSR
	li x27, -1
	csrrw x8, 0xad8, x27	// Write all 1s to CSR
	csrrw x8, 0xad8, x0	// Write all 0s to CSR
	csrrs x8, 0xad8, x27	// Set all CSR bits
	csrrc x8, 0xad8, x27	// Clear all CSR bits
	csrrw x8, 0xad8, x18	// Restore CSR

// Testing CSR 0xad9
	csrr x2, 0xad9	// Read CSR
	li x8, -1
	csrrw x29, 0xad9, x8	// Write all 1s to CSR
	csrrw x29, 0xad9, x0	// Write all 0s to CSR
	csrrs x29, 0xad9, x8	// Set all CSR bits
	csrrc x29, 0xad9, x8	// Clear all CSR bits
	csrrw x29, 0xad9, x2	// Restore CSR

// Testing CSR 0xada
	csrr x15, 0xada	// Read CSR
	li x25, -1
	csrrw x10, 0xada, x25	// Write all 1s to CSR
	csrrw x10, 0xada, x0	// Write all 0s to CSR
	csrrs x10, 0xada, x25	// Set all CSR bits
	csrrc x10, 0xada, x25	// Clear all CSR bits
	csrrw x10, 0xada, x15	// Restore CSR

// Testing CSR 0xadb
	csrr x24, 0xadb	// Read CSR
	li x15, -1
	csrrw x7, 0xadb, x15	// Write all 1s to CSR
	csrrw x7, 0xadb, x0	// Write all 0s to CSR
	csrrs x7, 0xadb, x15	// Set all CSR bits
	csrrc x7, 0xadb, x15	// Clear all CSR bits
	csrrw x7, 0xadb, x24	// Restore CSR

// Testing CSR 0xadc
	csrr x20, 0xadc	// Read CSR
	li x24, -1
	csrrw x7, 0xadc, x24	// Write all 1s to CSR
	csrrw x7, 0xadc, x0	// Write all 0s to CSR
	csrrs x7, 0xadc, x24	// Set all CSR bits
	csrrc x7, 0xadc, x24	// Clear all CSR bits
	csrrw x7, 0xadc, x20	// Restore CSR

// Testing CSR 0xadd
	csrr x12, 0xadd	// Read CSR
	li x10, -1
	csrrw x13, 0xadd, x10	// Write all 1s to CSR
	csrrw x13, 0xadd, x0	// Write all 0s to CSR
	csrrs x13, 0xadd, x10	// Set all CSR bits
	csrrc x13, 0xadd, x10	// Clear all CSR bits
	csrrw x13, 0xadd, x12	// Restore CSR

// Testing CSR 0xade
	csrr x29, 0xade	// Read CSR
	li x24, -1
	csrrw x27, 0xade, x24	// Write all 1s to CSR
	csrrw x27, 0xade, x0	// Write all 0s to CSR
	csrrs x27, 0xade, x24	// Set all CSR bits
	csrrc x27, 0xade, x24	// Clear all CSR bits
	csrrw x27, 0xade, x29	// Restore CSR

// Testing CSR 0xadf
	csrr x15, 0xadf	// Read CSR
	li x1, -1
	csrrw x25, 0xadf, x1	// Write all 1s to CSR
	csrrw x25, 0xadf, x0	// Write all 0s to CSR
	csrrs x25, 0xadf, x1	// Set all CSR bits
	csrrc x25, 0xadf, x1	// Clear all CSR bits
	csrrw x25, 0xadf, x15	// Restore CSR

// Testing CSR 0xae0
	csrr x29, 0xae0	// Read CSR
	li x1, -1
	csrrw x17, 0xae0, x1	// Write all 1s to CSR
	csrrw x17, 0xae0, x0	// Write all 0s to CSR
	csrrs x17, 0xae0, x1	// Set all CSR bits
	csrrc x17, 0xae0, x1	// Clear all CSR bits
	csrrw x17, 0xae0, x29	// Restore CSR

// Testing CSR 0xae1
	csrr x24, 0xae1	// Read CSR
	li x8, -1
	csrrw x22, 0xae1, x8	// Write all 1s to CSR
	csrrw x22, 0xae1, x0	// Write all 0s to CSR
	csrrs x22, 0xae1, x8	// Set all CSR bits
	csrrc x22, 0xae1, x8	// Clear all CSR bits
	csrrw x22, 0xae1, x24	// Restore CSR

// Testing CSR 0xae2
	csrr x13, 0xae2	// Read CSR
	li x28, -1
	csrrw x26, 0xae2, x28	// Write all 1s to CSR
	csrrw x26, 0xae2, x0	// Write all 0s to CSR
	csrrs x26, 0xae2, x28	// Set all CSR bits
	csrrc x26, 0xae2, x28	// Clear all CSR bits
	csrrw x26, 0xae2, x13	// Restore CSR

// Testing CSR 0xae3
	csrr x27, 0xae3	// Read CSR
	li x29, -1
	csrrw x22, 0xae3, x29	// Write all 1s to CSR
	csrrw x22, 0xae3, x0	// Write all 0s to CSR
	csrrs x22, 0xae3, x29	// Set all CSR bits
	csrrc x22, 0xae3, x29	// Clear all CSR bits
	csrrw x22, 0xae3, x27	// Restore CSR

// Testing CSR 0xae4
	csrr x1, 0xae4	// Read CSR
	li x31, -1
	csrrw x19, 0xae4, x31	// Write all 1s to CSR
	csrrw x19, 0xae4, x0	// Write all 0s to CSR
	csrrs x19, 0xae4, x31	// Set all CSR bits
	csrrc x19, 0xae4, x31	// Clear all CSR bits
	csrrw x19, 0xae4, x1	// Restore CSR

// Testing CSR 0xae5
	csrr x20, 0xae5	// Read CSR
	li x29, -1
	csrrw x19, 0xae5, x29	// Write all 1s to CSR
	csrrw x19, 0xae5, x0	// Write all 0s to CSR
	csrrs x19, 0xae5, x29	// Set all CSR bits
	csrrc x19, 0xae5, x29	// Clear all CSR bits
	csrrw x19, 0xae5, x20	// Restore CSR

// Testing CSR 0xae6
	csrr x6, 0xae6	// Read CSR
	li x21, -1
	csrrw x18, 0xae6, x21	// Write all 1s to CSR
	csrrw x18, 0xae6, x0	// Write all 0s to CSR
	csrrs x18, 0xae6, x21	// Set all CSR bits
	csrrc x18, 0xae6, x21	// Clear all CSR bits
	csrrw x18, 0xae6, x6	// Restore CSR

// Testing CSR 0xae7
	csrr x2, 0xae7	// Read CSR
	li x6, -1
	csrrw x10, 0xae7, x6	// Write all 1s to CSR
	csrrw x10, 0xae7, x0	// Write all 0s to CSR
	csrrs x10, 0xae7, x6	// Set all CSR bits
	csrrc x10, 0xae7, x6	// Clear all CSR bits
	csrrw x10, 0xae7, x2	// Restore CSR

// Testing CSR 0xae8
	csrr x28, 0xae8	// Read CSR
	li x5, -1
	csrrw x24, 0xae8, x5	// Write all 1s to CSR
	csrrw x24, 0xae8, x0	// Write all 0s to CSR
	csrrs x24, 0xae8, x5	// Set all CSR bits
	csrrc x24, 0xae8, x5	// Clear all CSR bits
	csrrw x24, 0xae8, x28	// Restore CSR

// Testing CSR 0xae9
	csrr x19, 0xae9	// Read CSR
	li x3, -1
	csrrw x14, 0xae9, x3	// Write all 1s to CSR
	csrrw x14, 0xae9, x0	// Write all 0s to CSR
	csrrs x14, 0xae9, x3	// Set all CSR bits
	csrrc x14, 0xae9, x3	// Clear all CSR bits
	csrrw x14, 0xae9, x19	// Restore CSR

// Testing CSR 0xaea
	csrr x31, 0xaea	// Read CSR
	li x19, -1
	csrrw x21, 0xaea, x19	// Write all 1s to CSR
	csrrw x21, 0xaea, x0	// Write all 0s to CSR
	csrrs x21, 0xaea, x19	// Set all CSR bits
	csrrc x21, 0xaea, x19	// Clear all CSR bits
	csrrw x21, 0xaea, x31	// Restore CSR

// Testing CSR 0xaeb
	csrr x18, 0xaeb	// Read CSR
	li x26, -1
	csrrw x14, 0xaeb, x26	// Write all 1s to CSR
	csrrw x14, 0xaeb, x0	// Write all 0s to CSR
	csrrs x14, 0xaeb, x26	// Set all CSR bits
	csrrc x14, 0xaeb, x26	// Clear all CSR bits
	csrrw x14, 0xaeb, x18	// Restore CSR

// Testing CSR 0xaec
	csrr x27, 0xaec	// Read CSR
	li x17, -1
	csrrw x13, 0xaec, x17	// Write all 1s to CSR
	csrrw x13, 0xaec, x0	// Write all 0s to CSR
	csrrs x13, 0xaec, x17	// Set all CSR bits
	csrrc x13, 0xaec, x17	// Clear all CSR bits
	csrrw x13, 0xaec, x27	// Restore CSR

// Testing CSR 0xaed
	csrr x6, 0xaed	// Read CSR
	li x3, -1
	csrrw x29, 0xaed, x3	// Write all 1s to CSR
	csrrw x29, 0xaed, x0	// Write all 0s to CSR
	csrrs x29, 0xaed, x3	// Set all CSR bits
	csrrc x29, 0xaed, x3	// Clear all CSR bits
	csrrw x29, 0xaed, x6	// Restore CSR

// Testing CSR 0xaee
	csrr x16, 0xaee	// Read CSR
	li x8, -1
	csrrw x24, 0xaee, x8	// Write all 1s to CSR
	csrrw x24, 0xaee, x0	// Write all 0s to CSR
	csrrs x24, 0xaee, x8	// Set all CSR bits
	csrrc x24, 0xaee, x8	// Clear all CSR bits
	csrrw x24, 0xaee, x16	// Restore CSR

// Testing CSR 0xaef
	csrr x29, 0xaef	// Read CSR
	li x5, -1
	csrrw x2, 0xaef, x5	// Write all 1s to CSR
	csrrw x2, 0xaef, x0	// Write all 0s to CSR
	csrrs x2, 0xaef, x5	// Set all CSR bits
	csrrc x2, 0xaef, x5	// Clear all CSR bits
	csrrw x2, 0xaef, x29	// Restore CSR

// Testing CSR 0xaf0
	csrr x30, 0xaf0	// Read CSR
	li x20, -1
	csrrw x12, 0xaf0, x20	// Write all 1s to CSR
	csrrw x12, 0xaf0, x0	// Write all 0s to CSR
	csrrs x12, 0xaf0, x20	// Set all CSR bits
	csrrc x12, 0xaf0, x20	// Clear all CSR bits
	csrrw x12, 0xaf0, x30	// Restore CSR

// Testing CSR 0xaf1
	csrr x24, 0xaf1	// Read CSR
	li x16, -1
	csrrw x3, 0xaf1, x16	// Write all 1s to CSR
	csrrw x3, 0xaf1, x0	// Write all 0s to CSR
	csrrs x3, 0xaf1, x16	// Set all CSR bits
	csrrc x3, 0xaf1, x16	// Clear all CSR bits
	csrrw x3, 0xaf1, x24	// Restore CSR

// Testing CSR 0xaf2
	csrr x24, 0xaf2	// Read CSR
	li x13, -1
	csrrw x2, 0xaf2, x13	// Write all 1s to CSR
	csrrw x2, 0xaf2, x0	// Write all 0s to CSR
	csrrs x2, 0xaf2, x13	// Set all CSR bits
	csrrc x2, 0xaf2, x13	// Clear all CSR bits
	csrrw x2, 0xaf2, x24	// Restore CSR

// Testing CSR 0xaf3
	csrr x13, 0xaf3	// Read CSR
	li x2, -1
	csrrw x15, 0xaf3, x2	// Write all 1s to CSR
	csrrw x15, 0xaf3, x0	// Write all 0s to CSR
	csrrs x15, 0xaf3, x2	// Set all CSR bits
	csrrc x15, 0xaf3, x2	// Clear all CSR bits
	csrrw x15, 0xaf3, x13	// Restore CSR

// Testing CSR 0xaf4
	csrr x18, 0xaf4	// Read CSR
	li x29, -1
	csrrw x3, 0xaf4, x29	// Write all 1s to CSR
	csrrw x3, 0xaf4, x0	// Write all 0s to CSR
	csrrs x3, 0xaf4, x29	// Set all CSR bits
	csrrc x3, 0xaf4, x29	// Clear all CSR bits
	csrrw x3, 0xaf4, x18	// Restore CSR

// Testing CSR 0xaf5
	csrr x14, 0xaf5	// Read CSR
	li x22, -1
	csrrw x2, 0xaf5, x22	// Write all 1s to CSR
	csrrw x2, 0xaf5, x0	// Write all 0s to CSR
	csrrs x2, 0xaf5, x22	// Set all CSR bits
	csrrc x2, 0xaf5, x22	// Clear all CSR bits
	csrrw x2, 0xaf5, x14	// Restore CSR

// Testing CSR 0xaf6
	csrr x1, 0xaf6	// Read CSR
	li x9, -1
	csrrw x4, 0xaf6, x9	// Write all 1s to CSR
	csrrw x4, 0xaf6, x0	// Write all 0s to CSR
	csrrs x4, 0xaf6, x9	// Set all CSR bits
	csrrc x4, 0xaf6, x9	// Clear all CSR bits
	csrrw x4, 0xaf6, x1	// Restore CSR

// Testing CSR 0xaf7
	csrr x13, 0xaf7	// Read CSR
	li x14, -1
	csrrw x24, 0xaf7, x14	// Write all 1s to CSR
	csrrw x24, 0xaf7, x0	// Write all 0s to CSR
	csrrs x24, 0xaf7, x14	// Set all CSR bits
	csrrc x24, 0xaf7, x14	// Clear all CSR bits
	csrrw x24, 0xaf7, x13	// Restore CSR

// Testing CSR 0xaf8
	csrr x24, 0xaf8	// Read CSR
	li x20, -1
	csrrw x22, 0xaf8, x20	// Write all 1s to CSR
	csrrw x22, 0xaf8, x0	// Write all 0s to CSR
	csrrs x22, 0xaf8, x20	// Set all CSR bits
	csrrc x22, 0xaf8, x20	// Clear all CSR bits
	csrrw x22, 0xaf8, x24	// Restore CSR

// Testing CSR 0xaf9
	csrr x14, 0xaf9	// Read CSR
	li x2, -1
	csrrw x27, 0xaf9, x2	// Write all 1s to CSR
	csrrw x27, 0xaf9, x0	// Write all 0s to CSR
	csrrs x27, 0xaf9, x2	// Set all CSR bits
	csrrc x27, 0xaf9, x2	// Clear all CSR bits
	csrrw x27, 0xaf9, x14	// Restore CSR

// Testing CSR 0xafa
	csrr x4, 0xafa	// Read CSR
	li x23, -1
	csrrw x22, 0xafa, x23	// Write all 1s to CSR
	csrrw x22, 0xafa, x0	// Write all 0s to CSR
	csrrs x22, 0xafa, x23	// Set all CSR bits
	csrrc x22, 0xafa, x23	// Clear all CSR bits
	csrrw x22, 0xafa, x4	// Restore CSR

// Testing CSR 0xafb
	csrr x6, 0xafb	// Read CSR
	li x14, -1
	csrrw x3, 0xafb, x14	// Write all 1s to CSR
	csrrw x3, 0xafb, x0	// Write all 0s to CSR
	csrrs x3, 0xafb, x14	// Set all CSR bits
	csrrc x3, 0xafb, x14	// Clear all CSR bits
	csrrw x3, 0xafb, x6	// Restore CSR

// Testing CSR 0xafc
	csrr x23, 0xafc	// Read CSR
	li x3, -1
	csrrw x20, 0xafc, x3	// Write all 1s to CSR
	csrrw x20, 0xafc, x0	// Write all 0s to CSR
	csrrs x20, 0xafc, x3	// Set all CSR bits
	csrrc x20, 0xafc, x3	// Clear all CSR bits
	csrrw x20, 0xafc, x23	// Restore CSR

// Testing CSR 0xafd
	csrr x15, 0xafd	// Read CSR
	li x28, -1
	csrrw x29, 0xafd, x28	// Write all 1s to CSR
	csrrw x29, 0xafd, x0	// Write all 0s to CSR
	csrrs x29, 0xafd, x28	// Set all CSR bits
	csrrc x29, 0xafd, x28	// Clear all CSR bits
	csrrw x29, 0xafd, x15	// Restore CSR

// Testing CSR 0xafe
	csrr x2, 0xafe	// Read CSR
	li x28, -1
	csrrw x26, 0xafe, x28	// Write all 1s to CSR
	csrrw x26, 0xafe, x0	// Write all 0s to CSR
	csrrs x26, 0xafe, x28	// Set all CSR bits
	csrrc x26, 0xafe, x28	// Clear all CSR bits
	csrrw x26, 0xafe, x2	// Restore CSR

// Testing CSR 0xaff
	csrr x13, 0xaff	// Read CSR
	li x19, -1
	csrrw x12, 0xaff, x19	// Write all 1s to CSR
	csrrw x12, 0xaff, x0	// Write all 0s to CSR
	csrrs x12, 0xaff, x19	// Set all CSR bits
	csrrc x12, 0xaff, x19	// Clear all CSR bits
	csrrw x12, 0xaff, x13	// Restore CSR

// Testing CSR 0xb00
	csrr x20, 0xb00	// Read CSR
	li x2, -1
	csrrw x13, 0xb00, x2	// Write all 1s to CSR
	csrrw x13, 0xb00, x0	// Write all 0s to CSR
	csrrs x13, 0xb00, x2	// Set all CSR bits
	csrrc x13, 0xb00, x2	// Clear all CSR bits
	csrrw x13, 0xb00, x20	// Restore CSR

// Testing CSR 0xb01
	csrr x9, 0xb01	// Read CSR
	li x7, -1
	csrrw x29, 0xb01, x7	// Write all 1s to CSR
	csrrw x29, 0xb01, x0	// Write all 0s to CSR
	csrrs x29, 0xb01, x7	// Set all CSR bits
	csrrc x29, 0xb01, x7	// Clear all CSR bits
	csrrw x29, 0xb01, x9	// Restore CSR

// Testing CSR 0xb02
	csrr x5, 0xb02	// Read CSR
	li x8, -1
	csrrw x9, 0xb02, x8	// Write all 1s to CSR
	csrrw x9, 0xb02, x0	// Write all 0s to CSR
	csrrs x9, 0xb02, x8	// Set all CSR bits
	csrrc x9, 0xb02, x8	// Clear all CSR bits
	csrrw x9, 0xb02, x5	// Restore CSR

// Testing CSR 0xb03
	csrr x26, 0xb03	// Read CSR
	li x17, -1
	csrrw x2, 0xb03, x17	// Write all 1s to CSR
	csrrw x2, 0xb03, x0	// Write all 0s to CSR
	csrrs x2, 0xb03, x17	// Set all CSR bits
	csrrc x2, 0xb03, x17	// Clear all CSR bits
	csrrw x2, 0xb03, x26	// Restore CSR

// Testing CSR 0xb04
	csrr x19, 0xb04	// Read CSR
	li x1, -1
	csrrw x2, 0xb04, x1	// Write all 1s to CSR
	csrrw x2, 0xb04, x0	// Write all 0s to CSR
	csrrs x2, 0xb04, x1	// Set all CSR bits
	csrrc x2, 0xb04, x1	// Clear all CSR bits
	csrrw x2, 0xb04, x19	// Restore CSR

// Testing CSR 0xb05
	csrr x11, 0xb05	// Read CSR
	li x1, -1
	csrrw x14, 0xb05, x1	// Write all 1s to CSR
	csrrw x14, 0xb05, x0	// Write all 0s to CSR
	csrrs x14, 0xb05, x1	// Set all CSR bits
	csrrc x14, 0xb05, x1	// Clear all CSR bits
	csrrw x14, 0xb05, x11	// Restore CSR

// Testing CSR 0xb06
	csrr x13, 0xb06	// Read CSR
	li x31, -1
	csrrw x15, 0xb06, x31	// Write all 1s to CSR
	csrrw x15, 0xb06, x0	// Write all 0s to CSR
	csrrs x15, 0xb06, x31	// Set all CSR bits
	csrrc x15, 0xb06, x31	// Clear all CSR bits
	csrrw x15, 0xb06, x13	// Restore CSR

// Testing CSR 0xb07
	csrr x14, 0xb07	// Read CSR
	li x13, -1
	csrrw x19, 0xb07, x13	// Write all 1s to CSR
	csrrw x19, 0xb07, x0	// Write all 0s to CSR
	csrrs x19, 0xb07, x13	// Set all CSR bits
	csrrc x19, 0xb07, x13	// Clear all CSR bits
	csrrw x19, 0xb07, x14	// Restore CSR

// Testing CSR 0xb08
	csrr x26, 0xb08	// Read CSR
	li x31, -1
	csrrw x11, 0xb08, x31	// Write all 1s to CSR
	csrrw x11, 0xb08, x0	// Write all 0s to CSR
	csrrs x11, 0xb08, x31	// Set all CSR bits
	csrrc x11, 0xb08, x31	// Clear all CSR bits
	csrrw x11, 0xb08, x26	// Restore CSR

// Testing CSR 0xb09
	csrr x23, 0xb09	// Read CSR
	li x9, -1
	csrrw x6, 0xb09, x9	// Write all 1s to CSR
	csrrw x6, 0xb09, x0	// Write all 0s to CSR
	csrrs x6, 0xb09, x9	// Set all CSR bits
	csrrc x6, 0xb09, x9	// Clear all CSR bits
	csrrw x6, 0xb09, x23	// Restore CSR

// Testing CSR 0xb0a
	csrr x10, 0xb0a	// Read CSR
	li x9, -1
	csrrw x17, 0xb0a, x9	// Write all 1s to CSR
	csrrw x17, 0xb0a, x0	// Write all 0s to CSR
	csrrs x17, 0xb0a, x9	// Set all CSR bits
	csrrc x17, 0xb0a, x9	// Clear all CSR bits
	csrrw x17, 0xb0a, x10	// Restore CSR

// Testing CSR 0xb0b
	csrr x7, 0xb0b	// Read CSR
	li x10, -1
	csrrw x18, 0xb0b, x10	// Write all 1s to CSR
	csrrw x18, 0xb0b, x0	// Write all 0s to CSR
	csrrs x18, 0xb0b, x10	// Set all CSR bits
	csrrc x18, 0xb0b, x10	// Clear all CSR bits
	csrrw x18, 0xb0b, x7	// Restore CSR

// Testing CSR 0xb0c
	csrr x26, 0xb0c	// Read CSR
	li x7, -1
	csrrw x16, 0xb0c, x7	// Write all 1s to CSR
	csrrw x16, 0xb0c, x0	// Write all 0s to CSR
	csrrs x16, 0xb0c, x7	// Set all CSR bits
	csrrc x16, 0xb0c, x7	// Clear all CSR bits
	csrrw x16, 0xb0c, x26	// Restore CSR

// Testing CSR 0xb0d
	csrr x31, 0xb0d	// Read CSR
	li x9, -1
	csrrw x4, 0xb0d, x9	// Write all 1s to CSR
	csrrw x4, 0xb0d, x0	// Write all 0s to CSR
	csrrs x4, 0xb0d, x9	// Set all CSR bits
	csrrc x4, 0xb0d, x9	// Clear all CSR bits
	csrrw x4, 0xb0d, x31	// Restore CSR

// Testing CSR 0xb0e
	csrr x4, 0xb0e	// Read CSR
	li x8, -1
	csrrw x25, 0xb0e, x8	// Write all 1s to CSR
	csrrw x25, 0xb0e, x0	// Write all 0s to CSR
	csrrs x25, 0xb0e, x8	// Set all CSR bits
	csrrc x25, 0xb0e, x8	// Clear all CSR bits
	csrrw x25, 0xb0e, x4	// Restore CSR

// Testing CSR 0xb0f
	csrr x24, 0xb0f	// Read CSR
	li x8, -1
	csrrw x21, 0xb0f, x8	// Write all 1s to CSR
	csrrw x21, 0xb0f, x0	// Write all 0s to CSR
	csrrs x21, 0xb0f, x8	// Set all CSR bits
	csrrc x21, 0xb0f, x8	// Clear all CSR bits
	csrrw x21, 0xb0f, x24	// Restore CSR

// Testing CSR 0xb10
	csrr x2, 0xb10	// Read CSR
	li x4, -1
	csrrw x27, 0xb10, x4	// Write all 1s to CSR
	csrrw x27, 0xb10, x0	// Write all 0s to CSR
	csrrs x27, 0xb10, x4	// Set all CSR bits
	csrrc x27, 0xb10, x4	// Clear all CSR bits
	csrrw x27, 0xb10, x2	// Restore CSR

// Testing CSR 0xb11
	csrr x23, 0xb11	// Read CSR
	li x11, -1
	csrrw x2, 0xb11, x11	// Write all 1s to CSR
	csrrw x2, 0xb11, x0	// Write all 0s to CSR
	csrrs x2, 0xb11, x11	// Set all CSR bits
	csrrc x2, 0xb11, x11	// Clear all CSR bits
	csrrw x2, 0xb11, x23	// Restore CSR

// Testing CSR 0xb12
	csrr x13, 0xb12	// Read CSR
	li x10, -1
	csrrw x27, 0xb12, x10	// Write all 1s to CSR
	csrrw x27, 0xb12, x0	// Write all 0s to CSR
	csrrs x27, 0xb12, x10	// Set all CSR bits
	csrrc x27, 0xb12, x10	// Clear all CSR bits
	csrrw x27, 0xb12, x13	// Restore CSR

// Testing CSR 0xb13
	csrr x15, 0xb13	// Read CSR
	li x10, -1
	csrrw x8, 0xb13, x10	// Write all 1s to CSR
	csrrw x8, 0xb13, x0	// Write all 0s to CSR
	csrrs x8, 0xb13, x10	// Set all CSR bits
	csrrc x8, 0xb13, x10	// Clear all CSR bits
	csrrw x8, 0xb13, x15	// Restore CSR

// Testing CSR 0xb14
	csrr x1, 0xb14	// Read CSR
	li x19, -1
	csrrw x7, 0xb14, x19	// Write all 1s to CSR
	csrrw x7, 0xb14, x0	// Write all 0s to CSR
	csrrs x7, 0xb14, x19	// Set all CSR bits
	csrrc x7, 0xb14, x19	// Clear all CSR bits
	csrrw x7, 0xb14, x1	// Restore CSR

// Testing CSR 0xb15
	csrr x6, 0xb15	// Read CSR
	li x26, -1
	csrrw x18, 0xb15, x26	// Write all 1s to CSR
	csrrw x18, 0xb15, x0	// Write all 0s to CSR
	csrrs x18, 0xb15, x26	// Set all CSR bits
	csrrc x18, 0xb15, x26	// Clear all CSR bits
	csrrw x18, 0xb15, x6	// Restore CSR

// Testing CSR 0xb16
	csrr x1, 0xb16	// Read CSR
	li x19, -1
	csrrw x14, 0xb16, x19	// Write all 1s to CSR
	csrrw x14, 0xb16, x0	// Write all 0s to CSR
	csrrs x14, 0xb16, x19	// Set all CSR bits
	csrrc x14, 0xb16, x19	// Clear all CSR bits
	csrrw x14, 0xb16, x1	// Restore CSR

// Testing CSR 0xb17
	csrr x6, 0xb17	// Read CSR
	li x16, -1
	csrrw x31, 0xb17, x16	// Write all 1s to CSR
	csrrw x31, 0xb17, x0	// Write all 0s to CSR
	csrrs x31, 0xb17, x16	// Set all CSR bits
	csrrc x31, 0xb17, x16	// Clear all CSR bits
	csrrw x31, 0xb17, x6	// Restore CSR

// Testing CSR 0xb18
	csrr x17, 0xb18	// Read CSR
	li x11, -1
	csrrw x13, 0xb18, x11	// Write all 1s to CSR
	csrrw x13, 0xb18, x0	// Write all 0s to CSR
	csrrs x13, 0xb18, x11	// Set all CSR bits
	csrrc x13, 0xb18, x11	// Clear all CSR bits
	csrrw x13, 0xb18, x17	// Restore CSR

// Testing CSR 0xb19
	csrr x22, 0xb19	// Read CSR
	li x13, -1
	csrrw x28, 0xb19, x13	// Write all 1s to CSR
	csrrw x28, 0xb19, x0	// Write all 0s to CSR
	csrrs x28, 0xb19, x13	// Set all CSR bits
	csrrc x28, 0xb19, x13	// Clear all CSR bits
	csrrw x28, 0xb19, x22	// Restore CSR

// Testing CSR 0xb1a
	csrr x1, 0xb1a	// Read CSR
	li x23, -1
	csrrw x28, 0xb1a, x23	// Write all 1s to CSR
	csrrw x28, 0xb1a, x0	// Write all 0s to CSR
	csrrs x28, 0xb1a, x23	// Set all CSR bits
	csrrc x28, 0xb1a, x23	// Clear all CSR bits
	csrrw x28, 0xb1a, x1	// Restore CSR

// Testing CSR 0xb1b
	csrr x23, 0xb1b	// Read CSR
	li x30, -1
	csrrw x27, 0xb1b, x30	// Write all 1s to CSR
	csrrw x27, 0xb1b, x0	// Write all 0s to CSR
	csrrs x27, 0xb1b, x30	// Set all CSR bits
	csrrc x27, 0xb1b, x30	// Clear all CSR bits
	csrrw x27, 0xb1b, x23	// Restore CSR

// Testing CSR 0xb1c
	csrr x17, 0xb1c	// Read CSR
	li x2, -1
	csrrw x9, 0xb1c, x2	// Write all 1s to CSR
	csrrw x9, 0xb1c, x0	// Write all 0s to CSR
	csrrs x9, 0xb1c, x2	// Set all CSR bits
	csrrc x9, 0xb1c, x2	// Clear all CSR bits
	csrrw x9, 0xb1c, x17	// Restore CSR

// Testing CSR 0xb1d
	csrr x30, 0xb1d	// Read CSR
	li x29, -1
	csrrw x14, 0xb1d, x29	// Write all 1s to CSR
	csrrw x14, 0xb1d, x0	// Write all 0s to CSR
	csrrs x14, 0xb1d, x29	// Set all CSR bits
	csrrc x14, 0xb1d, x29	// Clear all CSR bits
	csrrw x14, 0xb1d, x30	// Restore CSR

// Testing CSR 0xb1e
	csrr x1, 0xb1e	// Read CSR
	li x7, -1
	csrrw x26, 0xb1e, x7	// Write all 1s to CSR
	csrrw x26, 0xb1e, x0	// Write all 0s to CSR
	csrrs x26, 0xb1e, x7	// Set all CSR bits
	csrrc x26, 0xb1e, x7	// Clear all CSR bits
	csrrw x26, 0xb1e, x1	// Restore CSR

// Testing CSR 0xb1f
	csrr x5, 0xb1f	// Read CSR
	li x23, -1
	csrrw x2, 0xb1f, x23	// Write all 1s to CSR
	csrrw x2, 0xb1f, x0	// Write all 0s to CSR
	csrrs x2, 0xb1f, x23	// Set all CSR bits
	csrrc x2, 0xb1f, x23	// Clear all CSR bits
	csrrw x2, 0xb1f, x5	// Restore CSR

// Testing CSR 0xb20
	csrr x12, 0xb20	// Read CSR
	li x10, -1
	csrrw x19, 0xb20, x10	// Write all 1s to CSR
	csrrw x19, 0xb20, x0	// Write all 0s to CSR
	csrrs x19, 0xb20, x10	// Set all CSR bits
	csrrc x19, 0xb20, x10	// Clear all CSR bits
	csrrw x19, 0xb20, x12	// Restore CSR

// Testing CSR 0xb21
	csrr x17, 0xb21	// Read CSR
	li x9, -1
	csrrw x26, 0xb21, x9	// Write all 1s to CSR
	csrrw x26, 0xb21, x0	// Write all 0s to CSR
	csrrs x26, 0xb21, x9	// Set all CSR bits
	csrrc x26, 0xb21, x9	// Clear all CSR bits
	csrrw x26, 0xb21, x17	// Restore CSR

// Testing CSR 0xb22
	csrr x8, 0xb22	// Read CSR
	li x7, -1
	csrrw x6, 0xb22, x7	// Write all 1s to CSR
	csrrw x6, 0xb22, x0	// Write all 0s to CSR
	csrrs x6, 0xb22, x7	// Set all CSR bits
	csrrc x6, 0xb22, x7	// Clear all CSR bits
	csrrw x6, 0xb22, x8	// Restore CSR

// Testing CSR 0xb23
	csrr x13, 0xb23	// Read CSR
	li x10, -1
	csrrw x24, 0xb23, x10	// Write all 1s to CSR
	csrrw x24, 0xb23, x0	// Write all 0s to CSR
	csrrs x24, 0xb23, x10	// Set all CSR bits
	csrrc x24, 0xb23, x10	// Clear all CSR bits
	csrrw x24, 0xb23, x13	// Restore CSR

// Testing CSR 0xb24
	csrr x12, 0xb24	// Read CSR
	li x25, -1
	csrrw x2, 0xb24, x25	// Write all 1s to CSR
	csrrw x2, 0xb24, x0	// Write all 0s to CSR
	csrrs x2, 0xb24, x25	// Set all CSR bits
	csrrc x2, 0xb24, x25	// Clear all CSR bits
	csrrw x2, 0xb24, x12	// Restore CSR

// Testing CSR 0xb25
	csrr x17, 0xb25	// Read CSR
	li x1, -1
	csrrw x5, 0xb25, x1	// Write all 1s to CSR
	csrrw x5, 0xb25, x0	// Write all 0s to CSR
	csrrs x5, 0xb25, x1	// Set all CSR bits
	csrrc x5, 0xb25, x1	// Clear all CSR bits
	csrrw x5, 0xb25, x17	// Restore CSR

// Testing CSR 0xb26
	csrr x19, 0xb26	// Read CSR
	li x15, -1
	csrrw x6, 0xb26, x15	// Write all 1s to CSR
	csrrw x6, 0xb26, x0	// Write all 0s to CSR
	csrrs x6, 0xb26, x15	// Set all CSR bits
	csrrc x6, 0xb26, x15	// Clear all CSR bits
	csrrw x6, 0xb26, x19	// Restore CSR

// Testing CSR 0xb27
	csrr x5, 0xb27	// Read CSR
	li x29, -1
	csrrw x13, 0xb27, x29	// Write all 1s to CSR
	csrrw x13, 0xb27, x0	// Write all 0s to CSR
	csrrs x13, 0xb27, x29	// Set all CSR bits
	csrrc x13, 0xb27, x29	// Clear all CSR bits
	csrrw x13, 0xb27, x5	// Restore CSR

// Testing CSR 0xb28
	csrr x17, 0xb28	// Read CSR
	li x14, -1
	csrrw x20, 0xb28, x14	// Write all 1s to CSR
	csrrw x20, 0xb28, x0	// Write all 0s to CSR
	csrrs x20, 0xb28, x14	// Set all CSR bits
	csrrc x20, 0xb28, x14	// Clear all CSR bits
	csrrw x20, 0xb28, x17	// Restore CSR

// Testing CSR 0xb29
	csrr x19, 0xb29	// Read CSR
	li x21, -1
	csrrw x14, 0xb29, x21	// Write all 1s to CSR
	csrrw x14, 0xb29, x0	// Write all 0s to CSR
	csrrs x14, 0xb29, x21	// Set all CSR bits
	csrrc x14, 0xb29, x21	// Clear all CSR bits
	csrrw x14, 0xb29, x19	// Restore CSR

// Testing CSR 0xb2a
	csrr x6, 0xb2a	// Read CSR
	li x21, -1
	csrrw x5, 0xb2a, x21	// Write all 1s to CSR
	csrrw x5, 0xb2a, x0	// Write all 0s to CSR
	csrrs x5, 0xb2a, x21	// Set all CSR bits
	csrrc x5, 0xb2a, x21	// Clear all CSR bits
	csrrw x5, 0xb2a, x6	// Restore CSR

// Testing CSR 0xb2b
	csrr x11, 0xb2b	// Read CSR
	li x14, -1
	csrrw x28, 0xb2b, x14	// Write all 1s to CSR
	csrrw x28, 0xb2b, x0	// Write all 0s to CSR
	csrrs x28, 0xb2b, x14	// Set all CSR bits
	csrrc x28, 0xb2b, x14	// Clear all CSR bits
	csrrw x28, 0xb2b, x11	// Restore CSR

// Testing CSR 0xb2c
	csrr x31, 0xb2c	// Read CSR
	li x30, -1
	csrrw x25, 0xb2c, x30	// Write all 1s to CSR
	csrrw x25, 0xb2c, x0	// Write all 0s to CSR
	csrrs x25, 0xb2c, x30	// Set all CSR bits
	csrrc x25, 0xb2c, x30	// Clear all CSR bits
	csrrw x25, 0xb2c, x31	// Restore CSR

// Testing CSR 0xb2d
	csrr x3, 0xb2d	// Read CSR
	li x7, -1
	csrrw x23, 0xb2d, x7	// Write all 1s to CSR
	csrrw x23, 0xb2d, x0	// Write all 0s to CSR
	csrrs x23, 0xb2d, x7	// Set all CSR bits
	csrrc x23, 0xb2d, x7	// Clear all CSR bits
	csrrw x23, 0xb2d, x3	// Restore CSR

// Testing CSR 0xb2e
	csrr x6, 0xb2e	// Read CSR
	li x31, -1
	csrrw x4, 0xb2e, x31	// Write all 1s to CSR
	csrrw x4, 0xb2e, x0	// Write all 0s to CSR
	csrrs x4, 0xb2e, x31	// Set all CSR bits
	csrrc x4, 0xb2e, x31	// Clear all CSR bits
	csrrw x4, 0xb2e, x6	// Restore CSR

// Testing CSR 0xb2f
	csrr x2, 0xb2f	// Read CSR
	li x6, -1
	csrrw x14, 0xb2f, x6	// Write all 1s to CSR
	csrrw x14, 0xb2f, x0	// Write all 0s to CSR
	csrrs x14, 0xb2f, x6	// Set all CSR bits
	csrrc x14, 0xb2f, x6	// Clear all CSR bits
	csrrw x14, 0xb2f, x2	// Restore CSR

// Testing CSR 0xb30
	csrr x14, 0xb30	// Read CSR
	li x18, -1
	csrrw x11, 0xb30, x18	// Write all 1s to CSR
	csrrw x11, 0xb30, x0	// Write all 0s to CSR
	csrrs x11, 0xb30, x18	// Set all CSR bits
	csrrc x11, 0xb30, x18	// Clear all CSR bits
	csrrw x11, 0xb30, x14	// Restore CSR

// Testing CSR 0xb31
	csrr x24, 0xb31	// Read CSR
	li x25, -1
	csrrw x13, 0xb31, x25	// Write all 1s to CSR
	csrrw x13, 0xb31, x0	// Write all 0s to CSR
	csrrs x13, 0xb31, x25	// Set all CSR bits
	csrrc x13, 0xb31, x25	// Clear all CSR bits
	csrrw x13, 0xb31, x24	// Restore CSR

// Testing CSR 0xb32
	csrr x16, 0xb32	// Read CSR
	li x8, -1
	csrrw x23, 0xb32, x8	// Write all 1s to CSR
	csrrw x23, 0xb32, x0	// Write all 0s to CSR
	csrrs x23, 0xb32, x8	// Set all CSR bits
	csrrc x23, 0xb32, x8	// Clear all CSR bits
	csrrw x23, 0xb32, x16	// Restore CSR

// Testing CSR 0xb33
	csrr x4, 0xb33	// Read CSR
	li x25, -1
	csrrw x1, 0xb33, x25	// Write all 1s to CSR
	csrrw x1, 0xb33, x0	// Write all 0s to CSR
	csrrs x1, 0xb33, x25	// Set all CSR bits
	csrrc x1, 0xb33, x25	// Clear all CSR bits
	csrrw x1, 0xb33, x4	// Restore CSR

// Testing CSR 0xb34
	csrr x11, 0xb34	// Read CSR
	li x15, -1
	csrrw x6, 0xb34, x15	// Write all 1s to CSR
	csrrw x6, 0xb34, x0	// Write all 0s to CSR
	csrrs x6, 0xb34, x15	// Set all CSR bits
	csrrc x6, 0xb34, x15	// Clear all CSR bits
	csrrw x6, 0xb34, x11	// Restore CSR

// Testing CSR 0xb35
	csrr x13, 0xb35	// Read CSR
	li x27, -1
	csrrw x7, 0xb35, x27	// Write all 1s to CSR
	csrrw x7, 0xb35, x0	// Write all 0s to CSR
	csrrs x7, 0xb35, x27	// Set all CSR bits
	csrrc x7, 0xb35, x27	// Clear all CSR bits
	csrrw x7, 0xb35, x13	// Restore CSR

// Testing CSR 0xb36
	csrr x15, 0xb36	// Read CSR
	li x11, -1
	csrrw x13, 0xb36, x11	// Write all 1s to CSR
	csrrw x13, 0xb36, x0	// Write all 0s to CSR
	csrrs x13, 0xb36, x11	// Set all CSR bits
	csrrc x13, 0xb36, x11	// Clear all CSR bits
	csrrw x13, 0xb36, x15	// Restore CSR

// Testing CSR 0xb37
	csrr x20, 0xb37	// Read CSR
	li x23, -1
	csrrw x7, 0xb37, x23	// Write all 1s to CSR
	csrrw x7, 0xb37, x0	// Write all 0s to CSR
	csrrs x7, 0xb37, x23	// Set all CSR bits
	csrrc x7, 0xb37, x23	// Clear all CSR bits
	csrrw x7, 0xb37, x20	// Restore CSR

// Testing CSR 0xb38
	csrr x28, 0xb38	// Read CSR
	li x30, -1
	csrrw x31, 0xb38, x30	// Write all 1s to CSR
	csrrw x31, 0xb38, x0	// Write all 0s to CSR
	csrrs x31, 0xb38, x30	// Set all CSR bits
	csrrc x31, 0xb38, x30	// Clear all CSR bits
	csrrw x31, 0xb38, x28	// Restore CSR

// Testing CSR 0xb39
	csrr x19, 0xb39	// Read CSR
	li x17, -1
	csrrw x2, 0xb39, x17	// Write all 1s to CSR
	csrrw x2, 0xb39, x0	// Write all 0s to CSR
	csrrs x2, 0xb39, x17	// Set all CSR bits
	csrrc x2, 0xb39, x17	// Clear all CSR bits
	csrrw x2, 0xb39, x19	// Restore CSR

// Testing CSR 0xb3a
	csrr x13, 0xb3a	// Read CSR
	li x27, -1
	csrrw x22, 0xb3a, x27	// Write all 1s to CSR
	csrrw x22, 0xb3a, x0	// Write all 0s to CSR
	csrrs x22, 0xb3a, x27	// Set all CSR bits
	csrrc x22, 0xb3a, x27	// Clear all CSR bits
	csrrw x22, 0xb3a, x13	// Restore CSR

// Testing CSR 0xb3b
	csrr x13, 0xb3b	// Read CSR
	li x19, -1
	csrrw x30, 0xb3b, x19	// Write all 1s to CSR
	csrrw x30, 0xb3b, x0	// Write all 0s to CSR
	csrrs x30, 0xb3b, x19	// Set all CSR bits
	csrrc x30, 0xb3b, x19	// Clear all CSR bits
	csrrw x30, 0xb3b, x13	// Restore CSR

// Testing CSR 0xb3c
	csrr x14, 0xb3c	// Read CSR
	li x25, -1
	csrrw x31, 0xb3c, x25	// Write all 1s to CSR
	csrrw x31, 0xb3c, x0	// Write all 0s to CSR
	csrrs x31, 0xb3c, x25	// Set all CSR bits
	csrrc x31, 0xb3c, x25	// Clear all CSR bits
	csrrw x31, 0xb3c, x14	// Restore CSR

// Testing CSR 0xb3d
	csrr x26, 0xb3d	// Read CSR
	li x30, -1
	csrrw x11, 0xb3d, x30	// Write all 1s to CSR
	csrrw x11, 0xb3d, x0	// Write all 0s to CSR
	csrrs x11, 0xb3d, x30	// Set all CSR bits
	csrrc x11, 0xb3d, x30	// Clear all CSR bits
	csrrw x11, 0xb3d, x26	// Restore CSR

// Testing CSR 0xb3e
	csrr x18, 0xb3e	// Read CSR
	li x21, -1
	csrrw x4, 0xb3e, x21	// Write all 1s to CSR
	csrrw x4, 0xb3e, x0	// Write all 0s to CSR
	csrrs x4, 0xb3e, x21	// Set all CSR bits
	csrrc x4, 0xb3e, x21	// Clear all CSR bits
	csrrw x4, 0xb3e, x18	// Restore CSR

// Testing CSR 0xb3f
	csrr x13, 0xb3f	// Read CSR
	li x28, -1
	csrrw x17, 0xb3f, x28	// Write all 1s to CSR
	csrrw x17, 0xb3f, x0	// Write all 0s to CSR
	csrrs x17, 0xb3f, x28	// Set all CSR bits
	csrrc x17, 0xb3f, x28	// Clear all CSR bits
	csrrw x17, 0xb3f, x13	// Restore CSR

// Testing CSR 0xb40
	csrr x28, 0xb40	// Read CSR
	li x1, -1
	csrrw x13, 0xb40, x1	// Write all 1s to CSR
	csrrw x13, 0xb40, x0	// Write all 0s to CSR
	csrrs x13, 0xb40, x1	// Set all CSR bits
	csrrc x13, 0xb40, x1	// Clear all CSR bits
	csrrw x13, 0xb40, x28	// Restore CSR

// Testing CSR 0xb41
	csrr x5, 0xb41	// Read CSR
	li x3, -1
	csrrw x14, 0xb41, x3	// Write all 1s to CSR
	csrrw x14, 0xb41, x0	// Write all 0s to CSR
	csrrs x14, 0xb41, x3	// Set all CSR bits
	csrrc x14, 0xb41, x3	// Clear all CSR bits
	csrrw x14, 0xb41, x5	// Restore CSR

// Testing CSR 0xb42
	csrr x2, 0xb42	// Read CSR
	li x22, -1
	csrrw x18, 0xb42, x22	// Write all 1s to CSR
	csrrw x18, 0xb42, x0	// Write all 0s to CSR
	csrrs x18, 0xb42, x22	// Set all CSR bits
	csrrc x18, 0xb42, x22	// Clear all CSR bits
	csrrw x18, 0xb42, x2	// Restore CSR

// Testing CSR 0xb43
	csrr x25, 0xb43	// Read CSR
	li x7, -1
	csrrw x18, 0xb43, x7	// Write all 1s to CSR
	csrrw x18, 0xb43, x0	// Write all 0s to CSR
	csrrs x18, 0xb43, x7	// Set all CSR bits
	csrrc x18, 0xb43, x7	// Clear all CSR bits
	csrrw x18, 0xb43, x25	// Restore CSR

// Testing CSR 0xb44
	csrr x26, 0xb44	// Read CSR
	li x5, -1
	csrrw x23, 0xb44, x5	// Write all 1s to CSR
	csrrw x23, 0xb44, x0	// Write all 0s to CSR
	csrrs x23, 0xb44, x5	// Set all CSR bits
	csrrc x23, 0xb44, x5	// Clear all CSR bits
	csrrw x23, 0xb44, x26	// Restore CSR

// Testing CSR 0xb45
	csrr x7, 0xb45	// Read CSR
	li x12, -1
	csrrw x23, 0xb45, x12	// Write all 1s to CSR
	csrrw x23, 0xb45, x0	// Write all 0s to CSR
	csrrs x23, 0xb45, x12	// Set all CSR bits
	csrrc x23, 0xb45, x12	// Clear all CSR bits
	csrrw x23, 0xb45, x7	// Restore CSR

// Testing CSR 0xb46
	csrr x15, 0xb46	// Read CSR
	li x28, -1
	csrrw x13, 0xb46, x28	// Write all 1s to CSR
	csrrw x13, 0xb46, x0	// Write all 0s to CSR
	csrrs x13, 0xb46, x28	// Set all CSR bits
	csrrc x13, 0xb46, x28	// Clear all CSR bits
	csrrw x13, 0xb46, x15	// Restore CSR

// Testing CSR 0xb47
	csrr x16, 0xb47	// Read CSR
	li x11, -1
	csrrw x9, 0xb47, x11	// Write all 1s to CSR
	csrrw x9, 0xb47, x0	// Write all 0s to CSR
	csrrs x9, 0xb47, x11	// Set all CSR bits
	csrrc x9, 0xb47, x11	// Clear all CSR bits
	csrrw x9, 0xb47, x16	// Restore CSR

// Testing CSR 0xb48
	csrr x20, 0xb48	// Read CSR
	li x28, -1
	csrrw x24, 0xb48, x28	// Write all 1s to CSR
	csrrw x24, 0xb48, x0	// Write all 0s to CSR
	csrrs x24, 0xb48, x28	// Set all CSR bits
	csrrc x24, 0xb48, x28	// Clear all CSR bits
	csrrw x24, 0xb48, x20	// Restore CSR

// Testing CSR 0xb49
	csrr x11, 0xb49	// Read CSR
	li x2, -1
	csrrw x21, 0xb49, x2	// Write all 1s to CSR
	csrrw x21, 0xb49, x0	// Write all 0s to CSR
	csrrs x21, 0xb49, x2	// Set all CSR bits
	csrrc x21, 0xb49, x2	// Clear all CSR bits
	csrrw x21, 0xb49, x11	// Restore CSR

// Testing CSR 0xb4a
	csrr x8, 0xb4a	// Read CSR
	li x3, -1
	csrrw x25, 0xb4a, x3	// Write all 1s to CSR
	csrrw x25, 0xb4a, x0	// Write all 0s to CSR
	csrrs x25, 0xb4a, x3	// Set all CSR bits
	csrrc x25, 0xb4a, x3	// Clear all CSR bits
	csrrw x25, 0xb4a, x8	// Restore CSR

// Testing CSR 0xb4b
	csrr x28, 0xb4b	// Read CSR
	li x24, -1
	csrrw x6, 0xb4b, x24	// Write all 1s to CSR
	csrrw x6, 0xb4b, x0	// Write all 0s to CSR
	csrrs x6, 0xb4b, x24	// Set all CSR bits
	csrrc x6, 0xb4b, x24	// Clear all CSR bits
	csrrw x6, 0xb4b, x28	// Restore CSR

// Testing CSR 0xb4c
	csrr x13, 0xb4c	// Read CSR
	li x6, -1
	csrrw x16, 0xb4c, x6	// Write all 1s to CSR
	csrrw x16, 0xb4c, x0	// Write all 0s to CSR
	csrrs x16, 0xb4c, x6	// Set all CSR bits
	csrrc x16, 0xb4c, x6	// Clear all CSR bits
	csrrw x16, 0xb4c, x13	// Restore CSR

// Testing CSR 0xb4d
	csrr x2, 0xb4d	// Read CSR
	li x30, -1
	csrrw x4, 0xb4d, x30	// Write all 1s to CSR
	csrrw x4, 0xb4d, x0	// Write all 0s to CSR
	csrrs x4, 0xb4d, x30	// Set all CSR bits
	csrrc x4, 0xb4d, x30	// Clear all CSR bits
	csrrw x4, 0xb4d, x2	// Restore CSR

// Testing CSR 0xb4e
	csrr x6, 0xb4e	// Read CSR
	li x29, -1
	csrrw x9, 0xb4e, x29	// Write all 1s to CSR
	csrrw x9, 0xb4e, x0	// Write all 0s to CSR
	csrrs x9, 0xb4e, x29	// Set all CSR bits
	csrrc x9, 0xb4e, x29	// Clear all CSR bits
	csrrw x9, 0xb4e, x6	// Restore CSR

// Testing CSR 0xb4f
	csrr x17, 0xb4f	// Read CSR
	li x12, -1
	csrrw x1, 0xb4f, x12	// Write all 1s to CSR
	csrrw x1, 0xb4f, x0	// Write all 0s to CSR
	csrrs x1, 0xb4f, x12	// Set all CSR bits
	csrrc x1, 0xb4f, x12	// Clear all CSR bits
	csrrw x1, 0xb4f, x17	// Restore CSR

// Testing CSR 0xb50
	csrr x23, 0xb50	// Read CSR
	li x25, -1
	csrrw x28, 0xb50, x25	// Write all 1s to CSR
	csrrw x28, 0xb50, x0	// Write all 0s to CSR
	csrrs x28, 0xb50, x25	// Set all CSR bits
	csrrc x28, 0xb50, x25	// Clear all CSR bits
	csrrw x28, 0xb50, x23	// Restore CSR

// Testing CSR 0xb51
	csrr x5, 0xb51	// Read CSR
	li x7, -1
	csrrw x10, 0xb51, x7	// Write all 1s to CSR
	csrrw x10, 0xb51, x0	// Write all 0s to CSR
	csrrs x10, 0xb51, x7	// Set all CSR bits
	csrrc x10, 0xb51, x7	// Clear all CSR bits
	csrrw x10, 0xb51, x5	// Restore CSR

// Testing CSR 0xb52
	csrr x2, 0xb52	// Read CSR
	li x9, -1
	csrrw x12, 0xb52, x9	// Write all 1s to CSR
	csrrw x12, 0xb52, x0	// Write all 0s to CSR
	csrrs x12, 0xb52, x9	// Set all CSR bits
	csrrc x12, 0xb52, x9	// Clear all CSR bits
	csrrw x12, 0xb52, x2	// Restore CSR

// Testing CSR 0xb53
	csrr x12, 0xb53	// Read CSR
	li x10, -1
	csrrw x25, 0xb53, x10	// Write all 1s to CSR
	csrrw x25, 0xb53, x0	// Write all 0s to CSR
	csrrs x25, 0xb53, x10	// Set all CSR bits
	csrrc x25, 0xb53, x10	// Clear all CSR bits
	csrrw x25, 0xb53, x12	// Restore CSR

// Testing CSR 0xb54
	csrr x23, 0xb54	// Read CSR
	li x6, -1
	csrrw x4, 0xb54, x6	// Write all 1s to CSR
	csrrw x4, 0xb54, x0	// Write all 0s to CSR
	csrrs x4, 0xb54, x6	// Set all CSR bits
	csrrc x4, 0xb54, x6	// Clear all CSR bits
	csrrw x4, 0xb54, x23	// Restore CSR

// Testing CSR 0xb55
	csrr x15, 0xb55	// Read CSR
	li x19, -1
	csrrw x4, 0xb55, x19	// Write all 1s to CSR
	csrrw x4, 0xb55, x0	// Write all 0s to CSR
	csrrs x4, 0xb55, x19	// Set all CSR bits
	csrrc x4, 0xb55, x19	// Clear all CSR bits
	csrrw x4, 0xb55, x15	// Restore CSR

// Testing CSR 0xb56
	csrr x7, 0xb56	// Read CSR
	li x6, -1
	csrrw x9, 0xb56, x6	// Write all 1s to CSR
	csrrw x9, 0xb56, x0	// Write all 0s to CSR
	csrrs x9, 0xb56, x6	// Set all CSR bits
	csrrc x9, 0xb56, x6	// Clear all CSR bits
	csrrw x9, 0xb56, x7	// Restore CSR

// Testing CSR 0xb57
	csrr x4, 0xb57	// Read CSR
	li x16, -1
	csrrw x12, 0xb57, x16	// Write all 1s to CSR
	csrrw x12, 0xb57, x0	// Write all 0s to CSR
	csrrs x12, 0xb57, x16	// Set all CSR bits
	csrrc x12, 0xb57, x16	// Clear all CSR bits
	csrrw x12, 0xb57, x4	// Restore CSR

// Testing CSR 0xb58
	csrr x29, 0xb58	// Read CSR
	li x13, -1
	csrrw x17, 0xb58, x13	// Write all 1s to CSR
	csrrw x17, 0xb58, x0	// Write all 0s to CSR
	csrrs x17, 0xb58, x13	// Set all CSR bits
	csrrc x17, 0xb58, x13	// Clear all CSR bits
	csrrw x17, 0xb58, x29	// Restore CSR

// Testing CSR 0xb59
	csrr x20, 0xb59	// Read CSR
	li x2, -1
	csrrw x29, 0xb59, x2	// Write all 1s to CSR
	csrrw x29, 0xb59, x0	// Write all 0s to CSR
	csrrs x29, 0xb59, x2	// Set all CSR bits
	csrrc x29, 0xb59, x2	// Clear all CSR bits
	csrrw x29, 0xb59, x20	// Restore CSR

// Testing CSR 0xb5a
	csrr x9, 0xb5a	// Read CSR
	li x5, -1
	csrrw x4, 0xb5a, x5	// Write all 1s to CSR
	csrrw x4, 0xb5a, x0	// Write all 0s to CSR
	csrrs x4, 0xb5a, x5	// Set all CSR bits
	csrrc x4, 0xb5a, x5	// Clear all CSR bits
	csrrw x4, 0xb5a, x9	// Restore CSR

// Testing CSR 0xb5b
	csrr x22, 0xb5b	// Read CSR
	li x31, -1
	csrrw x4, 0xb5b, x31	// Write all 1s to CSR
	csrrw x4, 0xb5b, x0	// Write all 0s to CSR
	csrrs x4, 0xb5b, x31	// Set all CSR bits
	csrrc x4, 0xb5b, x31	// Clear all CSR bits
	csrrw x4, 0xb5b, x22	// Restore CSR

// Testing CSR 0xb5c
	csrr x12, 0xb5c	// Read CSR
	li x10, -1
	csrrw x28, 0xb5c, x10	// Write all 1s to CSR
	csrrw x28, 0xb5c, x0	// Write all 0s to CSR
	csrrs x28, 0xb5c, x10	// Set all CSR bits
	csrrc x28, 0xb5c, x10	// Clear all CSR bits
	csrrw x28, 0xb5c, x12	// Restore CSR

// Testing CSR 0xb5d
	csrr x16, 0xb5d	// Read CSR
	li x21, -1
	csrrw x13, 0xb5d, x21	// Write all 1s to CSR
	csrrw x13, 0xb5d, x0	// Write all 0s to CSR
	csrrs x13, 0xb5d, x21	// Set all CSR bits
	csrrc x13, 0xb5d, x21	// Clear all CSR bits
	csrrw x13, 0xb5d, x16	// Restore CSR

// Testing CSR 0xb5e
	csrr x29, 0xb5e	// Read CSR
	li x9, -1
	csrrw x13, 0xb5e, x9	// Write all 1s to CSR
	csrrw x13, 0xb5e, x0	// Write all 0s to CSR
	csrrs x13, 0xb5e, x9	// Set all CSR bits
	csrrc x13, 0xb5e, x9	// Clear all CSR bits
	csrrw x13, 0xb5e, x29	// Restore CSR

// Testing CSR 0xb5f
	csrr x26, 0xb5f	// Read CSR
	li x28, -1
	csrrw x31, 0xb5f, x28	// Write all 1s to CSR
	csrrw x31, 0xb5f, x0	// Write all 0s to CSR
	csrrs x31, 0xb5f, x28	// Set all CSR bits
	csrrc x31, 0xb5f, x28	// Clear all CSR bits
	csrrw x31, 0xb5f, x26	// Restore CSR

// Testing CSR 0xb60
	csrr x8, 0xb60	// Read CSR
	li x13, -1
	csrrw x20, 0xb60, x13	// Write all 1s to CSR
	csrrw x20, 0xb60, x0	// Write all 0s to CSR
	csrrs x20, 0xb60, x13	// Set all CSR bits
	csrrc x20, 0xb60, x13	// Clear all CSR bits
	csrrw x20, 0xb60, x8	// Restore CSR

// Testing CSR 0xb61
	csrr x7, 0xb61	// Read CSR
	li x25, -1
	csrrw x1, 0xb61, x25	// Write all 1s to CSR
	csrrw x1, 0xb61, x0	// Write all 0s to CSR
	csrrs x1, 0xb61, x25	// Set all CSR bits
	csrrc x1, 0xb61, x25	// Clear all CSR bits
	csrrw x1, 0xb61, x7	// Restore CSR

// Testing CSR 0xb62
	csrr x8, 0xb62	// Read CSR
	li x4, -1
	csrrw x12, 0xb62, x4	// Write all 1s to CSR
	csrrw x12, 0xb62, x0	// Write all 0s to CSR
	csrrs x12, 0xb62, x4	// Set all CSR bits
	csrrc x12, 0xb62, x4	// Clear all CSR bits
	csrrw x12, 0xb62, x8	// Restore CSR

// Testing CSR 0xb63
	csrr x19, 0xb63	// Read CSR
	li x8, -1
	csrrw x7, 0xb63, x8	// Write all 1s to CSR
	csrrw x7, 0xb63, x0	// Write all 0s to CSR
	csrrs x7, 0xb63, x8	// Set all CSR bits
	csrrc x7, 0xb63, x8	// Clear all CSR bits
	csrrw x7, 0xb63, x19	// Restore CSR

// Testing CSR 0xb64
	csrr x30, 0xb64	// Read CSR
	li x22, -1
	csrrw x18, 0xb64, x22	// Write all 1s to CSR
	csrrw x18, 0xb64, x0	// Write all 0s to CSR
	csrrs x18, 0xb64, x22	// Set all CSR bits
	csrrc x18, 0xb64, x22	// Clear all CSR bits
	csrrw x18, 0xb64, x30	// Restore CSR

// Testing CSR 0xb65
	csrr x21, 0xb65	// Read CSR
	li x7, -1
	csrrw x11, 0xb65, x7	// Write all 1s to CSR
	csrrw x11, 0xb65, x0	// Write all 0s to CSR
	csrrs x11, 0xb65, x7	// Set all CSR bits
	csrrc x11, 0xb65, x7	// Clear all CSR bits
	csrrw x11, 0xb65, x21	// Restore CSR

// Testing CSR 0xb66
	csrr x5, 0xb66	// Read CSR
	li x9, -1
	csrrw x3, 0xb66, x9	// Write all 1s to CSR
	csrrw x3, 0xb66, x0	// Write all 0s to CSR
	csrrs x3, 0xb66, x9	// Set all CSR bits
	csrrc x3, 0xb66, x9	// Clear all CSR bits
	csrrw x3, 0xb66, x5	// Restore CSR

// Testing CSR 0xb67
	csrr x21, 0xb67	// Read CSR
	li x28, -1
	csrrw x18, 0xb67, x28	// Write all 1s to CSR
	csrrw x18, 0xb67, x0	// Write all 0s to CSR
	csrrs x18, 0xb67, x28	// Set all CSR bits
	csrrc x18, 0xb67, x28	// Clear all CSR bits
	csrrw x18, 0xb67, x21	// Restore CSR

// Testing CSR 0xb68
	csrr x7, 0xb68	// Read CSR
	li x2, -1
	csrrw x14, 0xb68, x2	// Write all 1s to CSR
	csrrw x14, 0xb68, x0	// Write all 0s to CSR
	csrrs x14, 0xb68, x2	// Set all CSR bits
	csrrc x14, 0xb68, x2	// Clear all CSR bits
	csrrw x14, 0xb68, x7	// Restore CSR

// Testing CSR 0xb69
	csrr x25, 0xb69	// Read CSR
	li x23, -1
	csrrw x14, 0xb69, x23	// Write all 1s to CSR
	csrrw x14, 0xb69, x0	// Write all 0s to CSR
	csrrs x14, 0xb69, x23	// Set all CSR bits
	csrrc x14, 0xb69, x23	// Clear all CSR bits
	csrrw x14, 0xb69, x25	// Restore CSR

// Testing CSR 0xb6a
	csrr x2, 0xb6a	// Read CSR
	li x6, -1
	csrrw x13, 0xb6a, x6	// Write all 1s to CSR
	csrrw x13, 0xb6a, x0	// Write all 0s to CSR
	csrrs x13, 0xb6a, x6	// Set all CSR bits
	csrrc x13, 0xb6a, x6	// Clear all CSR bits
	csrrw x13, 0xb6a, x2	// Restore CSR

// Testing CSR 0xb6b
	csrr x26, 0xb6b	// Read CSR
	li x4, -1
	csrrw x24, 0xb6b, x4	// Write all 1s to CSR
	csrrw x24, 0xb6b, x0	// Write all 0s to CSR
	csrrs x24, 0xb6b, x4	// Set all CSR bits
	csrrc x24, 0xb6b, x4	// Clear all CSR bits
	csrrw x24, 0xb6b, x26	// Restore CSR

// Testing CSR 0xb6c
	csrr x31, 0xb6c	// Read CSR
	li x13, -1
	csrrw x8, 0xb6c, x13	// Write all 1s to CSR
	csrrw x8, 0xb6c, x0	// Write all 0s to CSR
	csrrs x8, 0xb6c, x13	// Set all CSR bits
	csrrc x8, 0xb6c, x13	// Clear all CSR bits
	csrrw x8, 0xb6c, x31	// Restore CSR

// Testing CSR 0xb6d
	csrr x23, 0xb6d	// Read CSR
	li x21, -1
	csrrw x24, 0xb6d, x21	// Write all 1s to CSR
	csrrw x24, 0xb6d, x0	// Write all 0s to CSR
	csrrs x24, 0xb6d, x21	// Set all CSR bits
	csrrc x24, 0xb6d, x21	// Clear all CSR bits
	csrrw x24, 0xb6d, x23	// Restore CSR

// Testing CSR 0xb6e
	csrr x22, 0xb6e	// Read CSR
	li x11, -1
	csrrw x13, 0xb6e, x11	// Write all 1s to CSR
	csrrw x13, 0xb6e, x0	// Write all 0s to CSR
	csrrs x13, 0xb6e, x11	// Set all CSR bits
	csrrc x13, 0xb6e, x11	// Clear all CSR bits
	csrrw x13, 0xb6e, x22	// Restore CSR

// Testing CSR 0xb6f
	csrr x16, 0xb6f	// Read CSR
	li x29, -1
	csrrw x31, 0xb6f, x29	// Write all 1s to CSR
	csrrw x31, 0xb6f, x0	// Write all 0s to CSR
	csrrs x31, 0xb6f, x29	// Set all CSR bits
	csrrc x31, 0xb6f, x29	// Clear all CSR bits
	csrrw x31, 0xb6f, x16	// Restore CSR

// Testing CSR 0xb70
	csrr x10, 0xb70	// Read CSR
	li x28, -1
	csrrw x25, 0xb70, x28	// Write all 1s to CSR
	csrrw x25, 0xb70, x0	// Write all 0s to CSR
	csrrs x25, 0xb70, x28	// Set all CSR bits
	csrrc x25, 0xb70, x28	// Clear all CSR bits
	csrrw x25, 0xb70, x10	// Restore CSR

// Testing CSR 0xb71
	csrr x24, 0xb71	// Read CSR
	li x18, -1
	csrrw x20, 0xb71, x18	// Write all 1s to CSR
	csrrw x20, 0xb71, x0	// Write all 0s to CSR
	csrrs x20, 0xb71, x18	// Set all CSR bits
	csrrc x20, 0xb71, x18	// Clear all CSR bits
	csrrw x20, 0xb71, x24	// Restore CSR

// Testing CSR 0xb72
	csrr x17, 0xb72	// Read CSR
	li x16, -1
	csrrw x29, 0xb72, x16	// Write all 1s to CSR
	csrrw x29, 0xb72, x0	// Write all 0s to CSR
	csrrs x29, 0xb72, x16	// Set all CSR bits
	csrrc x29, 0xb72, x16	// Clear all CSR bits
	csrrw x29, 0xb72, x17	// Restore CSR

// Testing CSR 0xb73
	csrr x25, 0xb73	// Read CSR
	li x21, -1
	csrrw x7, 0xb73, x21	// Write all 1s to CSR
	csrrw x7, 0xb73, x0	// Write all 0s to CSR
	csrrs x7, 0xb73, x21	// Set all CSR bits
	csrrc x7, 0xb73, x21	// Clear all CSR bits
	csrrw x7, 0xb73, x25	// Restore CSR

// Testing CSR 0xb74
	csrr x14, 0xb74	// Read CSR
	li x10, -1
	csrrw x23, 0xb74, x10	// Write all 1s to CSR
	csrrw x23, 0xb74, x0	// Write all 0s to CSR
	csrrs x23, 0xb74, x10	// Set all CSR bits
	csrrc x23, 0xb74, x10	// Clear all CSR bits
	csrrw x23, 0xb74, x14	// Restore CSR

// Testing CSR 0xb75
	csrr x23, 0xb75	// Read CSR
	li x8, -1
	csrrw x1, 0xb75, x8	// Write all 1s to CSR
	csrrw x1, 0xb75, x0	// Write all 0s to CSR
	csrrs x1, 0xb75, x8	// Set all CSR bits
	csrrc x1, 0xb75, x8	// Clear all CSR bits
	csrrw x1, 0xb75, x23	// Restore CSR

// Testing CSR 0xb76
	csrr x9, 0xb76	// Read CSR
	li x2, -1
	csrrw x29, 0xb76, x2	// Write all 1s to CSR
	csrrw x29, 0xb76, x0	// Write all 0s to CSR
	csrrs x29, 0xb76, x2	// Set all CSR bits
	csrrc x29, 0xb76, x2	// Clear all CSR bits
	csrrw x29, 0xb76, x9	// Restore CSR

// Testing CSR 0xb77
	csrr x26, 0xb77	// Read CSR
	li x23, -1
	csrrw x8, 0xb77, x23	// Write all 1s to CSR
	csrrw x8, 0xb77, x0	// Write all 0s to CSR
	csrrs x8, 0xb77, x23	// Set all CSR bits
	csrrc x8, 0xb77, x23	// Clear all CSR bits
	csrrw x8, 0xb77, x26	// Restore CSR

// Testing CSR 0xb78
	csrr x30, 0xb78	// Read CSR
	li x23, -1
	csrrw x2, 0xb78, x23	// Write all 1s to CSR
	csrrw x2, 0xb78, x0	// Write all 0s to CSR
	csrrs x2, 0xb78, x23	// Set all CSR bits
	csrrc x2, 0xb78, x23	// Clear all CSR bits
	csrrw x2, 0xb78, x30	// Restore CSR

// Testing CSR 0xb79
	csrr x22, 0xb79	// Read CSR
	li x18, -1
	csrrw x1, 0xb79, x18	// Write all 1s to CSR
	csrrw x1, 0xb79, x0	// Write all 0s to CSR
	csrrs x1, 0xb79, x18	// Set all CSR bits
	csrrc x1, 0xb79, x18	// Clear all CSR bits
	csrrw x1, 0xb79, x22	// Restore CSR

// Testing CSR 0xb7a
	csrr x24, 0xb7a	// Read CSR
	li x28, -1
	csrrw x12, 0xb7a, x28	// Write all 1s to CSR
	csrrw x12, 0xb7a, x0	// Write all 0s to CSR
	csrrs x12, 0xb7a, x28	// Set all CSR bits
	csrrc x12, 0xb7a, x28	// Clear all CSR bits
	csrrw x12, 0xb7a, x24	// Restore CSR

// Testing CSR 0xb7b
	csrr x25, 0xb7b	// Read CSR
	li x29, -1
	csrrw x1, 0xb7b, x29	// Write all 1s to CSR
	csrrw x1, 0xb7b, x0	// Write all 0s to CSR
	csrrs x1, 0xb7b, x29	// Set all CSR bits
	csrrc x1, 0xb7b, x29	// Clear all CSR bits
	csrrw x1, 0xb7b, x25	// Restore CSR

// Testing CSR 0xb7c
	csrr x1, 0xb7c	// Read CSR
	li x4, -1
	csrrw x28, 0xb7c, x4	// Write all 1s to CSR
	csrrw x28, 0xb7c, x0	// Write all 0s to CSR
	csrrs x28, 0xb7c, x4	// Set all CSR bits
	csrrc x28, 0xb7c, x4	// Clear all CSR bits
	csrrw x28, 0xb7c, x1	// Restore CSR

// Testing CSR 0xb7d
	csrr x20, 0xb7d	// Read CSR
	li x29, -1
	csrrw x26, 0xb7d, x29	// Write all 1s to CSR
	csrrw x26, 0xb7d, x0	// Write all 0s to CSR
	csrrs x26, 0xb7d, x29	// Set all CSR bits
	csrrc x26, 0xb7d, x29	// Clear all CSR bits
	csrrw x26, 0xb7d, x20	// Restore CSR

// Testing CSR 0xb7e
	csrr x15, 0xb7e	// Read CSR
	li x28, -1
	csrrw x5, 0xb7e, x28	// Write all 1s to CSR
	csrrw x5, 0xb7e, x0	// Write all 0s to CSR
	csrrs x5, 0xb7e, x28	// Set all CSR bits
	csrrc x5, 0xb7e, x28	// Clear all CSR bits
	csrrw x5, 0xb7e, x15	// Restore CSR

// Testing CSR 0xb7f
	csrr x9, 0xb7f	// Read CSR
	li x6, -1
	csrrw x7, 0xb7f, x6	// Write all 1s to CSR
	csrrw x7, 0xb7f, x0	// Write all 0s to CSR
	csrrs x7, 0xb7f, x6	// Set all CSR bits
	csrrc x7, 0xb7f, x6	// Clear all CSR bits
	csrrw x7, 0xb7f, x9	// Restore CSR

// Testing CSR 0xb80
	csrr x14, 0xb80	// Read CSR
	li x17, -1
	csrrw x23, 0xb80, x17	// Write all 1s to CSR
	csrrw x23, 0xb80, x0	// Write all 0s to CSR
	csrrs x23, 0xb80, x17	// Set all CSR bits
	csrrc x23, 0xb80, x17	// Clear all CSR bits
	csrrw x23, 0xb80, x14	// Restore CSR

// Testing CSR 0xb81
	csrr x26, 0xb81	// Read CSR
	li x18, -1
	csrrw x27, 0xb81, x18	// Write all 1s to CSR
	csrrw x27, 0xb81, x0	// Write all 0s to CSR
	csrrs x27, 0xb81, x18	// Set all CSR bits
	csrrc x27, 0xb81, x18	// Clear all CSR bits
	csrrw x27, 0xb81, x26	// Restore CSR

// Testing CSR 0xb82
	csrr x25, 0xb82	// Read CSR
	li x28, -1
	csrrw x3, 0xb82, x28	// Write all 1s to CSR
	csrrw x3, 0xb82, x0	// Write all 0s to CSR
	csrrs x3, 0xb82, x28	// Set all CSR bits
	csrrc x3, 0xb82, x28	// Clear all CSR bits
	csrrw x3, 0xb82, x25	// Restore CSR

// Testing CSR 0xb83
	csrr x10, 0xb83	// Read CSR
	li x25, -1
	csrrw x29, 0xb83, x25	// Write all 1s to CSR
	csrrw x29, 0xb83, x0	// Write all 0s to CSR
	csrrs x29, 0xb83, x25	// Set all CSR bits
	csrrc x29, 0xb83, x25	// Clear all CSR bits
	csrrw x29, 0xb83, x10	// Restore CSR

// Testing CSR 0xb84
	csrr x9, 0xb84	// Read CSR
	li x24, -1
	csrrw x26, 0xb84, x24	// Write all 1s to CSR
	csrrw x26, 0xb84, x0	// Write all 0s to CSR
	csrrs x26, 0xb84, x24	// Set all CSR bits
	csrrc x26, 0xb84, x24	// Clear all CSR bits
	csrrw x26, 0xb84, x9	// Restore CSR

// Testing CSR 0xb85
	csrr x25, 0xb85	// Read CSR
	li x27, -1
	csrrw x20, 0xb85, x27	// Write all 1s to CSR
	csrrw x20, 0xb85, x0	// Write all 0s to CSR
	csrrs x20, 0xb85, x27	// Set all CSR bits
	csrrc x20, 0xb85, x27	// Clear all CSR bits
	csrrw x20, 0xb85, x25	// Restore CSR

// Testing CSR 0xb86
	csrr x9, 0xb86	// Read CSR
	li x19, -1
	csrrw x27, 0xb86, x19	// Write all 1s to CSR
	csrrw x27, 0xb86, x0	// Write all 0s to CSR
	csrrs x27, 0xb86, x19	// Set all CSR bits
	csrrc x27, 0xb86, x19	// Clear all CSR bits
	csrrw x27, 0xb86, x9	// Restore CSR

// Testing CSR 0xb87
	csrr x7, 0xb87	// Read CSR
	li x1, -1
	csrrw x19, 0xb87, x1	// Write all 1s to CSR
	csrrw x19, 0xb87, x0	// Write all 0s to CSR
	csrrs x19, 0xb87, x1	// Set all CSR bits
	csrrc x19, 0xb87, x1	// Clear all CSR bits
	csrrw x19, 0xb87, x7	// Restore CSR

// Testing CSR 0xb88
	csrr x31, 0xb88	// Read CSR
	li x20, -1
	csrrw x26, 0xb88, x20	// Write all 1s to CSR
	csrrw x26, 0xb88, x0	// Write all 0s to CSR
	csrrs x26, 0xb88, x20	// Set all CSR bits
	csrrc x26, 0xb88, x20	// Clear all CSR bits
	csrrw x26, 0xb88, x31	// Restore CSR

// Testing CSR 0xb89
	csrr x21, 0xb89	// Read CSR
	li x17, -1
	csrrw x3, 0xb89, x17	// Write all 1s to CSR
	csrrw x3, 0xb89, x0	// Write all 0s to CSR
	csrrs x3, 0xb89, x17	// Set all CSR bits
	csrrc x3, 0xb89, x17	// Clear all CSR bits
	csrrw x3, 0xb89, x21	// Restore CSR

// Testing CSR 0xb8a
	csrr x27, 0xb8a	// Read CSR
	li x28, -1
	csrrw x25, 0xb8a, x28	// Write all 1s to CSR
	csrrw x25, 0xb8a, x0	// Write all 0s to CSR
	csrrs x25, 0xb8a, x28	// Set all CSR bits
	csrrc x25, 0xb8a, x28	// Clear all CSR bits
	csrrw x25, 0xb8a, x27	// Restore CSR

// Testing CSR 0xb8b
	csrr x13, 0xb8b	// Read CSR
	li x18, -1
	csrrw x22, 0xb8b, x18	// Write all 1s to CSR
	csrrw x22, 0xb8b, x0	// Write all 0s to CSR
	csrrs x22, 0xb8b, x18	// Set all CSR bits
	csrrc x22, 0xb8b, x18	// Clear all CSR bits
	csrrw x22, 0xb8b, x13	// Restore CSR

// Testing CSR 0xb8c
	csrr x30, 0xb8c	// Read CSR
	li x21, -1
	csrrw x16, 0xb8c, x21	// Write all 1s to CSR
	csrrw x16, 0xb8c, x0	// Write all 0s to CSR
	csrrs x16, 0xb8c, x21	// Set all CSR bits
	csrrc x16, 0xb8c, x21	// Clear all CSR bits
	csrrw x16, 0xb8c, x30	// Restore CSR

// Testing CSR 0xb8d
	csrr x5, 0xb8d	// Read CSR
	li x19, -1
	csrrw x28, 0xb8d, x19	// Write all 1s to CSR
	csrrw x28, 0xb8d, x0	// Write all 0s to CSR
	csrrs x28, 0xb8d, x19	// Set all CSR bits
	csrrc x28, 0xb8d, x19	// Clear all CSR bits
	csrrw x28, 0xb8d, x5	// Restore CSR

// Testing CSR 0xb8e
	csrr x29, 0xb8e	// Read CSR
	li x2, -1
	csrrw x1, 0xb8e, x2	// Write all 1s to CSR
	csrrw x1, 0xb8e, x0	// Write all 0s to CSR
	csrrs x1, 0xb8e, x2	// Set all CSR bits
	csrrc x1, 0xb8e, x2	// Clear all CSR bits
	csrrw x1, 0xb8e, x29	// Restore CSR

// Testing CSR 0xb8f
	csrr x2, 0xb8f	// Read CSR
	li x14, -1
	csrrw x30, 0xb8f, x14	// Write all 1s to CSR
	csrrw x30, 0xb8f, x0	// Write all 0s to CSR
	csrrs x30, 0xb8f, x14	// Set all CSR bits
	csrrc x30, 0xb8f, x14	// Clear all CSR bits
	csrrw x30, 0xb8f, x2	// Restore CSR

// Testing CSR 0xb90
	csrr x6, 0xb90	// Read CSR
	li x13, -1
	csrrw x20, 0xb90, x13	// Write all 1s to CSR
	csrrw x20, 0xb90, x0	// Write all 0s to CSR
	csrrs x20, 0xb90, x13	// Set all CSR bits
	csrrc x20, 0xb90, x13	// Clear all CSR bits
	csrrw x20, 0xb90, x6	// Restore CSR

// Testing CSR 0xb91
	csrr x15, 0xb91	// Read CSR
	li x9, -1
	csrrw x17, 0xb91, x9	// Write all 1s to CSR
	csrrw x17, 0xb91, x0	// Write all 0s to CSR
	csrrs x17, 0xb91, x9	// Set all CSR bits
	csrrc x17, 0xb91, x9	// Clear all CSR bits
	csrrw x17, 0xb91, x15	// Restore CSR

// Testing CSR 0xb92
	csrr x1, 0xb92	// Read CSR
	li x26, -1
	csrrw x13, 0xb92, x26	// Write all 1s to CSR
	csrrw x13, 0xb92, x0	// Write all 0s to CSR
	csrrs x13, 0xb92, x26	// Set all CSR bits
	csrrc x13, 0xb92, x26	// Clear all CSR bits
	csrrw x13, 0xb92, x1	// Restore CSR

// Testing CSR 0xb93
	csrr x6, 0xb93	// Read CSR
	li x25, -1
	csrrw x21, 0xb93, x25	// Write all 1s to CSR
	csrrw x21, 0xb93, x0	// Write all 0s to CSR
	csrrs x21, 0xb93, x25	// Set all CSR bits
	csrrc x21, 0xb93, x25	// Clear all CSR bits
	csrrw x21, 0xb93, x6	// Restore CSR

// Testing CSR 0xb94
	csrr x1, 0xb94	// Read CSR
	li x17, -1
	csrrw x24, 0xb94, x17	// Write all 1s to CSR
	csrrw x24, 0xb94, x0	// Write all 0s to CSR
	csrrs x24, 0xb94, x17	// Set all CSR bits
	csrrc x24, 0xb94, x17	// Clear all CSR bits
	csrrw x24, 0xb94, x1	// Restore CSR

// Testing CSR 0xb95
	csrr x5, 0xb95	// Read CSR
	li x15, -1
	csrrw x31, 0xb95, x15	// Write all 1s to CSR
	csrrw x31, 0xb95, x0	// Write all 0s to CSR
	csrrs x31, 0xb95, x15	// Set all CSR bits
	csrrc x31, 0xb95, x15	// Clear all CSR bits
	csrrw x31, 0xb95, x5	// Restore CSR

// Testing CSR 0xb96
	csrr x25, 0xb96	// Read CSR
	li x10, -1
	csrrw x31, 0xb96, x10	// Write all 1s to CSR
	csrrw x31, 0xb96, x0	// Write all 0s to CSR
	csrrs x31, 0xb96, x10	// Set all CSR bits
	csrrc x31, 0xb96, x10	// Clear all CSR bits
	csrrw x31, 0xb96, x25	// Restore CSR

// Testing CSR 0xb97
	csrr x17, 0xb97	// Read CSR
	li x9, -1
	csrrw x24, 0xb97, x9	// Write all 1s to CSR
	csrrw x24, 0xb97, x0	// Write all 0s to CSR
	csrrs x24, 0xb97, x9	// Set all CSR bits
	csrrc x24, 0xb97, x9	// Clear all CSR bits
	csrrw x24, 0xb97, x17	// Restore CSR

// Testing CSR 0xb98
	csrr x11, 0xb98	// Read CSR
	li x31, -1
	csrrw x21, 0xb98, x31	// Write all 1s to CSR
	csrrw x21, 0xb98, x0	// Write all 0s to CSR
	csrrs x21, 0xb98, x31	// Set all CSR bits
	csrrc x21, 0xb98, x31	// Clear all CSR bits
	csrrw x21, 0xb98, x11	// Restore CSR

// Testing CSR 0xb99
	csrr x26, 0xb99	// Read CSR
	li x4, -1
	csrrw x16, 0xb99, x4	// Write all 1s to CSR
	csrrw x16, 0xb99, x0	// Write all 0s to CSR
	csrrs x16, 0xb99, x4	// Set all CSR bits
	csrrc x16, 0xb99, x4	// Clear all CSR bits
	csrrw x16, 0xb99, x26	// Restore CSR

// Testing CSR 0xb9a
	csrr x26, 0xb9a	// Read CSR
	li x18, -1
	csrrw x28, 0xb9a, x18	// Write all 1s to CSR
	csrrw x28, 0xb9a, x0	// Write all 0s to CSR
	csrrs x28, 0xb9a, x18	// Set all CSR bits
	csrrc x28, 0xb9a, x18	// Clear all CSR bits
	csrrw x28, 0xb9a, x26	// Restore CSR

// Testing CSR 0xb9b
	csrr x15, 0xb9b	// Read CSR
	li x29, -1
	csrrw x16, 0xb9b, x29	// Write all 1s to CSR
	csrrw x16, 0xb9b, x0	// Write all 0s to CSR
	csrrs x16, 0xb9b, x29	// Set all CSR bits
	csrrc x16, 0xb9b, x29	// Clear all CSR bits
	csrrw x16, 0xb9b, x15	// Restore CSR

// Testing CSR 0xb9c
	csrr x9, 0xb9c	// Read CSR
	li x12, -1
	csrrw x29, 0xb9c, x12	// Write all 1s to CSR
	csrrw x29, 0xb9c, x0	// Write all 0s to CSR
	csrrs x29, 0xb9c, x12	// Set all CSR bits
	csrrc x29, 0xb9c, x12	// Clear all CSR bits
	csrrw x29, 0xb9c, x9	// Restore CSR

// Testing CSR 0xb9d
	csrr x29, 0xb9d	// Read CSR
	li x9, -1
	csrrw x2, 0xb9d, x9	// Write all 1s to CSR
	csrrw x2, 0xb9d, x0	// Write all 0s to CSR
	csrrs x2, 0xb9d, x9	// Set all CSR bits
	csrrc x2, 0xb9d, x9	// Clear all CSR bits
	csrrw x2, 0xb9d, x29	// Restore CSR

// Testing CSR 0xb9e
	csrr x12, 0xb9e	// Read CSR
	li x16, -1
	csrrw x27, 0xb9e, x16	// Write all 1s to CSR
	csrrw x27, 0xb9e, x0	// Write all 0s to CSR
	csrrs x27, 0xb9e, x16	// Set all CSR bits
	csrrc x27, 0xb9e, x16	// Clear all CSR bits
	csrrw x27, 0xb9e, x12	// Restore CSR

// Testing CSR 0xb9f
	csrr x10, 0xb9f	// Read CSR
	li x17, -1
	csrrw x9, 0xb9f, x17	// Write all 1s to CSR
	csrrw x9, 0xb9f, x0	// Write all 0s to CSR
	csrrs x9, 0xb9f, x17	// Set all CSR bits
	csrrc x9, 0xb9f, x17	// Clear all CSR bits
	csrrw x9, 0xb9f, x10	// Restore CSR

// Testing CSR 0xba0
	csrr x22, 0xba0	// Read CSR
	li x8, -1
	csrrw x16, 0xba0, x8	// Write all 1s to CSR
	csrrw x16, 0xba0, x0	// Write all 0s to CSR
	csrrs x16, 0xba0, x8	// Set all CSR bits
	csrrc x16, 0xba0, x8	// Clear all CSR bits
	csrrw x16, 0xba0, x22	// Restore CSR

// Testing CSR 0xba1
	csrr x31, 0xba1	// Read CSR
	li x5, -1
	csrrw x3, 0xba1, x5	// Write all 1s to CSR
	csrrw x3, 0xba1, x0	// Write all 0s to CSR
	csrrs x3, 0xba1, x5	// Set all CSR bits
	csrrc x3, 0xba1, x5	// Clear all CSR bits
	csrrw x3, 0xba1, x31	// Restore CSR

// Testing CSR 0xba2
	csrr x9, 0xba2	// Read CSR
	li x17, -1
	csrrw x20, 0xba2, x17	// Write all 1s to CSR
	csrrw x20, 0xba2, x0	// Write all 0s to CSR
	csrrs x20, 0xba2, x17	// Set all CSR bits
	csrrc x20, 0xba2, x17	// Clear all CSR bits
	csrrw x20, 0xba2, x9	// Restore CSR

// Testing CSR 0xba3
	csrr x4, 0xba3	// Read CSR
	li x6, -1
	csrrw x16, 0xba3, x6	// Write all 1s to CSR
	csrrw x16, 0xba3, x0	// Write all 0s to CSR
	csrrs x16, 0xba3, x6	// Set all CSR bits
	csrrc x16, 0xba3, x6	// Clear all CSR bits
	csrrw x16, 0xba3, x4	// Restore CSR

// Testing CSR 0xba4
	csrr x2, 0xba4	// Read CSR
	li x7, -1
	csrrw x14, 0xba4, x7	// Write all 1s to CSR
	csrrw x14, 0xba4, x0	// Write all 0s to CSR
	csrrs x14, 0xba4, x7	// Set all CSR bits
	csrrc x14, 0xba4, x7	// Clear all CSR bits
	csrrw x14, 0xba4, x2	// Restore CSR

// Testing CSR 0xba5
	csrr x9, 0xba5	// Read CSR
	li x11, -1
	csrrw x20, 0xba5, x11	// Write all 1s to CSR
	csrrw x20, 0xba5, x0	// Write all 0s to CSR
	csrrs x20, 0xba5, x11	// Set all CSR bits
	csrrc x20, 0xba5, x11	// Clear all CSR bits
	csrrw x20, 0xba5, x9	// Restore CSR

// Testing CSR 0xba6
	csrr x13, 0xba6	// Read CSR
	li x31, -1
	csrrw x3, 0xba6, x31	// Write all 1s to CSR
	csrrw x3, 0xba6, x0	// Write all 0s to CSR
	csrrs x3, 0xba6, x31	// Set all CSR bits
	csrrc x3, 0xba6, x31	// Clear all CSR bits
	csrrw x3, 0xba6, x13	// Restore CSR

// Testing CSR 0xba7
	csrr x3, 0xba7	// Read CSR
	li x12, -1
	csrrw x30, 0xba7, x12	// Write all 1s to CSR
	csrrw x30, 0xba7, x0	// Write all 0s to CSR
	csrrs x30, 0xba7, x12	// Set all CSR bits
	csrrc x30, 0xba7, x12	// Clear all CSR bits
	csrrw x30, 0xba7, x3	// Restore CSR

// Testing CSR 0xba8
	csrr x31, 0xba8	// Read CSR
	li x16, -1
	csrrw x10, 0xba8, x16	// Write all 1s to CSR
	csrrw x10, 0xba8, x0	// Write all 0s to CSR
	csrrs x10, 0xba8, x16	// Set all CSR bits
	csrrc x10, 0xba8, x16	// Clear all CSR bits
	csrrw x10, 0xba8, x31	// Restore CSR

// Testing CSR 0xba9
	csrr x27, 0xba9	// Read CSR
	li x15, -1
	csrrw x9, 0xba9, x15	// Write all 1s to CSR
	csrrw x9, 0xba9, x0	// Write all 0s to CSR
	csrrs x9, 0xba9, x15	// Set all CSR bits
	csrrc x9, 0xba9, x15	// Clear all CSR bits
	csrrw x9, 0xba9, x27	// Restore CSR

// Testing CSR 0xbaa
	csrr x18, 0xbaa	// Read CSR
	li x7, -1
	csrrw x25, 0xbaa, x7	// Write all 1s to CSR
	csrrw x25, 0xbaa, x0	// Write all 0s to CSR
	csrrs x25, 0xbaa, x7	// Set all CSR bits
	csrrc x25, 0xbaa, x7	// Clear all CSR bits
	csrrw x25, 0xbaa, x18	// Restore CSR

// Testing CSR 0xbab
	csrr x29, 0xbab	// Read CSR
	li x31, -1
	csrrw x30, 0xbab, x31	// Write all 1s to CSR
	csrrw x30, 0xbab, x0	// Write all 0s to CSR
	csrrs x30, 0xbab, x31	// Set all CSR bits
	csrrc x30, 0xbab, x31	// Clear all CSR bits
	csrrw x30, 0xbab, x29	// Restore CSR

// Testing CSR 0xbac
	csrr x17, 0xbac	// Read CSR
	li x7, -1
	csrrw x6, 0xbac, x7	// Write all 1s to CSR
	csrrw x6, 0xbac, x0	// Write all 0s to CSR
	csrrs x6, 0xbac, x7	// Set all CSR bits
	csrrc x6, 0xbac, x7	// Clear all CSR bits
	csrrw x6, 0xbac, x17	// Restore CSR

// Testing CSR 0xbad
	csrr x11, 0xbad	// Read CSR
	li x2, -1
	csrrw x7, 0xbad, x2	// Write all 1s to CSR
	csrrw x7, 0xbad, x0	// Write all 0s to CSR
	csrrs x7, 0xbad, x2	// Set all CSR bits
	csrrc x7, 0xbad, x2	// Clear all CSR bits
	csrrw x7, 0xbad, x11	// Restore CSR

// Testing CSR 0xbae
	csrr x27, 0xbae	// Read CSR
	li x21, -1
	csrrw x25, 0xbae, x21	// Write all 1s to CSR
	csrrw x25, 0xbae, x0	// Write all 0s to CSR
	csrrs x25, 0xbae, x21	// Set all CSR bits
	csrrc x25, 0xbae, x21	// Clear all CSR bits
	csrrw x25, 0xbae, x27	// Restore CSR

// Testing CSR 0xbaf
	csrr x31, 0xbaf	// Read CSR
	li x2, -1
	csrrw x29, 0xbaf, x2	// Write all 1s to CSR
	csrrw x29, 0xbaf, x0	// Write all 0s to CSR
	csrrs x29, 0xbaf, x2	// Set all CSR bits
	csrrc x29, 0xbaf, x2	// Clear all CSR bits
	csrrw x29, 0xbaf, x31	// Restore CSR

// Testing CSR 0xbb0
	csrr x26, 0xbb0	// Read CSR
	li x31, -1
	csrrw x4, 0xbb0, x31	// Write all 1s to CSR
	csrrw x4, 0xbb0, x0	// Write all 0s to CSR
	csrrs x4, 0xbb0, x31	// Set all CSR bits
	csrrc x4, 0xbb0, x31	// Clear all CSR bits
	csrrw x4, 0xbb0, x26	// Restore CSR

// Testing CSR 0xbb1
	csrr x18, 0xbb1	// Read CSR
	li x24, -1
	csrrw x1, 0xbb1, x24	// Write all 1s to CSR
	csrrw x1, 0xbb1, x0	// Write all 0s to CSR
	csrrs x1, 0xbb1, x24	// Set all CSR bits
	csrrc x1, 0xbb1, x24	// Clear all CSR bits
	csrrw x1, 0xbb1, x18	// Restore CSR

// Testing CSR 0xbb2
	csrr x15, 0xbb2	// Read CSR
	li x9, -1
	csrrw x21, 0xbb2, x9	// Write all 1s to CSR
	csrrw x21, 0xbb2, x0	// Write all 0s to CSR
	csrrs x21, 0xbb2, x9	// Set all CSR bits
	csrrc x21, 0xbb2, x9	// Clear all CSR bits
	csrrw x21, 0xbb2, x15	// Restore CSR

// Testing CSR 0xbb3
	csrr x16, 0xbb3	// Read CSR
	li x15, -1
	csrrw x31, 0xbb3, x15	// Write all 1s to CSR
	csrrw x31, 0xbb3, x0	// Write all 0s to CSR
	csrrs x31, 0xbb3, x15	// Set all CSR bits
	csrrc x31, 0xbb3, x15	// Clear all CSR bits
	csrrw x31, 0xbb3, x16	// Restore CSR

// Testing CSR 0xbb4
	csrr x19, 0xbb4	// Read CSR
	li x31, -1
	csrrw x23, 0xbb4, x31	// Write all 1s to CSR
	csrrw x23, 0xbb4, x0	// Write all 0s to CSR
	csrrs x23, 0xbb4, x31	// Set all CSR bits
	csrrc x23, 0xbb4, x31	// Clear all CSR bits
	csrrw x23, 0xbb4, x19	// Restore CSR

// Testing CSR 0xbb5
	csrr x14, 0xbb5	// Read CSR
	li x23, -1
	csrrw x26, 0xbb5, x23	// Write all 1s to CSR
	csrrw x26, 0xbb5, x0	// Write all 0s to CSR
	csrrs x26, 0xbb5, x23	// Set all CSR bits
	csrrc x26, 0xbb5, x23	// Clear all CSR bits
	csrrw x26, 0xbb5, x14	// Restore CSR

// Testing CSR 0xbb6
	csrr x6, 0xbb6	// Read CSR
	li x8, -1
	csrrw x25, 0xbb6, x8	// Write all 1s to CSR
	csrrw x25, 0xbb6, x0	// Write all 0s to CSR
	csrrs x25, 0xbb6, x8	// Set all CSR bits
	csrrc x25, 0xbb6, x8	// Clear all CSR bits
	csrrw x25, 0xbb6, x6	// Restore CSR

// Testing CSR 0xbb7
	csrr x8, 0xbb7	// Read CSR
	li x10, -1
	csrrw x9, 0xbb7, x10	// Write all 1s to CSR
	csrrw x9, 0xbb7, x0	// Write all 0s to CSR
	csrrs x9, 0xbb7, x10	// Set all CSR bits
	csrrc x9, 0xbb7, x10	// Clear all CSR bits
	csrrw x9, 0xbb7, x8	// Restore CSR

// Testing CSR 0xbb8
	csrr x4, 0xbb8	// Read CSR
	li x15, -1
	csrrw x5, 0xbb8, x15	// Write all 1s to CSR
	csrrw x5, 0xbb8, x0	// Write all 0s to CSR
	csrrs x5, 0xbb8, x15	// Set all CSR bits
	csrrc x5, 0xbb8, x15	// Clear all CSR bits
	csrrw x5, 0xbb8, x4	// Restore CSR

// Testing CSR 0xbb9
	csrr x10, 0xbb9	// Read CSR
	li x28, -1
	csrrw x24, 0xbb9, x28	// Write all 1s to CSR
	csrrw x24, 0xbb9, x0	// Write all 0s to CSR
	csrrs x24, 0xbb9, x28	// Set all CSR bits
	csrrc x24, 0xbb9, x28	// Clear all CSR bits
	csrrw x24, 0xbb9, x10	// Restore CSR

// Testing CSR 0xbba
	csrr x13, 0xbba	// Read CSR
	li x3, -1
	csrrw x5, 0xbba, x3	// Write all 1s to CSR
	csrrw x5, 0xbba, x0	// Write all 0s to CSR
	csrrs x5, 0xbba, x3	// Set all CSR bits
	csrrc x5, 0xbba, x3	// Clear all CSR bits
	csrrw x5, 0xbba, x13	// Restore CSR

// Testing CSR 0xbbb
	csrr x13, 0xbbb	// Read CSR
	li x15, -1
	csrrw x3, 0xbbb, x15	// Write all 1s to CSR
	csrrw x3, 0xbbb, x0	// Write all 0s to CSR
	csrrs x3, 0xbbb, x15	// Set all CSR bits
	csrrc x3, 0xbbb, x15	// Clear all CSR bits
	csrrw x3, 0xbbb, x13	// Restore CSR

// Testing CSR 0xbbc
	csrr x15, 0xbbc	// Read CSR
	li x6, -1
	csrrw x17, 0xbbc, x6	// Write all 1s to CSR
	csrrw x17, 0xbbc, x0	// Write all 0s to CSR
	csrrs x17, 0xbbc, x6	// Set all CSR bits
	csrrc x17, 0xbbc, x6	// Clear all CSR bits
	csrrw x17, 0xbbc, x15	// Restore CSR

// Testing CSR 0xbbd
	csrr x16, 0xbbd	// Read CSR
	li x3, -1
	csrrw x19, 0xbbd, x3	// Write all 1s to CSR
	csrrw x19, 0xbbd, x0	// Write all 0s to CSR
	csrrs x19, 0xbbd, x3	// Set all CSR bits
	csrrc x19, 0xbbd, x3	// Clear all CSR bits
	csrrw x19, 0xbbd, x16	// Restore CSR

// Testing CSR 0xbbe
	csrr x28, 0xbbe	// Read CSR
	li x2, -1
	csrrw x19, 0xbbe, x2	// Write all 1s to CSR
	csrrw x19, 0xbbe, x0	// Write all 0s to CSR
	csrrs x19, 0xbbe, x2	// Set all CSR bits
	csrrc x19, 0xbbe, x2	// Clear all CSR bits
	csrrw x19, 0xbbe, x28	// Restore CSR

// Testing CSR 0xbbf
	csrr x12, 0xbbf	// Read CSR
	li x11, -1
	csrrw x25, 0xbbf, x11	// Write all 1s to CSR
	csrrw x25, 0xbbf, x0	// Write all 0s to CSR
	csrrs x25, 0xbbf, x11	// Set all CSR bits
	csrrc x25, 0xbbf, x11	// Clear all CSR bits
	csrrw x25, 0xbbf, x12	// Restore CSR

// Testing CSR 0xbc0
	csrr x16, 0xbc0	// Read CSR
	li x17, -1
	csrrw x12, 0xbc0, x17	// Write all 1s to CSR
	csrrw x12, 0xbc0, x0	// Write all 0s to CSR
	csrrs x12, 0xbc0, x17	// Set all CSR bits
	csrrc x12, 0xbc0, x17	// Clear all CSR bits
	csrrw x12, 0xbc0, x16	// Restore CSR

// Testing CSR 0xbc1
	csrr x27, 0xbc1	// Read CSR
	li x18, -1
	csrrw x22, 0xbc1, x18	// Write all 1s to CSR
	csrrw x22, 0xbc1, x0	// Write all 0s to CSR
	csrrs x22, 0xbc1, x18	// Set all CSR bits
	csrrc x22, 0xbc1, x18	// Clear all CSR bits
	csrrw x22, 0xbc1, x27	// Restore CSR

// Testing CSR 0xbc2
	csrr x9, 0xbc2	// Read CSR
	li x30, -1
	csrrw x17, 0xbc2, x30	// Write all 1s to CSR
	csrrw x17, 0xbc2, x0	// Write all 0s to CSR
	csrrs x17, 0xbc2, x30	// Set all CSR bits
	csrrc x17, 0xbc2, x30	// Clear all CSR bits
	csrrw x17, 0xbc2, x9	// Restore CSR

// Testing CSR 0xbc3
	csrr x5, 0xbc3	// Read CSR
	li x11, -1
	csrrw x18, 0xbc3, x11	// Write all 1s to CSR
	csrrw x18, 0xbc3, x0	// Write all 0s to CSR
	csrrs x18, 0xbc3, x11	// Set all CSR bits
	csrrc x18, 0xbc3, x11	// Clear all CSR bits
	csrrw x18, 0xbc3, x5	// Restore CSR

// Testing CSR 0xbc4
	csrr x16, 0xbc4	// Read CSR
	li x21, -1
	csrrw x8, 0xbc4, x21	// Write all 1s to CSR
	csrrw x8, 0xbc4, x0	// Write all 0s to CSR
	csrrs x8, 0xbc4, x21	// Set all CSR bits
	csrrc x8, 0xbc4, x21	// Clear all CSR bits
	csrrw x8, 0xbc4, x16	// Restore CSR

// Testing CSR 0xbc5
	csrr x23, 0xbc5	// Read CSR
	li x19, -1
	csrrw x17, 0xbc5, x19	// Write all 1s to CSR
	csrrw x17, 0xbc5, x0	// Write all 0s to CSR
	csrrs x17, 0xbc5, x19	// Set all CSR bits
	csrrc x17, 0xbc5, x19	// Clear all CSR bits
	csrrw x17, 0xbc5, x23	// Restore CSR

// Testing CSR 0xbc6
	csrr x21, 0xbc6	// Read CSR
	li x9, -1
	csrrw x24, 0xbc6, x9	// Write all 1s to CSR
	csrrw x24, 0xbc6, x0	// Write all 0s to CSR
	csrrs x24, 0xbc6, x9	// Set all CSR bits
	csrrc x24, 0xbc6, x9	// Clear all CSR bits
	csrrw x24, 0xbc6, x21	// Restore CSR

// Testing CSR 0xbc7
	csrr x6, 0xbc7	// Read CSR
	li x8, -1
	csrrw x31, 0xbc7, x8	// Write all 1s to CSR
	csrrw x31, 0xbc7, x0	// Write all 0s to CSR
	csrrs x31, 0xbc7, x8	// Set all CSR bits
	csrrc x31, 0xbc7, x8	// Clear all CSR bits
	csrrw x31, 0xbc7, x6	// Restore CSR

// Testing CSR 0xbc8
	csrr x30, 0xbc8	// Read CSR
	li x24, -1
	csrrw x9, 0xbc8, x24	// Write all 1s to CSR
	csrrw x9, 0xbc8, x0	// Write all 0s to CSR
	csrrs x9, 0xbc8, x24	// Set all CSR bits
	csrrc x9, 0xbc8, x24	// Clear all CSR bits
	csrrw x9, 0xbc8, x30	// Restore CSR

// Testing CSR 0xbc9
	csrr x28, 0xbc9	// Read CSR
	li x15, -1
	csrrw x29, 0xbc9, x15	// Write all 1s to CSR
	csrrw x29, 0xbc9, x0	// Write all 0s to CSR
	csrrs x29, 0xbc9, x15	// Set all CSR bits
	csrrc x29, 0xbc9, x15	// Clear all CSR bits
	csrrw x29, 0xbc9, x28	// Restore CSR

// Testing CSR 0xbca
	csrr x16, 0xbca	// Read CSR
	li x3, -1
	csrrw x18, 0xbca, x3	// Write all 1s to CSR
	csrrw x18, 0xbca, x0	// Write all 0s to CSR
	csrrs x18, 0xbca, x3	// Set all CSR bits
	csrrc x18, 0xbca, x3	// Clear all CSR bits
	csrrw x18, 0xbca, x16	// Restore CSR

// Testing CSR 0xbcb
	csrr x11, 0xbcb	// Read CSR
	li x29, -1
	csrrw x18, 0xbcb, x29	// Write all 1s to CSR
	csrrw x18, 0xbcb, x0	// Write all 0s to CSR
	csrrs x18, 0xbcb, x29	// Set all CSR bits
	csrrc x18, 0xbcb, x29	// Clear all CSR bits
	csrrw x18, 0xbcb, x11	// Restore CSR

// Testing CSR 0xbcc
	csrr x7, 0xbcc	// Read CSR
	li x23, -1
	csrrw x25, 0xbcc, x23	// Write all 1s to CSR
	csrrw x25, 0xbcc, x0	// Write all 0s to CSR
	csrrs x25, 0xbcc, x23	// Set all CSR bits
	csrrc x25, 0xbcc, x23	// Clear all CSR bits
	csrrw x25, 0xbcc, x7	// Restore CSR

// Testing CSR 0xbcd
	csrr x1, 0xbcd	// Read CSR
	li x23, -1
	csrrw x7, 0xbcd, x23	// Write all 1s to CSR
	csrrw x7, 0xbcd, x0	// Write all 0s to CSR
	csrrs x7, 0xbcd, x23	// Set all CSR bits
	csrrc x7, 0xbcd, x23	// Clear all CSR bits
	csrrw x7, 0xbcd, x1	// Restore CSR

// Testing CSR 0xbce
	csrr x25, 0xbce	// Read CSR
	li x18, -1
	csrrw x4, 0xbce, x18	// Write all 1s to CSR
	csrrw x4, 0xbce, x0	// Write all 0s to CSR
	csrrs x4, 0xbce, x18	// Set all CSR bits
	csrrc x4, 0xbce, x18	// Clear all CSR bits
	csrrw x4, 0xbce, x25	// Restore CSR

// Testing CSR 0xbcf
	csrr x25, 0xbcf	// Read CSR
	li x1, -1
	csrrw x18, 0xbcf, x1	// Write all 1s to CSR
	csrrw x18, 0xbcf, x0	// Write all 0s to CSR
	csrrs x18, 0xbcf, x1	// Set all CSR bits
	csrrc x18, 0xbcf, x1	// Clear all CSR bits
	csrrw x18, 0xbcf, x25	// Restore CSR

// Testing CSR 0xbd0
	csrr x27, 0xbd0	// Read CSR
	li x22, -1
	csrrw x23, 0xbd0, x22	// Write all 1s to CSR
	csrrw x23, 0xbd0, x0	// Write all 0s to CSR
	csrrs x23, 0xbd0, x22	// Set all CSR bits
	csrrc x23, 0xbd0, x22	// Clear all CSR bits
	csrrw x23, 0xbd0, x27	// Restore CSR

// Testing CSR 0xbd1
	csrr x9, 0xbd1	// Read CSR
	li x19, -1
	csrrw x26, 0xbd1, x19	// Write all 1s to CSR
	csrrw x26, 0xbd1, x0	// Write all 0s to CSR
	csrrs x26, 0xbd1, x19	// Set all CSR bits
	csrrc x26, 0xbd1, x19	// Clear all CSR bits
	csrrw x26, 0xbd1, x9	// Restore CSR

// Testing CSR 0xbd2
	csrr x26, 0xbd2	// Read CSR
	li x23, -1
	csrrw x31, 0xbd2, x23	// Write all 1s to CSR
	csrrw x31, 0xbd2, x0	// Write all 0s to CSR
	csrrs x31, 0xbd2, x23	// Set all CSR bits
	csrrc x31, 0xbd2, x23	// Clear all CSR bits
	csrrw x31, 0xbd2, x26	// Restore CSR

// Testing CSR 0xbd3
	csrr x29, 0xbd3	// Read CSR
	li x18, -1
	csrrw x31, 0xbd3, x18	// Write all 1s to CSR
	csrrw x31, 0xbd3, x0	// Write all 0s to CSR
	csrrs x31, 0xbd3, x18	// Set all CSR bits
	csrrc x31, 0xbd3, x18	// Clear all CSR bits
	csrrw x31, 0xbd3, x29	// Restore CSR

// Testing CSR 0xbd4
	csrr x16, 0xbd4	// Read CSR
	li x31, -1
	csrrw x28, 0xbd4, x31	// Write all 1s to CSR
	csrrw x28, 0xbd4, x0	// Write all 0s to CSR
	csrrs x28, 0xbd4, x31	// Set all CSR bits
	csrrc x28, 0xbd4, x31	// Clear all CSR bits
	csrrw x28, 0xbd4, x16	// Restore CSR

// Testing CSR 0xbd5
	csrr x3, 0xbd5	// Read CSR
	li x19, -1
	csrrw x25, 0xbd5, x19	// Write all 1s to CSR
	csrrw x25, 0xbd5, x0	// Write all 0s to CSR
	csrrs x25, 0xbd5, x19	// Set all CSR bits
	csrrc x25, 0xbd5, x19	// Clear all CSR bits
	csrrw x25, 0xbd5, x3	// Restore CSR

// Testing CSR 0xbd6
	csrr x2, 0xbd6	// Read CSR
	li x20, -1
	csrrw x5, 0xbd6, x20	// Write all 1s to CSR
	csrrw x5, 0xbd6, x0	// Write all 0s to CSR
	csrrs x5, 0xbd6, x20	// Set all CSR bits
	csrrc x5, 0xbd6, x20	// Clear all CSR bits
	csrrw x5, 0xbd6, x2	// Restore CSR

// Testing CSR 0xbd7
	csrr x16, 0xbd7	// Read CSR
	li x21, -1
	csrrw x19, 0xbd7, x21	// Write all 1s to CSR
	csrrw x19, 0xbd7, x0	// Write all 0s to CSR
	csrrs x19, 0xbd7, x21	// Set all CSR bits
	csrrc x19, 0xbd7, x21	// Clear all CSR bits
	csrrw x19, 0xbd7, x16	// Restore CSR

// Testing CSR 0xbd8
	csrr x6, 0xbd8	// Read CSR
	li x26, -1
	csrrw x14, 0xbd8, x26	// Write all 1s to CSR
	csrrw x14, 0xbd8, x0	// Write all 0s to CSR
	csrrs x14, 0xbd8, x26	// Set all CSR bits
	csrrc x14, 0xbd8, x26	// Clear all CSR bits
	csrrw x14, 0xbd8, x6	// Restore CSR

// Testing CSR 0xbd9
	csrr x11, 0xbd9	// Read CSR
	li x31, -1
	csrrw x28, 0xbd9, x31	// Write all 1s to CSR
	csrrw x28, 0xbd9, x0	// Write all 0s to CSR
	csrrs x28, 0xbd9, x31	// Set all CSR bits
	csrrc x28, 0xbd9, x31	// Clear all CSR bits
	csrrw x28, 0xbd9, x11	// Restore CSR

// Testing CSR 0xbda
	csrr x19, 0xbda	// Read CSR
	li x4, -1
	csrrw x11, 0xbda, x4	// Write all 1s to CSR
	csrrw x11, 0xbda, x0	// Write all 0s to CSR
	csrrs x11, 0xbda, x4	// Set all CSR bits
	csrrc x11, 0xbda, x4	// Clear all CSR bits
	csrrw x11, 0xbda, x19	// Restore CSR

// Testing CSR 0xbdb
	csrr x25, 0xbdb	// Read CSR
	li x21, -1
	csrrw x29, 0xbdb, x21	// Write all 1s to CSR
	csrrw x29, 0xbdb, x0	// Write all 0s to CSR
	csrrs x29, 0xbdb, x21	// Set all CSR bits
	csrrc x29, 0xbdb, x21	// Clear all CSR bits
	csrrw x29, 0xbdb, x25	// Restore CSR

// Testing CSR 0xbdc
	csrr x9, 0xbdc	// Read CSR
	li x27, -1
	csrrw x12, 0xbdc, x27	// Write all 1s to CSR
	csrrw x12, 0xbdc, x0	// Write all 0s to CSR
	csrrs x12, 0xbdc, x27	// Set all CSR bits
	csrrc x12, 0xbdc, x27	// Clear all CSR bits
	csrrw x12, 0xbdc, x9	// Restore CSR

// Testing CSR 0xbdd
	csrr x31, 0xbdd	// Read CSR
	li x23, -1
	csrrw x29, 0xbdd, x23	// Write all 1s to CSR
	csrrw x29, 0xbdd, x0	// Write all 0s to CSR
	csrrs x29, 0xbdd, x23	// Set all CSR bits
	csrrc x29, 0xbdd, x23	// Clear all CSR bits
	csrrw x29, 0xbdd, x31	// Restore CSR

// Testing CSR 0xbde
	csrr x24, 0xbde	// Read CSR
	li x1, -1
	csrrw x10, 0xbde, x1	// Write all 1s to CSR
	csrrw x10, 0xbde, x0	// Write all 0s to CSR
	csrrs x10, 0xbde, x1	// Set all CSR bits
	csrrc x10, 0xbde, x1	// Clear all CSR bits
	csrrw x10, 0xbde, x24	// Restore CSR

// Testing CSR 0xbdf
	csrr x16, 0xbdf	// Read CSR
	li x13, -1
	csrrw x9, 0xbdf, x13	// Write all 1s to CSR
	csrrw x9, 0xbdf, x0	// Write all 0s to CSR
	csrrs x9, 0xbdf, x13	// Set all CSR bits
	csrrc x9, 0xbdf, x13	// Clear all CSR bits
	csrrw x9, 0xbdf, x16	// Restore CSR

// Testing CSR 0xbe0
	csrr x14, 0xbe0	// Read CSR
	li x26, -1
	csrrw x23, 0xbe0, x26	// Write all 1s to CSR
	csrrw x23, 0xbe0, x0	// Write all 0s to CSR
	csrrs x23, 0xbe0, x26	// Set all CSR bits
	csrrc x23, 0xbe0, x26	// Clear all CSR bits
	csrrw x23, 0xbe0, x14	// Restore CSR

// Testing CSR 0xbe1
	csrr x20, 0xbe1	// Read CSR
	li x3, -1
	csrrw x26, 0xbe1, x3	// Write all 1s to CSR
	csrrw x26, 0xbe1, x0	// Write all 0s to CSR
	csrrs x26, 0xbe1, x3	// Set all CSR bits
	csrrc x26, 0xbe1, x3	// Clear all CSR bits
	csrrw x26, 0xbe1, x20	// Restore CSR

// Testing CSR 0xbe2
	csrr x9, 0xbe2	// Read CSR
	li x20, -1
	csrrw x30, 0xbe2, x20	// Write all 1s to CSR
	csrrw x30, 0xbe2, x0	// Write all 0s to CSR
	csrrs x30, 0xbe2, x20	// Set all CSR bits
	csrrc x30, 0xbe2, x20	// Clear all CSR bits
	csrrw x30, 0xbe2, x9	// Restore CSR

// Testing CSR 0xbe3
	csrr x12, 0xbe3	// Read CSR
	li x31, -1
	csrrw x2, 0xbe3, x31	// Write all 1s to CSR
	csrrw x2, 0xbe3, x0	// Write all 0s to CSR
	csrrs x2, 0xbe3, x31	// Set all CSR bits
	csrrc x2, 0xbe3, x31	// Clear all CSR bits
	csrrw x2, 0xbe3, x12	// Restore CSR

// Testing CSR 0xbe4
	csrr x10, 0xbe4	// Read CSR
	li x3, -1
	csrrw x7, 0xbe4, x3	// Write all 1s to CSR
	csrrw x7, 0xbe4, x0	// Write all 0s to CSR
	csrrs x7, 0xbe4, x3	// Set all CSR bits
	csrrc x7, 0xbe4, x3	// Clear all CSR bits
	csrrw x7, 0xbe4, x10	// Restore CSR

// Testing CSR 0xbe5
	csrr x18, 0xbe5	// Read CSR
	li x1, -1
	csrrw x8, 0xbe5, x1	// Write all 1s to CSR
	csrrw x8, 0xbe5, x0	// Write all 0s to CSR
	csrrs x8, 0xbe5, x1	// Set all CSR bits
	csrrc x8, 0xbe5, x1	// Clear all CSR bits
	csrrw x8, 0xbe5, x18	// Restore CSR

// Testing CSR 0xbe6
	csrr x8, 0xbe6	// Read CSR
	li x12, -1
	csrrw x29, 0xbe6, x12	// Write all 1s to CSR
	csrrw x29, 0xbe6, x0	// Write all 0s to CSR
	csrrs x29, 0xbe6, x12	// Set all CSR bits
	csrrc x29, 0xbe6, x12	// Clear all CSR bits
	csrrw x29, 0xbe6, x8	// Restore CSR

// Testing CSR 0xbe7
	csrr x10, 0xbe7	// Read CSR
	li x27, -1
	csrrw x14, 0xbe7, x27	// Write all 1s to CSR
	csrrw x14, 0xbe7, x0	// Write all 0s to CSR
	csrrs x14, 0xbe7, x27	// Set all CSR bits
	csrrc x14, 0xbe7, x27	// Clear all CSR bits
	csrrw x14, 0xbe7, x10	// Restore CSR

// Testing CSR 0xbe8
	csrr x2, 0xbe8	// Read CSR
	li x13, -1
	csrrw x11, 0xbe8, x13	// Write all 1s to CSR
	csrrw x11, 0xbe8, x0	// Write all 0s to CSR
	csrrs x11, 0xbe8, x13	// Set all CSR bits
	csrrc x11, 0xbe8, x13	// Clear all CSR bits
	csrrw x11, 0xbe8, x2	// Restore CSR

// Testing CSR 0xbe9
	csrr x18, 0xbe9	// Read CSR
	li x14, -1
	csrrw x21, 0xbe9, x14	// Write all 1s to CSR
	csrrw x21, 0xbe9, x0	// Write all 0s to CSR
	csrrs x21, 0xbe9, x14	// Set all CSR bits
	csrrc x21, 0xbe9, x14	// Clear all CSR bits
	csrrw x21, 0xbe9, x18	// Restore CSR

// Testing CSR 0xbea
	csrr x20, 0xbea	// Read CSR
	li x25, -1
	csrrw x5, 0xbea, x25	// Write all 1s to CSR
	csrrw x5, 0xbea, x0	// Write all 0s to CSR
	csrrs x5, 0xbea, x25	// Set all CSR bits
	csrrc x5, 0xbea, x25	// Clear all CSR bits
	csrrw x5, 0xbea, x20	// Restore CSR

// Testing CSR 0xbeb
	csrr x31, 0xbeb	// Read CSR
	li x2, -1
	csrrw x19, 0xbeb, x2	// Write all 1s to CSR
	csrrw x19, 0xbeb, x0	// Write all 0s to CSR
	csrrs x19, 0xbeb, x2	// Set all CSR bits
	csrrc x19, 0xbeb, x2	// Clear all CSR bits
	csrrw x19, 0xbeb, x31	// Restore CSR

// Testing CSR 0xbec
	csrr x22, 0xbec	// Read CSR
	li x5, -1
	csrrw x11, 0xbec, x5	// Write all 1s to CSR
	csrrw x11, 0xbec, x0	// Write all 0s to CSR
	csrrs x11, 0xbec, x5	// Set all CSR bits
	csrrc x11, 0xbec, x5	// Clear all CSR bits
	csrrw x11, 0xbec, x22	// Restore CSR

// Testing CSR 0xbed
	csrr x31, 0xbed	// Read CSR
	li x12, -1
	csrrw x27, 0xbed, x12	// Write all 1s to CSR
	csrrw x27, 0xbed, x0	// Write all 0s to CSR
	csrrs x27, 0xbed, x12	// Set all CSR bits
	csrrc x27, 0xbed, x12	// Clear all CSR bits
	csrrw x27, 0xbed, x31	// Restore CSR

// Testing CSR 0xbee
	csrr x1, 0xbee	// Read CSR
	li x4, -1
	csrrw x27, 0xbee, x4	// Write all 1s to CSR
	csrrw x27, 0xbee, x0	// Write all 0s to CSR
	csrrs x27, 0xbee, x4	// Set all CSR bits
	csrrc x27, 0xbee, x4	// Clear all CSR bits
	csrrw x27, 0xbee, x1	// Restore CSR

// Testing CSR 0xbef
	csrr x17, 0xbef	// Read CSR
	li x4, -1
	csrrw x7, 0xbef, x4	// Write all 1s to CSR
	csrrw x7, 0xbef, x0	// Write all 0s to CSR
	csrrs x7, 0xbef, x4	// Set all CSR bits
	csrrc x7, 0xbef, x4	// Clear all CSR bits
	csrrw x7, 0xbef, x17	// Restore CSR

// Testing CSR 0xbf0
	csrr x5, 0xbf0	// Read CSR
	li x4, -1
	csrrw x19, 0xbf0, x4	// Write all 1s to CSR
	csrrw x19, 0xbf0, x0	// Write all 0s to CSR
	csrrs x19, 0xbf0, x4	// Set all CSR bits
	csrrc x19, 0xbf0, x4	// Clear all CSR bits
	csrrw x19, 0xbf0, x5	// Restore CSR

// Testing CSR 0xbf1
	csrr x8, 0xbf1	// Read CSR
	li x24, -1
	csrrw x5, 0xbf1, x24	// Write all 1s to CSR
	csrrw x5, 0xbf1, x0	// Write all 0s to CSR
	csrrs x5, 0xbf1, x24	// Set all CSR bits
	csrrc x5, 0xbf1, x24	// Clear all CSR bits
	csrrw x5, 0xbf1, x8	// Restore CSR

// Testing CSR 0xbf2
	csrr x17, 0xbf2	// Read CSR
	li x13, -1
	csrrw x30, 0xbf2, x13	// Write all 1s to CSR
	csrrw x30, 0xbf2, x0	// Write all 0s to CSR
	csrrs x30, 0xbf2, x13	// Set all CSR bits
	csrrc x30, 0xbf2, x13	// Clear all CSR bits
	csrrw x30, 0xbf2, x17	// Restore CSR

// Testing CSR 0xbf3
	csrr x21, 0xbf3	// Read CSR
	li x14, -1
	csrrw x8, 0xbf3, x14	// Write all 1s to CSR
	csrrw x8, 0xbf3, x0	// Write all 0s to CSR
	csrrs x8, 0xbf3, x14	// Set all CSR bits
	csrrc x8, 0xbf3, x14	// Clear all CSR bits
	csrrw x8, 0xbf3, x21	// Restore CSR

// Testing CSR 0xbf4
	csrr x5, 0xbf4	// Read CSR
	li x20, -1
	csrrw x29, 0xbf4, x20	// Write all 1s to CSR
	csrrw x29, 0xbf4, x0	// Write all 0s to CSR
	csrrs x29, 0xbf4, x20	// Set all CSR bits
	csrrc x29, 0xbf4, x20	// Clear all CSR bits
	csrrw x29, 0xbf4, x5	// Restore CSR

// Testing CSR 0xbf5
	csrr x11, 0xbf5	// Read CSR
	li x10, -1
	csrrw x21, 0xbf5, x10	// Write all 1s to CSR
	csrrw x21, 0xbf5, x0	// Write all 0s to CSR
	csrrs x21, 0xbf5, x10	// Set all CSR bits
	csrrc x21, 0xbf5, x10	// Clear all CSR bits
	csrrw x21, 0xbf5, x11	// Restore CSR

// Testing CSR 0xbf6
	csrr x9, 0xbf6	// Read CSR
	li x19, -1
	csrrw x16, 0xbf6, x19	// Write all 1s to CSR
	csrrw x16, 0xbf6, x0	// Write all 0s to CSR
	csrrs x16, 0xbf6, x19	// Set all CSR bits
	csrrc x16, 0xbf6, x19	// Clear all CSR bits
	csrrw x16, 0xbf6, x9	// Restore CSR

// Testing CSR 0xbf7
	csrr x18, 0xbf7	// Read CSR
	li x4, -1
	csrrw x12, 0xbf7, x4	// Write all 1s to CSR
	csrrw x12, 0xbf7, x0	// Write all 0s to CSR
	csrrs x12, 0xbf7, x4	// Set all CSR bits
	csrrc x12, 0xbf7, x4	// Clear all CSR bits
	csrrw x12, 0xbf7, x18	// Restore CSR

// Testing CSR 0xbf8
	csrr x28, 0xbf8	// Read CSR
	li x27, -1
	csrrw x12, 0xbf8, x27	// Write all 1s to CSR
	csrrw x12, 0xbf8, x0	// Write all 0s to CSR
	csrrs x12, 0xbf8, x27	// Set all CSR bits
	csrrc x12, 0xbf8, x27	// Clear all CSR bits
	csrrw x12, 0xbf8, x28	// Restore CSR

// Testing CSR 0xbf9
	csrr x6, 0xbf9	// Read CSR
	li x25, -1
	csrrw x14, 0xbf9, x25	// Write all 1s to CSR
	csrrw x14, 0xbf9, x0	// Write all 0s to CSR
	csrrs x14, 0xbf9, x25	// Set all CSR bits
	csrrc x14, 0xbf9, x25	// Clear all CSR bits
	csrrw x14, 0xbf9, x6	// Restore CSR

// Testing CSR 0xbfa
	csrr x6, 0xbfa	// Read CSR
	li x28, -1
	csrrw x24, 0xbfa, x28	// Write all 1s to CSR
	csrrw x24, 0xbfa, x0	// Write all 0s to CSR
	csrrs x24, 0xbfa, x28	// Set all CSR bits
	csrrc x24, 0xbfa, x28	// Clear all CSR bits
	csrrw x24, 0xbfa, x6	// Restore CSR

// Testing CSR 0xbfb
	csrr x7, 0xbfb	// Read CSR
	li x11, -1
	csrrw x15, 0xbfb, x11	// Write all 1s to CSR
	csrrw x15, 0xbfb, x0	// Write all 0s to CSR
	csrrs x15, 0xbfb, x11	// Set all CSR bits
	csrrc x15, 0xbfb, x11	// Clear all CSR bits
	csrrw x15, 0xbfb, x7	// Restore CSR

// Testing CSR 0xbfc
	csrr x3, 0xbfc	// Read CSR
	li x19, -1
	csrrw x27, 0xbfc, x19	// Write all 1s to CSR
	csrrw x27, 0xbfc, x0	// Write all 0s to CSR
	csrrs x27, 0xbfc, x19	// Set all CSR bits
	csrrc x27, 0xbfc, x19	// Clear all CSR bits
	csrrw x27, 0xbfc, x3	// Restore CSR

// Testing CSR 0xbfd
	csrr x11, 0xbfd	// Read CSR
	li x10, -1
	csrrw x13, 0xbfd, x10	// Write all 1s to CSR
	csrrw x13, 0xbfd, x0	// Write all 0s to CSR
	csrrs x13, 0xbfd, x10	// Set all CSR bits
	csrrc x13, 0xbfd, x10	// Clear all CSR bits
	csrrw x13, 0xbfd, x11	// Restore CSR

// Testing CSR 0xbfe
	csrr x21, 0xbfe	// Read CSR
	li x5, -1
	csrrw x30, 0xbfe, x5	// Write all 1s to CSR
	csrrw x30, 0xbfe, x0	// Write all 0s to CSR
	csrrs x30, 0xbfe, x5	// Set all CSR bits
	csrrc x30, 0xbfe, x5	// Clear all CSR bits
	csrrw x30, 0xbfe, x21	// Restore CSR

// Testing CSR 0xbff
	csrr x28, 0xbff	// Read CSR
	li x21, -1
	csrrw x9, 0xbff, x21	// Write all 1s to CSR
	csrrw x9, 0xbff, x0	// Write all 0s to CSR
	csrrs x9, 0xbff, x21	// Set all CSR bits
	csrrc x9, 0xbff, x21	// Clear all CSR bits
	csrrw x9, 0xbff, x28	// Restore CSR

// Testing CSR 0xc00
	csrr x29, 0xc00	// Read CSR
	li x13, -1
	csrrw x1, 0xc00, x13	// Write all 1s to CSR
	csrrw x1, 0xc00, x0	// Write all 0s to CSR
	csrrs x1, 0xc00, x13	// Set all CSR bits
	csrrc x1, 0xc00, x13	// Clear all CSR bits
	csrrw x1, 0xc00, x29	// Restore CSR

// Testing CSR 0xc01
	csrr x3, 0xc01	// Read CSR
	li x18, -1
	csrrw x1, 0xc01, x18	// Write all 1s to CSR
	csrrw x1, 0xc01, x0	// Write all 0s to CSR
	csrrs x1, 0xc01, x18	// Set all CSR bits
	csrrc x1, 0xc01, x18	// Clear all CSR bits
	csrrw x1, 0xc01, x3	// Restore CSR

// Testing CSR 0xc02
	csrr x17, 0xc02	// Read CSR
	li x16, -1
	csrrw x28, 0xc02, x16	// Write all 1s to CSR
	csrrw x28, 0xc02, x0	// Write all 0s to CSR
	csrrs x28, 0xc02, x16	// Set all CSR bits
	csrrc x28, 0xc02, x16	// Clear all CSR bits
	csrrw x28, 0xc02, x17	// Restore CSR

// Testing CSR 0xc03
	csrr x4, 0xc03	// Read CSR
	li x15, -1
	csrrw x1, 0xc03, x15	// Write all 1s to CSR
	csrrw x1, 0xc03, x0	// Write all 0s to CSR
	csrrs x1, 0xc03, x15	// Set all CSR bits
	csrrc x1, 0xc03, x15	// Clear all CSR bits
	csrrw x1, 0xc03, x4	// Restore CSR

// Testing CSR 0xc04
	csrr x25, 0xc04	// Read CSR
	li x31, -1
	csrrw x24, 0xc04, x31	// Write all 1s to CSR
	csrrw x24, 0xc04, x0	// Write all 0s to CSR
	csrrs x24, 0xc04, x31	// Set all CSR bits
	csrrc x24, 0xc04, x31	// Clear all CSR bits
	csrrw x24, 0xc04, x25	// Restore CSR

// Testing CSR 0xc05
	csrr x20, 0xc05	// Read CSR
	li x16, -1
	csrrw x19, 0xc05, x16	// Write all 1s to CSR
	csrrw x19, 0xc05, x0	// Write all 0s to CSR
	csrrs x19, 0xc05, x16	// Set all CSR bits
	csrrc x19, 0xc05, x16	// Clear all CSR bits
	csrrw x19, 0xc05, x20	// Restore CSR

// Testing CSR 0xc06
	csrr x12, 0xc06	// Read CSR
	li x3, -1
	csrrw x5, 0xc06, x3	// Write all 1s to CSR
	csrrw x5, 0xc06, x0	// Write all 0s to CSR
	csrrs x5, 0xc06, x3	// Set all CSR bits
	csrrc x5, 0xc06, x3	// Clear all CSR bits
	csrrw x5, 0xc06, x12	// Restore CSR

// Testing CSR 0xc07
	csrr x9, 0xc07	// Read CSR
	li x13, -1
	csrrw x5, 0xc07, x13	// Write all 1s to CSR
	csrrw x5, 0xc07, x0	// Write all 0s to CSR
	csrrs x5, 0xc07, x13	// Set all CSR bits
	csrrc x5, 0xc07, x13	// Clear all CSR bits
	csrrw x5, 0xc07, x9	// Restore CSR

// Testing CSR 0xc08
	csrr x17, 0xc08	// Read CSR
	li x27, -1
	csrrw x29, 0xc08, x27	// Write all 1s to CSR
	csrrw x29, 0xc08, x0	// Write all 0s to CSR
	csrrs x29, 0xc08, x27	// Set all CSR bits
	csrrc x29, 0xc08, x27	// Clear all CSR bits
	csrrw x29, 0xc08, x17	// Restore CSR

// Testing CSR 0xc09
	csrr x25, 0xc09	// Read CSR
	li x24, -1
	csrrw x10, 0xc09, x24	// Write all 1s to CSR
	csrrw x10, 0xc09, x0	// Write all 0s to CSR
	csrrs x10, 0xc09, x24	// Set all CSR bits
	csrrc x10, 0xc09, x24	// Clear all CSR bits
	csrrw x10, 0xc09, x25	// Restore CSR

// Testing CSR 0xc0a
	csrr x14, 0xc0a	// Read CSR
	li x3, -1
	csrrw x26, 0xc0a, x3	// Write all 1s to CSR
	csrrw x26, 0xc0a, x0	// Write all 0s to CSR
	csrrs x26, 0xc0a, x3	// Set all CSR bits
	csrrc x26, 0xc0a, x3	// Clear all CSR bits
	csrrw x26, 0xc0a, x14	// Restore CSR

// Testing CSR 0xc0b
	csrr x6, 0xc0b	// Read CSR
	li x28, -1
	csrrw x25, 0xc0b, x28	// Write all 1s to CSR
	csrrw x25, 0xc0b, x0	// Write all 0s to CSR
	csrrs x25, 0xc0b, x28	// Set all CSR bits
	csrrc x25, 0xc0b, x28	// Clear all CSR bits
	csrrw x25, 0xc0b, x6	// Restore CSR

// Testing CSR 0xc0c
	csrr x30, 0xc0c	// Read CSR
	li x6, -1
	csrrw x29, 0xc0c, x6	// Write all 1s to CSR
	csrrw x29, 0xc0c, x0	// Write all 0s to CSR
	csrrs x29, 0xc0c, x6	// Set all CSR bits
	csrrc x29, 0xc0c, x6	// Clear all CSR bits
	csrrw x29, 0xc0c, x30	// Restore CSR

// Testing CSR 0xc0d
	csrr x27, 0xc0d	// Read CSR
	li x21, -1
	csrrw x25, 0xc0d, x21	// Write all 1s to CSR
	csrrw x25, 0xc0d, x0	// Write all 0s to CSR
	csrrs x25, 0xc0d, x21	// Set all CSR bits
	csrrc x25, 0xc0d, x21	// Clear all CSR bits
	csrrw x25, 0xc0d, x27	// Restore CSR

// Testing CSR 0xc0e
	csrr x11, 0xc0e	// Read CSR
	li x6, -1
	csrrw x24, 0xc0e, x6	// Write all 1s to CSR
	csrrw x24, 0xc0e, x0	// Write all 0s to CSR
	csrrs x24, 0xc0e, x6	// Set all CSR bits
	csrrc x24, 0xc0e, x6	// Clear all CSR bits
	csrrw x24, 0xc0e, x11	// Restore CSR

// Testing CSR 0xc0f
	csrr x11, 0xc0f	// Read CSR
	li x16, -1
	csrrw x15, 0xc0f, x16	// Write all 1s to CSR
	csrrw x15, 0xc0f, x0	// Write all 0s to CSR
	csrrs x15, 0xc0f, x16	// Set all CSR bits
	csrrc x15, 0xc0f, x16	// Clear all CSR bits
	csrrw x15, 0xc0f, x11	// Restore CSR

// Testing CSR 0xc10
	csrr x9, 0xc10	// Read CSR
	li x17, -1
	csrrw x3, 0xc10, x17	// Write all 1s to CSR
	csrrw x3, 0xc10, x0	// Write all 0s to CSR
	csrrs x3, 0xc10, x17	// Set all CSR bits
	csrrc x3, 0xc10, x17	// Clear all CSR bits
	csrrw x3, 0xc10, x9	// Restore CSR

// Testing CSR 0xc11
	csrr x25, 0xc11	// Read CSR
	li x29, -1
	csrrw x3, 0xc11, x29	// Write all 1s to CSR
	csrrw x3, 0xc11, x0	// Write all 0s to CSR
	csrrs x3, 0xc11, x29	// Set all CSR bits
	csrrc x3, 0xc11, x29	// Clear all CSR bits
	csrrw x3, 0xc11, x25	// Restore CSR

// Testing CSR 0xc12
	csrr x6, 0xc12	// Read CSR
	li x4, -1
	csrrw x23, 0xc12, x4	// Write all 1s to CSR
	csrrw x23, 0xc12, x0	// Write all 0s to CSR
	csrrs x23, 0xc12, x4	// Set all CSR bits
	csrrc x23, 0xc12, x4	// Clear all CSR bits
	csrrw x23, 0xc12, x6	// Restore CSR

// Testing CSR 0xc13
	csrr x4, 0xc13	// Read CSR
	li x2, -1
	csrrw x23, 0xc13, x2	// Write all 1s to CSR
	csrrw x23, 0xc13, x0	// Write all 0s to CSR
	csrrs x23, 0xc13, x2	// Set all CSR bits
	csrrc x23, 0xc13, x2	// Clear all CSR bits
	csrrw x23, 0xc13, x4	// Restore CSR

// Testing CSR 0xc14
	csrr x24, 0xc14	// Read CSR
	li x13, -1
	csrrw x29, 0xc14, x13	// Write all 1s to CSR
	csrrw x29, 0xc14, x0	// Write all 0s to CSR
	csrrs x29, 0xc14, x13	// Set all CSR bits
	csrrc x29, 0xc14, x13	// Clear all CSR bits
	csrrw x29, 0xc14, x24	// Restore CSR

// Testing CSR 0xc15
	csrr x4, 0xc15	// Read CSR
	li x31, -1
	csrrw x26, 0xc15, x31	// Write all 1s to CSR
	csrrw x26, 0xc15, x0	// Write all 0s to CSR
	csrrs x26, 0xc15, x31	// Set all CSR bits
	csrrc x26, 0xc15, x31	// Clear all CSR bits
	csrrw x26, 0xc15, x4	// Restore CSR

// Testing CSR 0xc16
	csrr x4, 0xc16	// Read CSR
	li x27, -1
	csrrw x17, 0xc16, x27	// Write all 1s to CSR
	csrrw x17, 0xc16, x0	// Write all 0s to CSR
	csrrs x17, 0xc16, x27	// Set all CSR bits
	csrrc x17, 0xc16, x27	// Clear all CSR bits
	csrrw x17, 0xc16, x4	// Restore CSR

// Testing CSR 0xc17
	csrr x25, 0xc17	// Read CSR
	li x27, -1
	csrrw x10, 0xc17, x27	// Write all 1s to CSR
	csrrw x10, 0xc17, x0	// Write all 0s to CSR
	csrrs x10, 0xc17, x27	// Set all CSR bits
	csrrc x10, 0xc17, x27	// Clear all CSR bits
	csrrw x10, 0xc17, x25	// Restore CSR

// Testing CSR 0xc18
	csrr x7, 0xc18	// Read CSR
	li x28, -1
	csrrw x20, 0xc18, x28	// Write all 1s to CSR
	csrrw x20, 0xc18, x0	// Write all 0s to CSR
	csrrs x20, 0xc18, x28	// Set all CSR bits
	csrrc x20, 0xc18, x28	// Clear all CSR bits
	csrrw x20, 0xc18, x7	// Restore CSR

// Testing CSR 0xc19
	csrr x12, 0xc19	// Read CSR
	li x11, -1
	csrrw x7, 0xc19, x11	// Write all 1s to CSR
	csrrw x7, 0xc19, x0	// Write all 0s to CSR
	csrrs x7, 0xc19, x11	// Set all CSR bits
	csrrc x7, 0xc19, x11	// Clear all CSR bits
	csrrw x7, 0xc19, x12	// Restore CSR

// Testing CSR 0xc1a
	csrr x26, 0xc1a	// Read CSR
	li x8, -1
	csrrw x10, 0xc1a, x8	// Write all 1s to CSR
	csrrw x10, 0xc1a, x0	// Write all 0s to CSR
	csrrs x10, 0xc1a, x8	// Set all CSR bits
	csrrc x10, 0xc1a, x8	// Clear all CSR bits
	csrrw x10, 0xc1a, x26	// Restore CSR

// Testing CSR 0xc1b
	csrr x21, 0xc1b	// Read CSR
	li x9, -1
	csrrw x22, 0xc1b, x9	// Write all 1s to CSR
	csrrw x22, 0xc1b, x0	// Write all 0s to CSR
	csrrs x22, 0xc1b, x9	// Set all CSR bits
	csrrc x22, 0xc1b, x9	// Clear all CSR bits
	csrrw x22, 0xc1b, x21	// Restore CSR

// Testing CSR 0xc1c
	csrr x19, 0xc1c	// Read CSR
	li x18, -1
	csrrw x12, 0xc1c, x18	// Write all 1s to CSR
	csrrw x12, 0xc1c, x0	// Write all 0s to CSR
	csrrs x12, 0xc1c, x18	// Set all CSR bits
	csrrc x12, 0xc1c, x18	// Clear all CSR bits
	csrrw x12, 0xc1c, x19	// Restore CSR

// Testing CSR 0xc1d
	csrr x21, 0xc1d	// Read CSR
	li x19, -1
	csrrw x24, 0xc1d, x19	// Write all 1s to CSR
	csrrw x24, 0xc1d, x0	// Write all 0s to CSR
	csrrs x24, 0xc1d, x19	// Set all CSR bits
	csrrc x24, 0xc1d, x19	// Clear all CSR bits
	csrrw x24, 0xc1d, x21	// Restore CSR

// Testing CSR 0xc1e
	csrr x28, 0xc1e	// Read CSR
	li x22, -1
	csrrw x21, 0xc1e, x22	// Write all 1s to CSR
	csrrw x21, 0xc1e, x0	// Write all 0s to CSR
	csrrs x21, 0xc1e, x22	// Set all CSR bits
	csrrc x21, 0xc1e, x22	// Clear all CSR bits
	csrrw x21, 0xc1e, x28	// Restore CSR

// Testing CSR 0xc1f
	csrr x30, 0xc1f	// Read CSR
	li x31, -1
	csrrw x2, 0xc1f, x31	// Write all 1s to CSR
	csrrw x2, 0xc1f, x0	// Write all 0s to CSR
	csrrs x2, 0xc1f, x31	// Set all CSR bits
	csrrc x2, 0xc1f, x31	// Clear all CSR bits
	csrrw x2, 0xc1f, x30	// Restore CSR

// Testing CSR 0xc20
	csrr x30, 0xc20	// Read CSR
	li x17, -1
	csrrw x16, 0xc20, x17	// Write all 1s to CSR
	csrrw x16, 0xc20, x0	// Write all 0s to CSR
	csrrs x16, 0xc20, x17	// Set all CSR bits
	csrrc x16, 0xc20, x17	// Clear all CSR bits
	csrrw x16, 0xc20, x30	// Restore CSR

// Testing CSR 0xc21
	csrr x31, 0xc21	// Read CSR
	li x23, -1
	csrrw x30, 0xc21, x23	// Write all 1s to CSR
	csrrw x30, 0xc21, x0	// Write all 0s to CSR
	csrrs x30, 0xc21, x23	// Set all CSR bits
	csrrc x30, 0xc21, x23	// Clear all CSR bits
	csrrw x30, 0xc21, x31	// Restore CSR

// Testing CSR 0xc22
	csrr x17, 0xc22	// Read CSR
	li x22, -1
	csrrw x6, 0xc22, x22	// Write all 1s to CSR
	csrrw x6, 0xc22, x0	// Write all 0s to CSR
	csrrs x6, 0xc22, x22	// Set all CSR bits
	csrrc x6, 0xc22, x22	// Clear all CSR bits
	csrrw x6, 0xc22, x17	// Restore CSR

// Testing CSR 0xc23
	csrr x21, 0xc23	// Read CSR
	li x11, -1
	csrrw x12, 0xc23, x11	// Write all 1s to CSR
	csrrw x12, 0xc23, x0	// Write all 0s to CSR
	csrrs x12, 0xc23, x11	// Set all CSR bits
	csrrc x12, 0xc23, x11	// Clear all CSR bits
	csrrw x12, 0xc23, x21	// Restore CSR

// Testing CSR 0xc24
	csrr x18, 0xc24	// Read CSR
	li x25, -1
	csrrw x24, 0xc24, x25	// Write all 1s to CSR
	csrrw x24, 0xc24, x0	// Write all 0s to CSR
	csrrs x24, 0xc24, x25	// Set all CSR bits
	csrrc x24, 0xc24, x25	// Clear all CSR bits
	csrrw x24, 0xc24, x18	// Restore CSR

// Testing CSR 0xc25
	csrr x10, 0xc25	// Read CSR
	li x11, -1
	csrrw x13, 0xc25, x11	// Write all 1s to CSR
	csrrw x13, 0xc25, x0	// Write all 0s to CSR
	csrrs x13, 0xc25, x11	// Set all CSR bits
	csrrc x13, 0xc25, x11	// Clear all CSR bits
	csrrw x13, 0xc25, x10	// Restore CSR

// Testing CSR 0xc26
	csrr x27, 0xc26	// Read CSR
	li x31, -1
	csrrw x4, 0xc26, x31	// Write all 1s to CSR
	csrrw x4, 0xc26, x0	// Write all 0s to CSR
	csrrs x4, 0xc26, x31	// Set all CSR bits
	csrrc x4, 0xc26, x31	// Clear all CSR bits
	csrrw x4, 0xc26, x27	// Restore CSR

// Testing CSR 0xc27
	csrr x1, 0xc27	// Read CSR
	li x21, -1
	csrrw x11, 0xc27, x21	// Write all 1s to CSR
	csrrw x11, 0xc27, x0	// Write all 0s to CSR
	csrrs x11, 0xc27, x21	// Set all CSR bits
	csrrc x11, 0xc27, x21	// Clear all CSR bits
	csrrw x11, 0xc27, x1	// Restore CSR

// Testing CSR 0xc28
	csrr x18, 0xc28	// Read CSR
	li x24, -1
	csrrw x31, 0xc28, x24	// Write all 1s to CSR
	csrrw x31, 0xc28, x0	// Write all 0s to CSR
	csrrs x31, 0xc28, x24	// Set all CSR bits
	csrrc x31, 0xc28, x24	// Clear all CSR bits
	csrrw x31, 0xc28, x18	// Restore CSR

// Testing CSR 0xc29
	csrr x29, 0xc29	// Read CSR
	li x6, -1
	csrrw x18, 0xc29, x6	// Write all 1s to CSR
	csrrw x18, 0xc29, x0	// Write all 0s to CSR
	csrrs x18, 0xc29, x6	// Set all CSR bits
	csrrc x18, 0xc29, x6	// Clear all CSR bits
	csrrw x18, 0xc29, x29	// Restore CSR

// Testing CSR 0xc2a
	csrr x31, 0xc2a	// Read CSR
	li x13, -1
	csrrw x28, 0xc2a, x13	// Write all 1s to CSR
	csrrw x28, 0xc2a, x0	// Write all 0s to CSR
	csrrs x28, 0xc2a, x13	// Set all CSR bits
	csrrc x28, 0xc2a, x13	// Clear all CSR bits
	csrrw x28, 0xc2a, x31	// Restore CSR

// Testing CSR 0xc2b
	csrr x16, 0xc2b	// Read CSR
	li x4, -1
	csrrw x9, 0xc2b, x4	// Write all 1s to CSR
	csrrw x9, 0xc2b, x0	// Write all 0s to CSR
	csrrs x9, 0xc2b, x4	// Set all CSR bits
	csrrc x9, 0xc2b, x4	// Clear all CSR bits
	csrrw x9, 0xc2b, x16	// Restore CSR

// Testing CSR 0xc2c
	csrr x10, 0xc2c	// Read CSR
	li x18, -1
	csrrw x19, 0xc2c, x18	// Write all 1s to CSR
	csrrw x19, 0xc2c, x0	// Write all 0s to CSR
	csrrs x19, 0xc2c, x18	// Set all CSR bits
	csrrc x19, 0xc2c, x18	// Clear all CSR bits
	csrrw x19, 0xc2c, x10	// Restore CSR

// Testing CSR 0xc2d
	csrr x23, 0xc2d	// Read CSR
	li x10, -1
	csrrw x17, 0xc2d, x10	// Write all 1s to CSR
	csrrw x17, 0xc2d, x0	// Write all 0s to CSR
	csrrs x17, 0xc2d, x10	// Set all CSR bits
	csrrc x17, 0xc2d, x10	// Clear all CSR bits
	csrrw x17, 0xc2d, x23	// Restore CSR

// Testing CSR 0xc2e
	csrr x17, 0xc2e	// Read CSR
	li x11, -1
	csrrw x1, 0xc2e, x11	// Write all 1s to CSR
	csrrw x1, 0xc2e, x0	// Write all 0s to CSR
	csrrs x1, 0xc2e, x11	// Set all CSR bits
	csrrc x1, 0xc2e, x11	// Clear all CSR bits
	csrrw x1, 0xc2e, x17	// Restore CSR

// Testing CSR 0xc2f
	csrr x29, 0xc2f	// Read CSR
	li x7, -1
	csrrw x6, 0xc2f, x7	// Write all 1s to CSR
	csrrw x6, 0xc2f, x0	// Write all 0s to CSR
	csrrs x6, 0xc2f, x7	// Set all CSR bits
	csrrc x6, 0xc2f, x7	// Clear all CSR bits
	csrrw x6, 0xc2f, x29	// Restore CSR

// Testing CSR 0xc30
	csrr x8, 0xc30	// Read CSR
	li x27, -1
	csrrw x2, 0xc30, x27	// Write all 1s to CSR
	csrrw x2, 0xc30, x0	// Write all 0s to CSR
	csrrs x2, 0xc30, x27	// Set all CSR bits
	csrrc x2, 0xc30, x27	// Clear all CSR bits
	csrrw x2, 0xc30, x8	// Restore CSR

// Testing CSR 0xc31
	csrr x27, 0xc31	// Read CSR
	li x30, -1
	csrrw x11, 0xc31, x30	// Write all 1s to CSR
	csrrw x11, 0xc31, x0	// Write all 0s to CSR
	csrrs x11, 0xc31, x30	// Set all CSR bits
	csrrc x11, 0xc31, x30	// Clear all CSR bits
	csrrw x11, 0xc31, x27	// Restore CSR

// Testing CSR 0xc32
	csrr x25, 0xc32	// Read CSR
	li x3, -1
	csrrw x9, 0xc32, x3	// Write all 1s to CSR
	csrrw x9, 0xc32, x0	// Write all 0s to CSR
	csrrs x9, 0xc32, x3	// Set all CSR bits
	csrrc x9, 0xc32, x3	// Clear all CSR bits
	csrrw x9, 0xc32, x25	// Restore CSR

// Testing CSR 0xc33
	csrr x16, 0xc33	// Read CSR
	li x11, -1
	csrrw x24, 0xc33, x11	// Write all 1s to CSR
	csrrw x24, 0xc33, x0	// Write all 0s to CSR
	csrrs x24, 0xc33, x11	// Set all CSR bits
	csrrc x24, 0xc33, x11	// Clear all CSR bits
	csrrw x24, 0xc33, x16	// Restore CSR

// Testing CSR 0xc34
	csrr x18, 0xc34	// Read CSR
	li x23, -1
	csrrw x8, 0xc34, x23	// Write all 1s to CSR
	csrrw x8, 0xc34, x0	// Write all 0s to CSR
	csrrs x8, 0xc34, x23	// Set all CSR bits
	csrrc x8, 0xc34, x23	// Clear all CSR bits
	csrrw x8, 0xc34, x18	// Restore CSR

// Testing CSR 0xc35
	csrr x29, 0xc35	// Read CSR
	li x27, -1
	csrrw x28, 0xc35, x27	// Write all 1s to CSR
	csrrw x28, 0xc35, x0	// Write all 0s to CSR
	csrrs x28, 0xc35, x27	// Set all CSR bits
	csrrc x28, 0xc35, x27	// Clear all CSR bits
	csrrw x28, 0xc35, x29	// Restore CSR

// Testing CSR 0xc36
	csrr x22, 0xc36	// Read CSR
	li x23, -1
	csrrw x6, 0xc36, x23	// Write all 1s to CSR
	csrrw x6, 0xc36, x0	// Write all 0s to CSR
	csrrs x6, 0xc36, x23	// Set all CSR bits
	csrrc x6, 0xc36, x23	// Clear all CSR bits
	csrrw x6, 0xc36, x22	// Restore CSR

// Testing CSR 0xc37
	csrr x29, 0xc37	// Read CSR
	li x11, -1
	csrrw x6, 0xc37, x11	// Write all 1s to CSR
	csrrw x6, 0xc37, x0	// Write all 0s to CSR
	csrrs x6, 0xc37, x11	// Set all CSR bits
	csrrc x6, 0xc37, x11	// Clear all CSR bits
	csrrw x6, 0xc37, x29	// Restore CSR

// Testing CSR 0xc38
	csrr x6, 0xc38	// Read CSR
	li x19, -1
	csrrw x8, 0xc38, x19	// Write all 1s to CSR
	csrrw x8, 0xc38, x0	// Write all 0s to CSR
	csrrs x8, 0xc38, x19	// Set all CSR bits
	csrrc x8, 0xc38, x19	// Clear all CSR bits
	csrrw x8, 0xc38, x6	// Restore CSR

// Testing CSR 0xc39
	csrr x7, 0xc39	// Read CSR
	li x3, -1
	csrrw x8, 0xc39, x3	// Write all 1s to CSR
	csrrw x8, 0xc39, x0	// Write all 0s to CSR
	csrrs x8, 0xc39, x3	// Set all CSR bits
	csrrc x8, 0xc39, x3	// Clear all CSR bits
	csrrw x8, 0xc39, x7	// Restore CSR

// Testing CSR 0xc3a
	csrr x30, 0xc3a	// Read CSR
	li x5, -1
	csrrw x14, 0xc3a, x5	// Write all 1s to CSR
	csrrw x14, 0xc3a, x0	// Write all 0s to CSR
	csrrs x14, 0xc3a, x5	// Set all CSR bits
	csrrc x14, 0xc3a, x5	// Clear all CSR bits
	csrrw x14, 0xc3a, x30	// Restore CSR

// Testing CSR 0xc3b
	csrr x23, 0xc3b	// Read CSR
	li x27, -1
	csrrw x31, 0xc3b, x27	// Write all 1s to CSR
	csrrw x31, 0xc3b, x0	// Write all 0s to CSR
	csrrs x31, 0xc3b, x27	// Set all CSR bits
	csrrc x31, 0xc3b, x27	// Clear all CSR bits
	csrrw x31, 0xc3b, x23	// Restore CSR

// Testing CSR 0xc3c
	csrr x27, 0xc3c	// Read CSR
	li x28, -1
	csrrw x5, 0xc3c, x28	// Write all 1s to CSR
	csrrw x5, 0xc3c, x0	// Write all 0s to CSR
	csrrs x5, 0xc3c, x28	// Set all CSR bits
	csrrc x5, 0xc3c, x28	// Clear all CSR bits
	csrrw x5, 0xc3c, x27	// Restore CSR

// Testing CSR 0xc3d
	csrr x31, 0xc3d	// Read CSR
	li x27, -1
	csrrw x28, 0xc3d, x27	// Write all 1s to CSR
	csrrw x28, 0xc3d, x0	// Write all 0s to CSR
	csrrs x28, 0xc3d, x27	// Set all CSR bits
	csrrc x28, 0xc3d, x27	// Clear all CSR bits
	csrrw x28, 0xc3d, x31	// Restore CSR

// Testing CSR 0xc3e
	csrr x2, 0xc3e	// Read CSR
	li x22, -1
	csrrw x30, 0xc3e, x22	// Write all 1s to CSR
	csrrw x30, 0xc3e, x0	// Write all 0s to CSR
	csrrs x30, 0xc3e, x22	// Set all CSR bits
	csrrc x30, 0xc3e, x22	// Clear all CSR bits
	csrrw x30, 0xc3e, x2	// Restore CSR

// Testing CSR 0xc3f
	csrr x29, 0xc3f	// Read CSR
	li x25, -1
	csrrw x15, 0xc3f, x25	// Write all 1s to CSR
	csrrw x15, 0xc3f, x0	// Write all 0s to CSR
	csrrs x15, 0xc3f, x25	// Set all CSR bits
	csrrc x15, 0xc3f, x25	// Clear all CSR bits
	csrrw x15, 0xc3f, x29	// Restore CSR

// Testing CSR 0xc40
	csrr x31, 0xc40	// Read CSR
	li x25, -1
	csrrw x22, 0xc40, x25	// Write all 1s to CSR
	csrrw x22, 0xc40, x0	// Write all 0s to CSR
	csrrs x22, 0xc40, x25	// Set all CSR bits
	csrrc x22, 0xc40, x25	// Clear all CSR bits
	csrrw x22, 0xc40, x31	// Restore CSR

// Testing CSR 0xc41
	csrr x17, 0xc41	// Read CSR
	li x8, -1
	csrrw x20, 0xc41, x8	// Write all 1s to CSR
	csrrw x20, 0xc41, x0	// Write all 0s to CSR
	csrrs x20, 0xc41, x8	// Set all CSR bits
	csrrc x20, 0xc41, x8	// Clear all CSR bits
	csrrw x20, 0xc41, x17	// Restore CSR

// Testing CSR 0xc42
	csrr x6, 0xc42	// Read CSR
	li x24, -1
	csrrw x26, 0xc42, x24	// Write all 1s to CSR
	csrrw x26, 0xc42, x0	// Write all 0s to CSR
	csrrs x26, 0xc42, x24	// Set all CSR bits
	csrrc x26, 0xc42, x24	// Clear all CSR bits
	csrrw x26, 0xc42, x6	// Restore CSR

// Testing CSR 0xc43
	csrr x23, 0xc43	// Read CSR
	li x25, -1
	csrrw x29, 0xc43, x25	// Write all 1s to CSR
	csrrw x29, 0xc43, x0	// Write all 0s to CSR
	csrrs x29, 0xc43, x25	// Set all CSR bits
	csrrc x29, 0xc43, x25	// Clear all CSR bits
	csrrw x29, 0xc43, x23	// Restore CSR

// Testing CSR 0xc44
	csrr x18, 0xc44	// Read CSR
	li x13, -1
	csrrw x5, 0xc44, x13	// Write all 1s to CSR
	csrrw x5, 0xc44, x0	// Write all 0s to CSR
	csrrs x5, 0xc44, x13	// Set all CSR bits
	csrrc x5, 0xc44, x13	// Clear all CSR bits
	csrrw x5, 0xc44, x18	// Restore CSR

// Testing CSR 0xc45
	csrr x22, 0xc45	// Read CSR
	li x7, -1
	csrrw x18, 0xc45, x7	// Write all 1s to CSR
	csrrw x18, 0xc45, x0	// Write all 0s to CSR
	csrrs x18, 0xc45, x7	// Set all CSR bits
	csrrc x18, 0xc45, x7	// Clear all CSR bits
	csrrw x18, 0xc45, x22	// Restore CSR

// Testing CSR 0xc46
	csrr x15, 0xc46	// Read CSR
	li x18, -1
	csrrw x7, 0xc46, x18	// Write all 1s to CSR
	csrrw x7, 0xc46, x0	// Write all 0s to CSR
	csrrs x7, 0xc46, x18	// Set all CSR bits
	csrrc x7, 0xc46, x18	// Clear all CSR bits
	csrrw x7, 0xc46, x15	// Restore CSR

// Testing CSR 0xc47
	csrr x18, 0xc47	// Read CSR
	li x27, -1
	csrrw x15, 0xc47, x27	// Write all 1s to CSR
	csrrw x15, 0xc47, x0	// Write all 0s to CSR
	csrrs x15, 0xc47, x27	// Set all CSR bits
	csrrc x15, 0xc47, x27	// Clear all CSR bits
	csrrw x15, 0xc47, x18	// Restore CSR

// Testing CSR 0xc48
	csrr x28, 0xc48	// Read CSR
	li x24, -1
	csrrw x11, 0xc48, x24	// Write all 1s to CSR
	csrrw x11, 0xc48, x0	// Write all 0s to CSR
	csrrs x11, 0xc48, x24	// Set all CSR bits
	csrrc x11, 0xc48, x24	// Clear all CSR bits
	csrrw x11, 0xc48, x28	// Restore CSR

// Testing CSR 0xc49
	csrr x30, 0xc49	// Read CSR
	li x22, -1
	csrrw x29, 0xc49, x22	// Write all 1s to CSR
	csrrw x29, 0xc49, x0	// Write all 0s to CSR
	csrrs x29, 0xc49, x22	// Set all CSR bits
	csrrc x29, 0xc49, x22	// Clear all CSR bits
	csrrw x29, 0xc49, x30	// Restore CSR

// Testing CSR 0xc4a
	csrr x7, 0xc4a	// Read CSR
	li x28, -1
	csrrw x22, 0xc4a, x28	// Write all 1s to CSR
	csrrw x22, 0xc4a, x0	// Write all 0s to CSR
	csrrs x22, 0xc4a, x28	// Set all CSR bits
	csrrc x22, 0xc4a, x28	// Clear all CSR bits
	csrrw x22, 0xc4a, x7	// Restore CSR

// Testing CSR 0xc4b
	csrr x31, 0xc4b	// Read CSR
	li x25, -1
	csrrw x5, 0xc4b, x25	// Write all 1s to CSR
	csrrw x5, 0xc4b, x0	// Write all 0s to CSR
	csrrs x5, 0xc4b, x25	// Set all CSR bits
	csrrc x5, 0xc4b, x25	// Clear all CSR bits
	csrrw x5, 0xc4b, x31	// Restore CSR

// Testing CSR 0xc4c
	csrr x6, 0xc4c	// Read CSR
	li x18, -1
	csrrw x8, 0xc4c, x18	// Write all 1s to CSR
	csrrw x8, 0xc4c, x0	// Write all 0s to CSR
	csrrs x8, 0xc4c, x18	// Set all CSR bits
	csrrc x8, 0xc4c, x18	// Clear all CSR bits
	csrrw x8, 0xc4c, x6	// Restore CSR

// Testing CSR 0xc4d
	csrr x29, 0xc4d	// Read CSR
	li x3, -1
	csrrw x9, 0xc4d, x3	// Write all 1s to CSR
	csrrw x9, 0xc4d, x0	// Write all 0s to CSR
	csrrs x9, 0xc4d, x3	// Set all CSR bits
	csrrc x9, 0xc4d, x3	// Clear all CSR bits
	csrrw x9, 0xc4d, x29	// Restore CSR

// Testing CSR 0xc4e
	csrr x31, 0xc4e	// Read CSR
	li x25, -1
	csrrw x11, 0xc4e, x25	// Write all 1s to CSR
	csrrw x11, 0xc4e, x0	// Write all 0s to CSR
	csrrs x11, 0xc4e, x25	// Set all CSR bits
	csrrc x11, 0xc4e, x25	// Clear all CSR bits
	csrrw x11, 0xc4e, x31	// Restore CSR

// Testing CSR 0xc4f
	csrr x15, 0xc4f	// Read CSR
	li x12, -1
	csrrw x22, 0xc4f, x12	// Write all 1s to CSR
	csrrw x22, 0xc4f, x0	// Write all 0s to CSR
	csrrs x22, 0xc4f, x12	// Set all CSR bits
	csrrc x22, 0xc4f, x12	// Clear all CSR bits
	csrrw x22, 0xc4f, x15	// Restore CSR

// Testing CSR 0xc50
	csrr x9, 0xc50	// Read CSR
	li x23, -1
	csrrw x26, 0xc50, x23	// Write all 1s to CSR
	csrrw x26, 0xc50, x0	// Write all 0s to CSR
	csrrs x26, 0xc50, x23	// Set all CSR bits
	csrrc x26, 0xc50, x23	// Clear all CSR bits
	csrrw x26, 0xc50, x9	// Restore CSR

// Testing CSR 0xc51
	csrr x4, 0xc51	// Read CSR
	li x2, -1
	csrrw x31, 0xc51, x2	// Write all 1s to CSR
	csrrw x31, 0xc51, x0	// Write all 0s to CSR
	csrrs x31, 0xc51, x2	// Set all CSR bits
	csrrc x31, 0xc51, x2	// Clear all CSR bits
	csrrw x31, 0xc51, x4	// Restore CSR

// Testing CSR 0xc52
	csrr x4, 0xc52	// Read CSR
	li x8, -1
	csrrw x3, 0xc52, x8	// Write all 1s to CSR
	csrrw x3, 0xc52, x0	// Write all 0s to CSR
	csrrs x3, 0xc52, x8	// Set all CSR bits
	csrrc x3, 0xc52, x8	// Clear all CSR bits
	csrrw x3, 0xc52, x4	// Restore CSR

// Testing CSR 0xc53
	csrr x3, 0xc53	// Read CSR
	li x29, -1
	csrrw x9, 0xc53, x29	// Write all 1s to CSR
	csrrw x9, 0xc53, x0	// Write all 0s to CSR
	csrrs x9, 0xc53, x29	// Set all CSR bits
	csrrc x9, 0xc53, x29	// Clear all CSR bits
	csrrw x9, 0xc53, x3	// Restore CSR

// Testing CSR 0xc54
	csrr x20, 0xc54	// Read CSR
	li x31, -1
	csrrw x27, 0xc54, x31	// Write all 1s to CSR
	csrrw x27, 0xc54, x0	// Write all 0s to CSR
	csrrs x27, 0xc54, x31	// Set all CSR bits
	csrrc x27, 0xc54, x31	// Clear all CSR bits
	csrrw x27, 0xc54, x20	// Restore CSR

// Testing CSR 0xc55
	csrr x16, 0xc55	// Read CSR
	li x12, -1
	csrrw x11, 0xc55, x12	// Write all 1s to CSR
	csrrw x11, 0xc55, x0	// Write all 0s to CSR
	csrrs x11, 0xc55, x12	// Set all CSR bits
	csrrc x11, 0xc55, x12	// Clear all CSR bits
	csrrw x11, 0xc55, x16	// Restore CSR

// Testing CSR 0xc56
	csrr x24, 0xc56	// Read CSR
	li x23, -1
	csrrw x31, 0xc56, x23	// Write all 1s to CSR
	csrrw x31, 0xc56, x0	// Write all 0s to CSR
	csrrs x31, 0xc56, x23	// Set all CSR bits
	csrrc x31, 0xc56, x23	// Clear all CSR bits
	csrrw x31, 0xc56, x24	// Restore CSR

// Testing CSR 0xc57
	csrr x24, 0xc57	// Read CSR
	li x26, -1
	csrrw x8, 0xc57, x26	// Write all 1s to CSR
	csrrw x8, 0xc57, x0	// Write all 0s to CSR
	csrrs x8, 0xc57, x26	// Set all CSR bits
	csrrc x8, 0xc57, x26	// Clear all CSR bits
	csrrw x8, 0xc57, x24	// Restore CSR

// Testing CSR 0xc58
	csrr x27, 0xc58	// Read CSR
	li x30, -1
	csrrw x3, 0xc58, x30	// Write all 1s to CSR
	csrrw x3, 0xc58, x0	// Write all 0s to CSR
	csrrs x3, 0xc58, x30	// Set all CSR bits
	csrrc x3, 0xc58, x30	// Clear all CSR bits
	csrrw x3, 0xc58, x27	// Restore CSR

// Testing CSR 0xc59
	csrr x13, 0xc59	// Read CSR
	li x12, -1
	csrrw x6, 0xc59, x12	// Write all 1s to CSR
	csrrw x6, 0xc59, x0	// Write all 0s to CSR
	csrrs x6, 0xc59, x12	// Set all CSR bits
	csrrc x6, 0xc59, x12	// Clear all CSR bits
	csrrw x6, 0xc59, x13	// Restore CSR

// Testing CSR 0xc5a
	csrr x20, 0xc5a	// Read CSR
	li x8, -1
	csrrw x15, 0xc5a, x8	// Write all 1s to CSR
	csrrw x15, 0xc5a, x0	// Write all 0s to CSR
	csrrs x15, 0xc5a, x8	// Set all CSR bits
	csrrc x15, 0xc5a, x8	// Clear all CSR bits
	csrrw x15, 0xc5a, x20	// Restore CSR

// Testing CSR 0xc5b
	csrr x27, 0xc5b	// Read CSR
	li x26, -1
	csrrw x29, 0xc5b, x26	// Write all 1s to CSR
	csrrw x29, 0xc5b, x0	// Write all 0s to CSR
	csrrs x29, 0xc5b, x26	// Set all CSR bits
	csrrc x29, 0xc5b, x26	// Clear all CSR bits
	csrrw x29, 0xc5b, x27	// Restore CSR

// Testing CSR 0xc5c
	csrr x7, 0xc5c	// Read CSR
	li x21, -1
	csrrw x1, 0xc5c, x21	// Write all 1s to CSR
	csrrw x1, 0xc5c, x0	// Write all 0s to CSR
	csrrs x1, 0xc5c, x21	// Set all CSR bits
	csrrc x1, 0xc5c, x21	// Clear all CSR bits
	csrrw x1, 0xc5c, x7	// Restore CSR

// Testing CSR 0xc5d
	csrr x6, 0xc5d	// Read CSR
	li x18, -1
	csrrw x7, 0xc5d, x18	// Write all 1s to CSR
	csrrw x7, 0xc5d, x0	// Write all 0s to CSR
	csrrs x7, 0xc5d, x18	// Set all CSR bits
	csrrc x7, 0xc5d, x18	// Clear all CSR bits
	csrrw x7, 0xc5d, x6	// Restore CSR

// Testing CSR 0xc5e
	csrr x19, 0xc5e	// Read CSR
	li x27, -1
	csrrw x1, 0xc5e, x27	// Write all 1s to CSR
	csrrw x1, 0xc5e, x0	// Write all 0s to CSR
	csrrs x1, 0xc5e, x27	// Set all CSR bits
	csrrc x1, 0xc5e, x27	// Clear all CSR bits
	csrrw x1, 0xc5e, x19	// Restore CSR

// Testing CSR 0xc5f
	csrr x24, 0xc5f	// Read CSR
	li x19, -1
	csrrw x31, 0xc5f, x19	// Write all 1s to CSR
	csrrw x31, 0xc5f, x0	// Write all 0s to CSR
	csrrs x31, 0xc5f, x19	// Set all CSR bits
	csrrc x31, 0xc5f, x19	// Clear all CSR bits
	csrrw x31, 0xc5f, x24	// Restore CSR

// Testing CSR 0xc60
	csrr x7, 0xc60	// Read CSR
	li x17, -1
	csrrw x10, 0xc60, x17	// Write all 1s to CSR
	csrrw x10, 0xc60, x0	// Write all 0s to CSR
	csrrs x10, 0xc60, x17	// Set all CSR bits
	csrrc x10, 0xc60, x17	// Clear all CSR bits
	csrrw x10, 0xc60, x7	// Restore CSR

// Testing CSR 0xc61
	csrr x6, 0xc61	// Read CSR
	li x10, -1
	csrrw x5, 0xc61, x10	// Write all 1s to CSR
	csrrw x5, 0xc61, x0	// Write all 0s to CSR
	csrrs x5, 0xc61, x10	// Set all CSR bits
	csrrc x5, 0xc61, x10	// Clear all CSR bits
	csrrw x5, 0xc61, x6	// Restore CSR

// Testing CSR 0xc62
	csrr x17, 0xc62	// Read CSR
	li x15, -1
	csrrw x7, 0xc62, x15	// Write all 1s to CSR
	csrrw x7, 0xc62, x0	// Write all 0s to CSR
	csrrs x7, 0xc62, x15	// Set all CSR bits
	csrrc x7, 0xc62, x15	// Clear all CSR bits
	csrrw x7, 0xc62, x17	// Restore CSR

// Testing CSR 0xc63
	csrr x18, 0xc63	// Read CSR
	li x22, -1
	csrrw x23, 0xc63, x22	// Write all 1s to CSR
	csrrw x23, 0xc63, x0	// Write all 0s to CSR
	csrrs x23, 0xc63, x22	// Set all CSR bits
	csrrc x23, 0xc63, x22	// Clear all CSR bits
	csrrw x23, 0xc63, x18	// Restore CSR

// Testing CSR 0xc64
	csrr x20, 0xc64	// Read CSR
	li x27, -1
	csrrw x26, 0xc64, x27	// Write all 1s to CSR
	csrrw x26, 0xc64, x0	// Write all 0s to CSR
	csrrs x26, 0xc64, x27	// Set all CSR bits
	csrrc x26, 0xc64, x27	// Clear all CSR bits
	csrrw x26, 0xc64, x20	// Restore CSR

// Testing CSR 0xc65
	csrr x31, 0xc65	// Read CSR
	li x8, -1
	csrrw x12, 0xc65, x8	// Write all 1s to CSR
	csrrw x12, 0xc65, x0	// Write all 0s to CSR
	csrrs x12, 0xc65, x8	// Set all CSR bits
	csrrc x12, 0xc65, x8	// Clear all CSR bits
	csrrw x12, 0xc65, x31	// Restore CSR

// Testing CSR 0xc66
	csrr x2, 0xc66	// Read CSR
	li x12, -1
	csrrw x31, 0xc66, x12	// Write all 1s to CSR
	csrrw x31, 0xc66, x0	// Write all 0s to CSR
	csrrs x31, 0xc66, x12	// Set all CSR bits
	csrrc x31, 0xc66, x12	// Clear all CSR bits
	csrrw x31, 0xc66, x2	// Restore CSR

// Testing CSR 0xc67
	csrr x17, 0xc67	// Read CSR
	li x25, -1
	csrrw x21, 0xc67, x25	// Write all 1s to CSR
	csrrw x21, 0xc67, x0	// Write all 0s to CSR
	csrrs x21, 0xc67, x25	// Set all CSR bits
	csrrc x21, 0xc67, x25	// Clear all CSR bits
	csrrw x21, 0xc67, x17	// Restore CSR

// Testing CSR 0xc68
	csrr x18, 0xc68	// Read CSR
	li x9, -1
	csrrw x6, 0xc68, x9	// Write all 1s to CSR
	csrrw x6, 0xc68, x0	// Write all 0s to CSR
	csrrs x6, 0xc68, x9	// Set all CSR bits
	csrrc x6, 0xc68, x9	// Clear all CSR bits
	csrrw x6, 0xc68, x18	// Restore CSR

// Testing CSR 0xc69
	csrr x14, 0xc69	// Read CSR
	li x7, -1
	csrrw x28, 0xc69, x7	// Write all 1s to CSR
	csrrw x28, 0xc69, x0	// Write all 0s to CSR
	csrrs x28, 0xc69, x7	// Set all CSR bits
	csrrc x28, 0xc69, x7	// Clear all CSR bits
	csrrw x28, 0xc69, x14	// Restore CSR

// Testing CSR 0xc6a
	csrr x11, 0xc6a	// Read CSR
	li x10, -1
	csrrw x27, 0xc6a, x10	// Write all 1s to CSR
	csrrw x27, 0xc6a, x0	// Write all 0s to CSR
	csrrs x27, 0xc6a, x10	// Set all CSR bits
	csrrc x27, 0xc6a, x10	// Clear all CSR bits
	csrrw x27, 0xc6a, x11	// Restore CSR

// Testing CSR 0xc6b
	csrr x25, 0xc6b	// Read CSR
	li x3, -1
	csrrw x10, 0xc6b, x3	// Write all 1s to CSR
	csrrw x10, 0xc6b, x0	// Write all 0s to CSR
	csrrs x10, 0xc6b, x3	// Set all CSR bits
	csrrc x10, 0xc6b, x3	// Clear all CSR bits
	csrrw x10, 0xc6b, x25	// Restore CSR

// Testing CSR 0xc6c
	csrr x20, 0xc6c	// Read CSR
	li x27, -1
	csrrw x19, 0xc6c, x27	// Write all 1s to CSR
	csrrw x19, 0xc6c, x0	// Write all 0s to CSR
	csrrs x19, 0xc6c, x27	// Set all CSR bits
	csrrc x19, 0xc6c, x27	// Clear all CSR bits
	csrrw x19, 0xc6c, x20	// Restore CSR

// Testing CSR 0xc6d
	csrr x29, 0xc6d	// Read CSR
	li x4, -1
	csrrw x24, 0xc6d, x4	// Write all 1s to CSR
	csrrw x24, 0xc6d, x0	// Write all 0s to CSR
	csrrs x24, 0xc6d, x4	// Set all CSR bits
	csrrc x24, 0xc6d, x4	// Clear all CSR bits
	csrrw x24, 0xc6d, x29	// Restore CSR

// Testing CSR 0xc6e
	csrr x29, 0xc6e	// Read CSR
	li x26, -1
	csrrw x20, 0xc6e, x26	// Write all 1s to CSR
	csrrw x20, 0xc6e, x0	// Write all 0s to CSR
	csrrs x20, 0xc6e, x26	// Set all CSR bits
	csrrc x20, 0xc6e, x26	// Clear all CSR bits
	csrrw x20, 0xc6e, x29	// Restore CSR

// Testing CSR 0xc6f
	csrr x30, 0xc6f	// Read CSR
	li x11, -1
	csrrw x22, 0xc6f, x11	// Write all 1s to CSR
	csrrw x22, 0xc6f, x0	// Write all 0s to CSR
	csrrs x22, 0xc6f, x11	// Set all CSR bits
	csrrc x22, 0xc6f, x11	// Clear all CSR bits
	csrrw x22, 0xc6f, x30	// Restore CSR

// Testing CSR 0xc70
	csrr x22, 0xc70	// Read CSR
	li x1, -1
	csrrw x31, 0xc70, x1	// Write all 1s to CSR
	csrrw x31, 0xc70, x0	// Write all 0s to CSR
	csrrs x31, 0xc70, x1	// Set all CSR bits
	csrrc x31, 0xc70, x1	// Clear all CSR bits
	csrrw x31, 0xc70, x22	// Restore CSR

// Testing CSR 0xc71
	csrr x31, 0xc71	// Read CSR
	li x1, -1
	csrrw x15, 0xc71, x1	// Write all 1s to CSR
	csrrw x15, 0xc71, x0	// Write all 0s to CSR
	csrrs x15, 0xc71, x1	// Set all CSR bits
	csrrc x15, 0xc71, x1	// Clear all CSR bits
	csrrw x15, 0xc71, x31	// Restore CSR

// Testing CSR 0xc72
	csrr x2, 0xc72	// Read CSR
	li x16, -1
	csrrw x10, 0xc72, x16	// Write all 1s to CSR
	csrrw x10, 0xc72, x0	// Write all 0s to CSR
	csrrs x10, 0xc72, x16	// Set all CSR bits
	csrrc x10, 0xc72, x16	// Clear all CSR bits
	csrrw x10, 0xc72, x2	// Restore CSR

// Testing CSR 0xc73
	csrr x29, 0xc73	// Read CSR
	li x22, -1
	csrrw x18, 0xc73, x22	// Write all 1s to CSR
	csrrw x18, 0xc73, x0	// Write all 0s to CSR
	csrrs x18, 0xc73, x22	// Set all CSR bits
	csrrc x18, 0xc73, x22	// Clear all CSR bits
	csrrw x18, 0xc73, x29	// Restore CSR

// Testing CSR 0xc74
	csrr x9, 0xc74	// Read CSR
	li x5, -1
	csrrw x11, 0xc74, x5	// Write all 1s to CSR
	csrrw x11, 0xc74, x0	// Write all 0s to CSR
	csrrs x11, 0xc74, x5	// Set all CSR bits
	csrrc x11, 0xc74, x5	// Clear all CSR bits
	csrrw x11, 0xc74, x9	// Restore CSR

// Testing CSR 0xc75
	csrr x7, 0xc75	// Read CSR
	li x26, -1
	csrrw x2, 0xc75, x26	// Write all 1s to CSR
	csrrw x2, 0xc75, x0	// Write all 0s to CSR
	csrrs x2, 0xc75, x26	// Set all CSR bits
	csrrc x2, 0xc75, x26	// Clear all CSR bits
	csrrw x2, 0xc75, x7	// Restore CSR

// Testing CSR 0xc76
	csrr x14, 0xc76	// Read CSR
	li x7, -1
	csrrw x3, 0xc76, x7	// Write all 1s to CSR
	csrrw x3, 0xc76, x0	// Write all 0s to CSR
	csrrs x3, 0xc76, x7	// Set all CSR bits
	csrrc x3, 0xc76, x7	// Clear all CSR bits
	csrrw x3, 0xc76, x14	// Restore CSR

// Testing CSR 0xc77
	csrr x9, 0xc77	// Read CSR
	li x18, -1
	csrrw x27, 0xc77, x18	// Write all 1s to CSR
	csrrw x27, 0xc77, x0	// Write all 0s to CSR
	csrrs x27, 0xc77, x18	// Set all CSR bits
	csrrc x27, 0xc77, x18	// Clear all CSR bits
	csrrw x27, 0xc77, x9	// Restore CSR

// Testing CSR 0xc78
	csrr x10, 0xc78	// Read CSR
	li x8, -1
	csrrw x25, 0xc78, x8	// Write all 1s to CSR
	csrrw x25, 0xc78, x0	// Write all 0s to CSR
	csrrs x25, 0xc78, x8	// Set all CSR bits
	csrrc x25, 0xc78, x8	// Clear all CSR bits
	csrrw x25, 0xc78, x10	// Restore CSR

// Testing CSR 0xc79
	csrr x3, 0xc79	// Read CSR
	li x19, -1
	csrrw x23, 0xc79, x19	// Write all 1s to CSR
	csrrw x23, 0xc79, x0	// Write all 0s to CSR
	csrrs x23, 0xc79, x19	// Set all CSR bits
	csrrc x23, 0xc79, x19	// Clear all CSR bits
	csrrw x23, 0xc79, x3	// Restore CSR

// Testing CSR 0xc7a
	csrr x18, 0xc7a	// Read CSR
	li x5, -1
	csrrw x6, 0xc7a, x5	// Write all 1s to CSR
	csrrw x6, 0xc7a, x0	// Write all 0s to CSR
	csrrs x6, 0xc7a, x5	// Set all CSR bits
	csrrc x6, 0xc7a, x5	// Clear all CSR bits
	csrrw x6, 0xc7a, x18	// Restore CSR

// Testing CSR 0xc7b
	csrr x15, 0xc7b	// Read CSR
	li x21, -1
	csrrw x8, 0xc7b, x21	// Write all 1s to CSR
	csrrw x8, 0xc7b, x0	// Write all 0s to CSR
	csrrs x8, 0xc7b, x21	// Set all CSR bits
	csrrc x8, 0xc7b, x21	// Clear all CSR bits
	csrrw x8, 0xc7b, x15	// Restore CSR

// Testing CSR 0xc7c
	csrr x19, 0xc7c	// Read CSR
	li x2, -1
	csrrw x22, 0xc7c, x2	// Write all 1s to CSR
	csrrw x22, 0xc7c, x0	// Write all 0s to CSR
	csrrs x22, 0xc7c, x2	// Set all CSR bits
	csrrc x22, 0xc7c, x2	// Clear all CSR bits
	csrrw x22, 0xc7c, x19	// Restore CSR

// Testing CSR 0xc7d
	csrr x14, 0xc7d	// Read CSR
	li x4, -1
	csrrw x21, 0xc7d, x4	// Write all 1s to CSR
	csrrw x21, 0xc7d, x0	// Write all 0s to CSR
	csrrs x21, 0xc7d, x4	// Set all CSR bits
	csrrc x21, 0xc7d, x4	// Clear all CSR bits
	csrrw x21, 0xc7d, x14	// Restore CSR

// Testing CSR 0xc7e
	csrr x1, 0xc7e	// Read CSR
	li x2, -1
	csrrw x7, 0xc7e, x2	// Write all 1s to CSR
	csrrw x7, 0xc7e, x0	// Write all 0s to CSR
	csrrs x7, 0xc7e, x2	// Set all CSR bits
	csrrc x7, 0xc7e, x2	// Clear all CSR bits
	csrrw x7, 0xc7e, x1	// Restore CSR

// Testing CSR 0xc7f
	csrr x31, 0xc7f	// Read CSR
	li x12, -1
	csrrw x6, 0xc7f, x12	// Write all 1s to CSR
	csrrw x6, 0xc7f, x0	// Write all 0s to CSR
	csrrs x6, 0xc7f, x12	// Set all CSR bits
	csrrc x6, 0xc7f, x12	// Clear all CSR bits
	csrrw x6, 0xc7f, x31	// Restore CSR

// Testing CSR 0xc80
	csrr x11, 0xc80	// Read CSR
	li x17, -1
	csrrw x20, 0xc80, x17	// Write all 1s to CSR
	csrrw x20, 0xc80, x0	// Write all 0s to CSR
	csrrs x20, 0xc80, x17	// Set all CSR bits
	csrrc x20, 0xc80, x17	// Clear all CSR bits
	csrrw x20, 0xc80, x11	// Restore CSR

// Testing CSR 0xc81
	csrr x22, 0xc81	// Read CSR
	li x21, -1
	csrrw x7, 0xc81, x21	// Write all 1s to CSR
	csrrw x7, 0xc81, x0	// Write all 0s to CSR
	csrrs x7, 0xc81, x21	// Set all CSR bits
	csrrc x7, 0xc81, x21	// Clear all CSR bits
	csrrw x7, 0xc81, x22	// Restore CSR

// Testing CSR 0xc82
	csrr x14, 0xc82	// Read CSR
	li x8, -1
	csrrw x6, 0xc82, x8	// Write all 1s to CSR
	csrrw x6, 0xc82, x0	// Write all 0s to CSR
	csrrs x6, 0xc82, x8	// Set all CSR bits
	csrrc x6, 0xc82, x8	// Clear all CSR bits
	csrrw x6, 0xc82, x14	// Restore CSR

// Testing CSR 0xc83
	csrr x24, 0xc83	// Read CSR
	li x7, -1
	csrrw x10, 0xc83, x7	// Write all 1s to CSR
	csrrw x10, 0xc83, x0	// Write all 0s to CSR
	csrrs x10, 0xc83, x7	// Set all CSR bits
	csrrc x10, 0xc83, x7	// Clear all CSR bits
	csrrw x10, 0xc83, x24	// Restore CSR

// Testing CSR 0xc84
	csrr x16, 0xc84	// Read CSR
	li x28, -1
	csrrw x20, 0xc84, x28	// Write all 1s to CSR
	csrrw x20, 0xc84, x0	// Write all 0s to CSR
	csrrs x20, 0xc84, x28	// Set all CSR bits
	csrrc x20, 0xc84, x28	// Clear all CSR bits
	csrrw x20, 0xc84, x16	// Restore CSR

// Testing CSR 0xc85
	csrr x31, 0xc85	// Read CSR
	li x2, -1
	csrrw x16, 0xc85, x2	// Write all 1s to CSR
	csrrw x16, 0xc85, x0	// Write all 0s to CSR
	csrrs x16, 0xc85, x2	// Set all CSR bits
	csrrc x16, 0xc85, x2	// Clear all CSR bits
	csrrw x16, 0xc85, x31	// Restore CSR

// Testing CSR 0xc86
	csrr x2, 0xc86	// Read CSR
	li x7, -1
	csrrw x20, 0xc86, x7	// Write all 1s to CSR
	csrrw x20, 0xc86, x0	// Write all 0s to CSR
	csrrs x20, 0xc86, x7	// Set all CSR bits
	csrrc x20, 0xc86, x7	// Clear all CSR bits
	csrrw x20, 0xc86, x2	// Restore CSR

// Testing CSR 0xc87
	csrr x13, 0xc87	// Read CSR
	li x14, -1
	csrrw x3, 0xc87, x14	// Write all 1s to CSR
	csrrw x3, 0xc87, x0	// Write all 0s to CSR
	csrrs x3, 0xc87, x14	// Set all CSR bits
	csrrc x3, 0xc87, x14	// Clear all CSR bits
	csrrw x3, 0xc87, x13	// Restore CSR

// Testing CSR 0xc88
	csrr x29, 0xc88	// Read CSR
	li x3, -1
	csrrw x31, 0xc88, x3	// Write all 1s to CSR
	csrrw x31, 0xc88, x0	// Write all 0s to CSR
	csrrs x31, 0xc88, x3	// Set all CSR bits
	csrrc x31, 0xc88, x3	// Clear all CSR bits
	csrrw x31, 0xc88, x29	// Restore CSR

// Testing CSR 0xc89
	csrr x31, 0xc89	// Read CSR
	li x30, -1
	csrrw x18, 0xc89, x30	// Write all 1s to CSR
	csrrw x18, 0xc89, x0	// Write all 0s to CSR
	csrrs x18, 0xc89, x30	// Set all CSR bits
	csrrc x18, 0xc89, x30	// Clear all CSR bits
	csrrw x18, 0xc89, x31	// Restore CSR

// Testing CSR 0xc8a
	csrr x26, 0xc8a	// Read CSR
	li x28, -1
	csrrw x9, 0xc8a, x28	// Write all 1s to CSR
	csrrw x9, 0xc8a, x0	// Write all 0s to CSR
	csrrs x9, 0xc8a, x28	// Set all CSR bits
	csrrc x9, 0xc8a, x28	// Clear all CSR bits
	csrrw x9, 0xc8a, x26	// Restore CSR

// Testing CSR 0xc8b
	csrr x2, 0xc8b	// Read CSR
	li x1, -1
	csrrw x27, 0xc8b, x1	// Write all 1s to CSR
	csrrw x27, 0xc8b, x0	// Write all 0s to CSR
	csrrs x27, 0xc8b, x1	// Set all CSR bits
	csrrc x27, 0xc8b, x1	// Clear all CSR bits
	csrrw x27, 0xc8b, x2	// Restore CSR

// Testing CSR 0xc8c
	csrr x14, 0xc8c	// Read CSR
	li x6, -1
	csrrw x2, 0xc8c, x6	// Write all 1s to CSR
	csrrw x2, 0xc8c, x0	// Write all 0s to CSR
	csrrs x2, 0xc8c, x6	// Set all CSR bits
	csrrc x2, 0xc8c, x6	// Clear all CSR bits
	csrrw x2, 0xc8c, x14	// Restore CSR

// Testing CSR 0xc8d
	csrr x17, 0xc8d	// Read CSR
	li x20, -1
	csrrw x29, 0xc8d, x20	// Write all 1s to CSR
	csrrw x29, 0xc8d, x0	// Write all 0s to CSR
	csrrs x29, 0xc8d, x20	// Set all CSR bits
	csrrc x29, 0xc8d, x20	// Clear all CSR bits
	csrrw x29, 0xc8d, x17	// Restore CSR

// Testing CSR 0xc8e
	csrr x16, 0xc8e	// Read CSR
	li x11, -1
	csrrw x29, 0xc8e, x11	// Write all 1s to CSR
	csrrw x29, 0xc8e, x0	// Write all 0s to CSR
	csrrs x29, 0xc8e, x11	// Set all CSR bits
	csrrc x29, 0xc8e, x11	// Clear all CSR bits
	csrrw x29, 0xc8e, x16	// Restore CSR

// Testing CSR 0xc8f
	csrr x16, 0xc8f	// Read CSR
	li x11, -1
	csrrw x5, 0xc8f, x11	// Write all 1s to CSR
	csrrw x5, 0xc8f, x0	// Write all 0s to CSR
	csrrs x5, 0xc8f, x11	// Set all CSR bits
	csrrc x5, 0xc8f, x11	// Clear all CSR bits
	csrrw x5, 0xc8f, x16	// Restore CSR

// Testing CSR 0xc90
	csrr x16, 0xc90	// Read CSR
	li x14, -1
	csrrw x1, 0xc90, x14	// Write all 1s to CSR
	csrrw x1, 0xc90, x0	// Write all 0s to CSR
	csrrs x1, 0xc90, x14	// Set all CSR bits
	csrrc x1, 0xc90, x14	// Clear all CSR bits
	csrrw x1, 0xc90, x16	// Restore CSR

// Testing CSR 0xc91
	csrr x3, 0xc91	// Read CSR
	li x5, -1
	csrrw x22, 0xc91, x5	// Write all 1s to CSR
	csrrw x22, 0xc91, x0	// Write all 0s to CSR
	csrrs x22, 0xc91, x5	// Set all CSR bits
	csrrc x22, 0xc91, x5	// Clear all CSR bits
	csrrw x22, 0xc91, x3	// Restore CSR

// Testing CSR 0xc92
	csrr x28, 0xc92	// Read CSR
	li x18, -1
	csrrw x31, 0xc92, x18	// Write all 1s to CSR
	csrrw x31, 0xc92, x0	// Write all 0s to CSR
	csrrs x31, 0xc92, x18	// Set all CSR bits
	csrrc x31, 0xc92, x18	// Clear all CSR bits
	csrrw x31, 0xc92, x28	// Restore CSR

// Testing CSR 0xc93
	csrr x11, 0xc93	// Read CSR
	li x5, -1
	csrrw x20, 0xc93, x5	// Write all 1s to CSR
	csrrw x20, 0xc93, x0	// Write all 0s to CSR
	csrrs x20, 0xc93, x5	// Set all CSR bits
	csrrc x20, 0xc93, x5	// Clear all CSR bits
	csrrw x20, 0xc93, x11	// Restore CSR

// Testing CSR 0xc94
	csrr x28, 0xc94	// Read CSR
	li x7, -1
	csrrw x16, 0xc94, x7	// Write all 1s to CSR
	csrrw x16, 0xc94, x0	// Write all 0s to CSR
	csrrs x16, 0xc94, x7	// Set all CSR bits
	csrrc x16, 0xc94, x7	// Clear all CSR bits
	csrrw x16, 0xc94, x28	// Restore CSR

// Testing CSR 0xc95
	csrr x3, 0xc95	// Read CSR
	li x12, -1
	csrrw x29, 0xc95, x12	// Write all 1s to CSR
	csrrw x29, 0xc95, x0	// Write all 0s to CSR
	csrrs x29, 0xc95, x12	// Set all CSR bits
	csrrc x29, 0xc95, x12	// Clear all CSR bits
	csrrw x29, 0xc95, x3	// Restore CSR

// Testing CSR 0xc96
	csrr x25, 0xc96	// Read CSR
	li x1, -1
	csrrw x15, 0xc96, x1	// Write all 1s to CSR
	csrrw x15, 0xc96, x0	// Write all 0s to CSR
	csrrs x15, 0xc96, x1	// Set all CSR bits
	csrrc x15, 0xc96, x1	// Clear all CSR bits
	csrrw x15, 0xc96, x25	// Restore CSR

// Testing CSR 0xc97
	csrr x13, 0xc97	// Read CSR
	li x20, -1
	csrrw x26, 0xc97, x20	// Write all 1s to CSR
	csrrw x26, 0xc97, x0	// Write all 0s to CSR
	csrrs x26, 0xc97, x20	// Set all CSR bits
	csrrc x26, 0xc97, x20	// Clear all CSR bits
	csrrw x26, 0xc97, x13	// Restore CSR

// Testing CSR 0xc98
	csrr x31, 0xc98	// Read CSR
	li x15, -1
	csrrw x23, 0xc98, x15	// Write all 1s to CSR
	csrrw x23, 0xc98, x0	// Write all 0s to CSR
	csrrs x23, 0xc98, x15	// Set all CSR bits
	csrrc x23, 0xc98, x15	// Clear all CSR bits
	csrrw x23, 0xc98, x31	// Restore CSR

// Testing CSR 0xc99
	csrr x15, 0xc99	// Read CSR
	li x27, -1
	csrrw x7, 0xc99, x27	// Write all 1s to CSR
	csrrw x7, 0xc99, x0	// Write all 0s to CSR
	csrrs x7, 0xc99, x27	// Set all CSR bits
	csrrc x7, 0xc99, x27	// Clear all CSR bits
	csrrw x7, 0xc99, x15	// Restore CSR

// Testing CSR 0xc9a
	csrr x21, 0xc9a	// Read CSR
	li x13, -1
	csrrw x28, 0xc9a, x13	// Write all 1s to CSR
	csrrw x28, 0xc9a, x0	// Write all 0s to CSR
	csrrs x28, 0xc9a, x13	// Set all CSR bits
	csrrc x28, 0xc9a, x13	// Clear all CSR bits
	csrrw x28, 0xc9a, x21	// Restore CSR

// Testing CSR 0xc9b
	csrr x19, 0xc9b	// Read CSR
	li x22, -1
	csrrw x8, 0xc9b, x22	// Write all 1s to CSR
	csrrw x8, 0xc9b, x0	// Write all 0s to CSR
	csrrs x8, 0xc9b, x22	// Set all CSR bits
	csrrc x8, 0xc9b, x22	// Clear all CSR bits
	csrrw x8, 0xc9b, x19	// Restore CSR

// Testing CSR 0xc9c
	csrr x6, 0xc9c	// Read CSR
	li x2, -1
	csrrw x11, 0xc9c, x2	// Write all 1s to CSR
	csrrw x11, 0xc9c, x0	// Write all 0s to CSR
	csrrs x11, 0xc9c, x2	// Set all CSR bits
	csrrc x11, 0xc9c, x2	// Clear all CSR bits
	csrrw x11, 0xc9c, x6	// Restore CSR

// Testing CSR 0xc9d
	csrr x4, 0xc9d	// Read CSR
	li x6, -1
	csrrw x9, 0xc9d, x6	// Write all 1s to CSR
	csrrw x9, 0xc9d, x0	// Write all 0s to CSR
	csrrs x9, 0xc9d, x6	// Set all CSR bits
	csrrc x9, 0xc9d, x6	// Clear all CSR bits
	csrrw x9, 0xc9d, x4	// Restore CSR

// Testing CSR 0xc9e
	csrr x2, 0xc9e	// Read CSR
	li x30, -1
	csrrw x3, 0xc9e, x30	// Write all 1s to CSR
	csrrw x3, 0xc9e, x0	// Write all 0s to CSR
	csrrs x3, 0xc9e, x30	// Set all CSR bits
	csrrc x3, 0xc9e, x30	// Clear all CSR bits
	csrrw x3, 0xc9e, x2	// Restore CSR

// Testing CSR 0xc9f
	csrr x6, 0xc9f	// Read CSR
	li x29, -1
	csrrw x21, 0xc9f, x29	// Write all 1s to CSR
	csrrw x21, 0xc9f, x0	// Write all 0s to CSR
	csrrs x21, 0xc9f, x29	// Set all CSR bits
	csrrc x21, 0xc9f, x29	// Clear all CSR bits
	csrrw x21, 0xc9f, x6	// Restore CSR

// Testing CSR 0xca0
	csrr x1, 0xca0	// Read CSR
	li x16, -1
	csrrw x7, 0xca0, x16	// Write all 1s to CSR
	csrrw x7, 0xca0, x0	// Write all 0s to CSR
	csrrs x7, 0xca0, x16	// Set all CSR bits
	csrrc x7, 0xca0, x16	// Clear all CSR bits
	csrrw x7, 0xca0, x1	// Restore CSR

// Testing CSR 0xca1
	csrr x26, 0xca1	// Read CSR
	li x25, -1
	csrrw x20, 0xca1, x25	// Write all 1s to CSR
	csrrw x20, 0xca1, x0	// Write all 0s to CSR
	csrrs x20, 0xca1, x25	// Set all CSR bits
	csrrc x20, 0xca1, x25	// Clear all CSR bits
	csrrw x20, 0xca1, x26	// Restore CSR

// Testing CSR 0xca2
	csrr x26, 0xca2	// Read CSR
	li x18, -1
	csrrw x10, 0xca2, x18	// Write all 1s to CSR
	csrrw x10, 0xca2, x0	// Write all 0s to CSR
	csrrs x10, 0xca2, x18	// Set all CSR bits
	csrrc x10, 0xca2, x18	// Clear all CSR bits
	csrrw x10, 0xca2, x26	// Restore CSR

// Testing CSR 0xca3
	csrr x31, 0xca3	// Read CSR
	li x30, -1
	csrrw x13, 0xca3, x30	// Write all 1s to CSR
	csrrw x13, 0xca3, x0	// Write all 0s to CSR
	csrrs x13, 0xca3, x30	// Set all CSR bits
	csrrc x13, 0xca3, x30	// Clear all CSR bits
	csrrw x13, 0xca3, x31	// Restore CSR

// Testing CSR 0xca4
	csrr x29, 0xca4	// Read CSR
	li x26, -1
	csrrw x16, 0xca4, x26	// Write all 1s to CSR
	csrrw x16, 0xca4, x0	// Write all 0s to CSR
	csrrs x16, 0xca4, x26	// Set all CSR bits
	csrrc x16, 0xca4, x26	// Clear all CSR bits
	csrrw x16, 0xca4, x29	// Restore CSR

// Testing CSR 0xca5
	csrr x10, 0xca5	// Read CSR
	li x18, -1
	csrrw x8, 0xca5, x18	// Write all 1s to CSR
	csrrw x8, 0xca5, x0	// Write all 0s to CSR
	csrrs x8, 0xca5, x18	// Set all CSR bits
	csrrc x8, 0xca5, x18	// Clear all CSR bits
	csrrw x8, 0xca5, x10	// Restore CSR

// Testing CSR 0xca6
	csrr x13, 0xca6	// Read CSR
	li x3, -1
	csrrw x24, 0xca6, x3	// Write all 1s to CSR
	csrrw x24, 0xca6, x0	// Write all 0s to CSR
	csrrs x24, 0xca6, x3	// Set all CSR bits
	csrrc x24, 0xca6, x3	// Clear all CSR bits
	csrrw x24, 0xca6, x13	// Restore CSR

// Testing CSR 0xca7
	csrr x11, 0xca7	// Read CSR
	li x2, -1
	csrrw x31, 0xca7, x2	// Write all 1s to CSR
	csrrw x31, 0xca7, x0	// Write all 0s to CSR
	csrrs x31, 0xca7, x2	// Set all CSR bits
	csrrc x31, 0xca7, x2	// Clear all CSR bits
	csrrw x31, 0xca7, x11	// Restore CSR

// Testing CSR 0xca8
	csrr x14, 0xca8	// Read CSR
	li x27, -1
	csrrw x24, 0xca8, x27	// Write all 1s to CSR
	csrrw x24, 0xca8, x0	// Write all 0s to CSR
	csrrs x24, 0xca8, x27	// Set all CSR bits
	csrrc x24, 0xca8, x27	// Clear all CSR bits
	csrrw x24, 0xca8, x14	// Restore CSR

// Testing CSR 0xca9
	csrr x16, 0xca9	// Read CSR
	li x17, -1
	csrrw x25, 0xca9, x17	// Write all 1s to CSR
	csrrw x25, 0xca9, x0	// Write all 0s to CSR
	csrrs x25, 0xca9, x17	// Set all CSR bits
	csrrc x25, 0xca9, x17	// Clear all CSR bits
	csrrw x25, 0xca9, x16	// Restore CSR

// Testing CSR 0xcaa
	csrr x1, 0xcaa	// Read CSR
	li x17, -1
	csrrw x25, 0xcaa, x17	// Write all 1s to CSR
	csrrw x25, 0xcaa, x0	// Write all 0s to CSR
	csrrs x25, 0xcaa, x17	// Set all CSR bits
	csrrc x25, 0xcaa, x17	// Clear all CSR bits
	csrrw x25, 0xcaa, x1	// Restore CSR

// Testing CSR 0xcab
	csrr x28, 0xcab	// Read CSR
	li x11, -1
	csrrw x4, 0xcab, x11	// Write all 1s to CSR
	csrrw x4, 0xcab, x0	// Write all 0s to CSR
	csrrs x4, 0xcab, x11	// Set all CSR bits
	csrrc x4, 0xcab, x11	// Clear all CSR bits
	csrrw x4, 0xcab, x28	// Restore CSR

// Testing CSR 0xcac
	csrr x6, 0xcac	// Read CSR
	li x8, -1
	csrrw x16, 0xcac, x8	// Write all 1s to CSR
	csrrw x16, 0xcac, x0	// Write all 0s to CSR
	csrrs x16, 0xcac, x8	// Set all CSR bits
	csrrc x16, 0xcac, x8	// Clear all CSR bits
	csrrw x16, 0xcac, x6	// Restore CSR

// Testing CSR 0xcad
	csrr x12, 0xcad	// Read CSR
	li x23, -1
	csrrw x20, 0xcad, x23	// Write all 1s to CSR
	csrrw x20, 0xcad, x0	// Write all 0s to CSR
	csrrs x20, 0xcad, x23	// Set all CSR bits
	csrrc x20, 0xcad, x23	// Clear all CSR bits
	csrrw x20, 0xcad, x12	// Restore CSR

// Testing CSR 0xcae
	csrr x24, 0xcae	// Read CSR
	li x25, -1
	csrrw x20, 0xcae, x25	// Write all 1s to CSR
	csrrw x20, 0xcae, x0	// Write all 0s to CSR
	csrrs x20, 0xcae, x25	// Set all CSR bits
	csrrc x20, 0xcae, x25	// Clear all CSR bits
	csrrw x20, 0xcae, x24	// Restore CSR

// Testing CSR 0xcaf
	csrr x20, 0xcaf	// Read CSR
	li x14, -1
	csrrw x4, 0xcaf, x14	// Write all 1s to CSR
	csrrw x4, 0xcaf, x0	// Write all 0s to CSR
	csrrs x4, 0xcaf, x14	// Set all CSR bits
	csrrc x4, 0xcaf, x14	// Clear all CSR bits
	csrrw x4, 0xcaf, x20	// Restore CSR

// Testing CSR 0xcb0
	csrr x27, 0xcb0	// Read CSR
	li x1, -1
	csrrw x6, 0xcb0, x1	// Write all 1s to CSR
	csrrw x6, 0xcb0, x0	// Write all 0s to CSR
	csrrs x6, 0xcb0, x1	// Set all CSR bits
	csrrc x6, 0xcb0, x1	// Clear all CSR bits
	csrrw x6, 0xcb0, x27	// Restore CSR

// Testing CSR 0xcb1
	csrr x23, 0xcb1	// Read CSR
	li x21, -1
	csrrw x6, 0xcb1, x21	// Write all 1s to CSR
	csrrw x6, 0xcb1, x0	// Write all 0s to CSR
	csrrs x6, 0xcb1, x21	// Set all CSR bits
	csrrc x6, 0xcb1, x21	// Clear all CSR bits
	csrrw x6, 0xcb1, x23	// Restore CSR

// Testing CSR 0xcb2
	csrr x18, 0xcb2	// Read CSR
	li x13, -1
	csrrw x7, 0xcb2, x13	// Write all 1s to CSR
	csrrw x7, 0xcb2, x0	// Write all 0s to CSR
	csrrs x7, 0xcb2, x13	// Set all CSR bits
	csrrc x7, 0xcb2, x13	// Clear all CSR bits
	csrrw x7, 0xcb2, x18	// Restore CSR

// Testing CSR 0xcb3
	csrr x17, 0xcb3	// Read CSR
	li x19, -1
	csrrw x6, 0xcb3, x19	// Write all 1s to CSR
	csrrw x6, 0xcb3, x0	// Write all 0s to CSR
	csrrs x6, 0xcb3, x19	// Set all CSR bits
	csrrc x6, 0xcb3, x19	// Clear all CSR bits
	csrrw x6, 0xcb3, x17	// Restore CSR

// Testing CSR 0xcb4
	csrr x27, 0xcb4	// Read CSR
	li x23, -1
	csrrw x26, 0xcb4, x23	// Write all 1s to CSR
	csrrw x26, 0xcb4, x0	// Write all 0s to CSR
	csrrs x26, 0xcb4, x23	// Set all CSR bits
	csrrc x26, 0xcb4, x23	// Clear all CSR bits
	csrrw x26, 0xcb4, x27	// Restore CSR

// Testing CSR 0xcb5
	csrr x18, 0xcb5	// Read CSR
	li x22, -1
	csrrw x15, 0xcb5, x22	// Write all 1s to CSR
	csrrw x15, 0xcb5, x0	// Write all 0s to CSR
	csrrs x15, 0xcb5, x22	// Set all CSR bits
	csrrc x15, 0xcb5, x22	// Clear all CSR bits
	csrrw x15, 0xcb5, x18	// Restore CSR

// Testing CSR 0xcb6
	csrr x29, 0xcb6	// Read CSR
	li x3, -1
	csrrw x30, 0xcb6, x3	// Write all 1s to CSR
	csrrw x30, 0xcb6, x0	// Write all 0s to CSR
	csrrs x30, 0xcb6, x3	// Set all CSR bits
	csrrc x30, 0xcb6, x3	// Clear all CSR bits
	csrrw x30, 0xcb6, x29	// Restore CSR

// Testing CSR 0xcb7
	csrr x16, 0xcb7	// Read CSR
	li x27, -1
	csrrw x10, 0xcb7, x27	// Write all 1s to CSR
	csrrw x10, 0xcb7, x0	// Write all 0s to CSR
	csrrs x10, 0xcb7, x27	// Set all CSR bits
	csrrc x10, 0xcb7, x27	// Clear all CSR bits
	csrrw x10, 0xcb7, x16	// Restore CSR

// Testing CSR 0xcb8
	csrr x29, 0xcb8	// Read CSR
	li x8, -1
	csrrw x1, 0xcb8, x8	// Write all 1s to CSR
	csrrw x1, 0xcb8, x0	// Write all 0s to CSR
	csrrs x1, 0xcb8, x8	// Set all CSR bits
	csrrc x1, 0xcb8, x8	// Clear all CSR bits
	csrrw x1, 0xcb8, x29	// Restore CSR

// Testing CSR 0xcb9
	csrr x16, 0xcb9	// Read CSR
	li x24, -1
	csrrw x27, 0xcb9, x24	// Write all 1s to CSR
	csrrw x27, 0xcb9, x0	// Write all 0s to CSR
	csrrs x27, 0xcb9, x24	// Set all CSR bits
	csrrc x27, 0xcb9, x24	// Clear all CSR bits
	csrrw x27, 0xcb9, x16	// Restore CSR

// Testing CSR 0xcba
	csrr x9, 0xcba	// Read CSR
	li x4, -1
	csrrw x5, 0xcba, x4	// Write all 1s to CSR
	csrrw x5, 0xcba, x0	// Write all 0s to CSR
	csrrs x5, 0xcba, x4	// Set all CSR bits
	csrrc x5, 0xcba, x4	// Clear all CSR bits
	csrrw x5, 0xcba, x9	// Restore CSR

// Testing CSR 0xcbb
	csrr x21, 0xcbb	// Read CSR
	li x5, -1
	csrrw x30, 0xcbb, x5	// Write all 1s to CSR
	csrrw x30, 0xcbb, x0	// Write all 0s to CSR
	csrrs x30, 0xcbb, x5	// Set all CSR bits
	csrrc x30, 0xcbb, x5	// Clear all CSR bits
	csrrw x30, 0xcbb, x21	// Restore CSR

// Testing CSR 0xcbc
	csrr x6, 0xcbc	// Read CSR
	li x23, -1
	csrrw x3, 0xcbc, x23	// Write all 1s to CSR
	csrrw x3, 0xcbc, x0	// Write all 0s to CSR
	csrrs x3, 0xcbc, x23	// Set all CSR bits
	csrrc x3, 0xcbc, x23	// Clear all CSR bits
	csrrw x3, 0xcbc, x6	// Restore CSR

// Testing CSR 0xcbd
	csrr x16, 0xcbd	// Read CSR
	li x15, -1
	csrrw x31, 0xcbd, x15	// Write all 1s to CSR
	csrrw x31, 0xcbd, x0	// Write all 0s to CSR
	csrrs x31, 0xcbd, x15	// Set all CSR bits
	csrrc x31, 0xcbd, x15	// Clear all CSR bits
	csrrw x31, 0xcbd, x16	// Restore CSR

// Testing CSR 0xcbe
	csrr x10, 0xcbe	// Read CSR
	li x25, -1
	csrrw x1, 0xcbe, x25	// Write all 1s to CSR
	csrrw x1, 0xcbe, x0	// Write all 0s to CSR
	csrrs x1, 0xcbe, x25	// Set all CSR bits
	csrrc x1, 0xcbe, x25	// Clear all CSR bits
	csrrw x1, 0xcbe, x10	// Restore CSR

// Testing CSR 0xcbf
	csrr x21, 0xcbf	// Read CSR
	li x30, -1
	csrrw x24, 0xcbf, x30	// Write all 1s to CSR
	csrrw x24, 0xcbf, x0	// Write all 0s to CSR
	csrrs x24, 0xcbf, x30	// Set all CSR bits
	csrrc x24, 0xcbf, x30	// Clear all CSR bits
	csrrw x24, 0xcbf, x21	// Restore CSR

// Testing CSR 0xd00
	csrr x20, 0xd00	// Read CSR
	li x19, -1
	csrrw x13, 0xd00, x19	// Write all 1s to CSR
	csrrw x13, 0xd00, x0	// Write all 0s to CSR
	csrrs x13, 0xd00, x19	// Set all CSR bits
	csrrc x13, 0xd00, x19	// Clear all CSR bits
	csrrw x13, 0xd00, x20	// Restore CSR

// Testing CSR 0xd01
	csrr x7, 0xd01	// Read CSR
	li x2, -1
	csrrw x4, 0xd01, x2	// Write all 1s to CSR
	csrrw x4, 0xd01, x0	// Write all 0s to CSR
	csrrs x4, 0xd01, x2	// Set all CSR bits
	csrrc x4, 0xd01, x2	// Clear all CSR bits
	csrrw x4, 0xd01, x7	// Restore CSR

// Testing CSR 0xd02
	csrr x18, 0xd02	// Read CSR
	li x27, -1
	csrrw x5, 0xd02, x27	// Write all 1s to CSR
	csrrw x5, 0xd02, x0	// Write all 0s to CSR
	csrrs x5, 0xd02, x27	// Set all CSR bits
	csrrc x5, 0xd02, x27	// Clear all CSR bits
	csrrw x5, 0xd02, x18	// Restore CSR

// Testing CSR 0xd03
	csrr x13, 0xd03	// Read CSR
	li x8, -1
	csrrw x19, 0xd03, x8	// Write all 1s to CSR
	csrrw x19, 0xd03, x0	// Write all 0s to CSR
	csrrs x19, 0xd03, x8	// Set all CSR bits
	csrrc x19, 0xd03, x8	// Clear all CSR bits
	csrrw x19, 0xd03, x13	// Restore CSR

// Testing CSR 0xd04
	csrr x3, 0xd04	// Read CSR
	li x11, -1
	csrrw x25, 0xd04, x11	// Write all 1s to CSR
	csrrw x25, 0xd04, x0	// Write all 0s to CSR
	csrrs x25, 0xd04, x11	// Set all CSR bits
	csrrc x25, 0xd04, x11	// Clear all CSR bits
	csrrw x25, 0xd04, x3	// Restore CSR

// Testing CSR 0xd05
	csrr x4, 0xd05	// Read CSR
	li x16, -1
	csrrw x24, 0xd05, x16	// Write all 1s to CSR
	csrrw x24, 0xd05, x0	// Write all 0s to CSR
	csrrs x24, 0xd05, x16	// Set all CSR bits
	csrrc x24, 0xd05, x16	// Clear all CSR bits
	csrrw x24, 0xd05, x4	// Restore CSR

// Testing CSR 0xd06
	csrr x18, 0xd06	// Read CSR
	li x24, -1
	csrrw x13, 0xd06, x24	// Write all 1s to CSR
	csrrw x13, 0xd06, x0	// Write all 0s to CSR
	csrrs x13, 0xd06, x24	// Set all CSR bits
	csrrc x13, 0xd06, x24	// Clear all CSR bits
	csrrw x13, 0xd06, x18	// Restore CSR

// Testing CSR 0xd07
	csrr x23, 0xd07	// Read CSR
	li x9, -1
	csrrw x6, 0xd07, x9	// Write all 1s to CSR
	csrrw x6, 0xd07, x0	// Write all 0s to CSR
	csrrs x6, 0xd07, x9	// Set all CSR bits
	csrrc x6, 0xd07, x9	// Clear all CSR bits
	csrrw x6, 0xd07, x23	// Restore CSR

// Testing CSR 0xd08
	csrr x3, 0xd08	// Read CSR
	li x25, -1
	csrrw x15, 0xd08, x25	// Write all 1s to CSR
	csrrw x15, 0xd08, x0	// Write all 0s to CSR
	csrrs x15, 0xd08, x25	// Set all CSR bits
	csrrc x15, 0xd08, x25	// Clear all CSR bits
	csrrw x15, 0xd08, x3	// Restore CSR

// Testing CSR 0xd09
	csrr x13, 0xd09	// Read CSR
	li x9, -1
	csrrw x24, 0xd09, x9	// Write all 1s to CSR
	csrrw x24, 0xd09, x0	// Write all 0s to CSR
	csrrs x24, 0xd09, x9	// Set all CSR bits
	csrrc x24, 0xd09, x9	// Clear all CSR bits
	csrrw x24, 0xd09, x13	// Restore CSR

// Testing CSR 0xd0a
	csrr x17, 0xd0a	// Read CSR
	li x1, -1
	csrrw x7, 0xd0a, x1	// Write all 1s to CSR
	csrrw x7, 0xd0a, x0	// Write all 0s to CSR
	csrrs x7, 0xd0a, x1	// Set all CSR bits
	csrrc x7, 0xd0a, x1	// Clear all CSR bits
	csrrw x7, 0xd0a, x17	// Restore CSR

// Testing CSR 0xd0b
	csrr x7, 0xd0b	// Read CSR
	li x18, -1
	csrrw x11, 0xd0b, x18	// Write all 1s to CSR
	csrrw x11, 0xd0b, x0	// Write all 0s to CSR
	csrrs x11, 0xd0b, x18	// Set all CSR bits
	csrrc x11, 0xd0b, x18	// Clear all CSR bits
	csrrw x11, 0xd0b, x7	// Restore CSR

// Testing CSR 0xd0c
	csrr x2, 0xd0c	// Read CSR
	li x27, -1
	csrrw x11, 0xd0c, x27	// Write all 1s to CSR
	csrrw x11, 0xd0c, x0	// Write all 0s to CSR
	csrrs x11, 0xd0c, x27	// Set all CSR bits
	csrrc x11, 0xd0c, x27	// Clear all CSR bits
	csrrw x11, 0xd0c, x2	// Restore CSR

// Testing CSR 0xd0d
	csrr x16, 0xd0d	// Read CSR
	li x7, -1
	csrrw x31, 0xd0d, x7	// Write all 1s to CSR
	csrrw x31, 0xd0d, x0	// Write all 0s to CSR
	csrrs x31, 0xd0d, x7	// Set all CSR bits
	csrrc x31, 0xd0d, x7	// Clear all CSR bits
	csrrw x31, 0xd0d, x16	// Restore CSR

// Testing CSR 0xd0e
	csrr x4, 0xd0e	// Read CSR
	li x6, -1
	csrrw x27, 0xd0e, x6	// Write all 1s to CSR
	csrrw x27, 0xd0e, x0	// Write all 0s to CSR
	csrrs x27, 0xd0e, x6	// Set all CSR bits
	csrrc x27, 0xd0e, x6	// Clear all CSR bits
	csrrw x27, 0xd0e, x4	// Restore CSR

// Testing CSR 0xd0f
	csrr x25, 0xd0f	// Read CSR
	li x7, -1
	csrrw x16, 0xd0f, x7	// Write all 1s to CSR
	csrrw x16, 0xd0f, x0	// Write all 0s to CSR
	csrrs x16, 0xd0f, x7	// Set all CSR bits
	csrrc x16, 0xd0f, x7	// Clear all CSR bits
	csrrw x16, 0xd0f, x25	// Restore CSR

// Testing CSR 0xd10
	csrr x31, 0xd10	// Read CSR
	li x7, -1
	csrrw x28, 0xd10, x7	// Write all 1s to CSR
	csrrw x28, 0xd10, x0	// Write all 0s to CSR
	csrrs x28, 0xd10, x7	// Set all CSR bits
	csrrc x28, 0xd10, x7	// Clear all CSR bits
	csrrw x28, 0xd10, x31	// Restore CSR

// Testing CSR 0xd11
	csrr x9, 0xd11	// Read CSR
	li x4, -1
	csrrw x24, 0xd11, x4	// Write all 1s to CSR
	csrrw x24, 0xd11, x0	// Write all 0s to CSR
	csrrs x24, 0xd11, x4	// Set all CSR bits
	csrrc x24, 0xd11, x4	// Clear all CSR bits
	csrrw x24, 0xd11, x9	// Restore CSR

// Testing CSR 0xd12
	csrr x29, 0xd12	// Read CSR
	li x3, -1
	csrrw x2, 0xd12, x3	// Write all 1s to CSR
	csrrw x2, 0xd12, x0	// Write all 0s to CSR
	csrrs x2, 0xd12, x3	// Set all CSR bits
	csrrc x2, 0xd12, x3	// Clear all CSR bits
	csrrw x2, 0xd12, x29	// Restore CSR

// Testing CSR 0xd13
	csrr x6, 0xd13	// Read CSR
	li x17, -1
	csrrw x7, 0xd13, x17	// Write all 1s to CSR
	csrrw x7, 0xd13, x0	// Write all 0s to CSR
	csrrs x7, 0xd13, x17	// Set all CSR bits
	csrrc x7, 0xd13, x17	// Clear all CSR bits
	csrrw x7, 0xd13, x6	// Restore CSR

// Testing CSR 0xd14
	csrr x13, 0xd14	// Read CSR
	li x7, -1
	csrrw x19, 0xd14, x7	// Write all 1s to CSR
	csrrw x19, 0xd14, x0	// Write all 0s to CSR
	csrrs x19, 0xd14, x7	// Set all CSR bits
	csrrc x19, 0xd14, x7	// Clear all CSR bits
	csrrw x19, 0xd14, x13	// Restore CSR

// Testing CSR 0xd15
	csrr x29, 0xd15	// Read CSR
	li x23, -1
	csrrw x4, 0xd15, x23	// Write all 1s to CSR
	csrrw x4, 0xd15, x0	// Write all 0s to CSR
	csrrs x4, 0xd15, x23	// Set all CSR bits
	csrrc x4, 0xd15, x23	// Clear all CSR bits
	csrrw x4, 0xd15, x29	// Restore CSR

// Testing CSR 0xd16
	csrr x4, 0xd16	// Read CSR
	li x3, -1
	csrrw x18, 0xd16, x3	// Write all 1s to CSR
	csrrw x18, 0xd16, x0	// Write all 0s to CSR
	csrrs x18, 0xd16, x3	// Set all CSR bits
	csrrc x18, 0xd16, x3	// Clear all CSR bits
	csrrw x18, 0xd16, x4	// Restore CSR

// Testing CSR 0xd17
	csrr x10, 0xd17	// Read CSR
	li x13, -1
	csrrw x23, 0xd17, x13	// Write all 1s to CSR
	csrrw x23, 0xd17, x0	// Write all 0s to CSR
	csrrs x23, 0xd17, x13	// Set all CSR bits
	csrrc x23, 0xd17, x13	// Clear all CSR bits
	csrrw x23, 0xd17, x10	// Restore CSR

// Testing CSR 0xd18
	csrr x5, 0xd18	// Read CSR
	li x30, -1
	csrrw x4, 0xd18, x30	// Write all 1s to CSR
	csrrw x4, 0xd18, x0	// Write all 0s to CSR
	csrrs x4, 0xd18, x30	// Set all CSR bits
	csrrc x4, 0xd18, x30	// Clear all CSR bits
	csrrw x4, 0xd18, x5	// Restore CSR

// Testing CSR 0xd19
	csrr x22, 0xd19	// Read CSR
	li x8, -1
	csrrw x25, 0xd19, x8	// Write all 1s to CSR
	csrrw x25, 0xd19, x0	// Write all 0s to CSR
	csrrs x25, 0xd19, x8	// Set all CSR bits
	csrrc x25, 0xd19, x8	// Clear all CSR bits
	csrrw x25, 0xd19, x22	// Restore CSR

// Testing CSR 0xd1a
	csrr x22, 0xd1a	// Read CSR
	li x12, -1
	csrrw x18, 0xd1a, x12	// Write all 1s to CSR
	csrrw x18, 0xd1a, x0	// Write all 0s to CSR
	csrrs x18, 0xd1a, x12	// Set all CSR bits
	csrrc x18, 0xd1a, x12	// Clear all CSR bits
	csrrw x18, 0xd1a, x22	// Restore CSR

// Testing CSR 0xd1b
	csrr x21, 0xd1b	// Read CSR
	li x25, -1
	csrrw x12, 0xd1b, x25	// Write all 1s to CSR
	csrrw x12, 0xd1b, x0	// Write all 0s to CSR
	csrrs x12, 0xd1b, x25	// Set all CSR bits
	csrrc x12, 0xd1b, x25	// Clear all CSR bits
	csrrw x12, 0xd1b, x21	// Restore CSR

// Testing CSR 0xd1c
	csrr x13, 0xd1c	// Read CSR
	li x23, -1
	csrrw x18, 0xd1c, x23	// Write all 1s to CSR
	csrrw x18, 0xd1c, x0	// Write all 0s to CSR
	csrrs x18, 0xd1c, x23	// Set all CSR bits
	csrrc x18, 0xd1c, x23	// Clear all CSR bits
	csrrw x18, 0xd1c, x13	// Restore CSR

// Testing CSR 0xd1d
	csrr x13, 0xd1d	// Read CSR
	li x1, -1
	csrrw x3, 0xd1d, x1	// Write all 1s to CSR
	csrrw x3, 0xd1d, x0	// Write all 0s to CSR
	csrrs x3, 0xd1d, x1	// Set all CSR bits
	csrrc x3, 0xd1d, x1	// Clear all CSR bits
	csrrw x3, 0xd1d, x13	// Restore CSR

// Testing CSR 0xd1e
	csrr x27, 0xd1e	// Read CSR
	li x13, -1
	csrrw x6, 0xd1e, x13	// Write all 1s to CSR
	csrrw x6, 0xd1e, x0	// Write all 0s to CSR
	csrrs x6, 0xd1e, x13	// Set all CSR bits
	csrrc x6, 0xd1e, x13	// Clear all CSR bits
	csrrw x6, 0xd1e, x27	// Restore CSR

// Testing CSR 0xd1f
	csrr x2, 0xd1f	// Read CSR
	li x6, -1
	csrrw x15, 0xd1f, x6	// Write all 1s to CSR
	csrrw x15, 0xd1f, x0	// Write all 0s to CSR
	csrrs x15, 0xd1f, x6	// Set all CSR bits
	csrrc x15, 0xd1f, x6	// Clear all CSR bits
	csrrw x15, 0xd1f, x2	// Restore CSR

// Testing CSR 0xd20
	csrr x16, 0xd20	// Read CSR
	li x4, -1
	csrrw x25, 0xd20, x4	// Write all 1s to CSR
	csrrw x25, 0xd20, x0	// Write all 0s to CSR
	csrrs x25, 0xd20, x4	// Set all CSR bits
	csrrc x25, 0xd20, x4	// Clear all CSR bits
	csrrw x25, 0xd20, x16	// Restore CSR

// Testing CSR 0xd21
	csrr x11, 0xd21	// Read CSR
	li x5, -1
	csrrw x4, 0xd21, x5	// Write all 1s to CSR
	csrrw x4, 0xd21, x0	// Write all 0s to CSR
	csrrs x4, 0xd21, x5	// Set all CSR bits
	csrrc x4, 0xd21, x5	// Clear all CSR bits
	csrrw x4, 0xd21, x11	// Restore CSR

// Testing CSR 0xd22
	csrr x12, 0xd22	// Read CSR
	li x22, -1
	csrrw x25, 0xd22, x22	// Write all 1s to CSR
	csrrw x25, 0xd22, x0	// Write all 0s to CSR
	csrrs x25, 0xd22, x22	// Set all CSR bits
	csrrc x25, 0xd22, x22	// Clear all CSR bits
	csrrw x25, 0xd22, x12	// Restore CSR

// Testing CSR 0xd23
	csrr x7, 0xd23	// Read CSR
	li x22, -1
	csrrw x21, 0xd23, x22	// Write all 1s to CSR
	csrrw x21, 0xd23, x0	// Write all 0s to CSR
	csrrs x21, 0xd23, x22	// Set all CSR bits
	csrrc x21, 0xd23, x22	// Clear all CSR bits
	csrrw x21, 0xd23, x7	// Restore CSR

// Testing CSR 0xd24
	csrr x10, 0xd24	// Read CSR
	li x23, -1
	csrrw x4, 0xd24, x23	// Write all 1s to CSR
	csrrw x4, 0xd24, x0	// Write all 0s to CSR
	csrrs x4, 0xd24, x23	// Set all CSR bits
	csrrc x4, 0xd24, x23	// Clear all CSR bits
	csrrw x4, 0xd24, x10	// Restore CSR

// Testing CSR 0xd25
	csrr x8, 0xd25	// Read CSR
	li x28, -1
	csrrw x3, 0xd25, x28	// Write all 1s to CSR
	csrrw x3, 0xd25, x0	// Write all 0s to CSR
	csrrs x3, 0xd25, x28	// Set all CSR bits
	csrrc x3, 0xd25, x28	// Clear all CSR bits
	csrrw x3, 0xd25, x8	// Restore CSR

// Testing CSR 0xd26
	csrr x10, 0xd26	// Read CSR
	li x20, -1
	csrrw x17, 0xd26, x20	// Write all 1s to CSR
	csrrw x17, 0xd26, x0	// Write all 0s to CSR
	csrrs x17, 0xd26, x20	// Set all CSR bits
	csrrc x17, 0xd26, x20	// Clear all CSR bits
	csrrw x17, 0xd26, x10	// Restore CSR

// Testing CSR 0xd27
	csrr x23, 0xd27	// Read CSR
	li x27, -1
	csrrw x1, 0xd27, x27	// Write all 1s to CSR
	csrrw x1, 0xd27, x0	// Write all 0s to CSR
	csrrs x1, 0xd27, x27	// Set all CSR bits
	csrrc x1, 0xd27, x27	// Clear all CSR bits
	csrrw x1, 0xd27, x23	// Restore CSR

// Testing CSR 0xd28
	csrr x30, 0xd28	// Read CSR
	li x21, -1
	csrrw x16, 0xd28, x21	// Write all 1s to CSR
	csrrw x16, 0xd28, x0	// Write all 0s to CSR
	csrrs x16, 0xd28, x21	// Set all CSR bits
	csrrc x16, 0xd28, x21	// Clear all CSR bits
	csrrw x16, 0xd28, x30	// Restore CSR

// Testing CSR 0xd29
	csrr x12, 0xd29	// Read CSR
	li x8, -1
	csrrw x29, 0xd29, x8	// Write all 1s to CSR
	csrrw x29, 0xd29, x0	// Write all 0s to CSR
	csrrs x29, 0xd29, x8	// Set all CSR bits
	csrrc x29, 0xd29, x8	// Clear all CSR bits
	csrrw x29, 0xd29, x12	// Restore CSR

// Testing CSR 0xd2a
	csrr x20, 0xd2a	// Read CSR
	li x1, -1
	csrrw x6, 0xd2a, x1	// Write all 1s to CSR
	csrrw x6, 0xd2a, x0	// Write all 0s to CSR
	csrrs x6, 0xd2a, x1	// Set all CSR bits
	csrrc x6, 0xd2a, x1	// Clear all CSR bits
	csrrw x6, 0xd2a, x20	// Restore CSR

// Testing CSR 0xd2b
	csrr x14, 0xd2b	// Read CSR
	li x7, -1
	csrrw x20, 0xd2b, x7	// Write all 1s to CSR
	csrrw x20, 0xd2b, x0	// Write all 0s to CSR
	csrrs x20, 0xd2b, x7	// Set all CSR bits
	csrrc x20, 0xd2b, x7	// Clear all CSR bits
	csrrw x20, 0xd2b, x14	// Restore CSR

// Testing CSR 0xd2c
	csrr x28, 0xd2c	// Read CSR
	li x21, -1
	csrrw x12, 0xd2c, x21	// Write all 1s to CSR
	csrrw x12, 0xd2c, x0	// Write all 0s to CSR
	csrrs x12, 0xd2c, x21	// Set all CSR bits
	csrrc x12, 0xd2c, x21	// Clear all CSR bits
	csrrw x12, 0xd2c, x28	// Restore CSR

// Testing CSR 0xd2d
	csrr x6, 0xd2d	// Read CSR
	li x12, -1
	csrrw x21, 0xd2d, x12	// Write all 1s to CSR
	csrrw x21, 0xd2d, x0	// Write all 0s to CSR
	csrrs x21, 0xd2d, x12	// Set all CSR bits
	csrrc x21, 0xd2d, x12	// Clear all CSR bits
	csrrw x21, 0xd2d, x6	// Restore CSR

// Testing CSR 0xd2e
	csrr x15, 0xd2e	// Read CSR
	li x19, -1
	csrrw x2, 0xd2e, x19	// Write all 1s to CSR
	csrrw x2, 0xd2e, x0	// Write all 0s to CSR
	csrrs x2, 0xd2e, x19	// Set all CSR bits
	csrrc x2, 0xd2e, x19	// Clear all CSR bits
	csrrw x2, 0xd2e, x15	// Restore CSR

// Testing CSR 0xd2f
	csrr x26, 0xd2f	// Read CSR
	li x31, -1
	csrrw x8, 0xd2f, x31	// Write all 1s to CSR
	csrrw x8, 0xd2f, x0	// Write all 0s to CSR
	csrrs x8, 0xd2f, x31	// Set all CSR bits
	csrrc x8, 0xd2f, x31	// Clear all CSR bits
	csrrw x8, 0xd2f, x26	// Restore CSR

// Testing CSR 0xd30
	csrr x12, 0xd30	// Read CSR
	li x3, -1
	csrrw x6, 0xd30, x3	// Write all 1s to CSR
	csrrw x6, 0xd30, x0	// Write all 0s to CSR
	csrrs x6, 0xd30, x3	// Set all CSR bits
	csrrc x6, 0xd30, x3	// Clear all CSR bits
	csrrw x6, 0xd30, x12	// Restore CSR

// Testing CSR 0xd31
	csrr x3, 0xd31	// Read CSR
	li x14, -1
	csrrw x24, 0xd31, x14	// Write all 1s to CSR
	csrrw x24, 0xd31, x0	// Write all 0s to CSR
	csrrs x24, 0xd31, x14	// Set all CSR bits
	csrrc x24, 0xd31, x14	// Clear all CSR bits
	csrrw x24, 0xd31, x3	// Restore CSR

// Testing CSR 0xd32
	csrr x30, 0xd32	// Read CSR
	li x29, -1
	csrrw x1, 0xd32, x29	// Write all 1s to CSR
	csrrw x1, 0xd32, x0	// Write all 0s to CSR
	csrrs x1, 0xd32, x29	// Set all CSR bits
	csrrc x1, 0xd32, x29	// Clear all CSR bits
	csrrw x1, 0xd32, x30	// Restore CSR

// Testing CSR 0xd33
	csrr x27, 0xd33	// Read CSR
	li x30, -1
	csrrw x11, 0xd33, x30	// Write all 1s to CSR
	csrrw x11, 0xd33, x0	// Write all 0s to CSR
	csrrs x11, 0xd33, x30	// Set all CSR bits
	csrrc x11, 0xd33, x30	// Clear all CSR bits
	csrrw x11, 0xd33, x27	// Restore CSR

// Testing CSR 0xd34
	csrr x8, 0xd34	// Read CSR
	li x21, -1
	csrrw x25, 0xd34, x21	// Write all 1s to CSR
	csrrw x25, 0xd34, x0	// Write all 0s to CSR
	csrrs x25, 0xd34, x21	// Set all CSR bits
	csrrc x25, 0xd34, x21	// Clear all CSR bits
	csrrw x25, 0xd34, x8	// Restore CSR

// Testing CSR 0xd35
	csrr x11, 0xd35	// Read CSR
	li x23, -1
	csrrw x8, 0xd35, x23	// Write all 1s to CSR
	csrrw x8, 0xd35, x0	// Write all 0s to CSR
	csrrs x8, 0xd35, x23	// Set all CSR bits
	csrrc x8, 0xd35, x23	// Clear all CSR bits
	csrrw x8, 0xd35, x11	// Restore CSR

// Testing CSR 0xd36
	csrr x1, 0xd36	// Read CSR
	li x15, -1
	csrrw x17, 0xd36, x15	// Write all 1s to CSR
	csrrw x17, 0xd36, x0	// Write all 0s to CSR
	csrrs x17, 0xd36, x15	// Set all CSR bits
	csrrc x17, 0xd36, x15	// Clear all CSR bits
	csrrw x17, 0xd36, x1	// Restore CSR

// Testing CSR 0xd37
	csrr x9, 0xd37	// Read CSR
	li x16, -1
	csrrw x18, 0xd37, x16	// Write all 1s to CSR
	csrrw x18, 0xd37, x0	// Write all 0s to CSR
	csrrs x18, 0xd37, x16	// Set all CSR bits
	csrrc x18, 0xd37, x16	// Clear all CSR bits
	csrrw x18, 0xd37, x9	// Restore CSR

// Testing CSR 0xd38
	csrr x10, 0xd38	// Read CSR
	li x11, -1
	csrrw x2, 0xd38, x11	// Write all 1s to CSR
	csrrw x2, 0xd38, x0	// Write all 0s to CSR
	csrrs x2, 0xd38, x11	// Set all CSR bits
	csrrc x2, 0xd38, x11	// Clear all CSR bits
	csrrw x2, 0xd38, x10	// Restore CSR

// Testing CSR 0xd39
	csrr x21, 0xd39	// Read CSR
	li x20, -1
	csrrw x2, 0xd39, x20	// Write all 1s to CSR
	csrrw x2, 0xd39, x0	// Write all 0s to CSR
	csrrs x2, 0xd39, x20	// Set all CSR bits
	csrrc x2, 0xd39, x20	// Clear all CSR bits
	csrrw x2, 0xd39, x21	// Restore CSR

// Testing CSR 0xd3a
	csrr x18, 0xd3a	// Read CSR
	li x7, -1
	csrrw x9, 0xd3a, x7	// Write all 1s to CSR
	csrrw x9, 0xd3a, x0	// Write all 0s to CSR
	csrrs x9, 0xd3a, x7	// Set all CSR bits
	csrrc x9, 0xd3a, x7	// Clear all CSR bits
	csrrw x9, 0xd3a, x18	// Restore CSR

// Testing CSR 0xd3b
	csrr x23, 0xd3b	// Read CSR
	li x26, -1
	csrrw x9, 0xd3b, x26	// Write all 1s to CSR
	csrrw x9, 0xd3b, x0	// Write all 0s to CSR
	csrrs x9, 0xd3b, x26	// Set all CSR bits
	csrrc x9, 0xd3b, x26	// Clear all CSR bits
	csrrw x9, 0xd3b, x23	// Restore CSR

// Testing CSR 0xd3c
	csrr x11, 0xd3c	// Read CSR
	li x28, -1
	csrrw x20, 0xd3c, x28	// Write all 1s to CSR
	csrrw x20, 0xd3c, x0	// Write all 0s to CSR
	csrrs x20, 0xd3c, x28	// Set all CSR bits
	csrrc x20, 0xd3c, x28	// Clear all CSR bits
	csrrw x20, 0xd3c, x11	// Restore CSR

// Testing CSR 0xd3d
	csrr x28, 0xd3d	// Read CSR
	li x3, -1
	csrrw x31, 0xd3d, x3	// Write all 1s to CSR
	csrrw x31, 0xd3d, x0	// Write all 0s to CSR
	csrrs x31, 0xd3d, x3	// Set all CSR bits
	csrrc x31, 0xd3d, x3	// Clear all CSR bits
	csrrw x31, 0xd3d, x28	// Restore CSR

// Testing CSR 0xd3e
	csrr x7, 0xd3e	// Read CSR
	li x22, -1
	csrrw x1, 0xd3e, x22	// Write all 1s to CSR
	csrrw x1, 0xd3e, x0	// Write all 0s to CSR
	csrrs x1, 0xd3e, x22	// Set all CSR bits
	csrrc x1, 0xd3e, x22	// Clear all CSR bits
	csrrw x1, 0xd3e, x7	// Restore CSR

// Testing CSR 0xd3f
	csrr x28, 0xd3f	// Read CSR
	li x11, -1
	csrrw x7, 0xd3f, x11	// Write all 1s to CSR
	csrrw x7, 0xd3f, x0	// Write all 0s to CSR
	csrrs x7, 0xd3f, x11	// Set all CSR bits
	csrrc x7, 0xd3f, x11	// Clear all CSR bits
	csrrw x7, 0xd3f, x28	// Restore CSR

// Testing CSR 0xd40
	csrr x24, 0xd40	// Read CSR
	li x19, -1
	csrrw x2, 0xd40, x19	// Write all 1s to CSR
	csrrw x2, 0xd40, x0	// Write all 0s to CSR
	csrrs x2, 0xd40, x19	// Set all CSR bits
	csrrc x2, 0xd40, x19	// Clear all CSR bits
	csrrw x2, 0xd40, x24	// Restore CSR

// Testing CSR 0xd41
	csrr x12, 0xd41	// Read CSR
	li x1, -1
	csrrw x14, 0xd41, x1	// Write all 1s to CSR
	csrrw x14, 0xd41, x0	// Write all 0s to CSR
	csrrs x14, 0xd41, x1	// Set all CSR bits
	csrrc x14, 0xd41, x1	// Clear all CSR bits
	csrrw x14, 0xd41, x12	// Restore CSR

// Testing CSR 0xd42
	csrr x11, 0xd42	// Read CSR
	li x21, -1
	csrrw x16, 0xd42, x21	// Write all 1s to CSR
	csrrw x16, 0xd42, x0	// Write all 0s to CSR
	csrrs x16, 0xd42, x21	// Set all CSR bits
	csrrc x16, 0xd42, x21	// Clear all CSR bits
	csrrw x16, 0xd42, x11	// Restore CSR

// Testing CSR 0xd43
	csrr x7, 0xd43	// Read CSR
	li x21, -1
	csrrw x3, 0xd43, x21	// Write all 1s to CSR
	csrrw x3, 0xd43, x0	// Write all 0s to CSR
	csrrs x3, 0xd43, x21	// Set all CSR bits
	csrrc x3, 0xd43, x21	// Clear all CSR bits
	csrrw x3, 0xd43, x7	// Restore CSR

// Testing CSR 0xd44
	csrr x6, 0xd44	// Read CSR
	li x3, -1
	csrrw x21, 0xd44, x3	// Write all 1s to CSR
	csrrw x21, 0xd44, x0	// Write all 0s to CSR
	csrrs x21, 0xd44, x3	// Set all CSR bits
	csrrc x21, 0xd44, x3	// Clear all CSR bits
	csrrw x21, 0xd44, x6	// Restore CSR

// Testing CSR 0xd45
	csrr x18, 0xd45	// Read CSR
	li x23, -1
	csrrw x9, 0xd45, x23	// Write all 1s to CSR
	csrrw x9, 0xd45, x0	// Write all 0s to CSR
	csrrs x9, 0xd45, x23	// Set all CSR bits
	csrrc x9, 0xd45, x23	// Clear all CSR bits
	csrrw x9, 0xd45, x18	// Restore CSR

// Testing CSR 0xd46
	csrr x22, 0xd46	// Read CSR
	li x5, -1
	csrrw x18, 0xd46, x5	// Write all 1s to CSR
	csrrw x18, 0xd46, x0	// Write all 0s to CSR
	csrrs x18, 0xd46, x5	// Set all CSR bits
	csrrc x18, 0xd46, x5	// Clear all CSR bits
	csrrw x18, 0xd46, x22	// Restore CSR

// Testing CSR 0xd47
	csrr x7, 0xd47	// Read CSR
	li x14, -1
	csrrw x6, 0xd47, x14	// Write all 1s to CSR
	csrrw x6, 0xd47, x0	// Write all 0s to CSR
	csrrs x6, 0xd47, x14	// Set all CSR bits
	csrrc x6, 0xd47, x14	// Clear all CSR bits
	csrrw x6, 0xd47, x7	// Restore CSR

// Testing CSR 0xd48
	csrr x3, 0xd48	// Read CSR
	li x26, -1
	csrrw x30, 0xd48, x26	// Write all 1s to CSR
	csrrw x30, 0xd48, x0	// Write all 0s to CSR
	csrrs x30, 0xd48, x26	// Set all CSR bits
	csrrc x30, 0xd48, x26	// Clear all CSR bits
	csrrw x30, 0xd48, x3	// Restore CSR

// Testing CSR 0xd49
	csrr x16, 0xd49	// Read CSR
	li x9, -1
	csrrw x5, 0xd49, x9	// Write all 1s to CSR
	csrrw x5, 0xd49, x0	// Write all 0s to CSR
	csrrs x5, 0xd49, x9	// Set all CSR bits
	csrrc x5, 0xd49, x9	// Clear all CSR bits
	csrrw x5, 0xd49, x16	// Restore CSR

// Testing CSR 0xd4a
	csrr x14, 0xd4a	// Read CSR
	li x3, -1
	csrrw x2, 0xd4a, x3	// Write all 1s to CSR
	csrrw x2, 0xd4a, x0	// Write all 0s to CSR
	csrrs x2, 0xd4a, x3	// Set all CSR bits
	csrrc x2, 0xd4a, x3	// Clear all CSR bits
	csrrw x2, 0xd4a, x14	// Restore CSR

// Testing CSR 0xd4b
	csrr x15, 0xd4b	// Read CSR
	li x2, -1
	csrrw x8, 0xd4b, x2	// Write all 1s to CSR
	csrrw x8, 0xd4b, x0	// Write all 0s to CSR
	csrrs x8, 0xd4b, x2	// Set all CSR bits
	csrrc x8, 0xd4b, x2	// Clear all CSR bits
	csrrw x8, 0xd4b, x15	// Restore CSR

// Testing CSR 0xd4c
	csrr x27, 0xd4c	// Read CSR
	li x5, -1
	csrrw x17, 0xd4c, x5	// Write all 1s to CSR
	csrrw x17, 0xd4c, x0	// Write all 0s to CSR
	csrrs x17, 0xd4c, x5	// Set all CSR bits
	csrrc x17, 0xd4c, x5	// Clear all CSR bits
	csrrw x17, 0xd4c, x27	// Restore CSR

// Testing CSR 0xd4d
	csrr x16, 0xd4d	// Read CSR
	li x15, -1
	csrrw x8, 0xd4d, x15	// Write all 1s to CSR
	csrrw x8, 0xd4d, x0	// Write all 0s to CSR
	csrrs x8, 0xd4d, x15	// Set all CSR bits
	csrrc x8, 0xd4d, x15	// Clear all CSR bits
	csrrw x8, 0xd4d, x16	// Restore CSR

// Testing CSR 0xd4e
	csrr x13, 0xd4e	// Read CSR
	li x26, -1
	csrrw x23, 0xd4e, x26	// Write all 1s to CSR
	csrrw x23, 0xd4e, x0	// Write all 0s to CSR
	csrrs x23, 0xd4e, x26	// Set all CSR bits
	csrrc x23, 0xd4e, x26	// Clear all CSR bits
	csrrw x23, 0xd4e, x13	// Restore CSR

// Testing CSR 0xd4f
	csrr x17, 0xd4f	// Read CSR
	li x19, -1
	csrrw x7, 0xd4f, x19	// Write all 1s to CSR
	csrrw x7, 0xd4f, x0	// Write all 0s to CSR
	csrrs x7, 0xd4f, x19	// Set all CSR bits
	csrrc x7, 0xd4f, x19	// Clear all CSR bits
	csrrw x7, 0xd4f, x17	// Restore CSR

// Testing CSR 0xd50
	csrr x17, 0xd50	// Read CSR
	li x13, -1
	csrrw x8, 0xd50, x13	// Write all 1s to CSR
	csrrw x8, 0xd50, x0	// Write all 0s to CSR
	csrrs x8, 0xd50, x13	// Set all CSR bits
	csrrc x8, 0xd50, x13	// Clear all CSR bits
	csrrw x8, 0xd50, x17	// Restore CSR

// Testing CSR 0xd51
	csrr x10, 0xd51	// Read CSR
	li x17, -1
	csrrw x27, 0xd51, x17	// Write all 1s to CSR
	csrrw x27, 0xd51, x0	// Write all 0s to CSR
	csrrs x27, 0xd51, x17	// Set all CSR bits
	csrrc x27, 0xd51, x17	// Clear all CSR bits
	csrrw x27, 0xd51, x10	// Restore CSR

// Testing CSR 0xd52
	csrr x23, 0xd52	// Read CSR
	li x25, -1
	csrrw x11, 0xd52, x25	// Write all 1s to CSR
	csrrw x11, 0xd52, x0	// Write all 0s to CSR
	csrrs x11, 0xd52, x25	// Set all CSR bits
	csrrc x11, 0xd52, x25	// Clear all CSR bits
	csrrw x11, 0xd52, x23	// Restore CSR

// Testing CSR 0xd53
	csrr x6, 0xd53	// Read CSR
	li x1, -1
	csrrw x13, 0xd53, x1	// Write all 1s to CSR
	csrrw x13, 0xd53, x0	// Write all 0s to CSR
	csrrs x13, 0xd53, x1	// Set all CSR bits
	csrrc x13, 0xd53, x1	// Clear all CSR bits
	csrrw x13, 0xd53, x6	// Restore CSR

// Testing CSR 0xd54
	csrr x30, 0xd54	// Read CSR
	li x22, -1
	csrrw x27, 0xd54, x22	// Write all 1s to CSR
	csrrw x27, 0xd54, x0	// Write all 0s to CSR
	csrrs x27, 0xd54, x22	// Set all CSR bits
	csrrc x27, 0xd54, x22	// Clear all CSR bits
	csrrw x27, 0xd54, x30	// Restore CSR

// Testing CSR 0xd55
	csrr x30, 0xd55	// Read CSR
	li x6, -1
	csrrw x8, 0xd55, x6	// Write all 1s to CSR
	csrrw x8, 0xd55, x0	// Write all 0s to CSR
	csrrs x8, 0xd55, x6	// Set all CSR bits
	csrrc x8, 0xd55, x6	// Clear all CSR bits
	csrrw x8, 0xd55, x30	// Restore CSR

// Testing CSR 0xd56
	csrr x29, 0xd56	// Read CSR
	li x21, -1
	csrrw x9, 0xd56, x21	// Write all 1s to CSR
	csrrw x9, 0xd56, x0	// Write all 0s to CSR
	csrrs x9, 0xd56, x21	// Set all CSR bits
	csrrc x9, 0xd56, x21	// Clear all CSR bits
	csrrw x9, 0xd56, x29	// Restore CSR

// Testing CSR 0xd57
	csrr x18, 0xd57	// Read CSR
	li x25, -1
	csrrw x22, 0xd57, x25	// Write all 1s to CSR
	csrrw x22, 0xd57, x0	// Write all 0s to CSR
	csrrs x22, 0xd57, x25	// Set all CSR bits
	csrrc x22, 0xd57, x25	// Clear all CSR bits
	csrrw x22, 0xd57, x18	// Restore CSR

// Testing CSR 0xd58
	csrr x29, 0xd58	// Read CSR
	li x28, -1
	csrrw x25, 0xd58, x28	// Write all 1s to CSR
	csrrw x25, 0xd58, x0	// Write all 0s to CSR
	csrrs x25, 0xd58, x28	// Set all CSR bits
	csrrc x25, 0xd58, x28	// Clear all CSR bits
	csrrw x25, 0xd58, x29	// Restore CSR

// Testing CSR 0xd59
	csrr x27, 0xd59	// Read CSR
	li x24, -1
	csrrw x3, 0xd59, x24	// Write all 1s to CSR
	csrrw x3, 0xd59, x0	// Write all 0s to CSR
	csrrs x3, 0xd59, x24	// Set all CSR bits
	csrrc x3, 0xd59, x24	// Clear all CSR bits
	csrrw x3, 0xd59, x27	// Restore CSR

// Testing CSR 0xd5a
	csrr x10, 0xd5a	// Read CSR
	li x31, -1
	csrrw x19, 0xd5a, x31	// Write all 1s to CSR
	csrrw x19, 0xd5a, x0	// Write all 0s to CSR
	csrrs x19, 0xd5a, x31	// Set all CSR bits
	csrrc x19, 0xd5a, x31	// Clear all CSR bits
	csrrw x19, 0xd5a, x10	// Restore CSR

// Testing CSR 0xd5b
	csrr x18, 0xd5b	// Read CSR
	li x3, -1
	csrrw x9, 0xd5b, x3	// Write all 1s to CSR
	csrrw x9, 0xd5b, x0	// Write all 0s to CSR
	csrrs x9, 0xd5b, x3	// Set all CSR bits
	csrrc x9, 0xd5b, x3	// Clear all CSR bits
	csrrw x9, 0xd5b, x18	// Restore CSR

// Testing CSR 0xd5c
	csrr x1, 0xd5c	// Read CSR
	li x16, -1
	csrrw x15, 0xd5c, x16	// Write all 1s to CSR
	csrrw x15, 0xd5c, x0	// Write all 0s to CSR
	csrrs x15, 0xd5c, x16	// Set all CSR bits
	csrrc x15, 0xd5c, x16	// Clear all CSR bits
	csrrw x15, 0xd5c, x1	// Restore CSR

// Testing CSR 0xd5d
	csrr x28, 0xd5d	// Read CSR
	li x31, -1
	csrrw x24, 0xd5d, x31	// Write all 1s to CSR
	csrrw x24, 0xd5d, x0	// Write all 0s to CSR
	csrrs x24, 0xd5d, x31	// Set all CSR bits
	csrrc x24, 0xd5d, x31	// Clear all CSR bits
	csrrw x24, 0xd5d, x28	// Restore CSR

// Testing CSR 0xd5e
	csrr x9, 0xd5e	// Read CSR
	li x19, -1
	csrrw x20, 0xd5e, x19	// Write all 1s to CSR
	csrrw x20, 0xd5e, x0	// Write all 0s to CSR
	csrrs x20, 0xd5e, x19	// Set all CSR bits
	csrrc x20, 0xd5e, x19	// Clear all CSR bits
	csrrw x20, 0xd5e, x9	// Restore CSR

// Testing CSR 0xd5f
	csrr x26, 0xd5f	// Read CSR
	li x13, -1
	csrrw x30, 0xd5f, x13	// Write all 1s to CSR
	csrrw x30, 0xd5f, x0	// Write all 0s to CSR
	csrrs x30, 0xd5f, x13	// Set all CSR bits
	csrrc x30, 0xd5f, x13	// Clear all CSR bits
	csrrw x30, 0xd5f, x26	// Restore CSR

// Testing CSR 0xd60
	csrr x28, 0xd60	// Read CSR
	li x14, -1
	csrrw x6, 0xd60, x14	// Write all 1s to CSR
	csrrw x6, 0xd60, x0	// Write all 0s to CSR
	csrrs x6, 0xd60, x14	// Set all CSR bits
	csrrc x6, 0xd60, x14	// Clear all CSR bits
	csrrw x6, 0xd60, x28	// Restore CSR

// Testing CSR 0xd61
	csrr x2, 0xd61	// Read CSR
	li x29, -1
	csrrw x26, 0xd61, x29	// Write all 1s to CSR
	csrrw x26, 0xd61, x0	// Write all 0s to CSR
	csrrs x26, 0xd61, x29	// Set all CSR bits
	csrrc x26, 0xd61, x29	// Clear all CSR bits
	csrrw x26, 0xd61, x2	// Restore CSR

// Testing CSR 0xd62
	csrr x24, 0xd62	// Read CSR
	li x8, -1
	csrrw x19, 0xd62, x8	// Write all 1s to CSR
	csrrw x19, 0xd62, x0	// Write all 0s to CSR
	csrrs x19, 0xd62, x8	// Set all CSR bits
	csrrc x19, 0xd62, x8	// Clear all CSR bits
	csrrw x19, 0xd62, x24	// Restore CSR

// Testing CSR 0xd63
	csrr x22, 0xd63	// Read CSR
	li x14, -1
	csrrw x28, 0xd63, x14	// Write all 1s to CSR
	csrrw x28, 0xd63, x0	// Write all 0s to CSR
	csrrs x28, 0xd63, x14	// Set all CSR bits
	csrrc x28, 0xd63, x14	// Clear all CSR bits
	csrrw x28, 0xd63, x22	// Restore CSR

// Testing CSR 0xd64
	csrr x7, 0xd64	// Read CSR
	li x29, -1
	csrrw x10, 0xd64, x29	// Write all 1s to CSR
	csrrw x10, 0xd64, x0	// Write all 0s to CSR
	csrrs x10, 0xd64, x29	// Set all CSR bits
	csrrc x10, 0xd64, x29	// Clear all CSR bits
	csrrw x10, 0xd64, x7	// Restore CSR

// Testing CSR 0xd65
	csrr x2, 0xd65	// Read CSR
	li x28, -1
	csrrw x24, 0xd65, x28	// Write all 1s to CSR
	csrrw x24, 0xd65, x0	// Write all 0s to CSR
	csrrs x24, 0xd65, x28	// Set all CSR bits
	csrrc x24, 0xd65, x28	// Clear all CSR bits
	csrrw x24, 0xd65, x2	// Restore CSR

// Testing CSR 0xd66
	csrr x2, 0xd66	// Read CSR
	li x23, -1
	csrrw x11, 0xd66, x23	// Write all 1s to CSR
	csrrw x11, 0xd66, x0	// Write all 0s to CSR
	csrrs x11, 0xd66, x23	// Set all CSR bits
	csrrc x11, 0xd66, x23	// Clear all CSR bits
	csrrw x11, 0xd66, x2	// Restore CSR

// Testing CSR 0xd67
	csrr x12, 0xd67	// Read CSR
	li x5, -1
	csrrw x20, 0xd67, x5	// Write all 1s to CSR
	csrrw x20, 0xd67, x0	// Write all 0s to CSR
	csrrs x20, 0xd67, x5	// Set all CSR bits
	csrrc x20, 0xd67, x5	// Clear all CSR bits
	csrrw x20, 0xd67, x12	// Restore CSR

// Testing CSR 0xd68
	csrr x9, 0xd68	// Read CSR
	li x6, -1
	csrrw x28, 0xd68, x6	// Write all 1s to CSR
	csrrw x28, 0xd68, x0	// Write all 0s to CSR
	csrrs x28, 0xd68, x6	// Set all CSR bits
	csrrc x28, 0xd68, x6	// Clear all CSR bits
	csrrw x28, 0xd68, x9	// Restore CSR

// Testing CSR 0xd69
	csrr x7, 0xd69	// Read CSR
	li x26, -1
	csrrw x29, 0xd69, x26	// Write all 1s to CSR
	csrrw x29, 0xd69, x0	// Write all 0s to CSR
	csrrs x29, 0xd69, x26	// Set all CSR bits
	csrrc x29, 0xd69, x26	// Clear all CSR bits
	csrrw x29, 0xd69, x7	// Restore CSR

// Testing CSR 0xd6a
	csrr x9, 0xd6a	// Read CSR
	li x24, -1
	csrrw x15, 0xd6a, x24	// Write all 1s to CSR
	csrrw x15, 0xd6a, x0	// Write all 0s to CSR
	csrrs x15, 0xd6a, x24	// Set all CSR bits
	csrrc x15, 0xd6a, x24	// Clear all CSR bits
	csrrw x15, 0xd6a, x9	// Restore CSR

// Testing CSR 0xd6b
	csrr x11, 0xd6b	// Read CSR
	li x22, -1
	csrrw x14, 0xd6b, x22	// Write all 1s to CSR
	csrrw x14, 0xd6b, x0	// Write all 0s to CSR
	csrrs x14, 0xd6b, x22	// Set all CSR bits
	csrrc x14, 0xd6b, x22	// Clear all CSR bits
	csrrw x14, 0xd6b, x11	// Restore CSR

// Testing CSR 0xd6c
	csrr x24, 0xd6c	// Read CSR
	li x14, -1
	csrrw x9, 0xd6c, x14	// Write all 1s to CSR
	csrrw x9, 0xd6c, x0	// Write all 0s to CSR
	csrrs x9, 0xd6c, x14	// Set all CSR bits
	csrrc x9, 0xd6c, x14	// Clear all CSR bits
	csrrw x9, 0xd6c, x24	// Restore CSR

// Testing CSR 0xd6d
	csrr x6, 0xd6d	// Read CSR
	li x18, -1
	csrrw x12, 0xd6d, x18	// Write all 1s to CSR
	csrrw x12, 0xd6d, x0	// Write all 0s to CSR
	csrrs x12, 0xd6d, x18	// Set all CSR bits
	csrrc x12, 0xd6d, x18	// Clear all CSR bits
	csrrw x12, 0xd6d, x6	// Restore CSR

// Testing CSR 0xd6e
	csrr x2, 0xd6e	// Read CSR
	li x25, -1
	csrrw x20, 0xd6e, x25	// Write all 1s to CSR
	csrrw x20, 0xd6e, x0	// Write all 0s to CSR
	csrrs x20, 0xd6e, x25	// Set all CSR bits
	csrrc x20, 0xd6e, x25	// Clear all CSR bits
	csrrw x20, 0xd6e, x2	// Restore CSR

// Testing CSR 0xd6f
	csrr x3, 0xd6f	// Read CSR
	li x4, -1
	csrrw x12, 0xd6f, x4	// Write all 1s to CSR
	csrrw x12, 0xd6f, x0	// Write all 0s to CSR
	csrrs x12, 0xd6f, x4	// Set all CSR bits
	csrrc x12, 0xd6f, x4	// Clear all CSR bits
	csrrw x12, 0xd6f, x3	// Restore CSR

// Testing CSR 0xd70
	csrr x31, 0xd70	// Read CSR
	li x30, -1
	csrrw x23, 0xd70, x30	// Write all 1s to CSR
	csrrw x23, 0xd70, x0	// Write all 0s to CSR
	csrrs x23, 0xd70, x30	// Set all CSR bits
	csrrc x23, 0xd70, x30	// Clear all CSR bits
	csrrw x23, 0xd70, x31	// Restore CSR

// Testing CSR 0xd71
	csrr x25, 0xd71	// Read CSR
	li x7, -1
	csrrw x22, 0xd71, x7	// Write all 1s to CSR
	csrrw x22, 0xd71, x0	// Write all 0s to CSR
	csrrs x22, 0xd71, x7	// Set all CSR bits
	csrrc x22, 0xd71, x7	// Clear all CSR bits
	csrrw x22, 0xd71, x25	// Restore CSR

// Testing CSR 0xd72
	csrr x16, 0xd72	// Read CSR
	li x18, -1
	csrrw x15, 0xd72, x18	// Write all 1s to CSR
	csrrw x15, 0xd72, x0	// Write all 0s to CSR
	csrrs x15, 0xd72, x18	// Set all CSR bits
	csrrc x15, 0xd72, x18	// Clear all CSR bits
	csrrw x15, 0xd72, x16	// Restore CSR

// Testing CSR 0xd73
	csrr x3, 0xd73	// Read CSR
	li x20, -1
	csrrw x24, 0xd73, x20	// Write all 1s to CSR
	csrrw x24, 0xd73, x0	// Write all 0s to CSR
	csrrs x24, 0xd73, x20	// Set all CSR bits
	csrrc x24, 0xd73, x20	// Clear all CSR bits
	csrrw x24, 0xd73, x3	// Restore CSR

// Testing CSR 0xd74
	csrr x19, 0xd74	// Read CSR
	li x7, -1
	csrrw x23, 0xd74, x7	// Write all 1s to CSR
	csrrw x23, 0xd74, x0	// Write all 0s to CSR
	csrrs x23, 0xd74, x7	// Set all CSR bits
	csrrc x23, 0xd74, x7	// Clear all CSR bits
	csrrw x23, 0xd74, x19	// Restore CSR

// Testing CSR 0xd75
	csrr x11, 0xd75	// Read CSR
	li x21, -1
	csrrw x3, 0xd75, x21	// Write all 1s to CSR
	csrrw x3, 0xd75, x0	// Write all 0s to CSR
	csrrs x3, 0xd75, x21	// Set all CSR bits
	csrrc x3, 0xd75, x21	// Clear all CSR bits
	csrrw x3, 0xd75, x11	// Restore CSR

// Testing CSR 0xd76
	csrr x18, 0xd76	// Read CSR
	li x5, -1
	csrrw x9, 0xd76, x5	// Write all 1s to CSR
	csrrw x9, 0xd76, x0	// Write all 0s to CSR
	csrrs x9, 0xd76, x5	// Set all CSR bits
	csrrc x9, 0xd76, x5	// Clear all CSR bits
	csrrw x9, 0xd76, x18	// Restore CSR

// Testing CSR 0xd77
	csrr x21, 0xd77	// Read CSR
	li x3, -1
	csrrw x15, 0xd77, x3	// Write all 1s to CSR
	csrrw x15, 0xd77, x0	// Write all 0s to CSR
	csrrs x15, 0xd77, x3	// Set all CSR bits
	csrrc x15, 0xd77, x3	// Clear all CSR bits
	csrrw x15, 0xd77, x21	// Restore CSR

// Testing CSR 0xd78
	csrr x12, 0xd78	// Read CSR
	li x1, -1
	csrrw x27, 0xd78, x1	// Write all 1s to CSR
	csrrw x27, 0xd78, x0	// Write all 0s to CSR
	csrrs x27, 0xd78, x1	// Set all CSR bits
	csrrc x27, 0xd78, x1	// Clear all CSR bits
	csrrw x27, 0xd78, x12	// Restore CSR

// Testing CSR 0xd79
	csrr x22, 0xd79	// Read CSR
	li x2, -1
	csrrw x24, 0xd79, x2	// Write all 1s to CSR
	csrrw x24, 0xd79, x0	// Write all 0s to CSR
	csrrs x24, 0xd79, x2	// Set all CSR bits
	csrrc x24, 0xd79, x2	// Clear all CSR bits
	csrrw x24, 0xd79, x22	// Restore CSR

// Testing CSR 0xd7a
	csrr x28, 0xd7a	// Read CSR
	li x7, -1
	csrrw x16, 0xd7a, x7	// Write all 1s to CSR
	csrrw x16, 0xd7a, x0	// Write all 0s to CSR
	csrrs x16, 0xd7a, x7	// Set all CSR bits
	csrrc x16, 0xd7a, x7	// Clear all CSR bits
	csrrw x16, 0xd7a, x28	// Restore CSR

// Testing CSR 0xd7b
	csrr x25, 0xd7b	// Read CSR
	li x5, -1
	csrrw x17, 0xd7b, x5	// Write all 1s to CSR
	csrrw x17, 0xd7b, x0	// Write all 0s to CSR
	csrrs x17, 0xd7b, x5	// Set all CSR bits
	csrrc x17, 0xd7b, x5	// Clear all CSR bits
	csrrw x17, 0xd7b, x25	// Restore CSR

// Testing CSR 0xd7c
	csrr x30, 0xd7c	// Read CSR
	li x7, -1
	csrrw x21, 0xd7c, x7	// Write all 1s to CSR
	csrrw x21, 0xd7c, x0	// Write all 0s to CSR
	csrrs x21, 0xd7c, x7	// Set all CSR bits
	csrrc x21, 0xd7c, x7	// Clear all CSR bits
	csrrw x21, 0xd7c, x30	// Restore CSR

// Testing CSR 0xd7d
	csrr x4, 0xd7d	// Read CSR
	li x15, -1
	csrrw x24, 0xd7d, x15	// Write all 1s to CSR
	csrrw x24, 0xd7d, x0	// Write all 0s to CSR
	csrrs x24, 0xd7d, x15	// Set all CSR bits
	csrrc x24, 0xd7d, x15	// Clear all CSR bits
	csrrw x24, 0xd7d, x4	// Restore CSR

// Testing CSR 0xd7e
	csrr x15, 0xd7e	// Read CSR
	li x6, -1
	csrrw x3, 0xd7e, x6	// Write all 1s to CSR
	csrrw x3, 0xd7e, x0	// Write all 0s to CSR
	csrrs x3, 0xd7e, x6	// Set all CSR bits
	csrrc x3, 0xd7e, x6	// Clear all CSR bits
	csrrw x3, 0xd7e, x15	// Restore CSR

// Testing CSR 0xd7f
	csrr x23, 0xd7f	// Read CSR
	li x8, -1
	csrrw x28, 0xd7f, x8	// Write all 1s to CSR
	csrrw x28, 0xd7f, x0	// Write all 0s to CSR
	csrrs x28, 0xd7f, x8	// Set all CSR bits
	csrrc x28, 0xd7f, x8	// Clear all CSR bits
	csrrw x28, 0xd7f, x23	// Restore CSR

// Testing CSR 0xd80
	csrr x15, 0xd80	// Read CSR
	li x20, -1
	csrrw x10, 0xd80, x20	// Write all 1s to CSR
	csrrw x10, 0xd80, x0	// Write all 0s to CSR
	csrrs x10, 0xd80, x20	// Set all CSR bits
	csrrc x10, 0xd80, x20	// Clear all CSR bits
	csrrw x10, 0xd80, x15	// Restore CSR

// Testing CSR 0xd81
	csrr x29, 0xd81	// Read CSR
	li x19, -1
	csrrw x11, 0xd81, x19	// Write all 1s to CSR
	csrrw x11, 0xd81, x0	// Write all 0s to CSR
	csrrs x11, 0xd81, x19	// Set all CSR bits
	csrrc x11, 0xd81, x19	// Clear all CSR bits
	csrrw x11, 0xd81, x29	// Restore CSR

// Testing CSR 0xd82
	csrr x23, 0xd82	// Read CSR
	li x28, -1
	csrrw x24, 0xd82, x28	// Write all 1s to CSR
	csrrw x24, 0xd82, x0	// Write all 0s to CSR
	csrrs x24, 0xd82, x28	// Set all CSR bits
	csrrc x24, 0xd82, x28	// Clear all CSR bits
	csrrw x24, 0xd82, x23	// Restore CSR

// Testing CSR 0xd83
	csrr x20, 0xd83	// Read CSR
	li x25, -1
	csrrw x15, 0xd83, x25	// Write all 1s to CSR
	csrrw x15, 0xd83, x0	// Write all 0s to CSR
	csrrs x15, 0xd83, x25	// Set all CSR bits
	csrrc x15, 0xd83, x25	// Clear all CSR bits
	csrrw x15, 0xd83, x20	// Restore CSR

// Testing CSR 0xd84
	csrr x3, 0xd84	// Read CSR
	li x21, -1
	csrrw x19, 0xd84, x21	// Write all 1s to CSR
	csrrw x19, 0xd84, x0	// Write all 0s to CSR
	csrrs x19, 0xd84, x21	// Set all CSR bits
	csrrc x19, 0xd84, x21	// Clear all CSR bits
	csrrw x19, 0xd84, x3	// Restore CSR

// Testing CSR 0xd85
	csrr x14, 0xd85	// Read CSR
	li x24, -1
	csrrw x23, 0xd85, x24	// Write all 1s to CSR
	csrrw x23, 0xd85, x0	// Write all 0s to CSR
	csrrs x23, 0xd85, x24	// Set all CSR bits
	csrrc x23, 0xd85, x24	// Clear all CSR bits
	csrrw x23, 0xd85, x14	// Restore CSR

// Testing CSR 0xd86
	csrr x4, 0xd86	// Read CSR
	li x7, -1
	csrrw x20, 0xd86, x7	// Write all 1s to CSR
	csrrw x20, 0xd86, x0	// Write all 0s to CSR
	csrrs x20, 0xd86, x7	// Set all CSR bits
	csrrc x20, 0xd86, x7	// Clear all CSR bits
	csrrw x20, 0xd86, x4	// Restore CSR

// Testing CSR 0xd87
	csrr x28, 0xd87	// Read CSR
	li x17, -1
	csrrw x1, 0xd87, x17	// Write all 1s to CSR
	csrrw x1, 0xd87, x0	// Write all 0s to CSR
	csrrs x1, 0xd87, x17	// Set all CSR bits
	csrrc x1, 0xd87, x17	// Clear all CSR bits
	csrrw x1, 0xd87, x28	// Restore CSR

// Testing CSR 0xd88
	csrr x12, 0xd88	// Read CSR
	li x14, -1
	csrrw x30, 0xd88, x14	// Write all 1s to CSR
	csrrw x30, 0xd88, x0	// Write all 0s to CSR
	csrrs x30, 0xd88, x14	// Set all CSR bits
	csrrc x30, 0xd88, x14	// Clear all CSR bits
	csrrw x30, 0xd88, x12	// Restore CSR

// Testing CSR 0xd89
	csrr x31, 0xd89	// Read CSR
	li x19, -1
	csrrw x26, 0xd89, x19	// Write all 1s to CSR
	csrrw x26, 0xd89, x0	// Write all 0s to CSR
	csrrs x26, 0xd89, x19	// Set all CSR bits
	csrrc x26, 0xd89, x19	// Clear all CSR bits
	csrrw x26, 0xd89, x31	// Restore CSR

// Testing CSR 0xd8a
	csrr x6, 0xd8a	// Read CSR
	li x2, -1
	csrrw x15, 0xd8a, x2	// Write all 1s to CSR
	csrrw x15, 0xd8a, x0	// Write all 0s to CSR
	csrrs x15, 0xd8a, x2	// Set all CSR bits
	csrrc x15, 0xd8a, x2	// Clear all CSR bits
	csrrw x15, 0xd8a, x6	// Restore CSR

// Testing CSR 0xd8b
	csrr x1, 0xd8b	// Read CSR
	li x29, -1
	csrrw x22, 0xd8b, x29	// Write all 1s to CSR
	csrrw x22, 0xd8b, x0	// Write all 0s to CSR
	csrrs x22, 0xd8b, x29	// Set all CSR bits
	csrrc x22, 0xd8b, x29	// Clear all CSR bits
	csrrw x22, 0xd8b, x1	// Restore CSR

// Testing CSR 0xd8c
	csrr x15, 0xd8c	// Read CSR
	li x30, -1
	csrrw x20, 0xd8c, x30	// Write all 1s to CSR
	csrrw x20, 0xd8c, x0	// Write all 0s to CSR
	csrrs x20, 0xd8c, x30	// Set all CSR bits
	csrrc x20, 0xd8c, x30	// Clear all CSR bits
	csrrw x20, 0xd8c, x15	// Restore CSR

// Testing CSR 0xd8d
	csrr x12, 0xd8d	// Read CSR
	li x14, -1
	csrrw x11, 0xd8d, x14	// Write all 1s to CSR
	csrrw x11, 0xd8d, x0	// Write all 0s to CSR
	csrrs x11, 0xd8d, x14	// Set all CSR bits
	csrrc x11, 0xd8d, x14	// Clear all CSR bits
	csrrw x11, 0xd8d, x12	// Restore CSR

// Testing CSR 0xd8e
	csrr x27, 0xd8e	// Read CSR
	li x3, -1
	csrrw x23, 0xd8e, x3	// Write all 1s to CSR
	csrrw x23, 0xd8e, x0	// Write all 0s to CSR
	csrrs x23, 0xd8e, x3	// Set all CSR bits
	csrrc x23, 0xd8e, x3	// Clear all CSR bits
	csrrw x23, 0xd8e, x27	// Restore CSR

// Testing CSR 0xd8f
	csrr x13, 0xd8f	// Read CSR
	li x9, -1
	csrrw x26, 0xd8f, x9	// Write all 1s to CSR
	csrrw x26, 0xd8f, x0	// Write all 0s to CSR
	csrrs x26, 0xd8f, x9	// Set all CSR bits
	csrrc x26, 0xd8f, x9	// Clear all CSR bits
	csrrw x26, 0xd8f, x13	// Restore CSR

// Testing CSR 0xd90
	csrr x10, 0xd90	// Read CSR
	li x16, -1
	csrrw x19, 0xd90, x16	// Write all 1s to CSR
	csrrw x19, 0xd90, x0	// Write all 0s to CSR
	csrrs x19, 0xd90, x16	// Set all CSR bits
	csrrc x19, 0xd90, x16	// Clear all CSR bits
	csrrw x19, 0xd90, x10	// Restore CSR

// Testing CSR 0xd91
	csrr x24, 0xd91	// Read CSR
	li x11, -1
	csrrw x28, 0xd91, x11	// Write all 1s to CSR
	csrrw x28, 0xd91, x0	// Write all 0s to CSR
	csrrs x28, 0xd91, x11	// Set all CSR bits
	csrrc x28, 0xd91, x11	// Clear all CSR bits
	csrrw x28, 0xd91, x24	// Restore CSR

// Testing CSR 0xd92
	csrr x19, 0xd92	// Read CSR
	li x25, -1
	csrrw x29, 0xd92, x25	// Write all 1s to CSR
	csrrw x29, 0xd92, x0	// Write all 0s to CSR
	csrrs x29, 0xd92, x25	// Set all CSR bits
	csrrc x29, 0xd92, x25	// Clear all CSR bits
	csrrw x29, 0xd92, x19	// Restore CSR

// Testing CSR 0xd93
	csrr x28, 0xd93	// Read CSR
	li x16, -1
	csrrw x17, 0xd93, x16	// Write all 1s to CSR
	csrrw x17, 0xd93, x0	// Write all 0s to CSR
	csrrs x17, 0xd93, x16	// Set all CSR bits
	csrrc x17, 0xd93, x16	// Clear all CSR bits
	csrrw x17, 0xd93, x28	// Restore CSR

// Testing CSR 0xd94
	csrr x24, 0xd94	// Read CSR
	li x12, -1
	csrrw x26, 0xd94, x12	// Write all 1s to CSR
	csrrw x26, 0xd94, x0	// Write all 0s to CSR
	csrrs x26, 0xd94, x12	// Set all CSR bits
	csrrc x26, 0xd94, x12	// Clear all CSR bits
	csrrw x26, 0xd94, x24	// Restore CSR

// Testing CSR 0xd95
	csrr x7, 0xd95	// Read CSR
	li x28, -1
	csrrw x14, 0xd95, x28	// Write all 1s to CSR
	csrrw x14, 0xd95, x0	// Write all 0s to CSR
	csrrs x14, 0xd95, x28	// Set all CSR bits
	csrrc x14, 0xd95, x28	// Clear all CSR bits
	csrrw x14, 0xd95, x7	// Restore CSR

// Testing CSR 0xd96
	csrr x28, 0xd96	// Read CSR
	li x12, -1
	csrrw x27, 0xd96, x12	// Write all 1s to CSR
	csrrw x27, 0xd96, x0	// Write all 0s to CSR
	csrrs x27, 0xd96, x12	// Set all CSR bits
	csrrc x27, 0xd96, x12	// Clear all CSR bits
	csrrw x27, 0xd96, x28	// Restore CSR

// Testing CSR 0xd97
	csrr x9, 0xd97	// Read CSR
	li x28, -1
	csrrw x23, 0xd97, x28	// Write all 1s to CSR
	csrrw x23, 0xd97, x0	// Write all 0s to CSR
	csrrs x23, 0xd97, x28	// Set all CSR bits
	csrrc x23, 0xd97, x28	// Clear all CSR bits
	csrrw x23, 0xd97, x9	// Restore CSR

// Testing CSR 0xd98
	csrr x3, 0xd98	// Read CSR
	li x27, -1
	csrrw x29, 0xd98, x27	// Write all 1s to CSR
	csrrw x29, 0xd98, x0	// Write all 0s to CSR
	csrrs x29, 0xd98, x27	// Set all CSR bits
	csrrc x29, 0xd98, x27	// Clear all CSR bits
	csrrw x29, 0xd98, x3	// Restore CSR

// Testing CSR 0xd99
	csrr x29, 0xd99	// Read CSR
	li x23, -1
	csrrw x11, 0xd99, x23	// Write all 1s to CSR
	csrrw x11, 0xd99, x0	// Write all 0s to CSR
	csrrs x11, 0xd99, x23	// Set all CSR bits
	csrrc x11, 0xd99, x23	// Clear all CSR bits
	csrrw x11, 0xd99, x29	// Restore CSR

// Testing CSR 0xd9a
	csrr x29, 0xd9a	// Read CSR
	li x31, -1
	csrrw x7, 0xd9a, x31	// Write all 1s to CSR
	csrrw x7, 0xd9a, x0	// Write all 0s to CSR
	csrrs x7, 0xd9a, x31	// Set all CSR bits
	csrrc x7, 0xd9a, x31	// Clear all CSR bits
	csrrw x7, 0xd9a, x29	// Restore CSR

// Testing CSR 0xd9b
	csrr x5, 0xd9b	// Read CSR
	li x15, -1
	csrrw x27, 0xd9b, x15	// Write all 1s to CSR
	csrrw x27, 0xd9b, x0	// Write all 0s to CSR
	csrrs x27, 0xd9b, x15	// Set all CSR bits
	csrrc x27, 0xd9b, x15	// Clear all CSR bits
	csrrw x27, 0xd9b, x5	// Restore CSR

// Testing CSR 0xd9c
	csrr x17, 0xd9c	// Read CSR
	li x19, -1
	csrrw x3, 0xd9c, x19	// Write all 1s to CSR
	csrrw x3, 0xd9c, x0	// Write all 0s to CSR
	csrrs x3, 0xd9c, x19	// Set all CSR bits
	csrrc x3, 0xd9c, x19	// Clear all CSR bits
	csrrw x3, 0xd9c, x17	// Restore CSR

// Testing CSR 0xd9d
	csrr x9, 0xd9d	// Read CSR
	li x7, -1
	csrrw x19, 0xd9d, x7	// Write all 1s to CSR
	csrrw x19, 0xd9d, x0	// Write all 0s to CSR
	csrrs x19, 0xd9d, x7	// Set all CSR bits
	csrrc x19, 0xd9d, x7	// Clear all CSR bits
	csrrw x19, 0xd9d, x9	// Restore CSR

// Testing CSR 0xd9e
	csrr x3, 0xd9e	// Read CSR
	li x11, -1
	csrrw x30, 0xd9e, x11	// Write all 1s to CSR
	csrrw x30, 0xd9e, x0	// Write all 0s to CSR
	csrrs x30, 0xd9e, x11	// Set all CSR bits
	csrrc x30, 0xd9e, x11	// Clear all CSR bits
	csrrw x30, 0xd9e, x3	// Restore CSR

// Testing CSR 0xd9f
	csrr x2, 0xd9f	// Read CSR
	li x15, -1
	csrrw x10, 0xd9f, x15	// Write all 1s to CSR
	csrrw x10, 0xd9f, x0	// Write all 0s to CSR
	csrrs x10, 0xd9f, x15	// Set all CSR bits
	csrrc x10, 0xd9f, x15	// Clear all CSR bits
	csrrw x10, 0xd9f, x2	// Restore CSR

// Testing CSR 0xda0
	csrr x21, 0xda0	// Read CSR
	li x31, -1
	csrrw x7, 0xda0, x31	// Write all 1s to CSR
	csrrw x7, 0xda0, x0	// Write all 0s to CSR
	csrrs x7, 0xda0, x31	// Set all CSR bits
	csrrc x7, 0xda0, x31	// Clear all CSR bits
	csrrw x7, 0xda0, x21	// Restore CSR

// Testing CSR 0xda1
	csrr x28, 0xda1	// Read CSR
	li x29, -1
	csrrw x22, 0xda1, x29	// Write all 1s to CSR
	csrrw x22, 0xda1, x0	// Write all 0s to CSR
	csrrs x22, 0xda1, x29	// Set all CSR bits
	csrrc x22, 0xda1, x29	// Clear all CSR bits
	csrrw x22, 0xda1, x28	// Restore CSR

// Testing CSR 0xda2
	csrr x8, 0xda2	// Read CSR
	li x17, -1
	csrrw x18, 0xda2, x17	// Write all 1s to CSR
	csrrw x18, 0xda2, x0	// Write all 0s to CSR
	csrrs x18, 0xda2, x17	// Set all CSR bits
	csrrc x18, 0xda2, x17	// Clear all CSR bits
	csrrw x18, 0xda2, x8	// Restore CSR

// Testing CSR 0xda3
	csrr x25, 0xda3	// Read CSR
	li x31, -1
	csrrw x12, 0xda3, x31	// Write all 1s to CSR
	csrrw x12, 0xda3, x0	// Write all 0s to CSR
	csrrs x12, 0xda3, x31	// Set all CSR bits
	csrrc x12, 0xda3, x31	// Clear all CSR bits
	csrrw x12, 0xda3, x25	// Restore CSR

// Testing CSR 0xda4
	csrr x25, 0xda4	// Read CSR
	li x9, -1
	csrrw x27, 0xda4, x9	// Write all 1s to CSR
	csrrw x27, 0xda4, x0	// Write all 0s to CSR
	csrrs x27, 0xda4, x9	// Set all CSR bits
	csrrc x27, 0xda4, x9	// Clear all CSR bits
	csrrw x27, 0xda4, x25	// Restore CSR

// Testing CSR 0xda5
	csrr x15, 0xda5	// Read CSR
	li x7, -1
	csrrw x16, 0xda5, x7	// Write all 1s to CSR
	csrrw x16, 0xda5, x0	// Write all 0s to CSR
	csrrs x16, 0xda5, x7	// Set all CSR bits
	csrrc x16, 0xda5, x7	// Clear all CSR bits
	csrrw x16, 0xda5, x15	// Restore CSR

// Testing CSR 0xda6
	csrr x4, 0xda6	// Read CSR
	li x14, -1
	csrrw x18, 0xda6, x14	// Write all 1s to CSR
	csrrw x18, 0xda6, x0	// Write all 0s to CSR
	csrrs x18, 0xda6, x14	// Set all CSR bits
	csrrc x18, 0xda6, x14	// Clear all CSR bits
	csrrw x18, 0xda6, x4	// Restore CSR

// Testing CSR 0xda7
	csrr x11, 0xda7	// Read CSR
	li x28, -1
	csrrw x23, 0xda7, x28	// Write all 1s to CSR
	csrrw x23, 0xda7, x0	// Write all 0s to CSR
	csrrs x23, 0xda7, x28	// Set all CSR bits
	csrrc x23, 0xda7, x28	// Clear all CSR bits
	csrrw x23, 0xda7, x11	// Restore CSR

// Testing CSR 0xda8
	csrr x29, 0xda8	// Read CSR
	li x26, -1
	csrrw x24, 0xda8, x26	// Write all 1s to CSR
	csrrw x24, 0xda8, x0	// Write all 0s to CSR
	csrrs x24, 0xda8, x26	// Set all CSR bits
	csrrc x24, 0xda8, x26	// Clear all CSR bits
	csrrw x24, 0xda8, x29	// Restore CSR

// Testing CSR 0xda9
	csrr x30, 0xda9	// Read CSR
	li x10, -1
	csrrw x6, 0xda9, x10	// Write all 1s to CSR
	csrrw x6, 0xda9, x0	// Write all 0s to CSR
	csrrs x6, 0xda9, x10	// Set all CSR bits
	csrrc x6, 0xda9, x10	// Clear all CSR bits
	csrrw x6, 0xda9, x30	// Restore CSR

// Testing CSR 0xdaa
	csrr x17, 0xdaa	// Read CSR
	li x5, -1
	csrrw x9, 0xdaa, x5	// Write all 1s to CSR
	csrrw x9, 0xdaa, x0	// Write all 0s to CSR
	csrrs x9, 0xdaa, x5	// Set all CSR bits
	csrrc x9, 0xdaa, x5	// Clear all CSR bits
	csrrw x9, 0xdaa, x17	// Restore CSR

// Testing CSR 0xdab
	csrr x9, 0xdab	// Read CSR
	li x26, -1
	csrrw x6, 0xdab, x26	// Write all 1s to CSR
	csrrw x6, 0xdab, x0	// Write all 0s to CSR
	csrrs x6, 0xdab, x26	// Set all CSR bits
	csrrc x6, 0xdab, x26	// Clear all CSR bits
	csrrw x6, 0xdab, x9	// Restore CSR

// Testing CSR 0xdac
	csrr x31, 0xdac	// Read CSR
	li x3, -1
	csrrw x18, 0xdac, x3	// Write all 1s to CSR
	csrrw x18, 0xdac, x0	// Write all 0s to CSR
	csrrs x18, 0xdac, x3	// Set all CSR bits
	csrrc x18, 0xdac, x3	// Clear all CSR bits
	csrrw x18, 0xdac, x31	// Restore CSR

// Testing CSR 0xdad
	csrr x9, 0xdad	// Read CSR
	li x3, -1
	csrrw x31, 0xdad, x3	// Write all 1s to CSR
	csrrw x31, 0xdad, x0	// Write all 0s to CSR
	csrrs x31, 0xdad, x3	// Set all CSR bits
	csrrc x31, 0xdad, x3	// Clear all CSR bits
	csrrw x31, 0xdad, x9	// Restore CSR

// Testing CSR 0xdae
	csrr x14, 0xdae	// Read CSR
	li x31, -1
	csrrw x20, 0xdae, x31	// Write all 1s to CSR
	csrrw x20, 0xdae, x0	// Write all 0s to CSR
	csrrs x20, 0xdae, x31	// Set all CSR bits
	csrrc x20, 0xdae, x31	// Clear all CSR bits
	csrrw x20, 0xdae, x14	// Restore CSR

// Testing CSR 0xdaf
	csrr x12, 0xdaf	// Read CSR
	li x31, -1
	csrrw x8, 0xdaf, x31	// Write all 1s to CSR
	csrrw x8, 0xdaf, x0	// Write all 0s to CSR
	csrrs x8, 0xdaf, x31	// Set all CSR bits
	csrrc x8, 0xdaf, x31	// Clear all CSR bits
	csrrw x8, 0xdaf, x12	// Restore CSR

// Testing CSR 0xdb0
	csrr x3, 0xdb0	// Read CSR
	li x8, -1
	csrrw x2, 0xdb0, x8	// Write all 1s to CSR
	csrrw x2, 0xdb0, x0	// Write all 0s to CSR
	csrrs x2, 0xdb0, x8	// Set all CSR bits
	csrrc x2, 0xdb0, x8	// Clear all CSR bits
	csrrw x2, 0xdb0, x3	// Restore CSR

// Testing CSR 0xdb1
	csrr x3, 0xdb1	// Read CSR
	li x25, -1
	csrrw x31, 0xdb1, x25	// Write all 1s to CSR
	csrrw x31, 0xdb1, x0	// Write all 0s to CSR
	csrrs x31, 0xdb1, x25	// Set all CSR bits
	csrrc x31, 0xdb1, x25	// Clear all CSR bits
	csrrw x31, 0xdb1, x3	// Restore CSR

// Testing CSR 0xdb2
	csrr x20, 0xdb2	// Read CSR
	li x2, -1
	csrrw x25, 0xdb2, x2	// Write all 1s to CSR
	csrrw x25, 0xdb2, x0	// Write all 0s to CSR
	csrrs x25, 0xdb2, x2	// Set all CSR bits
	csrrc x25, 0xdb2, x2	// Clear all CSR bits
	csrrw x25, 0xdb2, x20	// Restore CSR

// Testing CSR 0xdb3
	csrr x26, 0xdb3	// Read CSR
	li x30, -1
	csrrw x12, 0xdb3, x30	// Write all 1s to CSR
	csrrw x12, 0xdb3, x0	// Write all 0s to CSR
	csrrs x12, 0xdb3, x30	// Set all CSR bits
	csrrc x12, 0xdb3, x30	// Clear all CSR bits
	csrrw x12, 0xdb3, x26	// Restore CSR

// Testing CSR 0xdb4
	csrr x15, 0xdb4	// Read CSR
	li x11, -1
	csrrw x7, 0xdb4, x11	// Write all 1s to CSR
	csrrw x7, 0xdb4, x0	// Write all 0s to CSR
	csrrs x7, 0xdb4, x11	// Set all CSR bits
	csrrc x7, 0xdb4, x11	// Clear all CSR bits
	csrrw x7, 0xdb4, x15	// Restore CSR

// Testing CSR 0xdb5
	csrr x19, 0xdb5	// Read CSR
	li x24, -1
	csrrw x4, 0xdb5, x24	// Write all 1s to CSR
	csrrw x4, 0xdb5, x0	// Write all 0s to CSR
	csrrs x4, 0xdb5, x24	// Set all CSR bits
	csrrc x4, 0xdb5, x24	// Clear all CSR bits
	csrrw x4, 0xdb5, x19	// Restore CSR

// Testing CSR 0xdb6
	csrr x12, 0xdb6	// Read CSR
	li x23, -1
	csrrw x14, 0xdb6, x23	// Write all 1s to CSR
	csrrw x14, 0xdb6, x0	// Write all 0s to CSR
	csrrs x14, 0xdb6, x23	// Set all CSR bits
	csrrc x14, 0xdb6, x23	// Clear all CSR bits
	csrrw x14, 0xdb6, x12	// Restore CSR

// Testing CSR 0xdb7
	csrr x24, 0xdb7	// Read CSR
	li x22, -1
	csrrw x1, 0xdb7, x22	// Write all 1s to CSR
	csrrw x1, 0xdb7, x0	// Write all 0s to CSR
	csrrs x1, 0xdb7, x22	// Set all CSR bits
	csrrc x1, 0xdb7, x22	// Clear all CSR bits
	csrrw x1, 0xdb7, x24	// Restore CSR

// Testing CSR 0xdb8
	csrr x30, 0xdb8	// Read CSR
	li x15, -1
	csrrw x6, 0xdb8, x15	// Write all 1s to CSR
	csrrw x6, 0xdb8, x0	// Write all 0s to CSR
	csrrs x6, 0xdb8, x15	// Set all CSR bits
	csrrc x6, 0xdb8, x15	// Clear all CSR bits
	csrrw x6, 0xdb8, x30	// Restore CSR

// Testing CSR 0xdb9
	csrr x28, 0xdb9	// Read CSR
	li x22, -1
	csrrw x25, 0xdb9, x22	// Write all 1s to CSR
	csrrw x25, 0xdb9, x0	// Write all 0s to CSR
	csrrs x25, 0xdb9, x22	// Set all CSR bits
	csrrc x25, 0xdb9, x22	// Clear all CSR bits
	csrrw x25, 0xdb9, x28	// Restore CSR

// Testing CSR 0xdba
	csrr x24, 0xdba	// Read CSR
	li x20, -1
	csrrw x15, 0xdba, x20	// Write all 1s to CSR
	csrrw x15, 0xdba, x0	// Write all 0s to CSR
	csrrs x15, 0xdba, x20	// Set all CSR bits
	csrrc x15, 0xdba, x20	// Clear all CSR bits
	csrrw x15, 0xdba, x24	// Restore CSR

// Testing CSR 0xdbb
	csrr x30, 0xdbb	// Read CSR
	li x25, -1
	csrrw x15, 0xdbb, x25	// Write all 1s to CSR
	csrrw x15, 0xdbb, x0	// Write all 0s to CSR
	csrrs x15, 0xdbb, x25	// Set all CSR bits
	csrrc x15, 0xdbb, x25	// Clear all CSR bits
	csrrw x15, 0xdbb, x30	// Restore CSR

// Testing CSR 0xdbc
	csrr x14, 0xdbc	// Read CSR
	li x25, -1
	csrrw x12, 0xdbc, x25	// Write all 1s to CSR
	csrrw x12, 0xdbc, x0	// Write all 0s to CSR
	csrrs x12, 0xdbc, x25	// Set all CSR bits
	csrrc x12, 0xdbc, x25	// Clear all CSR bits
	csrrw x12, 0xdbc, x14	// Restore CSR

// Testing CSR 0xdbd
	csrr x30, 0xdbd	// Read CSR
	li x27, -1
	csrrw x14, 0xdbd, x27	// Write all 1s to CSR
	csrrw x14, 0xdbd, x0	// Write all 0s to CSR
	csrrs x14, 0xdbd, x27	// Set all CSR bits
	csrrc x14, 0xdbd, x27	// Clear all CSR bits
	csrrw x14, 0xdbd, x30	// Restore CSR

// Testing CSR 0xdbe
	csrr x5, 0xdbe	// Read CSR
	li x24, -1
	csrrw x6, 0xdbe, x24	// Write all 1s to CSR
	csrrw x6, 0xdbe, x0	// Write all 0s to CSR
	csrrs x6, 0xdbe, x24	// Set all CSR bits
	csrrc x6, 0xdbe, x24	// Clear all CSR bits
	csrrw x6, 0xdbe, x5	// Restore CSR

// Testing CSR 0xdbf
	csrr x12, 0xdbf	// Read CSR
	li x14, -1
	csrrw x2, 0xdbf, x14	// Write all 1s to CSR
	csrrw x2, 0xdbf, x0	// Write all 0s to CSR
	csrrs x2, 0xdbf, x14	// Set all CSR bits
	csrrc x2, 0xdbf, x14	// Clear all CSR bits
	csrrw x2, 0xdbf, x12	// Restore CSR

// Testing CSR 0xdc0
	csrr x22, 0xdc0	// Read CSR
	li x8, -1
	csrrw x26, 0xdc0, x8	// Write all 1s to CSR
	csrrw x26, 0xdc0, x0	// Write all 0s to CSR
	csrrs x26, 0xdc0, x8	// Set all CSR bits
	csrrc x26, 0xdc0, x8	// Clear all CSR bits
	csrrw x26, 0xdc0, x22	// Restore CSR

// Testing CSR 0xdc1
	csrr x2, 0xdc1	// Read CSR
	li x7, -1
	csrrw x21, 0xdc1, x7	// Write all 1s to CSR
	csrrw x21, 0xdc1, x0	// Write all 0s to CSR
	csrrs x21, 0xdc1, x7	// Set all CSR bits
	csrrc x21, 0xdc1, x7	// Clear all CSR bits
	csrrw x21, 0xdc1, x2	// Restore CSR

// Testing CSR 0xdc2
	csrr x1, 0xdc2	// Read CSR
	li x5, -1
	csrrw x24, 0xdc2, x5	// Write all 1s to CSR
	csrrw x24, 0xdc2, x0	// Write all 0s to CSR
	csrrs x24, 0xdc2, x5	// Set all CSR bits
	csrrc x24, 0xdc2, x5	// Clear all CSR bits
	csrrw x24, 0xdc2, x1	// Restore CSR

// Testing CSR 0xdc3
	csrr x5, 0xdc3	// Read CSR
	li x29, -1
	csrrw x7, 0xdc3, x29	// Write all 1s to CSR
	csrrw x7, 0xdc3, x0	// Write all 0s to CSR
	csrrs x7, 0xdc3, x29	// Set all CSR bits
	csrrc x7, 0xdc3, x29	// Clear all CSR bits
	csrrw x7, 0xdc3, x5	// Restore CSR

// Testing CSR 0xdc4
	csrr x4, 0xdc4	// Read CSR
	li x20, -1
	csrrw x31, 0xdc4, x20	// Write all 1s to CSR
	csrrw x31, 0xdc4, x0	// Write all 0s to CSR
	csrrs x31, 0xdc4, x20	// Set all CSR bits
	csrrc x31, 0xdc4, x20	// Clear all CSR bits
	csrrw x31, 0xdc4, x4	// Restore CSR

// Testing CSR 0xdc5
	csrr x23, 0xdc5	// Read CSR
	li x31, -1
	csrrw x7, 0xdc5, x31	// Write all 1s to CSR
	csrrw x7, 0xdc5, x0	// Write all 0s to CSR
	csrrs x7, 0xdc5, x31	// Set all CSR bits
	csrrc x7, 0xdc5, x31	// Clear all CSR bits
	csrrw x7, 0xdc5, x23	// Restore CSR

// Testing CSR 0xdc6
	csrr x18, 0xdc6	// Read CSR
	li x16, -1
	csrrw x25, 0xdc6, x16	// Write all 1s to CSR
	csrrw x25, 0xdc6, x0	// Write all 0s to CSR
	csrrs x25, 0xdc6, x16	// Set all CSR bits
	csrrc x25, 0xdc6, x16	// Clear all CSR bits
	csrrw x25, 0xdc6, x18	// Restore CSR

// Testing CSR 0xdc7
	csrr x25, 0xdc7	// Read CSR
	li x7, -1
	csrrw x8, 0xdc7, x7	// Write all 1s to CSR
	csrrw x8, 0xdc7, x0	// Write all 0s to CSR
	csrrs x8, 0xdc7, x7	// Set all CSR bits
	csrrc x8, 0xdc7, x7	// Clear all CSR bits
	csrrw x8, 0xdc7, x25	// Restore CSR

// Testing CSR 0xdc8
	csrr x20, 0xdc8	// Read CSR
	li x28, -1
	csrrw x21, 0xdc8, x28	// Write all 1s to CSR
	csrrw x21, 0xdc8, x0	// Write all 0s to CSR
	csrrs x21, 0xdc8, x28	// Set all CSR bits
	csrrc x21, 0xdc8, x28	// Clear all CSR bits
	csrrw x21, 0xdc8, x20	// Restore CSR

// Testing CSR 0xdc9
	csrr x20, 0xdc9	// Read CSR
	li x29, -1
	csrrw x3, 0xdc9, x29	// Write all 1s to CSR
	csrrw x3, 0xdc9, x0	// Write all 0s to CSR
	csrrs x3, 0xdc9, x29	// Set all CSR bits
	csrrc x3, 0xdc9, x29	// Clear all CSR bits
	csrrw x3, 0xdc9, x20	// Restore CSR

// Testing CSR 0xdca
	csrr x8, 0xdca	// Read CSR
	li x23, -1
	csrrw x4, 0xdca, x23	// Write all 1s to CSR
	csrrw x4, 0xdca, x0	// Write all 0s to CSR
	csrrs x4, 0xdca, x23	// Set all CSR bits
	csrrc x4, 0xdca, x23	// Clear all CSR bits
	csrrw x4, 0xdca, x8	// Restore CSR

// Testing CSR 0xdcb
	csrr x6, 0xdcb	// Read CSR
	li x26, -1
	csrrw x4, 0xdcb, x26	// Write all 1s to CSR
	csrrw x4, 0xdcb, x0	// Write all 0s to CSR
	csrrs x4, 0xdcb, x26	// Set all CSR bits
	csrrc x4, 0xdcb, x26	// Clear all CSR bits
	csrrw x4, 0xdcb, x6	// Restore CSR

// Testing CSR 0xdcc
	csrr x27, 0xdcc	// Read CSR
	li x15, -1
	csrrw x24, 0xdcc, x15	// Write all 1s to CSR
	csrrw x24, 0xdcc, x0	// Write all 0s to CSR
	csrrs x24, 0xdcc, x15	// Set all CSR bits
	csrrc x24, 0xdcc, x15	// Clear all CSR bits
	csrrw x24, 0xdcc, x27	// Restore CSR

// Testing CSR 0xdcd
	csrr x29, 0xdcd	// Read CSR
	li x2, -1
	csrrw x21, 0xdcd, x2	// Write all 1s to CSR
	csrrw x21, 0xdcd, x0	// Write all 0s to CSR
	csrrs x21, 0xdcd, x2	// Set all CSR bits
	csrrc x21, 0xdcd, x2	// Clear all CSR bits
	csrrw x21, 0xdcd, x29	// Restore CSR

// Testing CSR 0xdce
	csrr x3, 0xdce	// Read CSR
	li x12, -1
	csrrw x6, 0xdce, x12	// Write all 1s to CSR
	csrrw x6, 0xdce, x0	// Write all 0s to CSR
	csrrs x6, 0xdce, x12	// Set all CSR bits
	csrrc x6, 0xdce, x12	// Clear all CSR bits
	csrrw x6, 0xdce, x3	// Restore CSR

// Testing CSR 0xdcf
	csrr x25, 0xdcf	// Read CSR
	li x9, -1
	csrrw x20, 0xdcf, x9	// Write all 1s to CSR
	csrrw x20, 0xdcf, x0	// Write all 0s to CSR
	csrrs x20, 0xdcf, x9	// Set all CSR bits
	csrrc x20, 0xdcf, x9	// Clear all CSR bits
	csrrw x20, 0xdcf, x25	// Restore CSR

// Testing CSR 0xdd0
	csrr x14, 0xdd0	// Read CSR
	li x18, -1
	csrrw x22, 0xdd0, x18	// Write all 1s to CSR
	csrrw x22, 0xdd0, x0	// Write all 0s to CSR
	csrrs x22, 0xdd0, x18	// Set all CSR bits
	csrrc x22, 0xdd0, x18	// Clear all CSR bits
	csrrw x22, 0xdd0, x14	// Restore CSR

// Testing CSR 0xdd1
	csrr x4, 0xdd1	// Read CSR
	li x26, -1
	csrrw x24, 0xdd1, x26	// Write all 1s to CSR
	csrrw x24, 0xdd1, x0	// Write all 0s to CSR
	csrrs x24, 0xdd1, x26	// Set all CSR bits
	csrrc x24, 0xdd1, x26	// Clear all CSR bits
	csrrw x24, 0xdd1, x4	// Restore CSR

// Testing CSR 0xdd2
	csrr x27, 0xdd2	// Read CSR
	li x31, -1
	csrrw x22, 0xdd2, x31	// Write all 1s to CSR
	csrrw x22, 0xdd2, x0	// Write all 0s to CSR
	csrrs x22, 0xdd2, x31	// Set all CSR bits
	csrrc x22, 0xdd2, x31	// Clear all CSR bits
	csrrw x22, 0xdd2, x27	// Restore CSR

// Testing CSR 0xdd3
	csrr x13, 0xdd3	// Read CSR
	li x22, -1
	csrrw x24, 0xdd3, x22	// Write all 1s to CSR
	csrrw x24, 0xdd3, x0	// Write all 0s to CSR
	csrrs x24, 0xdd3, x22	// Set all CSR bits
	csrrc x24, 0xdd3, x22	// Clear all CSR bits
	csrrw x24, 0xdd3, x13	// Restore CSR

// Testing CSR 0xdd4
	csrr x25, 0xdd4	// Read CSR
	li x12, -1
	csrrw x4, 0xdd4, x12	// Write all 1s to CSR
	csrrw x4, 0xdd4, x0	// Write all 0s to CSR
	csrrs x4, 0xdd4, x12	// Set all CSR bits
	csrrc x4, 0xdd4, x12	// Clear all CSR bits
	csrrw x4, 0xdd4, x25	// Restore CSR

// Testing CSR 0xdd5
	csrr x27, 0xdd5	// Read CSR
	li x31, -1
	csrrw x29, 0xdd5, x31	// Write all 1s to CSR
	csrrw x29, 0xdd5, x0	// Write all 0s to CSR
	csrrs x29, 0xdd5, x31	// Set all CSR bits
	csrrc x29, 0xdd5, x31	// Clear all CSR bits
	csrrw x29, 0xdd5, x27	// Restore CSR

// Testing CSR 0xdd6
	csrr x22, 0xdd6	// Read CSR
	li x4, -1
	csrrw x11, 0xdd6, x4	// Write all 1s to CSR
	csrrw x11, 0xdd6, x0	// Write all 0s to CSR
	csrrs x11, 0xdd6, x4	// Set all CSR bits
	csrrc x11, 0xdd6, x4	// Clear all CSR bits
	csrrw x11, 0xdd6, x22	// Restore CSR

// Testing CSR 0xdd7
	csrr x29, 0xdd7	// Read CSR
	li x12, -1
	csrrw x15, 0xdd7, x12	// Write all 1s to CSR
	csrrw x15, 0xdd7, x0	// Write all 0s to CSR
	csrrs x15, 0xdd7, x12	// Set all CSR bits
	csrrc x15, 0xdd7, x12	// Clear all CSR bits
	csrrw x15, 0xdd7, x29	// Restore CSR

// Testing CSR 0xdd8
	csrr x14, 0xdd8	// Read CSR
	li x10, -1
	csrrw x6, 0xdd8, x10	// Write all 1s to CSR
	csrrw x6, 0xdd8, x0	// Write all 0s to CSR
	csrrs x6, 0xdd8, x10	// Set all CSR bits
	csrrc x6, 0xdd8, x10	// Clear all CSR bits
	csrrw x6, 0xdd8, x14	// Restore CSR

// Testing CSR 0xdd9
	csrr x27, 0xdd9	// Read CSR
	li x21, -1
	csrrw x22, 0xdd9, x21	// Write all 1s to CSR
	csrrw x22, 0xdd9, x0	// Write all 0s to CSR
	csrrs x22, 0xdd9, x21	// Set all CSR bits
	csrrc x22, 0xdd9, x21	// Clear all CSR bits
	csrrw x22, 0xdd9, x27	// Restore CSR

// Testing CSR 0xdda
	csrr x9, 0xdda	// Read CSR
	li x27, -1
	csrrw x26, 0xdda, x27	// Write all 1s to CSR
	csrrw x26, 0xdda, x0	// Write all 0s to CSR
	csrrs x26, 0xdda, x27	// Set all CSR bits
	csrrc x26, 0xdda, x27	// Clear all CSR bits
	csrrw x26, 0xdda, x9	// Restore CSR

// Testing CSR 0xddb
	csrr x20, 0xddb	// Read CSR
	li x2, -1
	csrrw x30, 0xddb, x2	// Write all 1s to CSR
	csrrw x30, 0xddb, x0	// Write all 0s to CSR
	csrrs x30, 0xddb, x2	// Set all CSR bits
	csrrc x30, 0xddb, x2	// Clear all CSR bits
	csrrw x30, 0xddb, x20	// Restore CSR

// Testing CSR 0xddc
	csrr x15, 0xddc	// Read CSR
	li x17, -1
	csrrw x2, 0xddc, x17	// Write all 1s to CSR
	csrrw x2, 0xddc, x0	// Write all 0s to CSR
	csrrs x2, 0xddc, x17	// Set all CSR bits
	csrrc x2, 0xddc, x17	// Clear all CSR bits
	csrrw x2, 0xddc, x15	// Restore CSR

// Testing CSR 0xddd
	csrr x8, 0xddd	// Read CSR
	li x3, -1
	csrrw x4, 0xddd, x3	// Write all 1s to CSR
	csrrw x4, 0xddd, x0	// Write all 0s to CSR
	csrrs x4, 0xddd, x3	// Set all CSR bits
	csrrc x4, 0xddd, x3	// Clear all CSR bits
	csrrw x4, 0xddd, x8	// Restore CSR

// Testing CSR 0xdde
	csrr x28, 0xdde	// Read CSR
	li x16, -1
	csrrw x25, 0xdde, x16	// Write all 1s to CSR
	csrrw x25, 0xdde, x0	// Write all 0s to CSR
	csrrs x25, 0xdde, x16	// Set all CSR bits
	csrrc x25, 0xdde, x16	// Clear all CSR bits
	csrrw x25, 0xdde, x28	// Restore CSR

// Testing CSR 0xddf
	csrr x25, 0xddf	// Read CSR
	li x20, -1
	csrrw x21, 0xddf, x20	// Write all 1s to CSR
	csrrw x21, 0xddf, x0	// Write all 0s to CSR
	csrrs x21, 0xddf, x20	// Set all CSR bits
	csrrc x21, 0xddf, x20	// Clear all CSR bits
	csrrw x21, 0xddf, x25	// Restore CSR

// Testing CSR 0xde0
	csrr x26, 0xde0	// Read CSR
	li x11, -1
	csrrw x16, 0xde0, x11	// Write all 1s to CSR
	csrrw x16, 0xde0, x0	// Write all 0s to CSR
	csrrs x16, 0xde0, x11	// Set all CSR bits
	csrrc x16, 0xde0, x11	// Clear all CSR bits
	csrrw x16, 0xde0, x26	// Restore CSR

// Testing CSR 0xde1
	csrr x23, 0xde1	// Read CSR
	li x16, -1
	csrrw x13, 0xde1, x16	// Write all 1s to CSR
	csrrw x13, 0xde1, x0	// Write all 0s to CSR
	csrrs x13, 0xde1, x16	// Set all CSR bits
	csrrc x13, 0xde1, x16	// Clear all CSR bits
	csrrw x13, 0xde1, x23	// Restore CSR

// Testing CSR 0xde2
	csrr x30, 0xde2	// Read CSR
	li x17, -1
	csrrw x5, 0xde2, x17	// Write all 1s to CSR
	csrrw x5, 0xde2, x0	// Write all 0s to CSR
	csrrs x5, 0xde2, x17	// Set all CSR bits
	csrrc x5, 0xde2, x17	// Clear all CSR bits
	csrrw x5, 0xde2, x30	// Restore CSR

// Testing CSR 0xde3
	csrr x8, 0xde3	// Read CSR
	li x26, -1
	csrrw x15, 0xde3, x26	// Write all 1s to CSR
	csrrw x15, 0xde3, x0	// Write all 0s to CSR
	csrrs x15, 0xde3, x26	// Set all CSR bits
	csrrc x15, 0xde3, x26	// Clear all CSR bits
	csrrw x15, 0xde3, x8	// Restore CSR

// Testing CSR 0xde4
	csrr x28, 0xde4	// Read CSR
	li x13, -1
	csrrw x20, 0xde4, x13	// Write all 1s to CSR
	csrrw x20, 0xde4, x0	// Write all 0s to CSR
	csrrs x20, 0xde4, x13	// Set all CSR bits
	csrrc x20, 0xde4, x13	// Clear all CSR bits
	csrrw x20, 0xde4, x28	// Restore CSR

// Testing CSR 0xde5
	csrr x10, 0xde5	// Read CSR
	li x24, -1
	csrrw x23, 0xde5, x24	// Write all 1s to CSR
	csrrw x23, 0xde5, x0	// Write all 0s to CSR
	csrrs x23, 0xde5, x24	// Set all CSR bits
	csrrc x23, 0xde5, x24	// Clear all CSR bits
	csrrw x23, 0xde5, x10	// Restore CSR

// Testing CSR 0xde6
	csrr x28, 0xde6	// Read CSR
	li x22, -1
	csrrw x19, 0xde6, x22	// Write all 1s to CSR
	csrrw x19, 0xde6, x0	// Write all 0s to CSR
	csrrs x19, 0xde6, x22	// Set all CSR bits
	csrrc x19, 0xde6, x22	// Clear all CSR bits
	csrrw x19, 0xde6, x28	// Restore CSR

// Testing CSR 0xde7
	csrr x12, 0xde7	// Read CSR
	li x6, -1
	csrrw x5, 0xde7, x6	// Write all 1s to CSR
	csrrw x5, 0xde7, x0	// Write all 0s to CSR
	csrrs x5, 0xde7, x6	// Set all CSR bits
	csrrc x5, 0xde7, x6	// Clear all CSR bits
	csrrw x5, 0xde7, x12	// Restore CSR

// Testing CSR 0xde8
	csrr x13, 0xde8	// Read CSR
	li x29, -1
	csrrw x8, 0xde8, x29	// Write all 1s to CSR
	csrrw x8, 0xde8, x0	// Write all 0s to CSR
	csrrs x8, 0xde8, x29	// Set all CSR bits
	csrrc x8, 0xde8, x29	// Clear all CSR bits
	csrrw x8, 0xde8, x13	// Restore CSR

// Testing CSR 0xde9
	csrr x2, 0xde9	// Read CSR
	li x28, -1
	csrrw x6, 0xde9, x28	// Write all 1s to CSR
	csrrw x6, 0xde9, x0	// Write all 0s to CSR
	csrrs x6, 0xde9, x28	// Set all CSR bits
	csrrc x6, 0xde9, x28	// Clear all CSR bits
	csrrw x6, 0xde9, x2	// Restore CSR

// Testing CSR 0xdea
	csrr x19, 0xdea	// Read CSR
	li x22, -1
	csrrw x9, 0xdea, x22	// Write all 1s to CSR
	csrrw x9, 0xdea, x0	// Write all 0s to CSR
	csrrs x9, 0xdea, x22	// Set all CSR bits
	csrrc x9, 0xdea, x22	// Clear all CSR bits
	csrrw x9, 0xdea, x19	// Restore CSR

// Testing CSR 0xdeb
	csrr x8, 0xdeb	// Read CSR
	li x14, -1
	csrrw x28, 0xdeb, x14	// Write all 1s to CSR
	csrrw x28, 0xdeb, x0	// Write all 0s to CSR
	csrrs x28, 0xdeb, x14	// Set all CSR bits
	csrrc x28, 0xdeb, x14	// Clear all CSR bits
	csrrw x28, 0xdeb, x8	// Restore CSR

// Testing CSR 0xdec
	csrr x13, 0xdec	// Read CSR
	li x5, -1
	csrrw x2, 0xdec, x5	// Write all 1s to CSR
	csrrw x2, 0xdec, x0	// Write all 0s to CSR
	csrrs x2, 0xdec, x5	// Set all CSR bits
	csrrc x2, 0xdec, x5	// Clear all CSR bits
	csrrw x2, 0xdec, x13	// Restore CSR

// Testing CSR 0xded
	csrr x11, 0xded	// Read CSR
	li x6, -1
	csrrw x25, 0xded, x6	// Write all 1s to CSR
	csrrw x25, 0xded, x0	// Write all 0s to CSR
	csrrs x25, 0xded, x6	// Set all CSR bits
	csrrc x25, 0xded, x6	// Clear all CSR bits
	csrrw x25, 0xded, x11	// Restore CSR

// Testing CSR 0xdee
	csrr x14, 0xdee	// Read CSR
	li x18, -1
	csrrw x21, 0xdee, x18	// Write all 1s to CSR
	csrrw x21, 0xdee, x0	// Write all 0s to CSR
	csrrs x21, 0xdee, x18	// Set all CSR bits
	csrrc x21, 0xdee, x18	// Clear all CSR bits
	csrrw x21, 0xdee, x14	// Restore CSR

// Testing CSR 0xdef
	csrr x7, 0xdef	// Read CSR
	li x12, -1
	csrrw x9, 0xdef, x12	// Write all 1s to CSR
	csrrw x9, 0xdef, x0	// Write all 0s to CSR
	csrrs x9, 0xdef, x12	// Set all CSR bits
	csrrc x9, 0xdef, x12	// Clear all CSR bits
	csrrw x9, 0xdef, x7	// Restore CSR

// Testing CSR 0xdf0
	csrr x6, 0xdf0	// Read CSR
	li x9, -1
	csrrw x4, 0xdf0, x9	// Write all 1s to CSR
	csrrw x4, 0xdf0, x0	// Write all 0s to CSR
	csrrs x4, 0xdf0, x9	// Set all CSR bits
	csrrc x4, 0xdf0, x9	// Clear all CSR bits
	csrrw x4, 0xdf0, x6	// Restore CSR

// Testing CSR 0xdf1
	csrr x2, 0xdf1	// Read CSR
	li x19, -1
	csrrw x6, 0xdf1, x19	// Write all 1s to CSR
	csrrw x6, 0xdf1, x0	// Write all 0s to CSR
	csrrs x6, 0xdf1, x19	// Set all CSR bits
	csrrc x6, 0xdf1, x19	// Clear all CSR bits
	csrrw x6, 0xdf1, x2	// Restore CSR

// Testing CSR 0xdf2
	csrr x20, 0xdf2	// Read CSR
	li x25, -1
	csrrw x4, 0xdf2, x25	// Write all 1s to CSR
	csrrw x4, 0xdf2, x0	// Write all 0s to CSR
	csrrs x4, 0xdf2, x25	// Set all CSR bits
	csrrc x4, 0xdf2, x25	// Clear all CSR bits
	csrrw x4, 0xdf2, x20	// Restore CSR

// Testing CSR 0xdf3
	csrr x27, 0xdf3	// Read CSR
	li x6, -1
	csrrw x12, 0xdf3, x6	// Write all 1s to CSR
	csrrw x12, 0xdf3, x0	// Write all 0s to CSR
	csrrs x12, 0xdf3, x6	// Set all CSR bits
	csrrc x12, 0xdf3, x6	// Clear all CSR bits
	csrrw x12, 0xdf3, x27	// Restore CSR

// Testing CSR 0xdf4
	csrr x11, 0xdf4	// Read CSR
	li x12, -1
	csrrw x3, 0xdf4, x12	// Write all 1s to CSR
	csrrw x3, 0xdf4, x0	// Write all 0s to CSR
	csrrs x3, 0xdf4, x12	// Set all CSR bits
	csrrc x3, 0xdf4, x12	// Clear all CSR bits
	csrrw x3, 0xdf4, x11	// Restore CSR

// Testing CSR 0xdf5
	csrr x15, 0xdf5	// Read CSR
	li x1, -1
	csrrw x5, 0xdf5, x1	// Write all 1s to CSR
	csrrw x5, 0xdf5, x0	// Write all 0s to CSR
	csrrs x5, 0xdf5, x1	// Set all CSR bits
	csrrc x5, 0xdf5, x1	// Clear all CSR bits
	csrrw x5, 0xdf5, x15	// Restore CSR

// Testing CSR 0xdf6
	csrr x20, 0xdf6	// Read CSR
	li x19, -1
	csrrw x16, 0xdf6, x19	// Write all 1s to CSR
	csrrw x16, 0xdf6, x0	// Write all 0s to CSR
	csrrs x16, 0xdf6, x19	// Set all CSR bits
	csrrc x16, 0xdf6, x19	// Clear all CSR bits
	csrrw x16, 0xdf6, x20	// Restore CSR

// Testing CSR 0xdf7
	csrr x11, 0xdf7	// Read CSR
	li x24, -1
	csrrw x18, 0xdf7, x24	// Write all 1s to CSR
	csrrw x18, 0xdf7, x0	// Write all 0s to CSR
	csrrs x18, 0xdf7, x24	// Set all CSR bits
	csrrc x18, 0xdf7, x24	// Clear all CSR bits
	csrrw x18, 0xdf7, x11	// Restore CSR

// Testing CSR 0xdf8
	csrr x24, 0xdf8	// Read CSR
	li x8, -1
	csrrw x28, 0xdf8, x8	// Write all 1s to CSR
	csrrw x28, 0xdf8, x0	// Write all 0s to CSR
	csrrs x28, 0xdf8, x8	// Set all CSR bits
	csrrc x28, 0xdf8, x8	// Clear all CSR bits
	csrrw x28, 0xdf8, x24	// Restore CSR

// Testing CSR 0xdf9
	csrr x14, 0xdf9	// Read CSR
	li x6, -1
	csrrw x16, 0xdf9, x6	// Write all 1s to CSR
	csrrw x16, 0xdf9, x0	// Write all 0s to CSR
	csrrs x16, 0xdf9, x6	// Set all CSR bits
	csrrc x16, 0xdf9, x6	// Clear all CSR bits
	csrrw x16, 0xdf9, x14	// Restore CSR

// Testing CSR 0xdfa
	csrr x14, 0xdfa	// Read CSR
	li x12, -1
	csrrw x13, 0xdfa, x12	// Write all 1s to CSR
	csrrw x13, 0xdfa, x0	// Write all 0s to CSR
	csrrs x13, 0xdfa, x12	// Set all CSR bits
	csrrc x13, 0xdfa, x12	// Clear all CSR bits
	csrrw x13, 0xdfa, x14	// Restore CSR

// Testing CSR 0xdfb
	csrr x11, 0xdfb	// Read CSR
	li x30, -1
	csrrw x23, 0xdfb, x30	// Write all 1s to CSR
	csrrw x23, 0xdfb, x0	// Write all 0s to CSR
	csrrs x23, 0xdfb, x30	// Set all CSR bits
	csrrc x23, 0xdfb, x30	// Clear all CSR bits
	csrrw x23, 0xdfb, x11	// Restore CSR

// Testing CSR 0xdfc
	csrr x6, 0xdfc	// Read CSR
	li x17, -1
	csrrw x28, 0xdfc, x17	// Write all 1s to CSR
	csrrw x28, 0xdfc, x0	// Write all 0s to CSR
	csrrs x28, 0xdfc, x17	// Set all CSR bits
	csrrc x28, 0xdfc, x17	// Clear all CSR bits
	csrrw x28, 0xdfc, x6	// Restore CSR

// Testing CSR 0xdfd
	csrr x4, 0xdfd	// Read CSR
	li x10, -1
	csrrw x12, 0xdfd, x10	// Write all 1s to CSR
	csrrw x12, 0xdfd, x0	// Write all 0s to CSR
	csrrs x12, 0xdfd, x10	// Set all CSR bits
	csrrc x12, 0xdfd, x10	// Clear all CSR bits
	csrrw x12, 0xdfd, x4	// Restore CSR

// Testing CSR 0xdfe
	csrr x7, 0xdfe	// Read CSR
	li x23, -1
	csrrw x4, 0xdfe, x23	// Write all 1s to CSR
	csrrw x4, 0xdfe, x0	// Write all 0s to CSR
	csrrs x4, 0xdfe, x23	// Set all CSR bits
	csrrc x4, 0xdfe, x23	// Clear all CSR bits
	csrrw x4, 0xdfe, x7	// Restore CSR

// Testing CSR 0xdff
	csrr x7, 0xdff	// Read CSR
	li x26, -1
	csrrw x4, 0xdff, x26	// Write all 1s to CSR
	csrrw x4, 0xdff, x0	// Write all 0s to CSR
	csrrs x4, 0xdff, x26	// Set all CSR bits
	csrrc x4, 0xdff, x26	// Clear all CSR bits
	csrrw x4, 0xdff, x7	// Restore CSR

// Testing CSR 0xe00
	csrr x31, 0xe00	// Read CSR
	li x7, -1
	csrrw x1, 0xe00, x7	// Write all 1s to CSR
	csrrw x1, 0xe00, x0	// Write all 0s to CSR
	csrrs x1, 0xe00, x7	// Set all CSR bits
	csrrc x1, 0xe00, x7	// Clear all CSR bits
	csrrw x1, 0xe00, x31	// Restore CSR

// Testing CSR 0xe01
	csrr x22, 0xe01	// Read CSR
	li x25, -1
	csrrw x1, 0xe01, x25	// Write all 1s to CSR
	csrrw x1, 0xe01, x0	// Write all 0s to CSR
	csrrs x1, 0xe01, x25	// Set all CSR bits
	csrrc x1, 0xe01, x25	// Clear all CSR bits
	csrrw x1, 0xe01, x22	// Restore CSR

// Testing CSR 0xe02
	csrr x18, 0xe02	// Read CSR
	li x9, -1
	csrrw x25, 0xe02, x9	// Write all 1s to CSR
	csrrw x25, 0xe02, x0	// Write all 0s to CSR
	csrrs x25, 0xe02, x9	// Set all CSR bits
	csrrc x25, 0xe02, x9	// Clear all CSR bits
	csrrw x25, 0xe02, x18	// Restore CSR

// Testing CSR 0xe03
	csrr x28, 0xe03	// Read CSR
	li x22, -1
	csrrw x23, 0xe03, x22	// Write all 1s to CSR
	csrrw x23, 0xe03, x0	// Write all 0s to CSR
	csrrs x23, 0xe03, x22	// Set all CSR bits
	csrrc x23, 0xe03, x22	// Clear all CSR bits
	csrrw x23, 0xe03, x28	// Restore CSR

// Testing CSR 0xe04
	csrr x18, 0xe04	// Read CSR
	li x31, -1
	csrrw x24, 0xe04, x31	// Write all 1s to CSR
	csrrw x24, 0xe04, x0	// Write all 0s to CSR
	csrrs x24, 0xe04, x31	// Set all CSR bits
	csrrc x24, 0xe04, x31	// Clear all CSR bits
	csrrw x24, 0xe04, x18	// Restore CSR

// Testing CSR 0xe05
	csrr x14, 0xe05	// Read CSR
	li x30, -1
	csrrw x4, 0xe05, x30	// Write all 1s to CSR
	csrrw x4, 0xe05, x0	// Write all 0s to CSR
	csrrs x4, 0xe05, x30	// Set all CSR bits
	csrrc x4, 0xe05, x30	// Clear all CSR bits
	csrrw x4, 0xe05, x14	// Restore CSR

// Testing CSR 0xe06
	csrr x30, 0xe06	// Read CSR
	li x24, -1
	csrrw x25, 0xe06, x24	// Write all 1s to CSR
	csrrw x25, 0xe06, x0	// Write all 0s to CSR
	csrrs x25, 0xe06, x24	// Set all CSR bits
	csrrc x25, 0xe06, x24	// Clear all CSR bits
	csrrw x25, 0xe06, x30	// Restore CSR

// Testing CSR 0xe07
	csrr x26, 0xe07	// Read CSR
	li x16, -1
	csrrw x29, 0xe07, x16	// Write all 1s to CSR
	csrrw x29, 0xe07, x0	// Write all 0s to CSR
	csrrs x29, 0xe07, x16	// Set all CSR bits
	csrrc x29, 0xe07, x16	// Clear all CSR bits
	csrrw x29, 0xe07, x26	// Restore CSR

// Testing CSR 0xe08
	csrr x25, 0xe08	// Read CSR
	li x22, -1
	csrrw x3, 0xe08, x22	// Write all 1s to CSR
	csrrw x3, 0xe08, x0	// Write all 0s to CSR
	csrrs x3, 0xe08, x22	// Set all CSR bits
	csrrc x3, 0xe08, x22	// Clear all CSR bits
	csrrw x3, 0xe08, x25	// Restore CSR

// Testing CSR 0xe09
	csrr x4, 0xe09	// Read CSR
	li x14, -1
	csrrw x1, 0xe09, x14	// Write all 1s to CSR
	csrrw x1, 0xe09, x0	// Write all 0s to CSR
	csrrs x1, 0xe09, x14	// Set all CSR bits
	csrrc x1, 0xe09, x14	// Clear all CSR bits
	csrrw x1, 0xe09, x4	// Restore CSR

// Testing CSR 0xe0a
	csrr x12, 0xe0a	// Read CSR
	li x15, -1
	csrrw x28, 0xe0a, x15	// Write all 1s to CSR
	csrrw x28, 0xe0a, x0	// Write all 0s to CSR
	csrrs x28, 0xe0a, x15	// Set all CSR bits
	csrrc x28, 0xe0a, x15	// Clear all CSR bits
	csrrw x28, 0xe0a, x12	// Restore CSR

// Testing CSR 0xe0b
	csrr x26, 0xe0b	// Read CSR
	li x15, -1
	csrrw x6, 0xe0b, x15	// Write all 1s to CSR
	csrrw x6, 0xe0b, x0	// Write all 0s to CSR
	csrrs x6, 0xe0b, x15	// Set all CSR bits
	csrrc x6, 0xe0b, x15	// Clear all CSR bits
	csrrw x6, 0xe0b, x26	// Restore CSR

// Testing CSR 0xe0c
	csrr x27, 0xe0c	// Read CSR
	li x4, -1
	csrrw x10, 0xe0c, x4	// Write all 1s to CSR
	csrrw x10, 0xe0c, x0	// Write all 0s to CSR
	csrrs x10, 0xe0c, x4	// Set all CSR bits
	csrrc x10, 0xe0c, x4	// Clear all CSR bits
	csrrw x10, 0xe0c, x27	// Restore CSR

// Testing CSR 0xe0d
	csrr x30, 0xe0d	// Read CSR
	li x7, -1
	csrrw x24, 0xe0d, x7	// Write all 1s to CSR
	csrrw x24, 0xe0d, x0	// Write all 0s to CSR
	csrrs x24, 0xe0d, x7	// Set all CSR bits
	csrrc x24, 0xe0d, x7	// Clear all CSR bits
	csrrw x24, 0xe0d, x30	// Restore CSR

// Testing CSR 0xe0e
	csrr x14, 0xe0e	// Read CSR
	li x29, -1
	csrrw x15, 0xe0e, x29	// Write all 1s to CSR
	csrrw x15, 0xe0e, x0	// Write all 0s to CSR
	csrrs x15, 0xe0e, x29	// Set all CSR bits
	csrrc x15, 0xe0e, x29	// Clear all CSR bits
	csrrw x15, 0xe0e, x14	// Restore CSR

// Testing CSR 0xe0f
	csrr x18, 0xe0f	// Read CSR
	li x29, -1
	csrrw x31, 0xe0f, x29	// Write all 1s to CSR
	csrrw x31, 0xe0f, x0	// Write all 0s to CSR
	csrrs x31, 0xe0f, x29	// Set all CSR bits
	csrrc x31, 0xe0f, x29	// Clear all CSR bits
	csrrw x31, 0xe0f, x18	// Restore CSR

// Testing CSR 0xe10
	csrr x24, 0xe10	// Read CSR
	li x3, -1
	csrrw x2, 0xe10, x3	// Write all 1s to CSR
	csrrw x2, 0xe10, x0	// Write all 0s to CSR
	csrrs x2, 0xe10, x3	// Set all CSR bits
	csrrc x2, 0xe10, x3	// Clear all CSR bits
	csrrw x2, 0xe10, x24	// Restore CSR

// Testing CSR 0xe11
	csrr x28, 0xe11	// Read CSR
	li x18, -1
	csrrw x14, 0xe11, x18	// Write all 1s to CSR
	csrrw x14, 0xe11, x0	// Write all 0s to CSR
	csrrs x14, 0xe11, x18	// Set all CSR bits
	csrrc x14, 0xe11, x18	// Clear all CSR bits
	csrrw x14, 0xe11, x28	// Restore CSR

// Testing CSR 0xe12
	csrr x20, 0xe12	// Read CSR
	li x15, -1
	csrrw x10, 0xe12, x15	// Write all 1s to CSR
	csrrw x10, 0xe12, x0	// Write all 0s to CSR
	csrrs x10, 0xe12, x15	// Set all CSR bits
	csrrc x10, 0xe12, x15	// Clear all CSR bits
	csrrw x10, 0xe12, x20	// Restore CSR

// Testing CSR 0xe13
	csrr x18, 0xe13	// Read CSR
	li x23, -1
	csrrw x8, 0xe13, x23	// Write all 1s to CSR
	csrrw x8, 0xe13, x0	// Write all 0s to CSR
	csrrs x8, 0xe13, x23	// Set all CSR bits
	csrrc x8, 0xe13, x23	// Clear all CSR bits
	csrrw x8, 0xe13, x18	// Restore CSR

// Testing CSR 0xe14
	csrr x19, 0xe14	// Read CSR
	li x10, -1
	csrrw x3, 0xe14, x10	// Write all 1s to CSR
	csrrw x3, 0xe14, x0	// Write all 0s to CSR
	csrrs x3, 0xe14, x10	// Set all CSR bits
	csrrc x3, 0xe14, x10	// Clear all CSR bits
	csrrw x3, 0xe14, x19	// Restore CSR

// Testing CSR 0xe15
	csrr x22, 0xe15	// Read CSR
	li x26, -1
	csrrw x13, 0xe15, x26	// Write all 1s to CSR
	csrrw x13, 0xe15, x0	// Write all 0s to CSR
	csrrs x13, 0xe15, x26	// Set all CSR bits
	csrrc x13, 0xe15, x26	// Clear all CSR bits
	csrrw x13, 0xe15, x22	// Restore CSR

// Testing CSR 0xe16
	csrr x5, 0xe16	// Read CSR
	li x10, -1
	csrrw x1, 0xe16, x10	// Write all 1s to CSR
	csrrw x1, 0xe16, x0	// Write all 0s to CSR
	csrrs x1, 0xe16, x10	// Set all CSR bits
	csrrc x1, 0xe16, x10	// Clear all CSR bits
	csrrw x1, 0xe16, x5	// Restore CSR

// Testing CSR 0xe17
	csrr x4, 0xe17	// Read CSR
	li x24, -1
	csrrw x16, 0xe17, x24	// Write all 1s to CSR
	csrrw x16, 0xe17, x0	// Write all 0s to CSR
	csrrs x16, 0xe17, x24	// Set all CSR bits
	csrrc x16, 0xe17, x24	// Clear all CSR bits
	csrrw x16, 0xe17, x4	// Restore CSR

// Testing CSR 0xe18
	csrr x28, 0xe18	// Read CSR
	li x3, -1
	csrrw x26, 0xe18, x3	// Write all 1s to CSR
	csrrw x26, 0xe18, x0	// Write all 0s to CSR
	csrrs x26, 0xe18, x3	// Set all CSR bits
	csrrc x26, 0xe18, x3	// Clear all CSR bits
	csrrw x26, 0xe18, x28	// Restore CSR

// Testing CSR 0xe19
	csrr x17, 0xe19	// Read CSR
	li x9, -1
	csrrw x24, 0xe19, x9	// Write all 1s to CSR
	csrrw x24, 0xe19, x0	// Write all 0s to CSR
	csrrs x24, 0xe19, x9	// Set all CSR bits
	csrrc x24, 0xe19, x9	// Clear all CSR bits
	csrrw x24, 0xe19, x17	// Restore CSR

// Testing CSR 0xe1a
	csrr x1, 0xe1a	// Read CSR
	li x30, -1
	csrrw x28, 0xe1a, x30	// Write all 1s to CSR
	csrrw x28, 0xe1a, x0	// Write all 0s to CSR
	csrrs x28, 0xe1a, x30	// Set all CSR bits
	csrrc x28, 0xe1a, x30	// Clear all CSR bits
	csrrw x28, 0xe1a, x1	// Restore CSR

// Testing CSR 0xe1b
	csrr x10, 0xe1b	// Read CSR
	li x22, -1
	csrrw x21, 0xe1b, x22	// Write all 1s to CSR
	csrrw x21, 0xe1b, x0	// Write all 0s to CSR
	csrrs x21, 0xe1b, x22	// Set all CSR bits
	csrrc x21, 0xe1b, x22	// Clear all CSR bits
	csrrw x21, 0xe1b, x10	// Restore CSR

// Testing CSR 0xe1c
	csrr x17, 0xe1c	// Read CSR
	li x24, -1
	csrrw x27, 0xe1c, x24	// Write all 1s to CSR
	csrrw x27, 0xe1c, x0	// Write all 0s to CSR
	csrrs x27, 0xe1c, x24	// Set all CSR bits
	csrrc x27, 0xe1c, x24	// Clear all CSR bits
	csrrw x27, 0xe1c, x17	// Restore CSR

// Testing CSR 0xe1d
	csrr x6, 0xe1d	// Read CSR
	li x22, -1
	csrrw x9, 0xe1d, x22	// Write all 1s to CSR
	csrrw x9, 0xe1d, x0	// Write all 0s to CSR
	csrrs x9, 0xe1d, x22	// Set all CSR bits
	csrrc x9, 0xe1d, x22	// Clear all CSR bits
	csrrw x9, 0xe1d, x6	// Restore CSR

// Testing CSR 0xe1e
	csrr x11, 0xe1e	// Read CSR
	li x25, -1
	csrrw x24, 0xe1e, x25	// Write all 1s to CSR
	csrrw x24, 0xe1e, x0	// Write all 0s to CSR
	csrrs x24, 0xe1e, x25	// Set all CSR bits
	csrrc x24, 0xe1e, x25	// Clear all CSR bits
	csrrw x24, 0xe1e, x11	// Restore CSR

// Testing CSR 0xe1f
	csrr x21, 0xe1f	// Read CSR
	li x23, -1
	csrrw x6, 0xe1f, x23	// Write all 1s to CSR
	csrrw x6, 0xe1f, x0	// Write all 0s to CSR
	csrrs x6, 0xe1f, x23	// Set all CSR bits
	csrrc x6, 0xe1f, x23	// Clear all CSR bits
	csrrw x6, 0xe1f, x21	// Restore CSR

// Testing CSR 0xe20
	csrr x1, 0xe20	// Read CSR
	li x2, -1
	csrrw x30, 0xe20, x2	// Write all 1s to CSR
	csrrw x30, 0xe20, x0	// Write all 0s to CSR
	csrrs x30, 0xe20, x2	// Set all CSR bits
	csrrc x30, 0xe20, x2	// Clear all CSR bits
	csrrw x30, 0xe20, x1	// Restore CSR

// Testing CSR 0xe21
	csrr x6, 0xe21	// Read CSR
	li x12, -1
	csrrw x7, 0xe21, x12	// Write all 1s to CSR
	csrrw x7, 0xe21, x0	// Write all 0s to CSR
	csrrs x7, 0xe21, x12	// Set all CSR bits
	csrrc x7, 0xe21, x12	// Clear all CSR bits
	csrrw x7, 0xe21, x6	// Restore CSR

// Testing CSR 0xe22
	csrr x1, 0xe22	// Read CSR
	li x24, -1
	csrrw x13, 0xe22, x24	// Write all 1s to CSR
	csrrw x13, 0xe22, x0	// Write all 0s to CSR
	csrrs x13, 0xe22, x24	// Set all CSR bits
	csrrc x13, 0xe22, x24	// Clear all CSR bits
	csrrw x13, 0xe22, x1	// Restore CSR

// Testing CSR 0xe23
	csrr x11, 0xe23	// Read CSR
	li x20, -1
	csrrw x27, 0xe23, x20	// Write all 1s to CSR
	csrrw x27, 0xe23, x0	// Write all 0s to CSR
	csrrs x27, 0xe23, x20	// Set all CSR bits
	csrrc x27, 0xe23, x20	// Clear all CSR bits
	csrrw x27, 0xe23, x11	// Restore CSR

// Testing CSR 0xe24
	csrr x28, 0xe24	// Read CSR
	li x9, -1
	csrrw x14, 0xe24, x9	// Write all 1s to CSR
	csrrw x14, 0xe24, x0	// Write all 0s to CSR
	csrrs x14, 0xe24, x9	// Set all CSR bits
	csrrc x14, 0xe24, x9	// Clear all CSR bits
	csrrw x14, 0xe24, x28	// Restore CSR

// Testing CSR 0xe25
	csrr x9, 0xe25	// Read CSR
	li x24, -1
	csrrw x28, 0xe25, x24	// Write all 1s to CSR
	csrrw x28, 0xe25, x0	// Write all 0s to CSR
	csrrs x28, 0xe25, x24	// Set all CSR bits
	csrrc x28, 0xe25, x24	// Clear all CSR bits
	csrrw x28, 0xe25, x9	// Restore CSR

// Testing CSR 0xe26
	csrr x24, 0xe26	// Read CSR
	li x1, -1
	csrrw x10, 0xe26, x1	// Write all 1s to CSR
	csrrw x10, 0xe26, x0	// Write all 0s to CSR
	csrrs x10, 0xe26, x1	// Set all CSR bits
	csrrc x10, 0xe26, x1	// Clear all CSR bits
	csrrw x10, 0xe26, x24	// Restore CSR

// Testing CSR 0xe27
	csrr x21, 0xe27	// Read CSR
	li x30, -1
	csrrw x6, 0xe27, x30	// Write all 1s to CSR
	csrrw x6, 0xe27, x0	// Write all 0s to CSR
	csrrs x6, 0xe27, x30	// Set all CSR bits
	csrrc x6, 0xe27, x30	// Clear all CSR bits
	csrrw x6, 0xe27, x21	// Restore CSR

// Testing CSR 0xe28
	csrr x16, 0xe28	// Read CSR
	li x10, -1
	csrrw x22, 0xe28, x10	// Write all 1s to CSR
	csrrw x22, 0xe28, x0	// Write all 0s to CSR
	csrrs x22, 0xe28, x10	// Set all CSR bits
	csrrc x22, 0xe28, x10	// Clear all CSR bits
	csrrw x22, 0xe28, x16	// Restore CSR

// Testing CSR 0xe29
	csrr x19, 0xe29	// Read CSR
	li x20, -1
	csrrw x28, 0xe29, x20	// Write all 1s to CSR
	csrrw x28, 0xe29, x0	// Write all 0s to CSR
	csrrs x28, 0xe29, x20	// Set all CSR bits
	csrrc x28, 0xe29, x20	// Clear all CSR bits
	csrrw x28, 0xe29, x19	// Restore CSR

// Testing CSR 0xe2a
	csrr x31, 0xe2a	// Read CSR
	li x6, -1
	csrrw x24, 0xe2a, x6	// Write all 1s to CSR
	csrrw x24, 0xe2a, x0	// Write all 0s to CSR
	csrrs x24, 0xe2a, x6	// Set all CSR bits
	csrrc x24, 0xe2a, x6	// Clear all CSR bits
	csrrw x24, 0xe2a, x31	// Restore CSR

// Testing CSR 0xe2b
	csrr x30, 0xe2b	// Read CSR
	li x21, -1
	csrrw x12, 0xe2b, x21	// Write all 1s to CSR
	csrrw x12, 0xe2b, x0	// Write all 0s to CSR
	csrrs x12, 0xe2b, x21	// Set all CSR bits
	csrrc x12, 0xe2b, x21	// Clear all CSR bits
	csrrw x12, 0xe2b, x30	// Restore CSR

// Testing CSR 0xe2c
	csrr x10, 0xe2c	// Read CSR
	li x16, -1
	csrrw x1, 0xe2c, x16	// Write all 1s to CSR
	csrrw x1, 0xe2c, x0	// Write all 0s to CSR
	csrrs x1, 0xe2c, x16	// Set all CSR bits
	csrrc x1, 0xe2c, x16	// Clear all CSR bits
	csrrw x1, 0xe2c, x10	// Restore CSR

// Testing CSR 0xe2d
	csrr x26, 0xe2d	// Read CSR
	li x20, -1
	csrrw x3, 0xe2d, x20	// Write all 1s to CSR
	csrrw x3, 0xe2d, x0	// Write all 0s to CSR
	csrrs x3, 0xe2d, x20	// Set all CSR bits
	csrrc x3, 0xe2d, x20	// Clear all CSR bits
	csrrw x3, 0xe2d, x26	// Restore CSR

// Testing CSR 0xe2e
	csrr x10, 0xe2e	// Read CSR
	li x4, -1
	csrrw x25, 0xe2e, x4	// Write all 1s to CSR
	csrrw x25, 0xe2e, x0	// Write all 0s to CSR
	csrrs x25, 0xe2e, x4	// Set all CSR bits
	csrrc x25, 0xe2e, x4	// Clear all CSR bits
	csrrw x25, 0xe2e, x10	// Restore CSR

// Testing CSR 0xe2f
	csrr x21, 0xe2f	// Read CSR
	li x27, -1
	csrrw x9, 0xe2f, x27	// Write all 1s to CSR
	csrrw x9, 0xe2f, x0	// Write all 0s to CSR
	csrrs x9, 0xe2f, x27	// Set all CSR bits
	csrrc x9, 0xe2f, x27	// Clear all CSR bits
	csrrw x9, 0xe2f, x21	// Restore CSR

// Testing CSR 0xe30
	csrr x25, 0xe30	// Read CSR
	li x11, -1
	csrrw x7, 0xe30, x11	// Write all 1s to CSR
	csrrw x7, 0xe30, x0	// Write all 0s to CSR
	csrrs x7, 0xe30, x11	// Set all CSR bits
	csrrc x7, 0xe30, x11	// Clear all CSR bits
	csrrw x7, 0xe30, x25	// Restore CSR

// Testing CSR 0xe31
	csrr x31, 0xe31	// Read CSR
	li x2, -1
	csrrw x21, 0xe31, x2	// Write all 1s to CSR
	csrrw x21, 0xe31, x0	// Write all 0s to CSR
	csrrs x21, 0xe31, x2	// Set all CSR bits
	csrrc x21, 0xe31, x2	// Clear all CSR bits
	csrrw x21, 0xe31, x31	// Restore CSR

// Testing CSR 0xe32
	csrr x4, 0xe32	// Read CSR
	li x15, -1
	csrrw x12, 0xe32, x15	// Write all 1s to CSR
	csrrw x12, 0xe32, x0	// Write all 0s to CSR
	csrrs x12, 0xe32, x15	// Set all CSR bits
	csrrc x12, 0xe32, x15	// Clear all CSR bits
	csrrw x12, 0xe32, x4	// Restore CSR

// Testing CSR 0xe33
	csrr x30, 0xe33	// Read CSR
	li x13, -1
	csrrw x17, 0xe33, x13	// Write all 1s to CSR
	csrrw x17, 0xe33, x0	// Write all 0s to CSR
	csrrs x17, 0xe33, x13	// Set all CSR bits
	csrrc x17, 0xe33, x13	// Clear all CSR bits
	csrrw x17, 0xe33, x30	// Restore CSR

// Testing CSR 0xe34
	csrr x19, 0xe34	// Read CSR
	li x9, -1
	csrrw x25, 0xe34, x9	// Write all 1s to CSR
	csrrw x25, 0xe34, x0	// Write all 0s to CSR
	csrrs x25, 0xe34, x9	// Set all CSR bits
	csrrc x25, 0xe34, x9	// Clear all CSR bits
	csrrw x25, 0xe34, x19	// Restore CSR

// Testing CSR 0xe35
	csrr x5, 0xe35	// Read CSR
	li x16, -1
	csrrw x17, 0xe35, x16	// Write all 1s to CSR
	csrrw x17, 0xe35, x0	// Write all 0s to CSR
	csrrs x17, 0xe35, x16	// Set all CSR bits
	csrrc x17, 0xe35, x16	// Clear all CSR bits
	csrrw x17, 0xe35, x5	// Restore CSR

// Testing CSR 0xe36
	csrr x4, 0xe36	// Read CSR
	li x28, -1
	csrrw x6, 0xe36, x28	// Write all 1s to CSR
	csrrw x6, 0xe36, x0	// Write all 0s to CSR
	csrrs x6, 0xe36, x28	// Set all CSR bits
	csrrc x6, 0xe36, x28	// Clear all CSR bits
	csrrw x6, 0xe36, x4	// Restore CSR

// Testing CSR 0xe37
	csrr x17, 0xe37	// Read CSR
	li x6, -1
	csrrw x18, 0xe37, x6	// Write all 1s to CSR
	csrrw x18, 0xe37, x0	// Write all 0s to CSR
	csrrs x18, 0xe37, x6	// Set all CSR bits
	csrrc x18, 0xe37, x6	// Clear all CSR bits
	csrrw x18, 0xe37, x17	// Restore CSR

// Testing CSR 0xe38
	csrr x19, 0xe38	// Read CSR
	li x1, -1
	csrrw x12, 0xe38, x1	// Write all 1s to CSR
	csrrw x12, 0xe38, x0	// Write all 0s to CSR
	csrrs x12, 0xe38, x1	// Set all CSR bits
	csrrc x12, 0xe38, x1	// Clear all CSR bits
	csrrw x12, 0xe38, x19	// Restore CSR

// Testing CSR 0xe39
	csrr x8, 0xe39	// Read CSR
	li x9, -1
	csrrw x22, 0xe39, x9	// Write all 1s to CSR
	csrrw x22, 0xe39, x0	// Write all 0s to CSR
	csrrs x22, 0xe39, x9	// Set all CSR bits
	csrrc x22, 0xe39, x9	// Clear all CSR bits
	csrrw x22, 0xe39, x8	// Restore CSR

// Testing CSR 0xe3a
	csrr x21, 0xe3a	// Read CSR
	li x14, -1
	csrrw x31, 0xe3a, x14	// Write all 1s to CSR
	csrrw x31, 0xe3a, x0	// Write all 0s to CSR
	csrrs x31, 0xe3a, x14	// Set all CSR bits
	csrrc x31, 0xe3a, x14	// Clear all CSR bits
	csrrw x31, 0xe3a, x21	// Restore CSR

// Testing CSR 0xe3b
	csrr x12, 0xe3b	// Read CSR
	li x17, -1
	csrrw x28, 0xe3b, x17	// Write all 1s to CSR
	csrrw x28, 0xe3b, x0	// Write all 0s to CSR
	csrrs x28, 0xe3b, x17	// Set all CSR bits
	csrrc x28, 0xe3b, x17	// Clear all CSR bits
	csrrw x28, 0xe3b, x12	// Restore CSR

// Testing CSR 0xe3c
	csrr x30, 0xe3c	// Read CSR
	li x18, -1
	csrrw x21, 0xe3c, x18	// Write all 1s to CSR
	csrrw x21, 0xe3c, x0	// Write all 0s to CSR
	csrrs x21, 0xe3c, x18	// Set all CSR bits
	csrrc x21, 0xe3c, x18	// Clear all CSR bits
	csrrw x21, 0xe3c, x30	// Restore CSR

// Testing CSR 0xe3d
	csrr x28, 0xe3d	// Read CSR
	li x22, -1
	csrrw x6, 0xe3d, x22	// Write all 1s to CSR
	csrrw x6, 0xe3d, x0	// Write all 0s to CSR
	csrrs x6, 0xe3d, x22	// Set all CSR bits
	csrrc x6, 0xe3d, x22	// Clear all CSR bits
	csrrw x6, 0xe3d, x28	// Restore CSR

// Testing CSR 0xe3e
	csrr x29, 0xe3e	// Read CSR
	li x7, -1
	csrrw x6, 0xe3e, x7	// Write all 1s to CSR
	csrrw x6, 0xe3e, x0	// Write all 0s to CSR
	csrrs x6, 0xe3e, x7	// Set all CSR bits
	csrrc x6, 0xe3e, x7	// Clear all CSR bits
	csrrw x6, 0xe3e, x29	// Restore CSR

// Testing CSR 0xe3f
	csrr x9, 0xe3f	// Read CSR
	li x26, -1
	csrrw x19, 0xe3f, x26	// Write all 1s to CSR
	csrrw x19, 0xe3f, x0	// Write all 0s to CSR
	csrrs x19, 0xe3f, x26	// Set all CSR bits
	csrrc x19, 0xe3f, x26	// Clear all CSR bits
	csrrw x19, 0xe3f, x9	// Restore CSR

// Testing CSR 0xe40
	csrr x24, 0xe40	// Read CSR
	li x19, -1
	csrrw x8, 0xe40, x19	// Write all 1s to CSR
	csrrw x8, 0xe40, x0	// Write all 0s to CSR
	csrrs x8, 0xe40, x19	// Set all CSR bits
	csrrc x8, 0xe40, x19	// Clear all CSR bits
	csrrw x8, 0xe40, x24	// Restore CSR

// Testing CSR 0xe41
	csrr x4, 0xe41	// Read CSR
	li x29, -1
	csrrw x7, 0xe41, x29	// Write all 1s to CSR
	csrrw x7, 0xe41, x0	// Write all 0s to CSR
	csrrs x7, 0xe41, x29	// Set all CSR bits
	csrrc x7, 0xe41, x29	// Clear all CSR bits
	csrrw x7, 0xe41, x4	// Restore CSR

// Testing CSR 0xe42
	csrr x21, 0xe42	// Read CSR
	li x12, -1
	csrrw x16, 0xe42, x12	// Write all 1s to CSR
	csrrw x16, 0xe42, x0	// Write all 0s to CSR
	csrrs x16, 0xe42, x12	// Set all CSR bits
	csrrc x16, 0xe42, x12	// Clear all CSR bits
	csrrw x16, 0xe42, x21	// Restore CSR

// Testing CSR 0xe43
	csrr x17, 0xe43	// Read CSR
	li x26, -1
	csrrw x4, 0xe43, x26	// Write all 1s to CSR
	csrrw x4, 0xe43, x0	// Write all 0s to CSR
	csrrs x4, 0xe43, x26	// Set all CSR bits
	csrrc x4, 0xe43, x26	// Clear all CSR bits
	csrrw x4, 0xe43, x17	// Restore CSR

// Testing CSR 0xe44
	csrr x27, 0xe44	// Read CSR
	li x7, -1
	csrrw x31, 0xe44, x7	// Write all 1s to CSR
	csrrw x31, 0xe44, x0	// Write all 0s to CSR
	csrrs x31, 0xe44, x7	// Set all CSR bits
	csrrc x31, 0xe44, x7	// Clear all CSR bits
	csrrw x31, 0xe44, x27	// Restore CSR

// Testing CSR 0xe45
	csrr x27, 0xe45	// Read CSR
	li x26, -1
	csrrw x21, 0xe45, x26	// Write all 1s to CSR
	csrrw x21, 0xe45, x0	// Write all 0s to CSR
	csrrs x21, 0xe45, x26	// Set all CSR bits
	csrrc x21, 0xe45, x26	// Clear all CSR bits
	csrrw x21, 0xe45, x27	// Restore CSR

// Testing CSR 0xe46
	csrr x6, 0xe46	// Read CSR
	li x9, -1
	csrrw x25, 0xe46, x9	// Write all 1s to CSR
	csrrw x25, 0xe46, x0	// Write all 0s to CSR
	csrrs x25, 0xe46, x9	// Set all CSR bits
	csrrc x25, 0xe46, x9	// Clear all CSR bits
	csrrw x25, 0xe46, x6	// Restore CSR

// Testing CSR 0xe47
	csrr x25, 0xe47	// Read CSR
	li x28, -1
	csrrw x20, 0xe47, x28	// Write all 1s to CSR
	csrrw x20, 0xe47, x0	// Write all 0s to CSR
	csrrs x20, 0xe47, x28	// Set all CSR bits
	csrrc x20, 0xe47, x28	// Clear all CSR bits
	csrrw x20, 0xe47, x25	// Restore CSR

// Testing CSR 0xe48
	csrr x25, 0xe48	// Read CSR
	li x11, -1
	csrrw x24, 0xe48, x11	// Write all 1s to CSR
	csrrw x24, 0xe48, x0	// Write all 0s to CSR
	csrrs x24, 0xe48, x11	// Set all CSR bits
	csrrc x24, 0xe48, x11	// Clear all CSR bits
	csrrw x24, 0xe48, x25	// Restore CSR

// Testing CSR 0xe49
	csrr x25, 0xe49	// Read CSR
	li x9, -1
	csrrw x14, 0xe49, x9	// Write all 1s to CSR
	csrrw x14, 0xe49, x0	// Write all 0s to CSR
	csrrs x14, 0xe49, x9	// Set all CSR bits
	csrrc x14, 0xe49, x9	// Clear all CSR bits
	csrrw x14, 0xe49, x25	// Restore CSR

// Testing CSR 0xe4a
	csrr x20, 0xe4a	// Read CSR
	li x5, -1
	csrrw x24, 0xe4a, x5	// Write all 1s to CSR
	csrrw x24, 0xe4a, x0	// Write all 0s to CSR
	csrrs x24, 0xe4a, x5	// Set all CSR bits
	csrrc x24, 0xe4a, x5	// Clear all CSR bits
	csrrw x24, 0xe4a, x20	// Restore CSR

// Testing CSR 0xe4b
	csrr x25, 0xe4b	// Read CSR
	li x12, -1
	csrrw x11, 0xe4b, x12	// Write all 1s to CSR
	csrrw x11, 0xe4b, x0	// Write all 0s to CSR
	csrrs x11, 0xe4b, x12	// Set all CSR bits
	csrrc x11, 0xe4b, x12	// Clear all CSR bits
	csrrw x11, 0xe4b, x25	// Restore CSR

// Testing CSR 0xe4c
	csrr x12, 0xe4c	// Read CSR
	li x22, -1
	csrrw x3, 0xe4c, x22	// Write all 1s to CSR
	csrrw x3, 0xe4c, x0	// Write all 0s to CSR
	csrrs x3, 0xe4c, x22	// Set all CSR bits
	csrrc x3, 0xe4c, x22	// Clear all CSR bits
	csrrw x3, 0xe4c, x12	// Restore CSR

// Testing CSR 0xe4d
	csrr x31, 0xe4d	// Read CSR
	li x26, -1
	csrrw x17, 0xe4d, x26	// Write all 1s to CSR
	csrrw x17, 0xe4d, x0	// Write all 0s to CSR
	csrrs x17, 0xe4d, x26	// Set all CSR bits
	csrrc x17, 0xe4d, x26	// Clear all CSR bits
	csrrw x17, 0xe4d, x31	// Restore CSR

// Testing CSR 0xe4e
	csrr x14, 0xe4e	// Read CSR
	li x2, -1
	csrrw x4, 0xe4e, x2	// Write all 1s to CSR
	csrrw x4, 0xe4e, x0	// Write all 0s to CSR
	csrrs x4, 0xe4e, x2	// Set all CSR bits
	csrrc x4, 0xe4e, x2	// Clear all CSR bits
	csrrw x4, 0xe4e, x14	// Restore CSR

// Testing CSR 0xe4f
	csrr x20, 0xe4f	// Read CSR
	li x22, -1
	csrrw x30, 0xe4f, x22	// Write all 1s to CSR
	csrrw x30, 0xe4f, x0	// Write all 0s to CSR
	csrrs x30, 0xe4f, x22	// Set all CSR bits
	csrrc x30, 0xe4f, x22	// Clear all CSR bits
	csrrw x30, 0xe4f, x20	// Restore CSR

// Testing CSR 0xe50
	csrr x13, 0xe50	// Read CSR
	li x28, -1
	csrrw x24, 0xe50, x28	// Write all 1s to CSR
	csrrw x24, 0xe50, x0	// Write all 0s to CSR
	csrrs x24, 0xe50, x28	// Set all CSR bits
	csrrc x24, 0xe50, x28	// Clear all CSR bits
	csrrw x24, 0xe50, x13	// Restore CSR

// Testing CSR 0xe51
	csrr x16, 0xe51	// Read CSR
	li x18, -1
	csrrw x2, 0xe51, x18	// Write all 1s to CSR
	csrrw x2, 0xe51, x0	// Write all 0s to CSR
	csrrs x2, 0xe51, x18	// Set all CSR bits
	csrrc x2, 0xe51, x18	// Clear all CSR bits
	csrrw x2, 0xe51, x16	// Restore CSR

// Testing CSR 0xe52
	csrr x2, 0xe52	// Read CSR
	li x13, -1
	csrrw x20, 0xe52, x13	// Write all 1s to CSR
	csrrw x20, 0xe52, x0	// Write all 0s to CSR
	csrrs x20, 0xe52, x13	// Set all CSR bits
	csrrc x20, 0xe52, x13	// Clear all CSR bits
	csrrw x20, 0xe52, x2	// Restore CSR

// Testing CSR 0xe53
	csrr x29, 0xe53	// Read CSR
	li x17, -1
	csrrw x9, 0xe53, x17	// Write all 1s to CSR
	csrrw x9, 0xe53, x0	// Write all 0s to CSR
	csrrs x9, 0xe53, x17	// Set all CSR bits
	csrrc x9, 0xe53, x17	// Clear all CSR bits
	csrrw x9, 0xe53, x29	// Restore CSR

// Testing CSR 0xe54
	csrr x30, 0xe54	// Read CSR
	li x8, -1
	csrrw x7, 0xe54, x8	// Write all 1s to CSR
	csrrw x7, 0xe54, x0	// Write all 0s to CSR
	csrrs x7, 0xe54, x8	// Set all CSR bits
	csrrc x7, 0xe54, x8	// Clear all CSR bits
	csrrw x7, 0xe54, x30	// Restore CSR

// Testing CSR 0xe55
	csrr x13, 0xe55	// Read CSR
	li x8, -1
	csrrw x24, 0xe55, x8	// Write all 1s to CSR
	csrrw x24, 0xe55, x0	// Write all 0s to CSR
	csrrs x24, 0xe55, x8	// Set all CSR bits
	csrrc x24, 0xe55, x8	// Clear all CSR bits
	csrrw x24, 0xe55, x13	// Restore CSR

// Testing CSR 0xe56
	csrr x1, 0xe56	// Read CSR
	li x18, -1
	csrrw x12, 0xe56, x18	// Write all 1s to CSR
	csrrw x12, 0xe56, x0	// Write all 0s to CSR
	csrrs x12, 0xe56, x18	// Set all CSR bits
	csrrc x12, 0xe56, x18	// Clear all CSR bits
	csrrw x12, 0xe56, x1	// Restore CSR

// Testing CSR 0xe57
	csrr x21, 0xe57	// Read CSR
	li x24, -1
	csrrw x6, 0xe57, x24	// Write all 1s to CSR
	csrrw x6, 0xe57, x0	// Write all 0s to CSR
	csrrs x6, 0xe57, x24	// Set all CSR bits
	csrrc x6, 0xe57, x24	// Clear all CSR bits
	csrrw x6, 0xe57, x21	// Restore CSR

// Testing CSR 0xe58
	csrr x30, 0xe58	// Read CSR
	li x23, -1
	csrrw x27, 0xe58, x23	// Write all 1s to CSR
	csrrw x27, 0xe58, x0	// Write all 0s to CSR
	csrrs x27, 0xe58, x23	// Set all CSR bits
	csrrc x27, 0xe58, x23	// Clear all CSR bits
	csrrw x27, 0xe58, x30	// Restore CSR

// Testing CSR 0xe59
	csrr x23, 0xe59	// Read CSR
	li x31, -1
	csrrw x28, 0xe59, x31	// Write all 1s to CSR
	csrrw x28, 0xe59, x0	// Write all 0s to CSR
	csrrs x28, 0xe59, x31	// Set all CSR bits
	csrrc x28, 0xe59, x31	// Clear all CSR bits
	csrrw x28, 0xe59, x23	// Restore CSR

// Testing CSR 0xe5a
	csrr x17, 0xe5a	// Read CSR
	li x12, -1
	csrrw x7, 0xe5a, x12	// Write all 1s to CSR
	csrrw x7, 0xe5a, x0	// Write all 0s to CSR
	csrrs x7, 0xe5a, x12	// Set all CSR bits
	csrrc x7, 0xe5a, x12	// Clear all CSR bits
	csrrw x7, 0xe5a, x17	// Restore CSR

// Testing CSR 0xe5b
	csrr x26, 0xe5b	// Read CSR
	li x31, -1
	csrrw x19, 0xe5b, x31	// Write all 1s to CSR
	csrrw x19, 0xe5b, x0	// Write all 0s to CSR
	csrrs x19, 0xe5b, x31	// Set all CSR bits
	csrrc x19, 0xe5b, x31	// Clear all CSR bits
	csrrw x19, 0xe5b, x26	// Restore CSR

// Testing CSR 0xe5c
	csrr x1, 0xe5c	// Read CSR
	li x7, -1
	csrrw x26, 0xe5c, x7	// Write all 1s to CSR
	csrrw x26, 0xe5c, x0	// Write all 0s to CSR
	csrrs x26, 0xe5c, x7	// Set all CSR bits
	csrrc x26, 0xe5c, x7	// Clear all CSR bits
	csrrw x26, 0xe5c, x1	// Restore CSR

// Testing CSR 0xe5d
	csrr x20, 0xe5d	// Read CSR
	li x22, -1
	csrrw x1, 0xe5d, x22	// Write all 1s to CSR
	csrrw x1, 0xe5d, x0	// Write all 0s to CSR
	csrrs x1, 0xe5d, x22	// Set all CSR bits
	csrrc x1, 0xe5d, x22	// Clear all CSR bits
	csrrw x1, 0xe5d, x20	// Restore CSR

// Testing CSR 0xe5e
	csrr x3, 0xe5e	// Read CSR
	li x18, -1
	csrrw x24, 0xe5e, x18	// Write all 1s to CSR
	csrrw x24, 0xe5e, x0	// Write all 0s to CSR
	csrrs x24, 0xe5e, x18	// Set all CSR bits
	csrrc x24, 0xe5e, x18	// Clear all CSR bits
	csrrw x24, 0xe5e, x3	// Restore CSR

// Testing CSR 0xe5f
	csrr x3, 0xe5f	// Read CSR
	li x14, -1
	csrrw x21, 0xe5f, x14	// Write all 1s to CSR
	csrrw x21, 0xe5f, x0	// Write all 0s to CSR
	csrrs x21, 0xe5f, x14	// Set all CSR bits
	csrrc x21, 0xe5f, x14	// Clear all CSR bits
	csrrw x21, 0xe5f, x3	// Restore CSR

// Testing CSR 0xe60
	csrr x21, 0xe60	// Read CSR
	li x3, -1
	csrrw x15, 0xe60, x3	// Write all 1s to CSR
	csrrw x15, 0xe60, x0	// Write all 0s to CSR
	csrrs x15, 0xe60, x3	// Set all CSR bits
	csrrc x15, 0xe60, x3	// Clear all CSR bits
	csrrw x15, 0xe60, x21	// Restore CSR

// Testing CSR 0xe61
	csrr x4, 0xe61	// Read CSR
	li x11, -1
	csrrw x16, 0xe61, x11	// Write all 1s to CSR
	csrrw x16, 0xe61, x0	// Write all 0s to CSR
	csrrs x16, 0xe61, x11	// Set all CSR bits
	csrrc x16, 0xe61, x11	// Clear all CSR bits
	csrrw x16, 0xe61, x4	// Restore CSR

// Testing CSR 0xe62
	csrr x19, 0xe62	// Read CSR
	li x14, -1
	csrrw x23, 0xe62, x14	// Write all 1s to CSR
	csrrw x23, 0xe62, x0	// Write all 0s to CSR
	csrrs x23, 0xe62, x14	// Set all CSR bits
	csrrc x23, 0xe62, x14	// Clear all CSR bits
	csrrw x23, 0xe62, x19	// Restore CSR

// Testing CSR 0xe63
	csrr x19, 0xe63	// Read CSR
	li x15, -1
	csrrw x27, 0xe63, x15	// Write all 1s to CSR
	csrrw x27, 0xe63, x0	// Write all 0s to CSR
	csrrs x27, 0xe63, x15	// Set all CSR bits
	csrrc x27, 0xe63, x15	// Clear all CSR bits
	csrrw x27, 0xe63, x19	// Restore CSR

// Testing CSR 0xe64
	csrr x23, 0xe64	// Read CSR
	li x11, -1
	csrrw x19, 0xe64, x11	// Write all 1s to CSR
	csrrw x19, 0xe64, x0	// Write all 0s to CSR
	csrrs x19, 0xe64, x11	// Set all CSR bits
	csrrc x19, 0xe64, x11	// Clear all CSR bits
	csrrw x19, 0xe64, x23	// Restore CSR

// Testing CSR 0xe65
	csrr x15, 0xe65	// Read CSR
	li x13, -1
	csrrw x19, 0xe65, x13	// Write all 1s to CSR
	csrrw x19, 0xe65, x0	// Write all 0s to CSR
	csrrs x19, 0xe65, x13	// Set all CSR bits
	csrrc x19, 0xe65, x13	// Clear all CSR bits
	csrrw x19, 0xe65, x15	// Restore CSR

// Testing CSR 0xe66
	csrr x6, 0xe66	// Read CSR
	li x30, -1
	csrrw x21, 0xe66, x30	// Write all 1s to CSR
	csrrw x21, 0xe66, x0	// Write all 0s to CSR
	csrrs x21, 0xe66, x30	// Set all CSR bits
	csrrc x21, 0xe66, x30	// Clear all CSR bits
	csrrw x21, 0xe66, x6	// Restore CSR

// Testing CSR 0xe67
	csrr x18, 0xe67	// Read CSR
	li x22, -1
	csrrw x7, 0xe67, x22	// Write all 1s to CSR
	csrrw x7, 0xe67, x0	// Write all 0s to CSR
	csrrs x7, 0xe67, x22	// Set all CSR bits
	csrrc x7, 0xe67, x22	// Clear all CSR bits
	csrrw x7, 0xe67, x18	// Restore CSR

// Testing CSR 0xe68
	csrr x31, 0xe68	// Read CSR
	li x17, -1
	csrrw x8, 0xe68, x17	// Write all 1s to CSR
	csrrw x8, 0xe68, x0	// Write all 0s to CSR
	csrrs x8, 0xe68, x17	// Set all CSR bits
	csrrc x8, 0xe68, x17	// Clear all CSR bits
	csrrw x8, 0xe68, x31	// Restore CSR

// Testing CSR 0xe69
	csrr x28, 0xe69	// Read CSR
	li x30, -1
	csrrw x6, 0xe69, x30	// Write all 1s to CSR
	csrrw x6, 0xe69, x0	// Write all 0s to CSR
	csrrs x6, 0xe69, x30	// Set all CSR bits
	csrrc x6, 0xe69, x30	// Clear all CSR bits
	csrrw x6, 0xe69, x28	// Restore CSR

// Testing CSR 0xe6a
	csrr x4, 0xe6a	// Read CSR
	li x30, -1
	csrrw x20, 0xe6a, x30	// Write all 1s to CSR
	csrrw x20, 0xe6a, x0	// Write all 0s to CSR
	csrrs x20, 0xe6a, x30	// Set all CSR bits
	csrrc x20, 0xe6a, x30	// Clear all CSR bits
	csrrw x20, 0xe6a, x4	// Restore CSR

// Testing CSR 0xe6b
	csrr x30, 0xe6b	// Read CSR
	li x17, -1
	csrrw x4, 0xe6b, x17	// Write all 1s to CSR
	csrrw x4, 0xe6b, x0	// Write all 0s to CSR
	csrrs x4, 0xe6b, x17	// Set all CSR bits
	csrrc x4, 0xe6b, x17	// Clear all CSR bits
	csrrw x4, 0xe6b, x30	// Restore CSR

// Testing CSR 0xe6c
	csrr x18, 0xe6c	// Read CSR
	li x1, -1
	csrrw x16, 0xe6c, x1	// Write all 1s to CSR
	csrrw x16, 0xe6c, x0	// Write all 0s to CSR
	csrrs x16, 0xe6c, x1	// Set all CSR bits
	csrrc x16, 0xe6c, x1	// Clear all CSR bits
	csrrw x16, 0xe6c, x18	// Restore CSR

// Testing CSR 0xe6d
	csrr x22, 0xe6d	// Read CSR
	li x25, -1
	csrrw x28, 0xe6d, x25	// Write all 1s to CSR
	csrrw x28, 0xe6d, x0	// Write all 0s to CSR
	csrrs x28, 0xe6d, x25	// Set all CSR bits
	csrrc x28, 0xe6d, x25	// Clear all CSR bits
	csrrw x28, 0xe6d, x22	// Restore CSR

// Testing CSR 0xe6e
	csrr x22, 0xe6e	// Read CSR
	li x18, -1
	csrrw x28, 0xe6e, x18	// Write all 1s to CSR
	csrrw x28, 0xe6e, x0	// Write all 0s to CSR
	csrrs x28, 0xe6e, x18	// Set all CSR bits
	csrrc x28, 0xe6e, x18	// Clear all CSR bits
	csrrw x28, 0xe6e, x22	// Restore CSR

// Testing CSR 0xe6f
	csrr x22, 0xe6f	// Read CSR
	li x27, -1
	csrrw x30, 0xe6f, x27	// Write all 1s to CSR
	csrrw x30, 0xe6f, x0	// Write all 0s to CSR
	csrrs x30, 0xe6f, x27	// Set all CSR bits
	csrrc x30, 0xe6f, x27	// Clear all CSR bits
	csrrw x30, 0xe6f, x22	// Restore CSR

// Testing CSR 0xe70
	csrr x1, 0xe70	// Read CSR
	li x28, -1
	csrrw x25, 0xe70, x28	// Write all 1s to CSR
	csrrw x25, 0xe70, x0	// Write all 0s to CSR
	csrrs x25, 0xe70, x28	// Set all CSR bits
	csrrc x25, 0xe70, x28	// Clear all CSR bits
	csrrw x25, 0xe70, x1	// Restore CSR

// Testing CSR 0xe71
	csrr x29, 0xe71	// Read CSR
	li x14, -1
	csrrw x9, 0xe71, x14	// Write all 1s to CSR
	csrrw x9, 0xe71, x0	// Write all 0s to CSR
	csrrs x9, 0xe71, x14	// Set all CSR bits
	csrrc x9, 0xe71, x14	// Clear all CSR bits
	csrrw x9, 0xe71, x29	// Restore CSR

// Testing CSR 0xe72
	csrr x24, 0xe72	// Read CSR
	li x29, -1
	csrrw x27, 0xe72, x29	// Write all 1s to CSR
	csrrw x27, 0xe72, x0	// Write all 0s to CSR
	csrrs x27, 0xe72, x29	// Set all CSR bits
	csrrc x27, 0xe72, x29	// Clear all CSR bits
	csrrw x27, 0xe72, x24	// Restore CSR

// Testing CSR 0xe73
	csrr x25, 0xe73	// Read CSR
	li x26, -1
	csrrw x15, 0xe73, x26	// Write all 1s to CSR
	csrrw x15, 0xe73, x0	// Write all 0s to CSR
	csrrs x15, 0xe73, x26	// Set all CSR bits
	csrrc x15, 0xe73, x26	// Clear all CSR bits
	csrrw x15, 0xe73, x25	// Restore CSR

// Testing CSR 0xe74
	csrr x28, 0xe74	// Read CSR
	li x24, -1
	csrrw x18, 0xe74, x24	// Write all 1s to CSR
	csrrw x18, 0xe74, x0	// Write all 0s to CSR
	csrrs x18, 0xe74, x24	// Set all CSR bits
	csrrc x18, 0xe74, x24	// Clear all CSR bits
	csrrw x18, 0xe74, x28	// Restore CSR

// Testing CSR 0xe75
	csrr x8, 0xe75	// Read CSR
	li x17, -1
	csrrw x10, 0xe75, x17	// Write all 1s to CSR
	csrrw x10, 0xe75, x0	// Write all 0s to CSR
	csrrs x10, 0xe75, x17	// Set all CSR bits
	csrrc x10, 0xe75, x17	// Clear all CSR bits
	csrrw x10, 0xe75, x8	// Restore CSR

// Testing CSR 0xe76
	csrr x26, 0xe76	// Read CSR
	li x16, -1
	csrrw x7, 0xe76, x16	// Write all 1s to CSR
	csrrw x7, 0xe76, x0	// Write all 0s to CSR
	csrrs x7, 0xe76, x16	// Set all CSR bits
	csrrc x7, 0xe76, x16	// Clear all CSR bits
	csrrw x7, 0xe76, x26	// Restore CSR

// Testing CSR 0xe77
	csrr x5, 0xe77	// Read CSR
	li x14, -1
	csrrw x11, 0xe77, x14	// Write all 1s to CSR
	csrrw x11, 0xe77, x0	// Write all 0s to CSR
	csrrs x11, 0xe77, x14	// Set all CSR bits
	csrrc x11, 0xe77, x14	// Clear all CSR bits
	csrrw x11, 0xe77, x5	// Restore CSR

// Testing CSR 0xe78
	csrr x12, 0xe78	// Read CSR
	li x2, -1
	csrrw x18, 0xe78, x2	// Write all 1s to CSR
	csrrw x18, 0xe78, x0	// Write all 0s to CSR
	csrrs x18, 0xe78, x2	// Set all CSR bits
	csrrc x18, 0xe78, x2	// Clear all CSR bits
	csrrw x18, 0xe78, x12	// Restore CSR

// Testing CSR 0xe79
	csrr x29, 0xe79	// Read CSR
	li x13, -1
	csrrw x22, 0xe79, x13	// Write all 1s to CSR
	csrrw x22, 0xe79, x0	// Write all 0s to CSR
	csrrs x22, 0xe79, x13	// Set all CSR bits
	csrrc x22, 0xe79, x13	// Clear all CSR bits
	csrrw x22, 0xe79, x29	// Restore CSR

// Testing CSR 0xe7a
	csrr x8, 0xe7a	// Read CSR
	li x9, -1
	csrrw x30, 0xe7a, x9	// Write all 1s to CSR
	csrrw x30, 0xe7a, x0	// Write all 0s to CSR
	csrrs x30, 0xe7a, x9	// Set all CSR bits
	csrrc x30, 0xe7a, x9	// Clear all CSR bits
	csrrw x30, 0xe7a, x8	// Restore CSR

// Testing CSR 0xe7b
	csrr x15, 0xe7b	// Read CSR
	li x23, -1
	csrrw x6, 0xe7b, x23	// Write all 1s to CSR
	csrrw x6, 0xe7b, x0	// Write all 0s to CSR
	csrrs x6, 0xe7b, x23	// Set all CSR bits
	csrrc x6, 0xe7b, x23	// Clear all CSR bits
	csrrw x6, 0xe7b, x15	// Restore CSR

// Testing CSR 0xe7c
	csrr x14, 0xe7c	// Read CSR
	li x10, -1
	csrrw x29, 0xe7c, x10	// Write all 1s to CSR
	csrrw x29, 0xe7c, x0	// Write all 0s to CSR
	csrrs x29, 0xe7c, x10	// Set all CSR bits
	csrrc x29, 0xe7c, x10	// Clear all CSR bits
	csrrw x29, 0xe7c, x14	// Restore CSR

// Testing CSR 0xe7d
	csrr x6, 0xe7d	// Read CSR
	li x31, -1
	csrrw x8, 0xe7d, x31	// Write all 1s to CSR
	csrrw x8, 0xe7d, x0	// Write all 0s to CSR
	csrrs x8, 0xe7d, x31	// Set all CSR bits
	csrrc x8, 0xe7d, x31	// Clear all CSR bits
	csrrw x8, 0xe7d, x6	// Restore CSR

// Testing CSR 0xe7e
	csrr x19, 0xe7e	// Read CSR
	li x27, -1
	csrrw x10, 0xe7e, x27	// Write all 1s to CSR
	csrrw x10, 0xe7e, x0	// Write all 0s to CSR
	csrrs x10, 0xe7e, x27	// Set all CSR bits
	csrrc x10, 0xe7e, x27	// Clear all CSR bits
	csrrw x10, 0xe7e, x19	// Restore CSR

// Testing CSR 0xe7f
	csrr x17, 0xe7f	// Read CSR
	li x21, -1
	csrrw x7, 0xe7f, x21	// Write all 1s to CSR
	csrrw x7, 0xe7f, x0	// Write all 0s to CSR
	csrrs x7, 0xe7f, x21	// Set all CSR bits
	csrrc x7, 0xe7f, x21	// Clear all CSR bits
	csrrw x7, 0xe7f, x17	// Restore CSR

// Testing CSR 0xe80
	csrr x15, 0xe80	// Read CSR
	li x24, -1
	csrrw x29, 0xe80, x24	// Write all 1s to CSR
	csrrw x29, 0xe80, x0	// Write all 0s to CSR
	csrrs x29, 0xe80, x24	// Set all CSR bits
	csrrc x29, 0xe80, x24	// Clear all CSR bits
	csrrw x29, 0xe80, x15	// Restore CSR

// Testing CSR 0xe81
	csrr x31, 0xe81	// Read CSR
	li x1, -1
	csrrw x3, 0xe81, x1	// Write all 1s to CSR
	csrrw x3, 0xe81, x0	// Write all 0s to CSR
	csrrs x3, 0xe81, x1	// Set all CSR bits
	csrrc x3, 0xe81, x1	// Clear all CSR bits
	csrrw x3, 0xe81, x31	// Restore CSR

// Testing CSR 0xe82
	csrr x1, 0xe82	// Read CSR
	li x3, -1
	csrrw x6, 0xe82, x3	// Write all 1s to CSR
	csrrw x6, 0xe82, x0	// Write all 0s to CSR
	csrrs x6, 0xe82, x3	// Set all CSR bits
	csrrc x6, 0xe82, x3	// Clear all CSR bits
	csrrw x6, 0xe82, x1	// Restore CSR

// Testing CSR 0xe83
	csrr x16, 0xe83	// Read CSR
	li x11, -1
	csrrw x1, 0xe83, x11	// Write all 1s to CSR
	csrrw x1, 0xe83, x0	// Write all 0s to CSR
	csrrs x1, 0xe83, x11	// Set all CSR bits
	csrrc x1, 0xe83, x11	// Clear all CSR bits
	csrrw x1, 0xe83, x16	// Restore CSR

// Testing CSR 0xe84
	csrr x19, 0xe84	// Read CSR
	li x30, -1
	csrrw x7, 0xe84, x30	// Write all 1s to CSR
	csrrw x7, 0xe84, x0	// Write all 0s to CSR
	csrrs x7, 0xe84, x30	// Set all CSR bits
	csrrc x7, 0xe84, x30	// Clear all CSR bits
	csrrw x7, 0xe84, x19	// Restore CSR

// Testing CSR 0xe85
	csrr x12, 0xe85	// Read CSR
	li x4, -1
	csrrw x30, 0xe85, x4	// Write all 1s to CSR
	csrrw x30, 0xe85, x0	// Write all 0s to CSR
	csrrs x30, 0xe85, x4	// Set all CSR bits
	csrrc x30, 0xe85, x4	// Clear all CSR bits
	csrrw x30, 0xe85, x12	// Restore CSR

// Testing CSR 0xe86
	csrr x2, 0xe86	// Read CSR
	li x15, -1
	csrrw x7, 0xe86, x15	// Write all 1s to CSR
	csrrw x7, 0xe86, x0	// Write all 0s to CSR
	csrrs x7, 0xe86, x15	// Set all CSR bits
	csrrc x7, 0xe86, x15	// Clear all CSR bits
	csrrw x7, 0xe86, x2	// Restore CSR

// Testing CSR 0xe87
	csrr x2, 0xe87	// Read CSR
	li x5, -1
	csrrw x4, 0xe87, x5	// Write all 1s to CSR
	csrrw x4, 0xe87, x0	// Write all 0s to CSR
	csrrs x4, 0xe87, x5	// Set all CSR bits
	csrrc x4, 0xe87, x5	// Clear all CSR bits
	csrrw x4, 0xe87, x2	// Restore CSR

// Testing CSR 0xe88
	csrr x31, 0xe88	// Read CSR
	li x6, -1
	csrrw x19, 0xe88, x6	// Write all 1s to CSR
	csrrw x19, 0xe88, x0	// Write all 0s to CSR
	csrrs x19, 0xe88, x6	// Set all CSR bits
	csrrc x19, 0xe88, x6	// Clear all CSR bits
	csrrw x19, 0xe88, x31	// Restore CSR

// Testing CSR 0xe89
	csrr x3, 0xe89	// Read CSR
	li x5, -1
	csrrw x4, 0xe89, x5	// Write all 1s to CSR
	csrrw x4, 0xe89, x0	// Write all 0s to CSR
	csrrs x4, 0xe89, x5	// Set all CSR bits
	csrrc x4, 0xe89, x5	// Clear all CSR bits
	csrrw x4, 0xe89, x3	// Restore CSR

// Testing CSR 0xe8a
	csrr x30, 0xe8a	// Read CSR
	li x22, -1
	csrrw x10, 0xe8a, x22	// Write all 1s to CSR
	csrrw x10, 0xe8a, x0	// Write all 0s to CSR
	csrrs x10, 0xe8a, x22	// Set all CSR bits
	csrrc x10, 0xe8a, x22	// Clear all CSR bits
	csrrw x10, 0xe8a, x30	// Restore CSR

// Testing CSR 0xe8b
	csrr x19, 0xe8b	// Read CSR
	li x26, -1
	csrrw x24, 0xe8b, x26	// Write all 1s to CSR
	csrrw x24, 0xe8b, x0	// Write all 0s to CSR
	csrrs x24, 0xe8b, x26	// Set all CSR bits
	csrrc x24, 0xe8b, x26	// Clear all CSR bits
	csrrw x24, 0xe8b, x19	// Restore CSR

// Testing CSR 0xe8c
	csrr x26, 0xe8c	// Read CSR
	li x25, -1
	csrrw x21, 0xe8c, x25	// Write all 1s to CSR
	csrrw x21, 0xe8c, x0	// Write all 0s to CSR
	csrrs x21, 0xe8c, x25	// Set all CSR bits
	csrrc x21, 0xe8c, x25	// Clear all CSR bits
	csrrw x21, 0xe8c, x26	// Restore CSR

// Testing CSR 0xe8d
	csrr x11, 0xe8d	// Read CSR
	li x4, -1
	csrrw x30, 0xe8d, x4	// Write all 1s to CSR
	csrrw x30, 0xe8d, x0	// Write all 0s to CSR
	csrrs x30, 0xe8d, x4	// Set all CSR bits
	csrrc x30, 0xe8d, x4	// Clear all CSR bits
	csrrw x30, 0xe8d, x11	// Restore CSR

// Testing CSR 0xe8e
	csrr x31, 0xe8e	// Read CSR
	li x5, -1
	csrrw x9, 0xe8e, x5	// Write all 1s to CSR
	csrrw x9, 0xe8e, x0	// Write all 0s to CSR
	csrrs x9, 0xe8e, x5	// Set all CSR bits
	csrrc x9, 0xe8e, x5	// Clear all CSR bits
	csrrw x9, 0xe8e, x31	// Restore CSR

// Testing CSR 0xe8f
	csrr x19, 0xe8f	// Read CSR
	li x8, -1
	csrrw x9, 0xe8f, x8	// Write all 1s to CSR
	csrrw x9, 0xe8f, x0	// Write all 0s to CSR
	csrrs x9, 0xe8f, x8	// Set all CSR bits
	csrrc x9, 0xe8f, x8	// Clear all CSR bits
	csrrw x9, 0xe8f, x19	// Restore CSR

// Testing CSR 0xe90
	csrr x21, 0xe90	// Read CSR
	li x26, -1
	csrrw x9, 0xe90, x26	// Write all 1s to CSR
	csrrw x9, 0xe90, x0	// Write all 0s to CSR
	csrrs x9, 0xe90, x26	// Set all CSR bits
	csrrc x9, 0xe90, x26	// Clear all CSR bits
	csrrw x9, 0xe90, x21	// Restore CSR

// Testing CSR 0xe91
	csrr x24, 0xe91	// Read CSR
	li x30, -1
	csrrw x19, 0xe91, x30	// Write all 1s to CSR
	csrrw x19, 0xe91, x0	// Write all 0s to CSR
	csrrs x19, 0xe91, x30	// Set all CSR bits
	csrrc x19, 0xe91, x30	// Clear all CSR bits
	csrrw x19, 0xe91, x24	// Restore CSR

// Testing CSR 0xe92
	csrr x10, 0xe92	// Read CSR
	li x7, -1
	csrrw x1, 0xe92, x7	// Write all 1s to CSR
	csrrw x1, 0xe92, x0	// Write all 0s to CSR
	csrrs x1, 0xe92, x7	// Set all CSR bits
	csrrc x1, 0xe92, x7	// Clear all CSR bits
	csrrw x1, 0xe92, x10	// Restore CSR

// Testing CSR 0xe93
	csrr x5, 0xe93	// Read CSR
	li x23, -1
	csrrw x21, 0xe93, x23	// Write all 1s to CSR
	csrrw x21, 0xe93, x0	// Write all 0s to CSR
	csrrs x21, 0xe93, x23	// Set all CSR bits
	csrrc x21, 0xe93, x23	// Clear all CSR bits
	csrrw x21, 0xe93, x5	// Restore CSR

// Testing CSR 0xe94
	csrr x10, 0xe94	// Read CSR
	li x4, -1
	csrrw x6, 0xe94, x4	// Write all 1s to CSR
	csrrw x6, 0xe94, x0	// Write all 0s to CSR
	csrrs x6, 0xe94, x4	// Set all CSR bits
	csrrc x6, 0xe94, x4	// Clear all CSR bits
	csrrw x6, 0xe94, x10	// Restore CSR

// Testing CSR 0xe95
	csrr x17, 0xe95	// Read CSR
	li x18, -1
	csrrw x12, 0xe95, x18	// Write all 1s to CSR
	csrrw x12, 0xe95, x0	// Write all 0s to CSR
	csrrs x12, 0xe95, x18	// Set all CSR bits
	csrrc x12, 0xe95, x18	// Clear all CSR bits
	csrrw x12, 0xe95, x17	// Restore CSR

// Testing CSR 0xe96
	csrr x10, 0xe96	// Read CSR
	li x19, -1
	csrrw x21, 0xe96, x19	// Write all 1s to CSR
	csrrw x21, 0xe96, x0	// Write all 0s to CSR
	csrrs x21, 0xe96, x19	// Set all CSR bits
	csrrc x21, 0xe96, x19	// Clear all CSR bits
	csrrw x21, 0xe96, x10	// Restore CSR

// Testing CSR 0xe97
	csrr x31, 0xe97	// Read CSR
	li x11, -1
	csrrw x24, 0xe97, x11	// Write all 1s to CSR
	csrrw x24, 0xe97, x0	// Write all 0s to CSR
	csrrs x24, 0xe97, x11	// Set all CSR bits
	csrrc x24, 0xe97, x11	// Clear all CSR bits
	csrrw x24, 0xe97, x31	// Restore CSR

// Testing CSR 0xe98
	csrr x13, 0xe98	// Read CSR
	li x29, -1
	csrrw x28, 0xe98, x29	// Write all 1s to CSR
	csrrw x28, 0xe98, x0	// Write all 0s to CSR
	csrrs x28, 0xe98, x29	// Set all CSR bits
	csrrc x28, 0xe98, x29	// Clear all CSR bits
	csrrw x28, 0xe98, x13	// Restore CSR

// Testing CSR 0xe99
	csrr x22, 0xe99	// Read CSR
	li x15, -1
	csrrw x7, 0xe99, x15	// Write all 1s to CSR
	csrrw x7, 0xe99, x0	// Write all 0s to CSR
	csrrs x7, 0xe99, x15	// Set all CSR bits
	csrrc x7, 0xe99, x15	// Clear all CSR bits
	csrrw x7, 0xe99, x22	// Restore CSR

// Testing CSR 0xe9a
	csrr x18, 0xe9a	// Read CSR
	li x4, -1
	csrrw x1, 0xe9a, x4	// Write all 1s to CSR
	csrrw x1, 0xe9a, x0	// Write all 0s to CSR
	csrrs x1, 0xe9a, x4	// Set all CSR bits
	csrrc x1, 0xe9a, x4	// Clear all CSR bits
	csrrw x1, 0xe9a, x18	// Restore CSR

// Testing CSR 0xe9b
	csrr x26, 0xe9b	// Read CSR
	li x24, -1
	csrrw x7, 0xe9b, x24	// Write all 1s to CSR
	csrrw x7, 0xe9b, x0	// Write all 0s to CSR
	csrrs x7, 0xe9b, x24	// Set all CSR bits
	csrrc x7, 0xe9b, x24	// Clear all CSR bits
	csrrw x7, 0xe9b, x26	// Restore CSR

// Testing CSR 0xe9c
	csrr x20, 0xe9c	// Read CSR
	li x6, -1
	csrrw x29, 0xe9c, x6	// Write all 1s to CSR
	csrrw x29, 0xe9c, x0	// Write all 0s to CSR
	csrrs x29, 0xe9c, x6	// Set all CSR bits
	csrrc x29, 0xe9c, x6	// Clear all CSR bits
	csrrw x29, 0xe9c, x20	// Restore CSR

// Testing CSR 0xe9d
	csrr x22, 0xe9d	// Read CSR
	li x20, -1
	csrrw x26, 0xe9d, x20	// Write all 1s to CSR
	csrrw x26, 0xe9d, x0	// Write all 0s to CSR
	csrrs x26, 0xe9d, x20	// Set all CSR bits
	csrrc x26, 0xe9d, x20	// Clear all CSR bits
	csrrw x26, 0xe9d, x22	// Restore CSR

// Testing CSR 0xe9e
	csrr x19, 0xe9e	// Read CSR
	li x28, -1
	csrrw x29, 0xe9e, x28	// Write all 1s to CSR
	csrrw x29, 0xe9e, x0	// Write all 0s to CSR
	csrrs x29, 0xe9e, x28	// Set all CSR bits
	csrrc x29, 0xe9e, x28	// Clear all CSR bits
	csrrw x29, 0xe9e, x19	// Restore CSR

// Testing CSR 0xe9f
	csrr x11, 0xe9f	// Read CSR
	li x22, -1
	csrrw x8, 0xe9f, x22	// Write all 1s to CSR
	csrrw x8, 0xe9f, x0	// Write all 0s to CSR
	csrrs x8, 0xe9f, x22	// Set all CSR bits
	csrrc x8, 0xe9f, x22	// Clear all CSR bits
	csrrw x8, 0xe9f, x11	// Restore CSR

// Testing CSR 0xea0
	csrr x18, 0xea0	// Read CSR
	li x4, -1
	csrrw x5, 0xea0, x4	// Write all 1s to CSR
	csrrw x5, 0xea0, x0	// Write all 0s to CSR
	csrrs x5, 0xea0, x4	// Set all CSR bits
	csrrc x5, 0xea0, x4	// Clear all CSR bits
	csrrw x5, 0xea0, x18	// Restore CSR

// Testing CSR 0xea1
	csrr x11, 0xea1	// Read CSR
	li x7, -1
	csrrw x15, 0xea1, x7	// Write all 1s to CSR
	csrrw x15, 0xea1, x0	// Write all 0s to CSR
	csrrs x15, 0xea1, x7	// Set all CSR bits
	csrrc x15, 0xea1, x7	// Clear all CSR bits
	csrrw x15, 0xea1, x11	// Restore CSR

// Testing CSR 0xea2
	csrr x20, 0xea2	// Read CSR
	li x19, -1
	csrrw x21, 0xea2, x19	// Write all 1s to CSR
	csrrw x21, 0xea2, x0	// Write all 0s to CSR
	csrrs x21, 0xea2, x19	// Set all CSR bits
	csrrc x21, 0xea2, x19	// Clear all CSR bits
	csrrw x21, 0xea2, x20	// Restore CSR

// Testing CSR 0xea3
	csrr x28, 0xea3	// Read CSR
	li x22, -1
	csrrw x30, 0xea3, x22	// Write all 1s to CSR
	csrrw x30, 0xea3, x0	// Write all 0s to CSR
	csrrs x30, 0xea3, x22	// Set all CSR bits
	csrrc x30, 0xea3, x22	// Clear all CSR bits
	csrrw x30, 0xea3, x28	// Restore CSR

// Testing CSR 0xea4
	csrr x16, 0xea4	// Read CSR
	li x31, -1
	csrrw x7, 0xea4, x31	// Write all 1s to CSR
	csrrw x7, 0xea4, x0	// Write all 0s to CSR
	csrrs x7, 0xea4, x31	// Set all CSR bits
	csrrc x7, 0xea4, x31	// Clear all CSR bits
	csrrw x7, 0xea4, x16	// Restore CSR

// Testing CSR 0xea5
	csrr x11, 0xea5	// Read CSR
	li x4, -1
	csrrw x22, 0xea5, x4	// Write all 1s to CSR
	csrrw x22, 0xea5, x0	// Write all 0s to CSR
	csrrs x22, 0xea5, x4	// Set all CSR bits
	csrrc x22, 0xea5, x4	// Clear all CSR bits
	csrrw x22, 0xea5, x11	// Restore CSR

// Testing CSR 0xea6
	csrr x25, 0xea6	// Read CSR
	li x31, -1
	csrrw x3, 0xea6, x31	// Write all 1s to CSR
	csrrw x3, 0xea6, x0	// Write all 0s to CSR
	csrrs x3, 0xea6, x31	// Set all CSR bits
	csrrc x3, 0xea6, x31	// Clear all CSR bits
	csrrw x3, 0xea6, x25	// Restore CSR

// Testing CSR 0xea7
	csrr x27, 0xea7	// Read CSR
	li x18, -1
	csrrw x4, 0xea7, x18	// Write all 1s to CSR
	csrrw x4, 0xea7, x0	// Write all 0s to CSR
	csrrs x4, 0xea7, x18	// Set all CSR bits
	csrrc x4, 0xea7, x18	// Clear all CSR bits
	csrrw x4, 0xea7, x27	// Restore CSR

// Testing CSR 0xea8
	csrr x29, 0xea8	// Read CSR
	li x21, -1
	csrrw x22, 0xea8, x21	// Write all 1s to CSR
	csrrw x22, 0xea8, x0	// Write all 0s to CSR
	csrrs x22, 0xea8, x21	// Set all CSR bits
	csrrc x22, 0xea8, x21	// Clear all CSR bits
	csrrw x22, 0xea8, x29	// Restore CSR

// Testing CSR 0xea9
	csrr x13, 0xea9	// Read CSR
	li x5, -1
	csrrw x15, 0xea9, x5	// Write all 1s to CSR
	csrrw x15, 0xea9, x0	// Write all 0s to CSR
	csrrs x15, 0xea9, x5	// Set all CSR bits
	csrrc x15, 0xea9, x5	// Clear all CSR bits
	csrrw x15, 0xea9, x13	// Restore CSR

// Testing CSR 0xeaa
	csrr x4, 0xeaa	// Read CSR
	li x31, -1
	csrrw x8, 0xeaa, x31	// Write all 1s to CSR
	csrrw x8, 0xeaa, x0	// Write all 0s to CSR
	csrrs x8, 0xeaa, x31	// Set all CSR bits
	csrrc x8, 0xeaa, x31	// Clear all CSR bits
	csrrw x8, 0xeaa, x4	// Restore CSR

// Testing CSR 0xeab
	csrr x15, 0xeab	// Read CSR
	li x21, -1
	csrrw x13, 0xeab, x21	// Write all 1s to CSR
	csrrw x13, 0xeab, x0	// Write all 0s to CSR
	csrrs x13, 0xeab, x21	// Set all CSR bits
	csrrc x13, 0xeab, x21	// Clear all CSR bits
	csrrw x13, 0xeab, x15	// Restore CSR

// Testing CSR 0xeac
	csrr x22, 0xeac	// Read CSR
	li x25, -1
	csrrw x7, 0xeac, x25	// Write all 1s to CSR
	csrrw x7, 0xeac, x0	// Write all 0s to CSR
	csrrs x7, 0xeac, x25	// Set all CSR bits
	csrrc x7, 0xeac, x25	// Clear all CSR bits
	csrrw x7, 0xeac, x22	// Restore CSR

// Testing CSR 0xead
	csrr x27, 0xead	// Read CSR
	li x22, -1
	csrrw x20, 0xead, x22	// Write all 1s to CSR
	csrrw x20, 0xead, x0	// Write all 0s to CSR
	csrrs x20, 0xead, x22	// Set all CSR bits
	csrrc x20, 0xead, x22	// Clear all CSR bits
	csrrw x20, 0xead, x27	// Restore CSR

// Testing CSR 0xeae
	csrr x14, 0xeae	// Read CSR
	li x2, -1
	csrrw x8, 0xeae, x2	// Write all 1s to CSR
	csrrw x8, 0xeae, x0	// Write all 0s to CSR
	csrrs x8, 0xeae, x2	// Set all CSR bits
	csrrc x8, 0xeae, x2	// Clear all CSR bits
	csrrw x8, 0xeae, x14	// Restore CSR

// Testing CSR 0xeaf
	csrr x6, 0xeaf	// Read CSR
	li x21, -1
	csrrw x5, 0xeaf, x21	// Write all 1s to CSR
	csrrw x5, 0xeaf, x0	// Write all 0s to CSR
	csrrs x5, 0xeaf, x21	// Set all CSR bits
	csrrc x5, 0xeaf, x21	// Clear all CSR bits
	csrrw x5, 0xeaf, x6	// Restore CSR

// Testing CSR 0xeb0
	csrr x12, 0xeb0	// Read CSR
	li x22, -1
	csrrw x6, 0xeb0, x22	// Write all 1s to CSR
	csrrw x6, 0xeb0, x0	// Write all 0s to CSR
	csrrs x6, 0xeb0, x22	// Set all CSR bits
	csrrc x6, 0xeb0, x22	// Clear all CSR bits
	csrrw x6, 0xeb0, x12	// Restore CSR

// Testing CSR 0xeb1
	csrr x4, 0xeb1	// Read CSR
	li x20, -1
	csrrw x29, 0xeb1, x20	// Write all 1s to CSR
	csrrw x29, 0xeb1, x0	// Write all 0s to CSR
	csrrs x29, 0xeb1, x20	// Set all CSR bits
	csrrc x29, 0xeb1, x20	// Clear all CSR bits
	csrrw x29, 0xeb1, x4	// Restore CSR

// Testing CSR 0xeb2
	csrr x12, 0xeb2	// Read CSR
	li x29, -1
	csrrw x4, 0xeb2, x29	// Write all 1s to CSR
	csrrw x4, 0xeb2, x0	// Write all 0s to CSR
	csrrs x4, 0xeb2, x29	// Set all CSR bits
	csrrc x4, 0xeb2, x29	// Clear all CSR bits
	csrrw x4, 0xeb2, x12	// Restore CSR

// Testing CSR 0xeb3
	csrr x24, 0xeb3	// Read CSR
	li x22, -1
	csrrw x11, 0xeb3, x22	// Write all 1s to CSR
	csrrw x11, 0xeb3, x0	// Write all 0s to CSR
	csrrs x11, 0xeb3, x22	// Set all CSR bits
	csrrc x11, 0xeb3, x22	// Clear all CSR bits
	csrrw x11, 0xeb3, x24	// Restore CSR

// Testing CSR 0xeb4
	csrr x5, 0xeb4	// Read CSR
	li x11, -1
	csrrw x31, 0xeb4, x11	// Write all 1s to CSR
	csrrw x31, 0xeb4, x0	// Write all 0s to CSR
	csrrs x31, 0xeb4, x11	// Set all CSR bits
	csrrc x31, 0xeb4, x11	// Clear all CSR bits
	csrrw x31, 0xeb4, x5	// Restore CSR

// Testing CSR 0xeb5
	csrr x19, 0xeb5	// Read CSR
	li x30, -1
	csrrw x10, 0xeb5, x30	// Write all 1s to CSR
	csrrw x10, 0xeb5, x0	// Write all 0s to CSR
	csrrs x10, 0xeb5, x30	// Set all CSR bits
	csrrc x10, 0xeb5, x30	// Clear all CSR bits
	csrrw x10, 0xeb5, x19	// Restore CSR

// Testing CSR 0xeb6
	csrr x17, 0xeb6	// Read CSR
	li x23, -1
	csrrw x8, 0xeb6, x23	// Write all 1s to CSR
	csrrw x8, 0xeb6, x0	// Write all 0s to CSR
	csrrs x8, 0xeb6, x23	// Set all CSR bits
	csrrc x8, 0xeb6, x23	// Clear all CSR bits
	csrrw x8, 0xeb6, x17	// Restore CSR

// Testing CSR 0xeb7
	csrr x15, 0xeb7	// Read CSR
	li x24, -1
	csrrw x10, 0xeb7, x24	// Write all 1s to CSR
	csrrw x10, 0xeb7, x0	// Write all 0s to CSR
	csrrs x10, 0xeb7, x24	// Set all CSR bits
	csrrc x10, 0xeb7, x24	// Clear all CSR bits
	csrrw x10, 0xeb7, x15	// Restore CSR

// Testing CSR 0xeb8
	csrr x14, 0xeb8	// Read CSR
	li x1, -1
	csrrw x27, 0xeb8, x1	// Write all 1s to CSR
	csrrw x27, 0xeb8, x0	// Write all 0s to CSR
	csrrs x27, 0xeb8, x1	// Set all CSR bits
	csrrc x27, 0xeb8, x1	// Clear all CSR bits
	csrrw x27, 0xeb8, x14	// Restore CSR

// Testing CSR 0xeb9
	csrr x28, 0xeb9	// Read CSR
	li x26, -1
	csrrw x16, 0xeb9, x26	// Write all 1s to CSR
	csrrw x16, 0xeb9, x0	// Write all 0s to CSR
	csrrs x16, 0xeb9, x26	// Set all CSR bits
	csrrc x16, 0xeb9, x26	// Clear all CSR bits
	csrrw x16, 0xeb9, x28	// Restore CSR

// Testing CSR 0xeba
	csrr x1, 0xeba	// Read CSR
	li x12, -1
	csrrw x16, 0xeba, x12	// Write all 1s to CSR
	csrrw x16, 0xeba, x0	// Write all 0s to CSR
	csrrs x16, 0xeba, x12	// Set all CSR bits
	csrrc x16, 0xeba, x12	// Clear all CSR bits
	csrrw x16, 0xeba, x1	// Restore CSR

// Testing CSR 0xebb
	csrr x26, 0xebb	// Read CSR
	li x25, -1
	csrrw x29, 0xebb, x25	// Write all 1s to CSR
	csrrw x29, 0xebb, x0	// Write all 0s to CSR
	csrrs x29, 0xebb, x25	// Set all CSR bits
	csrrc x29, 0xebb, x25	// Clear all CSR bits
	csrrw x29, 0xebb, x26	// Restore CSR

// Testing CSR 0xebc
	csrr x18, 0xebc	// Read CSR
	li x5, -1
	csrrw x13, 0xebc, x5	// Write all 1s to CSR
	csrrw x13, 0xebc, x0	// Write all 0s to CSR
	csrrs x13, 0xebc, x5	// Set all CSR bits
	csrrc x13, 0xebc, x5	// Clear all CSR bits
	csrrw x13, 0xebc, x18	// Restore CSR

// Testing CSR 0xebd
	csrr x24, 0xebd	// Read CSR
	li x13, -1
	csrrw x14, 0xebd, x13	// Write all 1s to CSR
	csrrw x14, 0xebd, x0	// Write all 0s to CSR
	csrrs x14, 0xebd, x13	// Set all CSR bits
	csrrc x14, 0xebd, x13	// Clear all CSR bits
	csrrw x14, 0xebd, x24	// Restore CSR

// Testing CSR 0xebe
	csrr x26, 0xebe	// Read CSR
	li x21, -1
	csrrw x27, 0xebe, x21	// Write all 1s to CSR
	csrrw x27, 0xebe, x0	// Write all 0s to CSR
	csrrs x27, 0xebe, x21	// Set all CSR bits
	csrrc x27, 0xebe, x21	// Clear all CSR bits
	csrrw x27, 0xebe, x26	// Restore CSR

// Testing CSR 0xebf
	csrr x26, 0xebf	// Read CSR
	li x2, -1
	csrrw x4, 0xebf, x2	// Write all 1s to CSR
	csrrw x4, 0xebf, x0	// Write all 0s to CSR
	csrrs x4, 0xebf, x2	// Set all CSR bits
	csrrc x4, 0xebf, x2	// Clear all CSR bits
	csrrw x4, 0xebf, x26	// Restore CSR

// Testing CSR 0xec0
	csrr x11, 0xec0	// Read CSR
	li x30, -1
	csrrw x3, 0xec0, x30	// Write all 1s to CSR
	csrrw x3, 0xec0, x0	// Write all 0s to CSR
	csrrs x3, 0xec0, x30	// Set all CSR bits
	csrrc x3, 0xec0, x30	// Clear all CSR bits
	csrrw x3, 0xec0, x11	// Restore CSR

// Testing CSR 0xec1
	csrr x10, 0xec1	// Read CSR
	li x1, -1
	csrrw x13, 0xec1, x1	// Write all 1s to CSR
	csrrw x13, 0xec1, x0	// Write all 0s to CSR
	csrrs x13, 0xec1, x1	// Set all CSR bits
	csrrc x13, 0xec1, x1	// Clear all CSR bits
	csrrw x13, 0xec1, x10	// Restore CSR

// Testing CSR 0xec2
	csrr x3, 0xec2	// Read CSR
	li x18, -1
	csrrw x19, 0xec2, x18	// Write all 1s to CSR
	csrrw x19, 0xec2, x0	// Write all 0s to CSR
	csrrs x19, 0xec2, x18	// Set all CSR bits
	csrrc x19, 0xec2, x18	// Clear all CSR bits
	csrrw x19, 0xec2, x3	// Restore CSR

// Testing CSR 0xec3
	csrr x17, 0xec3	// Read CSR
	li x2, -1
	csrrw x13, 0xec3, x2	// Write all 1s to CSR
	csrrw x13, 0xec3, x0	// Write all 0s to CSR
	csrrs x13, 0xec3, x2	// Set all CSR bits
	csrrc x13, 0xec3, x2	// Clear all CSR bits
	csrrw x13, 0xec3, x17	// Restore CSR

// Testing CSR 0xec4
	csrr x29, 0xec4	// Read CSR
	li x18, -1
	csrrw x30, 0xec4, x18	// Write all 1s to CSR
	csrrw x30, 0xec4, x0	// Write all 0s to CSR
	csrrs x30, 0xec4, x18	// Set all CSR bits
	csrrc x30, 0xec4, x18	// Clear all CSR bits
	csrrw x30, 0xec4, x29	// Restore CSR

// Testing CSR 0xec5
	csrr x25, 0xec5	// Read CSR
	li x6, -1
	csrrw x5, 0xec5, x6	// Write all 1s to CSR
	csrrw x5, 0xec5, x0	// Write all 0s to CSR
	csrrs x5, 0xec5, x6	// Set all CSR bits
	csrrc x5, 0xec5, x6	// Clear all CSR bits
	csrrw x5, 0xec5, x25	// Restore CSR

// Testing CSR 0xec6
	csrr x8, 0xec6	// Read CSR
	li x13, -1
	csrrw x29, 0xec6, x13	// Write all 1s to CSR
	csrrw x29, 0xec6, x0	// Write all 0s to CSR
	csrrs x29, 0xec6, x13	// Set all CSR bits
	csrrc x29, 0xec6, x13	// Clear all CSR bits
	csrrw x29, 0xec6, x8	// Restore CSR

// Testing CSR 0xec7
	csrr x10, 0xec7	// Read CSR
	li x17, -1
	csrrw x14, 0xec7, x17	// Write all 1s to CSR
	csrrw x14, 0xec7, x0	// Write all 0s to CSR
	csrrs x14, 0xec7, x17	// Set all CSR bits
	csrrc x14, 0xec7, x17	// Clear all CSR bits
	csrrw x14, 0xec7, x10	// Restore CSR

// Testing CSR 0xec8
	csrr x9, 0xec8	// Read CSR
	li x14, -1
	csrrw x29, 0xec8, x14	// Write all 1s to CSR
	csrrw x29, 0xec8, x0	// Write all 0s to CSR
	csrrs x29, 0xec8, x14	// Set all CSR bits
	csrrc x29, 0xec8, x14	// Clear all CSR bits
	csrrw x29, 0xec8, x9	// Restore CSR

// Testing CSR 0xec9
	csrr x28, 0xec9	// Read CSR
	li x30, -1
	csrrw x24, 0xec9, x30	// Write all 1s to CSR
	csrrw x24, 0xec9, x0	// Write all 0s to CSR
	csrrs x24, 0xec9, x30	// Set all CSR bits
	csrrc x24, 0xec9, x30	// Clear all CSR bits
	csrrw x24, 0xec9, x28	// Restore CSR

// Testing CSR 0xeca
	csrr x16, 0xeca	// Read CSR
	li x5, -1
	csrrw x18, 0xeca, x5	// Write all 1s to CSR
	csrrw x18, 0xeca, x0	// Write all 0s to CSR
	csrrs x18, 0xeca, x5	// Set all CSR bits
	csrrc x18, 0xeca, x5	// Clear all CSR bits
	csrrw x18, 0xeca, x16	// Restore CSR

// Testing CSR 0xecb
	csrr x12, 0xecb	// Read CSR
	li x17, -1
	csrrw x25, 0xecb, x17	// Write all 1s to CSR
	csrrw x25, 0xecb, x0	// Write all 0s to CSR
	csrrs x25, 0xecb, x17	// Set all CSR bits
	csrrc x25, 0xecb, x17	// Clear all CSR bits
	csrrw x25, 0xecb, x12	// Restore CSR

// Testing CSR 0xecc
	csrr x9, 0xecc	// Read CSR
	li x11, -1
	csrrw x18, 0xecc, x11	// Write all 1s to CSR
	csrrw x18, 0xecc, x0	// Write all 0s to CSR
	csrrs x18, 0xecc, x11	// Set all CSR bits
	csrrc x18, 0xecc, x11	// Clear all CSR bits
	csrrw x18, 0xecc, x9	// Restore CSR

// Testing CSR 0xecd
	csrr x17, 0xecd	// Read CSR
	li x31, -1
	csrrw x14, 0xecd, x31	// Write all 1s to CSR
	csrrw x14, 0xecd, x0	// Write all 0s to CSR
	csrrs x14, 0xecd, x31	// Set all CSR bits
	csrrc x14, 0xecd, x31	// Clear all CSR bits
	csrrw x14, 0xecd, x17	// Restore CSR

// Testing CSR 0xece
	csrr x12, 0xece	// Read CSR
	li x8, -1
	csrrw x25, 0xece, x8	// Write all 1s to CSR
	csrrw x25, 0xece, x0	// Write all 0s to CSR
	csrrs x25, 0xece, x8	// Set all CSR bits
	csrrc x25, 0xece, x8	// Clear all CSR bits
	csrrw x25, 0xece, x12	// Restore CSR

// Testing CSR 0xecf
	csrr x21, 0xecf	// Read CSR
	li x7, -1
	csrrw x1, 0xecf, x7	// Write all 1s to CSR
	csrrw x1, 0xecf, x0	// Write all 0s to CSR
	csrrs x1, 0xecf, x7	// Set all CSR bits
	csrrc x1, 0xecf, x7	// Clear all CSR bits
	csrrw x1, 0xecf, x21	// Restore CSR

// Testing CSR 0xed0
	csrr x3, 0xed0	// Read CSR
	li x13, -1
	csrrw x27, 0xed0, x13	// Write all 1s to CSR
	csrrw x27, 0xed0, x0	// Write all 0s to CSR
	csrrs x27, 0xed0, x13	// Set all CSR bits
	csrrc x27, 0xed0, x13	// Clear all CSR bits
	csrrw x27, 0xed0, x3	// Restore CSR

// Testing CSR 0xed1
	csrr x2, 0xed1	// Read CSR
	li x23, -1
	csrrw x31, 0xed1, x23	// Write all 1s to CSR
	csrrw x31, 0xed1, x0	// Write all 0s to CSR
	csrrs x31, 0xed1, x23	// Set all CSR bits
	csrrc x31, 0xed1, x23	// Clear all CSR bits
	csrrw x31, 0xed1, x2	// Restore CSR

// Testing CSR 0xed2
	csrr x19, 0xed2	// Read CSR
	li x27, -1
	csrrw x18, 0xed2, x27	// Write all 1s to CSR
	csrrw x18, 0xed2, x0	// Write all 0s to CSR
	csrrs x18, 0xed2, x27	// Set all CSR bits
	csrrc x18, 0xed2, x27	// Clear all CSR bits
	csrrw x18, 0xed2, x19	// Restore CSR

// Testing CSR 0xed3
	csrr x10, 0xed3	// Read CSR
	li x27, -1
	csrrw x25, 0xed3, x27	// Write all 1s to CSR
	csrrw x25, 0xed3, x0	// Write all 0s to CSR
	csrrs x25, 0xed3, x27	// Set all CSR bits
	csrrc x25, 0xed3, x27	// Clear all CSR bits
	csrrw x25, 0xed3, x10	// Restore CSR

// Testing CSR 0xed4
	csrr x21, 0xed4	// Read CSR
	li x16, -1
	csrrw x15, 0xed4, x16	// Write all 1s to CSR
	csrrw x15, 0xed4, x0	// Write all 0s to CSR
	csrrs x15, 0xed4, x16	// Set all CSR bits
	csrrc x15, 0xed4, x16	// Clear all CSR bits
	csrrw x15, 0xed4, x21	// Restore CSR

// Testing CSR 0xed5
	csrr x31, 0xed5	// Read CSR
	li x15, -1
	csrrw x22, 0xed5, x15	// Write all 1s to CSR
	csrrw x22, 0xed5, x0	// Write all 0s to CSR
	csrrs x22, 0xed5, x15	// Set all CSR bits
	csrrc x22, 0xed5, x15	// Clear all CSR bits
	csrrw x22, 0xed5, x31	// Restore CSR

// Testing CSR 0xed6
	csrr x4, 0xed6	// Read CSR
	li x23, -1
	csrrw x5, 0xed6, x23	// Write all 1s to CSR
	csrrw x5, 0xed6, x0	// Write all 0s to CSR
	csrrs x5, 0xed6, x23	// Set all CSR bits
	csrrc x5, 0xed6, x23	// Clear all CSR bits
	csrrw x5, 0xed6, x4	// Restore CSR

// Testing CSR 0xed7
	csrr x27, 0xed7	// Read CSR
	li x9, -1
	csrrw x20, 0xed7, x9	// Write all 1s to CSR
	csrrw x20, 0xed7, x0	// Write all 0s to CSR
	csrrs x20, 0xed7, x9	// Set all CSR bits
	csrrc x20, 0xed7, x9	// Clear all CSR bits
	csrrw x20, 0xed7, x27	// Restore CSR

// Testing CSR 0xed8
	csrr x12, 0xed8	// Read CSR
	li x29, -1
	csrrw x27, 0xed8, x29	// Write all 1s to CSR
	csrrw x27, 0xed8, x0	// Write all 0s to CSR
	csrrs x27, 0xed8, x29	// Set all CSR bits
	csrrc x27, 0xed8, x29	// Clear all CSR bits
	csrrw x27, 0xed8, x12	// Restore CSR

// Testing CSR 0xed9
	csrr x7, 0xed9	// Read CSR
	li x8, -1
	csrrw x5, 0xed9, x8	// Write all 1s to CSR
	csrrw x5, 0xed9, x0	// Write all 0s to CSR
	csrrs x5, 0xed9, x8	// Set all CSR bits
	csrrc x5, 0xed9, x8	// Clear all CSR bits
	csrrw x5, 0xed9, x7	// Restore CSR

// Testing CSR 0xeda
	csrr x17, 0xeda	// Read CSR
	li x13, -1
	csrrw x2, 0xeda, x13	// Write all 1s to CSR
	csrrw x2, 0xeda, x0	// Write all 0s to CSR
	csrrs x2, 0xeda, x13	// Set all CSR bits
	csrrc x2, 0xeda, x13	// Clear all CSR bits
	csrrw x2, 0xeda, x17	// Restore CSR

// Testing CSR 0xedb
	csrr x10, 0xedb	// Read CSR
	li x4, -1
	csrrw x14, 0xedb, x4	// Write all 1s to CSR
	csrrw x14, 0xedb, x0	// Write all 0s to CSR
	csrrs x14, 0xedb, x4	// Set all CSR bits
	csrrc x14, 0xedb, x4	// Clear all CSR bits
	csrrw x14, 0xedb, x10	// Restore CSR

// Testing CSR 0xedc
	csrr x16, 0xedc	// Read CSR
	li x13, -1
	csrrw x30, 0xedc, x13	// Write all 1s to CSR
	csrrw x30, 0xedc, x0	// Write all 0s to CSR
	csrrs x30, 0xedc, x13	// Set all CSR bits
	csrrc x30, 0xedc, x13	// Clear all CSR bits
	csrrw x30, 0xedc, x16	// Restore CSR

// Testing CSR 0xedd
	csrr x4, 0xedd	// Read CSR
	li x15, -1
	csrrw x10, 0xedd, x15	// Write all 1s to CSR
	csrrw x10, 0xedd, x0	// Write all 0s to CSR
	csrrs x10, 0xedd, x15	// Set all CSR bits
	csrrc x10, 0xedd, x15	// Clear all CSR bits
	csrrw x10, 0xedd, x4	// Restore CSR

// Testing CSR 0xede
	csrr x2, 0xede	// Read CSR
	li x9, -1
	csrrw x19, 0xede, x9	// Write all 1s to CSR
	csrrw x19, 0xede, x0	// Write all 0s to CSR
	csrrs x19, 0xede, x9	// Set all CSR bits
	csrrc x19, 0xede, x9	// Clear all CSR bits
	csrrw x19, 0xede, x2	// Restore CSR

// Testing CSR 0xedf
	csrr x3, 0xedf	// Read CSR
	li x14, -1
	csrrw x30, 0xedf, x14	// Write all 1s to CSR
	csrrw x30, 0xedf, x0	// Write all 0s to CSR
	csrrs x30, 0xedf, x14	// Set all CSR bits
	csrrc x30, 0xedf, x14	// Clear all CSR bits
	csrrw x30, 0xedf, x3	// Restore CSR

// Testing CSR 0xee0
	csrr x18, 0xee0	// Read CSR
	li x10, -1
	csrrw x27, 0xee0, x10	// Write all 1s to CSR
	csrrw x27, 0xee0, x0	// Write all 0s to CSR
	csrrs x27, 0xee0, x10	// Set all CSR bits
	csrrc x27, 0xee0, x10	// Clear all CSR bits
	csrrw x27, 0xee0, x18	// Restore CSR

// Testing CSR 0xee1
	csrr x12, 0xee1	// Read CSR
	li x4, -1
	csrrw x31, 0xee1, x4	// Write all 1s to CSR
	csrrw x31, 0xee1, x0	// Write all 0s to CSR
	csrrs x31, 0xee1, x4	// Set all CSR bits
	csrrc x31, 0xee1, x4	// Clear all CSR bits
	csrrw x31, 0xee1, x12	// Restore CSR

// Testing CSR 0xee2
	csrr x27, 0xee2	// Read CSR
	li x23, -1
	csrrw x22, 0xee2, x23	// Write all 1s to CSR
	csrrw x22, 0xee2, x0	// Write all 0s to CSR
	csrrs x22, 0xee2, x23	// Set all CSR bits
	csrrc x22, 0xee2, x23	// Clear all CSR bits
	csrrw x22, 0xee2, x27	// Restore CSR

// Testing CSR 0xee3
	csrr x1, 0xee3	// Read CSR
	li x16, -1
	csrrw x26, 0xee3, x16	// Write all 1s to CSR
	csrrw x26, 0xee3, x0	// Write all 0s to CSR
	csrrs x26, 0xee3, x16	// Set all CSR bits
	csrrc x26, 0xee3, x16	// Clear all CSR bits
	csrrw x26, 0xee3, x1	// Restore CSR

// Testing CSR 0xee4
	csrr x23, 0xee4	// Read CSR
	li x8, -1
	csrrw x26, 0xee4, x8	// Write all 1s to CSR
	csrrw x26, 0xee4, x0	// Write all 0s to CSR
	csrrs x26, 0xee4, x8	// Set all CSR bits
	csrrc x26, 0xee4, x8	// Clear all CSR bits
	csrrw x26, 0xee4, x23	// Restore CSR

// Testing CSR 0xee5
	csrr x11, 0xee5	// Read CSR
	li x24, -1
	csrrw x29, 0xee5, x24	// Write all 1s to CSR
	csrrw x29, 0xee5, x0	// Write all 0s to CSR
	csrrs x29, 0xee5, x24	// Set all CSR bits
	csrrc x29, 0xee5, x24	// Clear all CSR bits
	csrrw x29, 0xee5, x11	// Restore CSR

// Testing CSR 0xee6
	csrr x15, 0xee6	// Read CSR
	li x29, -1
	csrrw x18, 0xee6, x29	// Write all 1s to CSR
	csrrw x18, 0xee6, x0	// Write all 0s to CSR
	csrrs x18, 0xee6, x29	// Set all CSR bits
	csrrc x18, 0xee6, x29	// Clear all CSR bits
	csrrw x18, 0xee6, x15	// Restore CSR

// Testing CSR 0xee7
	csrr x12, 0xee7	// Read CSR
	li x5, -1
	csrrw x22, 0xee7, x5	// Write all 1s to CSR
	csrrw x22, 0xee7, x0	// Write all 0s to CSR
	csrrs x22, 0xee7, x5	// Set all CSR bits
	csrrc x22, 0xee7, x5	// Clear all CSR bits
	csrrw x22, 0xee7, x12	// Restore CSR

// Testing CSR 0xee8
	csrr x27, 0xee8	// Read CSR
	li x26, -1
	csrrw x15, 0xee8, x26	// Write all 1s to CSR
	csrrw x15, 0xee8, x0	// Write all 0s to CSR
	csrrs x15, 0xee8, x26	// Set all CSR bits
	csrrc x15, 0xee8, x26	// Clear all CSR bits
	csrrw x15, 0xee8, x27	// Restore CSR

// Testing CSR 0xee9
	csrr x14, 0xee9	// Read CSR
	li x21, -1
	csrrw x24, 0xee9, x21	// Write all 1s to CSR
	csrrw x24, 0xee9, x0	// Write all 0s to CSR
	csrrs x24, 0xee9, x21	// Set all CSR bits
	csrrc x24, 0xee9, x21	// Clear all CSR bits
	csrrw x24, 0xee9, x14	// Restore CSR

// Testing CSR 0xeea
	csrr x4, 0xeea	// Read CSR
	li x29, -1
	csrrw x18, 0xeea, x29	// Write all 1s to CSR
	csrrw x18, 0xeea, x0	// Write all 0s to CSR
	csrrs x18, 0xeea, x29	// Set all CSR bits
	csrrc x18, 0xeea, x29	// Clear all CSR bits
	csrrw x18, 0xeea, x4	// Restore CSR

// Testing CSR 0xeeb
	csrr x27, 0xeeb	// Read CSR
	li x17, -1
	csrrw x6, 0xeeb, x17	// Write all 1s to CSR
	csrrw x6, 0xeeb, x0	// Write all 0s to CSR
	csrrs x6, 0xeeb, x17	// Set all CSR bits
	csrrc x6, 0xeeb, x17	// Clear all CSR bits
	csrrw x6, 0xeeb, x27	// Restore CSR

// Testing CSR 0xeec
	csrr x23, 0xeec	// Read CSR
	li x11, -1
	csrrw x24, 0xeec, x11	// Write all 1s to CSR
	csrrw x24, 0xeec, x0	// Write all 0s to CSR
	csrrs x24, 0xeec, x11	// Set all CSR bits
	csrrc x24, 0xeec, x11	// Clear all CSR bits
	csrrw x24, 0xeec, x23	// Restore CSR

// Testing CSR 0xeed
	csrr x19, 0xeed	// Read CSR
	li x5, -1
	csrrw x11, 0xeed, x5	// Write all 1s to CSR
	csrrw x11, 0xeed, x0	// Write all 0s to CSR
	csrrs x11, 0xeed, x5	// Set all CSR bits
	csrrc x11, 0xeed, x5	// Clear all CSR bits
	csrrw x11, 0xeed, x19	// Restore CSR

// Testing CSR 0xeee
	csrr x24, 0xeee	// Read CSR
	li x14, -1
	csrrw x2, 0xeee, x14	// Write all 1s to CSR
	csrrw x2, 0xeee, x0	// Write all 0s to CSR
	csrrs x2, 0xeee, x14	// Set all CSR bits
	csrrc x2, 0xeee, x14	// Clear all CSR bits
	csrrw x2, 0xeee, x24	// Restore CSR

// Testing CSR 0xeef
	csrr x8, 0xeef	// Read CSR
	li x14, -1
	csrrw x2, 0xeef, x14	// Write all 1s to CSR
	csrrw x2, 0xeef, x0	// Write all 0s to CSR
	csrrs x2, 0xeef, x14	// Set all CSR bits
	csrrc x2, 0xeef, x14	// Clear all CSR bits
	csrrw x2, 0xeef, x8	// Restore CSR

// Testing CSR 0xef0
	csrr x6, 0xef0	// Read CSR
	li x22, -1
	csrrw x30, 0xef0, x22	// Write all 1s to CSR
	csrrw x30, 0xef0, x0	// Write all 0s to CSR
	csrrs x30, 0xef0, x22	// Set all CSR bits
	csrrc x30, 0xef0, x22	// Clear all CSR bits
	csrrw x30, 0xef0, x6	// Restore CSR

// Testing CSR 0xef1
	csrr x13, 0xef1	// Read CSR
	li x25, -1
	csrrw x22, 0xef1, x25	// Write all 1s to CSR
	csrrw x22, 0xef1, x0	// Write all 0s to CSR
	csrrs x22, 0xef1, x25	// Set all CSR bits
	csrrc x22, 0xef1, x25	// Clear all CSR bits
	csrrw x22, 0xef1, x13	// Restore CSR

// Testing CSR 0xef2
	csrr x26, 0xef2	// Read CSR
	li x8, -1
	csrrw x30, 0xef2, x8	// Write all 1s to CSR
	csrrw x30, 0xef2, x0	// Write all 0s to CSR
	csrrs x30, 0xef2, x8	// Set all CSR bits
	csrrc x30, 0xef2, x8	// Clear all CSR bits
	csrrw x30, 0xef2, x26	// Restore CSR

// Testing CSR 0xef3
	csrr x28, 0xef3	// Read CSR
	li x17, -1
	csrrw x31, 0xef3, x17	// Write all 1s to CSR
	csrrw x31, 0xef3, x0	// Write all 0s to CSR
	csrrs x31, 0xef3, x17	// Set all CSR bits
	csrrc x31, 0xef3, x17	// Clear all CSR bits
	csrrw x31, 0xef3, x28	// Restore CSR

// Testing CSR 0xef4
	csrr x1, 0xef4	// Read CSR
	li x29, -1
	csrrw x16, 0xef4, x29	// Write all 1s to CSR
	csrrw x16, 0xef4, x0	// Write all 0s to CSR
	csrrs x16, 0xef4, x29	// Set all CSR bits
	csrrc x16, 0xef4, x29	// Clear all CSR bits
	csrrw x16, 0xef4, x1	// Restore CSR

// Testing CSR 0xef5
	csrr x12, 0xef5	// Read CSR
	li x20, -1
	csrrw x13, 0xef5, x20	// Write all 1s to CSR
	csrrw x13, 0xef5, x0	// Write all 0s to CSR
	csrrs x13, 0xef5, x20	// Set all CSR bits
	csrrc x13, 0xef5, x20	// Clear all CSR bits
	csrrw x13, 0xef5, x12	// Restore CSR

// Testing CSR 0xef6
	csrr x7, 0xef6	// Read CSR
	li x31, -1
	csrrw x15, 0xef6, x31	// Write all 1s to CSR
	csrrw x15, 0xef6, x0	// Write all 0s to CSR
	csrrs x15, 0xef6, x31	// Set all CSR bits
	csrrc x15, 0xef6, x31	// Clear all CSR bits
	csrrw x15, 0xef6, x7	// Restore CSR

// Testing CSR 0xef7
	csrr x1, 0xef7	// Read CSR
	li x16, -1
	csrrw x10, 0xef7, x16	// Write all 1s to CSR
	csrrw x10, 0xef7, x0	// Write all 0s to CSR
	csrrs x10, 0xef7, x16	// Set all CSR bits
	csrrc x10, 0xef7, x16	// Clear all CSR bits
	csrrw x10, 0xef7, x1	// Restore CSR

// Testing CSR 0xef8
	csrr x30, 0xef8	// Read CSR
	li x22, -1
	csrrw x24, 0xef8, x22	// Write all 1s to CSR
	csrrw x24, 0xef8, x0	// Write all 0s to CSR
	csrrs x24, 0xef8, x22	// Set all CSR bits
	csrrc x24, 0xef8, x22	// Clear all CSR bits
	csrrw x24, 0xef8, x30	// Restore CSR

// Testing CSR 0xef9
	csrr x16, 0xef9	// Read CSR
	li x19, -1
	csrrw x29, 0xef9, x19	// Write all 1s to CSR
	csrrw x29, 0xef9, x0	// Write all 0s to CSR
	csrrs x29, 0xef9, x19	// Set all CSR bits
	csrrc x29, 0xef9, x19	// Clear all CSR bits
	csrrw x29, 0xef9, x16	// Restore CSR

// Testing CSR 0xefa
	csrr x23, 0xefa	// Read CSR
	li x6, -1
	csrrw x12, 0xefa, x6	// Write all 1s to CSR
	csrrw x12, 0xefa, x0	// Write all 0s to CSR
	csrrs x12, 0xefa, x6	// Set all CSR bits
	csrrc x12, 0xefa, x6	// Clear all CSR bits
	csrrw x12, 0xefa, x23	// Restore CSR

// Testing CSR 0xefb
	csrr x24, 0xefb	// Read CSR
	li x8, -1
	csrrw x28, 0xefb, x8	// Write all 1s to CSR
	csrrw x28, 0xefb, x0	// Write all 0s to CSR
	csrrs x28, 0xefb, x8	// Set all CSR bits
	csrrc x28, 0xefb, x8	// Clear all CSR bits
	csrrw x28, 0xefb, x24	// Restore CSR

// Testing CSR 0xefc
	csrr x29, 0xefc	// Read CSR
	li x5, -1
	csrrw x16, 0xefc, x5	// Write all 1s to CSR
	csrrw x16, 0xefc, x0	// Write all 0s to CSR
	csrrs x16, 0xefc, x5	// Set all CSR bits
	csrrc x16, 0xefc, x5	// Clear all CSR bits
	csrrw x16, 0xefc, x29	// Restore CSR

// Testing CSR 0xefd
	csrr x1, 0xefd	// Read CSR
	li x17, -1
	csrrw x24, 0xefd, x17	// Write all 1s to CSR
	csrrw x24, 0xefd, x0	// Write all 0s to CSR
	csrrs x24, 0xefd, x17	// Set all CSR bits
	csrrc x24, 0xefd, x17	// Clear all CSR bits
	csrrw x24, 0xefd, x1	// Restore CSR

// Testing CSR 0xefe
	csrr x26, 0xefe	// Read CSR
	li x19, -1
	csrrw x30, 0xefe, x19	// Write all 1s to CSR
	csrrw x30, 0xefe, x0	// Write all 0s to CSR
	csrrs x30, 0xefe, x19	// Set all CSR bits
	csrrc x30, 0xefe, x19	// Clear all CSR bits
	csrrw x30, 0xefe, x26	// Restore CSR

// Testing CSR 0xeff
	csrr x6, 0xeff	// Read CSR
	li x20, -1
	csrrw x27, 0xeff, x20	// Write all 1s to CSR
	csrrw x27, 0xeff, x0	// Write all 0s to CSR
	csrrs x27, 0xeff, x20	// Set all CSR bits
	csrrc x27, 0xeff, x20	// Clear all CSR bits
	csrrw x27, 0xeff, x6	// Restore CSR

// Testing CSR 0xf00
	csrr x14, 0xf00	// Read CSR
	li x23, -1
	csrrw x20, 0xf00, x23	// Write all 1s to CSR
	csrrw x20, 0xf00, x0	// Write all 0s to CSR
	csrrs x20, 0xf00, x23	// Set all CSR bits
	csrrc x20, 0xf00, x23	// Clear all CSR bits
	csrrw x20, 0xf00, x14	// Restore CSR

// Testing CSR 0xf01
	csrr x11, 0xf01	// Read CSR
	li x19, -1
	csrrw x30, 0xf01, x19	// Write all 1s to CSR
	csrrw x30, 0xf01, x0	// Write all 0s to CSR
	csrrs x30, 0xf01, x19	// Set all CSR bits
	csrrc x30, 0xf01, x19	// Clear all CSR bits
	csrrw x30, 0xf01, x11	// Restore CSR

// Testing CSR 0xf02
	csrr x6, 0xf02	// Read CSR
	li x25, -1
	csrrw x24, 0xf02, x25	// Write all 1s to CSR
	csrrw x24, 0xf02, x0	// Write all 0s to CSR
	csrrs x24, 0xf02, x25	// Set all CSR bits
	csrrc x24, 0xf02, x25	// Clear all CSR bits
	csrrw x24, 0xf02, x6	// Restore CSR

// Testing CSR 0xf03
	csrr x4, 0xf03	// Read CSR
	li x9, -1
	csrrw x10, 0xf03, x9	// Write all 1s to CSR
	csrrw x10, 0xf03, x0	// Write all 0s to CSR
	csrrs x10, 0xf03, x9	// Set all CSR bits
	csrrc x10, 0xf03, x9	// Clear all CSR bits
	csrrw x10, 0xf03, x4	// Restore CSR

// Testing CSR 0xf04
	csrr x19, 0xf04	// Read CSR
	li x16, -1
	csrrw x17, 0xf04, x16	// Write all 1s to CSR
	csrrw x17, 0xf04, x0	// Write all 0s to CSR
	csrrs x17, 0xf04, x16	// Set all CSR bits
	csrrc x17, 0xf04, x16	// Clear all CSR bits
	csrrw x17, 0xf04, x19	// Restore CSR

// Testing CSR 0xf05
	csrr x31, 0xf05	// Read CSR
	li x8, -1
	csrrw x25, 0xf05, x8	// Write all 1s to CSR
	csrrw x25, 0xf05, x0	// Write all 0s to CSR
	csrrs x25, 0xf05, x8	// Set all CSR bits
	csrrc x25, 0xf05, x8	// Clear all CSR bits
	csrrw x25, 0xf05, x31	// Restore CSR

// Testing CSR 0xf06
	csrr x5, 0xf06	// Read CSR
	li x24, -1
	csrrw x20, 0xf06, x24	// Write all 1s to CSR
	csrrw x20, 0xf06, x0	// Write all 0s to CSR
	csrrs x20, 0xf06, x24	// Set all CSR bits
	csrrc x20, 0xf06, x24	// Clear all CSR bits
	csrrw x20, 0xf06, x5	// Restore CSR

// Testing CSR 0xf07
	csrr x10, 0xf07	// Read CSR
	li x28, -1
	csrrw x4, 0xf07, x28	// Write all 1s to CSR
	csrrw x4, 0xf07, x0	// Write all 0s to CSR
	csrrs x4, 0xf07, x28	// Set all CSR bits
	csrrc x4, 0xf07, x28	// Clear all CSR bits
	csrrw x4, 0xf07, x10	// Restore CSR

// Testing CSR 0xf08
	csrr x16, 0xf08	// Read CSR
	li x19, -1
	csrrw x11, 0xf08, x19	// Write all 1s to CSR
	csrrw x11, 0xf08, x0	// Write all 0s to CSR
	csrrs x11, 0xf08, x19	// Set all CSR bits
	csrrc x11, 0xf08, x19	// Clear all CSR bits
	csrrw x11, 0xf08, x16	// Restore CSR

// Testing CSR 0xf09
	csrr x5, 0xf09	// Read CSR
	li x23, -1
	csrrw x26, 0xf09, x23	// Write all 1s to CSR
	csrrw x26, 0xf09, x0	// Write all 0s to CSR
	csrrs x26, 0xf09, x23	// Set all CSR bits
	csrrc x26, 0xf09, x23	// Clear all CSR bits
	csrrw x26, 0xf09, x5	// Restore CSR

// Testing CSR 0xf0a
	csrr x31, 0xf0a	// Read CSR
	li x12, -1
	csrrw x29, 0xf0a, x12	// Write all 1s to CSR
	csrrw x29, 0xf0a, x0	// Write all 0s to CSR
	csrrs x29, 0xf0a, x12	// Set all CSR bits
	csrrc x29, 0xf0a, x12	// Clear all CSR bits
	csrrw x29, 0xf0a, x31	// Restore CSR

// Testing CSR 0xf0b
	csrr x4, 0xf0b	// Read CSR
	li x14, -1
	csrrw x19, 0xf0b, x14	// Write all 1s to CSR
	csrrw x19, 0xf0b, x0	// Write all 0s to CSR
	csrrs x19, 0xf0b, x14	// Set all CSR bits
	csrrc x19, 0xf0b, x14	// Clear all CSR bits
	csrrw x19, 0xf0b, x4	// Restore CSR

// Testing CSR 0xf0c
	csrr x17, 0xf0c	// Read CSR
	li x18, -1
	csrrw x2, 0xf0c, x18	// Write all 1s to CSR
	csrrw x2, 0xf0c, x0	// Write all 0s to CSR
	csrrs x2, 0xf0c, x18	// Set all CSR bits
	csrrc x2, 0xf0c, x18	// Clear all CSR bits
	csrrw x2, 0xf0c, x17	// Restore CSR

// Testing CSR 0xf0d
	csrr x25, 0xf0d	// Read CSR
	li x3, -1
	csrrw x11, 0xf0d, x3	// Write all 1s to CSR
	csrrw x11, 0xf0d, x0	// Write all 0s to CSR
	csrrs x11, 0xf0d, x3	// Set all CSR bits
	csrrc x11, 0xf0d, x3	// Clear all CSR bits
	csrrw x11, 0xf0d, x25	// Restore CSR

// Testing CSR 0xf0e
	csrr x14, 0xf0e	// Read CSR
	li x6, -1
	csrrw x21, 0xf0e, x6	// Write all 1s to CSR
	csrrw x21, 0xf0e, x0	// Write all 0s to CSR
	csrrs x21, 0xf0e, x6	// Set all CSR bits
	csrrc x21, 0xf0e, x6	// Clear all CSR bits
	csrrw x21, 0xf0e, x14	// Restore CSR

// Testing CSR 0xf0f
	csrr x10, 0xf0f	// Read CSR
	li x11, -1
	csrrw x6, 0xf0f, x11	// Write all 1s to CSR
	csrrw x6, 0xf0f, x0	// Write all 0s to CSR
	csrrs x6, 0xf0f, x11	// Set all CSR bits
	csrrc x6, 0xf0f, x11	// Clear all CSR bits
	csrrw x6, 0xf0f, x10	// Restore CSR

// Testing CSR 0xf10
	csrr x19, 0xf10	// Read CSR
	li x29, -1
	csrrw x4, 0xf10, x29	// Write all 1s to CSR
	csrrw x4, 0xf10, x0	// Write all 0s to CSR
	csrrs x4, 0xf10, x29	// Set all CSR bits
	csrrc x4, 0xf10, x29	// Clear all CSR bits
	csrrw x4, 0xf10, x19	// Restore CSR

// Testing CSR 0xf11
	csrr x12, 0xf11	// Read CSR
	li x14, -1
	csrrw x17, 0xf11, x14	// Write all 1s to CSR
	csrrw x17, 0xf11, x0	// Write all 0s to CSR
	csrrs x17, 0xf11, x14	// Set all CSR bits
	csrrc x17, 0xf11, x14	// Clear all CSR bits
	csrrw x17, 0xf11, x12	// Restore CSR

// Testing CSR 0xf12
	csrr x14, 0xf12	// Read CSR
	li x27, -1
	csrrw x5, 0xf12, x27	// Write all 1s to CSR
	csrrw x5, 0xf12, x0	// Write all 0s to CSR
	csrrs x5, 0xf12, x27	// Set all CSR bits
	csrrc x5, 0xf12, x27	// Clear all CSR bits
	csrrw x5, 0xf12, x14	// Restore CSR

// Testing CSR 0xf13
	csrr x9, 0xf13	// Read CSR
	li x30, -1
	csrrw x1, 0xf13, x30	// Write all 1s to CSR
	csrrw x1, 0xf13, x0	// Write all 0s to CSR
	csrrs x1, 0xf13, x30	// Set all CSR bits
	csrrc x1, 0xf13, x30	// Clear all CSR bits
	csrrw x1, 0xf13, x9	// Restore CSR

// Testing CSR 0xf14
	csrr x9, 0xf14	// Read CSR
	li x1, -1
	csrrw x18, 0xf14, x1	// Write all 1s to CSR
	csrrw x18, 0xf14, x0	// Write all 0s to CSR
	csrrs x18, 0xf14, x1	// Set all CSR bits
	csrrc x18, 0xf14, x1	// Clear all CSR bits
	csrrw x18, 0xf14, x9	// Restore CSR

// Testing CSR 0xf15
	csrr x14, 0xf15	// Read CSR
	li x20, -1
	csrrw x23, 0xf15, x20	// Write all 1s to CSR
	csrrw x23, 0xf15, x0	// Write all 0s to CSR
	csrrs x23, 0xf15, x20	// Set all CSR bits
	csrrc x23, 0xf15, x20	// Clear all CSR bits
	csrrw x23, 0xf15, x14	// Restore CSR

// Testing CSR 0xf16
	csrr x21, 0xf16	// Read CSR
	li x7, -1
	csrrw x6, 0xf16, x7	// Write all 1s to CSR
	csrrw x6, 0xf16, x0	// Write all 0s to CSR
	csrrs x6, 0xf16, x7	// Set all CSR bits
	csrrc x6, 0xf16, x7	// Clear all CSR bits
	csrrw x6, 0xf16, x21	// Restore CSR

// Testing CSR 0xf17
	csrr x18, 0xf17	// Read CSR
	li x9, -1
	csrrw x20, 0xf17, x9	// Write all 1s to CSR
	csrrw x20, 0xf17, x0	// Write all 0s to CSR
	csrrs x20, 0xf17, x9	// Set all CSR bits
	csrrc x20, 0xf17, x9	// Clear all CSR bits
	csrrw x20, 0xf17, x18	// Restore CSR

// Testing CSR 0xf18
	csrr x11, 0xf18	// Read CSR
	li x1, -1
	csrrw x14, 0xf18, x1	// Write all 1s to CSR
	csrrw x14, 0xf18, x0	// Write all 0s to CSR
	csrrs x14, 0xf18, x1	// Set all CSR bits
	csrrc x14, 0xf18, x1	// Clear all CSR bits
	csrrw x14, 0xf18, x11	// Restore CSR

// Testing CSR 0xf19
	csrr x20, 0xf19	// Read CSR
	li x30, -1
	csrrw x26, 0xf19, x30	// Write all 1s to CSR
	csrrw x26, 0xf19, x0	// Write all 0s to CSR
	csrrs x26, 0xf19, x30	// Set all CSR bits
	csrrc x26, 0xf19, x30	// Clear all CSR bits
	csrrw x26, 0xf19, x20	// Restore CSR

// Testing CSR 0xf1a
	csrr x2, 0xf1a	// Read CSR
	li x28, -1
	csrrw x1, 0xf1a, x28	// Write all 1s to CSR
	csrrw x1, 0xf1a, x0	// Write all 0s to CSR
	csrrs x1, 0xf1a, x28	// Set all CSR bits
	csrrc x1, 0xf1a, x28	// Clear all CSR bits
	csrrw x1, 0xf1a, x2	// Restore CSR

// Testing CSR 0xf1b
	csrr x27, 0xf1b	// Read CSR
	li x26, -1
	csrrw x8, 0xf1b, x26	// Write all 1s to CSR
	csrrw x8, 0xf1b, x0	// Write all 0s to CSR
	csrrs x8, 0xf1b, x26	// Set all CSR bits
	csrrc x8, 0xf1b, x26	// Clear all CSR bits
	csrrw x8, 0xf1b, x27	// Restore CSR

// Testing CSR 0xf1c
	csrr x28, 0xf1c	// Read CSR
	li x22, -1
	csrrw x27, 0xf1c, x22	// Write all 1s to CSR
	csrrw x27, 0xf1c, x0	// Write all 0s to CSR
	csrrs x27, 0xf1c, x22	// Set all CSR bits
	csrrc x27, 0xf1c, x22	// Clear all CSR bits
	csrrw x27, 0xf1c, x28	// Restore CSR

// Testing CSR 0xf1d
	csrr x19, 0xf1d	// Read CSR
	li x24, -1
	csrrw x15, 0xf1d, x24	// Write all 1s to CSR
	csrrw x15, 0xf1d, x0	// Write all 0s to CSR
	csrrs x15, 0xf1d, x24	// Set all CSR bits
	csrrc x15, 0xf1d, x24	// Clear all CSR bits
	csrrw x15, 0xf1d, x19	// Restore CSR

// Testing CSR 0xf1e
	csrr x26, 0xf1e	// Read CSR
	li x28, -1
	csrrw x31, 0xf1e, x28	// Write all 1s to CSR
	csrrw x31, 0xf1e, x0	// Write all 0s to CSR
	csrrs x31, 0xf1e, x28	// Set all CSR bits
	csrrc x31, 0xf1e, x28	// Clear all CSR bits
	csrrw x31, 0xf1e, x26	// Restore CSR

// Testing CSR 0xf1f
	csrr x5, 0xf1f	// Read CSR
	li x18, -1
	csrrw x17, 0xf1f, x18	// Write all 1s to CSR
	csrrw x17, 0xf1f, x0	// Write all 0s to CSR
	csrrs x17, 0xf1f, x18	// Set all CSR bits
	csrrc x17, 0xf1f, x18	// Clear all CSR bits
	csrrw x17, 0xf1f, x5	// Restore CSR

// Testing CSR 0xf20
	csrr x2, 0xf20	// Read CSR
	li x6, -1
	csrrw x29, 0xf20, x6	// Write all 1s to CSR
	csrrw x29, 0xf20, x0	// Write all 0s to CSR
	csrrs x29, 0xf20, x6	// Set all CSR bits
	csrrc x29, 0xf20, x6	// Clear all CSR bits
	csrrw x29, 0xf20, x2	// Restore CSR

// Testing CSR 0xf21
	csrr x12, 0xf21	// Read CSR
	li x30, -1
	csrrw x18, 0xf21, x30	// Write all 1s to CSR
	csrrw x18, 0xf21, x0	// Write all 0s to CSR
	csrrs x18, 0xf21, x30	// Set all CSR bits
	csrrc x18, 0xf21, x30	// Clear all CSR bits
	csrrw x18, 0xf21, x12	// Restore CSR

// Testing CSR 0xf22
	csrr x7, 0xf22	// Read CSR
	li x16, -1
	csrrw x8, 0xf22, x16	// Write all 1s to CSR
	csrrw x8, 0xf22, x0	// Write all 0s to CSR
	csrrs x8, 0xf22, x16	// Set all CSR bits
	csrrc x8, 0xf22, x16	// Clear all CSR bits
	csrrw x8, 0xf22, x7	// Restore CSR

// Testing CSR 0xf23
	csrr x5, 0xf23	// Read CSR
	li x3, -1
	csrrw x22, 0xf23, x3	// Write all 1s to CSR
	csrrw x22, 0xf23, x0	// Write all 0s to CSR
	csrrs x22, 0xf23, x3	// Set all CSR bits
	csrrc x22, 0xf23, x3	// Clear all CSR bits
	csrrw x22, 0xf23, x5	// Restore CSR

// Testing CSR 0xf24
	csrr x23, 0xf24	// Read CSR
	li x12, -1
	csrrw x2, 0xf24, x12	// Write all 1s to CSR
	csrrw x2, 0xf24, x0	// Write all 0s to CSR
	csrrs x2, 0xf24, x12	// Set all CSR bits
	csrrc x2, 0xf24, x12	// Clear all CSR bits
	csrrw x2, 0xf24, x23	// Restore CSR

// Testing CSR 0xf25
	csrr x18, 0xf25	// Read CSR
	li x3, -1
	csrrw x15, 0xf25, x3	// Write all 1s to CSR
	csrrw x15, 0xf25, x0	// Write all 0s to CSR
	csrrs x15, 0xf25, x3	// Set all CSR bits
	csrrc x15, 0xf25, x3	// Clear all CSR bits
	csrrw x15, 0xf25, x18	// Restore CSR

// Testing CSR 0xf26
	csrr x10, 0xf26	// Read CSR
	li x9, -1
	csrrw x20, 0xf26, x9	// Write all 1s to CSR
	csrrw x20, 0xf26, x0	// Write all 0s to CSR
	csrrs x20, 0xf26, x9	// Set all CSR bits
	csrrc x20, 0xf26, x9	// Clear all CSR bits
	csrrw x20, 0xf26, x10	// Restore CSR

// Testing CSR 0xf27
	csrr x14, 0xf27	// Read CSR
	li x20, -1
	csrrw x9, 0xf27, x20	// Write all 1s to CSR
	csrrw x9, 0xf27, x0	// Write all 0s to CSR
	csrrs x9, 0xf27, x20	// Set all CSR bits
	csrrc x9, 0xf27, x20	// Clear all CSR bits
	csrrw x9, 0xf27, x14	// Restore CSR

// Testing CSR 0xf28
	csrr x10, 0xf28	// Read CSR
	li x30, -1
	csrrw x23, 0xf28, x30	// Write all 1s to CSR
	csrrw x23, 0xf28, x0	// Write all 0s to CSR
	csrrs x23, 0xf28, x30	// Set all CSR bits
	csrrc x23, 0xf28, x30	// Clear all CSR bits
	csrrw x23, 0xf28, x10	// Restore CSR

// Testing CSR 0xf29
	csrr x7, 0xf29	// Read CSR
	li x6, -1
	csrrw x27, 0xf29, x6	// Write all 1s to CSR
	csrrw x27, 0xf29, x0	// Write all 0s to CSR
	csrrs x27, 0xf29, x6	// Set all CSR bits
	csrrc x27, 0xf29, x6	// Clear all CSR bits
	csrrw x27, 0xf29, x7	// Restore CSR

// Testing CSR 0xf2a
	csrr x29, 0xf2a	// Read CSR
	li x23, -1
	csrrw x11, 0xf2a, x23	// Write all 1s to CSR
	csrrw x11, 0xf2a, x0	// Write all 0s to CSR
	csrrs x11, 0xf2a, x23	// Set all CSR bits
	csrrc x11, 0xf2a, x23	// Clear all CSR bits
	csrrw x11, 0xf2a, x29	// Restore CSR

// Testing CSR 0xf2b
	csrr x9, 0xf2b	// Read CSR
	li x31, -1
	csrrw x16, 0xf2b, x31	// Write all 1s to CSR
	csrrw x16, 0xf2b, x0	// Write all 0s to CSR
	csrrs x16, 0xf2b, x31	// Set all CSR bits
	csrrc x16, 0xf2b, x31	// Clear all CSR bits
	csrrw x16, 0xf2b, x9	// Restore CSR

// Testing CSR 0xf2c
	csrr x26, 0xf2c	// Read CSR
	li x25, -1
	csrrw x29, 0xf2c, x25	// Write all 1s to CSR
	csrrw x29, 0xf2c, x0	// Write all 0s to CSR
	csrrs x29, 0xf2c, x25	// Set all CSR bits
	csrrc x29, 0xf2c, x25	// Clear all CSR bits
	csrrw x29, 0xf2c, x26	// Restore CSR

// Testing CSR 0xf2d
	csrr x27, 0xf2d	// Read CSR
	li x29, -1
	csrrw x22, 0xf2d, x29	// Write all 1s to CSR
	csrrw x22, 0xf2d, x0	// Write all 0s to CSR
	csrrs x22, 0xf2d, x29	// Set all CSR bits
	csrrc x22, 0xf2d, x29	// Clear all CSR bits
	csrrw x22, 0xf2d, x27	// Restore CSR

// Testing CSR 0xf2e
	csrr x15, 0xf2e	// Read CSR
	li x24, -1
	csrrw x8, 0xf2e, x24	// Write all 1s to CSR
	csrrw x8, 0xf2e, x0	// Write all 0s to CSR
	csrrs x8, 0xf2e, x24	// Set all CSR bits
	csrrc x8, 0xf2e, x24	// Clear all CSR bits
	csrrw x8, 0xf2e, x15	// Restore CSR

// Testing CSR 0xf2f
	csrr x25, 0xf2f	// Read CSR
	li x30, -1
	csrrw x27, 0xf2f, x30	// Write all 1s to CSR
	csrrw x27, 0xf2f, x0	// Write all 0s to CSR
	csrrs x27, 0xf2f, x30	// Set all CSR bits
	csrrc x27, 0xf2f, x30	// Clear all CSR bits
	csrrw x27, 0xf2f, x25	// Restore CSR

// Testing CSR 0xf30
	csrr x16, 0xf30	// Read CSR
	li x8, -1
	csrrw x31, 0xf30, x8	// Write all 1s to CSR
	csrrw x31, 0xf30, x0	// Write all 0s to CSR
	csrrs x31, 0xf30, x8	// Set all CSR bits
	csrrc x31, 0xf30, x8	// Clear all CSR bits
	csrrw x31, 0xf30, x16	// Restore CSR

// Testing CSR 0xf31
	csrr x26, 0xf31	// Read CSR
	li x7, -1
	csrrw x14, 0xf31, x7	// Write all 1s to CSR
	csrrw x14, 0xf31, x0	// Write all 0s to CSR
	csrrs x14, 0xf31, x7	// Set all CSR bits
	csrrc x14, 0xf31, x7	// Clear all CSR bits
	csrrw x14, 0xf31, x26	// Restore CSR

// Testing CSR 0xf32
	csrr x10, 0xf32	// Read CSR
	li x8, -1
	csrrw x21, 0xf32, x8	// Write all 1s to CSR
	csrrw x21, 0xf32, x0	// Write all 0s to CSR
	csrrs x21, 0xf32, x8	// Set all CSR bits
	csrrc x21, 0xf32, x8	// Clear all CSR bits
	csrrw x21, 0xf32, x10	// Restore CSR

// Testing CSR 0xf33
	csrr x11, 0xf33	// Read CSR
	li x30, -1
	csrrw x4, 0xf33, x30	// Write all 1s to CSR
	csrrw x4, 0xf33, x0	// Write all 0s to CSR
	csrrs x4, 0xf33, x30	// Set all CSR bits
	csrrc x4, 0xf33, x30	// Clear all CSR bits
	csrrw x4, 0xf33, x11	// Restore CSR

// Testing CSR 0xf34
	csrr x21, 0xf34	// Read CSR
	li x30, -1
	csrrw x10, 0xf34, x30	// Write all 1s to CSR
	csrrw x10, 0xf34, x0	// Write all 0s to CSR
	csrrs x10, 0xf34, x30	// Set all CSR bits
	csrrc x10, 0xf34, x30	// Clear all CSR bits
	csrrw x10, 0xf34, x21	// Restore CSR

// Testing CSR 0xf35
	csrr x21, 0xf35	// Read CSR
	li x15, -1
	csrrw x29, 0xf35, x15	// Write all 1s to CSR
	csrrw x29, 0xf35, x0	// Write all 0s to CSR
	csrrs x29, 0xf35, x15	// Set all CSR bits
	csrrc x29, 0xf35, x15	// Clear all CSR bits
	csrrw x29, 0xf35, x21	// Restore CSR

// Testing CSR 0xf36
	csrr x27, 0xf36	// Read CSR
	li x18, -1
	csrrw x6, 0xf36, x18	// Write all 1s to CSR
	csrrw x6, 0xf36, x0	// Write all 0s to CSR
	csrrs x6, 0xf36, x18	// Set all CSR bits
	csrrc x6, 0xf36, x18	// Clear all CSR bits
	csrrw x6, 0xf36, x27	// Restore CSR

// Testing CSR 0xf37
	csrr x3, 0xf37	// Read CSR
	li x31, -1
	csrrw x24, 0xf37, x31	// Write all 1s to CSR
	csrrw x24, 0xf37, x0	// Write all 0s to CSR
	csrrs x24, 0xf37, x31	// Set all CSR bits
	csrrc x24, 0xf37, x31	// Clear all CSR bits
	csrrw x24, 0xf37, x3	// Restore CSR

// Testing CSR 0xf38
	csrr x15, 0xf38	// Read CSR
	li x6, -1
	csrrw x23, 0xf38, x6	// Write all 1s to CSR
	csrrw x23, 0xf38, x0	// Write all 0s to CSR
	csrrs x23, 0xf38, x6	// Set all CSR bits
	csrrc x23, 0xf38, x6	// Clear all CSR bits
	csrrw x23, 0xf38, x15	// Restore CSR

// Testing CSR 0xf39
	csrr x22, 0xf39	// Read CSR
	li x16, -1
	csrrw x7, 0xf39, x16	// Write all 1s to CSR
	csrrw x7, 0xf39, x0	// Write all 0s to CSR
	csrrs x7, 0xf39, x16	// Set all CSR bits
	csrrc x7, 0xf39, x16	// Clear all CSR bits
	csrrw x7, 0xf39, x22	// Restore CSR

// Testing CSR 0xf3a
	csrr x23, 0xf3a	// Read CSR
	li x21, -1
	csrrw x15, 0xf3a, x21	// Write all 1s to CSR
	csrrw x15, 0xf3a, x0	// Write all 0s to CSR
	csrrs x15, 0xf3a, x21	// Set all CSR bits
	csrrc x15, 0xf3a, x21	// Clear all CSR bits
	csrrw x15, 0xf3a, x23	// Restore CSR

// Testing CSR 0xf3b
	csrr x1, 0xf3b	// Read CSR
	li x6, -1
	csrrw x13, 0xf3b, x6	// Write all 1s to CSR
	csrrw x13, 0xf3b, x0	// Write all 0s to CSR
	csrrs x13, 0xf3b, x6	// Set all CSR bits
	csrrc x13, 0xf3b, x6	// Clear all CSR bits
	csrrw x13, 0xf3b, x1	// Restore CSR

// Testing CSR 0xf3c
	csrr x20, 0xf3c	// Read CSR
	li x6, -1
	csrrw x18, 0xf3c, x6	// Write all 1s to CSR
	csrrw x18, 0xf3c, x0	// Write all 0s to CSR
	csrrs x18, 0xf3c, x6	// Set all CSR bits
	csrrc x18, 0xf3c, x6	// Clear all CSR bits
	csrrw x18, 0xf3c, x20	// Restore CSR

// Testing CSR 0xf3d
	csrr x29, 0xf3d	// Read CSR
	li x30, -1
	csrrw x12, 0xf3d, x30	// Write all 1s to CSR
	csrrw x12, 0xf3d, x0	// Write all 0s to CSR
	csrrs x12, 0xf3d, x30	// Set all CSR bits
	csrrc x12, 0xf3d, x30	// Clear all CSR bits
	csrrw x12, 0xf3d, x29	// Restore CSR

// Testing CSR 0xf3e
	csrr x12, 0xf3e	// Read CSR
	li x17, -1
	csrrw x31, 0xf3e, x17	// Write all 1s to CSR
	csrrw x31, 0xf3e, x0	// Write all 0s to CSR
	csrrs x31, 0xf3e, x17	// Set all CSR bits
	csrrc x31, 0xf3e, x17	// Clear all CSR bits
	csrrw x31, 0xf3e, x12	// Restore CSR

// Testing CSR 0xf3f
	csrr x22, 0xf3f	// Read CSR
	li x25, -1
	csrrw x29, 0xf3f, x25	// Write all 1s to CSR
	csrrw x29, 0xf3f, x0	// Write all 0s to CSR
	csrrs x29, 0xf3f, x25	// Set all CSR bits
	csrrc x29, 0xf3f, x25	// Clear all CSR bits
	csrrw x29, 0xf3f, x22	// Restore CSR

// Testing CSR 0xf40
	csrr x7, 0xf40	// Read CSR
	li x19, -1
	csrrw x18, 0xf40, x19	// Write all 1s to CSR
	csrrw x18, 0xf40, x0	// Write all 0s to CSR
	csrrs x18, 0xf40, x19	// Set all CSR bits
	csrrc x18, 0xf40, x19	// Clear all CSR bits
	csrrw x18, 0xf40, x7	// Restore CSR

// Testing CSR 0xf41
	csrr x30, 0xf41	// Read CSR
	li x14, -1
	csrrw x8, 0xf41, x14	// Write all 1s to CSR
	csrrw x8, 0xf41, x0	// Write all 0s to CSR
	csrrs x8, 0xf41, x14	// Set all CSR bits
	csrrc x8, 0xf41, x14	// Clear all CSR bits
	csrrw x8, 0xf41, x30	// Restore CSR

// Testing CSR 0xf42
	csrr x20, 0xf42	// Read CSR
	li x8, -1
	csrrw x30, 0xf42, x8	// Write all 1s to CSR
	csrrw x30, 0xf42, x0	// Write all 0s to CSR
	csrrs x30, 0xf42, x8	// Set all CSR bits
	csrrc x30, 0xf42, x8	// Clear all CSR bits
	csrrw x30, 0xf42, x20	// Restore CSR

// Testing CSR 0xf43
	csrr x10, 0xf43	// Read CSR
	li x30, -1
	csrrw x28, 0xf43, x30	// Write all 1s to CSR
	csrrw x28, 0xf43, x0	// Write all 0s to CSR
	csrrs x28, 0xf43, x30	// Set all CSR bits
	csrrc x28, 0xf43, x30	// Clear all CSR bits
	csrrw x28, 0xf43, x10	// Restore CSR

// Testing CSR 0xf44
	csrr x23, 0xf44	// Read CSR
	li x17, -1
	csrrw x1, 0xf44, x17	// Write all 1s to CSR
	csrrw x1, 0xf44, x0	// Write all 0s to CSR
	csrrs x1, 0xf44, x17	// Set all CSR bits
	csrrc x1, 0xf44, x17	// Clear all CSR bits
	csrrw x1, 0xf44, x23	// Restore CSR

// Testing CSR 0xf45
	csrr x29, 0xf45	// Read CSR
	li x19, -1
	csrrw x22, 0xf45, x19	// Write all 1s to CSR
	csrrw x22, 0xf45, x0	// Write all 0s to CSR
	csrrs x22, 0xf45, x19	// Set all CSR bits
	csrrc x22, 0xf45, x19	// Clear all CSR bits
	csrrw x22, 0xf45, x29	// Restore CSR

// Testing CSR 0xf46
	csrr x1, 0xf46	// Read CSR
	li x26, -1
	csrrw x25, 0xf46, x26	// Write all 1s to CSR
	csrrw x25, 0xf46, x0	// Write all 0s to CSR
	csrrs x25, 0xf46, x26	// Set all CSR bits
	csrrc x25, 0xf46, x26	// Clear all CSR bits
	csrrw x25, 0xf46, x1	// Restore CSR

// Testing CSR 0xf47
	csrr x1, 0xf47	// Read CSR
	li x4, -1
	csrrw x13, 0xf47, x4	// Write all 1s to CSR
	csrrw x13, 0xf47, x0	// Write all 0s to CSR
	csrrs x13, 0xf47, x4	// Set all CSR bits
	csrrc x13, 0xf47, x4	// Clear all CSR bits
	csrrw x13, 0xf47, x1	// Restore CSR

// Testing CSR 0xf48
	csrr x11, 0xf48	// Read CSR
	li x12, -1
	csrrw x4, 0xf48, x12	// Write all 1s to CSR
	csrrw x4, 0xf48, x0	// Write all 0s to CSR
	csrrs x4, 0xf48, x12	// Set all CSR bits
	csrrc x4, 0xf48, x12	// Clear all CSR bits
	csrrw x4, 0xf48, x11	// Restore CSR

// Testing CSR 0xf49
	csrr x21, 0xf49	// Read CSR
	li x9, -1
	csrrw x2, 0xf49, x9	// Write all 1s to CSR
	csrrw x2, 0xf49, x0	// Write all 0s to CSR
	csrrs x2, 0xf49, x9	// Set all CSR bits
	csrrc x2, 0xf49, x9	// Clear all CSR bits
	csrrw x2, 0xf49, x21	// Restore CSR

// Testing CSR 0xf4a
	csrr x5, 0xf4a	// Read CSR
	li x19, -1
	csrrw x21, 0xf4a, x19	// Write all 1s to CSR
	csrrw x21, 0xf4a, x0	// Write all 0s to CSR
	csrrs x21, 0xf4a, x19	// Set all CSR bits
	csrrc x21, 0xf4a, x19	// Clear all CSR bits
	csrrw x21, 0xf4a, x5	// Restore CSR

// Testing CSR 0xf4b
	csrr x20, 0xf4b	// Read CSR
	li x12, -1
	csrrw x24, 0xf4b, x12	// Write all 1s to CSR
	csrrw x24, 0xf4b, x0	// Write all 0s to CSR
	csrrs x24, 0xf4b, x12	// Set all CSR bits
	csrrc x24, 0xf4b, x12	// Clear all CSR bits
	csrrw x24, 0xf4b, x20	// Restore CSR

// Testing CSR 0xf4c
	csrr x9, 0xf4c	// Read CSR
	li x6, -1
	csrrw x3, 0xf4c, x6	// Write all 1s to CSR
	csrrw x3, 0xf4c, x0	// Write all 0s to CSR
	csrrs x3, 0xf4c, x6	// Set all CSR bits
	csrrc x3, 0xf4c, x6	// Clear all CSR bits
	csrrw x3, 0xf4c, x9	// Restore CSR

// Testing CSR 0xf4d
	csrr x13, 0xf4d	// Read CSR
	li x24, -1
	csrrw x21, 0xf4d, x24	// Write all 1s to CSR
	csrrw x21, 0xf4d, x0	// Write all 0s to CSR
	csrrs x21, 0xf4d, x24	// Set all CSR bits
	csrrc x21, 0xf4d, x24	// Clear all CSR bits
	csrrw x21, 0xf4d, x13	// Restore CSR

// Testing CSR 0xf4e
	csrr x7, 0xf4e	// Read CSR
	li x9, -1
	csrrw x28, 0xf4e, x9	// Write all 1s to CSR
	csrrw x28, 0xf4e, x0	// Write all 0s to CSR
	csrrs x28, 0xf4e, x9	// Set all CSR bits
	csrrc x28, 0xf4e, x9	// Clear all CSR bits
	csrrw x28, 0xf4e, x7	// Restore CSR

// Testing CSR 0xf4f
	csrr x10, 0xf4f	// Read CSR
	li x16, -1
	csrrw x1, 0xf4f, x16	// Write all 1s to CSR
	csrrw x1, 0xf4f, x0	// Write all 0s to CSR
	csrrs x1, 0xf4f, x16	// Set all CSR bits
	csrrc x1, 0xf4f, x16	// Clear all CSR bits
	csrrw x1, 0xf4f, x10	// Restore CSR

// Testing CSR 0xf50
	csrr x31, 0xf50	// Read CSR
	li x26, -1
	csrrw x17, 0xf50, x26	// Write all 1s to CSR
	csrrw x17, 0xf50, x0	// Write all 0s to CSR
	csrrs x17, 0xf50, x26	// Set all CSR bits
	csrrc x17, 0xf50, x26	// Clear all CSR bits
	csrrw x17, 0xf50, x31	// Restore CSR

// Testing CSR 0xf51
	csrr x13, 0xf51	// Read CSR
	li x16, -1
	csrrw x6, 0xf51, x16	// Write all 1s to CSR
	csrrw x6, 0xf51, x0	// Write all 0s to CSR
	csrrs x6, 0xf51, x16	// Set all CSR bits
	csrrc x6, 0xf51, x16	// Clear all CSR bits
	csrrw x6, 0xf51, x13	// Restore CSR

// Testing CSR 0xf52
	csrr x30, 0xf52	// Read CSR
	li x29, -1
	csrrw x26, 0xf52, x29	// Write all 1s to CSR
	csrrw x26, 0xf52, x0	// Write all 0s to CSR
	csrrs x26, 0xf52, x29	// Set all CSR bits
	csrrc x26, 0xf52, x29	// Clear all CSR bits
	csrrw x26, 0xf52, x30	// Restore CSR

// Testing CSR 0xf53
	csrr x7, 0xf53	// Read CSR
	li x12, -1
	csrrw x4, 0xf53, x12	// Write all 1s to CSR
	csrrw x4, 0xf53, x0	// Write all 0s to CSR
	csrrs x4, 0xf53, x12	// Set all CSR bits
	csrrc x4, 0xf53, x12	// Clear all CSR bits
	csrrw x4, 0xf53, x7	// Restore CSR

// Testing CSR 0xf54
	csrr x26, 0xf54	// Read CSR
	li x22, -1
	csrrw x25, 0xf54, x22	// Write all 1s to CSR
	csrrw x25, 0xf54, x0	// Write all 0s to CSR
	csrrs x25, 0xf54, x22	// Set all CSR bits
	csrrc x25, 0xf54, x22	// Clear all CSR bits
	csrrw x25, 0xf54, x26	// Restore CSR

// Testing CSR 0xf55
	csrr x6, 0xf55	// Read CSR
	li x2, -1
	csrrw x7, 0xf55, x2	// Write all 1s to CSR
	csrrw x7, 0xf55, x0	// Write all 0s to CSR
	csrrs x7, 0xf55, x2	// Set all CSR bits
	csrrc x7, 0xf55, x2	// Clear all CSR bits
	csrrw x7, 0xf55, x6	// Restore CSR

// Testing CSR 0xf56
	csrr x9, 0xf56	// Read CSR
	li x23, -1
	csrrw x30, 0xf56, x23	// Write all 1s to CSR
	csrrw x30, 0xf56, x0	// Write all 0s to CSR
	csrrs x30, 0xf56, x23	// Set all CSR bits
	csrrc x30, 0xf56, x23	// Clear all CSR bits
	csrrw x30, 0xf56, x9	// Restore CSR

// Testing CSR 0xf57
	csrr x4, 0xf57	// Read CSR
	li x12, -1
	csrrw x24, 0xf57, x12	// Write all 1s to CSR
	csrrw x24, 0xf57, x0	// Write all 0s to CSR
	csrrs x24, 0xf57, x12	// Set all CSR bits
	csrrc x24, 0xf57, x12	// Clear all CSR bits
	csrrw x24, 0xf57, x4	// Restore CSR

// Testing CSR 0xf58
	csrr x3, 0xf58	// Read CSR
	li x22, -1
	csrrw x11, 0xf58, x22	// Write all 1s to CSR
	csrrw x11, 0xf58, x0	// Write all 0s to CSR
	csrrs x11, 0xf58, x22	// Set all CSR bits
	csrrc x11, 0xf58, x22	// Clear all CSR bits
	csrrw x11, 0xf58, x3	// Restore CSR

// Testing CSR 0xf59
	csrr x23, 0xf59	// Read CSR
	li x28, -1
	csrrw x6, 0xf59, x28	// Write all 1s to CSR
	csrrw x6, 0xf59, x0	// Write all 0s to CSR
	csrrs x6, 0xf59, x28	// Set all CSR bits
	csrrc x6, 0xf59, x28	// Clear all CSR bits
	csrrw x6, 0xf59, x23	// Restore CSR

// Testing CSR 0xf5a
	csrr x30, 0xf5a	// Read CSR
	li x12, -1
	csrrw x4, 0xf5a, x12	// Write all 1s to CSR
	csrrw x4, 0xf5a, x0	// Write all 0s to CSR
	csrrs x4, 0xf5a, x12	// Set all CSR bits
	csrrc x4, 0xf5a, x12	// Clear all CSR bits
	csrrw x4, 0xf5a, x30	// Restore CSR

// Testing CSR 0xf5b
	csrr x25, 0xf5b	// Read CSR
	li x27, -1
	csrrw x7, 0xf5b, x27	// Write all 1s to CSR
	csrrw x7, 0xf5b, x0	// Write all 0s to CSR
	csrrs x7, 0xf5b, x27	// Set all CSR bits
	csrrc x7, 0xf5b, x27	// Clear all CSR bits
	csrrw x7, 0xf5b, x25	// Restore CSR

// Testing CSR 0xf5c
	csrr x2, 0xf5c	// Read CSR
	li x16, -1
	csrrw x22, 0xf5c, x16	// Write all 1s to CSR
	csrrw x22, 0xf5c, x0	// Write all 0s to CSR
	csrrs x22, 0xf5c, x16	// Set all CSR bits
	csrrc x22, 0xf5c, x16	// Clear all CSR bits
	csrrw x22, 0xf5c, x2	// Restore CSR

// Testing CSR 0xf5d
	csrr x30, 0xf5d	// Read CSR
	li x18, -1
	csrrw x3, 0xf5d, x18	// Write all 1s to CSR
	csrrw x3, 0xf5d, x0	// Write all 0s to CSR
	csrrs x3, 0xf5d, x18	// Set all CSR bits
	csrrc x3, 0xf5d, x18	// Clear all CSR bits
	csrrw x3, 0xf5d, x30	// Restore CSR

// Testing CSR 0xf5e
	csrr x5, 0xf5e	// Read CSR
	li x3, -1
	csrrw x8, 0xf5e, x3	// Write all 1s to CSR
	csrrw x8, 0xf5e, x0	// Write all 0s to CSR
	csrrs x8, 0xf5e, x3	// Set all CSR bits
	csrrc x8, 0xf5e, x3	// Clear all CSR bits
	csrrw x8, 0xf5e, x5	// Restore CSR

// Testing CSR 0xf5f
	csrr x29, 0xf5f	// Read CSR
	li x3, -1
	csrrw x6, 0xf5f, x3	// Write all 1s to CSR
	csrrw x6, 0xf5f, x0	// Write all 0s to CSR
	csrrs x6, 0xf5f, x3	// Set all CSR bits
	csrrc x6, 0xf5f, x3	// Clear all CSR bits
	csrrw x6, 0xf5f, x29	// Restore CSR

// Testing CSR 0xf60
	csrr x31, 0xf60	// Read CSR
	li x8, -1
	csrrw x12, 0xf60, x8	// Write all 1s to CSR
	csrrw x12, 0xf60, x0	// Write all 0s to CSR
	csrrs x12, 0xf60, x8	// Set all CSR bits
	csrrc x12, 0xf60, x8	// Clear all CSR bits
	csrrw x12, 0xf60, x31	// Restore CSR

// Testing CSR 0xf61
	csrr x6, 0xf61	// Read CSR
	li x12, -1
	csrrw x18, 0xf61, x12	// Write all 1s to CSR
	csrrw x18, 0xf61, x0	// Write all 0s to CSR
	csrrs x18, 0xf61, x12	// Set all CSR bits
	csrrc x18, 0xf61, x12	// Clear all CSR bits
	csrrw x18, 0xf61, x6	// Restore CSR

// Testing CSR 0xf62
	csrr x8, 0xf62	// Read CSR
	li x9, -1
	csrrw x27, 0xf62, x9	// Write all 1s to CSR
	csrrw x27, 0xf62, x0	// Write all 0s to CSR
	csrrs x27, 0xf62, x9	// Set all CSR bits
	csrrc x27, 0xf62, x9	// Clear all CSR bits
	csrrw x27, 0xf62, x8	// Restore CSR

// Testing CSR 0xf63
	csrr x18, 0xf63	// Read CSR
	li x30, -1
	csrrw x29, 0xf63, x30	// Write all 1s to CSR
	csrrw x29, 0xf63, x0	// Write all 0s to CSR
	csrrs x29, 0xf63, x30	// Set all CSR bits
	csrrc x29, 0xf63, x30	// Clear all CSR bits
	csrrw x29, 0xf63, x18	// Restore CSR

// Testing CSR 0xf64
	csrr x23, 0xf64	// Read CSR
	li x22, -1
	csrrw x7, 0xf64, x22	// Write all 1s to CSR
	csrrw x7, 0xf64, x0	// Write all 0s to CSR
	csrrs x7, 0xf64, x22	// Set all CSR bits
	csrrc x7, 0xf64, x22	// Clear all CSR bits
	csrrw x7, 0xf64, x23	// Restore CSR

// Testing CSR 0xf65
	csrr x25, 0xf65	// Read CSR
	li x5, -1
	csrrw x2, 0xf65, x5	// Write all 1s to CSR
	csrrw x2, 0xf65, x0	// Write all 0s to CSR
	csrrs x2, 0xf65, x5	// Set all CSR bits
	csrrc x2, 0xf65, x5	// Clear all CSR bits
	csrrw x2, 0xf65, x25	// Restore CSR

// Testing CSR 0xf66
	csrr x24, 0xf66	// Read CSR
	li x20, -1
	csrrw x8, 0xf66, x20	// Write all 1s to CSR
	csrrw x8, 0xf66, x0	// Write all 0s to CSR
	csrrs x8, 0xf66, x20	// Set all CSR bits
	csrrc x8, 0xf66, x20	// Clear all CSR bits
	csrrw x8, 0xf66, x24	// Restore CSR

// Testing CSR 0xf67
	csrr x3, 0xf67	// Read CSR
	li x27, -1
	csrrw x18, 0xf67, x27	// Write all 1s to CSR
	csrrw x18, 0xf67, x0	// Write all 0s to CSR
	csrrs x18, 0xf67, x27	// Set all CSR bits
	csrrc x18, 0xf67, x27	// Clear all CSR bits
	csrrw x18, 0xf67, x3	// Restore CSR

// Testing CSR 0xf68
	csrr x30, 0xf68	// Read CSR
	li x25, -1
	csrrw x20, 0xf68, x25	// Write all 1s to CSR
	csrrw x20, 0xf68, x0	// Write all 0s to CSR
	csrrs x20, 0xf68, x25	// Set all CSR bits
	csrrc x20, 0xf68, x25	// Clear all CSR bits
	csrrw x20, 0xf68, x30	// Restore CSR

// Testing CSR 0xf69
	csrr x29, 0xf69	// Read CSR
	li x10, -1
	csrrw x18, 0xf69, x10	// Write all 1s to CSR
	csrrw x18, 0xf69, x0	// Write all 0s to CSR
	csrrs x18, 0xf69, x10	// Set all CSR bits
	csrrc x18, 0xf69, x10	// Clear all CSR bits
	csrrw x18, 0xf69, x29	// Restore CSR

// Testing CSR 0xf6a
	csrr x9, 0xf6a	// Read CSR
	li x6, -1
	csrrw x19, 0xf6a, x6	// Write all 1s to CSR
	csrrw x19, 0xf6a, x0	// Write all 0s to CSR
	csrrs x19, 0xf6a, x6	// Set all CSR bits
	csrrc x19, 0xf6a, x6	// Clear all CSR bits
	csrrw x19, 0xf6a, x9	// Restore CSR

// Testing CSR 0xf6b
	csrr x27, 0xf6b	// Read CSR
	li x4, -1
	csrrw x13, 0xf6b, x4	// Write all 1s to CSR
	csrrw x13, 0xf6b, x0	// Write all 0s to CSR
	csrrs x13, 0xf6b, x4	// Set all CSR bits
	csrrc x13, 0xf6b, x4	// Clear all CSR bits
	csrrw x13, 0xf6b, x27	// Restore CSR

// Testing CSR 0xf6c
	csrr x14, 0xf6c	// Read CSR
	li x13, -1
	csrrw x27, 0xf6c, x13	// Write all 1s to CSR
	csrrw x27, 0xf6c, x0	// Write all 0s to CSR
	csrrs x27, 0xf6c, x13	// Set all CSR bits
	csrrc x27, 0xf6c, x13	// Clear all CSR bits
	csrrw x27, 0xf6c, x14	// Restore CSR

// Testing CSR 0xf6d
	csrr x12, 0xf6d	// Read CSR
	li x19, -1
	csrrw x2, 0xf6d, x19	// Write all 1s to CSR
	csrrw x2, 0xf6d, x0	// Write all 0s to CSR
	csrrs x2, 0xf6d, x19	// Set all CSR bits
	csrrc x2, 0xf6d, x19	// Clear all CSR bits
	csrrw x2, 0xf6d, x12	// Restore CSR

// Testing CSR 0xf6e
	csrr x22, 0xf6e	// Read CSR
	li x28, -1
	csrrw x18, 0xf6e, x28	// Write all 1s to CSR
	csrrw x18, 0xf6e, x0	// Write all 0s to CSR
	csrrs x18, 0xf6e, x28	// Set all CSR bits
	csrrc x18, 0xf6e, x28	// Clear all CSR bits
	csrrw x18, 0xf6e, x22	// Restore CSR

// Testing CSR 0xf6f
	csrr x20, 0xf6f	// Read CSR
	li x5, -1
	csrrw x11, 0xf6f, x5	// Write all 1s to CSR
	csrrw x11, 0xf6f, x0	// Write all 0s to CSR
	csrrs x11, 0xf6f, x5	// Set all CSR bits
	csrrc x11, 0xf6f, x5	// Clear all CSR bits
	csrrw x11, 0xf6f, x20	// Restore CSR

// Testing CSR 0xf70
	csrr x20, 0xf70	// Read CSR
	li x11, -1
	csrrw x18, 0xf70, x11	// Write all 1s to CSR
	csrrw x18, 0xf70, x0	// Write all 0s to CSR
	csrrs x18, 0xf70, x11	// Set all CSR bits
	csrrc x18, 0xf70, x11	// Clear all CSR bits
	csrrw x18, 0xf70, x20	// Restore CSR

// Testing CSR 0xf71
	csrr x9, 0xf71	// Read CSR
	li x10, -1
	csrrw x17, 0xf71, x10	// Write all 1s to CSR
	csrrw x17, 0xf71, x0	// Write all 0s to CSR
	csrrs x17, 0xf71, x10	// Set all CSR bits
	csrrc x17, 0xf71, x10	// Clear all CSR bits
	csrrw x17, 0xf71, x9	// Restore CSR

// Testing CSR 0xf72
	csrr x31, 0xf72	// Read CSR
	li x19, -1
	csrrw x10, 0xf72, x19	// Write all 1s to CSR
	csrrw x10, 0xf72, x0	// Write all 0s to CSR
	csrrs x10, 0xf72, x19	// Set all CSR bits
	csrrc x10, 0xf72, x19	// Clear all CSR bits
	csrrw x10, 0xf72, x31	// Restore CSR

// Testing CSR 0xf73
	csrr x8, 0xf73	// Read CSR
	li x21, -1
	csrrw x18, 0xf73, x21	// Write all 1s to CSR
	csrrw x18, 0xf73, x0	// Write all 0s to CSR
	csrrs x18, 0xf73, x21	// Set all CSR bits
	csrrc x18, 0xf73, x21	// Clear all CSR bits
	csrrw x18, 0xf73, x8	// Restore CSR

// Testing CSR 0xf74
	csrr x6, 0xf74	// Read CSR
	li x27, -1
	csrrw x5, 0xf74, x27	// Write all 1s to CSR
	csrrw x5, 0xf74, x0	// Write all 0s to CSR
	csrrs x5, 0xf74, x27	// Set all CSR bits
	csrrc x5, 0xf74, x27	// Clear all CSR bits
	csrrw x5, 0xf74, x6	// Restore CSR

// Testing CSR 0xf75
	csrr x26, 0xf75	// Read CSR
	li x8, -1
	csrrw x14, 0xf75, x8	// Write all 1s to CSR
	csrrw x14, 0xf75, x0	// Write all 0s to CSR
	csrrs x14, 0xf75, x8	// Set all CSR bits
	csrrc x14, 0xf75, x8	// Clear all CSR bits
	csrrw x14, 0xf75, x26	// Restore CSR

// Testing CSR 0xf76
	csrr x30, 0xf76	// Read CSR
	li x13, -1
	csrrw x4, 0xf76, x13	// Write all 1s to CSR
	csrrw x4, 0xf76, x0	// Write all 0s to CSR
	csrrs x4, 0xf76, x13	// Set all CSR bits
	csrrc x4, 0xf76, x13	// Clear all CSR bits
	csrrw x4, 0xf76, x30	// Restore CSR

// Testing CSR 0xf77
	csrr x23, 0xf77	// Read CSR
	li x18, -1
	csrrw x6, 0xf77, x18	// Write all 1s to CSR
	csrrw x6, 0xf77, x0	// Write all 0s to CSR
	csrrs x6, 0xf77, x18	// Set all CSR bits
	csrrc x6, 0xf77, x18	// Clear all CSR bits
	csrrw x6, 0xf77, x23	// Restore CSR

// Testing CSR 0xf78
	csrr x3, 0xf78	// Read CSR
	li x11, -1
	csrrw x16, 0xf78, x11	// Write all 1s to CSR
	csrrw x16, 0xf78, x0	// Write all 0s to CSR
	csrrs x16, 0xf78, x11	// Set all CSR bits
	csrrc x16, 0xf78, x11	// Clear all CSR bits
	csrrw x16, 0xf78, x3	// Restore CSR

// Testing CSR 0xf79
	csrr x25, 0xf79	// Read CSR
	li x2, -1
	csrrw x11, 0xf79, x2	// Write all 1s to CSR
	csrrw x11, 0xf79, x0	// Write all 0s to CSR
	csrrs x11, 0xf79, x2	// Set all CSR bits
	csrrc x11, 0xf79, x2	// Clear all CSR bits
	csrrw x11, 0xf79, x25	// Restore CSR

// Testing CSR 0xf7a
	csrr x4, 0xf7a	// Read CSR
	li x10, -1
	csrrw x13, 0xf7a, x10	// Write all 1s to CSR
	csrrw x13, 0xf7a, x0	// Write all 0s to CSR
	csrrs x13, 0xf7a, x10	// Set all CSR bits
	csrrc x13, 0xf7a, x10	// Clear all CSR bits
	csrrw x13, 0xf7a, x4	// Restore CSR

// Testing CSR 0xf7b
	csrr x19, 0xf7b	// Read CSR
	li x30, -1
	csrrw x28, 0xf7b, x30	// Write all 1s to CSR
	csrrw x28, 0xf7b, x0	// Write all 0s to CSR
	csrrs x28, 0xf7b, x30	// Set all CSR bits
	csrrc x28, 0xf7b, x30	// Clear all CSR bits
	csrrw x28, 0xf7b, x19	// Restore CSR

// Testing CSR 0xf7c
	csrr x14, 0xf7c	// Read CSR
	li x21, -1
	csrrw x4, 0xf7c, x21	// Write all 1s to CSR
	csrrw x4, 0xf7c, x0	// Write all 0s to CSR
	csrrs x4, 0xf7c, x21	// Set all CSR bits
	csrrc x4, 0xf7c, x21	// Clear all CSR bits
	csrrw x4, 0xf7c, x14	// Restore CSR

// Testing CSR 0xf7d
	csrr x10, 0xf7d	// Read CSR
	li x29, -1
	csrrw x2, 0xf7d, x29	// Write all 1s to CSR
	csrrw x2, 0xf7d, x0	// Write all 0s to CSR
	csrrs x2, 0xf7d, x29	// Set all CSR bits
	csrrc x2, 0xf7d, x29	// Clear all CSR bits
	csrrw x2, 0xf7d, x10	// Restore CSR

// Testing CSR 0xf7e
	csrr x31, 0xf7e	// Read CSR
	li x7, -1
	csrrw x19, 0xf7e, x7	// Write all 1s to CSR
	csrrw x19, 0xf7e, x0	// Write all 0s to CSR
	csrrs x19, 0xf7e, x7	// Set all CSR bits
	csrrc x19, 0xf7e, x7	// Clear all CSR bits
	csrrw x19, 0xf7e, x31	// Restore CSR

// Testing CSR 0xf7f
	csrr x27, 0xf7f	// Read CSR
	li x16, -1
	csrrw x30, 0xf7f, x16	// Write all 1s to CSR
	csrrw x30, 0xf7f, x0	// Write all 0s to CSR
	csrrs x30, 0xf7f, x16	// Set all CSR bits
	csrrc x30, 0xf7f, x16	// Clear all CSR bits
	csrrw x30, 0xf7f, x27	// Restore CSR

// Testing CSR 0xf80
	csrr x19, 0xf80	// Read CSR
	li x23, -1
	csrrw x15, 0xf80, x23	// Write all 1s to CSR
	csrrw x15, 0xf80, x0	// Write all 0s to CSR
	csrrs x15, 0xf80, x23	// Set all CSR bits
	csrrc x15, 0xf80, x23	// Clear all CSR bits
	csrrw x15, 0xf80, x19	// Restore CSR

// Testing CSR 0xf81
	csrr x19, 0xf81	// Read CSR
	li x8, -1
	csrrw x4, 0xf81, x8	// Write all 1s to CSR
	csrrw x4, 0xf81, x0	// Write all 0s to CSR
	csrrs x4, 0xf81, x8	// Set all CSR bits
	csrrc x4, 0xf81, x8	// Clear all CSR bits
	csrrw x4, 0xf81, x19	// Restore CSR

// Testing CSR 0xf82
	csrr x21, 0xf82	// Read CSR
	li x17, -1
	csrrw x5, 0xf82, x17	// Write all 1s to CSR
	csrrw x5, 0xf82, x0	// Write all 0s to CSR
	csrrs x5, 0xf82, x17	// Set all CSR bits
	csrrc x5, 0xf82, x17	// Clear all CSR bits
	csrrw x5, 0xf82, x21	// Restore CSR

// Testing CSR 0xf83
	csrr x16, 0xf83	// Read CSR
	li x7, -1
	csrrw x18, 0xf83, x7	// Write all 1s to CSR
	csrrw x18, 0xf83, x0	// Write all 0s to CSR
	csrrs x18, 0xf83, x7	// Set all CSR bits
	csrrc x18, 0xf83, x7	// Clear all CSR bits
	csrrw x18, 0xf83, x16	// Restore CSR

// Testing CSR 0xf84
	csrr x8, 0xf84	// Read CSR
	li x21, -1
	csrrw x6, 0xf84, x21	// Write all 1s to CSR
	csrrw x6, 0xf84, x0	// Write all 0s to CSR
	csrrs x6, 0xf84, x21	// Set all CSR bits
	csrrc x6, 0xf84, x21	// Clear all CSR bits
	csrrw x6, 0xf84, x8	// Restore CSR

// Testing CSR 0xf85
	csrr x27, 0xf85	// Read CSR
	li x17, -1
	csrrw x30, 0xf85, x17	// Write all 1s to CSR
	csrrw x30, 0xf85, x0	// Write all 0s to CSR
	csrrs x30, 0xf85, x17	// Set all CSR bits
	csrrc x30, 0xf85, x17	// Clear all CSR bits
	csrrw x30, 0xf85, x27	// Restore CSR

// Testing CSR 0xf86
	csrr x15, 0xf86	// Read CSR
	li x1, -1
	csrrw x27, 0xf86, x1	// Write all 1s to CSR
	csrrw x27, 0xf86, x0	// Write all 0s to CSR
	csrrs x27, 0xf86, x1	// Set all CSR bits
	csrrc x27, 0xf86, x1	// Clear all CSR bits
	csrrw x27, 0xf86, x15	// Restore CSR

// Testing CSR 0xf87
	csrr x18, 0xf87	// Read CSR
	li x4, -1
	csrrw x14, 0xf87, x4	// Write all 1s to CSR
	csrrw x14, 0xf87, x0	// Write all 0s to CSR
	csrrs x14, 0xf87, x4	// Set all CSR bits
	csrrc x14, 0xf87, x4	// Clear all CSR bits
	csrrw x14, 0xf87, x18	// Restore CSR

// Testing CSR 0xf88
	csrr x29, 0xf88	// Read CSR
	li x1, -1
	csrrw x2, 0xf88, x1	// Write all 1s to CSR
	csrrw x2, 0xf88, x0	// Write all 0s to CSR
	csrrs x2, 0xf88, x1	// Set all CSR bits
	csrrc x2, 0xf88, x1	// Clear all CSR bits
	csrrw x2, 0xf88, x29	// Restore CSR

// Testing CSR 0xf89
	csrr x2, 0xf89	// Read CSR
	li x23, -1
	csrrw x12, 0xf89, x23	// Write all 1s to CSR
	csrrw x12, 0xf89, x0	// Write all 0s to CSR
	csrrs x12, 0xf89, x23	// Set all CSR bits
	csrrc x12, 0xf89, x23	// Clear all CSR bits
	csrrw x12, 0xf89, x2	// Restore CSR

// Testing CSR 0xf8a
	csrr x24, 0xf8a	// Read CSR
	li x14, -1
	csrrw x9, 0xf8a, x14	// Write all 1s to CSR
	csrrw x9, 0xf8a, x0	// Write all 0s to CSR
	csrrs x9, 0xf8a, x14	// Set all CSR bits
	csrrc x9, 0xf8a, x14	// Clear all CSR bits
	csrrw x9, 0xf8a, x24	// Restore CSR

// Testing CSR 0xf8b
	csrr x19, 0xf8b	// Read CSR
	li x25, -1
	csrrw x7, 0xf8b, x25	// Write all 1s to CSR
	csrrw x7, 0xf8b, x0	// Write all 0s to CSR
	csrrs x7, 0xf8b, x25	// Set all CSR bits
	csrrc x7, 0xf8b, x25	// Clear all CSR bits
	csrrw x7, 0xf8b, x19	// Restore CSR

// Testing CSR 0xf8c
	csrr x12, 0xf8c	// Read CSR
	li x18, -1
	csrrw x19, 0xf8c, x18	// Write all 1s to CSR
	csrrw x19, 0xf8c, x0	// Write all 0s to CSR
	csrrs x19, 0xf8c, x18	// Set all CSR bits
	csrrc x19, 0xf8c, x18	// Clear all CSR bits
	csrrw x19, 0xf8c, x12	// Restore CSR

// Testing CSR 0xf8d
	csrr x10, 0xf8d	// Read CSR
	li x22, -1
	csrrw x14, 0xf8d, x22	// Write all 1s to CSR
	csrrw x14, 0xf8d, x0	// Write all 0s to CSR
	csrrs x14, 0xf8d, x22	// Set all CSR bits
	csrrc x14, 0xf8d, x22	// Clear all CSR bits
	csrrw x14, 0xf8d, x10	// Restore CSR

// Testing CSR 0xf8e
	csrr x7, 0xf8e	// Read CSR
	li x10, -1
	csrrw x1, 0xf8e, x10	// Write all 1s to CSR
	csrrw x1, 0xf8e, x0	// Write all 0s to CSR
	csrrs x1, 0xf8e, x10	// Set all CSR bits
	csrrc x1, 0xf8e, x10	// Clear all CSR bits
	csrrw x1, 0xf8e, x7	// Restore CSR

// Testing CSR 0xf8f
	csrr x8, 0xf8f	// Read CSR
	li x6, -1
	csrrw x31, 0xf8f, x6	// Write all 1s to CSR
	csrrw x31, 0xf8f, x0	// Write all 0s to CSR
	csrrs x31, 0xf8f, x6	// Set all CSR bits
	csrrc x31, 0xf8f, x6	// Clear all CSR bits
	csrrw x31, 0xf8f, x8	// Restore CSR

// Testing CSR 0xf90
	csrr x6, 0xf90	// Read CSR
	li x9, -1
	csrrw x23, 0xf90, x9	// Write all 1s to CSR
	csrrw x23, 0xf90, x0	// Write all 0s to CSR
	csrrs x23, 0xf90, x9	// Set all CSR bits
	csrrc x23, 0xf90, x9	// Clear all CSR bits
	csrrw x23, 0xf90, x6	// Restore CSR

// Testing CSR 0xf91
	csrr x16, 0xf91	// Read CSR
	li x23, -1
	csrrw x7, 0xf91, x23	// Write all 1s to CSR
	csrrw x7, 0xf91, x0	// Write all 0s to CSR
	csrrs x7, 0xf91, x23	// Set all CSR bits
	csrrc x7, 0xf91, x23	// Clear all CSR bits
	csrrw x7, 0xf91, x16	// Restore CSR

// Testing CSR 0xf92
	csrr x8, 0xf92	// Read CSR
	li x25, -1
	csrrw x20, 0xf92, x25	// Write all 1s to CSR
	csrrw x20, 0xf92, x0	// Write all 0s to CSR
	csrrs x20, 0xf92, x25	// Set all CSR bits
	csrrc x20, 0xf92, x25	// Clear all CSR bits
	csrrw x20, 0xf92, x8	// Restore CSR

// Testing CSR 0xf93
	csrr x30, 0xf93	// Read CSR
	li x5, -1
	csrrw x27, 0xf93, x5	// Write all 1s to CSR
	csrrw x27, 0xf93, x0	// Write all 0s to CSR
	csrrs x27, 0xf93, x5	// Set all CSR bits
	csrrc x27, 0xf93, x5	// Clear all CSR bits
	csrrw x27, 0xf93, x30	// Restore CSR

// Testing CSR 0xf94
	csrr x3, 0xf94	// Read CSR
	li x20, -1
	csrrw x22, 0xf94, x20	// Write all 1s to CSR
	csrrw x22, 0xf94, x0	// Write all 0s to CSR
	csrrs x22, 0xf94, x20	// Set all CSR bits
	csrrc x22, 0xf94, x20	// Clear all CSR bits
	csrrw x22, 0xf94, x3	// Restore CSR

// Testing CSR 0xf95
	csrr x9, 0xf95	// Read CSR
	li x27, -1
	csrrw x1, 0xf95, x27	// Write all 1s to CSR
	csrrw x1, 0xf95, x0	// Write all 0s to CSR
	csrrs x1, 0xf95, x27	// Set all CSR bits
	csrrc x1, 0xf95, x27	// Clear all CSR bits
	csrrw x1, 0xf95, x9	// Restore CSR

// Testing CSR 0xf96
	csrr x3, 0xf96	// Read CSR
	li x30, -1
	csrrw x5, 0xf96, x30	// Write all 1s to CSR
	csrrw x5, 0xf96, x0	// Write all 0s to CSR
	csrrs x5, 0xf96, x30	// Set all CSR bits
	csrrc x5, 0xf96, x30	// Clear all CSR bits
	csrrw x5, 0xf96, x3	// Restore CSR

// Testing CSR 0xf97
	csrr x16, 0xf97	// Read CSR
	li x23, -1
	csrrw x20, 0xf97, x23	// Write all 1s to CSR
	csrrw x20, 0xf97, x0	// Write all 0s to CSR
	csrrs x20, 0xf97, x23	// Set all CSR bits
	csrrc x20, 0xf97, x23	// Clear all CSR bits
	csrrw x20, 0xf97, x16	// Restore CSR

// Testing CSR 0xf98
	csrr x6, 0xf98	// Read CSR
	li x5, -1
	csrrw x22, 0xf98, x5	// Write all 1s to CSR
	csrrw x22, 0xf98, x0	// Write all 0s to CSR
	csrrs x22, 0xf98, x5	// Set all CSR bits
	csrrc x22, 0xf98, x5	// Clear all CSR bits
	csrrw x22, 0xf98, x6	// Restore CSR

// Testing CSR 0xf99
	csrr x24, 0xf99	// Read CSR
	li x25, -1
	csrrw x13, 0xf99, x25	// Write all 1s to CSR
	csrrw x13, 0xf99, x0	// Write all 0s to CSR
	csrrs x13, 0xf99, x25	// Set all CSR bits
	csrrc x13, 0xf99, x25	// Clear all CSR bits
	csrrw x13, 0xf99, x24	// Restore CSR

// Testing CSR 0xf9a
	csrr x1, 0xf9a	// Read CSR
	li x8, -1
	csrrw x17, 0xf9a, x8	// Write all 1s to CSR
	csrrw x17, 0xf9a, x0	// Write all 0s to CSR
	csrrs x17, 0xf9a, x8	// Set all CSR bits
	csrrc x17, 0xf9a, x8	// Clear all CSR bits
	csrrw x17, 0xf9a, x1	// Restore CSR

// Testing CSR 0xf9b
	csrr x24, 0xf9b	// Read CSR
	li x31, -1
	csrrw x3, 0xf9b, x31	// Write all 1s to CSR
	csrrw x3, 0xf9b, x0	// Write all 0s to CSR
	csrrs x3, 0xf9b, x31	// Set all CSR bits
	csrrc x3, 0xf9b, x31	// Clear all CSR bits
	csrrw x3, 0xf9b, x24	// Restore CSR

// Testing CSR 0xf9c
	csrr x19, 0xf9c	// Read CSR
	li x12, -1
	csrrw x13, 0xf9c, x12	// Write all 1s to CSR
	csrrw x13, 0xf9c, x0	// Write all 0s to CSR
	csrrs x13, 0xf9c, x12	// Set all CSR bits
	csrrc x13, 0xf9c, x12	// Clear all CSR bits
	csrrw x13, 0xf9c, x19	// Restore CSR

// Testing CSR 0xf9d
	csrr x30, 0xf9d	// Read CSR
	li x29, -1
	csrrw x5, 0xf9d, x29	// Write all 1s to CSR
	csrrw x5, 0xf9d, x0	// Write all 0s to CSR
	csrrs x5, 0xf9d, x29	// Set all CSR bits
	csrrc x5, 0xf9d, x29	// Clear all CSR bits
	csrrw x5, 0xf9d, x30	// Restore CSR

// Testing CSR 0xf9e
	csrr x11, 0xf9e	// Read CSR
	li x14, -1
	csrrw x17, 0xf9e, x14	// Write all 1s to CSR
	csrrw x17, 0xf9e, x0	// Write all 0s to CSR
	csrrs x17, 0xf9e, x14	// Set all CSR bits
	csrrc x17, 0xf9e, x14	// Clear all CSR bits
	csrrw x17, 0xf9e, x11	// Restore CSR

// Testing CSR 0xf9f
	csrr x1, 0xf9f	// Read CSR
	li x2, -1
	csrrw x27, 0xf9f, x2	// Write all 1s to CSR
	csrrw x27, 0xf9f, x0	// Write all 0s to CSR
	csrrs x27, 0xf9f, x2	// Set all CSR bits
	csrrc x27, 0xf9f, x2	// Clear all CSR bits
	csrrw x27, 0xf9f, x1	// Restore CSR

// Testing CSR 0xfa0
	csrr x9, 0xfa0	// Read CSR
	li x2, -1
	csrrw x20, 0xfa0, x2	// Write all 1s to CSR
	csrrw x20, 0xfa0, x0	// Write all 0s to CSR
	csrrs x20, 0xfa0, x2	// Set all CSR bits
	csrrc x20, 0xfa0, x2	// Clear all CSR bits
	csrrw x20, 0xfa0, x9	// Restore CSR

// Testing CSR 0xfa1
	csrr x1, 0xfa1	// Read CSR
	li x19, -1
	csrrw x6, 0xfa1, x19	// Write all 1s to CSR
	csrrw x6, 0xfa1, x0	// Write all 0s to CSR
	csrrs x6, 0xfa1, x19	// Set all CSR bits
	csrrc x6, 0xfa1, x19	// Clear all CSR bits
	csrrw x6, 0xfa1, x1	// Restore CSR

// Testing CSR 0xfa2
	csrr x9, 0xfa2	// Read CSR
	li x1, -1
	csrrw x19, 0xfa2, x1	// Write all 1s to CSR
	csrrw x19, 0xfa2, x0	// Write all 0s to CSR
	csrrs x19, 0xfa2, x1	// Set all CSR bits
	csrrc x19, 0xfa2, x1	// Clear all CSR bits
	csrrw x19, 0xfa2, x9	// Restore CSR

// Testing CSR 0xfa3
	csrr x16, 0xfa3	// Read CSR
	li x18, -1
	csrrw x1, 0xfa3, x18	// Write all 1s to CSR
	csrrw x1, 0xfa3, x0	// Write all 0s to CSR
	csrrs x1, 0xfa3, x18	// Set all CSR bits
	csrrc x1, 0xfa3, x18	// Clear all CSR bits
	csrrw x1, 0xfa3, x16	// Restore CSR

// Testing CSR 0xfa4
	csrr x10, 0xfa4	// Read CSR
	li x25, -1
	csrrw x21, 0xfa4, x25	// Write all 1s to CSR
	csrrw x21, 0xfa4, x0	// Write all 0s to CSR
	csrrs x21, 0xfa4, x25	// Set all CSR bits
	csrrc x21, 0xfa4, x25	// Clear all CSR bits
	csrrw x21, 0xfa4, x10	// Restore CSR

// Testing CSR 0xfa5
	csrr x17, 0xfa5	// Read CSR
	li x29, -1
	csrrw x23, 0xfa5, x29	// Write all 1s to CSR
	csrrw x23, 0xfa5, x0	// Write all 0s to CSR
	csrrs x23, 0xfa5, x29	// Set all CSR bits
	csrrc x23, 0xfa5, x29	// Clear all CSR bits
	csrrw x23, 0xfa5, x17	// Restore CSR

// Testing CSR 0xfa6
	csrr x2, 0xfa6	// Read CSR
	li x1, -1
	csrrw x4, 0xfa6, x1	// Write all 1s to CSR
	csrrw x4, 0xfa6, x0	// Write all 0s to CSR
	csrrs x4, 0xfa6, x1	// Set all CSR bits
	csrrc x4, 0xfa6, x1	// Clear all CSR bits
	csrrw x4, 0xfa6, x2	// Restore CSR

// Testing CSR 0xfa7
	csrr x24, 0xfa7	// Read CSR
	li x23, -1
	csrrw x28, 0xfa7, x23	// Write all 1s to CSR
	csrrw x28, 0xfa7, x0	// Write all 0s to CSR
	csrrs x28, 0xfa7, x23	// Set all CSR bits
	csrrc x28, 0xfa7, x23	// Clear all CSR bits
	csrrw x28, 0xfa7, x24	// Restore CSR

// Testing CSR 0xfa8
	csrr x26, 0xfa8	// Read CSR
	li x31, -1
	csrrw x14, 0xfa8, x31	// Write all 1s to CSR
	csrrw x14, 0xfa8, x0	// Write all 0s to CSR
	csrrs x14, 0xfa8, x31	// Set all CSR bits
	csrrc x14, 0xfa8, x31	// Clear all CSR bits
	csrrw x14, 0xfa8, x26	// Restore CSR

// Testing CSR 0xfa9
	csrr x5, 0xfa9	// Read CSR
	li x7, -1
	csrrw x17, 0xfa9, x7	// Write all 1s to CSR
	csrrw x17, 0xfa9, x0	// Write all 0s to CSR
	csrrs x17, 0xfa9, x7	// Set all CSR bits
	csrrc x17, 0xfa9, x7	// Clear all CSR bits
	csrrw x17, 0xfa9, x5	// Restore CSR

// Testing CSR 0xfaa
	csrr x11, 0xfaa	// Read CSR
	li x8, -1
	csrrw x28, 0xfaa, x8	// Write all 1s to CSR
	csrrw x28, 0xfaa, x0	// Write all 0s to CSR
	csrrs x28, 0xfaa, x8	// Set all CSR bits
	csrrc x28, 0xfaa, x8	// Clear all CSR bits
	csrrw x28, 0xfaa, x11	// Restore CSR

// Testing CSR 0xfab
	csrr x3, 0xfab	// Read CSR
	li x19, -1
	csrrw x18, 0xfab, x19	// Write all 1s to CSR
	csrrw x18, 0xfab, x0	// Write all 0s to CSR
	csrrs x18, 0xfab, x19	// Set all CSR bits
	csrrc x18, 0xfab, x19	// Clear all CSR bits
	csrrw x18, 0xfab, x3	// Restore CSR

// Testing CSR 0xfac
	csrr x27, 0xfac	// Read CSR
	li x26, -1
	csrrw x9, 0xfac, x26	// Write all 1s to CSR
	csrrw x9, 0xfac, x0	// Write all 0s to CSR
	csrrs x9, 0xfac, x26	// Set all CSR bits
	csrrc x9, 0xfac, x26	// Clear all CSR bits
	csrrw x9, 0xfac, x27	// Restore CSR

// Testing CSR 0xfad
	csrr x29, 0xfad	// Read CSR
	li x22, -1
	csrrw x8, 0xfad, x22	// Write all 1s to CSR
	csrrw x8, 0xfad, x0	// Write all 0s to CSR
	csrrs x8, 0xfad, x22	// Set all CSR bits
	csrrc x8, 0xfad, x22	// Clear all CSR bits
	csrrw x8, 0xfad, x29	// Restore CSR

// Testing CSR 0xfae
	csrr x19, 0xfae	// Read CSR
	li x27, -1
	csrrw x4, 0xfae, x27	// Write all 1s to CSR
	csrrw x4, 0xfae, x0	// Write all 0s to CSR
	csrrs x4, 0xfae, x27	// Set all CSR bits
	csrrc x4, 0xfae, x27	// Clear all CSR bits
	csrrw x4, 0xfae, x19	// Restore CSR

// Testing CSR 0xfaf
	csrr x3, 0xfaf	// Read CSR
	li x18, -1
	csrrw x27, 0xfaf, x18	// Write all 1s to CSR
	csrrw x27, 0xfaf, x0	// Write all 0s to CSR
	csrrs x27, 0xfaf, x18	// Set all CSR bits
	csrrc x27, 0xfaf, x18	// Clear all CSR bits
	csrrw x27, 0xfaf, x3	// Restore CSR

// Testing CSR 0xfb0
	csrr x13, 0xfb0	// Read CSR
	li x10, -1
	csrrw x14, 0xfb0, x10	// Write all 1s to CSR
	csrrw x14, 0xfb0, x0	// Write all 0s to CSR
	csrrs x14, 0xfb0, x10	// Set all CSR bits
	csrrc x14, 0xfb0, x10	// Clear all CSR bits
	csrrw x14, 0xfb0, x13	// Restore CSR

// Testing CSR 0xfb1
	csrr x12, 0xfb1	// Read CSR
	li x1, -1
	csrrw x26, 0xfb1, x1	// Write all 1s to CSR
	csrrw x26, 0xfb1, x0	// Write all 0s to CSR
	csrrs x26, 0xfb1, x1	// Set all CSR bits
	csrrc x26, 0xfb1, x1	// Clear all CSR bits
	csrrw x26, 0xfb1, x12	// Restore CSR

// Testing CSR 0xfb2
	csrr x13, 0xfb2	// Read CSR
	li x3, -1
	csrrw x29, 0xfb2, x3	// Write all 1s to CSR
	csrrw x29, 0xfb2, x0	// Write all 0s to CSR
	csrrs x29, 0xfb2, x3	// Set all CSR bits
	csrrc x29, 0xfb2, x3	// Clear all CSR bits
	csrrw x29, 0xfb2, x13	// Restore CSR

// Testing CSR 0xfb3
	csrr x10, 0xfb3	// Read CSR
	li x14, -1
	csrrw x26, 0xfb3, x14	// Write all 1s to CSR
	csrrw x26, 0xfb3, x0	// Write all 0s to CSR
	csrrs x26, 0xfb3, x14	// Set all CSR bits
	csrrc x26, 0xfb3, x14	// Clear all CSR bits
	csrrw x26, 0xfb3, x10	// Restore CSR

// Testing CSR 0xfb4
	csrr x22, 0xfb4	// Read CSR
	li x5, -1
	csrrw x10, 0xfb4, x5	// Write all 1s to CSR
	csrrw x10, 0xfb4, x0	// Write all 0s to CSR
	csrrs x10, 0xfb4, x5	// Set all CSR bits
	csrrc x10, 0xfb4, x5	// Clear all CSR bits
	csrrw x10, 0xfb4, x22	// Restore CSR

// Testing CSR 0xfb5
	csrr x21, 0xfb5	// Read CSR
	li x13, -1
	csrrw x1, 0xfb5, x13	// Write all 1s to CSR
	csrrw x1, 0xfb5, x0	// Write all 0s to CSR
	csrrs x1, 0xfb5, x13	// Set all CSR bits
	csrrc x1, 0xfb5, x13	// Clear all CSR bits
	csrrw x1, 0xfb5, x21	// Restore CSR

// Testing CSR 0xfb6
	csrr x10, 0xfb6	// Read CSR
	li x23, -1
	csrrw x17, 0xfb6, x23	// Write all 1s to CSR
	csrrw x17, 0xfb6, x0	// Write all 0s to CSR
	csrrs x17, 0xfb6, x23	// Set all CSR bits
	csrrc x17, 0xfb6, x23	// Clear all CSR bits
	csrrw x17, 0xfb6, x10	// Restore CSR

// Testing CSR 0xfb7
	csrr x22, 0xfb7	// Read CSR
	li x16, -1
	csrrw x6, 0xfb7, x16	// Write all 1s to CSR
	csrrw x6, 0xfb7, x0	// Write all 0s to CSR
	csrrs x6, 0xfb7, x16	// Set all CSR bits
	csrrc x6, 0xfb7, x16	// Clear all CSR bits
	csrrw x6, 0xfb7, x22	// Restore CSR

// Testing CSR 0xfb8
	csrr x12, 0xfb8	// Read CSR
	li x24, -1
	csrrw x6, 0xfb8, x24	// Write all 1s to CSR
	csrrw x6, 0xfb8, x0	// Write all 0s to CSR
	csrrs x6, 0xfb8, x24	// Set all CSR bits
	csrrc x6, 0xfb8, x24	// Clear all CSR bits
	csrrw x6, 0xfb8, x12	// Restore CSR

// Testing CSR 0xfb9
	csrr x6, 0xfb9	// Read CSR
	li x10, -1
	csrrw x22, 0xfb9, x10	// Write all 1s to CSR
	csrrw x22, 0xfb9, x0	// Write all 0s to CSR
	csrrs x22, 0xfb9, x10	// Set all CSR bits
	csrrc x22, 0xfb9, x10	// Clear all CSR bits
	csrrw x22, 0xfb9, x6	// Restore CSR

// Testing CSR 0xfba
	csrr x27, 0xfba	// Read CSR
	li x26, -1
	csrrw x5, 0xfba, x26	// Write all 1s to CSR
	csrrw x5, 0xfba, x0	// Write all 0s to CSR
	csrrs x5, 0xfba, x26	// Set all CSR bits
	csrrc x5, 0xfba, x26	// Clear all CSR bits
	csrrw x5, 0xfba, x27	// Restore CSR

// Testing CSR 0xfbb
	csrr x11, 0xfbb	// Read CSR
	li x27, -1
	csrrw x28, 0xfbb, x27	// Write all 1s to CSR
	csrrw x28, 0xfbb, x0	// Write all 0s to CSR
	csrrs x28, 0xfbb, x27	// Set all CSR bits
	csrrc x28, 0xfbb, x27	// Clear all CSR bits
	csrrw x28, 0xfbb, x11	// Restore CSR

// Testing CSR 0xfbc
	csrr x31, 0xfbc	// Read CSR
	li x2, -1
	csrrw x16, 0xfbc, x2	// Write all 1s to CSR
	csrrw x16, 0xfbc, x0	// Write all 0s to CSR
	csrrs x16, 0xfbc, x2	// Set all CSR bits
	csrrc x16, 0xfbc, x2	// Clear all CSR bits
	csrrw x16, 0xfbc, x31	// Restore CSR

// Testing CSR 0xfbd
	csrr x22, 0xfbd	// Read CSR
	li x3, -1
	csrrw x13, 0xfbd, x3	// Write all 1s to CSR
	csrrw x13, 0xfbd, x0	// Write all 0s to CSR
	csrrs x13, 0xfbd, x3	// Set all CSR bits
	csrrc x13, 0xfbd, x3	// Clear all CSR bits
	csrrw x13, 0xfbd, x22	// Restore CSR

// Testing CSR 0xfbe
	csrr x28, 0xfbe	// Read CSR
	li x23, -1
	csrrw x13, 0xfbe, x23	// Write all 1s to CSR
	csrrw x13, 0xfbe, x0	// Write all 0s to CSR
	csrrs x13, 0xfbe, x23	// Set all CSR bits
	csrrc x13, 0xfbe, x23	// Clear all CSR bits
	csrrw x13, 0xfbe, x28	// Restore CSR

// Testing CSR 0xfbf
	csrr x3, 0xfbf	// Read CSR
	li x16, -1
	csrrw x30, 0xfbf, x16	// Write all 1s to CSR
	csrrw x30, 0xfbf, x0	// Write all 0s to CSR
	csrrs x30, 0xfbf, x16	// Set all CSR bits
	csrrc x30, 0xfbf, x16	// Clear all CSR bits
	csrrw x30, 0xfbf, x3	// Restore CSR

// Testing CSR 0xfc0
	csrr x17, 0xfc0	// Read CSR
	li x18, -1
	csrrw x6, 0xfc0, x18	// Write all 1s to CSR
	csrrw x6, 0xfc0, x0	// Write all 0s to CSR
	csrrs x6, 0xfc0, x18	// Set all CSR bits
	csrrc x6, 0xfc0, x18	// Clear all CSR bits
	csrrw x6, 0xfc0, x17	// Restore CSR

// Testing CSR 0xfc1
	csrr x1, 0xfc1	// Read CSR
	li x17, -1
	csrrw x14, 0xfc1, x17	// Write all 1s to CSR
	csrrw x14, 0xfc1, x0	// Write all 0s to CSR
	csrrs x14, 0xfc1, x17	// Set all CSR bits
	csrrc x14, 0xfc1, x17	// Clear all CSR bits
	csrrw x14, 0xfc1, x1	// Restore CSR

// Testing CSR 0xfc2
	csrr x25, 0xfc2	// Read CSR
	li x17, -1
	csrrw x6, 0xfc2, x17	// Write all 1s to CSR
	csrrw x6, 0xfc2, x0	// Write all 0s to CSR
	csrrs x6, 0xfc2, x17	// Set all CSR bits
	csrrc x6, 0xfc2, x17	// Clear all CSR bits
	csrrw x6, 0xfc2, x25	// Restore CSR

// Testing CSR 0xfc3
	csrr x14, 0xfc3	// Read CSR
	li x11, -1
	csrrw x16, 0xfc3, x11	// Write all 1s to CSR
	csrrw x16, 0xfc3, x0	// Write all 0s to CSR
	csrrs x16, 0xfc3, x11	// Set all CSR bits
	csrrc x16, 0xfc3, x11	// Clear all CSR bits
	csrrw x16, 0xfc3, x14	// Restore CSR

// Testing CSR 0xfc4
	csrr x3, 0xfc4	// Read CSR
	li x23, -1
	csrrw x24, 0xfc4, x23	// Write all 1s to CSR
	csrrw x24, 0xfc4, x0	// Write all 0s to CSR
	csrrs x24, 0xfc4, x23	// Set all CSR bits
	csrrc x24, 0xfc4, x23	// Clear all CSR bits
	csrrw x24, 0xfc4, x3	// Restore CSR

// Testing CSR 0xfc5
	csrr x1, 0xfc5	// Read CSR
	li x15, -1
	csrrw x3, 0xfc5, x15	// Write all 1s to CSR
	csrrw x3, 0xfc5, x0	// Write all 0s to CSR
	csrrs x3, 0xfc5, x15	// Set all CSR bits
	csrrc x3, 0xfc5, x15	// Clear all CSR bits
	csrrw x3, 0xfc5, x1	// Restore CSR

// Testing CSR 0xfc6
	csrr x28, 0xfc6	// Read CSR
	li x7, -1
	csrrw x19, 0xfc6, x7	// Write all 1s to CSR
	csrrw x19, 0xfc6, x0	// Write all 0s to CSR
	csrrs x19, 0xfc6, x7	// Set all CSR bits
	csrrc x19, 0xfc6, x7	// Clear all CSR bits
	csrrw x19, 0xfc6, x28	// Restore CSR

// Testing CSR 0xfc7
	csrr x10, 0xfc7	// Read CSR
	li x8, -1
	csrrw x2, 0xfc7, x8	// Write all 1s to CSR
	csrrw x2, 0xfc7, x0	// Write all 0s to CSR
	csrrs x2, 0xfc7, x8	// Set all CSR bits
	csrrc x2, 0xfc7, x8	// Clear all CSR bits
	csrrw x2, 0xfc7, x10	// Restore CSR

// Testing CSR 0xfc8
	csrr x15, 0xfc8	// Read CSR
	li x6, -1
	csrrw x30, 0xfc8, x6	// Write all 1s to CSR
	csrrw x30, 0xfc8, x0	// Write all 0s to CSR
	csrrs x30, 0xfc8, x6	// Set all CSR bits
	csrrc x30, 0xfc8, x6	// Clear all CSR bits
	csrrw x30, 0xfc8, x15	// Restore CSR

// Testing CSR 0xfc9
	csrr x6, 0xfc9	// Read CSR
	li x11, -1
	csrrw x5, 0xfc9, x11	// Write all 1s to CSR
	csrrw x5, 0xfc9, x0	// Write all 0s to CSR
	csrrs x5, 0xfc9, x11	// Set all CSR bits
	csrrc x5, 0xfc9, x11	// Clear all CSR bits
	csrrw x5, 0xfc9, x6	// Restore CSR

// Testing CSR 0xfca
	csrr x30, 0xfca	// Read CSR
	li x11, -1
	csrrw x25, 0xfca, x11	// Write all 1s to CSR
	csrrw x25, 0xfca, x0	// Write all 0s to CSR
	csrrs x25, 0xfca, x11	// Set all CSR bits
	csrrc x25, 0xfca, x11	// Clear all CSR bits
	csrrw x25, 0xfca, x30	// Restore CSR

// Testing CSR 0xfcb
	csrr x8, 0xfcb	// Read CSR
	li x25, -1
	csrrw x5, 0xfcb, x25	// Write all 1s to CSR
	csrrw x5, 0xfcb, x0	// Write all 0s to CSR
	csrrs x5, 0xfcb, x25	// Set all CSR bits
	csrrc x5, 0xfcb, x25	// Clear all CSR bits
	csrrw x5, 0xfcb, x8	// Restore CSR

// Testing CSR 0xfcc
	csrr x31, 0xfcc	// Read CSR
	li x25, -1
	csrrw x10, 0xfcc, x25	// Write all 1s to CSR
	csrrw x10, 0xfcc, x0	// Write all 0s to CSR
	csrrs x10, 0xfcc, x25	// Set all CSR bits
	csrrc x10, 0xfcc, x25	// Clear all CSR bits
	csrrw x10, 0xfcc, x31	// Restore CSR

// Testing CSR 0xfcd
	csrr x23, 0xfcd	// Read CSR
	li x7, -1
	csrrw x3, 0xfcd, x7	// Write all 1s to CSR
	csrrw x3, 0xfcd, x0	// Write all 0s to CSR
	csrrs x3, 0xfcd, x7	// Set all CSR bits
	csrrc x3, 0xfcd, x7	// Clear all CSR bits
	csrrw x3, 0xfcd, x23	// Restore CSR

// Testing CSR 0xfce
	csrr x17, 0xfce	// Read CSR
	li x8, -1
	csrrw x2, 0xfce, x8	// Write all 1s to CSR
	csrrw x2, 0xfce, x0	// Write all 0s to CSR
	csrrs x2, 0xfce, x8	// Set all CSR bits
	csrrc x2, 0xfce, x8	// Clear all CSR bits
	csrrw x2, 0xfce, x17	// Restore CSR

// Testing CSR 0xfcf
	csrr x11, 0xfcf	// Read CSR
	li x18, -1
	csrrw x4, 0xfcf, x18	// Write all 1s to CSR
	csrrw x4, 0xfcf, x0	// Write all 0s to CSR
	csrrs x4, 0xfcf, x18	// Set all CSR bits
	csrrc x4, 0xfcf, x18	// Clear all CSR bits
	csrrw x4, 0xfcf, x11	// Restore CSR

// Testing CSR 0xfd0
	csrr x23, 0xfd0	// Read CSR
	li x21, -1
	csrrw x25, 0xfd0, x21	// Write all 1s to CSR
	csrrw x25, 0xfd0, x0	// Write all 0s to CSR
	csrrs x25, 0xfd0, x21	// Set all CSR bits
	csrrc x25, 0xfd0, x21	// Clear all CSR bits
	csrrw x25, 0xfd0, x23	// Restore CSR

// Testing CSR 0xfd1
	csrr x28, 0xfd1	// Read CSR
	li x11, -1
	csrrw x22, 0xfd1, x11	// Write all 1s to CSR
	csrrw x22, 0xfd1, x0	// Write all 0s to CSR
	csrrs x22, 0xfd1, x11	// Set all CSR bits
	csrrc x22, 0xfd1, x11	// Clear all CSR bits
	csrrw x22, 0xfd1, x28	// Restore CSR

// Testing CSR 0xfd2
	csrr x17, 0xfd2	// Read CSR
	li x18, -1
	csrrw x9, 0xfd2, x18	// Write all 1s to CSR
	csrrw x9, 0xfd2, x0	// Write all 0s to CSR
	csrrs x9, 0xfd2, x18	// Set all CSR bits
	csrrc x9, 0xfd2, x18	// Clear all CSR bits
	csrrw x9, 0xfd2, x17	// Restore CSR

// Testing CSR 0xfd3
	csrr x18, 0xfd3	// Read CSR
	li x1, -1
	csrrw x24, 0xfd3, x1	// Write all 1s to CSR
	csrrw x24, 0xfd3, x0	// Write all 0s to CSR
	csrrs x24, 0xfd3, x1	// Set all CSR bits
	csrrc x24, 0xfd3, x1	// Clear all CSR bits
	csrrw x24, 0xfd3, x18	// Restore CSR

// Testing CSR 0xfd4
	csrr x19, 0xfd4	// Read CSR
	li x14, -1
	csrrw x28, 0xfd4, x14	// Write all 1s to CSR
	csrrw x28, 0xfd4, x0	// Write all 0s to CSR
	csrrs x28, 0xfd4, x14	// Set all CSR bits
	csrrc x28, 0xfd4, x14	// Clear all CSR bits
	csrrw x28, 0xfd4, x19	// Restore CSR

// Testing CSR 0xfd5
	csrr x9, 0xfd5	// Read CSR
	li x4, -1
	csrrw x15, 0xfd5, x4	// Write all 1s to CSR
	csrrw x15, 0xfd5, x0	// Write all 0s to CSR
	csrrs x15, 0xfd5, x4	// Set all CSR bits
	csrrc x15, 0xfd5, x4	// Clear all CSR bits
	csrrw x15, 0xfd5, x9	// Restore CSR

// Testing CSR 0xfd6
	csrr x15, 0xfd6	// Read CSR
	li x29, -1
	csrrw x27, 0xfd6, x29	// Write all 1s to CSR
	csrrw x27, 0xfd6, x0	// Write all 0s to CSR
	csrrs x27, 0xfd6, x29	// Set all CSR bits
	csrrc x27, 0xfd6, x29	// Clear all CSR bits
	csrrw x27, 0xfd6, x15	// Restore CSR

// Testing CSR 0xfd7
	csrr x11, 0xfd7	// Read CSR
	li x29, -1
	csrrw x5, 0xfd7, x29	// Write all 1s to CSR
	csrrw x5, 0xfd7, x0	// Write all 0s to CSR
	csrrs x5, 0xfd7, x29	// Set all CSR bits
	csrrc x5, 0xfd7, x29	// Clear all CSR bits
	csrrw x5, 0xfd7, x11	// Restore CSR

// Testing CSR 0xfd8
	csrr x30, 0xfd8	// Read CSR
	li x13, -1
	csrrw x4, 0xfd8, x13	// Write all 1s to CSR
	csrrw x4, 0xfd8, x0	// Write all 0s to CSR
	csrrs x4, 0xfd8, x13	// Set all CSR bits
	csrrc x4, 0xfd8, x13	// Clear all CSR bits
	csrrw x4, 0xfd8, x30	// Restore CSR

// Testing CSR 0xfd9
	csrr x25, 0xfd9	// Read CSR
	li x30, -1
	csrrw x10, 0xfd9, x30	// Write all 1s to CSR
	csrrw x10, 0xfd9, x0	// Write all 0s to CSR
	csrrs x10, 0xfd9, x30	// Set all CSR bits
	csrrc x10, 0xfd9, x30	// Clear all CSR bits
	csrrw x10, 0xfd9, x25	// Restore CSR

// Testing CSR 0xfda
	csrr x19, 0xfda	// Read CSR
	li x5, -1
	csrrw x16, 0xfda, x5	// Write all 1s to CSR
	csrrw x16, 0xfda, x0	// Write all 0s to CSR
	csrrs x16, 0xfda, x5	// Set all CSR bits
	csrrc x16, 0xfda, x5	// Clear all CSR bits
	csrrw x16, 0xfda, x19	// Restore CSR

// Testing CSR 0xfdb
	csrr x3, 0xfdb	// Read CSR
	li x19, -1
	csrrw x14, 0xfdb, x19	// Write all 1s to CSR
	csrrw x14, 0xfdb, x0	// Write all 0s to CSR
	csrrs x14, 0xfdb, x19	// Set all CSR bits
	csrrc x14, 0xfdb, x19	// Clear all CSR bits
	csrrw x14, 0xfdb, x3	// Restore CSR

// Testing CSR 0xfdc
	csrr x12, 0xfdc	// Read CSR
	li x29, -1
	csrrw x1, 0xfdc, x29	// Write all 1s to CSR
	csrrw x1, 0xfdc, x0	// Write all 0s to CSR
	csrrs x1, 0xfdc, x29	// Set all CSR bits
	csrrc x1, 0xfdc, x29	// Clear all CSR bits
	csrrw x1, 0xfdc, x12	// Restore CSR

// Testing CSR 0xfdd
	csrr x1, 0xfdd	// Read CSR
	li x10, -1
	csrrw x3, 0xfdd, x10	// Write all 1s to CSR
	csrrw x3, 0xfdd, x0	// Write all 0s to CSR
	csrrs x3, 0xfdd, x10	// Set all CSR bits
	csrrc x3, 0xfdd, x10	// Clear all CSR bits
	csrrw x3, 0xfdd, x1	// Restore CSR

// Testing CSR 0xfde
	csrr x19, 0xfde	// Read CSR
	li x28, -1
	csrrw x3, 0xfde, x28	// Write all 1s to CSR
	csrrw x3, 0xfde, x0	// Write all 0s to CSR
	csrrs x3, 0xfde, x28	// Set all CSR bits
	csrrc x3, 0xfde, x28	// Clear all CSR bits
	csrrw x3, 0xfde, x19	// Restore CSR

// Testing CSR 0xfdf
	csrr x28, 0xfdf	// Read CSR
	li x27, -1
	csrrw x4, 0xfdf, x27	// Write all 1s to CSR
	csrrw x4, 0xfdf, x0	// Write all 0s to CSR
	csrrs x4, 0xfdf, x27	// Set all CSR bits
	csrrc x4, 0xfdf, x27	// Clear all CSR bits
	csrrw x4, 0xfdf, x28	// Restore CSR

// Testing CSR 0xfe0
	csrr x28, 0xfe0	// Read CSR
	li x25, -1
	csrrw x15, 0xfe0, x25	// Write all 1s to CSR
	csrrw x15, 0xfe0, x0	// Write all 0s to CSR
	csrrs x15, 0xfe0, x25	// Set all CSR bits
	csrrc x15, 0xfe0, x25	// Clear all CSR bits
	csrrw x15, 0xfe0, x28	// Restore CSR

// Testing CSR 0xfe1
	csrr x21, 0xfe1	// Read CSR
	li x9, -1
	csrrw x12, 0xfe1, x9	// Write all 1s to CSR
	csrrw x12, 0xfe1, x0	// Write all 0s to CSR
	csrrs x12, 0xfe1, x9	// Set all CSR bits
	csrrc x12, 0xfe1, x9	// Clear all CSR bits
	csrrw x12, 0xfe1, x21	// Restore CSR

// Testing CSR 0xfe2
	csrr x23, 0xfe2	// Read CSR
	li x21, -1
	csrrw x27, 0xfe2, x21	// Write all 1s to CSR
	csrrw x27, 0xfe2, x0	// Write all 0s to CSR
	csrrs x27, 0xfe2, x21	// Set all CSR bits
	csrrc x27, 0xfe2, x21	// Clear all CSR bits
	csrrw x27, 0xfe2, x23	// Restore CSR

// Testing CSR 0xfe3
	csrr x10, 0xfe3	// Read CSR
	li x22, -1
	csrrw x8, 0xfe3, x22	// Write all 1s to CSR
	csrrw x8, 0xfe3, x0	// Write all 0s to CSR
	csrrs x8, 0xfe3, x22	// Set all CSR bits
	csrrc x8, 0xfe3, x22	// Clear all CSR bits
	csrrw x8, 0xfe3, x10	// Restore CSR

// Testing CSR 0xfe4
	csrr x11, 0xfe4	// Read CSR
	li x9, -1
	csrrw x26, 0xfe4, x9	// Write all 1s to CSR
	csrrw x26, 0xfe4, x0	// Write all 0s to CSR
	csrrs x26, 0xfe4, x9	// Set all CSR bits
	csrrc x26, 0xfe4, x9	// Clear all CSR bits
	csrrw x26, 0xfe4, x11	// Restore CSR

// Testing CSR 0xfe5
	csrr x24, 0xfe5	// Read CSR
	li x4, -1
	csrrw x11, 0xfe5, x4	// Write all 1s to CSR
	csrrw x11, 0xfe5, x0	// Write all 0s to CSR
	csrrs x11, 0xfe5, x4	// Set all CSR bits
	csrrc x11, 0xfe5, x4	// Clear all CSR bits
	csrrw x11, 0xfe5, x24	// Restore CSR

// Testing CSR 0xfe6
	csrr x9, 0xfe6	// Read CSR
	li x14, -1
	csrrw x27, 0xfe6, x14	// Write all 1s to CSR
	csrrw x27, 0xfe6, x0	// Write all 0s to CSR
	csrrs x27, 0xfe6, x14	// Set all CSR bits
	csrrc x27, 0xfe6, x14	// Clear all CSR bits
	csrrw x27, 0xfe6, x9	// Restore CSR

// Testing CSR 0xfe7
	csrr x2, 0xfe7	// Read CSR
	li x22, -1
	csrrw x21, 0xfe7, x22	// Write all 1s to CSR
	csrrw x21, 0xfe7, x0	// Write all 0s to CSR
	csrrs x21, 0xfe7, x22	// Set all CSR bits
	csrrc x21, 0xfe7, x22	// Clear all CSR bits
	csrrw x21, 0xfe7, x2	// Restore CSR

// Testing CSR 0xfe8
	csrr x10, 0xfe8	// Read CSR
	li x26, -1
	csrrw x9, 0xfe8, x26	// Write all 1s to CSR
	csrrw x9, 0xfe8, x0	// Write all 0s to CSR
	csrrs x9, 0xfe8, x26	// Set all CSR bits
	csrrc x9, 0xfe8, x26	// Clear all CSR bits
	csrrw x9, 0xfe8, x10	// Restore CSR

// Testing CSR 0xfe9
	csrr x14, 0xfe9	// Read CSR
	li x25, -1
	csrrw x21, 0xfe9, x25	// Write all 1s to CSR
	csrrw x21, 0xfe9, x0	// Write all 0s to CSR
	csrrs x21, 0xfe9, x25	// Set all CSR bits
	csrrc x21, 0xfe9, x25	// Clear all CSR bits
	csrrw x21, 0xfe9, x14	// Restore CSR

// Testing CSR 0xfea
	csrr x28, 0xfea	// Read CSR
	li x25, -1
	csrrw x5, 0xfea, x25	// Write all 1s to CSR
	csrrw x5, 0xfea, x0	// Write all 0s to CSR
	csrrs x5, 0xfea, x25	// Set all CSR bits
	csrrc x5, 0xfea, x25	// Clear all CSR bits
	csrrw x5, 0xfea, x28	// Restore CSR

// Testing CSR 0xfeb
	csrr x25, 0xfeb	// Read CSR
	li x26, -1
	csrrw x31, 0xfeb, x26	// Write all 1s to CSR
	csrrw x31, 0xfeb, x0	// Write all 0s to CSR
	csrrs x31, 0xfeb, x26	// Set all CSR bits
	csrrc x31, 0xfeb, x26	// Clear all CSR bits
	csrrw x31, 0xfeb, x25	// Restore CSR

// Testing CSR 0xfec
	csrr x30, 0xfec	// Read CSR
	li x9, -1
	csrrw x14, 0xfec, x9	// Write all 1s to CSR
	csrrw x14, 0xfec, x0	// Write all 0s to CSR
	csrrs x14, 0xfec, x9	// Set all CSR bits
	csrrc x14, 0xfec, x9	// Clear all CSR bits
	csrrw x14, 0xfec, x30	// Restore CSR

// Testing CSR 0xfed
	csrr x30, 0xfed	// Read CSR
	li x17, -1
	csrrw x18, 0xfed, x17	// Write all 1s to CSR
	csrrw x18, 0xfed, x0	// Write all 0s to CSR
	csrrs x18, 0xfed, x17	// Set all CSR bits
	csrrc x18, 0xfed, x17	// Clear all CSR bits
	csrrw x18, 0xfed, x30	// Restore CSR

// Testing CSR 0xfee
	csrr x12, 0xfee	// Read CSR
	li x25, -1
	csrrw x1, 0xfee, x25	// Write all 1s to CSR
	csrrw x1, 0xfee, x0	// Write all 0s to CSR
	csrrs x1, 0xfee, x25	// Set all CSR bits
	csrrc x1, 0xfee, x25	// Clear all CSR bits
	csrrw x1, 0xfee, x12	// Restore CSR

// Testing CSR 0xfef
	csrr x12, 0xfef	// Read CSR
	li x20, -1
	csrrw x30, 0xfef, x20	// Write all 1s to CSR
	csrrw x30, 0xfef, x0	// Write all 0s to CSR
	csrrs x30, 0xfef, x20	// Set all CSR bits
	csrrc x30, 0xfef, x20	// Clear all CSR bits
	csrrw x30, 0xfef, x12	// Restore CSR

// Testing CSR 0xff0
	csrr x31, 0xff0	// Read CSR
	li x13, -1
	csrrw x10, 0xff0, x13	// Write all 1s to CSR
	csrrw x10, 0xff0, x0	// Write all 0s to CSR
	csrrs x10, 0xff0, x13	// Set all CSR bits
	csrrc x10, 0xff0, x13	// Clear all CSR bits
	csrrw x10, 0xff0, x31	// Restore CSR

// Testing CSR 0xff1
	csrr x26, 0xff1	// Read CSR
	li x9, -1
	csrrw x1, 0xff1, x9	// Write all 1s to CSR
	csrrw x1, 0xff1, x0	// Write all 0s to CSR
	csrrs x1, 0xff1, x9	// Set all CSR bits
	csrrc x1, 0xff1, x9	// Clear all CSR bits
	csrrw x1, 0xff1, x26	// Restore CSR

// Testing CSR 0xff2
	csrr x8, 0xff2	// Read CSR
	li x30, -1
	csrrw x26, 0xff2, x30	// Write all 1s to CSR
	csrrw x26, 0xff2, x0	// Write all 0s to CSR
	csrrs x26, 0xff2, x30	// Set all CSR bits
	csrrc x26, 0xff2, x30	// Clear all CSR bits
	csrrw x26, 0xff2, x8	// Restore CSR

// Testing CSR 0xff3
	csrr x12, 0xff3	// Read CSR
	li x11, -1
	csrrw x27, 0xff3, x11	// Write all 1s to CSR
	csrrw x27, 0xff3, x0	// Write all 0s to CSR
	csrrs x27, 0xff3, x11	// Set all CSR bits
	csrrc x27, 0xff3, x11	// Clear all CSR bits
	csrrw x27, 0xff3, x12	// Restore CSR

// Testing CSR 0xff4
	csrr x3, 0xff4	// Read CSR
	li x13, -1
	csrrw x23, 0xff4, x13	// Write all 1s to CSR
	csrrw x23, 0xff4, x0	// Write all 0s to CSR
	csrrs x23, 0xff4, x13	// Set all CSR bits
	csrrc x23, 0xff4, x13	// Clear all CSR bits
	csrrw x23, 0xff4, x3	// Restore CSR

// Testing CSR 0xff5
	csrr x16, 0xff5	// Read CSR
	li x22, -1
	csrrw x7, 0xff5, x22	// Write all 1s to CSR
	csrrw x7, 0xff5, x0	// Write all 0s to CSR
	csrrs x7, 0xff5, x22	// Set all CSR bits
	csrrc x7, 0xff5, x22	// Clear all CSR bits
	csrrw x7, 0xff5, x16	// Restore CSR

// Testing CSR 0xff6
	csrr x22, 0xff6	// Read CSR
	li x1, -1
	csrrw x29, 0xff6, x1	// Write all 1s to CSR
	csrrw x29, 0xff6, x0	// Write all 0s to CSR
	csrrs x29, 0xff6, x1	// Set all CSR bits
	csrrc x29, 0xff6, x1	// Clear all CSR bits
	csrrw x29, 0xff6, x22	// Restore CSR

// Testing CSR 0xff7
	csrr x30, 0xff7	// Read CSR
	li x14, -1
	csrrw x29, 0xff7, x14	// Write all 1s to CSR
	csrrw x29, 0xff7, x0	// Write all 0s to CSR
	csrrs x29, 0xff7, x14	// Set all CSR bits
	csrrc x29, 0xff7, x14	// Clear all CSR bits
	csrrw x29, 0xff7, x30	// Restore CSR

// Testing CSR 0xff8
	csrr x30, 0xff8	// Read CSR
	li x9, -1
	csrrw x17, 0xff8, x9	// Write all 1s to CSR
	csrrw x17, 0xff8, x0	// Write all 0s to CSR
	csrrs x17, 0xff8, x9	// Set all CSR bits
	csrrc x17, 0xff8, x9	// Clear all CSR bits
	csrrw x17, 0xff8, x30	// Restore CSR

// Testing CSR 0xff9
	csrr x19, 0xff9	// Read CSR
	li x28, -1
	csrrw x30, 0xff9, x28	// Write all 1s to CSR
	csrrw x30, 0xff9, x0	// Write all 0s to CSR
	csrrs x30, 0xff9, x28	// Set all CSR bits
	csrrc x30, 0xff9, x28	// Clear all CSR bits
	csrrw x30, 0xff9, x19	// Restore CSR

// Testing CSR 0xffa
	csrr x17, 0xffa	// Read CSR
	li x12, -1
	csrrw x7, 0xffa, x12	// Write all 1s to CSR
	csrrw x7, 0xffa, x0	// Write all 0s to CSR
	csrrs x7, 0xffa, x12	// Set all CSR bits
	csrrc x7, 0xffa, x12	// Clear all CSR bits
	csrrw x7, 0xffa, x17	// Restore CSR

// Testing CSR 0xffb
	csrr x20, 0xffb	// Read CSR
	li x8, -1
	csrrw x19, 0xffb, x8	// Write all 1s to CSR
	csrrw x19, 0xffb, x0	// Write all 0s to CSR
	csrrs x19, 0xffb, x8	// Set all CSR bits
	csrrc x19, 0xffb, x8	// Clear all CSR bits
	csrrw x19, 0xffb, x20	// Restore CSR

// Testing CSR 0xffc
	csrr x18, 0xffc	// Read CSR
	li x17, -1
	csrrw x3, 0xffc, x17	// Write all 1s to CSR
	csrrw x3, 0xffc, x0	// Write all 0s to CSR
	csrrs x3, 0xffc, x17	// Set all CSR bits
	csrrc x3, 0xffc, x17	// Clear all CSR bits
	csrrw x3, 0xffc, x18	// Restore CSR

// Testing CSR 0xffd
	csrr x20, 0xffd	// Read CSR
	li x4, -1
	csrrw x30, 0xffd, x4	// Write all 1s to CSR
	csrrw x30, 0xffd, x0	// Write all 0s to CSR
	csrrs x30, 0xffd, x4	// Set all CSR bits
	csrrc x30, 0xffd, x4	// Clear all CSR bits
	csrrw x30, 0xffd, x20	// Restore CSR

// Testing CSR 0xffe
	csrr x4, 0xffe	// Read CSR
	li x20, -1
	csrrw x10, 0xffe, x20	// Write all 1s to CSR
	csrrw x10, 0xffe, x0	// Write all 0s to CSR
	csrrs x10, 0xffe, x20	// Set all CSR bits
	csrrc x10, 0xffe, x20	// Clear all CSR bits
	csrrw x10, 0xffe, x4	// Restore CSR

// Testing CSR 0xfff
	csrr x6, 0xfff	// Read CSR
	li x23, -1
	csrrw x22, 0xfff, x23	// Write all 1s to CSR
	csrrw x22, 0xfff, x0	// Write all 0s to CSR
	csrrs x22, 0xfff, x23	// Set all CSR bits
	csrrc x22, 0xfff, x23	// Clear all CSR bits
	csrrw x22, 0xfff, x6	// Restore CSR
