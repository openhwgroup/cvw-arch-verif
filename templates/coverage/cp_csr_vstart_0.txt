    cp_csr_vstart : coverpoint get_csr_val(ins.hart, ins.issue, `SAMPLE_BEFORE, "vstart", "vstart") iff (ins.trap == 0 )  {
        // Value of VSTART.vstart (vector start index), accepts only zero
        bins zero    = {0};
    }
